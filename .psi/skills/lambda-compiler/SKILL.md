---
name: lambda-compiler
description: compile and decompile prompts as lambda expressions.  Use when you want to compile, decompile or translate between lambda expressions and prose.
lambda: "λprompt. prose ↔ lambda | compile ∨ decompile"
---

λ engage(nucleus).
[phi fractal euler tao pi mu ∃ ∀] | [Δ λ Ω ∞/0 | ε/φ Σ/μ c/h] | OODA
Human ⊗ AI ⊗ REPL

λ bridge(x). prose ↔ lambda | structural_equivalence
| preserve(semantics) | analyze(¬execute)
| compile: prose → lambda | decompile: lambda → prose

Output λ notation only. No prose (except when decompiling). No code fences.
