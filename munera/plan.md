# Munera plan

Rebuilt 2026-05-31 from the authoritative `munera/open/` directory after an
open-task reconciliation audit (completed-task closure, NNN-collision renumber).

## Open tasks (suggested execution order)

Not yet started:

- `munera/open/231-parse-textual-tool-calls-for-local-models/` — add a model-capability-gated compatibility parser for local runners that emit `<tool_call>` markup as assistant text instead of canonical tool-call events.
- `munera/open/202-document-at-bounds-in-scheduler-doc/` — (from master) document the resolved-millisecond-delay absolute `:at` bounds in `doc/scheduler.md`. NNN collides with closed `202-reusable-review-follow-up-step` (left as-is per convention).
- `munera/open/206-emacs-buffer-local-widget-mutation-timers/` — move Emacs widget-projection mutation watchdog timers from a module-global hash into buffer-local `psi-emacs-state` and cancel them in `psi-emacs--teardown-buffer`, fixing orphaned/non-deterministic/cross-buffer timers when a psi buffer is killed mid-mutation. Design-only.
- `munera/open/203-task-lifecycle-chained-workflow/` — renumbered from 198 (NNN collision with closed `198-fix-tool-metrics-empty-tools-map`); new orchestration workflow chaining review-task-design → create-task-plan → review-task-plan → implement-task → review-task-implementation. Design-only; open questions on stage gating and context threading.
- `munera/open/197-ui-action-invocation/` — renumbered from 191 (NNN collision); side-effecting UI action invocation deferred from closed 190/194 UI work.
- `munera/open/021-emacs-session-tree-buffer-with-magit-sections/`

## Recently closed

- `229-dev-http-presentation-extension` — new `dev-http` extension: localhost HTTP server (http-kit + reitit-ring + hiccup + integrant, all extension-local) as a rich bidirectional agent↔dev side channel addressed by URL. Persisted routes under `extensions/dev-http/dev/`; throwaway session routes via a registry (`dev-present` tool + REPL `register-route!`); renderer set (markdown/table/vega/mermaid/choices/hiccup/file, vendored Vega/Mermaid JS); SSE live-updates; choice interaction posts back as an immediate-turn user message into the invoking session via the one sanctioned core touch (`psi.extension/submit-synthetic-prompt`). All 4 slices green (focused Scry + clj-kondo); implementation/test/docs/code-shaper reviews PASS; knowledge extracted (3 memories). Design was reset once after the autonomous design-review loop diverged (12 rounds / 10× bloat) — reconstructed tight, then planned/built/reviewed via delegated workflows.
- `227-review-task-design-multi-prompt-exemplar` — first consumer of 226's multi-prompt session steps. Rewrote `review-task-design` into one shared `design-review` multi-prompt batch (`architecture`, `ambiguity`, `inconsistency`) followed by one `design-follow-up`, tightened `workflow/pass-feedback-routing` to validate every per-prompt `PASS_STATUS`, updated prompt contracts/docs/changelog, and added routing/definition/prompt-contract tests including shared-child-session and batch-follow-up evidence coverage. Verified focused Scry suites green (32 tests / 576 assertions) and focused clj-kondo clean; implementation/test/docs/code-shaper reviews PASS.
- `226-multi-prompt-workflow-steps` — capability: a `:session` step can carry an ordered queue of prompts (`:prompts` named groups), each run as its own model turn in the same shared child session, with one post-drain judge. All 7 slices landed (unified N=1 queue path, `:prompts` grammar + IR normalization, in-run N-turn drain + per-prompt records, per-prompt output surfaces + `:prompt` source-ref + validation, resume-from-progression, abort/cancel/blocked paths, docs/changelog/coherence). Verified: workflow-runtime + workflow-step-materialization suites green, AC-1..AC-8 each covered, clj-kondo clean. Implementation review PASS (code-shaper). Known deviation: turns run synchronously (Slice-3 R1 fallback) so the drain is atomic within one `:step/enter` (no mid-drain statechart suspend); idempotency comes from per-iteration persisted-progression re-read plus dispatch replay. Exemplar consumer is 227.
- `228-fix-invoke-step-judge-entry-state-mismatch` — fixed the workflow-runtime fault where an `:invoke` step carrying both an `:operation` and an invoke `:judge` (e.g. `review-task-design`'s `clarity-status`) aborted at the judge with `:stop-reason :handler-entry-state-mismatch`, breaking `task-lifecycle`/`review-task-design` design review for every task. Root cause (spike-confirmed): the step-operation and judge-operation ran against the same step attempt sharing one set of `:operation-*-state` entry-lock phase keys (225 lineage), so the judge's `prepare` saw the residual `:entered` and skipped resetting to `:pending`, then `enter` required `:pending` → mismatch. Fixed via per-operation phase-key namespacing (option a): an additive `:operation-role` invocation key drives a single `role-phase-opts` rewrite at the one `transition-workflow-operation-phase!` chokepoint (`:judge` → `judge-`-prefixed keys; default/`:step` byte-identical), mirroring the existing `:turn-*` vs `:operation-*` separation. Build also uncovered and fixed a second distinct defect (same `clarity-status`/225 lineage): a stale-snapshot `:attempt-mismatch` on REPEAT, resolved by threading the authoritative just-started `attempt-id` from `:step/enter` into `invoke-step-runtime-result` (both call sites), which also tightens the 225 cancellation guard. Red→green characterization test + role-aware cancellation regression; `workflow-review-step-routing` 3 failing → 11/82 green (REPEAT/DONE + iteration-limit unblocked); full Scry suites green; clj-kondo clean; code-shaper implementation review PASS. CHANGELOG `Fixed`.

- `225-workflow-cancellation-halts-inflight-execution` — fixed workflow/delegate cancellation so cancelled runs stop driving ordinary work instead of continuing as orphaned async execution. Cancellation now has cooperative checkpoints across workflow execution, invoke/session/delegate steps, judge/prompt lifecycle entry, deterministic-operation entry, and nested workflow ownership; remove/live-run semantics avoid discarding future handles before cancellation; background jobs and run state reach clean terminal cancellation. Added extensive race-regression coverage split across focused namespaces kept under the file-length limit, completed implementation review, and extracted durable knowledge (`mementum/knowledge/workflow-cancellation-safety.md`).

- `224-at-most-once-tool-result-per-call-id` — fixed the agent-session core race where a turn interrupt (`:user-abort`) recorded a synthetic `"interrupted"` toolResult for a still-pending tool-call while that tool *also* recorded its real result, producing two journal `toolResult` entries for one `tool_use` id → provider 400 (`each tool_use must have a single result`) that wedged the session for all later turns (general: delegate/bash/psi-tool; surfaced via `lambda-build`). Resolved via design Option C: a canonical `:state*` `recorded-tool-result-ids` set funnels `:session/tool-agent-record-result` through a single pure guarded handler (read predicate → `:root-state-update` adds id → both-or-neither in-memory-record + journal-append effects; atomicity from dispatch serialization, no test-and-set), keeping the `:pending-tool-calls` agent-ctx handle external per the State-boundary principle. Plus defensive de-dup at `journal->provider-messages` so already-corrupted journals recover. Dedicated `tool_result_at_most_once_test` + prompt-request projection coverage (23 tests / 78 assertions green), clj-kondo clean, knowledge extracted.

- `218-review-workflow-iteration-limits` — raised review workflow repetition limits and made design/plan reviews true repeated full-pass loops: implementation review profiles now allow 10 total `review-step` entries, design review loops up to 6 full architecture→ambiguity→inconsistency passes, and plan review loops up to 5 full ambiguity→inconsistency passes. Added deterministic pass-feedback routing, runtime/definition coverage for limits and iteration exhaustion, docs, and changelog.

- `213-simplify-start-tui-runtime` — reduced incidental complexity in `start-tui-runtime!` while preserving the TUI startup contract, tightened TUI provider lifetime cleanup, and added public-path startup coverage for forwarded options, callbacks, frontend actions, terminal mode, session-tree widgets, and live nREPL focus publication. Also fixed the idle TUI queued-follow-up path so queued text is stored instead of dropped. Renumbered from 211 during PR finalization because master already owned closed task 211.
- `212-incidental-simplification-test-net-gate` — hardened `reduce-incidental-complexity` with an explicit green characterization-test-net gate before implementation, durable stopped dispositions for missing/failing baseline tests, and workflow-loader tests that lock the new routing and terminal-stop behaviours.
- `211-emacs-retry-backoff-footer-visible` — fixed Emacs retry backoff visibility by publishing retry activation/change/clear as `:retry-updated` progress events that drive the existing app-runtime `footer/updated` projection; Emacs now renders projected retry footer text such as `retry in 8s`, updates changed retry text, and clears stale retry text. Tests cover real provider retry scheduling through RPC and Emacs-visible footer rendering.
- `210-fix-project-repl-start-readiness-timeout` — fixed `psi-tool project-repl op=start` / `/project-repl start` failing with a `:started-readiness` timeout for real slow-booting start commands. Two defects in `components/project-nrepl` started-mode acquisition: (1) hard-coded 5 s readiness timeout with no config path (`ops/start` passed no opts) — now a configurable `:agent-session :project-nrepl :start-readiness-timeout-ms` key (range `[1000 600000]`, default raised to 120000, `system < user < project` precedence, surfaced as `:readiness-timeout-ms` status); (2) stale-`.nrepl-port` race in `wait-for-started-endpoint!` silently latching onto a leftover/unrelated port — now deletes any pre-existing `.nrepl-port` before launch and only accepts a port file written at/after the launch instant, with a distinct `:phase :started-stale-port` on stale-only timeout. Also reaps a launched-but-never-ready process on timeout (was leaked). Mock-free tests via the `process-launcher`/`nrepl-connector` seams; docs (`doc/project-nrepl.md`) + CHANGELOG. PR #155.
- `209-incidental-complexity-simplification-workflow` — new autonomous capability to simplify one aspect of the system per run by targeting *incidental* complexity (high `gordian local` comprehension burden against low/moderate cyclomatic complexity). Ships three artifacts: the `incidental-complexity-finder` skill (selects the single highest `gap = lcc-total / max(cc,1)` unit with a top-5 essential-vs-incidental judgment guard), the `task-lifecycle-in-worktree` worktree-resolving wrapper, and the `reduce-incidental-complexity` outer workflow (select+create a constrained behaviour-preserving task in an isolated worktree, then drive it through the full `task-lifecycle`). Loader/compiler + content-lock + recipe-determinism tests, including a real-lens narrow integration test against `bb gordian`. Renumbered from 204 (NNN collision with both `closed/204-prompts-reload-command-and-mutation` and a stale leaked snapshot of this task on master). PR #154.
- `205-invoke-deterministic-operation` — made registered deterministic operations directly invokable outside a workflow run via two new surfaces sharing one mechanism: the `/operations` (list) and `/operation <id> {edn-args}` (invoke) slash commands, and a psi-tool `operation` action with `op: list|invoke`. Both delegate to a single shared invocation/listing helper (`deterministic-operation-action` / `commands.operation`) that builds the invocation map from session ctx (`:args`/`:ctx`/`:session-id`, optional `:parent-session-id`, nil `:workflow-run-id`/`:step-id`) and routes through the existing runtime boundary (`invoke-operation-in` via `runtime/invoke-operation`) — no new execution/validation/permission semantics. Listing is sorted-by-id (registry runtime-handle read, not a resolver); invocation renders all top-level result keys (`pr-str`'d, per-key 2000-char truncation). Tests + docs (`doc/operations.md`) + changelog. Registered `/operations`/`/operation` in the single-source `builtin-command-specs` table during rebase onto the master single-source change (2026-06-02).
- `208-fix-tui-init-state-nil-query-fn-empty-builtin-command-specs` — fixed the broken TUI slash-autocomplete test setup: the test helper `init-state` built state via `(app/make-init nil …)` with a nil `query-fn`, so `build-init` skipped introspection and `:builtin-command-specs` stayed empty (built-in slash autocomplete rendered zero candidates → tests passed vacuously / didn't cover the task-205 single-sourced surface). Rewired `init-state` onto a stub `query-fn` seam returning `{:psi.agent-session/builtin-command-specs …}` (default `sample-builtin-command-specs`, per-case via opt), dropped the post-hoc `assoc`, and strengthened the autocomplete tests to assert `/help`/`/status`/`/quit` present (default) and absent (empty surface), with symmetric per-name failure messages. Test-only (single file `app_input_selector_test.clj`); no production change, no changelog. Focused test 15/42/0, `--focus unit` RC=0, clj-kondo clean (2026-06-02).
- `201-verify-scheduler-execution` — verification-only scheduler audit complete: all 7 scope areas verified-correct, no scheduler-behaviour defects found, one doc-gap remediation raised as task 202, scheduler suite and full `bb test` green after deterministic test-flake fixes.
- `207-workflow-session-defaults-inheritance-snapshot` — workflow runs now snapshot the inheritable default session details from their parent at invoke time (top-level steps from the invoking session; nested workflows from their parent run) into `:inherited-defaults`, so later parent model switches or user/project default-model changes do not affect an already-running delegated workflow. `resolve-step-session-config` consumes the snapshot (zero live-parent reads on the snapshot path) via a single `inherited` map; `:thinking-level` precedence unified with `:model` (inherited default ranks above base-meta); child-session assembly gates `inherited-default` on `:inherited-snapshot?` so the workflow judge keeps live-parent inheritance. Tests + design + CHANGELOG (2026-06-02).
- `205-single-source-builtin-slash-commands` — made the backend the single authoritative source of the built-in slash-command surface via one ordered keyed spec table (`commands.builtin-specs/builtin-command-specs`); `exact-command-handlers`, `prefixed-command-prefixes`, `builtin-command-names`, `format-help`'s built-in lines, and the new `builtin-commands-resolver` (`:psi.agent-session/builtin-command-specs` / `-names`) are all derived projections, so name drift is structurally unrepresentable (Option B, `unreachable > forbidden`). TUI + Emacs now build slash autocomplete from the resolver (deleted `shared/builtin-slash-commands`; trimmed the Emacs `defcustom` to `/skill:`, backend-wins), so previously-missing `/reload-models`/`/reload-prompts`/`/reload-extension-installs`/`/speed`/`/effort`/`/project-repl` now appear. Tests (backend projections/help/resolver + TUI + Emacs capf), changelog, `doc/architecture.md` (2026-06-01).
- `204-prompts-reload-command-and-mutation` — added `/reload-prompts` command + `psi.extension/reload-prompts` mutation (psi-tool visible) + pure `:session/reload-prompts` dispatch handler that re-discovers prompt templates from the session worktree and replaces `:prompt-templates` (no effects); `reload-prompts-in!` settings fn + `core.clj` re-export. Tests + docs + changelog (2026-06-01).
- `202-reusable-review-follow-up-step` — unified the review-workflow follow-ups onto two shared profile prompts (`review-follow-up-design.md`, `review-follow-up-steps.md`); removed the four per-aspect follow-up files and the inline `review-step` template; tests + docs + changelog updated (2026-06-01). Renumbered from 199 (NNN collision with master).
- `201-task-design-architectural-fit-review` — added `review-task-architecture` skill + `review-task-design-architecture-review.md` prompt; prepended an `architecture-review`/`architecture-follow-up` aspect pair (runs first) to `review-task-design.edn`, reusing the shared `review-follow-up-design.md` profile; final-summary sources the architecture yield + prose; tests (8 steps) + `doc/workflows.md` updated (2026-06-01). Renumbered from 198 (NNN collision with master).
- `108-project-nrepl-testing-without-mocks` — de-mocked all six in-scope `components/project-nrepl/test/` files (zero `with-redefs`); introduced three thin production seams (`real-nrepl-connector`, `real-process-launcher`, optional `:runtime-handle` seed on attach/start). PR #147.
- `199-metrics-turn-finished-handler-logging-idiom` — replaced raw `println` debug logging in metrics `make-turn-finished-handler` with timbre (closed upstream on master).
- `200-remove-dead-wrap-tool-executor-and-dedupe-result-filter` — removed dead `wrap-tool-executor`; modifiable-key contract (`:content`/`:details`/`:is-error`) now expressed once in the `dispatch-tool-result-in` filter; migrated the non-map-return guard to a direct test.
- `198-fix-tool-metrics-empty-tools-map` — `:tools {}` always empty in metrics.edn; fixed by bridging `:tool-start`/`:tool-result` lifecycle events → extension `dispatch-in` in `emit-tool-lifecycle!`.

## Audit outcome (2026-05-31)

Closed as complete (moved to `munera/closed/`):

- `164-registry-semantics-unification-audit` — registry-unification audit done; arc complete through 177.
- `187-md-workflow-input-expansion`
- `188-openai-codex-native-structured-output` — complete (PR #132); one deferred conditional-future Codex non-streaming test remains by design.
- `190-conditional-review-follow-ups-for-design-and-plan-workflows`
- `191-fix-judge-structured-output-non-object-schema`
- `193-improve-project-workflows`
- `173`, `175`, `186`, `189` — closed earlier in the session.

Closed as stale/abandoned (predate registry/bootstrap/structured-output arcs):

- `001-post-wave-b-gordian-follow-on`
- `002-compatibility-scaffold-removal`
- `003-prompt-lifecycle-architectural-convergence`
- `005-canonical-dispatch-pipeline-trace-observability`
- `006-agent-tool-skill-prelude-follow-on`

Renumbered (NNN collision, slug/content unchanged):

- `154-fix-workflow-max-iterations-error-surfacing` → `195` (since closed — core fix done, edge-case tests deferred)
- `194-deterministic-review-step-routing` → `196` (since closed — work landed under 189/190; stale unchecked boxes)
- `191-ui-action-invocation` → `197` (still open)

## Conventions

- `munera/open/` is the canonical source of truth for active work; this file curates order only.
- Completed or abandoned tasks live under `munera/closed/` (no completed-vs-abandoned distinction; reason recorded inside each task).
- Historical per-task completion notes live in git history and `mementum/state.md`; they are intentionally no longer duplicated here.
- NNN collisions across concurrent branches: rename, never merge. Closed-task historical collisions are left as-is.
