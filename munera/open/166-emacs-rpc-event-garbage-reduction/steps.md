# Steps

Implementation steps are intentionally deferred.

Do not execute implementation work until the design review loop is complete and this file is replaced with an implementation checklist derived from the final `design.md` and `plan.md`.

Promotion from this placeholder is owned by the design-review workflow. The replacement implementation checklist may be written only after one full ambiguity-and-inconsistency review cycle adds no new actionable design feedback, and all `design-steps.md` items are checked or explicitly blocked in `implementation.md`.
