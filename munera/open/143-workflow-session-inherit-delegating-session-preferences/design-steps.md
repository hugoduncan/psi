# Design follow-up steps

- [x] Make the task’s review/follow-up surface explicit by adding `design-steps.md` to the task artifact set in `design.md` and/or `plan.md`, because this review pass is instructed to append actionable ambiguity follow-ups here but the task did not define or include that file.
- [ ] Add an explicit task-file obligation for the resume path, because `design.md` and `plan.md` both require preserving delegating-session identity through create/execute/resume flows but `steps.md` does not currently call out separate resume-path verification/proof even though resume is a distinct higher entrypoint.
