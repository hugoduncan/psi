# Design follow-up steps

- [ ] Make the task’s review/follow-up surface explicit by adding `design-steps.md` to the task artifact set in `design.md` and/or `plan.md`, because this review pass is instructed to append actionable ambiguity follow-ups here but the task did not define or include that file.
