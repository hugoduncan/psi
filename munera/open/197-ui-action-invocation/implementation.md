# Implementation notes

Created from task 190 because side-effecting UI action invocation was explicitly optional there and would broaden the queryability-first slice.

## Renumber (2026-05-31 audit)

Task id changed from `191-ui-action-invocation` to `197-ui-action-invocation` to resolve a Munera NNN collision (old number `191` was reused across concurrent branches). Slug and content unchanged; task remains open/active.
