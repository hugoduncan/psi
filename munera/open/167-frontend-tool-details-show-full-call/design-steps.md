# Design follow-up steps

- [x] Clarify the authoritative full-call data source and precedence when both parsed/structured arguments and raw argument strings are available, including how invalid or partially parsed JSON should render without silently dropping raw content.
- [x] Define the expanded-detail layout contract for both Emacs and TUI: section labels/order, whether the header summary remains separate from `Call` details, how empty/nil arguments render, and how multiline/nested arguments preserve full content without truncation.
- [x] Decide how tool-specific/extension call renderers interact with the new expanded full-call details, especially whether a renderer may replace the full call, must be accompanied by raw/structured fallback details, or applies only to the collapsed header.
- [x] Create `plan.md` and `steps.md` before implementation so the task has a concrete approach and checklist aligned with the finalized design acceptance criteria, including the required Emacs and TUI verification coverage.
- [x] Define a concrete frontend-detectable completeness rule for parsed arguments versus raw arguments in expanded `Call` rendering, including whether parsed arguments from live `tool/executing`/rehydrated summaries are trusted as complete when raw `arguments` is also present, and when both parsed and raw forms must be shown.
- [x] Align TUI verification with the close-toggle acceptance criterion: either add focused TUI toggled-closed coverage for expanded `Call`/response details, or update the design/plan to explicitly state why existing TUI collapsed-state coverage proves the close behavior.
- [x] Clarify tool-detail toggle granularity for Emacs and TUI: whether `C-c C-t`/`ctrl+o` must preserve the existing global tools-expanded mode for all tool rows or introduce row-local expansion, and align the "same row closed" acceptance wording and tests with that decision.
- [ ] Align `spec/tool-output-rendering.allium` with the finalized expanded full-call contract: expanded rendering must include generic `Call` details plus response/output details, not define expanded `visible_text` as response `output.text` only.
