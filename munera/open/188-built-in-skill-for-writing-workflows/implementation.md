- 2026-05-29 ambiguity review: found two actionable design ambiguities after reading `design.md`, the current project-local workflow skill, `doc/workflows.md`, built-in skill discovery/materialization seams, and representative built-in-skill task 186/185 precedents. First, the design requires replacing `.psi/skills/workflow/SKILL.md` and says the preferred outcome is removing it, but it does not define the discovery/collision behavior to prove during any temporary coexistence window before removal or while a compatibility stub exists; because runtime discovery explicitly gives project skills higher precedence than built-ins, the task must say whether implementation must remove the project-local file in the same slice or, if a stub is temporarily retained, how to prevent that higher-precedence project artifact from shadowing the built-in `workflow` skill in discovery, `/skills`, and `/skill:workflow`. Second, the minimum authoritative reference set still points workflow authors at broad classes like "representative workflow definitions already shipped in the repository" and "workflow runtime / agent-session seams that consume workflow definitions" without choosing the concrete canonical example workflows and code/test files the built-in skill should anchor to, which leaves the authored built-in skill free to drift toward different examples or omit key current-target grammar seams. Added follow-up items to `design-steps.md`.
- 2026-05-29 ambiguity follow-up execution: completed both newly added `design-steps.md` items without touching `plan.md` or `steps.md`. `design.md` now requires same-slice removal of `.psi/skills/workflow/SKILL.md` as the default cutover because current precedence is `project > user > built-in`, and it limits any temporary compatibility stub to an explicit, justified no-shadowing exception with proof across `:psi.agent-session/skills`, `:psi.skill/by-source`, `/skills`, and `/skill:workflow`. It also replaces open-ended workflow reference classes with concrete canonical authoring anchors: `doc/workflows.md`, `.psi/workflows/create-task-plan.edn`, `.psi/workflows/review-task-design.edn`, `.psi/workflows/planner.md`, `.psi/workflows/builder.md`, workflow-loader parser/compiler/authoring source + tests, and agent-session workflow bootstrap/core/execution/judge seams.
- 2026-05-29 inconsistency review: found one actionable design inconsistency after reading `design.md`, the current project-local workflow skill, `doc/workflows.md`, built-in packaged-skill precedent task 186, and the referenced workflow/skill packaging seams. `design.md` requires the new built-in `workflow` skill to preserve the current useful workflow-authoring role of `.psi/skills/workflow/SKILL.md`, be repository-specific, and point agents at the authoritative current workflow authoring/runtime seams, but its minimum authoritative reference set omits `doc/workflow-grammar.md` and `doc/workflow-grammar-concepts.md` even though `doc/workflows.md` names those as the formal and conceptual sources for the preferred target-authored grammar. That leaves the design asking the built-in skill to teach current repository workflow authoring while not requiring links to the canonical grammar references that define the syntax and concepts it says the skill should cover. Add those docs to the required authoritative reference set and verification/content anchors so the replacement skill cannot satisfy the task while pointing only to examples plus loader/runtime code.
- 2026-05-29 inconsistency follow-up execution: completed the newly added `design-steps.md` item without touching `plan.md` or `steps.md`. `design.md` now adds `doc/workflow-grammar.md` and `doc/workflow-grammar-concepts.md` to the minimum authoritative workflow authoring/reference set and to the targeted verification/content anchors, so the future built-in `workflow` skill must point to the repository's canonical formal and conceptual grammar docs alongside examples and runtime/code seams.
- 2026-05-29 ambiguity review repeat: found no new actionable ambiguity feedback after re-reading task artifacts (`design.md`, `plan.md`, `steps.md`, `design-steps.md`, `implementation.md`), the current project-local workflow skill, `doc/workflows.md`, representative built-in packaged-skill precedent task 186, and the referenced workflow/skill discovery, command, resolver, packaging, loader, and runtime seams/tests. Existing unchecked `steps.md` items already cover the remaining intended implementation and proof work, including built-in packaging, same-slice project-skill removal, higher-surface no-shadowing verification, and optional `/help` parity if implementation shows it is needed.
- 2026-05-29 ambiguity follow-up execution repeat: read `design-steps.md` history and current task artifacts to identify newly added ambiguity-review follow-ups. No unchecked `design-steps.md` items were added after the preceding ambiguity review pass (`5b5b451c`); the only later change was the already-completed inconsistency follow-up item committed in `e318071c`. Therefore no new ambiguity follow-up work was executable, `plan.md` and `steps.md` already remained aligned, and no task artifacts required changes beyond recording this no-op execution pass.
- 2026-05-29 inconsistency review repeat: found one new actionable inconsistency after re-reading `design.md`, `plan.md`, `steps.md`, `design-steps.md`, `implementation.md`, the current project-local workflow skill, `doc/workflows.md`, and the referenced skill/discovery/command/resolver seams plus task-186 precedent. The design verification/acceptance set explicitly requires higher-surface no-shadowing proof across `:psi.agent-session/skills`, `:psi.skill/by-source`, `/skills`, and `/skill:workflow`, and `plan.md` repeats representative higher discovery/listing/invocation proof with `/help` only as a possible extra parity surface. But `steps.md` currently names `/skills`, `/skill:workflow`, and optional `/help` without an explicit focused resolver/discovery proof item for `:psi.agent-session/skills` and `:psi.skill/by-source`. That leaves the executable checklist weaker than the required verification contract and could allow closure without the resolver-specific proof the task says is mandatory. Added an unchecked `design-steps.md` follow-up to align the checklist with the required higher-surface verification set.
- 2026-05-29 inconsistency follow-up execution repeat: completed the newly added `design-steps.md` item by strengthening the executable checklist and matching plan language. `steps.md` now separates mandatory focused resolver/discovery proof for `:psi.agent-session/skills` and `:psi.skill/by-source` from the command/listing/invocation proof for `/skills` and `/skill:workflow`, and `plan.md` now names that resolver/discovery verification explicitly. No other task artifacts needed changes.
- 2026-05-29 implementation pass: added the built-in packaged workflow skill at `bases/main/resources/psi/skills/workflow/SKILL.md`, migrated the project-local workflow guidance into concise repository-specific built-in content anchored to `doc/workflows.md`, `doc/workflow-grammar.md`, `doc/workflow-grammar-concepts.md`, canonical example workflows, and workflow loader/runtime seams, then removed `.psi/skills/workflow/SKILL.md` in the same slice to prevent project-precedence shadowing. Added focused proof across built-in materialization/discovery (`psi.prompt-assets.skills-test`), resolver/discovery (`psi.agent-session.resolvers-test`), `/skills` listing, and `/help` Skills-section surfaces (`psi.agent-session.commands-test`). Focused verification passed: `clojure -M:test --focus psi.prompt-assets.skills-test --focus psi.agent-session.commands-test --focus psi.agent-session.resolvers-test` (`96 tests, 497 assertions`). No separate old-path doc references remained after the cutover-focused search.
- 2026-05-29 task-implementation-review: re-read the review skill, task artifacts, built-in workflow skill content, referenced discovery/command/resolver tests, and cutover state; reran focused verification (`clojure -M:test --focus psi.prompt-assets.skills-test --focus psi.agent-session.commands-test --focus psi.agent-session.resolvers-test`, `96 tests, 497 assertions`, green). Found no new actionable implementation issues; existing steps already cover the implemented cutover and proof surfaces, so no new unchecked `steps.md` items were added.
- 2026-05-29 follow-up execution pass: read `steps.md` plus the supporting task artifacts (`implementation.md`, `design.md`, `plan.md`) to identify unchecked actionable follow-up items from the preceding review pass. There were no unchecked `steps.md` items remaining and the latest implementation review added no new actionable follow-ups, so no further execution work was possible in this pass. Left `steps.md` unchanged and recorded this no-op execution result here.
