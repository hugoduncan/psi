- [x] Define the required replacement cutover semantics for `.psi/skills/workflow/SKILL.md` under current discovery precedence (`project > user > built-in`): either require same-slice removal before verification, or specify an explicit temporary compatibility-stub mechanism that cannot shadow the built-in `workflow` skill in discovery, `/skills`, `:psi.skill/by-source`, and `/skill:workflow`.
- [x] Fix the minimum authoritative reference set to concrete canonical workflow authoring anchors instead of open-ended classes: choose the specific example workflow files under `.psi/workflows/` and the exact workflow loader/runtime/docs/test files that the built-in `workflow` skill must point to for the current repository grammar and conventions.
- [x] Add `doc/workflow-grammar.md` and `doc/workflow-grammar-concepts.md` to the minimum authoritative reference set and the built-in skill's required content anchors. `design.md` requires the replacement skill to preserve current repository-specific workflow authoring guidance, but `doc/workflows.md` explicitly makes those two docs the formal and conceptual canonical sources for the preferred target-authored grammar; without requiring them, the task can be satisfied by examples plus code seams while omitting the repository's stated canonical grammar references.
- [x] Align the executable review checklist with the required higher-surface verification set: add an explicit `steps.md` item for focused resolver/discovery proof that built-in `workflow` is visible and non-shadowed on `:psi.agent-session/skills` and `:psi.skill/by-source`, rather than leaving those mandatory acceptance/verification surfaces implied only by a broader `/skills` + `/skill:workflow` step or an optional `/help` parity note.
