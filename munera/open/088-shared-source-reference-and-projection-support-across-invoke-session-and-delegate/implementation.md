2026-05-04
- Design review: task artifacts require appending review notes here and adding follow-ups to `design-steps.md`, but neither file existed in this task directory; created both so the review/output surface is explicit.
- Design review: new actionable ambiguity — the task says invoke/session/delegate should share one canonical source/reference/projection resolution substrate, but it does not identify the intended canonical namespace/API boundary or whether older authoring-only compatibility helpers are meant to converge in-scope versus remain separate.
- Follow-up execution: made the review/output surface explicit in task docs — `design.md` and `plan.md` now name `design-steps.md` as the actionable ambiguity surface and `implementation.md` as the dated review/blocker log.
- Follow-up execution: documented the canonical boundary for task 088 as a shared workflow-runtime IR source-resolution substrate. Current runtime semantics are concentrated in `components/agent-session/src/psi/agent_session/workflow_step_prep.clj`; authoring/compiler helpers in `workflow_file_authoring_session.clj`, `workflow_current_ir_compiler.clj`, and `workflow_target_ir_compiler.clj` are explicitly treated as authored-syntax translation/compatibility seams rather than long-term runtime resolution owners.
- Design review: new actionable inconsistency — task 088's design/plan/steps say first-cut `:path` and `:projection` semantics should be shared across invoke/session/delegate, but the referenced compatibility authoring seam in `components/agent-session/src/psi/agent_session/workflow_file_authoring_session.clj` and its tests still encode a different projection contract (`:projection :text|:full|{:path ...}` on workflow-file `:session` inputs) than the canonical IR/doc surface in `doc/workflow-ir.md` (`:path` sibling plus IR `:projection` like `:none`/`:full`/`{:type :tail ...}`). The task files do not currently say whether that authored-syntax divergence is an intentional preserved compatibility surface for 088 or a convergence target that must be retired/translated explicitly.
- Follow-up execution: decided and documented that the legacy workflow-file `:session` projection contract remains an explicit compatibility authoring surface for task 088 for now, but must be treated as authored-syntax translation into canonical IR source-spec semantics rather than as already-identical runtime projection language.
- Follow-up execution: extracted the canonical runtime-owned source resolution substrate to `components/agent-session/src/psi/agent_session/workflow_source_resolution.clj` and rewired `workflow_step_prep.clj` to delegate canonical IR-shaped source-spec resolution there.
- Verification note: focused `bb clojure:test:unit --focus-ns psi.agent-session.workflow-step-prep-test,psi.agent-session.workflow-ir-runtime-adoption-test` did not stay isolated because the task alias still ran the broader randomized unit suite; the suite exposed a pre-existing expectation drift in `workflow_statechart_runtime_test.clj` around richer normalized session outputs (`:final-llm-reply`, `:transcript`, `:result`) now present alongside legacy `:text`.
- Follow-up execution: expanded the runtime-owned substrate in `workflow_source_resolution.clj` beyond bare source-spec application so it now canonically owns invoke-arg resolution, template-var materialization/rendering, contribution materialization, delegate context resolution, and delegate prompt-string rendering for normalized IR shapes.
- Follow-up execution: rewired `workflow_step_prep.clj` template rendering to delegate to the shared runtime substrate rather than keeping a local re-encoding of template var resolution semantics.
- Follow-up execution: added `workflow_source_resolution_test.clj` to prove shared mixed-form source semantics across invoke args, session template contributions, delegate prompt/context materialization, transcript projection handling, and invalid source-spec rejection.
