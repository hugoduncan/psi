# 062 — Steps

- [ ] Define per-step override authoring for system prompt / tools / skills / model / thinking as peer keys in `:session`
- [ ] Implement override handling in step prep
- [ ] Preserve inherited defaults when overrides are absent, while honoring explicit empty tool/skill overrides
- [ ] Reject malformed override values and unsupported `:session` keys for the implemented `060`–`062` surface
- [ ] Add focused tests
- [ ] Run focused workflow tests
