# 062 — Plan

1. Define the authoring shape for per-step session overrides under `:session`.
2. Route those fields through `workflow_step_prep.clj`.
3. Preserve default inheritance when overrides are absent.
4. Add focused tests for override semantics.
5. Run focused workflow tests.
