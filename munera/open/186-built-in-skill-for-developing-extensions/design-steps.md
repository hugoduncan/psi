- [x] Create `plan.md`, `steps.md`, and `implementation.md` before implementation/review execution so the task satisfies the required Munera task artifact set and the review/implementation loops have canonical surfaces to update.
- [x] Clarify the canonical built-in skill name and packaged resource path/provenance to implement (for example `psi/skills/<skill-name>/SKILL.md` with the resulting runtime skill `:name`), so packaging, discovery checks, and docs all verify the same identity.
- [x] Define the minimum authoritative reference set the skill must point to for extension development, including the exact GitHub URL for `doc/extension-api.md` and the specific code/doc seams to cite for extension API, manifest/install model, permissions, and dispatch/runtime boundaries.
- [x] Clarify whether the skill scope is new-extension creation only or also includes modifying/debugging existing extensions, so the authored guidance and usefulness verification do not diverge.
- [x] Define what counts as sufficient verification for "discoverable and usable" without prose-fragile assertions: which discovery/introspection/invocation/readability surfaces must prove the built-in skill is present with built-in provenance and readable through ordinary skill file semantics.
- [x] Align the task artifacts on the concrete source-tree authoring location for the packaged built-in skill. Current design/plan/steps name only the built-artifact resource path `psi/skills/extension-development/SKILL.md`, but the built-in packaging precedent from tasks 184/185 and current resource layout require authoring under `bases/main/resources/psi/skills/extension-development/SKILL.md` so implementation does not diverge between repo write path and packaged resource identity.
- [x] Align verification planning with the discovery/listing surfaces the design already requires. The design says normal discovery/introspection and command/UI-oriented skill listing surfaces must prove visibility for `extension-development`, but plan/steps currently scope focused proof only to prompt-assets built-in discovery/materialization/invocation; add explicit task coverage for representative higher surfaces such as resolver/command listing proof or narrow the design if those higher surfaces are intentionally out of scope.
