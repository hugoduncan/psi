- [ ] Clarify whether this build task should preserve the existing thrown-error contracts of current direct-adopter registry components (`workflow-registry`, `tool-registry`, and `command-registry`) during the first integration scaffolding, or whether follow-on adoption tasks are explicitly allowed to introduce the new explicit result-map semantics as a breaking lower-layer change.
- [ ] Choose and record the authoritative component path / namespace family for the new shared registry component so implementation and focused tests have a single unambiguous home.
- [ ] Specify the initial root-state host/constructor boundary for declared registries: which lower API creates or declares a registry id in root state, and what minimal declared-registry state shape tests should treat as authoritative.
