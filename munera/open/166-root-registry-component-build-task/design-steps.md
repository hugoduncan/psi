- [x] Clarify whether this build task should preserve the existing thrown-error contracts of current direct-adopter registry components (`workflow-registry`, `tool-registry`, and `command-registry`) during the first integration scaffolding, or whether follow-on adoption tasks are explicitly allowed to introduce the new explicit result-map semantics as a breaking lower-layer change.
- [x] Choose and record the authoritative component path / namespace family for the new shared registry component so implementation and focused tests have a single unambiguous home.
- [x] Specify the initial root-state host/constructor boundary for declared registries: which lower API creates or declares a registry id in root state, and what minimal declared-registry state shape tests should treat as authoritative.
- [x] Add an explicit implementation/test step for the required list-entries operation, including the contract that listing an unknown registry fails explicitly rather than silently declaring or treating it as empty.
- [x] Add an explicit implementation/test step for registry declaration semantics: declaration is idempotent for known registries, and register/unregister/clear/list operations do not implicitly declare missing registries.
