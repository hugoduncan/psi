# 183 design follow-up steps

- [x] Clarify child-session common-core inheritance gap: 9 of 17 common-core fields are not inherited from parent in child-session (they get `initial-session` defaults). Design must state whether this divergence is intentional-to-preserve or a gap-to-flag, and the shared classification docstring must reflect whichever answer is chosen.
- [x] Resolve `:nucleus-prelude-override` dual classification: it's a preference in init.clj (carried as-is) but a prompt-building input in child-session (consumed during prompt derivation). Design classification comment should distinguish "carried as-is" fields from "consumed during derivation" fields, or explain why the distinction doesn't matter for this task.
- [x] Define "authoritative vs derived" for the acceptance criterion: either add a column/annotation to the field classification that marks each field as authoritative (set by user/config, persisted) or derived (set by runtime, transient), or remove/reword the AC if the role-category classification already satisfies the intent.
- [ ] Fix child-session inherited field count: header says "(8 fields)" but only 7 bullet points are listed; `:nucleus-prelude-override` is explicitly "NOT set as a standalone field." Either correct to 7 inherited / 10 not-inherited (adding `:nucleus-prelude-override` to not-inherited), or promote `:nucleus-prelude-override` to a bullet point if it counts as inherited. Align the same count in scope (line 40) and child-session documentation requirement (line 170).
- [ ] Expand child-session documentation scope to cover all three constant groups: scope and child-session documentation requirement only mention common-core fields, but child-session also inherits/derives all 4 prompt-state fields and both model-identity fields. The documentation requirement should cover the child-session's relationship to `prompt-state-fields` and `model-identity-fields`, not just `common-inherited-fields`.
