- [x] Resolve base tool derivation gap: decide whether this task requires base tool registration in tool-registry as a prerequisite, defines an alternative derivation source (e.g. runtime agent data atom), or is blocked until base tools are registered. Update design.md scope/constraints and consumer migration paths accordingly.
- [x] Specify the on-demand derivation API: name the function/component that resolves `tool-ids → tool definition maps`, define its signature and inputs (ctx? registry? session-id?), and update the consumer table migration paths to reference it instead of vague "derive from `:tool-ids`".
- [x] Add missing consumer `auto_session_name.clj:224` to the consumer table with its migration path (passes `:tool-defs []` to `psi.extension/create-child-session`).
- [x] Distinguish event/contract fields from session-state fields in the consumer table: reclassify `session_lifecycle.clj:126`, `mutations/session.clj:89`, `workflow_judge.clj:61`, and `child_session_contract.clj:15` as dispatch event/contract consumers, and update their migration paths to describe the event contract change (not just session-state removal).
- [x] Clarify `tool-authority-fields` helper fate: decide whether it is removed, reshaped to return only `{:tool-ids ids}`, or kept with the current shape; update the callers' merge patterns in the design accordingly.
- [x] Move `context.clj:134` from the "session-state read sites" table to the "dispatch event/contract fields" table with migration path: "Pass `:tool-ids` instead of `:tool-defs`; upstream callers provide `:tool-ids`".
- [x] Reclassify `attempts.clj:70` and `statechart_runtime.clj:90`: move to the dispatch event/contract table or remove from the consumer table, depending on the step-config output decision (see next item). Update migration paths — "Derive from `:tool-ids` via `resolve-tool-defs`" is wrong for these pass-through sites.
- [x] Decide and document the step-config output field: does `workflow_step_session_config/core.clj:196` output `:tool-defs` (derived maps) or `:tool-ids` (strings) in the step-config map? This determines whether downstream `statechart_runtime.clj:90` and `attempts.clj:70` need migration at all.
- [x] Remove `system_prompt.clj:391-392` from the consumer table — it is a function parameter docstring, not a session-state read or contract field. The callers that pass tool-defs to this function are already listed separately.
- [x] Remove or reclassify `app_runtime.clj:364,468` from the session-state read sites consumer table: both are prompt build-opts construction sites using local `refreshed-tool-defs` variables, not session-state reads. Same category as already-removed `system_prompt.clj:391-392`. Add to "Removed from consumer table" section with rationale.
- [x] Correct the `:active-tools` removal step: `:active-tools` does not exist in session schema (`model.clj`) or `initial-session` defaults — it is only injected at runtime by `tool-authority-fields`. Update steps.md to describe the actual removal surface: (a) `tool-authority-fields` removal (already scoped), (b) `prompt_request.clj:279` read-site migration. Remove the incorrect "from schema and `initial-session` defaults" wording for `:active-tools`.
- [x] Clarify the scheduler session-config contract change: `psi_tool_scheduler.clj:78` `session-config-supported-keys` and `scheduler.clj:58` handler destructure `:tool-defs` from the AI-facing scheduler create API. Decide: (a) does the scheduler accept `:tool-ids` (strings) and derive tool-defs internally via `resolve-tool-defs`, or (b) does it continue accepting `:tool-defs` (maps) as a user-facing convenience? Document the API contract change and any persisted-schedule compatibility implications in design.md.
- [ ] Address `child_session_state.clj` line 98 write site: `child-session-base-state*` explicitly writes `:tool-defs tool-defs` into the child session data map. After `tool-authority-fields` removal, this would write `:tool-defs nil`, violating AC 1. Update the consumer table migration path and steps.md step 3 to explicitly include removing the `:tool-defs` key from the child session data map construction.
- [ ] Deduplicate `prompt_request.clj:279` across steps: the same `:active-tools` → `:tool-ids` migration appears in both step 2 ("Migrate session-state read sites") and step 4 ("Remove fields from schema, lifecycle, and helpers"). Consolidate into one step to avoid ambiguity about execution order.
