# Workflow `resume-run` fails with "not blocked" error when status is `:running`

> Auto-generated by the context-manager post-turn tooling-friction analyzer (task 239). No human review has occurred yet — verify before acting.

## Friction

The agent attempted to resume a workflow run that was in `:running` status, but the tool rejected it with a validation error stating "Workflow run is not blocked". This indicates a mismatch between the agent's mental model of workflow control (likely expecting `resume` to continue execution or handle pending states) and the tool's strict state machine (which likely requires `resume` only for `:blocked` states, or requires a different action for `:running` states). This creates friction in automating workflow progression.

## Evidence

Turn 2 shows `:psi-tool/workflow-op :resume-run` resulting in `:psi-tool/overall-status :error` with message "Workflow run is not blocked" and data `{:status :running}`.

## Suggested change

Update the `workflow` tool documentation or schema to clearly distinguish between actions for `:running` vs `:blocked` states, or implement a `continue-run` or `poll-run` action for `:running` states to avoid validation errors. Alternatively, the tool could auto-detect the correct action based on the current status if the intent is simply "proceed".
