# Design follow-up steps

- [x] Re-diagnose the root cause in design.md: the test ns already requires `[org.httpkit.client :as http-client]` and http-kit 2.8.0 is already declared in both root `deps.edn` and `extensions/dev-http/deps.edn`. The warning exists because `org.httpkit.client/get`/`post` are generated by the private `defreq` macro in http-kit's client.clj, so clj-kondo's analysis never registers the vars. Do not fix via a project-global `.clj-kondo/config.edn` `:unresolved-symbol` suppression: per `λ lint(f)` (fix > suppress(inline) > exclude ≫ suppress(global)) and `λ fix(bug)` (workaround ⇝ complexity), a global exclude would mask genuine unresolved-var errors elsewhere and create the false-negative noise the friction report itself cites.
- [x] Implement the fix through the existing per-library import mechanism: extend `.clj-kondo/imports/http-kit/http-kit/config.edn` with a client-side hook (or namespace export under `.clj-kondo/`) that registers the `defreq`-generated vars, mirroring the existing server-side `with-channel` hook (`.clj-kondo/imports/http-kit/http-kit/httpkit/with_channel.clj`). Keep the change localized to the http-kit import (one rule cluster, minimal mechanism) rather than editing project-global `.clj-kondo/config.edn` for an http-kit-specific concern.
- [x] Add an acceptance criterion with an executable proof surface per change_chain: re-run `clojure -M:lint --lint extensions/dev-http/test/extensions/dev_http_test.clj` and require zero unresolved-var warnings with no new warnings. Evidence shows two warnings — `http-client/get` (line 572) and `http-client/post` (line 737) — while design.md cites only `post`; acceptance must cover both. design.md also lacks the protocol-required acceptance section (munera design artifact: what/why, goal, context, constraints, acceptance).
- [x] Resolve the fix-mechanism ambiguity in the Suggested change: it joins two disjoint remedies with 'or' (update the clj-kondo configuration vs ensure the dependency is properly declared), and 'include the correct namespace' alone admits several mechanisms (adding the ns to clj-kondo analysis, an `:unresolved-symbol` exclusion, or a defreq-aware hook). Commit the design to one mechanism and state what "the linter can resolve the var" means for acceptance: analysis-level resolution, not warning silencing via exclusion/ignore.
- [x] Pin the verification surface for acceptance: state which lint invocation proves the fix — the repo-wide `bb lint` / `clojure -M:lint` step (the surface where the friction actually occurred, and where a residual second warning would still surface) vs a targeted single-file lint — and what 'no unresolved-var warnings' means there (clean for this file, no new warnings elsewhere).
- [ ] Reconcile plan.md decision 1's facility choice with design.md's Mechanism text: design.md names a closed two-facility set (an `analyze-call`/namespace hook, or a `:namespaces` var declaration with `:defined-by clojure.core/defn`), but the plan picks `:lint-as {org.httpkit.client/defreq clojure.core/def}` — a third facility — and asserts the design "still holds". Verified in the clj-kondo 2025.09.19 jar source: there is no top-level `:namespaces` var-registration config key (`:defined-by` is analysis output, not config input; the only `:namespaces` config references are linter options), so one of design.md's two named facilities does not exist in the pinned clj-kondo and the chosen facility is outside the other. Update design.md Mechanism to name `:lint-as` as the chosen facility (or record the facility examples as non-exhaustive within the committed per-library registration) so the implementation slice is not rejected as mechanism drift.
- [ ] Reconcile AC2's confinement clause with the plan's `.gitignore` edit: AC2 says the change is confined to `.clj-kondo/imports/http-kit/http-kit/`, but plan.md decision 2 and steps.md slice 4 add `.gitignore` (negation to track the import dir) to the intended change set while asserting "AC2 holds literally". Amend AC2 to explicitly permit the enabling `.gitignore` edit (the lint-config mechanism stays confined; `.gitignore` is enabling metadata), or the slice-4 AC2 gate and the design's acceptance text will disagree.
- [ ] Reconcile the stale-cache provenance contradiction: implementation.md's design-review note claims "2.5.3/2.8.0-beta3/2.9.0-beta1 also in ~/.m2 but unused", but the stale cache plan.md R3 cites as the warning source (`.clj-kondo/.cache/v1/clj/org.httpkit.client.transit.json`, built 2026-06-29) records `~:filename .../http-kit-2.9.0-beta1.jar:org/httpkit/client.clj` — the baseline analysis came from a non-pinned http-kit version. Record the actual provenance in design.md Context / plan.md R3 (correcting the "unused" claim), and add a slice-2 verification that the rebuilt cache entry records the 2.8.0 jar (grep `~:filename`), since clj-kondo cache entries are ns-keyed, not version-keyed — a silently version-mismatched cache is exactly the R4 recurrence risk.
