- 2026-05-29 ambiguity review: found two actionable design gaps after reading `design.md` plus current workflow run/session/runtime surfaces. First, the task uses both "terminal completed state" and "completed workflow run" but does not say whether retention applies only to `:completed` runs or to the full terminal set (`:completed`, `:failed`, `:cancelled`, `:timed-out`), which current runtime/introspection surfaces already distinguish. Second, the retention configuration note is too vague for implementation: the design must name the authoritative configuration/ownership surface and when the effective retention count is read for a run (global runtime config vs per-originating-session persisted state vs inherited session default at run creation).
- 2026-05-29 ambiguity follow-up execution: completed both newly added `design-steps.md` items. `design.md` now defines the retained terminal status set as the canonical workflow runtime statuses `:completed`, `:failed`, and `:cancelled` (excluding nonexistent workflow-run `:timed-out`), and it names the authoritative retention configuration surface as runtime context config `[:config :completed-workflow-run-retention-count]`, read at terminal transition time rather than snapshotted at run creation.
- 2026-05-29 inconsistency review: found two actionable design inconsistencies after re-reading `design.md` against current workflow/session artifacts. First, `design.md` treats a removed run as having one associated workflow-owned session, but runtime surfaces record multiple per-run child-session links across attempts (`:execution-session-id`) and judge passes (`:judge-session-id`), so the cleanup target set is underspecified and inconsistent with the referenced model. Second, the design says cleanup removes workflow-owned sessions for removed runs, but current session lifecycle primitives distinguish single-session close from whole-tree close; without naming whether retention cleanup closes just linked sessions or linked workflow-owned subtrees, the design can either leak descendants or over-delete.
- 2026-05-29 inconsistency follow-up execution: completed both newly added `design-steps.md` items. `design.md` now defines the authoritative per-run cleanup target as the deduplicated union of attempt `:execution-session-id` and `:judge-session-id` links recorded on the removed run, scoped only to workflow-owned linked roots. It also specifies whole-tree cleanup via session-tree close semantics for each linked root so workflow-owned descendants are removed with their parent and unrelated sessions are not inferred or broadened into scope.
- 2026-05-29 ambiguity review repeat: found one new actionable ambiguity after re-reading task artifacts plus current workflow run resolvers/tests. The task’s cleanup contract is defined over the full linked workflow-owned session-root set (execution + judge ids), but the referenced current higher read/introspection surface still projects only `:psi.workflow.run/execution-session-ids`. Plan/scope should say whether task 189 intentionally leaves introspection asymmetric or must also align/add a canonical linked-session projection and proof updates so read surfaces match the cleanup contract.
- 2026-05-29 ambiguity follow-up execution repeat: completed the newly added `design-steps.md` item from commit `d46f61bc`. Re-read current workflow run resolvers/tests, chose to align introspection rather than leave cleanup broader than read surfaces, and updated `design.md`, `plan.md`, and `steps.md` to require a canonical linked-session projection derived from the same deduplicated execution-plus-judge session-id set used by retention cleanup. Marked the design-step done; left implementation checklist items for later code/test execution.
- 2026-05-29 inconsistency review repeat: found one new actionable inconsistency after re-reading `design.md`, `plan.md`, `steps.md`, and current workflow runtime/resolver surfaces. The task requires deterministic newest-first retention ordering by terminal transition time, but the artifacts never define a tie-breaker when multiple retained terminal runs for the same originating session share the same terminal timestamp. Without an explicit secondary ordering surface, keep/remove selection can diverge across implementation and tests despite the determinism requirement.
- 2026-05-29 inconsistency follow-up execution repeat: completed the newly added `design-steps.md` tie-breaker item from commit `2e486485`. Re-read the current workflow runtime model and confirmed canonical workflow run creation order already exists at `[:workflows :run-order]`, so the task now defines equal-` :finished-at` retention ordering by later creation order treated as newer. Updated `design.md`, `plan.md`, and `steps.md`, including a focused proof item for the equal-` :finished-at` case; no older pre-review design steps were executed.
- 2026-05-29 implementation pass: added `psi.agent-session.workflow-run-retention` with effective retention lookup/defaulting, negative-config rejection, retained-terminal selection, deterministic keep/remove ordering, authoritative linked session-id derivation, and cleanup application that tree-closes workflow-owned linked session roots before removing older runs. Wired cleanup into canonical execute/resume/cancel workflow terminalization mutations, aligned workflow resolvers with new `:psi.workflow.run/linked-session-ids`, and added focused proof for linked-session projection, negative-config rejection, default retention tree cleanup, zero-retention immediate removal, equal-`:finished-at` tie-breaking, non-terminal immunity, and per-originating-session isolation.
- 2026-05-29 implementation follow-up pass: completed the remaining focused retention proof by adding one test that removes multiple linked execution/judge workflow-owned roots recorded on the same removed run and one test that proves explicit retention `2` keeps the two newest retained terminal runs. Re-ran focused retention tests and lint; both passed.
- 2026-05-29 implementation review: found one actionable proof gap. The task requires wiring retention cleanup into canonical execute/resume/cancel terminalization paths, but `canonical_workflows_test.clj` still lacks focused mutation-surface proof that those mutations actually trigger retention removal rather than only exercising the helper directly. Add mutation-level tests that terminalize runs via execute/resume/cancel and assert older retained runs plus linked workflow-owned session trees are removed from state.
- 2026-05-29 implementation follow-up pass: completed the newly added mutation-surface proof item by adding focused `canonical_workflows_test.clj` coverage for execute, resume, and cancel terminalization paths. Each mutation now proves retention cleanup is triggered from the public mutation seam by removing an older retained terminal run and its linked workflow-owned child session while preserving the originating parent session. Focused mutation tests passed (`12 tests, 109 assertions`).
- 2026-05-29 task-test-review: found one actionable proof gap. Task 189 calls out affected workflow introspection/listing expectations, but current tests do not prove the public `list-workflow-runs` mutation surface reflects retention cleanup after terminalization removes older retained runs. Add focused mutation-surface proof that listing after execute/resume/cancel cleanup excludes removed runs while retaining the newest run(s).
- 2026-05-29 follow-up execution: completed the newly added listing proof by extending `list-workflow-runs-test` with execute/resume/cancel terminalization cases. Each public mutation now proves the post-cleanup listing excludes the removed older retained run and keeps only the newest retained run with the expected retained terminal status. This also satisfied the remaining checklist item to update affected listing expectations.
- 2026-05-29 test-shaper review: re-read the test-shaper skill, task artifacts, `workflow_run_retention.clj`, `canonical_workflows_test.clj`, and `workflow_resolvers_test.clj`, then re-ran focused retention/resolver proof (`14 tests, 143 assertions`). No new actionable test-shaping feedback found; current proof stays behavior-focused, deterministic, and locally comprehensible without redundant overlap.
- 2026-05-29 follow-up execution: completed the remaining unchecked seam-confirmation step after re-reading the implementation and proof. The canonical terminal transition seam is the public `psi.workflow/execute-run`, `psi.workflow/resume-run`, and `psi.workflow/cancel-run` mutation paths in `canonical_workflows.clj`, which invoke retention cleanup immediately after terminalization. The authoritative newest-first ordering time surface is workflow-run `:finished-at`, written by canonical terminal state transitions in `workflow-runtime` (`statechart_runtime.clj` for complete/fail and `core.clj` for cancel), with `[:workflows :run-order]` as the deterministic equal-` :finished-at` tie-breaker already covered by focused proof. No code change was needed; task checklist updated to reflect the already-implemented seam.
- 2026-05-29 docs review: found one actionable docs gap. Task 189 changes user-visible workflow introspection/listing behavior by automatically retaining only the newest completed workflow runs per originating session and cleaning up linked workflow-owned sessions, but no user-facing docs (`README.md`, `doc/`) or `CHANGELOG.md` entry describe that behavior or the new retention configuration surface `[:config :completed-workflow-run-retention-count]`.
- 2026-05-29 follow-up execution: completed the remaining docs tasks. Updated `doc/workflows.md` with workflow-run retention and linked workflow-owned child-session cleanup behavior, added a top-level `README.md` note pointing to the behavior and config surface, and added a `CHANGELOG.md` Unreleased Changed entry because workflow listing/introspection now drops older retained terminal runs after cleanup.
- 2026-05-29 code-shaper review: no new actionable shaping issues after re-reading task artifacts, `workflow_run_retention.clj`, canonical workflow mutations/tests, resolver proof, focused tests, and lint. The retention helper stays narrow and locally comprehensible, cleanup remains isolated to explicit linked workflow-owned roots, ordering/trigger logic is deterministic, and mutation-surface proof covers the public terminalization seams without obvious duplication or broadening.

- 2026-05-29 follow-up execution: re-read `steps.md`, `implementation.md`, `design.md`, and `plan.md` after the preceding review pass to execute newly added actionable follow-ups. No unchecked `steps.md` or `design-steps.md` items remained; prior follow-up work already completed the seam-confirmation, mutation/listing proof, and docs/changelog updates. Left checklist state unchanged and proceeded to commit this no-op execution pass.
