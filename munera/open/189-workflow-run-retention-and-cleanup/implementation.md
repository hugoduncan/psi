- 2026-05-29 ambiguity review: found two actionable design gaps after reading `design.md` plus current workflow run/session/runtime surfaces. First, the task uses both "terminal completed state" and "completed workflow run" but does not say whether retention applies only to `:completed` runs or to the full terminal set (`:completed`, `:failed`, `:cancelled`, `:timed-out`), which current runtime/introspection surfaces already distinguish. Second, the retention configuration note is too vague for implementation: the design must name the authoritative configuration/ownership surface and when the effective retention count is read for a run (global runtime config vs per-originating-session persisted state vs inherited session default at run creation).
- 2026-05-29 ambiguity follow-up execution: completed both newly added `design-steps.md` items. `design.md` now defines the retained terminal status set as the canonical workflow runtime statuses `:completed`, `:failed`, and `:cancelled` (excluding nonexistent workflow-run `:timed-out`), and it names the authoritative retention configuration surface as runtime context config `[:config :completed-workflow-run-retention-count]`, read at terminal transition time rather than snapshotted at run creation.
- 2026-05-29 inconsistency review: found two actionable design inconsistencies after re-reading `design.md` against current workflow/session artifacts. First, `design.md` treats a removed run as having one associated workflow-owned session, but runtime surfaces record multiple per-run child-session links across attempts (`:execution-session-id`) and judge passes (`:judge-session-id`), so the cleanup target set is underspecified and inconsistent with the referenced model. Second, the design says cleanup removes workflow-owned sessions for removed runs, but current session lifecycle primitives distinguish single-session close from whole-tree close; without naming whether retention cleanup closes just linked sessions or linked workflow-owned subtrees, the design can either leak descendants or over-delete.
