- [x] Define the exact retained status set for this task. Decide whether retention cleanup applies only to runs with `:status :completed` or to all terminal workflow run statuses already present in runtime/introspection (`:completed`, `:failed`, `:cancelled`, `:timed-out`). Then align `design.md` terminology, behavior, and acceptance criteria so "completed workflow run", "terminal completed state", and "non-terminal" cannot be read inconsistently.
- [x] Define the authoritative retention-configuration surface and read timing. Specify whether the completed-workflow-run retention count is a global runtime setting, a per-originating-session persisted attribute/default, or another explicit surface; state where it lives, how a run finds its effective count, and whether cleanup uses the value current at completion time or a value snapshotted earlier.
- [x] Align session-cleanup scope with actual workflow-owned session linkage. `design.md` says a removed run has one associated workflow-owned session and removes "sessions owned only by removed workflow runs", but runtime artifacts show a run can accumulate multiple workflow-owned child sessions across attempts (`:execution-session-id`) plus judge sessions (`:judge-session-id`). Decide the authoritative owned-session set for one run and whether cleanup closes every workflow-owned session linked to that run, only execution sessions, or a narrower subset.
- [x] Define whether workflow retention cleanup removes individual workflow-owned sessions or whole workflow-owned subtrees. Current session lifecycle primitives distinguish `close-session-in!` from `close-session-tree-in!`, and workflow-owned child sessions may themselves have descendants. Specify the intended removal scope so session cleanup cannot leak descendants or over-delete unrelated sessions.
