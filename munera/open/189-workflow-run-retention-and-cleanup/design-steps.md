- [ ] Define the exact retained status set for this task. Decide whether retention cleanup applies only to runs with `:status :completed` or to all terminal workflow run statuses already present in runtime/introspection (`:completed`, `:failed`, `:cancelled`, `:timed-out`). Then align `design.md` terminology, behavior, and acceptance criteria so "completed workflow run", "terminal completed state", and "non-terminal" cannot be read inconsistently.
- [ ] Define the authoritative retention-configuration surface and read timing. Specify whether the completed-workflow-run retention count is a global runtime setting, a per-originating-session persisted attribute/default, or another explicit surface; state where it lives, how a run finds its effective count, and whether cleanup uses the value current at completion time or a value snapshotted earlier.
