2026-05-04 — design follow-up pass
- Resolved ambiguity: this slice will reject compiled current-grammar `:workflow-runtime` refs at runtime execution entry rather than widening canonical IR or translating them implicitly.
- Resolved ambiguity: observer-facing workflow run / step-run / attempt / history surfaces stay shape-stable in this slice; IR adoption is internal execution-substrate work.
- Updated `design.md`, `plan.md`, and `steps.md` to encode both decisions and to require focused execution-entry + observability regression proof during implementation.
- Review: actionable — task artifacts do not yet settle how this slice handles current-grammar `:workflow-runtime` refs before runtime execution adopts normalized IR. The current compatibility compiler explicitly preserves `:workflow-runtime` source refs in compiled IR with `:compat` breadcrumbs, and the focused proof in `components/agent-session/test/psi/agent_session/workflow_current_ir_compiler_test.clj` shows that such IR is structurally invalid at the present `workflow_ir.clj` boundary. Because this task’s design requires existing authored workflows to keep running once execution pivots to IR, the artifacts need an explicit decision: either runtime adoption excludes/ rejects workflows that still rely on `:workflow-runtime`, or this slice must translate/admit those refs before execution. Encode that rule in design/plan/steps and add focused proof before implementation.
- Review: actionable — the task says "adapt attempt/result recording and observability surfaces to IR concepts" but does not identify the concrete runtime-owned observability contract that must stay stable across the pivot. Existing execution/result surfaces still expose current accepted-result maps and step-centric data via `components/agent-session/src/psi/agent_session/workflow_execution.clj` and its tests, while the IR introduces typed execution forms (`:session`, `:invoke`, `:delegate`) plus compiler-owned `:compat` metadata. The artifacts need to state whether attempt/history/inspection surfaces remain current run-state shapes with only execution internals changing, or whether any observer-facing step/result shape is expected to shift to IR terminology. Encode the intended boundary in design/plan/steps and prove it with focused regression coverage so execution and introspection cannot drift independently.
