# Design follow-up steps

Ambiguity review follow-ups (2026-06-01). Each item resolves an actionable
ambiguity in `design.md`; resolve in design before/while executing.

- [x] Specify the seam-injection mechanism that replaces `with-redefs` for the nREPL-client and process-start seams. State explicitly how the nullable implementation is supplied to production code at test time (e.g. fn carried in `ctx`/runtime state like the existing `:runtime-handle :client-session`, a passed argument, or an options map) and require the same mechanism across `client_test.clj`, `started_test.clj`, and `attach_test.clj`. Note that `client.clj` currently resolves `nrepl.core/connect|client|client-session` inline via `requiring-resolve` and `started.clj`'s `start-process!` is private, so a new injection point is required.
- [x] Resolve the `ops_test.clj` scope contradiction: design Problem audit and Acceptance name only five files, but the 2026-06-01 re-audit found `with-redefs` on `psi.project-nrepl.eval/eval-instance-in!` there. Decide and state in design whether `ops_test.clj` is in scope (de-mocked via the shared eval/client seam) or explicitly out of scope, and update the Acceptance file list to match.
- [x] Define the concrete target shape for de-mocking the `config_test.clj` `resolve-config` merge test: state which of the three "preferred seam shapes" applies to it, whether the existing file-backed `read-project-preferences-test` makes the `resolve-config` merge-precedence proof redundant, and account for the real `read-project-preferences` `:version 1` key the current redef tests omit.
- [x] Clarify the Acceptance "at least one note ... for nREPL and process infrastructure": state whether a single combined `implementation.md` note suffices or each infrastructure seam (nREPL client, process start) needs its own documented strategy note.
- [x] Resolve the `commands_test.clj` operational-test "or" (real runtime+config vs. split formatting/parsing tests): state whether command-layer operational routing for `eval-op`/`interrupt` must be proven through real `eval-instance-in!`/`interrupt-instance-in!` (eval_test style with an installed in-memory client-session) or may be reduced to formatting/parsing tests, so the routing-coverage expectation is unambiguous.
