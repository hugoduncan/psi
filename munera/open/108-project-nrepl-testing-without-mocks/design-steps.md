# Design follow-up steps

Ambiguity review follow-ups (2026-06-01). Each item resolves an actionable
ambiguity in `design.md`; resolve in design before/while executing.

- [x] Specify the seam-injection mechanism that replaces `with-redefs` for the nREPL-client and process-start seams. State explicitly how the nullable implementation is supplied to production code at test time (e.g. fn carried in `ctx`/runtime state like the existing `:runtime-handle :client-session`, a passed argument, or an options map) and require the same mechanism across `client_test.clj`, `started_test.clj`, and `attach_test.clj`. Note that `client.clj` currently resolves `nrepl.core/connect|client|client-session` inline via `requiring-resolve` and `started.clj`'s `start-process!` is private, so a new injection point is required.
- [x] Resolve the `ops_test.clj` scope contradiction: design Problem audit and Acceptance name only five files, but the 2026-06-01 re-audit found `with-redefs` on `psi.project-nrepl.eval/eval-instance-in!` there. Decide and state in design whether `ops_test.clj` is in scope (de-mocked via the shared eval/client seam) or explicitly out of scope, and update the Acceptance file list to match.
- [x] Define the concrete target shape for de-mocking the `config_test.clj` `resolve-config` merge test: state which of the three "preferred seam shapes" applies to it, whether the existing file-backed `read-project-preferences-test` makes the `resolve-config` merge-precedence proof redundant, and account for the real `read-project-preferences` `:version 1` key the current redef tests omit.
- [x] Clarify the Acceptance "at least one note ... for nREPL and process infrastructure": state whether a single combined `implementation.md` note suffices or each infrastructure seam (nREPL client, process start) needs its own documented strategy note.
- [x] Resolve the `commands_test.clj` operational-test "or" (real runtime+config vs. split formatting/parsing tests): state whether command-layer operational routing for `eval-op`/`interrupt` must be proven through real `eval-instance-in!`/`interrupt-instance-in!` (eval_test style with an installed in-memory client-session) or may be reduced to formatting/parsing tests, so the routing-coverage expectation is unambiguous.

Inconsistency review follow-ups (2026-06-01). Each item resolves an actionable
internal inconsistency in `design.md` or a mismatch with referenced artifacts.

- [x] Reconcile the `:nrepl-connector` seam contract with `client.clj`: the design says the seam returns `{:transport :client :client-session :session-id}` with the inline `requiring-resolve` block as its real default, but that block in `client.clj` produces only transport/client/session-fn — `:session-id` is derived afterward from `session-fn` metadata with a throw-on-missing-session. State whether session-id derivation (and the missing-session-id throw) live inside the seam default or remain in `connect-instance-in!`, so the seam contract matches the source it claims to wrap.
- [x] Correct the `commands_test.clj` redef audit: design's Problem audit lists `resolve-config`, `eval-op`, and `interrupt`, but the current `commands_test.clj` redefines only `psi.project-nrepl.ops/eval-op` and `psi.project-nrepl.ops/interrupt` (the missing-start-command test already uses a real temp worktree; no `resolve-config` redef). Update the Problem audit (and note the matching error at implementation.md line 65) to match the real file.
- [x] Resolve the `config_test.clj` precedence contradiction: the design's `resolve-config` decision proposes proving precedence via shared `project.edn` + local `project.local.edn`, calling it "the same merge precedence the redef test asserted," but that is the project-internal shared/local merge (inside `read-project-preferences`, already covered by `read-project-preferences-test`), not the user-vs-project precedence the redef `resolve-config-test` actually proves. This contradicts the design's own "NOT redundant with `read-project-preferences-test`" claim. Restate the concrete target so the reshaped `resolve-config` test exercises user-vs-project (system<user<project) layering without colliding with the project-layer reader's existing proof.
- [x] Complete the `started_test.clj` seam assignment and resolve the runtime-handle overwrite conflict: design requires started_test to stop redefining both `start-process!` and `connect-instance-in!`, but the Seam-injection section assigns only `:process-launcher` to it and never states it must also seed `:nrepl-connector` (since `start-instance-in!` calls `connect-instance-in!` internally). Additionally, `start-instance-in!` overwrites `:runtime-handle {:process ...}` before calling connect, which drops any pre-seeded seam fn — so the "seed at acquisition / production call sites unchanged" mechanism cannot work for started-mode as written. State which seams started_test seeds and whether a production change (merge rather than overwrite `:runtime-handle`, or alternate seed point) is required.
