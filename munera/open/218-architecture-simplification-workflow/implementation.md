# Implementation Notes

- 2026-06-07 architecture-fit review found actionable follow-up **AR1**. The design fits the architecture-targets → Munera task → test-net → Gordian validation shape and preserves the invoking-worktree boundary, but its new post-implementation architecture review gate is not yet anchored to the existing `review-step`/`review-follow-up-steps` review-loop infrastructure or explicitly scoped to this architecture workflow. A bespoke review loop, or broadening generic `review-task-implementation` for all tasks, would fight the project's workflow/review architecture. Added AR1 to `design-steps.md`. PASS_STATUS ACTIONABLE_FEEDBACK.

- 2026-06-07 design follow-up **AR1** executed. Updated `design.md` to anchor the post-implementation architecture review gate in the existing `review-step` / `review-follow-up-steps` machinery, pass selected Gordian target/evidence plus task artifacts as context, and keep the gate workflow-local to `reduce-architectural-complexity` rather than broadening generic `review-task-implementation` or adding a bespoke loop. Marked AR1 done in `design-steps.md`. PASS_STATUS FOLLOW_UPS_COMPLETE.

- 2026-06-07 ambiguity review found actionable follow-ups **AMB1–AMB5**. Re-read `design.md`, `design-steps.md`, `implementation.md`, `.psi/skills/gordian/SKILL.md`, `.psi/workflows/reduce-incidental-complexity.edn`, and the shared `review-step` / implementation-review workflow definitions without reviewing `plan.md` or `steps.md`. New ambiguities: no-target selector routing token is unspecified; architecture candidate types do not yet map to concrete target/source areas for clean-baseline, coverage, and diff gates; baseline/evidence artifact filenames, formats, and root-relative references are not pinned; the new implementation architecture review skill's exact name/path is still only a candidate; and the post-implementation review sequence is ambiguous about generic `review-task-implementation` vs explicit workflow-local review-step gates. Added AMB1–AMB5 to `design-steps.md`. PASS_STATUS ACTIONABLE_FEEDBACK

- 2026-06-07 design ambiguity follow-ups **AMB1–AMB5** executed. AMB1 pins no-target/uninterpretable `architecture-targets` to `PASS_STATUS: ACTIONABLE_FEEDBACK` → `workflow/pass-status-routing` `"REPEAT"` → `:done`, with target-created `REVIEW_COMPLETE` → `"DONE"` → `review-task-design` and no raw status tokens in `:on` maps. AMB2 defines namespace/family/pair/community membership and root-relative source-area recording rules used consistently by baseline, coverage, diff, blast-radius, and reviews. AMB3 pins generated task artifacts: `before-diagnose.edn`, `architecture-targets.edn`, optional `target-issues.edn` or `target-issues-unavailable.edn`, `characterization-baseline.edn`, and after/compare/gate artifacts, all worktree-root-relative. AMB4 fixes skill identity/path to `.psi/skills/review-implementation-architecture/SKILL.md` with `:skill` value `review-implementation-architecture`. AMB5 replaces a generic `review-task-implementation` wrapper call with explicit workflow-local `review-step` gates: task-implementation-review, task-test-review, review-implementation-architecture, test-shaper, review-task-docs, and code-shaper. PASS_STATUS FOLLOW_UPS_COMPLETE.

- 2026-06-07 design inconsistency review found actionable follow-ups **INC1–INC3**. Re-read `design.md`, existing `design-steps.md` / `implementation.md`, `.psi/skills/gordian/SKILL.md`, the current `bb gordian architecture-targets --edn` and `target-issues --edn` envelopes, `.psi/workflows/reduce-incidental-complexity.edn`, `.psi/workflows/review-step.edn`, `.psi/workflows/review-task-implementation.edn`, and workflow grammar/IR docs without reviewing `plan.md` or `steps.md`. New inconsistencies: the selector/output contract names `:architecture-target-ranking` and permits JSON while the task-local artifact contract and live Gordian EDN use top-level `:winner` / `:candidates`; the workflow sequence requires before/after validation artifacts in post-implementation review context but has no explicit producer/gate between `implement-task` and the review-step chain; and the acceptance-test bullet only names architecture/test/code shape review gates despite the design requiring all six ordered post-implementation gates. Added INC1–INC3 to `design-steps.md`. PASS_STATUS ACTIONABLE_FEEDBACK

- 2026-06-07 design inconsistency follow-ups **INC1–INC3** executed. INC1 aligns the selector with the live authoritative `bb gordian architecture-targets --edn` top-level `:winner` / `:candidates` envelope and removes JSON / `:architecture-target-ranking` selection requirements. INC2 adds a workflow-local `validation-capture` gate after `implement-task` and before post-implementation reviews; failures write EDN failure artifacts, append repair notes, and route back to implementation before reviews. INC3 synchronizes acceptance-test expectations to lock validation-capture ordering plus all six ordered review-step gates: task-implementation-review, task-test-review, review-implementation-architecture, test-shaper, review-task-docs, and code-shaper. PASS_STATUS FOLLOW_UPS_COMPLETE.

- 2026-06-07 plan/steps ambiguity review found actionable follow-ups **PA1–PA4**. Re-read `plan.md`, `steps.md`, `implementation.md`, `design.md`, `.psi/workflows/reduce-incidental-complexity.edn`, `.psi/workflows/review-step.edn`, `.psi/workflows/review-task-implementation.edn`, workflow grammar/IR docs, and the Gordian command surfaces. New ambiguities: `select-and-create` does not specify how to route a valid target when required pre-task `before-diagnose.edn` capture fails; downstream delegates alternately require full handoff text and a bare generated task path without a defined extraction/source mechanism; the architecture review-step context says it includes artifact evidence without pinning which workflow sources vs task-local file reads provide that evidence; and Slice 5's real `architecture-targets --edn` envelope test does not state whether it is a live integration shape test or a fixture/content-lock, risking brittle candidate-value assertions. Added PA1–PA4 to `steps.md`. PASS_STATUS ACTIONABLE_FEEDBACK

- 2026-06-07 plan/steps ambiguity follow-ups **PA1–PA4** executed. PA1 pins all-or-nothing pre-task capture: unreadable/failing mandatory `architecture-targets`/`before-diagnose` capture creates no partial task, emits no `munera_task_path:`, reports command failure details, and ends `PASS_STATUS: ACTIONABLE_FEEDBACK`. PA2 chooses an explicit `extract-task-path` session step after target-created selection; downstream task consumers use that yielded root-relative task path while full selection handoff remains context. PA3 specifies exact `review-implementation-architecture` context sources and clarifies Gordian evidence is read from task-local files, not auto-inlined by workflow runtime. PA4 requires fixture/content-lock selector tests plus an optional live narrow `bb gordian architecture-targets --edn` envelope shape test that skips/falls back when unavailable and avoids repository-specific candidate assertions. Updated `plan.md` and Slice 2/4/5 checklist obligations; no code/tests/docs changed in this follow-up pass. PASS_STATUS FOLLOW_UPS_COMPLETE.

- 2026-06-07 plan/steps inconsistency review found actionable follow-ups **PI1–PI3**. Re-read `plan.md`, `steps.md`, `implementation.md`, `design.md`, `.psi/workflows/reduce-incidental-complexity.edn`, `.psi/workflows/review-step.edn`, `.psi/workflows/review-task-implementation.edn`, `.psi/workflows/implement-task.edn`, workflow grammar/IR docs, and live Gordian command help/envelope. New inconsistencies: `design.md` still routes the successful `select-and-create` selector directly to `review-task-design` while plan/steps require `extract-task-path` as the single downstream task-path source; PA2 routes malformed `extract-task-path` output to `terminal-stop-summary` but the terminal summary scope is only described for post-task no-implementation gate failures with a task identity; and the architecture review evidence contract says artifacts are received "in context" in design while plan/steps require task-local file reads rather than runtime artifact inlining. Added PI1–PI3 to `steps.md`. PASS_STATUS ACTIONABLE_FEEDBACK

- 2026-06-07 plan/steps inconsistency follow-ups **PI1–PI3** executed. PI1 synchronizes the selector success topology so `select-and-create` routes target-present `"DONE"` only to `extract-task-path`, and downstream task consumers cannot bypass the extracted path. PI2 expands `terminal-stop-summary` to cover malformed `extract-task-path` handoffs before design with no validated task path, distinct from post-task no-implementation gate stops. PI3 replaces design wording that implied Gordian artifacts are inlined in review context with the path-read boundary: workflow context carries yields/summaries, and `review-implementation-architecture` reads task-local root-relative artifact files. Updated planned tests accordingly; no code/tests/docs changed in this follow-up pass. PASS_STATUS FOLLOW_UPS_COMPLETE.

- 2026-06-07 implementation Slice 1 complete. Preflight observed `bb gordian architecture-targets --edn` emits the authoritative top-level EDN envelope: a map with `:winner` and `:candidates`; current repository run returned a `:winner` map whose selected keys include `:candidate/id [:family "psi.rpc.session"]`, `:candidate/type :family`, `:candidate/label "psi.rpc.session"`, `:members [...]`, `:score 102.89000000000014`, and `:confidence :low`, plus 294 top-level candidates. Smoke-checked `bb gordian target-issues --candidate '[:family "psi.rpc.session"]' --edn`; it exited 0 and emitted readable EDN with `:issues`, confirming the supported informational framing path for the current family winner. Added `.psi/skills/review-implementation-architecture/SKILL.md` with exact frontmatter name `review-implementation-architecture`; it requires task-local Gordian artifact reads, Munera artifacts, project architecture sources, and judgement of behaviour preservation, target fit, blast-radius discipline, architectural improvement/no-regression, and absence of shim/adapter complexity. Verified discovery with `psi.prompt-assets.skills/discover-skills` over `.psi/skills`; the new skill was found with no matching diagnostics. PASS_STATUS MORE_WORK_REMAINS.

- 2026-06-07 implementation Slice 2 complete. Added `.psi/workflows/reduce-architectural-complexity.edn` shell through generated-task plan review. The workflow now has `select-and-create` with invoking-worktree constraints, `bb gordian architecture-targets --edn` top-level `:winner` / `:candidates` selection, all-or-nothing pre-task capture including `before-diagnose.edn`, informational `target-issues.edn` / `target-issues-unavailable.edn`, namespace/family/pair/community membership and source-area resolution rules, generated `design.md` contract, commit/handoff obligations, and normalized pass-status routing (`REVIEW_COMPLETE` → `DONE` → `extract-task-path`, `ACTIONABLE_FEEDBACK` → `REPEAT` → done). Added `extract-task-path` as the single target-present task identity boundary, wired design/plan delegates to its yielded path, and added the malformed-handoff pre-design `terminal-stop-summary` branch. Verified with workflow-loader `load-edn-only` that the EDN registers `reduce-architectural-complexity` with no errors. This slice intentionally stops after `review-task-plan`; test-net, validation, review chain, docs, and content-lock tests remain. PASS_STATUS MORE_WORK_REMAINS.

- 2026-06-07 implementation Slice 3 complete. Extended `.psi/workflows/reduce-architectural-complexity.edn` with the pre-simplification architecture test-net gate: `review-task-plan` now routes to `clean-baseline`, which reads task-local Gordian artifacts and writes `characterization-baseline.edn`; `coverage-review` judges observable state/output coverage and emits sufficient coverage / `CHARACTERIZATION_STATUS: FIXABLE_GAPS` / `CHARACTERIZATION_STATUS: INFEASIBLE`; `coverage-disposition` routes only the immediately preceding coverage result; `coverage-fix` is constrained to characterization tests and minimal testability seams and loops back to coverage review; `diff-gate` compares from the baseline HEAD and blocks unclassified source changes or premature simplification before `implement-task`. Verified the EDN loads with workflow-loader and the success topology reaches `implement-task` only after clean baseline, coverage review, and diff gate. PASS_STATUS MORE_WORK_REMAINS.

- 2026-06-07 implementation Slice 4 complete. Added the post-implementation half of `reduce-architectural-complexity`: `implement-task` now routes to workflow-local `validation-capture`; validation captures `after-diagnose.edn`, `after-architecture-targets.edn`, `architecture-compare.edn`, and `architecture-gate.edn`, writes EDN failure maps plus repair notes on non-zero/unreadable commands, routes failed validation back to `implement-task`, and routes successful validation into ordered `review-step` gates. Added six explicit review delegates in order: `task-implementation-review`, `task-test-review`, `review-implementation-architecture`, `test-shaper`, `review-task-docs`, and `code-shaper`, all using `extract-task-path` as input. The architecture review context is exactly workflow yields (`:workflow-original`, `select-and-create`, `clean-baseline`, `coverage-review`, `diff-gate`, `implement-task`, `validation-capture`, `review-implementation-correctness`, `review-implementation-tests`); artifact contents remain task-local file reads. Added completed-run `final-summary` and retained two-shape `terminal-stop-summary`. Verified full workflow loads with workflow-loader. PASS_STATUS MORE_WORK_REMAINS.

- 2026-06-07 implementation Slice 5 complete. Added focused workflow-loader/content-lock coverage in `components/workflow-loader/test/psi/workflow_loader/task_218_workflow_definitions_test.clj`. Tests lock workflow loading/name registration, full step topology, selector/no-target/target-created prompt contracts, all-or-nothing pre-task capture, unsupported target-issues informational branch, invoking-worktree constraints, candidate membership rules, `extract-task-path` as sole downstream task identity, pre-design terminal stop, test-net routing, validation-capture ordering/failure routing, six ordered `review-step` gates and exact skill values, no generic `review-task-implementation` delegate, skill discoverability, architecture-review context/file-read boundary, direct delegate co-loading, and a live narrow `bb gordian architecture-targets --edn` envelope shape check. Verification green: `clj-paren-repair` no changes needed after final edit; focused Scry namespace 5 tests / 138 assertions; targeted clj-kondo 0 errors / 0 warnings. PASS_STATUS MORE_WORK_REMAINS.

- 2026-06-07 implementation Slice 6 complete. Updated `doc/workflows.md` with the user-facing `reduce-architectural-complexity` guide, invoking-worktree/no-worktree-switch requirement, architecture target scope, test-net/validation/review sequence, and distinction from `reduce-incidental-complexity`; added a README workflow pointer; and added a CHANGELOG `[Unreleased]` Added entry naming the workflow plus `review-implementation-architecture` gate/skill. Coherence check confirmed consistent workflow name, task-local artifact paths, `extract-task-path` boundary, normalized pass-status routes, validation artifact names, and six review-step order across design/plan/steps/skill/workflow/tests/docs/changelog. Final verification green: focused Scry `psi.workflow-loader.task-218-workflow-definitions-test` 5 tests / 138 assertions, targeted clj-kondo 0 errors / 0 warnings, and `git diff --check` clean. PASS_STATUS IMPLEMENTATION_COMPLETE.
