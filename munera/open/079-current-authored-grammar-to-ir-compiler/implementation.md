- Review: actionable — task artifacts do not yet settle the compiled IR surface for current `:result-schema`/accepted-result outputs. Current runtime bindings and prompts demonstrably read beyond `:outputs` into envelopes like `:diagnostics` (for example `components/agent-session/test/psi/agent_session/workflow_execution_test.clj` uses `{:source :step-output :path ["step-1-discover" :diagnostics :summary]}`), while `doc/workflow-ir.md` says step refs should target declared step-local `:outputs` or `:yields`. Decide whether the current-grammar compiler must lift compatibility-readable envelope fields such as diagnostics/blocked/result into declared IR outputs, or whether current `:step-output` refs to non-`:outputs` paths are intentionally out of scope/invalid for migration. Encode the rule in design/plan/steps and prove it with golden tests before implementation.
- 2026-05-04 — Resolved the accepted-result-envelope ambiguity in task artifacts. Decision: current-grammar compilation preserves whole-envelope and non-`:outputs` accepted-result reads (notably `:diagnostics` and `:blocked`) via narrowly-scoped IR `:compat` metadata, while canonical IR refs remain limited to explicit `:outputs` and `:yields`. Evidence read before settling the rule: `workflow_execution_test.clj` projection proof uses `:path ["step-1-discover" :diagnostics :summary]`; `workflow_step_prep_test.clj` preload materialization uses whole-envelope `:path ["plan"]`; `workflow_file_compiler_test.clj` proves current authoring already compiles non-adjacent accepted-result projections to current `:step-output` paths. Follow-up still open: add golden tests and, when implementation starts, tighten `doc/workflow-ir.md` / migration docs to reflect the compatibility contract explicitly.
- Review: actionable — task artifacts still do not state what the current required `:result-schema` compiles to, if anything. `doc/workflow-grammar-current.md` requires `:result-schema` on each current-authored step, but `doc/workflow-ir.md` and `components/agent-session/src/psi/agent_session/workflow_ir.clj` have no corresponding IR field or validation hook. Settle whether the compiler intentionally drops `:result-schema`, preserves it only as `:compat` breadcrumbs, derives IR `:outputs`/validation hints from it, or rejects unsupported schemas; then encode that rule in design/plan/steps and prove it with golden tests.
- 2026-05-04 — Resolved the current `:result-schema` ambiguity in task artifacts. Decision: normalized IR does not gain a canonical `:result-schema` field in this slice; current required `:result-schema` is preserved only as narrow step-level `:compat` metadata so authored/runtime breadcrumbs survive migration without expanding canonical IR execution semantics. Evidence read before settling the rule: `doc/workflow-grammar-current.md` requires `:result-schema`; `doc/workflow-ir.md` intentionally keeps canonical IR centered on execution form, outputs, yields, judge, routing, and compatibility metadata; `workflow_ir.clj` has no canonical result-schema field or semantic hook, so introducing one here would be speculative redesign rather than compatibility compilation. Follow-up still open: implement compiler + golden tests proving representative current-authored steps retain `:result-schema` only via `:compat`, then tighten migration/IR docs accordingly.
