# Design follow-up steps

- [x] Create `plan.md` and `steps.md` before implementation so the approach, target files, sequencing, verification commands, and risks are explicit and reviewable.
- [x] Choose the concrete OpenAI provider-native mechanism and transport surface for this slice, explicitly reconciling the design's public `/v1/responses` preference with the current `:openai-completions` and `:openai-codex-responses` adapters and documenting whether a new platform Responses transport/API enum is introduced or chat-completions JSON Schema response format is used first.
- [x] Specify the exact observable strategy metadata surface for both streaming and non-streaming provider calls, including where `:provider-native`, `:prompted-json`, `:repair-parse`, or `:unsupported` is emitted/stored and how callers read it without guessing from request shape.
- [x] Define Anthropic forced structured-output tool composition with ordinary tools: deterministic synthetic tool naming/collision handling, `tool_choice` behavior when user tools are also present, and how extracted synthetic tool input is separated from normal assistant tool calls.
- [x] Specify the concrete result/event field that carries provider-extracted structured-output payloads for both non-streaming and streaming calls, including Anthropic synthetic tool extraction, and state that this payload is not surfaced as an ordinary assistant tool call.
- [x] Clarify the AI-vs-workflow validation boundary for this slice: whether AI adapters validate/coerce the structured payload against the request schema, or only return extracted/raw structured payload plus strategy metadata for the workflow/runtime validation layer to handle later.
- [x] Resolve the residual testing ambiguity around "local validation is still invoked after provider response extraction": specify whether task 169 proves only that AI results preserve raw/extracted payloads for later workflow validation, or also adds an AI-level caller/test seam that invokes Malli validation in this slice.
- [x] Clarify structured-output capability semantics: define whether `:supported?` means any structured-output strategy including prompted fallback, provider-native support only, or request-time support under fallback policy; specify how fallback-only and unsupported models should be represented.
- [x] Specify the concrete prompted-JSON fallback request behavior for task 169: whether fallback selection injects schema-guided JSON instructions at the AI adapter boundary, relies on caller/runtime prompt text, or only reports fallback metadata, and define the request-shape test target.
- [x] Align referenced workflow structured-output docs with the finalized fallback boundary: AI adapters inject prompted-JSON schema instructions and report strategy metadata, while workflow/runtime parses/coerces/validates extracted/raw payloads before exposing structured values.
- [x] Choose the concrete streaming structured-output strategy/result metadata surface for task 169: either extend AI stream event schemas with named `:structured-output-strategy` and `:structured-output-result` events, or specify the exact existing callback/capture API; update the implementation plan and test target so providers expose one unambiguous surface.
- [x] Specify absent structured-output capability semantics for built-in and user/custom model descriptions: whether missing `:capabilities :structured-output` is invalid, defaults to unsupported, defaults to fallback-only, or is populated by migration/defaulting logic, and how strategy selection treats it.
- [x] Specify how runtime auth-path/transport overrides affect structured-output capabilities, especially OAuth-backed `openai/gpt-5.5`: define whether `resolve-runtime-model` must replace/normalize `[:capabilities :structured-output]` when it changes `:api` to `:openai-codex-responses`, or whether strategy selection recomputes effective capability from the resolved runtime API so Codex fallback-only/unsupported behavior cannot inherit platform Chat Completions native capability.
