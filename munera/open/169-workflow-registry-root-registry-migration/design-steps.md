- [x] Resolve whether `design.md` should require migrating the canonical persisted workflow-definition path away from `[:workflows :definitions]` or explicitly treat `session-state/state-path`, `workflow-runtime.model`, and any direct-path tests/docs as out of scope compatibility surfaces for this task.
- [x] Refine the seam inventory to explicitly classify extension-runtime `:loaded-definitions` as either an in-scope higher read/projection seam to keep coherent with `workflow-registry` after migration or an out-of-scope cache that does not need migration-guard coverage.
- [x] Refine `steps.md`/implementation scope to explicitly audit and migrate helper/test higher semantic seams that still write or read workflow definitions by direct `[:workflows :definitions ...]` shape when they should exercise the authoritative `workflow-registry` boundary instead, while preserving only intentional canonical-path compatibility assertions.
