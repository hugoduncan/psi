- [ ] Clarify the exact capability-selection seam for ChatGPT/Codex native structured output: define whether the authoritative discriminator is resolved base URL/transport (`https://chatgpt.com/backend-api/codex/responses`), auth mode/OAuth account context, `:api :openai-codex-responses`, or a combination, and name the runtime function/data seam where that decision must occur so public OpenAI Responses is not accidentally treated as the same capability.
