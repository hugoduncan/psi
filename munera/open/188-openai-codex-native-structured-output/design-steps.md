- [x] Clarify the exact capability-selection seam for ChatGPT/Codex native structured output: define whether the authoritative discriminator is resolved base URL/transport (`https://chatgpt.com/backend-api/codex/responses`), auth mode/OAuth account context, `:api :openai-codex-responses`, or a combination, and name the runtime function/data seam where that decision must occur so public OpenAI Responses is not accidentally treated as the same capability.
- [x] Align the design's current-code inventory and acceptance wording with actual OAuth runtime routing: `gpt-5.5` can currently resolve to `:openai-codex-responses` under OAuth with fallback-only structured-output capability, so task scope/tests must cover transport-resolved Codex models generally rather than implying `gpt-5.4` is the only built-in Codex-backed case or that Codex capability can stay coherent through `models.clj` annotation alone.
