- [x] Clarify the migration/validation boundary for mandatory `:format-request`: design and plan now require this task to migrate every currently registered built-in and owned extension tool in the runtime catalog, permit only narrowly scoped named temporary shims during in-flight rollout, and remove any such shim before task completion.
- [x] Clarify the exact RPC/backend surface that must carry the canonical preformatted compact header string: design and plan now name `:call-summary` on canonical tool lifecycle events/payloads — including pre-completion `tool/executing` and derived RPC-facing tool row payloads — as the authoritative transport surface.
- [ ] Align plan verification with the fixed task scope: remove the stale “follow-on Emacs decision” wording and require proof that Emacs consumes RPC/backend `:call-summary` in this task.
