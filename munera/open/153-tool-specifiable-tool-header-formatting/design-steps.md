- [ ] Clarify the migration/validation boundary for mandatory `:format-request`: design and plan currently require canonical registrations to reject missing formatters, but do not say whether this task must migrate every existing registered tool in the system or introduce a scoped compatibility allowlist/shim during rollout.
- [ ] Clarify the exact RPC/backend surface that must carry the canonical preformatted compact header string (`:call-summary` is named in the per-tool format specs, but design/plan do not identify the authoritative event/payload shape(s) that must expose it, leaving acceptance ambiguous for implementation and review).
