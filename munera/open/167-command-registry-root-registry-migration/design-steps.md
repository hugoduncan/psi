- [ ] Clarify the compatibility contract for duplicate command names across built-in provenance ids after migration: design.md preserves the current visible merged surface and mentions boundary collision/precedence, but it never states whether same-name collisions between two built-in providers are allowed and, if so, what deterministic winner rule the adapter must preserve or define over root-registry's unordered built-in storage.
- [ ] Clarify whether command listing order is part of the public compatibility contract for this migration: current `command-registry` tests explicitly prove `all-commands-in` returns first-encounter order by extension registration order and built-ins listed first, but design.md and steps.md mention merged visibility/precedence without explicitly requiring this listing-order behavior to be preserved or intentionally de-scoped.
- [ ] Clarify the implementation surface for `design-steps.md` itself: this review protocol requires ambiguity follow-up items there, but the task was created without that file, so future review/build passes need an explicit task artifact surface for design follow-up items to avoid ambiguity about where new design actions should be recorded.
