# Implementation

Created the task for migrating `command-registry` onto the shared `root-registry` substrate from task `166`.

Starting point:

- `command-registry` is the strongest first migration target
- the goal is to move storage ownership to `root-registry`
- current command-registry public behavior should remain intact
- any compatibility behavior that differs from raw root-registry semantics should live at the command-registry layer

Next step:

- audit current command-registry storage mechanics and public contract against the new root-registry API, then shape the migration around the minimum compatibility adapter needed

- 2026-05-21 ambiguity review: actionable feedback found. `design.md` / `plan.md` preserve merged visibility and collision behavior in broad terms, but they do not pin (1) how same-name collisions across multiple built-in provenance ids should behave once built-ins are stored through unordered root-registry entries, (2) whether current `all-commands-in` ordering guarantees (`built-ins first`, then first-encounter extension-registration order) are part of the migration contract, or (3) where design follow-up items should live for this task given `design-steps.md` was absent. Added new unchecked items to `design-steps.md`; no duplicates found.
- 2026-05-21 ambiguity follow-up: completed all newly added design-step items. `design.md` now explicitly preserves merged read-path precedence (`built-ins` over same-name extensions) and `all-commands-in` ordering (`built-ins first`, then first-encounter extension-registration order). It also narrows built-in/built-in collision scope: preserve same-provenance replacement, but do not broaden cross-provenance semantics in this migration. `plan.md` now carries those compatibility constraints explicitly, and `steps.md` now records that design-review follow-up items live in `design-steps.md` rather than `steps.md`.
- 2026-05-21 inconsistency review: no new actionable inconsistency feedback. `design.md`, `plan.md`, `steps.md`, and `design-steps.md` now align on the preserved compatibility surface: built-ins shadow same-name extensions on merged reads, `all-commands-in` preserves built-ins-first then extension registration order, built-in/built-in collisions remain intentionally narrowed to same-provenance replacement only, and design-review follow-up items live in `design-steps.md`. Referenced current command-registry and root-registry code/tests are consistent with that task boundary; no new unchecked `design-steps.md` items added.
- 2026-05-21 design-step execution pass: read the preloaded inconsistency-review result and re-checked `design-steps.md`; no newly added unchecked design-step items were present, so no design/plan changes were needed and `steps.md` implementation items were intentionally not executed.
- 2026-05-21 implementation pass: migrated `components/command-registry/src/psi/command_registry/registry.clj` onto `psi.root-registry.registry` while preserving the command-registry public boundary. Extension registrations now store one root-registry entry per extension path with a nested `:commands` map; built-ins store one root-registry entry per built-in provenance id with a nested command map and artificial `:built-in` ownership. Compatibility stays in the adapter layer: extension registration still requires prior extension presence in `:extensions`, validation still throws at the command-registry boundary, built-ins still shadow same-name extension commands on merged reads, and `all-commands-in` still lists built-ins first then extension commands by extension registration order. Focused tests were updated to prove migrated storage through the root-registry area rather than the removed legacy `:built-in-commands` storage path, while preserving behavior assertions for lookup/list semantics. Verification for this pass: `clojure -M:test --focus psi.command-registry.registry-test`, `clj-kondo --lint components/command-registry/src components/command-registry/test`, and `clojure -M:fmt -m cljfmt.main check components/command-registry/src/psi/command_registry/registry.clj components/command-registry/test/psi/command_registry/registry_test.clj` all passed.
- 2026-05-21 implementation review: no new actionable feedback. Reviewed `design.md`, `plan.md`, `steps.md`, `implementation.md`, `design-steps.md`, `components/command-registry/src/psi/command_registry/registry.clj`, `components/command-registry/test/psi/command_registry/registry_test.clj`, and `components/root-registry/src/psi/root_registry/registry.clj` against the task intent and architecture. The migrated adapter keeps root-registry storage ownership below the command boundary, preserves the specified built-in-over-extension merged read behavior and listing order, and keeps command-specific validation/compatibility local rather than reintroducing parallel storage. Re-ran focused test, lint, and format checks; all passed. No new unchecked `steps.md` follow-up items added.
- 2026-05-21 follow-up execution pass: read the preloaded implementation-review result plus `steps.md`, `implementation.md`, `design.md`, `plan.md`, and `design-steps.md`. No newly added unchecked implementation follow-up items were present in `steps.md`, and no blocking item required a partial execution update. Left task checklists unchanged because there was no new actionable work to execute from the preceding review pass.
- 2026-05-21 test review: no new actionable feedback. Reviewed `design.md`, `plan.md`, `steps.md`, `implementation.md`, `design-steps.md`, `components/command-registry/src/psi/command_registry/registry.clj`, `components/command-registry/test/psi/command_registry/registry_test.clj`, and `components/root-registry/src/psi/root_registry/registry.clj` against the task-test-review skill. Focused tests still cover the preserved task behaviour from `design.md`: extension-path precondition and command-name validation, same-extension replacement, exact-name identity without slash normalization, built-ins listed before extensions in `all-commands-in`, first-registration-wins ordering across extensions, built-in-over-extension merged lookup precedence, built-in registration without prior extension registration, and same-provenance built-in replacement. Re-ran `clojure -M:test --focus psi.command-registry.registry-test`, `clj-kondo --lint components/command-registry/src components/command-registry/test`, and `clojure -M:fmt -m cljfmt.main check components/command-registry/src/psi/command_registry/registry.clj components/command-registry/test/psi/command_registry/registry_test.clj`; all passed. No new unchecked `steps.md` follow-up items added.
- 2026-05-21 follow-up execution pass: re-read the preloaded review result plus `steps.md`, `implementation.md`, `design.md`, `plan.md`, and `design-steps.md`. No newly added unchecked actionable follow-up items were present in `steps.md`, and all `design-steps.md` items were already complete from earlier design review follow-up. No task files required further implementation changes; left checklists unchanged because there was no new executable follow-up work from the preceding review pass.
- 2026-05-21 test-shaper review: actionable feedback found. Reviewed `design.md`, `plan.md`, `steps.md`, `implementation.md`, `components/command-registry/src/psi/command_registry/registry.clj`, `components/command-registry/test/psi/command_registry/registry_test.clj`, and `components/root-registry/src/psi/root_registry/registry.clj` for clarity, signal, determinism, and behavioral coverage. Current tests prove built-ins-first ordering only for a single built-in provenance id; they do not pin the ordering contract when multiple built-in provenance ids each contribute commands, even though `all-commands-in` now derives built-in iteration from unordered root-registry entries. Added one unchecked `steps.md` follow-up to either prove deterministic multi-provenance built-in ordering or narrow the public contract if that ordering is intentionally unsupported. Focused test and lint checks still pass.
- 2026-05-21 follow-up execution pass: read the preloaded test-shaper review result and executed the newly added unchecked implementation follow-up from `steps.md`. Added a focused `all-commands-in` test covering two built-in provenance ids plus one extension command; current adapter behavior preserves built-in provenance registration order before extension commands, so the task contract did not need narrowing. Marked the follow-up step done. Verification: `clojure -M:test --focus psi.command-registry.registry-test`, `clj-kondo --lint components/command-registry/src components/command-registry/test`, and `clojure -M:fmt -m cljfmt.main check components/command-registry/src/psi/command_registry/registry.clj components/command-registry/test/psi/command_registry/registry_test.clj` all passed.
