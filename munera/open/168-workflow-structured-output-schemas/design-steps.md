# Design follow-up steps

- [x] Create `plan.md` and `steps.md` before implementation so the approach, target files, sequencing, verification commands, and risks are explicit and reviewable.
- [x] Choose the concrete structured-output contract shape for both authored workflow definitions and normalized IR, explicitly reconciling the design's conceptual `:output {:mode :structured ...}` examples with the existing `:outputs` step-local output surface and `:name`-based IR shape.
- [x] Specify the first standard schema/example to land in this slice (for example, workflow judge review result vs bug reproduction classification), including whether it is test-only or migrates one existing workflow.
- [x] Align `doc/workflow-grammar.md` with the chosen contract by adding session-step and LLM-judge `outputs?` grammar support and expanding `outputs` beyond delegate handoff, so the authored grammar no longer contradicts the design/plan examples.
- [x] Specify the structured-output wire format and coercion rules for prompted fallback, especially how JSON/EDN parsed values map to Malli keyword enums before validation and what is recorded when coercion fails.
- [x] Align `doc/workflows.md` with the structured-output contract so the guide no longer says `:handoff` is the only standardized structured export key or avoids a broader output surface; it should mention session/judge structured `:outputs` and point to grammar/IR docs for details.
- [x] Align `doc/workflow-ir.md` with the chosen structured-output contract by specifying normalized session-step structured `:outputs`, judge-local structured `:outputs`, canonical envelope metadata, and downstream reference semantics so implementers do not have to infer IR shape from design prose.
- [ ] Reconcile judge-local structured outputs with downstream source-ref semantics: explicitly specify whether judge outputs are only available to transition evaluation or are promoted/exported through the parent step's `{:step ... :output ...}` surface, then align design, workflow grammar, IR docs, and user guide wording.
