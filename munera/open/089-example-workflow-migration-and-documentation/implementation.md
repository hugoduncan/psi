- Design review: new actionable ambiguity — the task says to migrate a "small set of representative workflows" and cover invoke/session/delegate plus shared refs/outputs/yields "where practical", but it does not identify the minimum authoritative example set or whether coverage may be split across multiple workflows/docs/tests versus needing one coherent executable example. That leaves acceptance and step completion open to conflicting interpretations.
- Design review: new actionable ambiguity — the task requires docs that make the preferred target authoring path obvious, but it does not say which documentation surface is authoritative for that guidance (`README.md`, a workflow guide under `doc/`, `doc/workflow-grammar*.md`, or workflow-local examples). Without an explicit primary doc target, implementers can satisfy the task with materially different documentation placements.
- Resolved ambiguity follow-up: updated `design.md` and `plan.md` to make `plan-build`, `plan-build-review`, and `gh-bug-triage-modular` the minimum authoritative example set; clarified that coverage may be distributed across that coherent set; and made `doc/workflows.md` the primary authoring guide with workflow-local descriptions / grammar cross-links as secondary doc surfaces.
- Design/plan/steps inconsistency review: new actionable gap — `design.md` and `plan.md` explicitly require the migrated example set/docs to cover shared data references, `outputs`, and `yields` where implemented and teachable, but the primary doc surface `doc/workflows.md` currently teaches only `:session` input/reference/preload flow and does not explain `outputs`/`yields` or explicitly bound them as not yet part of the example-led authoring guidance.
- Follow-up resolved: narrowed the task design/plan/steps claim to the currently taught example-led surfaces (`:session` input/reference/preload plus shared references/context flow), leaving `outputs`/`yields` out of the promised authoring coverage until `doc/workflows.md` teaches them concretely.
- Implementation start: migrated the minimum authoritative example set toward the converged target grammar surface.
  - `plan-build.md` now uses target-authored inline `:session` steps with ordered `:contributions` and prior-step `:yield :text` references.
  - `plan-build-review.md` now uses target-authored inline `:session` steps end-to-end, making the example-led plan → build → review chain read in the preferred converged style.
  - `gh-bug-triage-modular.md` now uses target-authored `:delegate` steps with explicit `:target`, template `:prompt-string`, and ordered `:context` forwarding, including projected transcript context on the final handoff step.
- Compiler follow-on: extended `workflow_file_compiler.clj` so `.psi/workflows/*.md` files can preserve target-authored `{:steps [{:type ...} ...]}` definitions instead of forcing them through the older current-authored multi-step compilation path.
- Documentation update: rewrote `doc/workflows.md` as the canonical example-led authoring guide for this slice, centered on the migrated `plan-build`, `plan-build-review`, and `gh-bug-triage-modular` examples, with concise current → target mapping guidance and explicit boundary notes.
- Test coverage added:
  - `workflow_file_compiler_target_authoring_test.clj` for target-authored file compilation vs legacy current-authored compatibility.
  - updated `workflow_migration_validation_test.clj` to assert `plan-build` and `plan-build-review` now compile as target-authored workflows, while `gh-bug-triage-modular` remains the richer executable current-authored orchestration example.
- Verification/reconciliation outcome:
  - extending the workflow file compiler to accept target-authored `.psi/workflows/*.md` definitions is viable and covered.
  - migrating `plan-build` and `plan-build-review` all the way to checked-in target-authored workflow files is executable today.
  - attempting to migrate `gh-bug-triage-modular` all the way to a checked-in target-authored delegate workflow surfaced a real implementation boundary: current target delegate semantics do not yet provide the exact yield/output surfaces needed to preserve the existing executable bug-triage flow without redesigning downstream callee contracts.
  - resolved by tightening the final implementation to keep `gh-bug-triage-modular` executable in the current-authored surface while making `doc/workflows.md` explicitly teach the delegate-oriented target mapping and call out the boundary rather than implying full end-to-end migration already exists.
