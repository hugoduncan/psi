- Design review: new actionable ambiguity — the task says to migrate a "small set of representative workflows" and cover invoke/session/delegate plus shared refs/outputs/yields "where practical", but it does not identify the minimum authoritative example set or whether coverage may be split across multiple workflows/docs/tests versus needing one coherent executable example. That leaves acceptance and step completion open to conflicting interpretations.
- Design review: new actionable ambiguity — the task requires docs that make the preferred target authoring path obvious, but it does not say which documentation surface is authoritative for that guidance (`README.md`, a workflow guide under `doc/`, `doc/workflow-grammar*.md`, or workflow-local examples). Without an explicit primary doc target, implementers can satisfy the task with materially different documentation placements.
- Resolved ambiguity follow-up: updated `design.md` and `plan.md` to make `plan-build`, `plan-build-review`, and `gh-bug-triage-modular` the minimum authoritative example set; clarified that coverage may be distributed across that coherent set; and made `doc/workflows.md` the primary authoring guide with workflow-local descriptions / grammar cross-links as secondary doc surfaces.
- Design/plan/steps inconsistency review: new actionable gap — `design.md` and `plan.md` explicitly require the migrated example set/docs to cover shared data references, `outputs`, and `yields` where implemented and teachable, but the primary doc surface `doc/workflows.md` currently teaches only `:session` input/reference/preload flow and does not explain `outputs`/`yields` or explicitly bound them as not yet part of the example-led authoring guidance.
- Follow-up resolved: narrowed the task design/plan/steps claim to the currently taught example-led surfaces (`:session` input/reference/preload plus shared references/context flow), leaving `outputs`/`yields` out of the promised authoring coverage until `doc/workflows.md` teaches them concretely.
