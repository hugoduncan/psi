- [x] Resolve the design/plan inconsistency around the mutation execution seam by updating `plan.md` to treat `psi.agent-session.extensions.runtime-eql/run-extension-mutation-in!` as already chosen, and narrow the remaining open design question to the explicit session-targeting override needed so caller-supplied `params :session-id` stays authoritative for cross-session lifecycle mutations.
- [x] Align the compact session-summary implementation approach with the exact v1 field contract: `design.md` requires `:psi.agent-session/context-session-summaries` entries to include `:psi.session-info/updated`, but the currently referenced canonical `context-sessions` projection in `resolvers/session.clj` does not expose any updated timestamp. Decide and record whether the summary attr will extend the shared session-info source/projection to supply `updated` canonically or use another existing canonical source that already carries it, then reflect that choice in `plan.md`/`steps.md` before coding.
