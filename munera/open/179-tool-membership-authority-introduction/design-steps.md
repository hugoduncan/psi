- [x] Add `plan.md` and `steps.md` before execution so this task has an explicit review/update order, implementation approach, and bounded decomposition for the authority-introduction slice.
- [x] Decide and record the concrete authoritative session tool field for this slice. Resolve whether plain exact-name membership (`:tool-ids`) is sufficient, or whether current workflow/prompt narrowing semantics require a richer selection shape; name the field explicitly and state why that shape fully covers current direct mutation and child-session needs.
- [x] Reconcile the new authoritative tool field with the existing session `:active-tools` field. Specify whether `:active-tools` is removed, renamed, retained only as a derived compatibility projection, or merged into the new authority field so session tool authority is singular rather than split across multiple name-based fields.
- [x] Define the interim parent→child tool authority fallback rule for this slice while workflow child-session request/contract/context and `child_session_state` still accept `:tool-defs`. State whether missing child override means inherit parent `:tool-ids` authority first and derive child `:tool-defs` second, and whether explicit compatibility `:tool-defs` child inputs must always be normalized into child `:tool-ids` rather than remaining an alternate authority path.
- [x] Name `:session/add-tool` as a direct tool-authority mutation seam in the plan and decide its `:tool-ids` behavior. Currently it dispatches `:runtime/agent-set-tools` but never updates session `:tool-defs`/`:active-tools` — state whether this handler must also derive/persist `:tool-ids` (and derived `:tool-defs`/`:active-tools`) or whether its runtime-only effect is intentional and `:tool-ids` authority is only set by `:session/set-active-tools`.
  - Resolved: `:session/add-tool` must derive/persist `:tool-ids` and derived fields. Added to design.md "Direct mutation seams" section and plan.md "Direct seams to align" section.
- [x] Name `refresh-active-tools-in!` (`extension_runtime.clj`) and `refresh-active-tools!` (`workflow/bootstrap.clj`) as authority-feeding seams in the plan's "direct seams to align" section, since both merge runtime+registry tools and dispatch `:session/set-active-tools`, making them indirect but critical authority paths.
  - Resolved: Both named in plan.md under new "Authority-feeding seams to verify" section. They flow through `:session/set-active-tools` so inherit correct behavior once the handler is authority-first; require verification during implementation.
- [x] Clarify whether `:active-tools` is a persisted session-state schema field or ephemeral runtime state. It does not appear in `session-state/model.clj` schema, so it may silently disappear on session resume. The design must say whether this slice adds it to the schema as an optional derived field, or accepts that it is ephemeral and documents that `:turn/active-tools` in `prompt_request.clj` may be nil after resume.
  - Resolved: `:active-tools` is ephemeral runtime state. Not in schema, not in any lifecycle `select-keys`. This slice does not promote it. Nil after resume is acceptable because tool bootstrap re-populates it before first prompt. Documented in design.md "`:active-tools` persistence status" section and plan.md "`:active-tools` status" section.
- [x] Enumerate which `session-state/init.clj` lifecycle paths (new-session, resume-session, fork-session) must include `:tool-ids` in their baseline field-copy sets, so the implementation step "session model/init" is unambiguous about scope.
  - Resolved: All three paths (`initialize-new-session-state`, `initialize-resumed-session-state`, `initialize-forked-session-state`) must add `:tool-ids` to their `select-keys`. `model/initial-session` must default `:tool-ids []`. Documented in design.md "Session lifecycle paths requiring `:tool-ids`" section and plan.md.
- [x] Resolve the base-tool registry gap in the derivation rule. Design.md derivation rule says "Start from canonical tool-registry definitions" and "resolving each id against the canonical registry," but base tools (`read`, `bash`, `edit`, `write`, `psi-tool`) are assembled as local tool-def maps in `app-runtime` (`tools/all-tools` + `make-session-scoped-psi-tool`) and never registered in the tool-registry. The plan's handler description says "normalize incoming tool maps → derive `:tool-ids` → persist `:tool-ids` → derive/persist `:tool-defs`" without clarifying the `:tool-defs` derivation source. Decide: (a) this slice requires base tools to be registered in the tool-registry so the derivation rule works as stated, or (b) the derivation rule must be restated to derive `:tool-defs` from the normalized incoming tool maps (with the registry as a secondary/verification source) rather than from registry-only lookup, or (c) the derivation rule is aspirational and compatibility `:tool-defs` in this slice is just the normalized incoming maps persisted alongside `:tool-ids`.
  - Resolved: chose option (b). Restated derivation rule in design.md: `:tool-defs` is materialized from normalized incoming tool maps (containing both base and extension tools), not from registry-only lookup. Base tools are not in the registry in this slice. Registering them via `register-built-in-tool-in!` is deferred. Updated design.md, plan.md, and implementation.md.
