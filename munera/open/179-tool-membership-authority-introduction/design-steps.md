- [ ] Add `plan.md` and `steps.md` before execution so this task has an explicit review/update order, implementation approach, and bounded decomposition for the authority-introduction slice.
- [ ] Decide and record the concrete authoritative session tool field for this slice. Resolve whether plain exact-name membership (`:tool-ids`) is sufficient, or whether current workflow/prompt narrowing semantics require a richer selection shape; name the field explicitly and state why that shape fully covers current direct mutation and child-session needs.
- [ ] Reconcile the new authoritative tool field with the existing session `:active-tools` field. Specify whether `:active-tools` is removed, renamed, retained only as a derived compatibility projection, or merged into the new authority field so session tool authority is singular rather than split across multiple name-based fields.
- [ ] Define the interim parent→child tool authority fallback rule for this slice while workflow child-session request/contract/context and `child_session_state` still accept `:tool-defs`. State whether missing child override means inherit parent `:tool-ids` authority first and derive child `:tool-defs` second, and whether explicit compatibility `:tool-defs` child inputs must always be normalized into child `:tool-ids` rather than remaining an alternate authority path.
