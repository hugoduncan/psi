- 2026-05-25 ψ ambiguity review: found actionable ambiguities — the task lacks required `plan.md`, `steps.md`, and `design-steps.md`, so there is no explicit execution/decomposition surface for the intended authority introduction slice; and `design.md` leaves the authoritative tool session shape undecided (`:tool-ids` vs richer selection) even though current direct mutation and child-session seams (`:session/set-active-tools`, workflow child-session contract/context/child_session_state) require a precise contract now. It also does not specify whether this slice must rename or reconcile the existing `:active-tools` session field that currently persists a set of tool names alongside authoritative-looking `:tool-defs`, leaving authority between those fields ambiguous.
- 2026-05-25 ψ ambiguity review: found one new actionable ambiguity — `plan.md` selects authoritative `:tool-ids` and defers broader workflow/lifecycle migration, but the task does not state the interim parent→child fallback rule while workflow child-session request/contract/context and `child_session_state` still accept `:tool-defs`. The design must say whether child-session state now inherits parent `:tool-ids` when no override is supplied and derives child `:tool-defs` from those ids, and whether explicit child `:tool-defs` inputs in this compatibility slice must also derive/persist child `:tool-ids` rather than bypassing the new authority field.
- 2026-05-25 ψ ambiguity follow-up execution: added missing `steps.md`, resolved singular session tool authority to ordered exact-name `:tool-ids`, classified `:active-tools` as derived compatibility-only alongside derived `:tool-defs`, and defined the interim parent→child rule: inherit parent `:tool-ids` when no child override is provided, and normalize any compatibility `:tool-defs` child override back into child `:tool-ids` rather than allowing a second persisted authority path. Left implementation `steps.md` untouched per instruction.
- 2026-05-25 ψ ambiguity review: found four new actionable ambiguities after reading current code: (1) `:session/add-tool` handler (`session_mutations.clj:511`) is a direct tool-authority mutation seam not named in plan's "direct seams to align" — it adds a tool via runtime effect only, never updates session `:tool-defs`/`:active-tools`/`:tool-ids`, so its authority relationship is undefined; (2) `refresh-active-tools-in!` in `extension_runtime.clj:112` and `refresh-active-tools!` in `workflow/bootstrap.clj:28` are authority-feeding paths that merge runtime+registry tools and dispatch `:session/set-active-tools`, but are not named as seams requiring alignment; (3) `:active-tools` is classified as "retained derived compatibility projection" but it does not appear in `session-state/model.clj` schema and is only written by the `:session/set-active-tools` handler into the session map — the design must clarify whether `:active-tools` is a persisted schema field that survives session persistence/resume or is ephemeral runtime state that may silently disappear on resume; (4) `steps.md` item "Implement authoritative session `:tool-ids` introduction in session model/init" is ambiguous about which `init.clj` lifecycle paths are in scope — new-session, resume-session, and fork-session all copy specific field sets from baseline session data and all three would need `:tool-ids` added, but the task does not enumerate them.
- 2026-05-25 ψ ambiguity follow-up execution: resolved all four ambiguities from the preceding review pass. (1) `:session/add-tool` named as direct mutation seam in design.md and plan.md — must derive/persist `:tool-ids` and derived fields, not just fire runtime effect. (2) `refresh-active-tools-in!` and `refresh-active-tools!` named in plan.md under new "Authority-feeding seams to verify" section — both flow through `:session/set-active-tools` so inherit correct authority once handler migrates; require verification during implementation. (3) `:active-tools` confirmed ephemeral: not in `agent-session-schema`, not in any lifecycle `select-keys`; this slice does not promote it to schema; nil after resume is acceptable because tool bootstrap re-populates before first prompt. (4) All three `init.clj` lifecycle paths enumerated in design.md: `initialize-new-session-state`, `initialize-resumed-session-state`, `initialize-forked-session-state` must add `:tool-ids` to `select-keys`; `model/initial-session` must default `:tool-ids []`.
- 2026-05-25 ψ inconsistency review: found one actionable inconsistency — design.md derivation rule says "Start from canonical tool-registry definitions → Read session `:tool-ids` → Materialize `:tool-defs` by resolving each id against the canonical registry" but base tools (`read`, `bash`, `edit`, `write`, `psi-tool`) are assembled as local tool-def maps in `app-runtime` and never registered in the tool-registry. The plan's handler description says "normalize incoming tool maps → derive `:tool-ids` → persist `:tool-ids` → derive/persist `:tool-defs`" without clarifying whether compatibility `:tool-defs` is re-derived from registry lookup (which would lose base tools) or persisted from the normalized incoming maps (which contradicts the derivation rule's "resolve against canonical registry" wording). The task must decide whether this slice requires registering base tools in the tool-registry so the derivation rule works as stated, or whether the derivation rule must be restated to allow `:tool-defs` derivation from the incoming normalized tool maps rather than from registry-only lookup.
- 2026-05-25 ψ inconsistency follow-up execution: resolved base-tool registry gap. Chose option (b): restated the derivation rule. In this compatibility slice, `:tool-defs` is derived from the normalized incoming tool maps (which already contain both base and extension tools), not from registry-only lookup. This matches how the code actually works — authority-feeding paths (`refresh-active-tools-in!`, `refresh-active-tools!`) merge runtime base tools with registry extension tools before dispatching. Registering base tools in the tool-registry (via the existing but unused `register-built-in-tool-in!`) is deferred to a future slice. Updated design.md derivation rule, plan.md handler description, and plan.md deferred items.
- 2026-05-25 ψ implementation: completed all three steps. Changes:
  - `model.clj`: `:tool-ids` added to `agent-session-schema` as required `[:vector :string]`, and to `initial-session` as `[]`
  - `init.clj`: `:tool-ids` added to `select-keys` in `initialize-new-session-state`, `initialize-resumed-session-state`, `initialize-forked-session-state`
  - `session_mutations.clj`: `:session/set-active-tools` handler now derives `:tool-ids` first, then `:active-tools` and `:tool-defs` as compatibility projections. `:session/add-tool` handler now persists `:tool-ids` and derived fields (previously only dispatched `:runtime/agent-set-tools` effect without updating session state fields).
  - No deviations from design. Both authority-feeding seams (`refresh-active-tools-in!`, `refresh-active-tools!`) verified to flow through `:session/set-active-tools` and inherit correct authority behavior.
  - Tests: `tool_authority_test.clj` (schema, lifecycle), `tool_authority_handlers_test.clj` (handler authority). All pass alongside full unit+extension suites.
