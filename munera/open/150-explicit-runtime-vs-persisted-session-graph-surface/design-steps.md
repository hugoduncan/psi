- [x] Decide whether `:psi.agent-session/context-session-summaries` is intentionally out of scope for the new explicit runtime naming surface or must also gain a preferred `:psi.runtime-session/*` counterpart in this task. `design.md` raises this as a key question, but `plan.md`/`steps.md` do not resolve it, leaving the runtime surface boundary ambiguous.
- [x] Specify the discoverability contract for compatibility attrs during migration. `design.md` says old attrs remain available while new attrs become authoritative and preferred, and `plan.md` says to keep graph introspection clear about the new preferred surface, but neither artifact says whether old root attrs stay in `:psi.graph/root-queryable-attrs`, move out of that preferred discovery surface, or remain listed with some parallel proof/documentation rule. Record the expected introspection behaviour so implementation/tests/docs have one target.
- [x] Identify the concrete internal caller/docs migration set required in-task to make the new names the default in practice. `plan.md` says to update internal callers/docs/examples that would undermine the clarity goal, but does not enumerate the already-referenced prompt, Emacs `/resume`, TUI resume action, graph docs, and resolver/graph tests that currently teach or consume the old attrs; make the minimum required migration set explicit before implementation.
- [x] Remove literal patch markers and the duplicated/renumbered “Prove behaviour and migration compatibility” entry from `plan.md` so the task has one canonical ordered implementation plan consistent with `steps.md` and `implementation.md`.
