# Design follow-up steps

- [x] ARCH: Make turn-augmentation registration/execution explicitly capability- and permission-gated for privileged extensions, rather than leaving manifest/permission declaration as an optional planning question.
- [x] ARCH: Require augmenter input to be a bounded core-owned projection/resolver payload, not raw `ctx`, direct atom access, or hidden runtime handles.
- [x] ARCH: Require augmentation child-session creation to route through the core/session lifecycle with canonical parent turn provenance and recursion-suppression state, not extension-local session management.
- [x] ARCH: Specify deterministic registration/invocation/result ordering for multiple augmenters so the same state and turn event produce the same recorded operations.
- [x] ARCH: Require accepted augmentation operations and diagnostics to be stored as canonical turn-scoped session/journal state consumed by pure request preparation, not only in runtime locals, extension diagnostics, or the bounded dispatch log.
- [x] AMB: Define the exact turn-augmentation input contract field set and names, including which of original user message, session metadata, effective cwd, existing session context, and turn/session ids are provided.
- [x] AMB: Define the precise turn-id creation/reuse point and lifecycle ordering around prompt submission, augmentation recording, and `prompt-prepare-request` so implementers know which event or state owns the turn id before augmentation runs.
- [x] AMB: Define how an append-context-block operation is rendered into the prepared request, including role/layer, formatting, and ordering relative to system prompt layers, extension prompt contributions, runtime metadata, conversation history, and the submitted user message.
- [x] AMB: Choose one unsupported-operation behavior for v1 augmentation results, replacing “rejected or ignored” with a single rule and diagnostic expectation.
- [x] AMB: Define partial-result semantics when one augmenter fails or when one augmenter returns a mixture of valid and invalid operations: whether valid operations from the same provider or other providers are still applied, and how this is diagnosed.
- [x] AMB: Define canceled/stale-result handling as either diagnostic recording or complete ignore, with the exact persisted state/test expectation for each case.
- [x] AMB: Define the minimum augmentation diagnostics schema/status taxonomy and query/summary surface needed to verify success, no-op, failure, unsupported operation, stale/canceled result, and replay behavior.
- [x] AMB: Define replay lookup behavior for augmentation records, including how the pre-turn phase detects replay mode for a turn and what happens when recorded operations are missing, malformed, or refer to a different turn id.
- [x] AMB: Define the concrete turn-augmenter registration API contract, including function name, argument map/function shape, return/error behavior, replacement semantics, and whether registrations are process-global, extension-scoped, or session-scoped before per-session authorization is checked.
- [x] AMB: Define the concrete augmenter result envelope, including how valid empty results, provider diagnostics, child-session ids, and operation vectors are returned so core can distinguish no-op, success, invalid, unsupported, and failed providers without inferring from ad hoc shapes.
- [x] AMB: Define the v1 path for an augmenter to launch helper sessions while still receiving only the bounded turn projection as invocation input; current resolution: use existing extension session APIs and make recursion avoidance extension-owned.
- [x] AMB: Define the concrete prepared-request introspection representation for the inserted augmentation context message, replacing the illustrative `such as {:id :turn/augmentation-context ...}` with the exact layer/message/summary fields tests should assert.
- [x] AMB: Define which session context, if any, is used for registration-time session-available capability checks for extension-scoped/runtime turn-augmenter registrations, including whether extension-load registration is manifest-only with per-session invocation gating or must be tied to a specific current session.
- [x] AMB: Define overall augmentation record status aggregation rules from provider outcomes, including no registered/authorized augmenters, all no-op, all unauthorized, all failed/invalid, and mixed success/no-op/failure cases.
- [x] AMB: Define the canonical augmentation record and prepared-request summary for helper-session recursion avoidance; current resolution: helpers are ordinary sessions and the extension returns an ordinary `:no-op` result for helper sessions it tracks.
- [x] AMB: Define whether core injects/normalizes `:append-context-block` `:source` provenance from the registered provider key and verified child-session ids, or validates extension-supplied `:source`, including the diagnostic for mismatched/spoofed provenance.
- [x] AMB: Define duplicate `:append-context-block` `:id` behavior across accepted operations/providers for one turn: allowed with deterministic order, namespaced/rewritten by core, or rejected with provider diagnostics.
- [x] AMB: Define validation and diagnostic behavior for result envelopes whose `:turn-augmentation/child-session-ids` are malformed; current resolution: v1 treats ids as provider-supplied provenance only and validates shape, not dedicated API origin.
- [x] AMB: Define the concrete bounds and source helper for the `:turn-augmentation/history` tail/snippet projection so tests can assert bounded augmenter input without relying on implementation-default choices.

- [x] AMB: Define whether v1 needs an invocation-scoped child-session API; current resolution: no dedicated augmentation child-session API in v1, use existing extension session APIs.
- [x] AMB: Define cancellation/unblocking semantics for in-flight turn-augmenter invocation under no explicit deadline, including whether parent cancellation interrupts/skips outstanding providers, how pending provider diagnostics are recorded, and when the parent lifecycle resumes.
- [x] AMB: Define what `:session/prompt-prepare-request` must do for a non-suppressed live turn when no canonical augmentation record exists for that `turn-id`: fail closed, record a no-op/missing diagnostic, or reject the lifecycle as invalid.
- [x] AMB: Define how the `workflow run id if any` mentioned in the pre-turn lifecycle is represented in augmentation events, the bounded turn projection, canonical records, diagnostics, or intentionally omitted from each.
- [x] AMB: Define turn-augmenter registration lifecycle on extension reload, unload, disable, or failed reload, including whether existing registrations are removed, replaced, skipped, or diagnosed at invocation.
- [x] AMB: Define helper-session configuration responsibility; current resolution: options/defaults/validation follow existing extension session mutation behavior, not a new augmentation-specific API.
- [x] AMB: Remove the `:append-context-block` `:trust` model for v1; handlers do not supply trust, core does not inject trust, and accepted extension-returned content is injected as turn augmentation context without trust-based rendering or validation.
- [x] AMB: Define how an augmenter drives helper-session work; current resolution: use existing session run mutation as in auto-session-name, and the pre-turn barrier waits for the augmenter handler result rather than owning a dedicated child-run protocol.
- [x] INC: Reconcile stale-result overall status semantics: the cancellation/stale rules say late stale provider diagnostic updates preserve the already-closed overall status, while status aggregation says stale phase closure records overall `:stale`; define whether `:stale` can be a terminal overall record status and, if so, which transition creates it.
- [ ] ARCH: Specify that live turn-augmenter invocation runs at the dispatch effect/runtime boundary, with results recorded through dispatch-owned state updates, rather than inside a pure dispatch handler; arbitrary extension work, helper-session runs, file/model access, and late-result handling must preserve effects-as-data and replay semantics.
- [ ] AMB: Define the canonical extension identity used for turn-augmenter stable keys, deterministic sorting, provenance, diagnostics, and unload/reload cleanup (for example manifest id vs extension path/package id), including normalization and collision behavior.
- [ ] AMB: Define the concrete manifest/effective-permission schema and session-available capability lookup for `:psi.capability/turn-augmentation`, including whether invocation checks a session-level capability flag, an extension-scoped capability entry, or both.
- [ ] AMB: Define the machine-readable provider diagnostic schema for validation failures, including where reason keywords such as `:invalid-child-session-ids` and `:provenance-mismatch` are stored/exposed and precedence when one provider result has multiple failure reasons.
- [ ] AMB: Define the concrete resolver/EQL surface for latest/addressed turn-augmentation summaries, including attribute names/query shape for session-id, turn-id, workflow-run-id, overall status, accepted operation count, provider statuses, child-session ids, replay status, and diagnostic reasons.
- [ ] AMB: Define the minimal `extensions/context-manager` v1 augmentation behavior used to demonstrate the mechanism, including when it returns `:no-op` versus an `:append-context-block`, the block id/title/content source, and whether helper sessions are exercised or intentionally not used in the scaffold.
