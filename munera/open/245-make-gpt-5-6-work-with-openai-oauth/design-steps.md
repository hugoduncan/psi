# Design follow-up steps

- [ ] Resolve ambiguity: specify the evidence/source of truth that determines the correct OAuth runtime policy for `gpt-5.6` (supported Codex backend id vs different supported transport) so implementation does not have to guess between catalog aliasing, transport rerouting, or another policy.
