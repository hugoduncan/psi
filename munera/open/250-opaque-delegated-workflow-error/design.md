# Opaque "Delegated workflow failed" error message

> Auto-generated by the context-manager post-turn tooling-friction analyzer (task 239). No human review has occurred yet — verify before acting.

## Friction

The agent receives a generic `:error "Delegated workflow failed"` message with `:result nil` when a delegated workflow fails. This provides no insight into *why* the delegation failed (e.g., tool error, timeout, invalid contract), forcing the agent to guess or re-run blindly. This is a noisy/slow feedback loop caused by insufficient error propagation in the tooling.

## Evidence

Turns 1 and 2 show `:error "Delegated workflow failed"` with no underlying cause details.

## Suggested change

Modify the delegation runner/tool to propagate the specific error message or stack trace from the delegated context into the parent run's `:error` field, rather than swallowing it behind a generic string.
