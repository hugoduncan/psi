- Review: ambiguity — task does not yet fix whether runtime-owned semantic validation beyond raw Malli shape belongs in this slice (for example prior-step-only refs, `:on`/`:judge` coupling, or yield/output-key cross-reference checks); decide boundary and encode it in design/tests before implementation.
- Decision: canonical home for normalized IR validation should be a dedicated runtime-owned namespace under `components/agent-session/src/psi/agent_session/`, adjacent to but separate from legacy `workflow_model.clj`, so the compiled IR boundary does not blur with current definition/run state schemas.
- Decision: this slice should own Malli structural schema plus a minimal semantic-validation pass for IR-intrinsic invariants only: prior-step-only refs, `:on` requires `:judge`, and `:yields`/source-ref output-key cross-reference checks against step-local `:outputs`. Broader resolution/execution semantics remain deferred.
- Review: inconsistency — task artifacts mark the validation-boundary decision as settled (`steps.md` checked off and `implementation.md` records the decision), but the repo still has no `workflow_ir` runtime namespace or focused tests implementing/proving that boundary; current code/tests remain on legacy `workflow_model` only, so task progress is overstated relative to observed implementation.
- Implemented: added runtime-owned `components/agent-session/src/psi/agent_session/workflow_ir.clj` as the canonical normalized IR validation boundary, separate from legacy `workflow_model.clj`.
- Implemented: encoded Malli structural schemas for top-level IR, tagged `:invoke`/`:session`/`:delegate` steps, shared source refs/specs, contributions, outputs, yields, judges, routing, and explicitly optional `:compat` metadata.
- Implemented: added minimal semantic validation in `workflow_ir.clj` for the decided slice-owned invariants only — prior-step-only refs, `:on` requires `:judge`, and source-ref cross-checks against declared prior-step outputs/yield fields.
- Proof: added `components/agent-session/test/psi/agent_session/workflow_ir_test.clj` covering representative valid IR, representative malformed structural shapes, and focused semantic-boundary failures for non-prior refs, missing output keys, missing yield fields, and `:on` without `:judge`.
- Verification: `clojure -M:test --focus psi.agent-session.workflow-ir-test` -> `3 tests, 41 assertions, 0 failures`.
- Review: ambiguity — `doc/workflow-ir.md` says default yields are supplied during normalization when authored input omits `:yields`, but this task leaves unresolved whether the runtime-owned IR validation boundary should require compilers to materialize those defaults before validation or should accept omitted `:yields` and apply defaults locally; make one boundary authoritative and prove it with focused tests.
- Review: ambiguity — `doc/workflow-ir.md` says a session step's canonical first-cut output is `:final-llm-reply`, yet the schema permits session steps with `{:type :text :text :final-llm-reply}` yields and no declared `:outputs` entry for `:final-llm-reply`; decide whether this slice requires declared output-key presence for local yield references as well as cross-step refs, and encode that invariant or document the intentional exception.
- Review: ambiguity — `doc/workflow-ir.md` defines `:steps` as `[ir-step+]`, but the implemented top-level schema accepts an empty steps vector; decide whether empty workflows are invalid at the IR boundary and tighten schema/tests or explicitly document why emptiness is allowed here.
- Review: ambiguity — the semantic pass enforces that `:on` requires `:judge`, but it does not decide whether a present `:judge` must also have a non-empty `:on` routing table; settle whether a judge without routes is valid normalized IR or an incomplete form that this slice should reject.
- Decision: default `:yields` are compiler-owned normalization, not validator-owned fill-in. The runtime-owned IR boundary now rejects missing `:yields` with `:missing-yields` so compiled IR must arrive fully normalized.
- Decision: local output-key-bearing yields (`:data`/`:text`) must reference a declared key in the same step's `:outputs` map. The validator now rejects missing local output declarations with `:missing-local-yield-output-key`.
- Decision: normalized workflow IR must contain at least one step. The top-level Malli schema now requires `[:steps [:vector {:min 1} ir-step-schema]]`, and empty workflows are structurally invalid.
- Decision: normalized IR requires symmetric judge/routing presence. `:on` without `:judge` remains invalid, and a present `:judge` now also requires a non-empty `:on` routing table, reported as `:judge-without-routing`.
- Proof: updated `components/agent-session/test/psi/agent_session/workflow_ir_test.clj` with focused tests for missing `:yields`, missing local yield output declaration, empty `:steps`, and `:judge` without routing.
- Verification: `clojure -M:test --focus psi.agent-session.workflow-ir-test` -> `3 tests, 48 assertions, 0 failures`.
- Review: no new actionable inconsistency feedback found on this pass — task artifacts, `doc/workflow-ir.md`, `components/agent-session/src/psi/agent_session/workflow_ir.clj`, and `components/agent-session/test/psi/agent_session/workflow_ir_test.clj` are currently aligned on the validation boundary, required `:yields`, non-empty `:steps`, symmetric `:judge`/`:on` presence, and local/cross-step output-reference rules.
- Review: no new actionable ambiguity feedback found on this pass — `design.md`, `plan.md`, `steps.md`, `implementation.md`, `doc/workflow-ir.md`, and the focused `workflow_ir` code/tests remain aligned, so no additional follow-up items were added.
- Execution: reviewed the preloaded ambiguity-review result, re-checked `steps.md`, `implementation.md`, `design.md`, and `plan.md`, and confirmed there are no newly added unchecked follow-up items remaining for task `078`.
- Verification: reran `clojure -M:test --focus psi.agent-session.workflow-ir-test` -> `3 tests, 48 assertions, 0 failures`.
- Review: no new actionable inconsistency feedback found on this pass — `design.md`, `plan.md`, `steps.md`, `implementation.md`, `doc/workflow-ir.md`, `components/agent-session/src/psi/agent_session/workflow_ir.clj`, and `components/agent-session/test/psi/agent_session/workflow_ir_test.clj` remain aligned on scope, validation boundary, required normalized `:yields`, non-empty `:steps`, symmetric `:judge`/`:on` presence, and the focused proof surface.
- Review: no new actionable ambiguity feedback found on this pass — reread task artifacts plus `doc/workflow-ir.md`, `components/agent-session/src/psi/agent_session/workflow_ir.clj`, and `components/agent-session/test/psi/agent_session/workflow_ir_test.clj`; verified the focused workflow IR test still passes (`clojure -M:test --focus psi.agent-session.workflow-ir-test` -> `3 tests, 48 assertions, 0 failures`); no additional follow-up steps were added.
