# Design follow-up steps

- [x] Clarify whether native structured-output JSON `null` is in scope and must be preserved as `:payload nil` without `:parse-error? true`, since `parse-json-value` currently returns `{:parsed? true :payload nil}` but provider result code that gates on payload truthiness would still treat nil as absent.
- [x] Specify whether structured-output judge retries must call `execute-judge-turn!` with the same structured-output opts/schema on each retry, or intentionally retry as plain text; acceptance says retry the structured-output code path, but current plain-text retry feedback shape does not state how provider-native structured-output options are retained.
- [x] Reconcile the `parse-json-object` constraint with current code: after the three in-scope provider result-extraction call sites are changed, grep shows no remaining non-helper call sites, so design should not claim it is still used elsewhere for non-structured-output purposes unless a referenced remaining use exists.
- [x] Reconcile the minimal-fix constraint with the secondary workflow-judge retry-loop fix: the design currently says only provider result-extraction sites change, but acceptance/scope also requires changing `psi.agent-session.workflow-judge`.
- [x] Reconcile the non-object judge enum value type in `design.md`: referenced `psi.workflow-runtime.structured-output-schemas/judge-routing-result-schema` and existing tests use string enum values (`[:enum "REPEAT" "DONE"]`, value `"DONE"`), but the design examples/acceptance mention keyword enum values (`[:enum :REPEAT :DONE]`, value `:DONE`).
- [x] Reconcile native-only wording with referenced provider code: the three `parse-json-object` extraction sites are shared by provider-native and prompted-JSON structured-output result emission, so `design.md` should explicitly state whether non-object `:payload` preservation applies to prompted-JSON results at those same sites or only to native results despite the shared implementation seam.
- [x] Reconcile prompted-JSON fallback instructions with non-object structured-output schemas: `psi.ai.structured-output/json-only-instruction` currently says “Return exactly one JSON object,” but `design.md` requires prompted-JSON result handling for bare string-enum and other non-object JSON Schema outputs at the shared provider extraction sites.
- [ ] Reconcile the broad provider structured-output/null preservation requirement with `psi.ai.providers.openai.codex-structured-output/structured-output-result`: either explicitly include Codex in scope or state why that existing provider structured-output result surface is out of scope despite also needing parse-success checks that preserve `:payload nil`.
