# Implementation notes

2026-05-30 — Design ambiguity review: found two actionable ambiguities: whether JSON `null` must be preserved as `:payload nil`, and whether structured judge retries must re-send the structured-output opts/schema on every retry.

2026-05-30 — Ambiguity follow-up: clarified design that JSON `null` is a valid native structured-output payload and must be preserved as present `:payload nil` without parse error; clarified that structured-output judge retries must pass the original structured-output opts/schema to `execute-judge-turn!` on every retry. Marked both ambiguity design-steps complete.

2026-05-30 — Design inconsistency review: found two actionable inconsistencies: design says `parse-json-object` is still used elsewhere for non-structured-output purposes, but referenced code shows all current call sites are structured-output result extraction sites in scope; design also says the fix is minimal and only provider result-extraction sites change while the same design requires a workflow-judge retry-loop change.

2026-05-30 — Inconsistency follow-up: reconciled the `parse-json-object` constraint with current grep evidence by retaining the helper as an object-only API while no remaining non-helper call sites are expected after the three provider extraction sites move. Reconciled the minimal-fix constraint by defining minimality across both root-cause seams: three provider extraction sites plus only the workflow-judge structured-output validation-failure retry path. Marked both inconsistency design-steps complete.

2026-05-30 — Design ambiguity review: re-read design and referenced structured-output/provider/judge code. No new actionable ambiguities found; existing ambiguity follow-ups for JSON null preservation and structured retry opts retention remain resolved.

2026-05-30 — Ambiguity follow-up execution: re-read `design-steps.md`; there are no newly added unchecked ambiguity follow-up items to execute, so `design.md` needed no change and no blocking design-step remains.

2026-05-30 — Design inconsistency review: found one new actionable inconsistency: the design's enum-schema examples/acceptance use keyword enum values (`[:enum :REPEAT :DONE]` and expected `:DONE`), while the referenced reusable judge-routing schema/tests use string enum values (`[:enum "REPEAT" "DONE"]` and expected `"DONE"`).

2026-05-30 — Inconsistency follow-up: reconciled the non-object judge enum value type with referenced runtime schema/tests. `design.md` now names the judge routing schema as `[:enum "REPEAT" "DONE"]` and expects `:value "DONE"`, matching `psi.workflow-runtime.structured-output-schemas/judge-routing-result-schema` and existing tests. Marked the design-step complete.

2026-05-30 — Design ambiguity review: re-read current design after enum reconciliation plus referenced provider extraction, structured-output envelope, and workflow-judge retry code. No new actionable ambiguities found.

2026-05-30 — Ambiguity follow-up execution: re-read `design-steps.md` after the preceding ambiguity-review pass. No newly added unchecked ambiguity items are present; no `design.md` changes were needed and no blocking ambiguity follow-up remains.

2026-05-30 — Design inconsistency review: found one new actionable inconsistency: `design.md` describes the three provider extraction edits as native structured-output payload sites and frames the intent around provider-native output, but referenced Anthropic/OpenAI code shows the same `parse-json-object` extraction helpers also emit prompted-JSON structured-output results at those call sites.

2026-05-30 — Inconsistency follow-up: reconciled native-only wording with referenced provider code. `design.md` now states that the three provider structured-output extraction sites are shared seams and non-object `:payload` preservation applies to both provider-native and prompted-JSON structured-output results emitted by those sites. Marked the design-step complete.

2026-05-30 — Design ambiguity review: re-read current design after shared provider extraction seam clarification plus referenced provider extraction, structured-output envelope, schema, and workflow-judge retry code. No new actionable ambiguities found; existing ambiguity follow-ups remain resolved and `design-steps.md` needs no new unchecked items.

2026-05-30 — Ambiguity follow-up execution: re-read `design-steps.md` after the preceding ambiguity-review pass. No newly added unchecked ambiguity follow-up items are present; no `design.md` changes were needed and no blocking ambiguity follow-up remains.

2026-05-30 — Design inconsistency review: found one new actionable inconsistency: `design.md` now requires non-object payload preservation for prompted-JSON structured-output results at the shared provider extraction sites, but referenced `psi.ai.structured-output/json-only-instruction` still tells prompted-JSON models to “Return exactly one JSON object,” which conflicts with bare string-enum/non-object schemas.

2026-05-30 — Inconsistency follow-up: reconciled prompted-JSON fallback instructions with non-object structured-output schemas. `design.md` now includes `json-only-instruction` wording in scope, requires prompted-JSON instructions to request exactly one JSON value matching the supplied JSON Schema rather than exactly one JSON object, and extends acceptance/minimality wording accordingly. Marked the design-step complete.

2026-05-30 — Design ambiguity review: re-read current design plus referenced provider extraction helpers, prompted-JSON instruction helper, workflow structured-output envelope/schema code, and workflow-judge retry seam. No new actionable ambiguities found; existing ambiguity follow-ups remain resolved and `design-steps.md` needs no new unchecked items.

2026-05-30 — Ambiguity follow-up execution: re-read `design-steps.md` after the preceding ambiguity-review pass. No newly added unchecked ambiguity follow-up items are present, so no `design.md` change was needed and no blocking ambiguity design-step remains.

2026-05-30 — Design inconsistency review: found one new actionable inconsistency: `design.md` says any valid JSON value from the provider structured-output result surface, including `null`, must be preserved, but the affected-file/scope list excludes the existing Codex structured-output result helper, which is also a provider structured-output result surface and currently gates parse success on `:payload` truthiness.

2026-05-30 — Inconsistency follow-up: reconciled the provider structured-output/null preservation requirement with the existing OpenAI Codex structured-output result helper. `design.md` now explicitly includes `psi.ai.providers.openai.codex-structured-output/structured-output-result` in scope for the shared provider result invariant, notes that it already uses `parse-json-value`, and requires implementation/tests to preserve parse-success checks independent of payload truthiness so JSON `null` remains a present `:payload nil`. Marked the design-step complete.

2026-05-30 — Design ambiguity review: re-reviewed design after Codex scope clarification against referenced provider result helpers, prompted-JSON instruction wording, workflow structured-output envelope/schema code, and workflow-judge retry seam. No new actionable ambiguities found; `design-steps.md` already contains only completed ambiguity/inconsistency follow-ups.

2026-05-30 — Ambiguity follow-up execution: re-read `design-steps.md` after the latest ambiguity-review pass (`dedb3e2b`). No newly added unchecked ambiguity follow-up items are present; `design.md` needed no changes and no blocking ambiguity design-step remains.

2026-05-30 — Design inconsistency review: re-read current design against referenced provider result helpers, prompted-JSON instruction helper, workflow structured-output envelope/schema code, and workflow-judge retry seam. No new actionable inconsistencies found; existing inconsistency follow-ups are already captured and checked in `design-steps.md`.

2026-05-30 — Inconsistency follow-up execution: re-read `design-steps.md` after the preceding inconsistency-review pass. No newly added unchecked inconsistency follow-up items are present; `design.md` needed no changes and no blocking inconsistency design-step remains.

2026-05-30 — Plan ambiguity review: found one actionable ambiguity in the OpenAI chat-completions provider-result slice. The plan/steps require switching parsed payload extraction to any JSON value, but referenced OpenAI code currently stores `:raw-payload` as the parsed payload while Anthropic/Codex store raw JSON text; the plan does not say whether OpenAI should preserve that legacy parsed `:raw-payload` shape or normalize it to raw text for scalar/array/null outputs.

2026-05-30 — Ambiguity follow-up: clarified the plan/steps for OpenAI chat-completions `:raw-payload` semantics after switching structured-output extraction to `parse-json-value`. OpenAI chat-completions should preserve `:raw-payload` as the raw JSON response text, matching Anthropic and Codex, while parsed scalar/array/object/boolean/number/`null` values live only in `:payload`. Marked the ambiguity design-step complete.

2026-05-30 — Plan inconsistency review: found two new actionable inconsistencies. First, `plan.md` says OpenAI chat-completions regression coverage includes object payloads, while `steps.md` only lists string/number/boolean/array/`null` for the OpenAI provider-native and prompted-JSON test items. Second, the design/plan identify two OpenAI chat-completions result sites (streaming and non-streaming) that must preserve identical semantics, but the OpenAI regression-test steps do not say whether both result sites are covered for provider-native and prompted-JSON strategies.

2026-05-30 — Inconsistency follow-up: reconciled OpenAI chat-completions regression-test coverage between `plan.md` and `steps.md`. `steps.md` now explicitly requires provider-native and prompted-JSON tests for object payloads in addition to string, number, boolean, array, and `null`, matching the plan's payload matrix. It also explicitly requires coverage for both streaming and non-streaming structured-output result paths for provider-native and prompted-JSON strategies, or a terse recorded reason if an existing public seam covers both equivalently. Marked both inconsistency design-steps complete.

2026-05-30 — Plan ambiguity review: found one new actionable ambiguity: the plan/steps require Anthropic provider result extraction to preserve every valid JSON value via `parse-json-value`, but Slice 2 only calls for an Anthropic bare-string regression while existing referenced Anthropic tests currently classify array/non-object JSON as parse failure. The implementation plan should clarify whether Anthropic regression coverage must update valid array/number/boolean/null expectations too, or whether bare string is intentionally sufficient.

2026-05-30 — Ambiguity follow-up: clarified Anthropic provider regression coverage for non-object JSON values. `plan.md` now requires Anthropic JSON Schema output payload extraction coverage for string, number, boolean, array, object, and `null`, including present `:payload nil`, and says existing Anthropic parse-failure expectations should treat only invalid JSON as parse failure. `steps.md` now mirrors that coverage and requires updating prior non-object parse-failure expectations. Marked the ambiguity design-step complete.

2026-05-30 — Plan inconsistency review: re-read `design.md`, `plan.md`, `steps.md`, `implementation.md`, and referenced provider/judge code/tests. No new actionable inconsistencies found; existing completed follow-ups already reconcile provider payload matrices, OpenAI `:raw-payload`, streaming/non-streaming coverage, prompted-JSON wording, Anthropic parse-failure expectations, Codex `null` preservation, and structured judge retry opts retention.

2026-05-30 — Inconsistency follow-up execution: re-read `design-steps.md` after preceding inconsistency-review commit `ac10e100`. No newly added unchecked inconsistency follow-up items are present; all existing design follow-up steps predate that review pass and remain checked, so no task artifact changes were required beyond this note and no blocking inconsistency item remains.

2026-05-30 — Plan ambiguity review: found one new actionable ambiguity: `design.md` says the Anthropic structured-output result helper is a shared extraction seam for provider-native JSON Schema output and prompted-JSON fallback, but `plan.md`/`steps.md` only require the expanded Anthropic non-object payload regression matrix for JSON Schema output. The implementation plan should clarify whether Anthropic prompted-JSON fallback also needs non-object payload coverage, or explicitly record why helper-level/JSON Schema coverage is sufficient for that shared seam.

2026-05-30 — Ambiguity follow-up: clarified Anthropic prompted-JSON fallback regression coverage for non-object JSON values. Because Anthropic `structured-output-result` is a shared seam for JSON Schema output and prompted-JSON fallback, `plan.md` and `steps.md` now require the same string/number/boolean/array/object/`null` payload matrix for Anthropic prompted-JSON fallback, including present `:payload nil`. Marked the design-step complete.

2026-05-30 — Plan inconsistency review: re-read `design.md`, `plan.md`, `steps.md`, `design-steps.md`, `implementation.md`, and referenced structured-output/provider/workflow-judge code/tests after the Anthropic prompted-JSON fallback coverage clarification. No new actionable inconsistencies found; `plan.md` and `steps.md` now consistently cover provider-native and prompted-JSON payload matrices for Anthropic/OpenAI, OpenAI streaming/non-streaming result paths and raw-payload semantics, Codex `null` preservation, prompted-JSON instruction wording, and structured-output judge retry behavior/opts retention.

2026-05-30 — Plan ambiguity review: re-read `design.md`, `plan.md`, `steps.md`, `design-steps.md`, `implementation.md`, and referenced structured-output/provider/workflow-judge code/test seams. No new actionable ambiguities found; the plan and steps already specify provider-native and prompted-JSON payload matrices, OpenAI raw-payload semantics and streaming/non-streaming coverage, Anthropic JSON Schema and prompted-JSON fallback coverage, Codex `null` preservation, prompted-JSON instruction wording, and structured-output judge retry opts retention.

2026-05-30 — Ambiguity follow-up execution: re-read `design-steps.md` after preceding ambiguity-review commit `3f847108`. No newly added unchecked ambiguity follow-up items are present; all existing design follow-up steps predate that review pass and are already checked. No `plan.md` or `steps.md` changes were needed, and no blocking ambiguity item remains.

2026-05-30 — Plan inconsistency review: found one new actionable inconsistency: `plan.md`/`steps.md` require structured-output judge validation failures to retry until success or exhaustion, but the referenced existing workflow-judge test `execute-judge-invalid-structured-output-fails-locally-test` still states invalid structured output fails “without ... retry” and asserts only the initial prompt call, conflicting with the planned retry behavior/tests.

2026-05-30 — Inconsistency follow-up: reconciled the existing workflow-judge invalid structured-output test with planned retry behavior. `plan.md` now explicitly requires updating `execute-judge-invalid-structured-output-fails-locally-test` so it no longer expects immediate no-retry failure, and `steps.md` now calls out retargeting that test to retry-exhaustion coverage or replacing it with equivalent exhausted-retry coverage. Marked the design-step complete.

2026-05-30 — Plan ambiguity review: re-read `plan.md`, `steps.md`, `design.md`, `design-steps.md`, `implementation.md`, and referenced structured-output/provider/workflow-judge code/tests after the invalid structured-output retry-test reconciliation. No new actionable ambiguities found; the plan/steps already specify provider JSON-value extraction, raw-payload semantics, provider-native/prompted-JSON test matrices, streaming/non-streaming OpenAI coverage, prompted-JSON wording, Codex `null` preservation, structured-output retry opts retention, retry success/exhaustion behavior, unsupported-output no-retry behavior, and retargeting the existing invalid structured-output test.

2026-05-30 — Ambiguity follow-up execution: re-read `design-steps.md` after preceding ambiguity-review commit `8b63df2f`. No newly added unchecked ambiguity follow-up items are present; all existing design follow-up steps predate that review pass and are already checked. No `plan.md` or `steps.md` changes were needed, and no blocking ambiguity item remains.

2026-05-30 — Plan inconsistency review: re-read `design.md`, `plan.md`, `steps.md`, `design-steps.md`, `implementation.md`, plus referenced structured-output provider helpers, prompted-JSON instruction helper, workflow structured-output envelope code, and workflow-judge invalid structured-output tests. No new actionable inconsistencies found; no unchecked `design-steps.md` items were added.

2026-05-30 — Inconsistency follow-up execution: re-read `design-steps.md` after preceding inconsistency-review commit `67a80481`. No newly added unchecked inconsistency follow-up items are present; all existing design follow-up steps predate that review pass and are already checked. No `plan.md` or `steps.md` changes were needed, and no blocking inconsistency item remains.

2026-05-30 — Plan ambiguity review: re-read `plan.md`, `steps.md`, `design.md`, `design-steps.md`, `implementation.md`, and referenced provider/structured-output/workflow-judge code plus existing Anthropic/OpenAI/workflow-judge tests. No new actionable ambiguities found; `design-steps.md` already contains only checked follow-ups, and the plan/steps remain explicit about provider JSON-value extraction, `:raw-payload` semantics, JSON `null` key presence, provider-native/prompted-JSON test matrices, OpenAI streaming/non-streaming coverage, prompted-JSON wording, Codex `null` preservation, structured-output retry opts retention, retry success/exhaustion behavior, unsupported-output no-retry behavior, and retargeting the existing invalid structured-output test.

2026-05-30 — Ambiguity follow-up execution: re-read `design-steps.md` after preceding ambiguity-review commit `9f880433`. No newly added unchecked ambiguity follow-up items are present; all existing design follow-up steps predate that review pass and are already checked. No `plan.md` or `steps.md` changes were needed, and no blocking ambiguity item remains.

2026-05-30 — Plan inconsistency review: re-read `design.md`, `plan.md`, `steps.md`, `design-steps.md`, `implementation.md`, and referenced structured-output/provider/workflow-judge code/tests. No new actionable inconsistencies found; current plan/steps remain aligned on provider JSON-value extraction, prompted-JSON JSON-value wording, Anthropic/OpenAI/Codex payload/null coverage, OpenAI raw-payload and streaming/non-streaming coverage, structured-output judge retry opts retention, retry exhaustion, unsupported-output no-retry behavior, and retargeting the existing invalid structured-output test.

2026-05-30 — Inconsistency follow-up execution: re-read `design-steps.md` after preceding inconsistency-review commit `1ecf6068`. No newly added unchecked inconsistency follow-up items are present; all existing design follow-up steps predate that review pass and are already checked. No `plan.md` or `steps.md` changes were needed, and no blocking inconsistency item remains.

2026-05-30 — Plan ambiguity review: re-read `plan.md`, `steps.md`, `design.md`, `design-steps.md`, `implementation.md`, and referenced structured-output/provider/workflow-judge code/tests. No new actionable ambiguities found; `design-steps.md` already contains only checked follow-ups, and the plan/steps remain explicit about provider JSON-value extraction, JSON `null` key presence, OpenAI raw-payload semantics, Anthropic/OpenAI provider-native and prompted-JSON matrices, OpenAI streaming/non-streaming coverage, Codex `null` preservation, prompted-JSON wording, structured-output retry opts retention, retry success/exhaustion, unsupported-output no-retry behavior, and retargeting the existing invalid structured-output test.

2026-05-30 — Ambiguity follow-up execution: re-read `design-steps.md` after preceding ambiguity-review commit `3e2e60ac`. No newly added unchecked ambiguity follow-up items are present; all existing design follow-up steps predate that review pass and are already checked. No `plan.md` or `steps.md` changes were needed, and no blocking ambiguity item remains.

2026-05-30 — Plan inconsistency review: found one new actionable inconsistency across task files. `plan.md` and `steps.md` require OpenAI chat-completions provider-native and prompted-JSON regression coverage for object payloads, but `design.md` acceptance criterion 3 omits object from the OpenAI payload list despite the surrounding design scope requiring any valid JSON value. Added an unchecked design follow-up to reconcile the acceptance criterion with the implementation plan.

2026-05-30 — Inconsistency follow-up: reconciled OpenAI object payload coverage between the implementation plan and `design.md` acceptance criterion 3. AC3 now includes object payloads alongside string, number, boolean, array, and `null`, matching `plan.md`/`steps.md` and the broader any-valid-JSON-value scope. Marked the design-step complete.

2026-05-30 — Plan ambiguity review: re-read `plan.md`, `steps.md`, `design.md`, `design-steps.md`, `implementation.md`, plus referenced structured-output provider helpers, prompted-JSON instruction helper, workflow structured-output envelope code/tests, and workflow-judge retry tests. No new actionable ambiguities found; current plan/steps remain explicit about provider JSON-value extraction, `:payload nil` key presence, OpenAI `:raw-payload` raw-text semantics, provider-native and prompted-JSON payload matrices for Anthropic/OpenAI, OpenAI streaming/non-streaming coverage, Codex `null` preservation, prompted-JSON wording, structured-output retry opts retention, retry success/exhaustion, unsupported-output no-retry behavior, and retargeting the existing invalid structured-output test.

2026-05-30 — Ambiguity follow-up execution: re-read `design-steps.md` after preceding ambiguity-review commit `8ef272d6`. No newly added unchecked ambiguity follow-up items are present; all existing design follow-up steps predate that review pass and are already checked. No `plan.md` or `steps.md` changes were needed, and no blocking ambiguity item remains.

2026-05-30 — Plan inconsistency review: re-read `design.md`, `plan.md`, `steps.md`, `design-steps.md`, `implementation.md`, and referenced structured-output/provider/workflow-judge source/test seams. No new actionable inconsistencies found; the task files remain aligned on any-valid-JSON-value provider extraction, prompted-JSON JSON-value wording, OpenAI raw-text `:raw-payload`, Anthropic/OpenAI provider-native and prompted-JSON payload matrices, Codex `null` preservation, structured-output retry opts retention, retry success/exhaustion, unsupported-output no-retry behavior, and retargeting the existing invalid structured-output test.

2026-05-30 — Inconsistency follow-up execution: re-read `design-steps.md` after preceding inconsistency-review commit `955cf3bc`. No newly added unchecked inconsistency follow-up items are present; all existing design follow-up steps predate that review pass and are already checked. No `plan.md` or `steps.md` changes were needed, and no blocking inconsistency item remains.

2026-05-30 — Plan ambiguity review: re-read `plan.md`, `steps.md`, `design.md`, `design-steps.md`, `implementation.md`, and referenced structured-output/provider/workflow-judge source and tests. No new actionable ambiguities found; the plan/steps remain explicit about provider JSON-value extraction, JSON `null` as present `:payload nil`, OpenAI raw-text `:raw-payload`, Anthropic/OpenAI provider-native and prompted-JSON payload matrices, OpenAI streaming/non-streaming coverage, Codex `null` preservation, prompted-JSON JSON-value wording, structured-output retry opts retention, retry success/exhaustion, unsupported-output no-retry behavior, and retargeting the existing invalid structured-output test.

2026-05-30 — Ambiguity follow-up execution: re-read `design-steps.md` after preceding ambiguity-review commit `3425d38e`. No newly added unchecked ambiguity follow-up items are present; all existing design follow-up steps predate that review pass and remain checked. No `plan.md` or `steps.md` changes were needed, and no blocking ambiguity item remains.

2026-05-30 — Plan inconsistency review: re-read `design.md`, `plan.md`, `steps.md`, `design-steps.md`, `implementation.md`, and referenced structured-output/provider/workflow-judge source and tests. No new actionable inconsistencies found; the task files remain aligned on any-valid-JSON-value provider extraction, `:payload nil` key presence, Anthropic/OpenAI provider-native and prompted-JSON payload matrices, OpenAI raw-text `:raw-payload`, OpenAI streaming/non-streaming coverage, Codex `null` preservation, prompted-JSON JSON-value wording, structured-output judge retry opts retention, retry success/exhaustion, unsupported-output no-retry behavior, and retargeting the existing invalid structured-output test.

2026-05-30 — Inconsistency follow-up execution: re-read `design-steps.md` after preceding inconsistency-review commit `7dc82279`. No newly added unchecked inconsistency follow-up items are present; all existing design follow-up steps predate that review pass and remain checked. No `plan.md` or `steps.md` changes were needed, and no blocking inconsistency item remains.

2026-05-30 — Implementation pass: completed provider JSON-value extraction and structured-output judge retry slices. Anthropic and OpenAI chat-completions structured-output result seams now use `parse-json-value` and preserve successful JSON `null` as present `:payload nil`; OpenAI chat-completions `:raw-payload` is raw response text; Codex now checks parse success via `:parsed?`. Prompted-JSON fallback wording now requests exactly one JSON value. Workflow judge structured-output validation failures now retry up to `max-judge-retries`, preserving the original structured-output opts/schema on retry; unsupported structured output still fails immediately. Added provider/helper regression coverage for Anthropic JSON Schema + prompted-JSON payload matrices, OpenAI chat-completions provider-native + prompted-JSON payload matrices, Codex `null`, prompted-JSON instruction wording, and workflow-judge retry success/exhaustion behavior. Updated the changelog because the fix is user-visible. Verification: focused `clojure -M:test --focus psi.ai.providers.anthropic-structured-output-test --focus psi.ai.providers.openai-structured-output-test --focus psi.agent-session.workflow-judge-test --focus psi.workflow-runtime.structured-output-test` passed (47 tests, 356 assertions); targeted `clj-kondo` passed; `bb test` passed.

2026-05-30 — Independent completion verification pass: re-read task artifacts and confirmed all planned implementation steps are already checked. Re-ran focused structured-output/provider/workflow-judge tests (`clojure -M:test --focus psi.ai.providers.anthropic-structured-output-test --focus psi.ai.providers.openai-structured-output-test --focus psi.agent-session.workflow-judge-test --focus psi.workflow-runtime.structured-output-test`) successfully: 47 tests, 356 assertions, 0 failures. Re-ran targeted `clj-kondo` over affected source/test paths; it reported no errors or warnings, only unrelated info-level findings in existing agent-session tests. Re-ran full `bb test`; all tests passed. No additional implementation work remains for task 190.

2026-05-30 — Independent implementation pass verification: task artifacts already showed all implementation and verification steps complete, with no unchecked follow-up items. Re-ran focused structured-output/provider/workflow-judge tests: `clojure -M:test --focus psi.ai.providers.anthropic-structured-output-test --focus psi.ai.providers.openai-structured-output-test --focus psi.agent-session.workflow-judge-test --focus psi.workflow-runtime.structured-output-test` passed (47 tests, 356 assertions). Re-ran targeted `clj-kondo` over affected AI/agent-session/workflow-runtime source and test paths; no errors or warnings, only pre-existing info-level findings outside this task. Re-ran full `bb test`; all tests passed. No additional concrete implementation work remains for task 190.

2026-05-30 — Independent completion re-verification pass: task artifacts still show every implementation, test, and verification step checked with no unchecked design follow-up items. Re-ran focused structured-output/provider/workflow-judge tests: `clojure -M:test --focus psi.ai.providers.anthropic-structured-output-test --focus psi.ai.providers.openai-structured-output-test --focus psi.agent-session.workflow-judge-test --focus psi.workflow-runtime.structured-output-test` passed (47 tests, 356 assertions). Re-ran targeted `clj-kondo` over affected AI, agent-session, and workflow-runtime source/test paths; no errors or warnings, only existing info-level findings in unrelated agent-session tests. Re-ran full `bb test`; all tests passed. No concrete implementation work remains for task 190.

2026-05-30 — Independent no-op implementation verification pass: re-read task artifacts, confirmed no unchecked task follow-ups remain, and found no concrete implementation slice available. Re-ran focused structured-output/provider/workflow-judge tests: `clojure -M:test --focus psi.ai.providers.anthropic-structured-output-test --focus psi.ai.providers.openai-structured-output-test --focus psi.agent-session.workflow-judge-test --focus psi.workflow-runtime.structured-output-test` passed (47 tests, 356 assertions). Re-ran targeted `clj-kondo` over affected AI, agent-session, and workflow-runtime source/test paths; no errors or warnings, only pre-existing info-level findings in unrelated agent-session tests. Re-ran full `bb test`; all tests passed. Task 190 remains implementation-complete.

2026-05-30 — Independent requested implementation pass: re-read task artifacts (`design.md`, `plan.md`, `steps.md`, `implementation.md`, `design-steps.md`) and confirmed all implementation/test/verification checklist items are complete with no unchecked follow-ups. Re-ran focused structured-output/provider/workflow-judge tests: `clojure -M:test --focus psi.ai.providers.anthropic-structured-output-test --focus psi.ai.providers.openai-structured-output-test --focus psi.agent-session.workflow-judge-test --focus psi.workflow-runtime.structured-output-test` passed (47 tests, 356 assertions). Re-ran targeted `clj-kondo` over affected AI, agent-session, and workflow-runtime source/test paths; 0 errors, 0 warnings. Re-ran full `bb test`; all tests passed. No concrete implementation slice remains for task 190.

2026-05-30 — Implementation review: found one actionable coverage/implementation-structure issue. OpenAI chat-completions streaming and non-streaming structured-output result construction are separate code paths, but the payload matrix only exercises the non-streaming helper for string/number/boolean/array/object/null and both strategies; streaming public tests currently cover only provider-native object payload. The recorded equivalence is therefore not proven unless the result construction is shared. Added a follow-up to either add direct streaming prompted-JSON/non-object/null coverage or factor the duplicate result construction behind a shared helper.

2026-05-30 — Implementation review follow-up: refactored OpenAI Chat Completions streaming and non-streaming structured-output result construction through a shared private `structured-output-result` helper. The existing all-JSON-value payload matrix now exercises the exact result construction used by both paths for provider-native and prompted-JSON strategies, including non-object values and present `:payload nil`. Marked the implementation-review follow-up complete.
Verification: focused OpenAI structured-output tests passed (`clojure -M:test --focus psi.ai.providers.openai-structured-output-test`: 9 tests, 99 assertions); targeted `clj-kondo` over the changed OpenAI source/test files passed with 0 errors and 0 warnings; focused structured-output/provider/workflow-judge tests passed (`clojure -M:test --focus psi.ai.providers.anthropic-structured-output-test --focus psi.ai.providers.openai-structured-output-test --focus psi.agent-session.workflow-judge-test --focus psi.workflow-runtime.structured-output-test`: 47 tests, 356 assertions); full `bb test` passed.

2026-05-30 — Implementation review: found one actionable documentation coherence issue. The implementation updates prompted-JSON instructions and provider result handling to support any valid JSON value, but `doc/workflow-grammar.md` and `doc/workflow-ir.md` still describe prompted fallback/provider-native structured output as requiring a single JSON object/schema-constrained object. These user-facing docs now conflict with non-object structured-output schemas such as judge string enums.

2026-05-30 — Implementation review documentation follow-up: updated `doc/workflow-grammar.md` and `doc/workflow-ir.md` so prompted-JSON and provider-native structured-output docs describe one JSON value matching the declared JSON Schema rather than only an object. Preserved the guidance that authors should use a map/object schema when they need multiple named fields or downstream `:path` references. Marked the implementation-review follow-up complete.

2026-05-30 — Implementation review: found one actionable documentation/API-contract coherence issue. `psi.agent-session.workflow-judge` now retries invalid structured-output judge turns up to the built-in judge retry limit, but `doc/workflow-ir.md` still says omitted `:on-invalid` is fail-fast and bounded retry/repair only occurs when explicit in `:on-invalid`. That conflicts with the landed judge behavior and can mislead workflow authors/reviewers about default structured-output judge retries.

2026-05-30 — Implementation review invalid-policy documentation follow-up: reconciled `doc/workflow-ir.md` with landed workflow-judge behavior. The structured-output spec section now distinguishes session structured-output fail-fast defaults from LLM judge structured-output retries, states invalid judge structured outputs retry by default up to the built-in judge retry limit with the original structured-output opts/schema preserved, and states unsupported structured output still fails immediately. Marked the implementation-review follow-up complete.

2026-05-30 — Implementation re-review: re-read task artifacts plus referenced provider structured-output result helpers, prompted-JSON instruction wording, workflow-judge retry code, provider/workflow-judge tests, changelog, and workflow docs. Focused structured-output/provider/workflow-judge tests passed (`clojure -M:test --focus psi.ai.providers.anthropic-structured-output-test --focus psi.ai.providers.openai-structured-output-test --focus psi.agent-session.workflow-judge-test --focus psi.workflow-runtime.structured-output-test`: 47 tests, 356 assertions). No new actionable implementation feedback found.

2026-05-30 — Test review: found one actionable coverage gap. Workflow-judge retry tests prove structured-output opts/schema are preserved for the first retry on the eventual-success path, but the retry-exhaustion test only counts prompts and does not assert that every exhausted retry turn received the original structured-output opts/schema. A regression that drops opts on the final retry/exhaustion path could still pass.

2026-05-30 — Test review follow-up: strengthened `execute-judge-invalid-structured-output-fails-locally-test` so the retry-exhaustion path records every judge turn and asserts each exhausted structured-output retry call carries a non-nil structured-output opts map equal to the original request, including the original JSON Schema. Marked the test-review follow-up complete. Verification: `clojure -M:test --focus psi.agent-session.workflow-judge-test` passed (15 tests, 83 assertions).

2026-05-30 — Implementation review: re-read the task artifacts plus provider structured-output result helpers, prompted-JSON instruction wording, workflow-judge retry implementation, provider/workflow tests, workflow docs, and changelog. Focused structured-output/provider/workflow-judge tests passed (`clojure -M:test --focus psi.ai.providers.anthropic-structured-output-test --focus psi.ai.providers.openai-structured-output-test --focus psi.agent-session.workflow-judge-test --focus psi.workflow-runtime.structured-output-test`: 47 tests, 359 assertions). Targeted clj-kondo over affected source/test paths passed with 0 errors and 0 warnings. No new actionable implementation feedback found.

2026-05-30 — Test review: found one actionable acceptance-coverage gap. Tests cover Anthropic `structured-output-result` preserving a bare string payload and separately cover workflow envelope string-enum validation from raw output / generic metadata, but no test passes the Anthropic native structured-output result metadata from a bare string (`:source :anthropic/json-schema-output`, `:payload "DONE"`) into `structured-output/output-result` and asserts the judge routing schema validates to `"DONE"`. AC2 specifically requires the envelope receiving that provider result to validate, so add an integrated provider-result-to-envelope regression.

2026-05-30 — Test review follow-up: added an integrated Anthropic-native-provider-result-to-workflow-envelope regression. `structured-output-envelope-anthropic-native-string-payload-test` now builds Anthropic JSON Schema output metadata from raw `"\"DONE\""`, passes it to `psi.workflow-runtime.structured-output/output-result` with the judge routing schema, and asserts `:status :valid`, `:value "DONE"`, and preserved `:source`, `:native-mechanism`, `:payload`, and `:raw-payload`. Marked the test-review follow-up complete. Verification: `clojure -M:test --focus psi.workflow-runtime.structured-output-test` passed (10 tests, 60 assertions); `clojure -M:test --focus psi.ai.providers.anthropic-structured-output-test --focus psi.workflow-runtime.structured-output-test` passed (24 tests, 184 assertions); `clj-kondo --lint components/workflow-runtime/test/psi/workflow_runtime/structured_output_test.clj` passed with 0 errors and 0 warnings.

2026-05-30 — Test re-review: applied `task-test-review` after the Anthropic provider-result-to-envelope follow-up. Re-read task artifacts plus provider result helpers, structured-output envelope/runtime code, workflow-judge retry code, workflow docs, changelog, and focused provider/workflow tests. Focused structured-output/provider/workflow-judge tests passed (`clojure -M:test --focus psi.ai.providers.anthropic-structured-output-test --focus psi.ai.providers.openai-structured-output-test --focus psi.agent-session.workflow-judge-test --focus psi.workflow-runtime.structured-output-test`: 48 tests, 366 assertions). Targeted clj-kondo over affected source/test paths passed with 0 errors and 0 warnings. Full `bb test` passed. No new actionable test feedback found.

2026-05-30 — Test-shaper review: re-read task artifacts, provider/result helper tests, workflow envelope tests, and workflow-judge retry tests through the clarity/signal/robustness lens. Focused structured-output/provider/workflow-judge suite passed (`clojure -M:test --focus psi.ai.providers.anthropic-structured-output-test --focus psi.ai.providers.openai-structured-output-test --focus psi.agent-session.workflow-judge-test --focus psi.workflow-runtime.structured-output-test`: 48 tests, 366 assertions). Tests are behavior-focused around provider payload preservation, envelope validation, retry behavior, and opts/schema retention; no new actionable test-shaping feedback found.

2026-05-30 — Documentation review: applied `review-task-docs` to task 190. Re-read `README.md`, `CHANGELOG.md`, `doc/workflow-grammar.md`, `doc/workflow-ir.md`, `doc/workflows.md`, `doc/custom-providers.md`, task artifacts, and referenced structured-output/provider/workflow-judge implementation/tests. User-facing docs already describe structured outputs as one JSON value (including scalar/array/object/boolean/number/null when allowed), preserve map/object guidance for multiple named fields and `:path`, distinguish session fail-fast from LLM judge invalid-output retries with original opts/schema, and include an Unreleased changelog fix. No new actionable documentation feedback found.
