- [ ] Make the review/output surface explicit for this task: keep `implementation.md` and `design-steps.md` present in the task directory, or update the task artifacts to name the canonical alternatives so review-note and follow-up recording are unambiguous.
- [ ] Specify the authoritative delegated invocation payload shape that reaches the callee workflow runtime, including the exact local `:workflow-input` value, the exact local `:workflow-original` value, and whether forwarded `:context` stays a bare ordered vector or is wrapped in a boundary envelope with target/prompt/caller metadata.
- [ ] Add at least one focused delegate execution proof that asserts the concrete callee-visible `:workflow-input` and `:workflow-original` values, so delegated-boundary semantics are executable rather than only descriptive.
