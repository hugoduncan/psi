- [x] Make the review/output surface explicit for this task: keep `implementation.md` and `design-steps.md` present in the task directory, or update the task artifacts to name the canonical alternatives so review-note and follow-up recording are unambiguous.
- [x] Specify the authoritative delegated invocation payload shape that reaches the callee workflow runtime, including the exact local `:workflow-input` value, the exact local `:workflow-original` value, and whether forwarded `:context` stays a bare ordered vector or is wrapped in a boundary envelope with target/prompt/caller metadata.
- [x] Add at least one focused delegate execution proof that asserts the concrete callee-visible `:workflow-input` and `:workflow-original` values, so delegated-boundary semantics are executable rather than only descriptive.
- [x] Reconcile the task-local dependency/orchestration story for shared source/reference/projection reuse: either update `munera/plan.md` so task ordering and active task inventory explicitly reflect the stated dependency on task `088`, or revise this task's `design.md`/`plan.md` dependency note to reference the actual predecessor task(s) that must land first.
