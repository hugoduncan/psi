2026-06-08: Architecture-fit design review read `design.md` and consulted `AGENTS.md`, `META.md`, `doc/architecture.md`, current workflow grammar/IR docs, deterministic-operation docs, and current routing/registration/workflow EDN seams. Found no new actionable architectural-fit feedback. The design aligns with the recorded workflow runtime boundary by keeping generic exact-marker parsing/operation registration in runtime code while moving workflow-specific marker names, allowed routes, topology, proof artifacts, and business rules into authored workflow EDNs/prompts/tests; immediate removal of specialized operation ids also matches the no-compatibility-shim principle. PASS_STATUS REVIEW_COMPLETE

2026-06-08: Ambiguity design review read `design.md` and consulted `task-design`, current routing/registration code, simplification workflow EDNs, workflow-loader/routing tests, workflow IR docs, and deterministic-operation docs. Found actionable ambiguities AMB1–AMB3: invalid exact-marker operation args are only preferred open questions, mixed-candidate error precedence is unspecified, and user-visible docs/changelog impact of changing registered operation ids is undecided. PASS_STATUS ACTIONABLE_FEEDBACK


2026-06-08: Design ambiguity follow-ups AMB1–AMB3 executed. `design.md` now pins the generic `workflow/exact-marker-routing` invalid-argument contract (`:invalid-route-marker-args`, required `:text`, `^[A-Z_]+$` marker labels/routes, non-empty distinct vector routes, accumulated `:details :errors`), multi-candidate precedence (`:ambiguous-route-marker` always wins after arg validation and includes all candidate lines plus classifications), and user-visible operation-id docs/changelog policy (CHANGELOG required because `/operations`, `/operation`, and `psi-tool operation` expose registered ids; docs updated only if they mention these ids). Marked AMB1–AMB3 complete in `design-steps.md`. PASS_STATUS FOLLOW_UPS_COMPLETE

2026-06-08: Inconsistency design review read `design.md` and consulted `AGENTS.md` workflow runtime boundary, current workflow routing/operation registration code, simplification workflow EDNs, workflow grammar/IR/materialization docs/code, deterministic-operation docs, and current routing/workflow-loader tests. Found no new actionable inconsistency feedback: the design's generic `workflow/exact-marker-routing` contract, authored EDN ownership of marker labels/routes, removal of specialized operation ids, tests, docs/changelog requirements, and source-spec vs resolved-arg boundary are internally consistent and match referenced artifacts after AMB1–AMB3. PASS_STATUS REVIEW_COMPLETE

2026-06-08: Plan ambiguity review read `plan.md`, `steps.md`, `implementation.md`, `design.md`, current routing/registration code, simplification workflow EDNs, routing/operation tests, workflow-loader content-lock tests, and docs/changelog references. Found actionable plan/steps ambiguities PA1–PA3: removal sequencing can transiently leave `workflow.core` pointing at deleted specialized wrappers, focused verification is described generically rather than pinned to concrete Scry namespaces/commands, and the final runtime-boundary cleanup check is not concrete enough to prove specialized marker vocabularies left generic runtime code while remaining in authored workflow EDNs/tests. PASS_STATUS ACTIONABLE_FEEDBACK

2026-06-08: Plan ambiguity follow-ups PA1–PA3 executed. Refined `plan.md` and `steps.md` to keep specialized parser wrappers as temporary compile-safe delegators until the same Slice 4 edit/commit that removes old operation registrations; pinned focused verification commands for routing parser (`psi.agent-session.workflow.routing-test`), built-in operation registration/invocation (`psi.agent-session.workflow-delegate-review-step-live-test`), workflow-loader task 209/218/220 namespaces, clj-kondo, and EDN read checks; added precise final runtime-boundary grep assertions separating generic runtime source (must not contain workflow-owned marker/route policy) from authored workflow EDNs/content-lock tests (must contain expected marker labels/routes). PASS_STATUS FOLLOW_UPS_COMPLETE

2026-06-08: Plan inconsistency review read `plan.md`, `steps.md`, `implementation.md`, `design.md`, current routing/registration code, simplification workflow EDNs, routing/operation tests, and workflow-loader content-lock tests. Found actionable inconsistency PI1: the plan's compile-safe Slice 4 removes old built-in operation ids while Slice 5 migrates checked-in workflow EDNs afterward, creating a repository state where authored workflows still reference removed operations; the handoff must either migrate EDNs in the same commit as removal or keep old registrations until workflow references are gone. PASS_STATUS ACTIONABLE_FEEDBACK

2026-06-08: Plan inconsistency follow-up PI1 executed. Refined `plan.md` and `steps.md` so specialized operation-id removal and both simplification workflow EDN migrations happen as one coherent Slice 4 handoff commit: add `workflow/exact-marker-routing`, migrate architecture/incidental validation/proof disposition calls, then remove old registrations/wrappers/constants without any committed state where checked-in EDNs still reference removed operation ids. Slice 5 is now post-handoff EDN/topology verification. Marked PI1 complete in `steps.md`. PASS_STATUS FOLLOW_UPS_COMPLETE

2026-06-08: Slice 1 preflight/source mapping complete. Runtime-specific policy to remove lives in `workflow.routing` (`proof-sync-routes`, `validation-capture-routes`, specialized parse wrappers) and `workflow.core` old built-in operation registrations. Architecture workflow disposition steps currently call `workflow/validation-capture-disposition-routing` from `validation-capture` and `workflow/proof-sync-disposition-routing` from `proof-sync`; incidental workflow does the same, with validation text sourced from `incidental-validation-capture`. README/doc/CHANGELOG search found old operation ids only in workflow EDNs and tests, not user docs or changelog, so docs likely need no operation-id edit; CHANGELOG still required for the user-visible registered operation surface. PASS_STATUS MORE_WORK_REMAINS

2026-06-08: Slices 2–3 complete. `routing/parse-exact-marker-routing` is now public and parameterized by `:text`, `:marker-label`, and `:allowed-routes`; it validates args first with accumulated `:invalid-route-marker-args` diagnostics, classifies exact/malformed/unsupported candidates, and makes multi-candidate ambiguity win with complete candidate diagnostics. Specialized proof-sync/validation-capture wrappers/constants remain temporarily as compile-safe delegators until the coherent operation/EDN handoff. Replaced specialized parser tests with arbitrary `QUALITY_GATE` exact-marker coverage for valid/missing/prose/duplicate/unsupported/malformed/mixed-candidate/invalid-arg cases. Verification: `clj-paren-repair components/agent-session/src/psi/agent_session/workflow/routing.clj components/agent-session/test/psi/agent_session/workflow/routing_test.clj` no changes needed; `bb clojure:test:scry --namespace psi.agent-session.workflow.routing-test` passed 7 tests / 153 assertions. PASS_STATUS MORE_WORK_REMAINS

2026-06-08: Slice 4 coherent operation/EDN handoff and Slice 5 verification complete. Registered `workflow/exact-marker-routing`, removed old proof-sync/validation-capture operation registrations and specialized routing wrappers/constants, and migrated both simplification workflow EDNs so validation/proof disposition steps invoke the generic operation with workflow-owned marker labels and allowed route vectors. Disposition `:on` route topology is unchanged. Built-in operation tests now assert new-id presence, old-id absence, valid arbitrary marker invocation, and invalid-arg registry invocation returning `:invalid-route-marker-args`. Verification: `bb clojure:test:scry --namespace psi.agent-session.workflow-delegate-review-step-live-test` passed 3 tests / 18 assertions; EDN read check passed for both workflows; old operation-id grep over both workflow EDNs returned no matches. PASS_STATUS MORE_WORK_REMAINS

2026-06-08: Slice 6 workflow-loader/content-lock update complete. Task 209, 218, and 220 workflow-loader tests now expect `workflow/exact-marker-routing` plus exact authored validation/proof `:marker-label` and `:allowed-routes` args while preserving route topology and prompt route-label locks. Verification: `bb clojure:test:scry --dir components/workflow-loader/test --namespace psi.workflow-loader.task-209-workflow-definitions-test --namespace psi.workflow-loader.task-218-workflow-definitions-test --namespace psi.workflow-loader.task-220-workflow-proof-gates-test` passed 15 tests / 802 assertions. PASS_STATUS MORE_WORK_REMAINS

2026-06-08: Slice 7 docs/changelog/final verification complete. Added CHANGELOG `[Unreleased]` entry for new `workflow/exact-marker-routing` and removal of specialized proof-sync/validation-capture operation ids; README/doc required no edit because preflight found no explicit operation-id mentions. Verification green: routing Scry 7/153, built-in operation Scry 3/18, workflow-loader task 209/218/220 Scry 15/802, EDN read check for both simplification workflows, targeted clj-kondo 0/0, runtime-boundary negative grep clean, authored-policy positive grep found only expected workflow EDNs/content-lock tests, and `git diff --check` clean. PASS_STATUS IMPLEMENTATION_COMPLETE

2026-06-08: Implementation review (task-implementation-review) read design/plan/steps and inspected `routing.clj`, `core.clj` operation registration, both simplification workflow EDNs, the routing parser tests, and the live registry smoke tests. No new actionable feedback. Code matches design: one public parameterized `parse-exact-marker-routing`, arg validation before parsing with accumulated `:invalid-route-marker-args` errors, candidate precedence (missing→ambiguous→exact→unsupported→malformed) with complete per-candidate diagnostics, and a single registered `workflow/exact-marker-routing` operation. Architecture-fit good: workflow-owned marker labels/allowed routes live only in authored EDNs/content-lock tests; generic runtime is vocabulary-free (boundary negative grep clean) and `:on` topology unchanged. Helpers reuse existing routing idioms (`route-token?`, candidate classification) with no unnecessary abstraction or structural performance concern. Verified green: routing Scry 7/153, live operation Scry 3/18, workflow-loader task 209/218/220 Scry 15/802, clj-kondo 0/0 on changed files, both workflow EDNs read as EDN, and old operation-id references remain only in correct `(is (nil? ...))` absence assertions plus historical task-220/state artifacts. CHANGELOG `[Unreleased]` records the operation-surface change. PASS_STATUS REVIEW_COMPLETE

2026-06-09: Test review (task-test-review) read `design.md` and inspected `routing_test.clj`, the live registry smoke tests in `workflow_delegate_review_step_live_test.clj`, and the task 209/218/220 workflow-loader content-lock tests. Tests are well-formed and use arbitrary `QUALITY_GATE`/routes; infra deps in the added operation-registration smoke tests use the real deterministic-operation registry (no mocks/stubs). Coverage matches the design acceptance criteria for valid/missing/duplicate/malformed/unsupported/surrounding-prose/leading-whitespace/whitespace-before-colon/trailing-whitespace/same-line-extra-text/invalid-args/mixed-candidate precedence, and both simplification workflows assert authored marker labels + allowed routes. One actionable coverage gap: the design rule "a column-0 line starting with the marker label but without a marker colon is ordinary prose, not a candidate" is unexercised — the only prose-mention test puts the label mid-line, so the `marker-prefix? ∧ ¬marker-attempt?` → `:ordinary` branch is never hit; a regression collapsing `marker-attempt?` into `marker-prefix?` would reclassify prose like "QUALITY_GATE recommends APPROVE" as a malformed candidate (making a single valid marker reply ambiguous) undetected. Minor secondary gap: marker-label arg validation only tests hyphen + non-string; lowercase/digit/space/blank marker-label and nil/list `:allowed-routes` rejection are untested. PASS_STATUS ACTIONABLE_FEEDBACK

2026-06-09: Test-review follow-ups TR1–TR2 executed (test-only). TR1: added a `parse-exact-marker-routing` test asserting a column-0 line that starts with the marker label but has no marker colon ("QUALITY_GATE recommends APPROVE") routes to `:missing-route-marker` (not `:malformed-route-marker`), covering the `marker-prefix? ∧ ¬marker-attempt?` → `:ordinary` branch. TR2: broadened invalid marker-label arg cases (lowercase `quality_gate`, digit `QUALITY1`, space `QUALITY GATE`, blank `""` — all `:invalid-marker-label`) and added `:allowed-routes` nil and list (`'("APPROVE")`) rejection cases (both `:non-vector-allowed-routes`). No production code change. Verification green: `bb clojure:test:scry --namespace psi.agent-session.workflow.routing-test` 7 tests / 173 assertions pass, clj-kondo 0/0 on the test file, `git diff --check` clean. PASS_STATUS FOLLOW_UPS_COMPLETE

2026-06-09: Test re-review (task-test-review) re-inspected `routing_test.clj`, the live registry smoke tests in `workflow_delegate_review_step_live_test.clj`, and task 209/218/220 content-lock tests against `design.md` acceptance criteria. No new actionable feedback. Tests are well-formed and cover every design-listed case (valid/missing/duplicate/malformed{leading-ws,ws-before-colon,missing-space,trailing-ws,same-line-extra,lowercase}/unsupported/prose-mention{mid-line,column-0 TR1}/surrounding-prose/invalid-args{TR2 lowercase,digit,space,blank marker-label; nil,list,set,empty allowed-routes; multi-error accumulation}/mixed-candidate precedence); both simplification workflows assert authored marker labels + allowed routes; live smoke tests cover registration presence, old-id absence, valid invocation, and invalid-arg `:invalid-route-marker-args` without throwing. Added smoke tests use the real deterministic-operation registry (no mocks/stubs); the only `with-redefs` turn stub is in a pre-existing delegate end-to-end test outside task 221's scope. Prior TR1/TR2 follow-ups are present. Verified runtime green: `bb clojure:test:scry --namespace psi.agent-session.workflow.routing-test` 7 tests / 173 assertions pass. PASS_STATUS REVIEW_COMPLETE
