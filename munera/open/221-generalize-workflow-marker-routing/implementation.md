2026-06-08: Architecture-fit design review read `design.md` and consulted `AGENTS.md`, `META.md`, `doc/architecture.md`, current workflow grammar/IR docs, deterministic-operation docs, and current routing/registration/workflow EDN seams. Found no new actionable architectural-fit feedback. The design aligns with the recorded workflow runtime boundary by keeping generic exact-marker parsing/operation registration in runtime code while moving workflow-specific marker names, allowed routes, topology, proof artifacts, and business rules into authored workflow EDNs/prompts/tests; immediate removal of specialized operation ids also matches the no-compatibility-shim principle. PASS_STATUS REVIEW_COMPLETE

2026-06-08: Ambiguity design review read `design.md` and consulted `task-design`, current routing/registration code, simplification workflow EDNs, workflow-loader/routing tests, workflow IR docs, and deterministic-operation docs. Found actionable ambiguities AMB1–AMB3: invalid exact-marker operation args are only preferred open questions, mixed-candidate error precedence is unspecified, and user-visible docs/changelog impact of changing registered operation ids is undecided. PASS_STATUS ACTIONABLE_FEEDBACK


2026-06-08: Design ambiguity follow-ups AMB1–AMB3 executed. `design.md` now pins the generic `workflow/exact-marker-routing` invalid-argument contract (`:invalid-route-marker-args`, required `:text`, `^[A-Z_]+$` marker labels/routes, non-empty distinct vector routes, accumulated `:details :errors`), multi-candidate precedence (`:ambiguous-route-marker` always wins after arg validation and includes all candidate lines plus classifications), and user-visible operation-id docs/changelog policy (CHANGELOG required because `/operations`, `/operation`, and `psi-tool operation` expose registered ids; docs updated only if they mention these ids). Marked AMB1–AMB3 complete in `design-steps.md`. PASS_STATUS FOLLOW_UPS_COMPLETE

2026-06-08: Inconsistency design review read `design.md` and consulted `AGENTS.md` workflow runtime boundary, current workflow routing/operation registration code, simplification workflow EDNs, workflow grammar/IR/materialization docs/code, deterministic-operation docs, and current routing/workflow-loader tests. Found no new actionable inconsistency feedback: the design's generic `workflow/exact-marker-routing` contract, authored EDN ownership of marker labels/routes, removal of specialized operation ids, tests, docs/changelog requirements, and source-spec vs resolved-arg boundary are internally consistent and match referenced artifacts after AMB1–AMB3. PASS_STATUS REVIEW_COMPLETE

2026-06-08: Plan ambiguity review read `plan.md`, `steps.md`, `implementation.md`, `design.md`, current routing/registration code, simplification workflow EDNs, routing/operation tests, workflow-loader content-lock tests, and docs/changelog references. Found actionable plan/steps ambiguities PA1–PA3: removal sequencing can transiently leave `workflow.core` pointing at deleted specialized wrappers, focused verification is described generically rather than pinned to concrete Scry namespaces/commands, and the final runtime-boundary cleanup check is not concrete enough to prove specialized marker vocabularies left generic runtime code while remaining in authored workflow EDNs/tests. PASS_STATUS ACTIONABLE_FEEDBACK

2026-06-08: Plan ambiguity follow-ups PA1–PA3 executed. Refined `plan.md` and `steps.md` to keep specialized parser wrappers as temporary compile-safe delegators until the same Slice 4 edit/commit that removes old operation registrations; pinned focused verification commands for routing parser (`psi.agent-session.workflow.routing-test`), built-in operation registration/invocation (`psi.agent-session.workflow-delegate-review-step-live-test`), workflow-loader task 209/218/220 namespaces, clj-kondo, and EDN read checks; added precise final runtime-boundary grep assertions separating generic runtime source (must not contain workflow-owned marker/route policy) from authored workflow EDNs/content-lock tests (must contain expected marker labels/routes). PASS_STATUS FOLLOW_UPS_COMPLETE

2026-06-08: Plan inconsistency review read `plan.md`, `steps.md`, `implementation.md`, `design.md`, current routing/registration code, simplification workflow EDNs, routing/operation tests, and workflow-loader content-lock tests. Found actionable inconsistency PI1: the plan's compile-safe Slice 4 removes old built-in operation ids while Slice 5 migrates checked-in workflow EDNs afterward, creating a repository state where authored workflows still reference removed operations; the handoff must either migrate EDNs in the same commit as removal or keep old registrations until workflow references are gone. PASS_STATUS ACTIONABLE_FEEDBACK

2026-06-08: Plan inconsistency follow-up PI1 executed. Refined `plan.md` and `steps.md` so specialized operation-id removal and both simplification workflow EDN migrations happen as one coherent Slice 4 handoff commit: add `workflow/exact-marker-routing`, migrate architecture/incidental validation/proof disposition calls, then remove old registrations/wrappers/constants without any committed state where checked-in EDNs still reference removed operation ids. Slice 5 is now post-handoff EDN/topology verification. Marked PI1 complete in `steps.md`. PASS_STATUS FOLLOW_UPS_COMPLETE
