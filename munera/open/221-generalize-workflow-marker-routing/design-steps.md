# Design follow-up steps

- [ ] AMB1 — Resolve and record the exact invalid-argument contract for `workflow/exact-marker-routing`: valid `:text`, `:marker-label`, and `:allowed-routes` shapes; whether marker labels/routes must match `[A-Z_]+`; how missing/non-string text, empty/non-vector/duplicate/invalid routes, and invalid marker labels are tagged; and required parser/operation tests.
- [ ] AMB2 — Resolve and record error precedence/diagnostics when a reply contains multiple marker candidates of mixed kinds (valid + malformed, valid + unsupported, multiple malformed/unsupported). State whether multiple candidates always return `:ambiguous-route-marker` with all candidate lines or whether malformed/unsupported wins, and lock tests accordingly.
- [ ] AMB3 — Resolve and record whether replacing/removing deterministic operation ids is user-visible through `/operations`/`/operation` and therefore requires CHANGELOG/docs updates even if individual old operation names are not currently documented.
