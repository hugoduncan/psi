# 231 — Steps

## Slice 1 — Capability model surface

- [x] Inspect existing model capability schemas/loading paths in `components/ai` and identify the lowest-level namespace for textual-tool-call capability helpers.
- [x] Extend model capability schema/validation to accept `{:textual-tool-calls #{:xml}}` without requiring the key on existing/default models.
- [x] Add a pure predicate such as `supports-textual-tool-calls-format?` that checks the resolved model map for a requested format.
- [x] Add focused tests that custom/user model maps can declare `#{:xml}` and that omitted/empty capabilities return disabled.

## Slice 2 — Pure XML-like parser

- [x] Implement a pure parser for exact lowercase `<tool_call>...</tool_call>` blocks containing exactly one `<function=TOOL_NAME>...</function>` block.
- [x] Enforce `[A-Za-z0-9_-]+` for tool and parameter names and reject whitespace, attributes, namespaces, dots, slashes, quotes, and entity decoding.
- [x] Parse one or more nested `<parameter=PARAM_NAME>...</parameter>` blocks, trimming only at tag boundaries and preserving internal text/newlines/metacharacters.
- [x] Treat duplicate parameter names, missing/multiple function blocks, zero parameters, or parameter blocks outside the function as malformed block-level no-ops.
- [x] Return enough information to remove only successfully parsed exact block spans while leaving malformed/partial markup unchanged.
- [x] Add parser tests for nominal `bash`, multiple calls, multi-parameter calls, duplicate parameters, malformed cardinality/nesting, unsupported case/grammar variants, and surrounding-text preservation.

## Slice 3 — Canonical normalization

- [x] Implement one pure normalizer that takes `turn-id`, resolved `ai-model`, assistant text/content, and existing provider tool calls and returns canonical assistant content.
- [x] Gate parsing entirely on the resolved model capability containing `:xml`; when disabled, return text/content unchanged.
- [x] Convert each parsed call to canonical `{:type :tool-call :id ... :name ... :arguments ...}` content using JSON object string arguments.
- [x] Preserve provider-emitted tool-call ids and indexes when present, and generate recovered-call ids with the same per-turn canonical id convention using final-order, non-colliding content indexes.
- [x] Preserve response order for residual text, provider-emitted tool calls, and recovered textual tool calls while removing exact parsed blocks from assistant prose.
- [x] Keep malformed or partial markup as ordinary text and continue converting other well-formed blocks in the same response.
- [x] Add normalizer tests for enabled/disabled capability, canonical JSON arguments, multiple calls order, mixed valid/malformed markup, provider/recovered/text interleaving, id/index non-collision, and text removal/preservation.

## Slice 4 — Turn-runtime integration

- [x] Wire streaming final assembly to call the shared normalizer after text accumulation and provider tool-call completion but before delivering the final assistant message.
- [x] Wire non-streaming assistant responses to call the same shared normalizer before returning the turn result.
- [x] Ensure the integration uses the already-resolved runtime `ai-model` passed through the turn attempt and does not require `agent-session` from `turn-runtime`.
- [x] Preserve existing thinking, error, logprob, usage, and structured-output behavior when no textual tool-call parsing occurs.
- [x] Add/adjust turn-runtime tests for streaming and non-streaming normalization paths where practical.

## Slice 5 — Existing execution-path coverage

- [x] Add an end-to-end or focused turn/session test proving a capability-enabled textual `bash` block reaches the existing tool execution path and records an ordinary tool result.
- [x] Add coverage that unknown or unavailable parsed tool names follow the same failure/policy behavior as canonical provider-emitted tool calls.
- [x] Add coverage that frontier/default models preserve textual `<tool_call>` content as assistant text and execute no tool.
- [x] Run focused Scry suites for parser/model/turn-runtime/session tool-call behavior and fix regressions.
- [x] Run `clj-kondo` on changed Clojure files and fix warnings rather than suppressing globally.

## Slice 6 — Docs and changelog

- [x] Update custom/local model documentation to show `{:capabilities {:textual-tool-calls #{:xml}}}` opt-in.
- [x] Document that frontier/provider-native models should not enable textual tool-call recovery and that malformed markup is left as text.
- [x] Add a user-visible `CHANGELOG.md` entry under `[Unreleased]` for the new local-model compatibility capability.
- [x] Re-run relevant docs/tests checks if available.

## Implementation review follow-ups

- [x] Preserve original streaming content-index order when provider-emitted tool calls, recovered textual tool calls, and residual text coexist; the current streaming assembly still builds one text block before all provider tool calls, so the normalizer cannot satisfy the design requirement for content-position ordering in mixed responses.
- [x] Preserve provider-surface content-index information through normalization/id allocation so recovered textual tool-call ids are generated after considering provider tool-call content indexes even when provider calls have non-canonical provider ids.
- [x] Allow literal `<function=...>` text inside parameter values; the parser currently rejects any function body containing that substring, which can incorrectly make otherwise valid command text malformed instead of preserving parameter text.
- [x] Preserve streaming interleaving when text arrives in multiple provider content indexes around provider tool calls; the current accumulator still merges all text deltas into one `:text-buffer` at the first text index, so `text(index 0) → provider tool(index 1) → text/recovered call(index 2)` is emitted before the provider tool instead of in source order.
- [x] Recover later well-formed textual tool-call blocks when an earlier malformed or partial `<tool_call>` prefix is present; the current non-overlapping regex scan can consume `broken <tool_call> ... <tool_call>valid...</tool_call>` as one malformed block and prevent conversion of the valid inner/later exact block, contrary to mixed malformed/well-formed response semantics.
- [x] Allocate recovered textual tool-call ids/content indexes in final content order after preceding provider-emitted blocks; the current allocator starts at `0`, so a recovered call in text at provider content-index `2` after a provider tool at index `1` can receive `turn-id/toolcall/0`, which conflicts with the content-position convention even though it avoids id collision.
- [x] Preserve streaming provider content-index metadata until after textual-tool-call normalization; `build-final-content` currently strips `:content-index` before `normalize-assistant-message`, so a recovered call after a provider tool at content-index `1` can still be assigned `turn-id/toolcall/1` instead of an index allocated after the provider/text positions. Add a streaming regression that fails on the current `provider index 1 → text/recovered index 2` case.
- [x] Preserve literal `<tool_call>` / `</tool_call>` text inside parameter values; the parser currently rejects any function body containing those substrings, which can make otherwise valid command text malformed instead of preserving parameter text.
- [x] Do not reserve a removed source text block's content index when the block is fully replaced by a recovered textual tool call; a provider-indexed text block at content-index `2` containing only `<tool_call>...</tool_call>` should yield recovered id `turn-id/toolcall/2`, not skip to `/3` because the removed text block's index was treated as already used.
- [x] Preserve non-collision with existing canonical tool-call ids when reusing a fully replaced text block's source content index; if existing content already has `turn-id/toolcall/2` and a fully replaced text block also has `:content-index 2`, the recovered call must not also receive `turn-id/toolcall/2`.
- [x] Count preceding unindexed provider-emitted tool-call/content blocks when allocating recovered textual tool-call ids; a non-streaming assistant message containing an existing provider tool call without `:content-index` followed by a recovered textual call should allocate the recovered id after that provider block in final content order, not `turn-id/toolcall/0`.
- [x] Treat an unindexed existing provider/content block whose id is already `TURN_ID/toolcall/N` as occupying index `N` instead of allocating an additional hidden position; a preceding unindexed `turn-id/toolcall/0` block followed by one recovered textual call should allocate the recovered call as `/1`, not skip to `/2`. Add a focused regression.
- [x] Separate, revert, or explicitly document as pre-existing/prerequisite the unrelated changes present in this task diff (entity-resolution/review workflow artifacts, OpenAI `parallel_tool_calls`, and the write-tool description tweak), so the task implementation matches the textual-tool-call design scope.
- [x] Preserve literal `<parameter=...>` text inside parameter values; the parser currently rejects parameter values containing that substring, which can make otherwise valid command text malformed instead of preserving parameter text.
- [x] Do not reserve a removed source text block's content index when the block is fully replaced by multiple recovered textual tool calls; a provider-indexed text block at content-index `2` containing only two adjacent `<tool_call>...</tool_call>` blocks should yield recovered ids starting at `turn-id/toolcall/2`, not skip to `/3` because the removed text block's index was treated as already used.
- [x] Reject multiple `<function=...>` blocks inside one `<tool_call>` even when separated by whitespace or parameter-looking text; the parser currently accepts a second function block as literal parameter text in cases like `</function> <function=...>`, violating the exactly-one-function rule.
- [x] Preserve literal adjacent `<tool_call><function=...>` text inside parameter values; the parser currently rejects any function body containing that exact substring even when it appears inside a parsed parameter value, so otherwise valid command text such as `printf '<tool_call><function=literal>'` becomes malformed instead of preserving parameter text.
- [x] Preserve complete nested well-formed `<tool_call>...</tool_call>` text inside parameter values as literal parameter text; the current candidate scan can reject the outer call and recover/execute the inner literal block instead, e.g. `printf '<tool_call><function=x><parameter=y>z</parameter></function></tool_call>'`, violating parameter preservation and mixed malformed/well-formed semantics.
- [x] Do not recover and execute a nested well-formed `<tool_call>...</tool_call>` that appears inside the parameter text of an outer malformed `<tool_call>` block; for example, an outer block made malformed by duplicate parameters should be preserved as ordinary text rather than executing the nested literal block.
- [x] Reject an outer `<tool_call>` with multiple `<function=...>` blocks when the first function contains literal nested well-formed `<tool_call>...</tool_call>` text in a parameter; the parser can currently accept the outer call and swallow the first `</parameter></function>` plus the second function block into the parameter value instead of treating the outer block as malformed.
- [x] Do not recover and execute a nested well-formed `<tool_call>...</tool_call>` that appears inside an outer malformed `<tool_call>` block with no valid parameter structure; for example, an outer `<function=outer>...</function>` containing only a nested valid textual call should remain ordinary text rather than executing the nested call.
- [x] Do not recover and execute a nested well-formed `<tool_call>...</tool_call>` that appears inside an outer malformed `<tool_call>` block whose parameter markup occurs outside the function block; for example, an outer block with `<parameter=x>...nested valid call...</parameter><function=outer>...</function>` should remain ordinary text rather than executing the nested call.
- [x] Do not recover and execute a nested well-formed textual tool call that appears inside an outer malformed `<tool_call>` block whose function tag uses an invalid tool-name grammar; for example, an outer `<function=outer.bad><parameter=x>...nested valid call...</parameter></function>` should remain ordinary text rather than executing the nested call.
- [x] Do not recover and execute a nested well-formed textual tool call that appears inside an outer malformed `<tool_call>` block whose parameter tag uses an invalid parameter-name grammar; for example, an outer `<parameter=x.y>...nested valid call...</parameter>` should remain ordinary text rather than executing the nested call.
- [x] Preserve incomplete nested `<tool_call><function=...><parameter=...>` text inside an outer parameter value; the parser currently recovers the apparent inner call by stealing the outer parameter/function/tool-call close tags, e.g. `printf '<tool_call><function=literal><parameter=x>'`, which executes literal parameter text instead of preserving the outer `bash` call's command string.
- [x] Recover a well-formed textual tool-call block even when surrounding assistant prose includes quote characters immediately before or after the block; the parser currently suppresses conversion based on adjacent quotes outside the exact `<tool_call>...</tool_call>` span, which violates the requirement to remove/execute exact parsed blocks while preserving surrounding non-tool text.
- [x] Preserve unquoted incomplete nested `<tool_call><function=...><parameter=...>` text inside an outer parameter value; the parser still rejects the outer call and recovers the apparent inner call by stealing the outer close tags, so a command string such as `printf <tool_call><function=literal><parameter=x>` executes the literal inner `literal` call instead of preserving the outer `bash` call's command string.
- [x] Do not recover and execute a nested well-formed textual tool call that appears inside an outer malformed `<tool_call>` block with multiple `<function=...>` blocks when the nested call is inside a later function block; for example, an outer block containing `<function=a>...</function><function=b><parameter=y>...nested valid call...</parameter></function>` should remain ordinary text rather than executing the nested call.
- [x] Do not recover and execute a nested well-formed textual tool call that appears inside an unterminated outer textual tool-call parameter; for example, `<tool_call><function=bash><parameter=command>printf <tool_call><function=literal><parameter=x>y</parameter></function></tool_call>` should remain ordinary text rather than executing the nested `literal` call.
- [x] Do not recover and execute a nested well-formed textual tool call that appears inside an unterminated outer textual tool-call parameter when the malformed outer block is preceded by ordinary assistant prose; the current suppression only covers the string-start variant, so `prefix <tool_call><function=bash><parameter=command>printf <tool_call><function=literal><parameter=x>y</parameter></function></tool_call>` still executes the nested `literal` call instead of preserving the whole text.
- [x] Bound or linearize textual tool-call parsing so capability-enabled local-model responses with many `<tool_call>`/`</tool_call>` markers cannot impose quadratic-plus normalization cost; add a stress/regression test for malformed many-marker output.
- [x] Preserve valid textual tool-call blocks whose parameter values contain more than eight literal `</tool_call>` substrings before the real outer close tag; the current per-open close-candidate cap can stop before the actual close and leave an otherwise well-formed call unparsed, violating parameter-text preservation.
- [x] Recover later well-formed textual tool-call blocks that appear after an earlier malformed outer block containing nested tool-call-like text; the current malformed-enclosure suppression can treat the earlier outer `<tool_call>` as enclosing the later independent valid block and suppress conversion of the later block, violating mixed malformed/well-formed response semantics.
- [x] Preserve valid textual tool-call blocks whose parameter values contain more than 64 literal `</tool_call>` substrings before the real outer close, or replace the fixed per-open candidate cap with a parser strategy that preserves arbitrary valid parameter text while retaining bounded/linear malformed-input behavior; the current cap leaves otherwise well-formed calls unparsed once the real close appears after the 64th literal close substring.
- [x] Preserve or explicitly document the maximum supported textual tool-call block length; `max-candidate-span-chars` currently leaves otherwise well-formed capability-enabled calls over 65,536 characters as ordinary assistant text even though the design specifies parameter text preservation without a size limit.
- [x] Reconcile parser/tests/docs with the strict/simple `:xml` contract: tag-looking textual-tool-call markup inside parameter values is unsupported, nested recovery is disabled, and later independent valid blocks still recover.
- [x] Add a malformed many-unclosed-open-marker parser stress regression and fix candidate scanning so capability-enabled responses with many `<tool_call>` opens before a far/lone close are bounded/linear; the current `candidate-for-open` path calls `str/index-of` for each open and can rescan the same suffix repeatedly, while the existing stress test only covers balanced many-marker output.
- [x] Reject tag-looking `<function=...>` / `<parameter=...>` substrings inside parameter values even when the embedded name does not match the valid identifier grammar; the strict parser currently accepts values like `printf '<function=x.y>'`, although the design says any tag-looking textual-tool-call markup inside parameter text makes the enclosing block malformed/no-op.
- [x] Prevent recovery of nested textual tool calls inside an oversized or otherwise skipped outer `<tool_call>` candidate; `candidate-tool-call-spans` drops candidates over `max-candidate-span-chars`, so an inner well-formed block after that bound can be parsed/executed even though oversized markup is documented as ordinary text and nested recovery is unsupported.
- [x] Allocate recovered textual tool-call ids/content indexes in final content order when a provider-indexed text block begins with a recovered call and only has residual text after it; the current normalizer reserves the source text block's content index for trailing text and assigns the leading recovered call the next index, so a source block at `:content-index 2` containing `<tool_call>...</tool_call> after` yields `turn-id/toolcall/3` before residual text at hidden index `2`, violating the content-position/id convention.
- [x] Allocate later recovered textual tool-call ids after intervening residual text when a provider-indexed text block begins with a recovered call but contains another recovered call later; after the leading call reuses the source content index, the current allocator can reserve the intervening residual text at the same hidden index and assign the later recovered call the next id, so `<tool_call>first</tool_call> mid <tool_call>second</tool_call>` at `:content-index 2` can yield the second call as `turn-id/toolcall/3` even though the residual `mid` text should occupy the next final-order position and the second call should allocate after it.
- [x] Avoid reparsing the same source text block repeatedly during normalization/id allocation; `next-content-index-fn` currently calls `leading-recovered-text-block?` for the same block during setup and on every recovered-call allocation, while `textual-tool-call-content` parses it again, so a capability-enabled response with many recovered calls in one text block can re-run the full parser many times. Cache/thread parsed-call metadata per block so normalization remains bounded/linear.

## Test review follow-ups

- [ ] Add parser/normalizer tests proving uppercase letters in valid tool and parameter identifiers are accepted and preserved exactly (case-sensitive, not normalized).
- [ ] Add parser rejection tests for the remaining disallowed identifier/tag-name forms from the design—quotes, namespace/colon, slash, and entity-encoded names—so the narrow grammar is executable rather than implied by regex implementation.
