- Ambiguity review 2026-05-21: actionable ambiguities found. This task directory only contains `design.md`, but the requested review protocol requires `plan.md`, `steps.md`, `design-steps.md`, and `implementation.md`; without those artifacts, the canonical task-local surfaces for approach, execution, and append-only review notes are implicit rather than explicit. Added `design-steps.md` follow-ups to create and define those missing artifact surfaces. Additional actionable ambiguity: `design.md` says task `171` should assume task `172` is complete, but current `munera/plan.md` does not place `172` ahead of `171`, so execution order and blocking status are underspecified unless the task artifacts make that prerequisite explicit.
- 2026-05-22 ψ design-follow-up(ambiguities): created the missing `plan.md` and `steps.md` artifacts, recorded their task-local roles, and added an explicit prerequisite guard stating that task `171` executes only after closed task `172` and must preserve `172`'s unordered deterministic-operation listing contract without reintroducing adapter-owned ordering metadata or guarantees. Marked both newly added ambiguity follow-up items done in `design-steps.md`.
- Inconsistency review 2026-05-22: actionable inconsistency found. `design.md` acceptance requires the task artifacts to record the final authoritative-vs-derived registry-object state split after migration, but `plan.md` and `steps.md` currently cover seam inventory, code migration, tests, and verification only; they do not include an explicit execution/documentation step to capture that required post-migration artifact update.
- 2026-05-22 ψ design-follow-up(inconsistency): added an explicit final artifact-update step to `steps.md` and extended `plan.md` so execution now reserves a concrete pass for recording the final authoritative-vs-derived registry-object state split after migration.
- Ambiguity review 2026-05-21: actionable ambiguity found. `design.md` requires shared `root-registry` to become the authoritative canonical owner for deterministic operations and requires higher-seam coherence beyond the lower adapter, but the task artifacts do not yet say whether extension introspection surfaces that currently read `:extensions ... :operations` in `components/agent-session/src/psi/agent_session/extensions.clj` remain intentionally extension-registry-owned projections, must be derived from the migrated deterministic-operation shared store, or must be kept explicitly synchronized. Without that ownership decision, the migration could satisfy invoke-time/runtime seams while leaving `extension-detail-in`, `operation-ids-in`, and related tests coherent only by stale parallel extension-local storage.
- 2026-05-22 ψ design-follow-up(ambiguities): audited the named extension introspection seams in `components/agent-session/src/psi/agent_session/extensions.clj` and the related tests/runtime registration paths. Clarified in `design.md` and `plan.md` that post-migration ownership splits intentionally: shared `root-registry` is the sole canonical runtime owner for deterministic-operation entries, while extension-registry `:extensions ... :operations` remains an explicitly synchronized extension-facing projection for `operation-ids-in`, `extension-detail-in`, `extension-details-in`, and `summary-in`. Higher-seam proofs must cover both runtime invoke/cleanup coherence and projection coherence so extension-local reads cannot masquerade as canonical runtime storage.
- Inconsistency review 2026-05-22: actionable inconsistency found. `design.md` inventories extension/runtime registration via `components/agent-session/src/psi/agent_session/extensions/runtime_fns.clj` `:register-deterministic-operation-fn` as a required higher seam and requires tests to prove both runtime invoke/cleanup coherence and extension-introspection projection coherence, but `steps.md` only reserves explicit proof work for runtime registration and reload/unregister cleanup. It does not include a concrete execution item to verify the extension-introspection projection surfaces named in the design (`operation-ids-in`, `extension-detail-in`, `extension-details-in`, and `summary-in`), so the execution checklist can satisfy its listed seams while still omitting one of the acceptance-critical higher-surface proof families.
- 2026-05-22 ψ design-follow-up(inconsistency): added a dedicated `steps.md` checklist item reserving focused extension-introspection projection proofs for `operation-ids-in`, `extension-detail-in`, `extension-details-in`, and `summary-in`, then marked the newly added `design-steps.md` follow-up done.
- 2026-05-22 ψ implementation: inventoried the active seams before changing code. Write seams exercised by this pass: direct `deterministic-operation-registry/register-operation-in!`, extension runtime registration via `extensions.runtime-fns/:register-deterministic-operation-fn`, extension unload cleanup via `extensions/unregister-extension-in!`, and reload cleanup via `extensions/unregister-all-in!` / `reload-extensions-in!`. Read/projection seams verified or updated in this pass: `get-operation-in`, `all-operations-in`, `operation-ids-in`, `operation-count-in`, `invoke-operation-in`, and extension projection surfaces `operation-ids-in`, `extension-detail-in`, `extension-details-in`, and `summary-in`.
- 2026-05-22 ψ implementation: migrated `psi.deterministic-operation-registry.registry` onto shared `root-registry` storage under dedicated registry id `:deterministic-operations`. The registry object state is now `{:root-state ...}` only; shared `root-registry` entries are canonical for operations, adapter-local state is host-only and rebuildable from the shared store, no local canonical `:operations` map remains, and no adapter-local ordering metadata remains. Adapter-owned behaviour preserved intentionally: normalization stays in `defs`, duplicate lower `:duplicate-id` results are translated into the existing public duplicate-registration throw, missing invoke ids still throw `:missing-deterministic-operation`, and extension cleanup remains nil-tolerant by translating lower `clear-by-extension` not-found results into no-op public behaviour. Runtime-owned registrations without `:ext-path` now use shared owner id `:runtime` so canonical storage remains valid without reintroducing a parallel local store.
- 2026-05-22 ψ verification: `clojure -M:test --focus psi.deterministic-operation-registry.registry-test --focus psi.agent-session.extensions-test` ✅ (32 tests, 166 assertions, 0 failures). `clj-kondo --lint components/deterministic-operation-registry/src components/deterministic-operation-registry/test components/agent-session/src components/agent-session/test` ✅ (0 errors, 0 warnings).
