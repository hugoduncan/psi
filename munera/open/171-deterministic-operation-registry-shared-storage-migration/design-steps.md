- [x] Add the missing task artifacts and their roles to the design surface: this task currently defines only `design.md`, so future review/execution passes do not have an explicit canonical place for approach (`plan.md`), implementation work (`steps.md`), and append-only review/decision notes (`implementation.md`).
- [x] Clarify the migration ordering precondition in the task artifacts: `design.md` says this task should assume task `172` is complete, but `munera/plan.md` does not place `172` before `171`, so the task needs an explicit task-local guard for whether `171` is blocked on `172` landing first or must absorb that prerequisite itself before execution starts.
- [x] Add an explicit task-artifact follow-up for recording the final authoritative-vs-derived registry-object state split after migration: `design.md` acceptance requires the task artifacts to capture that final split, but `plan.md` and `steps.md` currently stop at code/tests/seam proofs and do not reserve a concrete execution/documentation step for updating the task artifacts with that post-migration state classification.
- [x] Clarify ownership for extension introspection surfaces after migration: task `171` explicitly moves canonical deterministic-operation storage to shared `root-registry`, but task artifacts do not yet state whether extension-facing introspection like `operation-ids-in` / `extension-detail-in` in `components/agent-session/src/psi/agent_session/extensions.clj` should remain extension-registry-owned projections, be derived from the migrated deterministic-operation store, or be kept synchronized explicitly. Record the intended owner/projection rule so the migration cannot leave stale parallel `:operations` reads above the adapter boundary while still appearing coherent in invoke-only tests.
- [ ] Add an explicit execution step for extension-introspection projection proofs: `design.md` acceptance and higher-seam guards require focused proof that extension-facing projection surfaces (`operation-ids-in`, `extension-detail-in`, `extension-details-in`, and `summary-in` in `components/agent-session/src/psi/agent_session/extensions.clj`) remain coherent after the runtime canonical-owner migration, but `steps.md` currently names only runtime registration and reload/unregister seam proofs. Reserve a concrete checklist item so implementation cannot finish with invoke/cleanup coverage only while omitting the required projection-coherence proof family.
