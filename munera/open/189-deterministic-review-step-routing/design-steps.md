# Design follow-up steps

- [x] Decide whether legacy/structured review status mismatch validation is mandatory in this slice: either require `workflow/pass-status-routing` to accept structured status and return `:status-mismatch`, or remove `:status-mismatch` from this task and state that routing uses only the legacy `PASS_STATUS:` token.
- [x] Specify the exact deterministic routing shape in workflow IR/runtime terms: whether `review-status` is an `:invoke` judge attached to `review`, a separate `:invoke` step with a supported routing mechanism, or another first-class deterministic control primitive.
- [x] Define the concrete `review` step outputs/yields after adding `:review-result`, including how `:final-llm-reply` / `:yield :text` remains available for status parsing and follow-up context.
- [x] Align the `:structured-status` source path with structured-output source resolution: use `{:from {:step "review" :output :review-result} :path [:status]}` for the validated value, or explicitly design a raw-envelope ref if the operation must inspect `[:structured-output :value :status]`.
- [x] Specify how the `review` actor can simultaneously preserve the human-readable final reply with legacy `PASS_STATUS:` token and produce `:review-result` structured output when provider-native structured output is unavailable: choose provider-native-only/fallback-none for `:review-result`, allow prompted-JSON with a different prose/token contract, or otherwise define an implementable fallback behavior.
- [x] Place the review-step max-iteration guard on the transition that preserves the intended loop count under runtime semantics (likely `follow-up` → `review`), or explicitly state that the new topology intentionally counts `follow-up` executions instead of review passes.
- [x] Decide whether this task must implement and wire same-step `:structured-status {:from {:step "review" :output :review-result} :path [:status]}` for `workflow/pass-status-routing` now: if yes, require the invoke-judge self-ref exception/source-resolution behavior and mismatch tests in this slice; if no, explicitly narrow the mismatch guarantee for `review-step` and add a separate follow-up for structured-status routing.
- [x] Resolve the native structured-output vs prose `PASS_STATUS:` contract: either specify a proven provider-native mechanism that preserves an independent human-readable final reply while producing `:review-result`, or change the review-step design/tests so routing does not require both a prose legacy token and native structured output from the same actor response.
- [x] Specify parser cardinality for duplicate/extra `PASS_STATUS:` lines: duplicate identical known tokens and a known token plus any malformed `PASS_STATUS:` line must either be accepted or fail with a named deterministic reason.
- [x] Define the invoke-judge error mapping for `workflow/pass-status-routing` operation errors: how `:status :error` becomes recorded judge output, routing failure reason/details, terminal run status, and diagnostic text without executing follow-up.
- [x] Align the deterministic `follow-up` loopback with scoped/runtime-available primitives: either add `workflow/constant-routing` to this task's scope/acceptance/tests/registration requirements, or choose and specify another implementable non-LLM loopback mechanism for `follow-up` → `review` with `:max-iterations 6`.
- [x] Align loader/definition proof expectations with the deterministic topology by replacing the legacy `review-status` three-step assertions with coverage for invoke-judge routing on `review`, invoke-judge constant loopback on `follow-up`, and absence of the old session/LLM `review-status` step.
- [x] Clarify task-189 proof ownership against stale adjacent task/code surfaces: the current checked-in `.psi/workflows/review-step.edn`, loader test `components/workflow-loader/test/psi/workflow_loader/workflow_definitions_test.clj`, and task-186 notes still assert the legacy three-step `review`/`follow-up`/`review-status` shape, while task 189 marks deterministic-topology loader proof items done. Decide whether task 189 remains design-only until those repo surfaces are migrated, or restate the checked proof items/implementation notes so they do not claim landed deterministic loader coverage before code/tests/workflow definitions actually match.
- [x] Align task-189/task-186 review-step routing authority so task files stop disagreeing about the source of truth: task 189 now declares itself design-only until implementation migrates repo workflow/test surfaces, but task 186 still contains active review-step notes that describe deterministic routing as already landed behavior. Either update task 186 to mark those routing notes historical/non-authoritative in favor of task 189, or explicitly re-scope task 189 so both task directories make the same authority/implementation-state claim.
