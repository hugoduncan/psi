- [x] Extend the inventory to include non-agent-session proofs and projections that still encode built-in workflow as extension-owned state, especially `components/app-runtime/test/psi/gordian_launcher_manifest_runtime_boundary_test.clj` asserting `built-in:workflow` lives in the extension registry.
  - decided this proof should migrate to a built-in runtime-boundary invariant/projection rather than remain an extension-registry exception, because task success explicitly requires canonical built-in workflow bootstrap to stop seeding extension identity
- [x] Reconcile the prompt contribution label decision with the chosen built-in provenance model
  - [x] decide whether `# Extension Prompt Contributions` remains as temporary wording debt or changes in this task
    - keep `# Extension Prompt Contributions` unchanged in this task as deliberate temporary wording debt; this slice is about correcting built-in registration provenance, not broadening into prompt-copy churn
  - [x] inventory and update/record affected prompt assembly and proof surfaces, including `components/agent-session/test/psi/agent_session/prompt_lifecycle_test.clj`, so task artifacts stop implying the label is both preserved behavior and removable wording debt
    - affected rendering/proof surfaces recorded: `components/prompt-assets/src/psi/prompt_assets/system_prompt.clj`, `components/agent-session/test/psi/agent_session/prompt_lifecycle_test.clj`, and `components/turn-runtime/test/psi/turn_runtime/request_test.clj`; these should preserve the current heading while the built-in workflow contribution stops reaching the shared prompt store through extension-shaped registration
- [x] Make the built-in lifecycle event scope explicit against the existing session lifecycle surface
  - [x] classify whether built-in workflow depends only on `session_switch` or also on any of `session_before_switch`, `session_before_fork`, or `session_fork`, because `session_lifecycle.clj` dispatches all of them through extension events today while the task artifacts currently migrate only `session_switch`
    - inventory result: `components/agent-session/src/psi/agent_session/workflow/core.clj` registers only `(:on api) "session_switch"`; no built-in workflow code registers handlers for `session_before_switch`, `session_before_fork`, or `session_fork`
  - [x] if built-in workflow intentionally depends only on `session_switch`, record that as an explicit non-goal/invariant so the built-in lifecycle registration path is not left ambiguous relative to the broader extension lifecycle surface
    - invariant: this task migrates only built-in workflow's `session_switch` dependency to a built-in lifecycle path; `session_before_switch`, `session_before_fork`, and `session_fork` remain extension-only lifecycle surfaces and are explicit non-goals for this slice unless later workflow behavior adds a concrete dependency on them
- [x] Add the built-in provenance identifier decision explicitly to the task artifacts
  - [x] decide whether `built-in:workflow` survives only as a non-extension provenance key or is replaced entirely by another built-in identifier
    - retain `built-in:workflow` as the stable built-in provenance identifier for workflow-owned surfaces; remove it only from extension-owned identity state and proofs that equate it with an extension install
  - [x] classify every affected projection/proof that currently treats `built-in:workflow` as extension identity, including `components/agent-session/src/psi/agent_session/resolvers/extensions.clj`, `components/agent-session/test/psi/agent_session/workflow_built_in_targeting_test.clj`, `components/agent-session/test/psi/agent_session/workflow_reload_runtime_test.clj`, `components/agent-session/test/psi/agent_session/workflow_async_path_test.clj`, and `components/app-runtime/test/psi/gordian_launcher_manifest_runtime_boundary_test.clj`, so implementation has an unambiguous migration target for built-in visibility and preserved command/tool lookup behavior
    - `resolvers/extensions.clj`: narrow extension-oriented projections to true extensions only; if built-in visibility is still needed, expose it through a built-in-aware projection rather than extension-registry projection
    - `workflow_built_in_targeting_test.clj`: stop fetching the delegate tool from `[:extensions built-in:workflow :tools ...]`; prove explicit runtime session targeting through the built-in registration/lookup path instead
    - `workflow_reload_runtime_test.clj`: stop proving built-in command/tool preservation only through extension-registry query helpers; prove reload preservation through built-in-aware command/tool availability after namespace reload
    - `workflow_async_path_test.clj`: stop treating command lookup from extension-registry-backed storage as proof of built-in ownership; prove the async delegate path through built-in-aware command resolution instead
    - `gordian_launcher_manifest_runtime_boundary_test.clj`: replace the extension-registry assertion on `built-in:workflow` with a runtime-boundary invariant proving workflow built-ins are available without extension identity seeding
