2026-06-05 architecture review: Design fits the app-runtime/RPC/Emacs ownership split by preferring the adapter-neutral footer projection and backend/RPC invalidation path. New architectural follow-up AR1: tighten the fallback clause so `session/updated` may only trigger delivery/recomputation of the app-runtime-owned footer projection, not introduce Emacs-local retry footer formatting or a parallel retry display.

2026-06-05 follow-up execution: AR1 completed. Updated `design.md` fallback note to allow `session/updated` only as a trigger/reuse path for the app-runtime-owned footer projection when `footer/updated` is unavailable, and to explicitly forbid Emacs-synthesized retry footer wording or a parallel Emacs-only retry display. Marked AR1 done in `design-steps.md`. No code, plan, or implementation steps touched.

2026-06-05 ambiguity review: Found two new actionable design ambiguities. DR1: clarify whether the time-dependent `retry in Ns` footer must refresh/count down during a single backoff window, or whether one emission at retry activation is sufficient. DR2: clarify the required Emacs-visible event coverage matrix for active/change/clear retry states, especially whether `session/updated`-fallback-only delivery must be tested in addition to the primary `footer/updated` path.


2026-06-05 follow-up execution: DR1 and DR2 completed. DR1 resolved freshness by making retry footer refresh event-driven: render on retry activation, backend-published retry changes, and clear; no Emacs-local per-second countdown timer or parallel countdown model is required. DR2 added the required coverage matrix: backend/RPC trigger proof plus Emacs-visible `footer/updated` active/change/clear coverage, with separate fallback coverage only if the implementation relies on `session/updated` because `footer/updated` is unavailable. Marked DR1/DR2 done in `design-steps.md`. No `plan.md`, `steps.md`, or code touched.

2026-06-05 inconsistency review: Reviewed `design.md` against the referenced app-runtime footer/retry formatter, RPC event payload/emission helpers, and Emacs event/footer projection handlers. No new actionable inconsistencies found. The design consistently keeps retry wording in the app-runtime footer projection, uses `footer/updated` as the primary Emacs-visible path, constrains any `session/updated` fallback to triggering/reusing that projection, and requires visible-rendering coverage for active/change/clear states without contradicting the referenced artifacts.

2026-06-05 plan/steps ambiguity review: Found two new actionable ambiguities. PA1: define the backend/RPC trigger test seam so it proves the real retry-state mutation/invalidation path, not just direct footer payload construction or manual event emission. PA2: define what counts as a materially changed retry state for changed-state publication/coverage, so implementers do not under-test only an arbitrary retry-map edit.

2026-06-05 follow-up execution: PA1 and PA2 completed. PA1 clarified the Slice 1 backend/RPC trigger test seam in `plan.md` and `steps.md`: the retry activation test must drive the real provider retry-state mutation/invalidation edge used by retry scheduling, not only construct `footer-updated-payload` or manually emit `footer/updated`. PA2 clarified changed-retry coverage in `plan.md` and `steps.md`: use a backend-published retry change that alters visible footer text, with representative fields including wait/resume time, attempt/source, or rate-limit remaining/reset detail; do not rely on an arbitrary retry-map edit whose projected footer text is unchanged. Marked PA1/PA2 done in `steps.md`. No code, tests, docs, or changelog touched; these were plan/steps clarification follow-ups.

2026-06-05 plan/steps inconsistency review: Reviewed `plan.md`, `steps.md`, `implementation.md`, prior design follow-ups, and referenced footer/retry/RPC/Emacs event artifacts. No new actionable inconsistencies found. The plan and steps consistently require the primary `footer/updated` path, backend/RPC trigger proof through the real retry-state mutation/invalidation edge, visible-text-changing retry-change coverage, active/change/clear Emacs-visible rendering coverage, and a constrained `session/updated` fallback only if the primary path is unavailable.

2026-06-05 implementation pass: Completed Slice 1 and the activation/clear parts of Slice 2. Traced provider retry writes to `turn-runtime.core/mark-active-retry!` and clears to `clear-active-retry!`; both update canonical session `:retry`/`:retry-attempt`. The missing Emacs-visible edge was the RPC progress loop: it refreshed footer on `:tool-result` only, while provider-boundary retry state changes emitted no progress event and therefore no in-backoff `footer/updated`. Added `:retry-updated` progress events after retry mark/clear; RPC progress draining now treats `:retry-updated` as a footer-refresh-only event. Also set active retry maps to `:active? true`, matching `retry-display/retry-status-text`/footer ownership. Added backend/RPC regression coverage driving `execute-prepared-request!` through the real provider retry scheduling mutation, using the real progress loop to assert a `footer/updated` containing app-runtime retry text and matching `:session-id`, followed by a clear footer with stale retry text removed. No `session/updated` fallback used. Remaining concrete work: changed-retry coverage/fix if needed, Emacs-visible rendering tests, final verification/docs assessment.

2026-06-05 implementation pass: Completed remaining Slice 2 changed-retry coverage, Slice 3 Emacs-visible rendering coverage, Slice 4 fallback decision, and Slice 5 verification. Extended the backend/RPC retry footer regression to schedule two real provider-boundary retries through `execute-prepared-request!`: first `Retry-After: 8`/remaining `0`, then a backend-published changed retry `Retry-After: 4`/remaining `2`, asserting fresh `footer/updated` payloads contain `retry in 8s` and later `retry in 4s · remaining 2/5000`, followed by clear with stale retry text removed. Added Emacs UI `footer/updated` tests that assert rendered buffer/projection footer text for active retry, changed retry replacement, and clear removal; Emacs receives projected text and does not synthesize retry wording. No `session/updated` fallback is used or required: the primary backend/RPC `footer/updated` path covers activation, visible change, and clear. No user docs update needed because no documented retry/footer command or event contract changed; CHANGELOG already records the user-visible bug fix. Verification: `bb clojure:test:scry --namespace psi.rpc-prompt-test`, `psi.app-runtime.footer-test`, `psi.turn-runtime.response-mode-test`; focused Emacs retry footer ERT selector; full `bb emacs:test`; full `bb clojure:test:unit`; `clj-paren-repair` on touched Clojure test; targeted `clj-kondo` 0 errors/2 pre-existing unresolved-var warnings in `turn-runtime.core`. PASS_STATUS: IMPLEMENTATION_COMPLETE.

2026-06-05 implementation review: Reviewed production changes in turn-runtime retry state publication and RPC progress/footer emission, plus backend/RPC and Emacs-visible tests against `design.md` and `plan.md`. No new actionable implementation issues found. The implementation keeps app-runtime as retry footer owner, publishes `footer/updated` through the RPC progress boundary for retry activation/change/clear, avoids an Emacs-local retry formatter/fallback, and covers visible active/change/clear rendering. Focused review verification reran `bb clojure:test:scry --namespace psi.rpc-prompt-test` and `bb emacs:test` (full Emacs suite; selector flag ignored by task runner), both green. PASS_STATUS: REVIEW_COMPLETE.

2026-06-05 test review: Reviewed backend/RPC and Emacs-visible retry footer tests against `design.md`, `plan.md`, and the referenced retry/footer/RPC/Emacs event code. No new actionable test issues found. Backend/RPC coverage drives `execute-prepared-request!` through the provider retry scheduling path with real retry-state mutation/progress-loop footer emission, covering activation, visible changed retry metadata, and clear. Emacs coverage asserts rendered projection/buffer footer text for active/change/clear `footer/updated` events and feeds backend-projected text rather than formatting retry wording locally. Focused `bb clojure:test:scry --namespace psi.rpc-prompt-test` green. PASS_STATUS: REVIEW_COMPLETE.

2026-06-05 test-shaper review: Reviewed backend/RPC retry footer trigger tests and Emacs-visible footer rendering tests for clarity, signal, robustness, determinism, and economical coverage. No new actionable test-shaping issues found. The backend/RPC test exercises one coherent boundary contract through the real provider retry scheduling path and progress loop; the Emacs tests split active/change/clear visible rendering into clear behaviors, assert projected buffer/footer text rather than internal retry storage, and avoid local retry formatting assumptions. PASS_STATUS: REVIEW_COMPLETE.

2026-06-05 docs review: Reviewed user-facing docs for the implemented retry footer visibility fix. `CHANGELOG.md` contains an Unreleased Fixed entry for the user-visible bug fix. No README/doc guide update is needed because no user command, flag, configuration, documented RPC topic, or documented footer/event contract changed; the fix makes the already-documented `footer/updated` path carry the existing app-runtime retry footer projection at the right times. No new actionable documentation issues found. PASS_STATUS: REVIEW_COMPLETE.

2026-06-05 code-shaper review: Found one new actionable code-shaping issue. CS1: `clear-active-retry!` always mutates session retry fields and emits `:retry-updated`, so ordinary successful/non-retry prompt completions publish an extra footer-refresh progress event even when there was no active retry state to clear. This weakens the event's meaning and adds avoidable footer churn at the retry/publication boundary. Shape the clear path so retry-clear publication is conditional on an actual retry/attempt/abort state transition while preserving clear-after-retry behavior. PASS_STATUS: ACTIONABLE_FEEDBACK.

2026-06-05 follow-up execution: CS1 completed. Shaped `clear-active-retry!` so it first checks the existing session state and only clears/emits `:retry-updated` when retry-related state is actually present: active `:retry`, positive `:retry-attempt`, or `:provider-retry-abort-requested?`. Ordinary successful/non-retry completions no longer publish a retry footer refresh, while clear-after-retry and abort cleanup still publish because those states satisfy the guard. Added `execute-prepared-request-success-without-retry-does-not-emit-retry-updated-test` to lock the non-retry success path. Verification: `bb clojure:test:scry --namespace psi.turn-runtime.response-mode-test` green (19 tests/124 assertions), `bb clojure:test:scry --namespace psi.rpc-prompt-test` green (5 tests/44 assertions), `clj-paren-repair` success, targeted `clj-kondo` 0 errors/3 pre-existing unresolved-var warnings. PASS_STATUS: REVIEW_COMPLETE.
