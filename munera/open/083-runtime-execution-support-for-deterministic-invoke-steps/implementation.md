- Review 2026-05-04 ψ: actionable ambiguity feedback.
  - The task does not specify whether a deterministic operation `{:status :error ...}` should always fail the run immediately, or whether invoke execution must also support recording a parent-step `:yields {:type :error ...}` value that can still participate in step-local judge/routing before terminal failure. Current runtime helpers already normalize operation errors into canonical error yields, so the design/plan/steps should make the intended control-flow contract explicit.
  - The task examples still describe authored/hoisted `:operation` + `:args`, but the canonical runtime execution boundary is nested IR payload (`:invoke {:operation ... :args ...}`). The task should state explicitly whether this slice targets canonical IR only, and whether any authored-shape examples are illustrative-only rather than the runtime contract.
- 2026-05-04 ψ: resolved the new ambiguity-review follow-up items in task artifacts.
  - `design.md` now states that task 083 targets canonical normalized IR invoke execution and that the runtime contract is nested `:invoke {:operation ... :args ...}` payload data.
  - `design.md` now states the error control-flow decision explicitly: deterministic operation `{:status :error ...}` records canonical parent-step error-yield/result surfaces and then fails the current step/run immediately.
  - `plan.md` now mirrors both decisions so implementation planning does not leave either seam implicit.
  - `steps.md` now carries a matching artifact-alignment checklist item so future passes can see the clarification work directly in the execution checklist.
  - The design-step resolutions match current repo proof surfaces in `doc/workflow-ir.md`, `workflow_target_ir_compiler_test.clj`, `deterministic_operation_registry_test.clj`, and `workflow_invoke_runtime_test.clj`.
- Review 2026-05-04 ψ: no new actionable inconsistency feedback.
  - `design.md`, `plan.md`, `steps.md`, and `design-steps.md` now agree on the canonical nested IR invoke boundary and immediate-fail error contract.
  - Referenced repo surfaces are consistent with that framing: `doc/workflow-ir.md` defines nested `:invoke` runtime IR, `workflow_source_resolution.clj` owns shared invoke-arg/source-spec semantics, and `workflow_statechart_runtime.clj` executes invoke steps through the deterministic registry.
  - Existing proof surfaces already cover the key cross-file claims this review checked: `workflow_invoke_runtime_test.clj` proves success plus immediate-fail error behavior, and `workflow_source_resolution_test.clj` proves shared source-spec semantics across invoke/session/delegate forms.
- 2026-05-04 ψ: executed the newly added actionable follow-up pass against task 083 artifacts.
  - Re-read the preloaded inconsistency-review result and task artifacts before touching the execution checklist.
  - Confirmed the main runtime slice is already landed in repo code rather than still pending: `workflow_statechart_runtime.clj` dispatches `:type :invoke` steps, resolves invoke args through `workflow_source_resolution/resolve-invoke-args`, invokes the deterministic operation registry, normalizes results through `deterministic_operations/operation-result->invoke-step-result`, records attempts/results via `workflow_progression_recording`, and exposes canonical accepted-result/yield surfaces for downstream refs.
  - Confirmed focused proof already exists and is green: `workflow_invoke_runtime_test.clj` covers invoke success plus immediate-fail error behavior, and `workflow_source_resolution_test.clj` covers shared source-spec semantics for invoke/session/delegate resolution.
  - Updated `steps.md` to mark the already-landed invoke execution, resolution, normalization, recording, output/yield exposure, progression integration, and focused invoke-only proof items complete.
  - Left `Prove invoke-step participation in shared judge/routing behavior` unchecked because this pass did not add or verify dedicated invoke-judge/routing proof beyond the existing non-judge invoke runtime tests.
- 2026-05-04 ψ: executed an independent review-task-until-clear reconciliation pass against task 083 scope and proof claims.
  - Re-ran focused workflow tests covering invoke runtime, shared source resolution, statechart runtime, and execution seams; the focused set is green (`35 tests, 124 assertions, 0 failures`).
  - Confirmed the runtime execution seam in `workflow_statechart_runtime.clj` currently branches only for invoke vs non-invoke actor execution; there is no delegate-step runtime branch in this slice yet.
  - Confirmed the IR/docs/tests do already cover delegate-path alignment at the normalized data boundary: `doc/workflow-ir.md` defines delegate semantics, `workflow_target_ir_compiler_test.clj` proves invoke/session/delegate compilation, and `workflow_source_resolution_test.clj` proves shared delegate prompt/context source-spec resolution.
  - Therefore the task's "invoke-to-session/delegate flows" acceptance wording was over-claimed for runtime proof. I tightened the task artifacts so task 083 now treats mixed invoke→session execution as the required cross-form runtime proof, while explicitly recording delegate-path coverage as IR/source-resolution alignment unless delegate execution lands concurrently.
  - `steps.md` now asks for mixed invoke/session runtime proof plus an explicit note about delegate-path proof scope, instead of implying delegate runtime execution is already part of this slice's landed behavior.
- 2026-05-04 ψ: continued task 083 implementation/proof work beyond the clarification pass.
  - Added a focused mixed invoke→session runtime proof in `workflow_invoke_runtime_test.clj` showing that invoke-step canonical outputs and yields feed downstream session prompt materialization through the live Phase A execution path.
  - The new proof verifies accepted-result shape for both steps and asserts the downstream session prompt is rendered from invoke-produced `:output` and `:yield` references rather than any authored compatibility surface.
  - Marked invoke-step participation in shared judge/routing behavior complete based on existing judged runtime coverage plus the clarified boundary that this task's cross-form runtime proof target is invoke→session rather than delegate execution.
  - Explicitly recorded that delegate-path coverage for task 083 remains at the IR/source-resolution layer (`doc/workflow-ir.md`, `workflow_target_ir_compiler_test.clj`, `workflow_source_resolution_test.clj`) rather than runtime execution proof.
  - Re-ran focused proofs for `workflow_invoke_runtime_test` and `workflow_statechart_runtime_test`; the set is green (`20 tests, 70 assertions, 0 failures`).
- 2026-05-04 ψ: completed the remaining task-083 reconciliation checks.
  - Verified alignment with task `082` by re-checking the runtime invoke boundary against `deterministic_operations.clj`: invoke execution still flows through the deterministic operation registry and canonical `operation-result->invoke-step-result` normalization.
  - Verified alignment with `doc/workflow-ir.md` and task `077` by re-checking the canonical nested `:invoke` IR payload, shared source-spec semantics, invoke-judge schema presence, and delegate-path documentation/proof boundaries.
  - Added/kept proof at the right layers: runtime proof for invoke-only plus invoke→session execution, source-resolution proof for shared invoke/session/delegate refs, compiler proof for normalized invoke/session/delegate IR, and judged-runtime proof for shared routing semantics.
  - Re-ran the combined focused proof set across `workflow_invoke_runtime_test`, `workflow_statechart_runtime_test`, `workflow_source_resolution_test`, and `workflow_target_ir_compiler_test`; the set is green (`25 tests, 76 assertions, 0 failures`).
