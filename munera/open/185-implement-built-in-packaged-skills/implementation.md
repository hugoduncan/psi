- 2026-05-26: Task created from closed design task `184-package-skills-inside-psi-easiest-path`. This task implements the chosen design: dedicated built-in skill packaging path plus resource materialization to readable disk, with explicit precedence `:extra-paths` > project > user-global > built-in.
- 2026-05-26 ambiguity review: found one actionable gap. Design/plan/steps require managed materialization and non-source-tree proof, but they do not yet fix the cache-root ownership/refresh contract tightly enough for implementation, verification, and docs to converge. Added a `design-steps.md` follow-up to choose the authoritative cache location family plus version/hash reuse vs refresh semantics while preserving stable absolute paths within a runtime.
- 2026-05-26 ambiguity follow-up execution: completed the new `design-steps.md` item without touching implementation `steps.md`. The task now fixes built-in skill materialization to a psi-owned user-global cache root under `~/.psi/agent/`, chooses deterministic version/content-addressed snapshots instead of stable-in-place mutation, and defines reuse-on-match versus refresh-on-build/resource-change semantics so packaged skill paths stay stable within one runtime while upgrades remain deterministic.
- 2026-05-26 inconsistency review: found three new actionable inconsistencies across task artifacts and current skill-loading surfaces. (1) Task 185 now requires built-in-lowest explicit precedence with `:extra-paths` > project > user-global > built-in winner selection, but current `discover-skills` semantics/tests still use first-discovered wins with user-global loaded before project, so the task must explicitly choose and align the new canonical precedence behavior before implementation. (2) Acceptance/plan/steps require deterministic same-source tie handling but never choose the tie-break rule, leaving code/tests/docs free to diverge. (3) The task asks for built-in provenance in capability/introspection surfaces, but current skill read-models are `:source`/enriched-skill based rather than a named capability surface, so the exact surface and representation need to be fixed in the task artifacts.
- 2026-05-26 inconsistency follow-up execution: completed all newly added `design-steps.md` items without touching implementation `steps.md`. The task now explicitly changes canonical collision selection from accidental first-discovered-wins to precedence-aware winner selection over ordinary skill `:source` classes (`:path` > `:project` > `:user` > `:built-in`), keeps `:disabled true` loading only `:extra-paths`, fixes same-source tie handling to configured source-container order then lexicographic canonical absolute skill file path, and defines built-in provenance on existing skill read-model surfaces as ordinary `:source :built-in` metadata in summaries/detail/groupings/diagnostics.
- 2026-05-26 implementation pass: completed the first concrete built-in packaged-skills slice. Added a canonical packaged skill resource at `bases/main/resources/psi/skills/work-independently/SKILL.md`, implemented packaged built-in skill resource enumeration plus deterministic materialization under `~/.psi/agent/built-in-skills/<snapshot>`, and wired built-in discovery into `discover-skills` as ordinary file-backed `:source :built-in` skills. This pass also replaced accidental first-discovered collision behavior with explicit precedence-aware winner selection (`:path` > `:project` > `:user` > `:built-in`) and collision diagnostics that name winner and shadowed candidates. Focused tests were added for materialization, built-in discovery provenance, precedence, disabled-mode suppression, and invocation continuity. Remaining work is broader startup/build/non-source-tree integration proof, same-source tie-break proof hardening, docs, and wider verification.
- 2026-05-26 implementation pass 2: completed the user-facing docs and targeted verification slice. `README.md` now documents packaged built-in skills, runtime materialization under `~/.psi/agent/built-in-skills/<snapshot>/`, precedence/override rules, same-source tie handling, and `--no-skills` suppression semantics; `doc/workflows.md` now explains that workflow-requested skills remain ordinary file-backed skills and can be overridden by project or user definitions. Tightened snapshot determinism by including the packaged built-in resource path set in the snapshot id calculation, and added focused proof that the snapshot path changes when the packaged resource set changes. Verification for this slice: `bb test --focus psi.prompt-assets.skills-test` and `clj-kondo --lint components/prompt-assets/src components/prompt-assets/test` both passed. Remaining work is startup/root-storage integration proof, non-source-tree packaged execution proof, and broader final verification.
- 2026-05-26 implementation pass 3: completed the remaining discovery-proof slice. Added deterministic same-source tie tests proving configured container order wins before lexicographic canonical file path, and added packaged-jar proof that the built library jar contains `psi/skills/work-independently/SKILL.md`. Re-verified focused prompt-assets tests plus broader startup/read-model surfaces with `bb test --focus psi.prompt-assets.skills-test --focus psi.agent-session.model-dispatch-test --focus psi.agent-session.query-graph-tools-test --focus psi.prompt-assets.system-prompt-test`, and reran targeted prompt-assets lint successfully. Remaining open work is proving startup-discovered built-in skills are registered through the canonical session root-storage path.
- 2026-05-26 implementation pass 4: closed the remaining canonical-registration proof and repaired two verification mismatches. Re-read startup/bootstrap paths and confirmed discovered built-in skills are hydrated via `psi.agent-session.bootstrap/load-startup-resources-in!`, which writes startup skills through `psi.skill-registry.root-storage/set-skills-in-root-state` rather than any parallel store or legacy embedded session `:skills` vector. Marked the final unchecked task step done. While rerunning targeted verification, fixed precedence winner selection to actually implement the documented rule (higher source precedence, earlier container order, then lexicographically earlier canonical skill path), and moved the jar-resource proof from a stale checked-in packaging smoke artifact assumption to the real built library jar plus an explicit build-jar smoke assertion. This keeps task verification aligned with the actual packaging surface used for non-source-tree execution.
