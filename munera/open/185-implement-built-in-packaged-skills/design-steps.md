- [ ] Clarify the built-in skill materialization cache contract: choose the authoritative cache root family/location (for example XDG/cache vs psi-owned app dir), define whether the materialization path is per-version/per-content-hash or stable-in-place, and state the refresh/reuse rule that keeps packaged skill reads deterministic across upgrades while preserving stable readable absolute paths within one runtime.
