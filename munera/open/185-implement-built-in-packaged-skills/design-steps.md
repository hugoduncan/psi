- [x] Clarify the built-in skill materialization cache contract: use a psi-owned user-global cache root under `~/.psi/agent/`, materialize built-in skills into deterministic version/content-addressed snapshots, reuse snapshots when packaged build/resources match, and refresh by selecting a new snapshot path when they differ so paths stay stable within one runtime while upgrades remain deterministic.
