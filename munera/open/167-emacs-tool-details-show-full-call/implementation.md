# Implementation notes

- 2026-05-22 ambiguity review: design is directionally clear, but expanded call-detail source/format still needs tightening before planning. New follow-ups added to `design-steps.md` for raw-vs-structured argument precedence, Emacs/TUI layout parity, and extension renderer behavior.


- 2026-05-22 ambiguity follow-up execution: completed all newly added design follow-ups. `design.md` now defines full-call data precedence with raw fallback for invalid/partial parses, the shared Emacs/TUI expanded layout contract (`Call` before response details, header remains separate, nil/empty explicit, no summary truncation), and the rule that tool-specific/extension renderers may not replace generic auditable call details unless they provide the same complete call plus raw fallback. Marked the three ambiguity design-steps done.

- 2026-05-23 inconsistency review: found the task has refined `design.md`/completed `design-steps.md` but no `plan.md` or `steps.md`, which conflicts with Munera required pre-execution artifacts and leaves the implementation/test checklist absent despite design acceptance requiring focused Emacs and TUI coverage. Added one follow-up to create the missing execution artifacts before implementation.

- 2026-05-23 inconsistency follow-up execution: created `plan.md` and `steps.md` before implementation. The plan now defines the Emacs/TUI rendering approach, data-precedence decisions, risks, and verification strategy aligned with the finalized acceptance criteria. The implementation checklist now covers Emacs and TUI inspection, rendering, focused coverage, and verification. Marked the new design-step done.

- 2026-05-23 ambiguity review: found one remaining actionable ambiguity. The design requires parsed arguments to be treated as absent/incomplete/partial/suspected-partial in some cases, but does not define a concrete frontend-detectable completeness rule, so Emacs and TUI implementers could diverge on when to render parsed only vs parsed plus raw fallback. Added a design-step to make that decision explicit.

- 2026-05-23 ambiguity follow-up execution: completed the parsed/raw completeness design-step. `design.md` now defines trusted complete argument fields, excludes summary/preview/ellipsized display text from completeness, specifies when parsed-only versus parsed-plus-raw versus raw fallback rendering is required, and aligns live `tool/executing`/rehydrated canonical argument fields across Emacs and TUI. Marked the design-step done.

- 2026-05-23 inconsistency review: found one new actionable inconsistency. `design.md` acceptance requires toggling the same row closed to remove expanded call/response details for frontends that support closing detail rows, and TUI `ctrl+o` does support closing, but `plan.md`/`steps.md` only require TUI collapsed+expanded coverage while Emacs gets explicit toggled-closed coverage. Added a design-step to align TUI verification with the close behavior or explicitly document why existing coverage is sufficient.

- 2026-05-23 inconsistency follow-up execution: aligned TUI verification with the close-toggle acceptance criterion by requiring focused TUI collapsed, expanded/detail, and toggled-closed coverage in `design.md`, `plan.md`, and `steps.md`. Marked the design-step done; implementation `steps.md` items remain unexecuted as requested.

- 2026-05-23 ambiguity review: found one new actionable ambiguity. The design and acceptance describe `C-c C-t`/tool-detail toggling in row-local terms ("same row closed"), while the current Emacs implementation and TUI state use a global tools-expanded mode. Added a design-step to decide whether the change must preserve global expand/collapse semantics or introduce/require row-local detail state, and to align acceptance/tests accordingly.

- 2026-05-23 ambiguity follow-up execution: clarified tool-detail toggle granularity. The task preserves existing global Emacs `C-c C-t` and TUI `ctrl+o` tools-expanded semantics rather than introducing row-local expansion. Updated acceptance, plan decisions, and implementation test checklist to require global collapsed/expanded/toggled-closed coverage. Marked the design-step done.

- 2026-05-23 inconsistency review: re-read `design.md`, `plan.md`, `steps.md`, `design-steps.md`, implementation notes, and the referenced Emacs/TUI rendering/tests. No new actionable inconsistency feedback found; task artifacts now consistently preserve global tool-detail toggles, require generic `Call` before response details, use trusted complete argument fields with raw fallback, and require collapsed/expanded/toggled-closed coverage in both frontends.

- 2026-05-23 inconsistency follow-up execution: no newly added unchecked `design-steps.md` items remained after the preloaded inconsistency review result. Re-read `design-steps.md`, `design.md`, `plan.md`, and `steps.md`; all actionable design follow-ups are already complete, so no design/plan/steps changes were needed and implementation `steps.md` items were intentionally not executed.

- 2026-05-23 ambiguity review: re-read `design.md`, `plan.md`, `steps.md`, `design-steps.md`, implementation notes, referenced Emacs/TUI rendering/tests, and `spec/tool-output-rendering.allium`. No new actionable ambiguity feedback found; the task now unambiguously preserves global toggles, requires generic first-level `Call` details before response output, defines trusted parsed/raw completeness rules, and requires collapsed/expanded/toggled-closed coverage in both frontends.

- 2026-05-23 ambiguity follow-up execution: no newly added unchecked `design-steps.md` items remained after the preloaded ambiguity review result. Re-read `design-steps.md`, `design.md`, `plan.md`, and `steps.md`; all actionable ambiguity design follow-ups are already complete, so no design/plan/steps changes were needed and implementation `steps.md` items were intentionally not executed.

- 2026-05-23 inconsistency review: found one new actionable inconsistency. The task design/plan require expanded Emacs/TUI tool details to render a generic `Call` section before response output, but referenced `spec/tool-output-rendering.allium` still states expanded `visible_text` equals only `output.text`; that shared renderer contract would reject or obscure the new call+response expanded layout. Added a design-step to align the spec with the finalized full-call contract before implementation.

- 2026-05-23 inconsistency follow-up execution: aligned `spec/tool-output-rendering.allium` with the finalized expanded full-call contract. The expanded rendering rule now requires visible `Call` details with tool name and parsed/raw/empty argument handling, plus response/output text, instead of defining expanded visible text as response output only. Marked the design-step done; implementation `steps.md` items remain unexecuted as requested.

- 2026-05-23 ambiguity review repeat: re-read `design.md`, `plan.md`, `steps.md`, `design-steps.md`, prior implementation notes, `spec/tool-output-rendering.allium`, and referenced Emacs/TUI tool rendering/tests. No new actionable ambiguity feedback found; the design and execution checklist remain clear on global toggle semantics, generic `Call` plus response layout, parsed/raw completeness, raw fallback, and collapsed/expanded/toggled-closed coverage.

- 2026-05-23 ambiguity follow-up execution repeat: no newly added unchecked `design-steps.md` items remained after the preloaded ambiguity review result. Re-read `design-steps.md`, `design.md`, `plan.md`, `steps.md`, and `spec/tool-output-rendering.allium`; all actionable ambiguity follow-ups are already complete, so no design/plan/steps/spec changes were needed and implementation `steps.md` items were intentionally not executed.

- 2026-05-23 inconsistency review repeat after spec alignment: re-read `design.md`, `plan.md`, `steps.md`, `design-steps.md`, prior implementation notes, `spec/tool-output-rendering.allium`, and referenced Emacs/TUI tool rendering/tests. No new actionable inconsistency feedback found; the artifacts remain aligned on global toggle semantics, generic first-level `Call` details plus response output, parsed/raw completeness with raw fallback, extension-renderer constraints, and collapsed/expanded/toggled-closed coverage in both frontends.

- 2026-05-23 inconsistency follow-up execution repeat: no newly added unchecked `design-steps.md` items remained after the preloaded inconsistency review result. Re-read `design-steps.md`, `design.md`, `plan.md`, and `steps.md`; all actionable design follow-ups are already complete, so no design/plan/steps changes were needed and implementation `steps.md` items were intentionally not executed.

- 2026-05-23 ambiguity review repeat 2: re-read `design.md`, `plan.md`, `steps.md`, `design-steps.md`, prior implementation notes, `spec/tool-output-rendering.allium`, and referenced Emacs/TUI tool rendering/tests. No new actionable ambiguity feedback found; the task remains explicit about global toggle semantics, generic first-level `Call` details before response output, trusted parsed/raw completeness and fallback rules, extension-renderer constraints, and collapsed/expanded/toggled-closed frontend coverage.

- 2026-05-23 ambiguity follow-up execution repeat 2: no newly added unchecked `design-steps.md` items remained after the preloaded ambiguity review result. Re-read `design-steps.md`, `design.md`, `plan.md`, `steps.md`, and `spec/tool-output-rendering.allium`; all actionable ambiguity follow-ups are already complete, so no design/plan/steps/spec changes were needed and implementation `steps.md` items were intentionally not executed.

- 2026-05-23 inconsistency review repeat 2: re-read `design.md`, `plan.md`, `steps.md`, `design-steps.md`, prior implementation notes, `spec/tool-output-rendering.allium`, and referenced Emacs/TUI tool rendering/tests. No new actionable inconsistency feedback found; task artifacts remain aligned on global tool-detail toggles, generic expanded `Call` details before response/output, trusted parsed/raw completeness with raw fallback, extension-renderer constraints, and collapsed/expanded/toggled-closed coverage in both frontends.

- 2026-05-23 inconsistency follow-up execution repeat 2: no newly added unchecked `design-steps.md` items remained after the preloaded inconsistency review result. Re-read `design-steps.md`, `design.md`, `plan.md`, and `steps.md`; all actionable design follow-ups are already complete, so no design/plan/steps changes were needed and implementation `steps.md` items were intentionally not executed.


- 2026-05-23 implementation pass: implemented generic expanded `Call` details in Emacs tool rows and TUI tool rendering, preserving collapsed summaries and global toggles. Emacs expanded rows now render `Call`/`Tool`/`Arguments` before `Response`; TUI expanded rows prepend the same generic call block before result/warnings. Added Emacs and TUI collapsed/expanded/toggled-closed coverage with long nested call arguments, updated adjacent Emacs expectations for the new expanded layout, and verified `bb emacs:test --focus psi-tool-output-mode-test` plus `clojure -M:test --focus psi.tui.app-view-runtime-test` green.

- 2026-05-23 implementation review: found one actionable implementation gap. The TUI expanded rendering path prepends the generic `Call` block only when using the built-in result renderer; if an extension `:render-result-fn` is present, `render-tool-calls` renders the extension result directly and omits the generic auditable `Call` section, contrary to the design/Allium requirement that extension renderers may not replace full call details unless they prove equivalent completeness.

- 2026-05-23 scope update: user explicitly dropped TUI equivalence from task 167. Updated design, plan, steps, and design-steps so 167 is Emacs-scoped only. The prior implementation-review gap about TUI extension `:render-result-fn` coverage is no longer an acceptance blocker for this task; any remaining TUI parity/extension-renderer work should be handled by a separate task if desired.

- 2026-05-23 implementation review: found one actionable Emacs gap. `psi-emacs--tool-call-arguments-detail` always prefers non-nil `parsed-args` over raw `arguments`; for tools whose lifecycle telemetry intentionally projects/filter parsed args (notably `psi-tool/telemetry-args`) while raw `arguments` remains the complete invocation, expanded details can omit auditable raw fields despite the design requiring raw fallback when parsed completeness cannot be proven.
