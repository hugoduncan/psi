# Implementation notes

- 2026-05-24 ambiguity review: found actionable ambiguities in the task artifacts. `plan.md` and `steps.md` are absent, so sequencing/verification cannot be reviewed yet. Added design follow-ups for exact Anthropic JSON Schema request/header contract, direct response/stream extraction shape, built-in model mechanism assignment, native mechanism selection between JSON Schema and forced tool use, and live smoke invocation/skip criteria.

- 2026-05-24 inconsistency review: found no new actionable inconsistency feedback. Existing design-step coverage already captures the only blocking cross-artifact mismatch: `plan.md`/`steps.md` are absent, so sequencing cannot yet be compared against design; current design/code/docs mismatches around Anthropic forced-tool-only capability, exact JSON Schema request/response surfaces, model assignment, mechanism selection, and live smoke policy are already represented by unchecked `design-steps.md` items.

- 2026-05-24 executed inconsistency follow-up items: created `plan.md` and `steps.md`; refined `design.md` with exact Anthropic JSON Schema `output_format` request shape, `structured-outputs-2025-11-13` beta header, response/stream extraction and parse-failure surfaces, concrete current-catalog model mechanism assignment, JSON Schema-vs-forced-tool selection policy, and guarded live smoke invocation/skip/non-secret note format. Marked all existing `design-steps.md` items done.

- 2026-05-24 ambiguity review repeat: found new actionable ambiguity feedback after reviewing refined design/plan/steps plus current AI model/provider code and task 170. Documentation scope names "AI docs" and "task 170 dependency text" without exact files/sections, making the docs step unverifiable. Live-smoke scope says Anthropic OAuth/API but the current provider seam visibly resolves `:api-key`/`ANTHROPIC_API_KEY`; the concrete OAuth resolver path to exercise is unspecified. Added focused follow-up items; avoided duplicating earlier request/response/model-selection smoke-shape items.

- 2026-05-24 executed ambiguity follow-up repeat: clarified exact documentation targets/sections for task 171 (`components/ai/README.md`, `doc/custom-providers.md`, and task 170 design dependency/out-of-scope wording) and specified the live-smoke credential seam as the Anthropic provider `:api-key`/`ANTHROPIC_API_KEY` path. OAuth smoke is only in scope when an OAuth bearer token is supplied through that same provider seam; no separate OAuth resolver integration is required for task 171.

- 2026-05-24 inconsistency review repeat: found one new actionable cross-artifact inconsistency. Refined scope/testing/plan make the live smoke credential seam provider `:api-key`/`ANTHROPIC_API_KEY` with OAuth only when supplied through that seam, but acceptance criterion 11 still names a live Anthropic OAuth smoke test as the requirement. Added a design follow-up to align the acceptance wording with the refined live-smoke seam; no duplicate notes/steps were added.

- 2026-05-24 executed inconsistency follow-up repeat: aligned acceptance criterion 11 with the refined live-smoke credential seam. The acceptance requirement now names guarded Anthropic JSON Schema native smoke verification through the provider `:api-key`/`ANTHROPIC_API_KEY` seam, with OAuth only when supplied through that same seam; marked the design-step done.

- 2026-05-24 ambiguity review repeat 2: found one new actionable ambiguity after reviewing task artifacts and current Anthropic/core provider code. The design/steps require Anthropic JSON Schema native non-streaming extraction/top-level `:structured-output`, but the current Anthropic provider exposes only streaming (`:stream`) and no `:execute` non-streaming path; artifacts do not say whether task 171 must add Anthropic non-streaming execution or only factor/test an extraction helper for future non-streaming support.

- 2026-05-24 executed ambiguity follow-up repeat 2: clarified that task 171 adds an Anthropic Messages provider `:execute` non-streaming path returning top-level `:structured-output`, rather than limiting non-streaming work to helper-only extraction tests. Updated plan/steps to include that local provider execution slice; task 170 still owns workflow integration.

- 2026-05-24 inconsistency review repeat 2: found one new actionable cross-artifact inconsistency. `design.md` request behavior says `:output_format :strict true` but immediately says `:strict` follows normalized request `:strict?` and defaults true, so implementers/tests cannot know whether `:strict? false` is honored or ignored. Added a focused design follow-up; avoided duplicating prior request/header and non-streaming-boundary items.

- 2026-05-24 executed inconsistency follow-up repeat 2: resolved the Anthropic JSON Schema `:output_format :strict` inconsistency by choosing normalized request semantics. Requests default `:strict` to true when `[:structured-output :strict?]` is omitted, honor explicit `:strict? false` by sending `:strict false`, and plan/test expectations now require coverage for both cases. Marked the design-step done.

- 2026-05-24 ambiguity review repeat 3: found no new actionable ambiguity feedback after rechecking design/plan/steps/design-steps, current Anthropic/model/core structured-output code, AI docs, custom-provider docs, and task 170 dependency wording. Existing checked design-steps already resolve prior ambiguity around request/header shape, strict semantics, response/stream extraction, model assignment, mechanism selection, live-smoke seam, docs targets, and Anthropic non-streaming `:execute` scope.

- 2026-05-24 executed ambiguity follow-up repeat 3: preloaded ambiguity review found no new actionable feedback, and `design-steps.md` has no unchecked follow-up items. No `steps.md` implementation items were executed.

- 2026-05-24 inconsistency review repeat 3: found no new actionable inconsistency feedback after rechecking design/plan/steps/design-steps, prior implementation notes, current Anthropic/model/core/schema structured-output code, AI docs, custom-provider docs, and task 170 dependency wording. Existing checked design-steps and unchecked implementation steps already cover the remaining design-vs-code/docs deltas: JSON Schema native enum/helper support, model capability assignment, request/header shape, non-streaming `:execute`, streaming/non-streaming extraction, docs, task 170 wording, and live smoke seam.

- 2026-05-24 executed inconsistency follow-up repeat 3: preloaded inconsistency review found no new actionable feedback, and `design-steps.md` has no unchecked follow-up items. No `steps.md` implementation items were executed.

- 2026-05-24 ambiguity review repeat 4: found no new actionable ambiguity feedback after rechecking design/plan/steps/design-steps, prior implementation notes, current Anthropic/provider structured-output helpers, core non-streaming provider seam, model capability assignment code, AI/custom-provider docs, and task 170 dependency wording. Existing checked design-steps already resolve prior ambiguities around exact request/header shape, strict semantics, response/stream extraction, model assignment, mechanism selection, live-smoke seam, docs targets, and Anthropic non-streaming `:execute` scope; remaining unchecked `steps.md` items are implementation work, not design ambiguity.

- 2026-05-24 executed ambiguity follow-up repeat 4: preloaded ambiguity review found no new actionable feedback, and `design-steps.md` has no unchecked follow-up items. No `steps.md` implementation items were executed.

- 2026-05-24 inconsistency review repeat 4: found no new actionable inconsistency feedback after rechecking design/plan/steps/design-steps, prior implementation notes, current Anthropic/model/schema structured-output code, AI/custom-provider docs, and task 170 dependency wording. The remaining cross-artifact deltas are already represented by unchecked implementation steps: add `:anthropic/json-schema-output` schema/helper support, assign model capabilities, implement request/header/non-streaming/streaming extraction behavior, update docs/task 170 wording, add live smoke path, and run verification.

- 2026-05-24 executed inconsistency follow-up repeat 4: preloaded inconsistency review found no new actionable feedback, and `design-steps.md` has no unchecked follow-up items. No `steps.md` implementation items were executed.

- 2026-05-24 implementation pass: added `:anthropic/json-schema-output` capability/schema support; assigned JSON Schema native to documented Claude 4.5+ catalog keys and kept older/current unverified keys on forced-tool native. Anthropic request building now selects JSON Schema `output_format` plus `structured-outputs-2025-11-13` beta only for that mechanism, honors omitted strict as true and explicit `:strict? false`, and preserves forced-tool/prompted fallback separation. Added Anthropic Messages `:execute` non-streaming path with top-level `:structured-output`; streaming now accumulates JSON Schema text and emits `:structured-output-result` while preserving text deltas. Updated AI/custom-provider docs and task 170 dependency wording. Added deterministic provider/model tests plus opt-in `psi.ai.providers.anthropic-live-structured-output-test` skip path. Verification: focused Anthropic/model/user tests `33 tests, 199 assertions, 0 failures`; broader OpenAI+Anthropic/model/user structured-output tests `38 tests, 220 assertions, 0 failures`; live smoke focused default run skipped by env gate with `1 tests, 1 assertions, 0 failures`.

- 2026-05-24 live Anthropic OAuth smoke follow-up: ran guarded JSON Schema native smoke through the provider `ANTHROPIC_API_KEY` seam using the stored Anthropic OAuth access token as the effective bearer token; no token value or request headers recorded. First live run reached Anthropic and proved the beta API rejects `output_format.name` as an extra input. After removing `name`, the second live run proved `output_format.strict` is also rejected as an extra input. Updated the Anthropic JSON Schema output request/schema/tests/design/plan so outbound `output_format` is exactly `{:type "json_schema" :schema ...}` plus the `structured-outputs-2025-11-13` beta header; schema name and strictness remain Psi metadata only. Verification after fix: focused Anthropic/model/user tests `33 tests, 199 assertions, 0 failures`; live Anthropic JSON Schema native smoke `1 tests, 5 assertions, 0 failures` using model `:sonnet-4.5` / `claude-sonnet-4-5` and native mechanism `:anthropic/json-schema-output`.

- 2026-05-24 task-implementation-review: found one new actionable implementation-test gap after rechecking task artifacts, Anthropic request/execute/stream extraction code, docs, task 170 wording, and focused verification. JSON Schema output parse-failure behavior is implemented in the shared result helper, but no focused test proves non-streaming or streaming Anthropic JSON Schema native invalid/non-object output surfaces `:parse-error? true` with `:raw-payload` and no `:payload`.

- 2026-05-24 executed task-implementation-review follow-up: added focused Anthropic JSON Schema native parse-failure coverage for non-streaming `:execute` and streaming result events. Invalid JSON and non-object JSON cases now assert `:parse-error? true`, preserved `:raw-payload`, source/native mechanism `:anthropic/json-schema-output`, and omitted `:payload`. Verification: `clojure -M:test --focus psi.ai.providers.anthropic-structured-output-test --focus psi.ai.model-registry-test --focus psi.ai.user-models-test` => `35 tests, 221 assertions, 0 failures`.

- 2026-05-24 task-implementation-review repeat: found no new actionable implementation feedback after rechecking task artifacts, Anthropic schema/request/execute/stream code, model capability assignment, docs/task-170 wording, live-smoke seam, and focused Anthropic/model/user tests. Existing parse-failure follow-up is implemented and verified; no new steps added.

- 2026-05-24 executed task-implementation-review repeat follow-up: preloaded implementation review found no new actionable feedback, and `steps.md` has no newly added unchecked follow-up items. No implementation steps were executed.

- 2026-05-24 task-implementation-review repeat 2: found no new actionable implementation feedback after rechecking the task artifacts, Anthropic structured-output helpers/request/header construction, non-streaming execute and streaming extraction paths, model capability assignment, docs/task-170 wording, live-smoke seam, and focused Anthropic/model/user verification. Existing parse-failure coverage remains implemented; no new `steps.md` follow-up items added. Verification: `clojure -M:test --focus psi.ai.providers.anthropic-structured-output-test --focus psi.ai.model-registry-test --focus psi.ai.user-models-test` => `35 tests, 221 assertions, 0 failures`.
