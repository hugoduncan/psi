# Implementation notes

2026-06-14 architectural-fit review — ψ: Reviewed design.md against AGENTS.md workflow-runtime boundary/VSM principles, doc/architecture.md, doc/workflow-grammar.md multi-prompt session-config rules, and the current review prompt files. The authored-workflow topology and per-prompt-output routing fit the architecture. New actionable misfit found: A1 — the design says the merged prompt-groups reuse the existing review prompt files, but prompt-group `:prompt-workflow` imports only markdown bodies while tools/skills are shared at the step level. Without an explicit union step config, the merge would silently drop the architecture/task-design/work-independently skills and required tools from the current prompt frontmatter. Added A1 to design-steps.md. PASS_STATUS: ACTIONABLE_FEEDBACK

2026-06-14 follow-up A1 — ψ: Completed. Updated design.md to require the merged `design-review` multi-prompt session step to declare the step-level union config (`read`, `bash`, `edit`, `write`; `work-independently`, `review-task-architecture`, `task-design`) because prompt-group `:prompt-workflow` imports only markdown bodies, not frontmatter capabilities. Marked A1 done.

2026-06-14 ambiguity review — ψ: Reviewed design.md against task-design criteria, workflow grammar/docs, current `review-task-design.edn`, review prompt bodies, follow-up prompt, and pass-feedback routing. New actionable ambiguities found: B1 exact merged route map / max-iteration placement; B2 concrete prompt changes required for the “read once in turn 1” contract; B3 single follow-up selection semantics for all items added by the three-prompt batch vs stale unchecked items. Added B1–B3 to design-steps.md. PASS_STATUS: ACTIONABLE_FEEDBACK

2026-06-14 follow-up B1-B3 — ψ: Completed. Updated design.md with the exact merged `design-review`/`design-follow-up` topology, direct post-drain `pass-feedback-routing`, `DONE` to `final-summary`, `REPEAT` to `design-follow-up`, and `:max-iterations 6` on the transition back to `design-review`; specified prompt-body changes so turn 1 loads design + architecture sources and later prompts reuse shared context with targeted re-reads only; clarified that the single design follow-up executes unchecked items newly added by the whole preceding three-prompt batch and that `review-follow-up-design.md` wording should be updated accordingly. Marked B1–B3 done.

2026-06-14 inconsistency review — ψ: Reviewed design.md against the current `review-task-design.edn`, review prompt bodies, `doc/workflow-grammar.md` multi-prompt semantics, and `workflow/pass-feedback-routing` implementation. New actionable inconsistency found: C1 — the design says the merged post-drain judge reuses existing `pass-feedback-routing` and routes DONE only when all three prompt replies returned `REVIEW_COMPLETE`, but the existing operation only checks whether any reply has `PASS_STATUS: ACTIONABLE_FEEDBACK`; in the current unmerged topology each phase's own `pass-status-routing` validates malformed/missing statuses before `clarity-status`, and the merged topology removes those per-phase judges. Added C1 to design-steps.md. PASS_STATUS: ACTIONABLE_FEEDBACK

2026-06-14 follow-up C1 — ψ: Completed. Updated design.md to resolve the merged topology's routing-status validation inconsistency by requiring an in-place semantic tightening of `workflow/pass-feedback-routing`: every supplied prompt/phase reply must parse as exactly one allowed `PASS_STATUS: ACTIONABLE_FEEDBACK|REVIEW_COMPLETE` before the operation chooses `REPEAT`/`DONE`; missing, duplicate, malformed, or disallowed statuses now produce deterministic operation errors instead of silent `DONE`. Updated scope and acceptance criteria to require routing tests for valid and invalid pass-feedback cases. Marked C1 done.

2026-06-14 architectural-fit review — ψ: Re-reviewed design.md against AGENTS.md VSM/workflow-runtime boundary principles, META.md session/runtime ownership model, doc/architecture.md, and the 226 multi-prompt grammar/docs. No new actionable architectural-fit feedback found: the topology remains authored workflow policy rather than runtime policy, per-prompt output routing stays replay/data-flow based instead of filesystem-state based, shared step-level session capabilities match the multi-prompt contract, and final-summary per-prompt refs use the supported source-ref surface. No design-steps.md changes needed. PASS_STATUS: REVIEW_COMPLETE

2026-06-14 ambiguity re-review — ψ: Re-reviewed design.md for ambiguity against task-design criteria, doc/workflow-grammar.md multi-prompt semantics, current review-task-design/review-task-plan workflow definitions, review prompt bodies, review-follow-up-design.md, doc/workflows.md, and current pass-feedback-routing implementation. No new actionable ambiguity feedback found: the design now specifies the merged route map, step-level shared session capabilities, prompt-body context-loading rules, post-batch follow-up scope, pass-feedback validation semantics, final-summary per-prompt refs, scope, and acceptance criteria clearly enough for planning. No design-steps.md changes needed. PASS_STATUS: REVIEW_COMPLETE

2026-06-14 inconsistency re-review — ψ: Reviewed design.md against current Munera/task state, the closed 226 task artifacts, current multi-prompt workflow grammar/docs, review-task-design.edn, review prompt bodies, and pass-feedback-routing. New actionable inconsistency found: C2 — design.md still says 227 is ready only once 226 lands and warns not to plan/implement before 226's grammar is real, but 226 is now closed/landed and the grammar/docs are present. Added C2 to design-steps.md. PASS_STATUS: ACTIONABLE_FEEDBACK

2026-06-14 follow-up C2 — ψ: Completed. Updated design.md Status/Depends-on to state that task 226 has landed and the prerequisite is satisfied, while preserving that 227 consumes existing multi-prompt surfaces and adds no new runtime capability. Marked C2 done.

2026-06-14 architectural-fit review — ψ: Re-reviewed design.md against AGENTS.md VSM/workflow-runtime-boundary principles, META.md session/runtime ownership model, doc/architecture.md, doc/workflow-grammar.md multi-prompt semantics, doc/workflows.md review-loop contract, and the current review workflow/prompt artifacts. No new actionable architectural-fit feedback found: the design keeps topology/policy in authored workflow definitions, uses generic deterministic routing over recorded per-prompt outputs rather than mutable filesystem state, declares shared step-level session capabilities per the multi-prompt contract, preserves replay/data-flow-oriented final-summary refs, and treats batch-review-then-follow-up as an explicit non-compat topology decision rather than a shim. No design-steps.md changes needed. PASS_STATUS: REVIEW_COMPLETE

2026-06-14 ambiguity review — ψ: Re-reviewed design.md for ambiguities against task-design criteria, doc/workflow-grammar.md multi-prompt semantics, doc/workflows.md review/follow-up contract, current review-task-design.edn, review prompt bodies, review-follow-up-design.md, and pass-feedback-routing implementation. New actionable ambiguity found: D1 — the design clearly states that the single post-batch follow-up must execute only unchecked design-steps newly added by the immediately preceding three-prompt batch, but the proposed `design-follow-up` step receives only the generic follow-up prompt and no explicit evidence/context mechanism to distinguish batch-added items from pre-existing stale unchecked items. Added D1 to design-steps.md. PASS_STATUS: ACTIONABLE_FEEDBACK

2026-06-14 follow-up D1 — ψ: Completed. Updated design.md to make `design-follow-up` item selection explicit while preserving the shared `review-follow-up-design.md` prompt shape: the follow-up uses task-scoped git history and `design-steps.md` diffs to identify unchecked checklist items added by the immediately preceding contiguous three-prompt review batch, executes only those still-unchecked candidate items, leaves stale/pre-existing items untouched, and records a terse block note instead of guessing when the batch baseline or item match is ambiguous. Updated acceptance criterion 3 accordingly. Marked D1 done.

2026-06-14 inconsistency review — ψ: Re-reviewed design.md against the current `review-task-design.edn`, review prompt bodies, `review-follow-up-design.md`, `doc/workflow-grammar.md`/concepts multi-prompt and per-prompt source-ref semantics, current `pass-feedback-routing`/`pass-status-routing` implementation, `doc/workflows.md`, and closed task 226 artifacts. No new actionable inconsistency feedback found: the design is internally consistent about the deliberate batch-review-then-follow-up topology, step-level union session config, validated three-output pass-feedback routing, per-prompt final-summary refs, 226 dependency status, and git/task-file evidence rule for post-batch follow-up item selection. No design-steps.md changes needed. PASS_STATUS: REVIEW_COMPLETE

2026-06-14 architectural-fit review — ψ: Re-reviewed design.md only for architectural fit against AGENTS.md VSM/workflow-runtime boundary, META.md session/runtime ownership, and doc/architecture.md adapter/runtime ownership principles. No new actionable architectural-fit feedback found: the design keeps workflow topology/policy in authored workflow definitions, uses deterministic per-prompt output routing instead of mutable filesystem-state routing, preserves shared-session capability ownership at the step config, and avoids shims by making the batch-review-then-follow-up topology explicit. No design-steps.md changes needed. PASS_STATUS: REVIEW_COMPLETE

2026-06-14 ambiguity review — ψ: Independently re-reviewed design.md for ambiguities against task-design criteria, current multi-prompt workflow grammar/docs, `review-task-design.edn`, the three review prompt bodies, `review-follow-up-design.md`, `doc/workflows.md`, and current `pass-feedback-routing`/`pass-status-routing` behavior. No new actionable ambiguity feedback found beyond already-resolved A1/B1-B3/C1-C2/D1: the design now unambiguously states the merged route map, shared step capabilities, prompt context-loading/re-read rules, validated per-prompt routing, post-batch follow-up evidence rule, final-summary refs, scope, and acceptance criteria. No design-steps.md changes needed. PASS_STATUS: REVIEW_COMPLETE

2026-06-14 inconsistency review — ψ: Independently re-reviewed design.md for internal consistency and against the referenced workflow artifacts: current `review-task-design.edn`, the three review prompt bodies, `review-follow-up-design.md`, doc/workflow-grammar.md multi-prompt rules, doc/workflow-grammar-concepts.md/source-ref semantics, 226's closed design, current `pass-feedback-routing`/`pass-status-routing`, and doc/workflows.md's existing review-loop description. No new actionable inconsistency feedback found: the design consistently frames 227 as a deliberate topology redesign, requires the correct step-level union session config for prompt-group bodies, routes over validated per-prompt `:final-llm-reply` outputs, preserves the six-review-pass bound by targeting `design-review`, migrates final-summary to legal per-prompt output refs, and defines post-batch follow-up item selection via git/task-file evidence rather than routing on filesystem state. No design-steps.md changes needed. PASS_STATUS: REVIEW_COMPLETE

2026-06-14 plan ambiguity review — ψ: Reviewed plan.md and steps.md against design.md, current `review-task-design.edn`, the review/follow-up prompt bodies, `workflow/pass-feedback-routing`/`pass-status-routing`, workflow grammar/concepts docs for multi-prompt/per-prompt refs, existing workflow definition tests, review-routing runtime tests, and doc/workflows.md. No new actionable plan/steps ambiguity feedback found: the plan and checklist specify the slice order, merged topology, shared step config, validated per-prompt routing, prompt/follow-up contract updates, definition/runtime/doc test coverage, verification commands, and changelog work clearly enough to implement. No steps.md changes needed. PASS_STATUS: REVIEW_COMPLETE

2026-06-14 plan inconsistency review — ψ: Reviewed plan.md and steps.md against design.md and referenced workflow/runtime/test/docs artifacts. New actionable inconsistency found: P1 — plan.md states tests should be updated before or with each behavioral change, but steps.md schedules the workflow-definition, prompt-contract, and review-routing test alignment only in Slice 4 after the Slice 2 topology rewrite and Slice 3 prompt contract changes. Added an unchecked Slice 4 follow-up to either move/add those test updates alongside the behavioral-change slices or revise the plan requirement. PASS_STATUS: ACTIONABLE_FEEDBACK

2026-06-14 follow-up P1 — ψ: Completed. Resolved the plan/steps test-timing inconsistency by revising plan.md to remove the stricter “before or with each behavioral change” requirement; Slice 4 remains the explicit definition/runtime alignment gate after routing/topology/prompt edits and before docs/final verification. Marked the P1 follow-up done in steps.md.

2026-06-14 plan ambiguity re-review — ψ: Re-reviewed plan.md and steps.md for ambiguities after the P1 follow-up, against design.md, design-steps.md, current review-task-design/review-task-plan workflow definitions, review prompt bodies, review-follow-up-design.md, workflow grammar/concepts docs for multi-prompt and per-prompt refs, pass-feedback/pass-status routing code, workflow definition tests, review-routing tests, and doc/workflows.md. No new actionable plan/steps ambiguity feedback found: the plan and checklist now state the routing validation, merged topology, prompt/follow-up contracts, definition/runtime test alignment gate, docs/changelog updates, and verification work clearly enough to implement. No steps.md changes needed. PASS_STATUS: REVIEW_COMPLETE

2026-06-14 plan inconsistency re-review — ψ: Re-reviewed plan.md and steps.md for inconsistencies after the P1 follow-up, against design.md, design-steps.md, current review-task-design/review-task-plan workflow definitions, review prompt bodies, review-follow-up-design.md, workflow grammar/concepts docs, pass-feedback/pass-status routing code, workflow definition tests, review-routing tests, doc/workflows.md, and the prior implementation notes. No new actionable inconsistency feedback found: plan.md and steps.md now consistently describe the routing-validation slice, merged workflow topology, prompt/follow-up contract updates, dedicated definition/runtime test-alignment gate, docs/changelog work, verification cleanup, and resolved P1 test-timing decision. No steps.md changes needed. PASS_STATUS: REVIEW_COMPLETE

2026-06-14 implementation slice 1 — ψ: Tightened `workflow/pass-feedback-routing` by moving the pass-level parser into `workflow.routing`, validating every supplied reply with the existing `PASS_STATUS` grammar and allowed review statuses `ACTIONABLE_FEEDBACK|REVIEW_COMPLETE`, returning `:invalid-pass-feedback` with per-key validation failures on malformed input, and preserving `REPEAT` iff any valid reply is actionable. Added focused pure routing tests for valid DONE/REPEAT and missing/duplicate/malformed/disallowed status errors. Marked Slice 1 complete in steps.md.

2026-06-14 implementation slice 2 — ψ: Rewrote `.psi/workflows/review-task-design.edn` to the merged authored topology: one step-level configured multi-prompt `design-review` step with ordered `architecture`, `ambiguity`, and `inconsistency` prompt groups; direct post-drain `workflow/pass-feedback-routing` over per-prompt `:final-llm-reply` outputs; one `design-follow-up` using `review-follow-up-design.md`; `:max-iterations 6` on `design-follow-up --DONE--> design-review`; and final-summary sources migrated from removed per-phase `:yield` refs to legal per-prompt `:output` refs. Removed stale per-phase step names and `clarity-status`. Marked Slice 2 complete in steps.md.
