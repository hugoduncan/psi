- 2026-05-24 ambiguity follow-up execution: created the missing `plan.md`, `steps.md`, and `implementation.md` task artifacts; refined `design.md` to inventory concrete prompt contribution write/read seams and to make the authoritative-storage boundary explicit. Resulting design now states that task `177` migrates canonical per-session prompt contribution storage onto `root-registry`, and any session-local `:prompt-contributions` vectors must become derived adapter-owned projection/cache state or be removed rather than remain independently authoritative.
- 2026-05-24 inconsistency review: actionable inconsistency found. `design.md` says prompt contributions should live in a dedicated per-session root-registry instance, but the current root-registry substrate is a shared top-level root-state area and current session init/resume/fork/child flows still copy raw `:prompt-contributions` vectors. Added a follow-up to define the exact root-state storage topology and the session lifecycle copy/derive rules so session scoping is unambiguous.
- 2026-05-24 inconsistency follow-up execution: completed the new `design-steps.md` item. `design.md` now replaces the incorrect per-session-root-registry wording with a shared `:root-registries` topology, makes session scoping explicit via session-owned prompt membership rather than separate registry hosts, and names the new lifecycle rule that new/resume/fork/child flows must copy or reconstruct canonical membership state and derive prompt maps from root-registry-backed definitions instead of copying raw `:prompt-contributions` vectors as authority.
- 2026-05-24 implementation pass: migrated prompt contribution authority onto a new `psi.prompt-registry.root-storage` adapter over shared `root-registry` storage. Session state now carries canonical `:prompt-contribution-ids` membership, prompt handlers mutate shared root-registry entries then rebuild derived session prompt vectors/system prompts, and prompt-request/session/extension resolver reads now source prompt contributions from root-backed authority rather than trusting stale session-local vectors. Added focused root-storage contract coverage plus higher-surface coherence coverage for introspection against stale local vectors, updated child-session tests for membership inheritance, and verified with focused prompt/session tests, targeted lint, and full `bb test`.
- 2026-05-24 follow-up implementation pass: found two workflow execution tests still seeding only legacy session-local `:prompt-contributions` vectors. Updated those fixtures to seed canonical `:prompt-contribution-ids` membership plus shared root-registry prompt entries so the tests prove migrated authority rather than stale compatibility state. Re-ran focused workflow/prompt/session verification and full `bb test`; both are green.
