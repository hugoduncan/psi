- 2026-05-24 ambiguity review: actionable ambiguity found. This task currently only has `design.md`; `plan.md`, `steps.md`, `implementation.md`, and `design-steps.md` are missing, so the canonical task-local surfaces for execution approach, checklist tracking, append-only review notes, and design-review follow-up are implicit rather than explicit. That alone leaves the requested review target underspecified. Additional actionable ambiguity: `design.md` names broad read/projection surfaces (session-state prompt assembly seams, resolver/introspection/extension-detail projections, mutation result projections, test/helper seams) but does not yet inventory which concrete current files/seams are in-scope post-176 versus already single-id/session-vector compatibility surfaces. In particular, current code shows prompt contribution authority still lives in session `:prompt-contributions` vectors (`dispatch_handlers/prompt_handlers.clj`, `session_state/state.clj`, `resolvers/session.clj`, `resolvers/extensions.clj`, child-session copy paths, prompt-request/request prep), while task `177` says root-registry becomes authoritative; the task artifacts do not yet specify whether this migration also replaces session-local authoritative storage, or whether root-registry authority is only for extension-owned canonical contribution storage with session-local prompt snapshots/projections retained as derived/copy state. Added follow-up items to `design-steps.md`; no duplicates found.
