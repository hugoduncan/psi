- [x] Create the missing Munera task artifacts `plan.md`, `steps.md`, and `implementation.md`, and make the task-local review/execution surface explicit: `implementation.md` is append-only review/discovery log, `design-steps.md` is the design-review follow-up checklist, and `steps.md` is reserved for executable implementation work.
- [x] Inventory the concrete write seams and read/projection seams named only broadly in `design.md`, distinguishing which current files are in scope for this migration versus already-compatible higher surfaces. At minimum resolve whether task `177` must touch: `components/agent-session/src/psi/agent_session/dispatch_handlers/prompt_handlers.clj`, `components/session-state/src/psi/session_state/state.clj`, `components/agent-session/src/psi/agent_session/resolvers/session.clj`, `components/agent-session/src/psi/agent_session/resolvers/extensions.clj`, child-session prompt-contribution inheritance/copy paths, prompt-request/request-prep surfaces, extension-detail/introspection projections, and nullable helper/query surfaces.
- [x] Make the authoritative-storage boundary unambiguous: specify whether `root-registry` replaces session-local `:prompt-contributions` vectors as the canonical per-session owner, or whether `root-registry` owns extension prompt entries while session-local prompt-contribution vectors remain a derived/session-scoped projection or snapshot. If the latter, name the synchronization owner and guard tests required to prove session vectors are no longer independently authoritative.
- [ ] Resolve the storage-topology inconsistency between the design’s “dedicated per-session root-registry instance” language and the actual shared root-state `root-registry` substrate. Name the precise canonical location of prompt contributions in root state, how entries remain session-scoped there, and which session init/resume/fork/child flows copy or derive that root-registry-backed state instead of copying raw `:prompt-contributions` vectors.
