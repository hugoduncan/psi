# Design follow-up steps

## Inconsistency review
- [ ] Reconcile the Constraints "byte-identical for all currently-advertised skills/workflows" statement with the Apply-the-field/Acceptance requirement to flip some currently-advertised skills/workflows to `advertise: false` in this same task. Scope the byte-identical invariant to items whose `advertise` remains absent/`true` (as the Acceptance criterion already phrases it), so the constraint does not contradict the deliberate removal of the `review-*`/`issue-*` skills and sub-only workflows from the system context.
