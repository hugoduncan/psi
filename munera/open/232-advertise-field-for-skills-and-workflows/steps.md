# Steps — 232 `advertise` field

## Slice 1 — Mechanism
- [x] Skills: parse `advertise` in `parse-skill-file`, propagate via `->skill`.
- [x] Skills: exclude `advertise: false` in `format-skills-for-prompt` and `-lambda` (shared `prompt-hidden?`).
- [x] EDN workflows: filter `:advertise false` in `build-prompt-contribution`.
- [x] Markdown workflows: allow + parse `advertise` frontmatter; propagate into definition.
- [x] Tests for skills parse + filtering (absent/true/false; coexist with disable-model-invocation).
- [x] Tests for workflow prompt-contribution filtering and markdown parse/compile.
- [x] Run verification (clj-kondo clean + 30 tests pass).

## Slice 2 — Apply the field
- [x] Flip enumerated review-*/issue-* skills to `advertise: false` (7 skills).
- [x] Flip enumerated sub-only workflows to `advertise: false` (22 md + 8 edn).
- [x] Verify dropped from system context, still registered/invocable (live load check).

## Implementation review follow-up
- [x] Add a compiler test that an **EDN** workflow with `:advertise false` propagates into the compiled definition. The EDN path relies on implicit `config` passthrough (no explicit defaulting, unlike the markdown path) and is currently untested — a future change to EDN config handling could silently drop `:advertise` with no failing test.
- [x] Add a test (or assert in an existing one) for the acceptance criterion that a non-advertised skill/workflow remains registered and invocable by name; this is currently only live-verified, not guarded by an automated test.
- [x] Reconcile the markdown/EDN default asymmetry or document it: markdown compile sets explicit `:advertise true` when absent, while the EDN path leaves `:advertise` absent (nil). Behaviour is correct (filter uses `false?`), but the asymmetry is an inconsistency worth a deliberate decision.
- [x] Resolve the untracked `doc/agent-facets.md` (overlaps the advertise/"conditionally advertised" topic but is not part of any task commit): commit it intentionally or remove it.

## Test review follow-up
- [x] Strengthen the workflow invocability assertion: `non-advertised-workflow-stays-listed-and-invocable-test` only asserts the name appears in `available-workflows-text` (a listing-presence proxy) and infers invocability via a comment. Acceptance criterion 2 requires that `/delegate <name>` and sub-step invocation *still run* an `:advertise false` workflow. The parallel skill test performs a real `invoke-skill`; the workflow side does not exercise an actual run/sub-step. Add a test that actually executes (or resolves-for-execution through the loader/runtime) an `:advertise false` workflow, so a future change that drops it from registration/execution is caught.
- [x] Add a test that `format-skills-for-prompt-lambda` excludes `advertise: false` skills. The acceptance criterion names both `format-skills-for-prompt` **and** `-lambda` as system-context formatters that must drop non-advertised skills, but `-lambda` currently has no test at all (neither advertise filtering nor general output). Shared `prompt-hidden?` is an implementation detail; a refactor could diverge the two formatters with no failing test.

## Test review follow-up (test-shaper pass)
- [x] Add a `->skill` propagation assertion for `:advertise`. `skill-construction-test` builds its `parsed` map without `:advertise` and never asserts the field on the produced skill, so the `->skill` → `:advertise (:advertise parsed)` propagation (skills.clj L170) is unguarded. A future change that drops the key from `->skill` would make non-advertised skills default back to advertised (filter uses `(false? :advertise)`) and leak into the system context with no failing test. Assert both that explicit `:advertise false` is carried through and that the absent case behaves correctly.
- [x] Reconcile the name/assertion mismatch in `non-advertised-workflow-stays-listed-and-invocable-test`. The name claims "invocable" but the body only asserts listing presence in `available-workflows-text` and infers invocability via a comment; actual execution-resolution is now covered by `resolve-runnable-definition-test`. Rename to reflect what it actually checks (user-facing listing presence) and drop the redundant invocability inference so two tests don't appear to cover the same claim and a failure points at the real contract.

## Test review follow-up (test-shaper pass 2)
- [x] Relocate the two EDN-config advertise tests in `components/workflow-loader/test/psi/workflow_loader/compiler_test.clj` ("advertise false in edn config propagates into the compiled definition" and "advertise absent from edn config leaves advertise absent in the definition") out of the `compile-markdown-workflow-file-test` deftest. They assert EDN-config behaviour but live under a markdown-named deftest, while a dedicated `compile-edn-prompt-workflow-test` deftest already exists. Move them there (or into a focused `advertise`/edn deftest) so naming matches concern and a future EDN-config regression surfaces under an accurately named test.

## Test review follow-up (test-shaper pass 4)
- [x] Anchor the bare-substring **positive** presence assertions in the exclusion tests on the entry form, matching the negatives anchored in pass 3, so each test is internally consistent. After pass 3 the negatives are delimited (`"<name>internal</name>"`, `"internal → "`, `"- internal:"`) but the positives in the same tests stay bare: `format-skills-for-prompt-test`/`-lambda-test` exclusion cases assert `(str/includes? result "visible")` (skills_test:434,445,498,509) and `non-advertised-workflow-stays-listed-test` asserts `(str/includes? (available-workflows-text defs) "internal")` (text_test:42, bare). Bare positives are weaker and inconsistent with the anchored negatives — a regression that emits the entry in a changed shape still passes, and a failure does not point at the listing contract. Anchor each on the entry form (`"<name>visible</name>"` for the XML formatter, `"visible → "` for the lambda formatter, the listing form for `available-workflows-text`).

## Test review follow-up (test-shaper pass 3)
- [x] Tighten the bare-substring negative exclusion assertions to the actual listing format, for consistency with the delimited positive/negative assertions used in the XML formatter tests. The XML `format-skills-for-prompt` exclusion tests assert `(not (str/includes? result "<name>internal</name>"))` (delimited), but the parallel `format-skills-for-prompt-lambda-test` exclusion tests assert `(not (str/includes? result "internal"))` / `"hidden"` (bare substring, skills_test:499,510) and `build-prompt-contribution-test` / `non-advertised-workflow-stays-listed-test` / `resolve-runnable-definition-test` assert `(not (str/includes? ... "internal"))` (bare substring, text_test:20,40,54). Bare-substring negatives are weaker and inconsistent: they don't anchor on the entry contract (lambda `internal → …`; workflow listing `- internal: …`) so a regression that emits the entry in a changed shape can still pass, and a failure does not point at the listing contract. Anchor each negative on the entry form (e.g. `"internal → "` for the lambda formatter, `"- internal:"` for the workflow listing).

## Docs review follow-up (second pass)
- [x] Fix the CHANGELOG accuracy: the entry attributes one combined invocation-path list to "the skill/workflow" — "remains invocable via `/skill:name`, `/delegate <name>`, the user-facing `/delegate list`, direct file read, and as a workflow sub-step". This implies each surface is reachable via *all* listed paths, but `/skill:name` is skills-only while `/delegate <name>`/`/delegate list`/sub-step are workflows-only. A reader could conclude a skill is invocable via `/delegate` or a workflow via `/skill:name`. Split the sentence per surface (skills: `/skill:name` + direct file read; workflows: `/delegate <name>` + `/delegate list` + sub-step) or otherwise disambiguate which paths apply to which. `doc/workflows.md` already keeps the two surfaces in separate paragraphs and is accurate — only the CHANGELOG needs the fix.

## Docs review follow-up
- [x] Fix the CHANGELOG (and `doc/workflows.md`) description of which skills are flipped to `advertise: false`. Both describe them as the `review-*`/`issue-*` skills, but two of the seven actually-flipped skills — `task-implementation-review` and `task-test-review` — match neither glob. A user reading the changelog/doc would not realize those two skills are now hidden from the system context. Either enumerate the actual set or describe it accurately (e.g. "the `review-*`, `task-*-review`, and `issue-*` workflow-internal skills").

## Docs review follow-up (fourth pass)
- [x] Reconcile the sub-only-workflow example lists between the CHANGELOG `advertise` entry and `doc/workflows.md`. The CHANGELOG enumerates the flipped sub-only workflows as "`*-core` review loops, `*-final-summary`/`*-implement-pass` steps, prompt sub-reviews, `*-in-worktree` handoff wrappers, and modular `gh-*` sub-steps", but `doc/workflows.md` (the "Hiding a workflow from the model with `advertise`" section) gives the same illustrative list minus the final category ("`*-core` review loops, `*-final-summary`/`*-implement-pass` steps, prompt sub-reviews, and `*-in-worktree` handoff wrappers"). The `gh-*` sub-steps were actually flipped (6 markdown `gh-bug-*`/`gh-issue-*` plus `gh-bug-request-more-info`), so the doc's omission is a completeness gap and an inconsistency between two user-facing docs describing the same change. Add the `gh-*` sub-step category to the `doc/workflows.md` example list (or drop it from the CHANGELOG) so the two surfaces agree.

## Code-shaper review follow-up
- [x] Reconcile skill-visibility partition with `:advertise` (consistency / single-source-of-truth). Decision: "visible/available to model" means "appears in the system context". Promoted the canonical predicate to `skill-registry/prompt-hidden?` (the only place both `prompt-assets` and `tui` can share — `tui` cannot reach `prompt-assets`) plus `visible-skills`/`hidden-skills`. Routed `skill-summary` `:visible-count`/`:hidden-count`, `visible-skills`, `hidden-skills`, and `enrich-skill` `:is-available-to-model` through it; prompt-assets `prompt-hidden?` now aliases the registry one. Added registry + introspection tests asserting `advertise: false` partitions as hidden.
- [x] Remove the duplicated visibility filter in `tui/app/render.clj` `banner-rows`. Now calls `skill-registry/visible-skills`, removing the third inline copy of the rule.

## Code-shaper review follow-up (second pass)
- [x] Route the residual inline visibility partitions in `prompt-assets/skills.clj` through the canonical partition helpers (consistency / single-source-of-truth). The prior code-shaper pass promoted `prompt-hidden?` to the registry and added `visible-skills`/`hidden-skills`, but three call-sites still recompute the partition inline rather than reusing those helpers: `format-skills-for-prompt` (L520) and `format-skills-for-prompt-lambda` (L543) both `(remove prompt-hidden? (skill-registry/all-skills skills))` — exactly what `skill-registry/visible-skills` returns — and `skill-summary` (L599-600) recomputes counts via `(remove prompt-hidden? ordered-skills)` / `(filter prompt-hidden? ordered-skills)` rather than counting `visible-skills`/`hidden-skills`. The partition rule is therefore re-expressed in four places (the two helpers plus three inline recomputations) instead of one. Have the formatters consume `visible-skills` and `skill-summary` derive its counts from the partition helpers so the partition has a single source.

## Docs review follow-up (third pass)
- [x] Fix the invocability/listing category error for `/delegate list` in both the CHANGELOG `advertise` entry and `doc/workflows.md`. Both place the user-facing `/delegate list` among the *invocation* paths for a hidden workflow: the CHANGELOG says a hidden workflow "remains invocable: … via `/delegate <name>`, the user-facing `/delegate list`, and as a workflow sub-step"; `doc/workflows.md` says it "remains invocable via `/delegate <name>`, as a delegate sub-step …, and in the user-facing `/delegate list`." `/delegate list` is a listing/discovery surface (the workflow still *appears* there per design Q2), not a way to *invoke* the workflow. A reader could conclude `/delegate list` runs the workflow. Reword so the actual invocation paths (`/delegate <name>` and sub-step) are separated from the "still appears in `/delegate list`" listing claim.
