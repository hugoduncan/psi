# Steps

- [x] Update workflow loader discovery so workflow roots scan both `.md` and `.edn` files, preserve same-kind later-wins precedence, and fail clearly for mixed-kind duplicate workflow names.
- [x] Add/reshape parser and compiler seams so `.md` files compile as single-step markdown workflows and `.edn` files compile as multi-step workflow definitions.
- [x] Implement single-step `.md` validation and compilation: required `name`/`description`, explicit allowed frontmatter keys, non-empty markdown body, no EDN workflow-definition block, exactly one canonical `:session` step.
- [x] Implement `.edn` `:session` step `:prompt-workflow` support with relative path resolution, wrong-kind/missing-target errors, dual-prompt-source rejection, and step-local override of referenced markdown config.
- [x] Add focused tests for mixed file-kind discovery, duplicate-name collision behavior, single-step markdown compilation/validation, and `:prompt-workflow` reference/merge/error cases.
- [x] Update workflow authoring/reload docs to describe `.md` vs `.edn` workflow kinds, `.psi/workflows/` discovery, `:prompt-workflow` reuse, and the transitional status of existing checked-in multi-step `.md` built-ins/examples until a follow-on migration task moves them to `.edn`.
- [x] Run focused verification for loader/compiler/runtime/doc-adjacent tests and record results.
- [x] Align focused mixed-kind collision proof and residual docs/comments with the finalized mixed-kind duplicate-name load-error contract.
- [x] Reject non-relative `:prompt-workflow` references so absolute paths and `..` escapes outside the consuming workflow authoring tree fail clearly, and add focused compiler proof for the relative-only contract.
- [ ] Migrate checked-in `.psi/workflows/` artifacts so repository examples match the new file-kind contract: move multi-step EDN-bodied markdown workflows to `.edn`, and reshape any intended standalone prompt workflows into valid single-step markdown.
  - The current focused migration proof still fails because checked-in `planner.md`, `builder.md`, and `reviewer.md` remain legacy EDN-bodied markdown and many other `.psi/workflows/*.md` artifacts are still multi-step transitional files.
- [ ] Add focused proof that the checked-in `.psi/workflows/` corpus matches the finalized file-kind split contract after artifact migration, so repo-level validation covers both valid standalone single-step markdown workflows and migrated multi-step `.edn` workflows.
- [x] Resolve the repository-level mixed-kind collision state in `.psi/workflows/`: do not leave sibling same-name `.md` + `.edn` files checked in under the finalized loader contract. Either revert to the explicit deferred-migration compatibility shape or complete the file-kind migration atomically so checked-in workflow discovery is loadable again.
  - This pass removed collisions for `implement-task`, `plan-build-review`, `review-step`, `gh-bug-triage-modular`, `plan-build`, `delegate-build-review`, and `prompt-build`, but many same-name `.md` + `.edn` pairs still remain elsewhere in the corpus.
- [x] Repair the broken checked-in multi-step workflow sample set required by `workflow-migration-validation-test`: at minimum make `implement-task`, `plan-build-review`, `review-step`, and `gh-bug-triage-modular` exist as loadable `.edn`-only workflows under the finalized contract, with no sibling `.md` files and successful compile to non-empty `:steps`.
- [ ] Fix invalid checked-in workflow artifacts uncovered by corpus validation, including the remaining `.md` files that are still EDN-bodied despite being expected to satisfy the finalized single-step markdown contract.
  - Remaining blockers are the still-unmigrated EDN-bodied standalone `.md` artifacts (`gh-bug-discover-and-read`, `gh-bug-post-repro`, `gh-bug-reproduce`, `gh-issue-create-worktree`, `gh-issue-push-intent`, `gh-issue-task-intent`, `implement-task-in-worktree`).
- [ ] Align task evidence with the current repo-corpus proof count and contract language.
  - The latest recorded corpus verification in `implementation.md` says `workflow-migration-validation-test` passed with `25 assertions`, but the current focused run is `5 tests, 24 assertions`; refresh the task note so review evidence matches the actual proof seam.
- [ ] Clarify the deferred-migration story across task artifacts and docs for the remaining EDN-bodied `.md` workflows.
  - `design.md` and `plan.md` say legacy multi-step `.md` artifacts are deferred transitional compatibility surfaces, but `doc/workflows.md` still calls `planner`/`builder`/`reviewer` the authoritative examples while separately claiming many checked-in multi-step `.md` artifacts remain transitional. Add explicit wording for the remaining EDN-bodied `.md` wrappers (`gh-bug-discover-and-read`, `gh-bug-post-repro`, `gh-bug-reproduce`, `gh-issue-create-worktree`, `gh-issue-push-intent`, `gh-issue-task-intent`, `implement-task-in-worktree`) so user-facing docs and task evidence describe the same current corpus state.
- [x] Broaden `workflow-migration-validation-test` from a curated-required-workflow subset to corpus-wide contract validation, so the checked-in workflow tree cannot silently drift into invalid mixed-kind collisions or malformed migrated artifacts outside the named sample set.
- [x] Tighten `workflow-migration-validation-test` so required finalized multi-step sample workflows only assert compile success once their checked-in `.edn` artifacts actually satisfy the canonical loader/compiler contract; until then, the proof should isolate malformed `.edn` sample artifacts as explicit migration blockers instead of failing indirectly via missing `by-name` definitions.
  - The proof now requires the checked-in `.edn` artifacts to exist, but only asserts compiled `:steps` for whichever required samples currently make it into `by-name`; missing compiled definitions remain isolated migration blockers rather than indirect test explosions.
- [x] Add explicit corpus-shape proof for invalid checked-in `.md` artifacts that still violate the finalized single-step markdown contract, so empty-body markdown and EDN-bodied markdown blockers are named directly rather than only surfacing through aggregate parse-error fallout.
  - The test now enumerates the exact empty-body markdown blocker set and the exact EDN-bodied-markdown blocker set from the checked-in corpus.
