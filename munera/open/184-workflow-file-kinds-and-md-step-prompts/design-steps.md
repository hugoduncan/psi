- [x] Create missing `plan.md`, `steps.md`, and `implementation.md` before implementation/review so the task has the required Munera execution surfaces.
- [x] Specify the canonical discovery/loading contract now that current runtime/docs load only `.psi/workflows/*.md`: choose whether workflow discovery becomes mixed `.md` + `.edn` in the same roots, how precedence/collision works across file kinds, and whether `/delegate-reload` plus built-in docs/config paths change.
- [x] Define the standalone execution/compilation target for a single-step `.md` workflow precisely: what canonical session-step shape it compiles into, where workflow-level metadata (`name`/`description`) ends and step-execution frontmatter begins, and how "exactly one prompt-driven step" is represented in runtime IR.
- [x] Replace the unresolved frontmatter contract with an explicit allowed-key schema and context rules: name which keys are valid in `.md` frontmatter, whether they reuse existing session-step field names exactly, and which keys are forbidden when the `.md` file is referenced from an `.edn` workflow step versus run standalone.
- [x] Define the exact `.edn` authoring syntax for reusing a single-step `.md` workflow as a step prompt source, including the field name, whether the reference targets a file path or workflow name, and how it composes with existing session-step prompt/contribution fields so authors cannot express two competing prompt sources at once.
- [x] Define canonical path resolution for referenced `.md` workflow files together with rename-safe identity/error behavior: relative-to-consuming-file vs workflow-root resolution, whether cross-root/global references are allowed, and how wrong-kind, missing-file, or duplicate-name collisions are reported.
- [x] Resolve the artifact-scope inconsistency between the new file-kind contract and the repository's current checked-in workflow/doc/test surfaces: design says multi-step workflows are authored as `.edn` while single-step workflows are `.md`, but plan/steps do not yet say whether this task must migrate the repo's existing multi-step `.psi/workflows/*.md` examples/built-ins/tests/docs to `.edn` or explicitly leave that migration to a follow-on. Record one explicit scope decision and align plan/steps/docs expectations to it.
