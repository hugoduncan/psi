# Implementation notes

- 2026-06-06 architecture review: found actionable architectural-fit follow-ups AR1/AR2. AR1 constrains the live `/session-profile` command path to backend single-source command specs plus dispatch/resolver-owned session mutations rather than adapter/local atom writes. AR2 separates canonical workflow-run profile snapshots from the narrow concrete `:inherited-defaults` delegation snapshot so profile-name resolution stays deterministic without widening inherited defaults or re-reading mutable config. PASS_STATUS ACTIONABLE_FEEDBACK.

- 2026-06-06 design follow-up AR1/AR2 executed. AR1: design now pins `/session-profile` and `/session-profiles` to the backend single-source built-in command specs, existing slash-command dispatch, session-owned mutation/dispatch state changes, and EQL resolver-owned reads; adapters must not carry local profile lists/caches or direct atom writes. AR2: design now separates a canonical workflow-run session-profile snapshot from the task-207 narrow concrete `:inherited-defaults` snapshot; top-level and delegated runs resolve profile names from immutable workflow-run profile snapshots, while delegate inheritance passes only profile-derived concrete model/thinking/speed/effort defaults through `:inherited-defaults`. Marked AR1/AR2 done. PASS_STATUS FOLLOW_UPS_COMPLETE.

- 2026-06-06 ambiguity review: found actionable design ambiguities AMB1–AMB5. AMB1 requires choosing whether profile deep-merge changes global user/project `:agent-session` config resolution or is a profile-specific merge path. AMB2 requires pinning the exact supported profile key set beyond model/thinking/speed/effort or explicitly limiting this slice to those fields. AMB3 requires defining invalid/partial profile resolution behavior. AMB4 requires defining persistence/journaling/resume semantics for applying and clearing a live selected profile. AMB5 requires pinning which workflow step forms accept `:session-profile`/concrete profile-affecting overrides and where the key is stored in authored/canonical grammar. PASS_STATUS ACTIONABLE_FEEDBACK.

- 2026-06-06 design ambiguity follow-ups AMB1–AMB5 executed. AMB1 chooses a profile-specific deep-merge resolver for only `:agent-session :session-profiles`, leaving non-profile config resolution unchanged. AMB2 limits supported profile fields to model provider/id, thinking, speed, and effort only. AMB3 defines valid partial profiles, atomic invalid-profile failure, `/session-profiles` invalid diagnostics, and workflow blocking before child-session creation. AMB4 pins live-session persistence: model/thinking use existing journaling; speed/effort stay session-transient like `/speed` and `/effort`; selected-profile metadata is session-local, not journaled/resumed/inherited, and clear removes only that metadata. AMB5 pins authored workflow placement to compact top-level keys on `:session`/`:delegate` steps plus markdown frontmatter, excludes legacy nested `:session`, `:invoke`, and LLM judge profile keys, and specifies canonical effective step storage without widening `:inherited-defaults`. Marked AMB1–AMB5 done. PASS_STATUS FOLLOW_UPS_COMPLETE.

- 2026-06-06 inconsistency review: found actionable design inconsistencies INC1–INC3. INC1: profile names are described as open keywords while `/session-profile clear` reserves `clear` as a command action, leaving `:clear` ambiguous/unaddressed. INC2: design says workflow steps can currently set/retain direct `:speed-mode` and `:effort-override` overrides, but referenced workflow grammar/compiler artifacts currently omit those keys from authored step config. INC3: delegate profile/explicit speed-effort propagation conflicts with task-207 `effective-config->snapshot` artifacts that intentionally source speed/effort from the parent snapshot and ignore effective config. PASS_STATUS ACTIONABLE_FEEDBACK.

- 2026-06-06 design inconsistency follow-ups INC1–INC3 executed. INC1 reserves `:clear` as unavailable so `/session-profile clear` remains unambiguous and `/session-profiles` reports configured `:clear` as reserved/invalid. INC2 narrows authored workflow direct overrides to currently supported `:model` and `:thinking-level`, leaving direct `:speed-mode`/`:effort-override` step/frontmatter keys out of scope while allowing profile-derived speed/effort. INC3 specifies delegate projection precedence for profile-derived speed/effort: present fields in the resolved delegating effective config outrank parent snapshot values, otherwise task-207 parent-snapshot fallback remains. Marked INC1–INC3 done. PASS_STATUS FOLLOW_UPS_COMPLETE.

- 2026-06-06 plan/steps ambiguity review: found actionable follow-ups PA1–PA5. PA1 pins the delegate-step canonical IR path for profile session config; PA2 decides nil effort as concrete clear vs no-setting; PA3 pins `/session-profile` token normalization; PA4 pins model+thinking application order/clamping; PA5 pins the impure snapshot-capture boundary feeding pure workflow-run creation and delegated copy/derive semantics. PASS_STATUS ACTIONABLE_FEEDBACK.

- 2026-06-06 plan/steps ambiguity follow-ups PA1–PA5 executed. PA1 pins delegate canonical profile config to `[:delegate :session :session-profile]` with direct delegate model/thinking beside it at `[:delegate :session]`, consumed by target IR compilation, normalized run storage, `resolve-step-session-config`, and delegate inherited-defaults projection. PA2 chooses explicit `:effort-override nil` as a valid concrete effort-clear where key presence outranks truthiness. PA3 accepts bare or EDN-style keyword tokens for profile names, with raw bare `clear` as the clear action and `:clear` reserved/unavailable. PA4 specifies validate-first atomic live application with model-before-thinking ordering and final thinking clamped against the resulting model. PA5 pins session-profile snapshot capture to impure top-level workflow invocation boundaries, passed into pure `workflow-runtime.core/create-run` as `:session-profile-snapshot`, while delegated runs copy/derive from the parent snapshot without config reads. Updated plan.md and marked PA1–PA5 done in steps.md; no code/tests/docs changed. PASS_STATUS FOLLOW_UPS_COMPLETE.

- 2026-06-06 plan/steps inconsistency review: found actionable follow-ups PI1–PI4. PI1: PA1's delegate canonical path `[:delegate :session]` is not reflected in current canonical IR/model schema validation, so the path could be compiled then rejected or dropped. PI2: PA2's explicit `:effort-override nil` concrete clear is only called out for validation/projection, but existing resolver/child-session boundaries use nil-as-absent and need presence-aware handling end to end. PI3: PA3 command-token normalization is specified in the plan but not represented by concrete command test/work items. PI4: PA5 names specific impure snapshot-capture boundaries and pure create-run storage, while steps remain generic enough to miss psi-tool/Pathom/delegate wiring. PASS_STATUS ACTIONABLE_FEEDBACK.

- 2026-06-06 plan/steps inconsistency follow-ups PI1–PI4 executed. PI1 now pins the delegate canonical session-config as optional `[:delegate :session]` with `:session-profile`, `:model`, and `:thinking-level`, and requires IR/model schema plus compiler/normalization/validation preservation tests. PI2 extends explicit `:effort-override nil` presence-aware semantics across profile snapshots, resolver output, nested snapshot projection, delegate inherited-defaults, and child-session creation. PI3 adds a command normalizer/parser seam and concrete token test matrix for bare/EDN profile names, raw `clear`, reserved `:clear`, and rejected compound tokens. PI4 names the Pathom and psi-tool top-level snapshot producers, pure `workflow-runtime.core/create-run` snapshot storage boundary, and delegate copied/derived child snapshot wiring. Updated plan.md and marked PI1–PI4 done in steps.md; no code/tests/docs changed in this follow-up pass because the task is still pre-implementation and the follow-ups refine implementation obligations. PASS_STATUS FOLLOW_UPS_COMPLETE.

- 2026-06-06 implementation pass: completed Slice 1. Added `psi.shared-config.session-profiles` as the profile domain/config-resolution layer: supported field constants, reserved `:clear`, profile-specific deep merge over user/project-shared/project-local config, supported-field filtering, model-registry materialization, validation/diagnostic shape, readable settings, `effective-profiles`, `profile-snapshot`, and selection helper. Added mock-free tests using real temp config files plus the real built-in model registry for deep merge, precedence, unknown-key ignoring, partial profiles, invalid model pairs/enums, empty profiles, reserved `:clear`, and explicit nil effort presence. Added `psi/ai` dependency to `shared-config` for model-registry validation. Verification green: `psi.shared-config.session-profiles-test` Scry 4/29, `psi.shared-config.resolution-test` Scry 7/31, `clj-kondo --lint components/shared-config/src components/shared-config/test` 0/0.

- 2026-06-06 implementation pass: completed Slice 2 live command surface/observability. Added selected-session-profile metadata to canonical session state; added session-owned `:session/apply-session-profile` and `:session/clear-session-profile` dispatch handlers plus public wrappers; live profile application now validates before dispatch, applies model-before-thinking with model/thinking journal effects, applies speed/effort as transient runtime/session fields, stores selected-profile metadata, and clear removes metadata only. Added EQL profile/snapshot/selected-profile resolvers, backend single-source `/session-profiles` and `/session-profile` specs/routing, command token normalization (`planning`/`:planning`, raw `clear`, reserved `:clear`, reject compound tokens), command output/error formatting, and mock-free command/resolver tests over real temp config files and model registry. Also normalized registry-derived model maps with `:reasoning` so existing clamp semantics work with `:supports-reasoning` catalog entries. Verification green: `psi.shared-config.session-profiles-test` Scry 4/29; `psi.agent-session.commands-session-profile-test` Scry 5/36; `psi.agent-session.commands-builtin-specs-test` Scry 12/59; `psi.agent-session.builtin-commands-resolver-test` Scry 3/26; `psi.agent-session.model-dispatch-test` Scry 12/153; targeted clj-kondo 0/0; file-length check green. PASS_STATUS MORE_WORK_REMAINS.

- 2026-06-06 implementation pass: completed Slice 3 workflow grammar/canonical IR. Added compact `:session-profile` support for target-authored session steps, delegate steps (stored at `[:delegate :session :session-profile]` with direct delegate `:model`/`:thinking-level`), and single-step markdown frontmatter. Workflow IR schemas now validate the canonical session-profile fields and close session/delegate/judge/step maps so unsupported nested `{:session {:session-profile ...}}`, invoke-step profile keys, LLM judge profile keys, and direct authored speed/effort keys fail at the canonical boundary. Added focused mock-free compiler/schema tests for session/delegate/markdown placement, unchanged no-profile workflows, unsupported placements, and direct speed/effort exclusion. Verification green: Scry `psi.workflow-loader.session-profile-grammar-test`, parser/compiler, target-ir compiler, workflow IR (20 tests/225 assertions); checked-in workflow definition/migration Scry (17 tests/258 assertions); targeted clj-kondo over workflow-loader/runtime 0/0. PASS_STATUS MORE_WORK_REMAINS.

- 2026-06-06 implementation pass: completed Slices 4–6. Added canonical workflow-run `:session-profile-snapshot` schema/storage; Pathom `psi.workflow/create-run` and psi-tool workflow create-run now snapshot effective profiles from the invoking session worktree, omitting ignored unknown config keys. Delegated runs copy the parent run's immutable snapshot rather than reading config. `resolve-step-session-config` now reads compact profile config from session steps and delegate `[:delegate :session]`, resolves names against the stored snapshot, fails unknown/invalid profiles before attempt creation, and merges with precedence explicit step model/thinking > profile > inherited snapshot > fallback. Profile-derived speed/effort flow into effective config and delegate child `:inherited-defaults`; effort nil is preserved by key presence through step config, snapshot projection, attempt request, dispatch, and child-session creation. Task-207 speed/effort fallback remains when no profile-derived fields are present. Updated docs and CHANGELOG. Verification green: full unit Scry/Kaocha suite, targeted clj-kondo 0/0, clj-paren-repair success. PASS_STATUS IMPLEMENTATION_COMPLETE.

- 2026-06-06 implementation-review pass: found actionable follow-ups IR1–IR3. IR1: canonical session-step `:session` config now carries direct `:thinking-level`, but workflow IR schema still treats it as an extra key, so profile/explicit-thinking session workflows fail during run creation. IR2: `effective-config->snapshot` now projects any effective `:speed-mode`/`:effort-override`, regressing task-207 no-profile delegate fallback because inherited values are indistinguishable from profile-derived values. IR3: new workflow session-profile test helper shadows `clojure.core/name`, producing nil diagnostic messages for invalid-profile records and preventing the invalid-profile pre-attempt failure test from exercising its intended branch. Verification during review: `clojure -M:test --focus psi.workflow-step-session-config.session-profiles-test` errors; `clojure -M:test --focus psi.workflow-step-session-config.inheritance-snapshot-test` fails/errors. PASS_STATUS ACTIONABLE_FEEDBACK.

- 2026-06-06 implementation-review follow-ups IR1–IR3 executed. IR1: added `:thinking-level` to the canonical session-step IR `:session` schema so session steps may validate `:session-profile` with explicit thinking, and focused session-profile coverage now creates a run where explicit thinking overrides profile thinking. IR2: `resolve-step-session-config` now carries an internal `::profile-derived-fields` marker from resolved profile settings through effective config; `effective-config->snapshot` uses that marker so only profile-derived speed/effort outrank the parent run snapshot, restoring task-207 no-profile delegate fallback while preserving profile nil-effort clears. IR3: renamed the invalid-profile test helper argument so diagnostic `:message` is always a string and the invalid-profile test reaches profile-resolution failure. Verification green: `clojure -M:test --focus psi.workflow-step-session-config.session-profiles-test`, `clojure -M:test --focus psi.workflow-step-session-config.inheritance-snapshot-test`, `clojure -M:test --focus psi.workflow-loader.session-profile-grammar-test`, `clojure -M:test --focus psi.workflow-runtime.ir-test`, targeted clj-kondo 0/0, full `bb clojure:test:unit`, file-length check green. PASS_STATUS FOLLOW_UPS_COMPLETE.

- 2026-06-06 implementation-review pass 2: found actionable follow-up IR4. Rechecked task artifacts, workflow IR schemas/compiler/tests, session-profile resolution paths, command/docs surfaces, and prior IR1–IR3 fixes. IR4: canonical LLM judge `[:judge :session :session-profile]` is still accepted because `llm-judge-schema` reuses the general session spec; the existing rejection test only puts `:session-profile` beside `:type :llm`, so it does not cover the real canonical judge-session location. This violates the design boundary that LLM judge specs do not accept profiles in this task. Added unchecked IR4 to `steps.md`. PASS_STATUS ACTIONABLE_FEEDBACK.

- 2026-06-06 implementation-review follow-up IR4 executed. Split canonical LLM judge session validation from the general session-step schema by adding `llm-judge-session-spec-schema` without `:session-profile`, so `[:judge :session :session-profile]` is rejected while existing judge model/thinking/tools/system/etc. fields remain allowed. Added focused workflow-loader grammar coverage for the real canonical judge-session profile placement, preserving the existing top-level judge-profile rejection test. Verification green: `bb clojure:test:scry --namespace psi.workflow-loader.session-profile-grammar-test`, `clojure -M:test --focus psi.workflow-runtime.ir-test --no-capture-output`, `bb clojure:test:scry --namespace psi.workflow-runtime.target-ir-compiler-test`, `bb clojure:test:scry --namespace psi.agent-session.workflow-target-ir-compiler-test`, targeted clj-kondo 0/0. PASS_STATUS FOLLOW_UPS_COMPLETE.
- 2026-06-06 implementation-review pass 3: found actionable follow-up IR5. Rechecked task artifacts, profile resolver/command/snapshot paths, workflow profile resolution, docs, and prior IR1–IR4 fixes. IR5: `resolve-profiles` builds a plain `sorted-map` before profile-name validation, so mixed keyword/non-keyword effective config profile names (for example `:coding` plus `"oops"`) throw `ClassCastException` before `/session-profiles`, the EQL snapshot resolver, or workflow create-run can surface the intended invalid-profile-name diagnostic. PASS_STATUS ACTIONABLE_FEEDBACK.

- 2026-06-06 implementation-review follow-up IR5 executed. `resolve-profiles` no longer builds a plain keyword-only `sorted-map` before validation; it uses a deterministic heterogeneous-key comparator so mixed config names like `:coding` plus `"oops"` resolve without `ClassCastException`. Non-keyword names now surface `:invalid-profile-name` diagnostics through `resolve-profile`, `/session-profiles`, EQL/session snapshots, and Pathom workflow create-run snapshots. Workflow-runtime session-profile snapshot schema was widened only for invalid diagnostic entries (`:profiles` keys/name and `:invalid-profile-names` may be non-keyword) while `:valid-profile-names` stays keyword-only. Added focused shared-config, command listing, and workflow snapshot coverage. Verification green: Scry `psi.shared-config.session-profiles-test`, `psi.agent-session.commands-session-profile-test`, `psi.agent-session.mutations.canonical-workflows-snapshot-test`, `psi.agent-session.workflow-tools-test`, `psi.workflow-runtime.ir-test`; targeted clj-kondo 0/0. PASS_STATUS FOLLOW_UPS_COMPLETE.

- 2026-06-06 implementation-review pass 4: found actionable follow-up IR6. Rechecked task artifacts, profile name validation/listing/selection, workflow profile resolution, docs, and prior IR1–IR5 fixes. IR6: namespaced keyword profile names such as `:team/coding` are currently accepted as valid by `resolve-profile` and workflow snapshots, but `/session-profiles` renders them with `(name ...)` as `coding` while `/session-profile :team/coding` is rejected by the command token parser and `/session-profile coding` selects a different unqualified key or fails. This leaves a listed-valid profile unselectable/misleading, contrary to the command contract for selectable profile names. PASS_STATUS ACTIONABLE_FEEDBACK.
