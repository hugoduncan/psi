- Review 2026-05-18: actionable ambiguities found. The task requests appending review notes to `implementation.md` and adding ambiguity follow-ups to `design-steps.md`, but neither file existed in this task directory, making the canonical review/follow-up surface ambiguous. Created both files. Additional actionable ambiguity: `design.md` and `plan.md` require identifying the authoritative built-in tool definition owner(s) for `read`, `bash`, `edit`, `write`, and `psi-tool`, but `steps.md` only says to inventory them and never requires recording the settled owner mapping and rationale back into a task artifact before moving prompt text. Add an explicit artifact step to record that owner map and rationale before implementation.
- Follow-up 2026-05-18: completed the ambiguity follow-up by making the artifact roles explicit in `design.md` and `plan.md`.
- Follow-up 2026-05-18: source inspection settled the built-in owner map before implementation changes:
  - `read` → `components/agent-session/src/psi/agent_session/tools.clj` `read-tool`; this map already owns the built-in tool name, label, prose description, parameters, and request formatting metadata, and `all-tools`/cwd-scoped wrappers project from it.
  - `bash` → `components/agent-session/src/psi/agent_session/tools.clj` `bash-tool`; same ownership shape as `read`, so prompt description belongs on that canonical built-in definition map.
  - `edit` → `components/agent-session/src/psi/agent_session/tools.clj` `edit-tool`; same canonical built-in definition owner used by registration and wrappers.
  - `write` → `components/agent-session/src/psi/agent_session/tools.clj` `write-tool`; same canonical built-in definition owner used by registration and wrappers.
  - `psi-tool` → `components/agent-session/src/psi/agent_session/psi_tool.clj` `psi-tool`; this namespace owns the canonical psi-tool contract map and runtime-specific action surface, while `psi.agent-session.tools` only aliases it.
- Follow-up 2026-05-18: prompt assembly still carries a built-in-only description table in `components/prompt-assets/src/psi/prompt_assets/system_prompt.clj`, while canonical normalization in `components/tool-registry/src/psi/tool_registry/defs.clj` already preserves `:description` and optional `:lambda-description`. Session-state schema already acknowledges `:lambda-description` for skills, but `:tool-defs` remains `[:vector :map]`, so the description contract exists canonically without a stronger stored tool schema yet.
