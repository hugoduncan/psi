- Review 2026-05-18: actionable ambiguities found. The task requests appending review notes to `implementation.md` and adding ambiguity follow-ups to `design-steps.md`, but neither file existed in this task directory, making the canonical review/follow-up surface ambiguous. Created both files. Additional actionable ambiguity: `design.md` and `plan.md` require identifying the authoritative built-in tool definition owner(s) for `read`, `bash`, `edit`, `write`, and `psi-tool`, but `steps.md` only says to inventory them and never requires recording the settled owner mapping and rationale back into a task artifact before moving prompt text. Add an explicit artifact step to record that owner map and rationale before implementation.
- Follow-up 2026-05-18: completed the ambiguity follow-up by making the artifact roles explicit in `design.md` and `plan.md`.
- Follow-up 2026-05-18: source inspection settled the built-in owner map before implementation changes:
  - `read` → `components/agent-session/src/psi/agent_session/tools.clj` `read-tool`; this map already owns the built-in tool name, label, prose description, parameters, and request formatting metadata, and `all-tools`/cwd-scoped wrappers project from it.
  - `bash` → `components/agent-session/src/psi/agent_session/tools.clj` `bash-tool`; same ownership shape as `read`, so prompt description belongs on that canonical built-in definition map.
  - `edit` → `components/agent-session/src/psi/agent_session/tools.clj` `edit-tool`; same canonical built-in definition owner used by registration and wrappers.
  - `write` → `components/agent-session/src/psi/agent_session/tools.clj` `write-tool`; same canonical built-in definition owner used by registration and wrappers.
  - `psi-tool` → `components/agent-session/src/psi/agent_session/psi_tool.clj` `psi-tool`; this namespace owns the canonical psi-tool contract map and runtime-specific action surface, while `psi.agent-session.tools` only aliases it.
- Follow-up 2026-05-18: prompt assembly still carries a built-in-only description table in `components/prompt-assets/src/psi/prompt_assets/system_prompt.clj`, while canonical normalization in `components/tool-registry/src/psi/tool_registry/defs.clj` already preserves `:description` and optional `:lambda-description`. Session-state schema already acknowledges `:lambda-description` for skills, but `:tool-defs` remains `[:vector :map]`, so the description contract exists canonically without a stronger stored tool schema yet.
- Review 2026-05-18: actionable inconsistency found. `design.md` and `plan.md` say prompt assembly should render from normalized tool definition maps through one shared built-in/extension path, but `steps.md` only requires updating system-prompt tests for rendering behavior and never requires proving the actual prompt-assembly input/path change. Existing code still exposes a split API in `psi.prompt-assets.system-prompt/build-system-prompt` (`:selected-tools` names plus separate `:extension-tool-descriptions` maps), so without an explicit follow-up the task can satisfy the current checklist while leaving the split model at the prompt-assembly boundary. Add a design-step that requires either converging that prompt-assembly input to normalized tool-definition maps or explicitly recording the narrow retained split and why it still satisfies the one-path ownership goal.
- Follow-up 2026-05-18: completed the inconsistency follow-up by making the prompt boundary explicit in task artifacts. Source inspection confirmed the retained inconsistency is at `psi.prompt-assets.system-prompt/build-system-prompt`: prompt handlers and child-session state already carry canonical `:tool-defs`, but prompt assembly still accepts built-ins as `:selected-tools` names and extension prompt data as a separate `:extension-tool-descriptions` list. Updated `design.md` and `plan.md` to require converging `build-system-prompt` onto normalized `:tool-defs` inputs, with `:selected-tools` allowed only as deterministic filtering rather than as the rendering source of truth.
- Implementation 2026-05-18: converged tool prompt descriptions onto canonical tool definition maps.
  - Added `:lambda-description` to the authoritative built-in tool maps in `components/agent-session/src/psi/agent_session/tools.clj` for `read`, `bash`, `edit`, and `write`.
  - Added `:lambda-description` to the authoritative `psi-tool` map in `components/agent-session/src/psi/agent_session/psi_tool.clj`.
  - Simplified `components/agent-session/src/psi/agent_session/tools.clj/all-tools` to project from the canonical built-in tool maps via `assoc`, preserving prompt description fields and execution fns together.
- Implementation 2026-05-18: removed the built-in-only prompt description table from `components/prompt-assets/src/psi/prompt_assets/system_prompt.clj`.
  - Prompt rendering now uses `psi.tool-registry.defs/normalize-tool-def` and a single mode-aware description selector.
  - Shared fallback rule implemented in one place: prose uses `:description`; lambda uses non-blank `:lambda-description` or falls back to `:description`.
  - `build-system-prompt` now accepts canonical `:tool-defs` and uses `:selected-tools` only as an allowlist filter over those definitions.
- Implementation 2026-05-18: propagated canonical `:tool-defs` inputs through prompt assembly callers.
  - `components/agent-session/src/psi/agent_session/child_session_state.clj` now stores `:tool-defs` in the default child prompt build opts.
  - `components/agent-session/src/psi/agent_session/dispatch_handlers/prompt_handlers.clj` now rebuilds base prompts from session `:tool-defs` rather than from a built-in/extension split.
  - `components/app-runtime/src/psi/app_runtime.clj` now supplies canonical built-in plus runtime `psi-tool` definitions directly to `build-system-prompt` during bootstrap.
- Implementation 2026-05-18: kept schema tightening narrow for this slice.
  - No broad session-state tool schema redesign was required.
  - Canonical normalization already preserved `:description` and optional `:lambda-description`, so proof surface changes were sufficient here.
- Verification 2026-05-18:
  - `clojure -M:test --focus psi.tool-registry.defs-test --focus psi.prompt-assets.system-prompt-test --focus psi.agent-session.child-session-state-test`
  - Result: `15 tests, 145 assertions, 0 failures`.
- Review 2026-05-18: actionable implementation issue found. `components/agent-session/src/psi/agent_session/tools.clj` moved built-in `:lambda-description` onto the canonical tool maps and `all-tools` preserves it, but the cwd-scoped projection helpers `make-tools-with-cwd` and `make-read-only-tools-with-cwd` still rebuild maps by hand with only `:name`, `:label`, `:description`, and `:parameters`. That silently drops the canonical `:lambda-description` field for cwd-scoped built-in tools, so child/session-scoped consumers using those helpers can still lose the uniform prompt-description contract this task says should travel with the canonical tool definition map. Add a follow-up to preserve `:lambda-description` (and preferably project from the canonical tool map rather than re-enumerating fields) in cwd-scoped tool helpers.
- Follow-up 2026-05-18: updated cwd-scoped built-in tool helpers to project from the canonical built-in maps via a shared `with-execute` helper rather than hand-rebuilding partial maps. This preserves `:description`, `:lambda-description`, and any future canonical prompt-description fields uniformly for both full and read-only cwd-scoped tool sets.
- Verification 2026-05-18: added focused proof in `components/agent-session/test/psi/agent_session/tools_test.clj` that `make-tools-with-cwd` and `make-read-only-tools-with-cwd` preserve canonical prose and lambda description fields for cwd-scoped built-ins.
- Review 2026-05-18: actionable test gap found. The task changes `:session/refresh-system-prompt` to rebuild from session `:tool-defs` plus `:skills`, but the current proof set does not cover that refresh path with canonical `:tool-defs` input. Rerunning the focused review suite exposed a real regression in `components/agent-session/test/psi/agent_session/config_compaction_test.clj`: `register-skill refreshes the system prompt so newly added skills appear` now rebuilds a prompt whose tool section is `(none)` and whose skills section is absent, because the stored `:system-prompt-build-opts` in that test carry `:skills []` and no `:tool-defs`, while `prompt_handlers.clj` rebuilds from session state and then explicitly passes `:prompt-contributions nil`. Add focused follow-up work to prove and preserve the refreshed-prompt contract: when `:session/refresh-system-prompt` runs after skill/tool registration, the rebuild must continue to include the live session `:tool-defs` and `:skills` rather than silently dropping them because stale build opts contained empty placeholders.
- Follow-up 2026-05-18: fixed the refresh rebuild contract in `components/agent-session/src/psi/agent_session/dispatch_handlers/prompt_handlers.clj` so `:session/refresh-system-prompt` overlays live session `:tool-defs` and `:skills` only when those live collections are non-empty. This preserves startup/default prompt rebuild behavior when session state has not yet been populated, while ensuring post-registration refreshes ignore stale placeholder `:skills []` or omitted `:tool-defs` in stored `:system-prompt-build-opts`.
- Verification 2026-05-18: strengthened `components/agent-session/test/psi/agent_session/config_compaction_test.clj` to exercise the registration-driven refresh path with canonical session `:tool-defs`, proving both pre- and post-skill-registration prompts retain the live tool section and that skill registration adds the skill section on refresh.
- Verification 2026-05-18: `clojure -M:test --focus psi.agent-session.config-compaction-test --focus psi.prompt-assets.system-prompt-test --focus psi.tool-registry.defs-test --focus psi.agent-session.child-session-state-test --focus psi.agent-session.tools-test` → `40 tests, 392 assertions, 0 failures`.
- Review 2026-05-18: no new actionable test-shaper feedback found after reading the task artifacts and the referenced prompt/tool/session tests and rerunning the focused proof set. The current tests are already behavior-focused and high-signal for the ownership-convergence slice: they prove canonical `:lambda-description` preservation in normalization, shared built-in/extension prompt fallback behavior, cwd-scoped tool projection preserving prompt-description fields, and the live `:session/refresh-system-prompt` rebuild contract. No new follow-up steps added.
- Follow-up 2026-05-18: executed the newly added actionable review items by re-reading `steps.md`, `implementation.md`, `design.md`, and `plan.md` against the current source/tests. All follow-up checklist items added by the preceding review pass were already completed in the task artifacts and codebase; there were no remaining newly added unchecked actionable items to implement.
- Verification 2026-05-18: confirmed `steps.md` contains no unchecked follow-up items and the worktree is clean, so there was no additional mechanism change to make before closing this execution pass.
- Review 2026-05-18: no new actionable code-shaper feedback found after re-reading the task artifacts and the touched prompt/tool/session sources and rerunning the focused proof set. The current shape is locally comprehensible and consistent with the task’s ownership goal: built-in canonical tool maps in `components/agent-session/src/psi/agent_session/tools.clj` and `components/agent-session/src/psi/agent_session/psi_tool.clj` now own both prose and lambda prompt descriptions; `components/prompt-assets/src/psi/prompt_assets/system_prompt.clj` applies one shared mode-aware selector over normalized tool definition maps instead of a built-in-only table; and `components/agent-session/src/psi/agent_session/dispatch_handlers/prompt_handlers.clj` refreshes prompts from live session `:tool-defs`/`:skills` rather than stale split inputs. Focused verification remains green (`40 tests, 392 assertions, 0 failures`). No new follow-up steps added.
