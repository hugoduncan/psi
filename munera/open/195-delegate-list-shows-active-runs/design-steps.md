- [x] Clarify the `delegate list` visibility and retention contract: whether results are scoped to the invoking/originating session or global; which run/job field is the authoritative owner/session marker; and which terminal or retained statuses, if any, must remain listed versus hidden.
- [x] Clarify `delegate list` handling for retained `:timed-out` delegate background jobs: how they join to canonical workflow runs whose status model has no `:timed-out`, what status/list entry should display, and whether the surfaced run id is manageable by `continue` or only by `remove`.
- [x] Clarify `delegate list` handling for malformed same-session non-terminal delegate workflow background jobs whose `workflow-id` is missing or blank: whether this is an actionable inconsistency/tool error, and ensure it cannot be silently ignored or converted into an empty list.
- [x] Clarify delegate `remove` behavior for active/non-terminal listed runs: after `remove` deletes the canonical workflow run, specify whether the delegate background job/inflight execution is cancelled, marked terminal, removed, or intentionally hidden, so a valid remove cannot leave a same-session non-terminal background job pointing at a missing canonical run that `delegate list` must report as corruption.
- [x] Clarify `delegate list` behavior when the delegate background-job registry/query surface is unavailable, missing, or unreadable: because background jobs are the authoritative visibility marker, this must surface an actionable tool error instead of silently treating the background-job set as empty and returning no visible runs.
- [x] Clarify the exact delegate background-job eligibility predicate for `delegate list`: whether visibility requires `tool-name = "delegate"` plus `job-kind = :workflow` and `workflow-ext-path = "built-in:workflow"` (or equivalent canonical provenance), and whether same-session delegate jobs outside that workflow-delegate predicate are ignored or reported as actionable corruption.
- [x] Resolve the contradictory `delegate list` handling for same-session delegate workflow jobs with foreign/non-`"built-in:workflow"` `workflow-ext-path`: the design currently says different provenance should be ignored and also says non-built-in delegate workflow provenance is an actionable malformed-job error; specify the single intended outcome or a non-overlapping distinction.
- [x] Clarify `delegate list` handling for retained terminal same-session delegate workflow background jobs whose `workflow-id` is missing, nil, non-string, or blank: specify whether they are hidden as non-manageable retained history, reported as actionable corruption, or handled by another explicit rule.
- [x] Clarify `delegate list` handling when multiple eligible same-session delegate workflow background jobs reference the same canonical `workflow-id`: specify whether the list de-duplicates to one run entry, which delegate/background status is displayed, or whether duplicate eligible jobs are an actionable inconsistency/tool error.
- [x] Clarify deterministic newest/most-recent selection for terminal-only duplicate eligible delegate background jobs with the same canonical `workflow-id`: specify which background-job timestamp/sequence fields and tie-breakers choose the displayed retained background status.
- [ ] Clarify deterministic ordering for the final `delegate list` visible run entries when multiple same-session delegate workflow runs are present: specify whether entries sort by background-job start/completion/order fields, canonical workflow creation/order fields, status groups, run id, or another explicit stable ordering.
