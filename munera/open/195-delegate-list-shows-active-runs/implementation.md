2026-05-30 design ambiguity review: found actionable ambiguity in the session-visibility/retention contract for `delegate list`. The design requires invoking-session visibility and preserving retained terminal runs if intended, but does not state the authoritative policy for whether list is session-scoped or global, which run metadata carries ownership, or which terminal/retained statuses should appear.

2026-05-30 ambiguity follow-up: clarified `delegate list` is invoking-session scoped via delegate background-job `thread-id = :psi.agent-session/session-id`; background jobs provide visibility/ownership and canonical workflow runs provide run status/management identity. Non-terminal same-session delegate jobs must list, retained terminal jobs may list while their canonical workflow run remains, removed canonical runs must disappear, unrelated sessions stay hidden, and missing canonical runs for non-terminal delegate jobs should surface as actionable inconsistency rather than empty list.

2026-05-30 design inconsistency review: found one actionable inconsistency between the design's terminal delegate-job visibility rules and the referenced workflow runtime/status model. The design includes retained `:timed-out` delegate background jobs in `delegate list`, but also says canonical workflow runs are authoritative for listed status/manageability; canonical workflow run statuses and `delegate continue` support do not include a `:timed-out` run status, so the design must specify how timed-out background jobs join/display/manage against canonical workflow runs.


2026-05-30 inconsistency follow-up: clarified retained `:timed-out` delegate background jobs are delegate-tool/job retention state, not canonical workflow status. `delegate list` should still join them to existing canonical workflow runs, show canonical run status as primary, show delegate/background status separately as timed out, allow `remove` while the canonical run exists, and allow `continue` only when the canonical workflow status is one of the existing supported continue statuses (`:blocked`, `:completed`, `:failed`, `:cancelled`). Missing canonical runs for terminal timed-out jobs are hidden, not synthesized.

2026-05-30 design ambiguity review: found one new actionable ambiguity in the delegate-list join contract. The design requires non-terminal same-session delegate background jobs to always list and to join `workflow-id` to a canonical workflow run, but it only specifies the missing-canonical-run case; it does not state whether a malformed/non-terminal delegate workflow job with missing or blank `workflow-id` should surface an actionable inconsistency or be ignored.

2026-05-30 ambiguity follow-up: clarified malformed same-session non-terminal delegate workflow background jobs with missing/nil/non-string/blank `workflow-id` are actionable delegate-list inconsistencies/tool errors. They must not be silently ignored, coerced into an empty list, or turned into synthetic canonical workflow run ids. Marked the ambiguity design-step complete.

2026-05-30 design inconsistency review: found one actionable inconsistency in the remove/list contract. The design says listed active/non-terminal delegate runs expose ids usable by `remove`, and `remove` deletes the canonical workflow run, but it also says same-session non-terminal delegate background jobs whose canonical workflow run is missing must surface an actionable inconsistency/error. Without specifying background-job/inflight cleanup or an intentional post-remove visibility rule, a valid remove of an active listed run could create the exact malformed non-terminal missing-canonical state that `delegate list` must treat as corruption.


2026-05-30 inconsistency follow-up: clarified `delegate remove` for active/non-terminal listed runs. A successful remove must delete the canonical workflow run and also cancel, remove, mark terminal, or equivalently hide the same-session delegate background job so later `delegate list` cannot encounter a non-terminal job pointing at the removed canonical run. If cleanup cannot be performed, remove should fail actionably and leave the run visible/manageable. Marked the design-step complete.

2026-05-30 design ambiguity review: found one new actionable ambiguity in the delegate-list read path. The design makes same-session delegate background jobs the authoritative visibility/ownership marker and reserves empty lists for true no-visible-run cases, but it does not explicitly state what `delegate list` should do when the background-job registry/query surface is unavailable, missing, or unreadable while canonical workflow runs may exist. This should be an actionable tool error rather than being treated as an empty background-job set.

2026-05-30 ambiguity follow-up: clarified unavailable/missing/unreadable delegate background-job registry/query behavior. Because background jobs are the authoritative delegate-list visibility marker, `delegate list` must surface an actionable tool error for registry/query unavailability or unreadable/non-query results, must not treat that condition as an empty job set, and must not fall back to global canonical workflow runs as a substitute visibility source. Marked the ambiguity design-step complete.

2026-05-30 design inconsistency review: re-read design.md, existing design-steps.md/history, task-design guidance, delegate workflow core/text implementation, background-job registry/resolver/runtime code, canonical workflow run mutations, and built-in workflow docs. No new actionable design inconsistencies found beyond the already captured and resolved visibility, timed-out status, malformed workflow-id, active remove cleanup, and background-job read-surface items. design-steps.md unchanged.

2026-05-30 design ambiguity review: re-read design.md, existing ambiguity/inconsistency review history, task-design guidance, delegate workflow core/text/orchestration implementation, background-job registry/runtime/resolver code, and canonical workflow action surfaces. No new actionable design ambiguities found beyond already captured and resolved session visibility, retention, timed-out status, malformed workflow-id, active remove cleanup, and background-job read-surface behavior. design-steps.md unchanged.

2026-05-30 design inconsistency review: re-read design.md, existing design-steps.md/history, task-design guidance, delegate workflow core/text/orchestration implementation, background-job runtime/resolver/projection code, canonical workflow run resolver/action surfaces, and workflow/delegate docs. No new actionable design inconsistencies found beyond the already captured and resolved visibility, timed-out status, malformed workflow-id, active remove cleanup, and background-job read-surface items. design-steps.md unchanged.

2026-05-30 design ambiguity review: found one new actionable ambiguity in the delegate background-job visibility predicate. The design says visible runs come from same-session `tool-name = "delegate"` workflow jobs joined by `workflow-id`, but it does not explicitly say whether a job must also have `job-kind = :workflow` and/or `workflow-ext-path = "built-in:workflow"`, nor how same-session `delegate` jobs with non-workflow kind or a foreign/missing workflow provenance should be treated. This should be clarified so list cannot accidentally include unrelated delegate jobs or report corruption for jobs outside the workflow-delegate contract.

2026-05-30 ambiguity follow-up: clarified the exact `delegate list` background-job eligibility predicate. Visible workflow-delegate jobs must be same-session `tool-name = "delegate"`, `job-kind = :workflow`, and `workflow-ext-path = "built-in:workflow"`; jobs outside that predicate are ignored unless they claim delegate workflow kind with missing/foreign provenance, which is an actionable list inconsistency/tool error. Marked the design-step complete.

2026-05-30 design inconsistency review: found one new actionable inconsistency in the workflow-delegate background-job provenance predicate. The design first says same-session jobs with a different `workflow-ext-path` are outside the delegate-workflow list contract and should be ignored, but the next sentence says same-session `tool-name = "delegate"`, `job-kind = :workflow` jobs with non-`"built-in:workflow"` provenance are malformed and should surface an actionable `delegate list` inconsistency/tool error. Those cases overlap for foreign workflow provenance, so the design must choose one outcome or separate foreign non-delegate jobs from malformed delegate workflow jobs.

2026-05-30 inconsistency follow-up: resolved the foreign-provenance predicate contradiction. Same-session jobs with different `tool-name` or non-workflow `job-kind` are ignored as outside the workflow-delegate list contract; same-session `tool-name = "delegate"`, `job-kind = :workflow` jobs are inside the contract, and missing/blank/foreign `workflow-ext-path` is an actionable malformed delegate workflow job. Marked the design-step complete.

2026-05-30 design ambiguity review: found one new actionable ambiguity in retained terminal delegate workflow corruption handling. The design specifies malformed missing/blank `workflow-id` as an actionable error for non-terminal same-session delegate workflow jobs, but does not state whether retained terminal same-session delegate workflow jobs with missing/nil/non-string/blank `workflow-id` should be hidden as non-manageable history, reported as corruption, or handled some other way.


2026-05-30 ambiguity follow-up: clarified retained terminal same-session delegate workflow background jobs with missing/nil/non-string/blank `workflow-id` are hidden as non-manageable retained history, not actionable list corruption. They must not synthesize run ids or appear continuable/removable; if no other valid visible jobs exist, the ordinary empty-list result is allowed. Marked the ambiguity design-step complete.

2026-05-30 design inconsistency review: re-read design.md, existing design-steps.md/history, task-design guidance, delegate workflow core/text/orchestration implementation, background-job state/runtime code, canonical workflow run list/remove code, and workflow/delegate docs. No new actionable design inconsistencies found beyond already captured and resolved visibility, retained terminal/timed-out handling, malformed workflow-id, active remove cleanup, and provenance/read-surface rules. design-steps.md unchanged.

2026-05-30 design ambiguity review: re-read design.md, existing design-steps.md/history, task-design guidance, delegate workflow core/text/orchestration implementation, background-job state/runtime/projection code, canonical workflow run action surfaces, workflow/delegate docs, and background-job test matrix. No new actionable design ambiguities found beyond already captured and resolved session visibility, retained terminal/timed-out handling, malformed workflow-id, active remove cleanup, provenance, and background-job read-surface rules. design-steps.md unchanged.

2026-05-30 design inconsistency review: re-read design.md, existing design-steps.md/history, task-design guidance, delegate workflow core/text/orchestration implementation, background-job state/runtime/resolver code, canonical workflow run/remove/list surfaces, and workflow/delegate docs. No new actionable design inconsistencies found beyond already captured and resolved visibility, retained terminal/timed-out handling, malformed workflow-id, active remove cleanup, provenance, and background-job read-surface rules. design-steps.md unchanged.

2026-05-30 design ambiguity review: found one new actionable ambiguity in duplicate eligible delegate background-job handling. The design forms listed entries by joining each same-session eligible background job's `workflow-id` to a canonical workflow run, but it does not state what happens when multiple eligible same-session delegate workflow background jobs reference the same canonical `workflow-id` (for example retained terminal history plus a newer active/continued job, or corrupted duplicate non-terminal jobs). The design should specify whether `delegate list` de-duplicates to one run entry, which background/job status is displayed, or reports an actionable duplicate-job inconsistency.
2026-05-30 ambiguity follow-up: clarified duplicate eligible same-session delegate workflow background-job handling. `delegate list` de-duplicates by canonical `workflow-id` to one management entry; one non-terminal duplicate takes displayed delegate/background status over retained terminal history, terminal-only duplicates use the newest retained status while the canonical run exists, and multiple non-terminal duplicates for the same run are actionable duplicate-job inconsistencies. Marked the ambiguity design-step complete.

2026-05-30 design inconsistency review: re-read design.md, existing design-steps.md/history, task-design guidance, delegate workflow core/text/orchestration implementation, background-job state/runtime/resolver surfaces, canonical workflow run list/continue/remove mutations, and workflow/delegate docs. No new actionable design inconsistencies found beyond already captured and resolved visibility, retained terminal/timed-out handling, malformed workflow-id, active remove cleanup, provenance/read-surface, and duplicate-job rules. design-steps.md unchanged.

2026-05-30 design ambiguity review: re-read design.md, existing design-steps.md/history, task-design guidance, delegate workflow core/text/orchestration implementation, background-job runtime/state surfaces, canonical workflow run action surfaces, and workflow display docs. No new actionable design ambiguities found beyond already captured and resolved session visibility, retention/timed-out handling, malformed workflow-id, active remove cleanup, provenance/read-surface, and duplicate-job rules. design-steps.md unchanged.

2026-05-30 design inconsistency review: re-read design.md, existing design-steps.md/history, task-design guidance, delegate workflow list/run/continue/remove implementation, delegate text projection, orchestration background-job provenance, background-job registry/runtime/resolver tests, canonical workflow run status/remove model, and workflow/delegate docs. No new actionable design inconsistencies found beyond already captured and resolved visibility, retained terminal/timed-out handling, malformed workflow-id, active remove cleanup, provenance/read-surface, and duplicate-job rules. design-steps.md unchanged.

2026-05-30 design ambiguity review: found one new actionable ambiguity in terminal-only duplicate delegate background-job display. The design says duplicate terminal retained jobs for the same canonical `workflow-id` should use the newest/most recent retained background status, but it does not define the ordering/tie-breaker fields (for example `completed-at`, `completed-seq`, `job-seq`, `started-at`, or `job-id`). Because the background-job model has multiple possible ordering fields, the design should specify deterministic newest selection so implementation and tests cannot choose different terminal statuses.


2026-05-30 ambiguity follow-up: clarified deterministic newest selection for terminal-only duplicate eligible delegate workflow background jobs. Terminal-only duplicate groups choose the displayed background status by newest `completed-at`, then `completed-seq`, then `job-seq`, then lexicographic `job-id`; missing ordering fields lose to present values. Marked the ambiguity design-step complete.

2026-05-30 design inconsistency review: re-read design.md, existing design-steps.md/history, task-design guidance, delegate workflow list/run/continue/remove implementation, delegate text projection, orchestration background-job provenance, background-job state/runtime/resolver surfaces, canonical workflow run resolver/remove/status model, and workflow docs/retention contract. No new actionable design inconsistencies found beyond already captured and resolved visibility, retained terminal/timed-out handling, malformed workflow-id, active remove cleanup, provenance/read-surface, duplicate-job, and terminal duplicate ordering rules. design-steps.md unchanged.

2026-05-30 design ambiguity review: found one new actionable ambiguity in overall `delegate list` row ordering. The design defines deterministic newest selection inside terminal-only duplicate groups, but it does not specify the ordering of the final visible run entries when multiple same-session delegate workflow runs are listed. Since visibility is derived from background jobs while management/status data comes from canonical workflow runs, implementation/tests could reasonably order by background `started-at`, canonical `created-at`, completion time, run registry order, or run id; the design should choose a stable ordering.

2026-05-30 ambiguity follow-up: clarified final `delegate list` visible row ordering. Rows sort newest-first by the selected representative delegate/background job `started-at`, then `job-seq`, then `job-id`, then canonical `run-id`; missing ordering fields lose to present values, and rows are not status-grouped or canonical-registry ordered. Marked the ambiguity design-step complete.

2026-05-30 design inconsistency review: found one new actionable inconsistency between the duplicate/continue list contract and the referenced background-job identity model. The design treats retained terminal history plus a newer active/continued delegate job for the same canonical `workflow-id` as a valid duplicate group, but current delegate background jobs derive `tool-call-id` solely as `delegate/<run-id>` and the background-job registry enforces one resolved job per `tool-call-id`; a blocked-run `continue` for the same canonical run cannot create the newer active job unless the design specifies an attempt-specific background-job identity or another explicit reconciliation rule.

2026-05-30 inconsistency follow-up: resolved delegate background-job identity for resumed/continued attempts. The design now treats background-job `tool-call-id` as execution-attempt identity rather than canonical management identity, requires attempt-specific unique tool-call ids (or an explicit retire/remove alternative before reuse) when retained history may collide, keeps `workflow-id` as the canonical run id used by list/continue/remove, and preserves duplicate reduction semantics for retained terminal history plus one newer active attempt. Marked the design-step complete.

2026-05-30 design ambiguity review: re-read design.md, existing design-steps.md/history, task-design guidance, delegate workflow core/text/orchestration code, background-job state/runtime/query surfaces, canonical workflow run mutations/status/retention docs, and workflow/delegate user docs. No new actionable design ambiguities found beyond the already captured and resolved session visibility, retention/timed-out handling, malformed workflow-id, active remove cleanup, provenance/read-surface, duplicate-job, ordering, and attempt-identity rules. design-steps.md unchanged.

2026-05-30 design inconsistency review: re-read design.md, existing design-steps.md/history, task-design/work-independently guidance, delegate workflow core/orchestration/text implementation, background-job registry/runtime/resolver surfaces, canonical workflow create/list/resume/remove/status code, workflow-runtime status model, and workflow/delegate user docs. No new actionable design inconsistencies found beyond already captured and resolved visibility, retention/timed-out handling, malformed workflow-id, active remove cleanup, provenance/read-surface, duplicate-job/ordering, and attempt-identity rules. design-steps.md unchanged.
2026-05-30 design ambiguity review: found one new actionable ambiguity in the canonical workflow status to delegate background-job status mapping. The design makes canonical `:blocked` runs continuable/listable and says retained terminal delegate jobs remain visible while the canonical run exists, but the background-job terminal status set has no `:blocked` value and the design does not specify whether a workflow that stops blocked should mark/display the delegate background job as completed, failed, or another explicit wrapper state. This should be clarified so `delegate list` does not misrepresent a blocked continuable run as failed or leave implementation/tests to choose different mappings.

2026-05-30 ambiguity follow-up: clarified canonical `:blocked` workflow run mapping to delegate background-job status. A delegate attempt that reaches a blocked workflow pause marks the wrapper background job terminal `:completed`; `delegate list` displays canonical status `:blocked` as the primary/manageability status and background status `:completed` separately, so the run remains continuable/removable while the canonical run exists. Marked the design-step complete.

2026-05-30 design inconsistency review: re-read design.md after the blocked-run mapping clarification, existing design-steps.md/history, task-design guidance, background-tool-jobs spec/test matrix, delegate workflow list/run/continue/remove/orchestration/text code, background-job registry/runtime/mutation surfaces, canonical workflow status/list/remove/resume code, and workflow/delegate docs. No new actionable design inconsistencies found beyond already captured and resolved visibility, retention/timed-out handling, malformed workflow-id, active remove cleanup, provenance/read-surface, duplicate-job/ordering, attempt-identity, and blocked-run wrapper-status rules. design-steps.md unchanged.
2026-05-30 plan/steps ambiguity review: found one new actionable ambiguity. The plan notes namespaced-versus-unqualified background-job shapes as a risk, but Slice 2/3 do not decide whether the pure delegate-list projection accepts query-shaped `:psi.background-job/*` maps, registry-shaped unqualified maps, or both, nor where normalization belongs. Added an unchecked Slice 2 follow-up to make the projection/caller normalization boundary explicit.

2026-05-30 ambiguity follow-up: resolved the Slice 2 projection input-shape boundary. The plan now requires the pure delegate-list projection to accept canonical unqualified delegate background-job maps only; callers own normalization from namespaced `:psi.background-job/*` query maps before projection. The projection can still reject malformed/mixed shapes actionably, but should not duplicate broad query-shape interpretation. Checked the newly added Slice 2 ambiguity item in `steps.md`.

2026-05-30 plan/steps inconsistency review: re-read design.md, plan.md, steps.md, implementation.md, design-steps.md, delegate workflow core/text/orchestration code, background-job state/runtime code, workflow async/tool tests, and delegate docs references. No new actionable plan/steps inconsistencies found beyond the already resolved projection input-shape boundary and existing planned slices for visibility, projection, attempt identity, blocked status, remove cleanup, coverage, and docs/coherence. steps.md unchanged.

2026-05-30 implementation pass: completed Slice 1 code-path characterization and Slice 2 pure projection extraction. Authoritative surfaces found: `psi.agent-session.workflow.core/delegate-list` currently reads canonical runs via `psi.workflow/list-runs` and background jobs via `query-fn [:psi.agent-session/background-jobs]`; delegate async start/resume writes background jobs through `orchestration/start-background-job!` → mutation `psi.extension/start-background-job`, backed by `psi.agent-session.background-jobs/start-background-job`; canonical workflow runs are created/listed/resumed/removed via `psi.workflow/*` mutations backed by `psi.workflow-runtime.core`. Added `psi.agent-session.workflow.delegate-list/project-visible-runs`, a pure unqualified-shape projection implementing same-session eligibility, malformed provenance/workflow-id errors, missing canonical rules, terminal-history hiding, duplicate reduction, deterministic terminal representative selection, and final row ordering. Added focused mock-free state/output tests for active same-session visibility, unrelated exclusion, malformed errors, terminal hiding, duplicate handling, and ordering. Verification: `clojure -M:test --focus psi.agent-session.workflow-delegate-list-test` passed (9 tests, 27 assertions).

2026-05-30 verification update: after formatting check, `clj-paren-repair components/agent-session/src/psi/agent_session/workflow/delegate_list.clj components/agent-session/test/psi/agent_session/workflow_delegate_list_test.clj` reported no changes needed; reran `clojure -M:test --focus psi.agent-session.workflow-delegate-list-test` green (9 tests, 27 assertions); targeted `clj-kondo --lint components/agent-session/src/psi/agent_session/workflow/delegate_list.clj components/agent-session/test/psi/agent_session/workflow_delegate_list_test.clj` green with 0 errors/warnings.

2026-05-30 implementation pass: completed Slice 3 wiring. `psi.agent-session.workflow.core/delegate-list` now requires the current invoking session id, reads the background-job query surface as the authoritative delegate visibility marker, fails actionably when the query surface is absent/failing/non-shaped, normalizes EQL background-job rows at the caller boundary, and feeds `delegate-list/project-visible-runs` instead of falling back to global canonical runs. `psi.agent-session.workflow.text` now renders only projected visible delegate rows, with canonical workflow status as primary status and delegate/background status separately as `[delegate <status>]`; empty active-runs output is now only from a successful projection with no visible rows. Verification: `clojure -M:test --focus psi.agent-session.workflow-delegate-list-test` green (9 tests, 27 assertions); `clojure -M:test --focus psi.agent-session.workflow-async-path-test` green (2 tests, 8 assertions); targeted `clj-kondo` over changed workflow list/text/projection source and projection test green with 0 errors/warnings.

2026-05-30 implementation pass: completed Slice 4 attempt identity and blocked wrapper-status coherence. `orchestration/background-job-tool-call-id` now creates attempt-specific ids shaped `delegate/<run-id>/<uuid>` while preserving `workflow-id` as the canonical management id on background jobs. Blocked canonical workflow results now mark the delegate/background wrapper attempt terminal `:completed` and include both canonical `:blocked` and delegate `:completed` in the terminal payload, so `delegate list` can continue to render canonical status as primary with completed wrapper status separately. Added focused mock-free tests for attempt-specific ids, canonical workflow-id preservation, blocked publication mapping, blocked list projection, and retained-terminal-plus-active duplicate reduction. Verification: `clojure -M:test --focus psi.agent-session.workflow-async-path-test --focus psi.agent-session.workflow-delegate-list-test` green (14 tests, 50 assertions); targeted `clj-kondo` over changed workflow orchestration/projection source and focused tests green with 0 errors/warnings.

2026-05-30 implementation pass: completed Slice 5 active remove cleanup. `delegate remove` now reads the same same-session background-job visibility surface used by `delegate list`, normalizes EQL job rows at the caller boundary, finds matching active/non-terminal built-in workflow delegate jobs by canonical `workflow-id`, and marks them terminal `:cancelled` with suppressed terminal message before deleting the canonical workflow run. If background-job read or terminalization fails, remove returns an actionable error and skips canonical workflow removal so the run remains visible/manageable. Retained terminal job history is then hidden after canonical run deletion by the existing projection rule. Added focused mock-free tests for successful active cleanup before canonical removal and cleanup-failure rollback behavior. Updated CHANGELOG for the visible delegate list/remove fix.

2026-05-30 implementation pass: completed Slice 6 regression and boundary coverage. Added mock-free delegate-list tool-path tests proving same-session active runs render in `delegate list`, unrelated-session jobs are excluded, active-run output is non-empty for the observed failure mode, and listed canonical run ids can be passed to `delegate continue` for blocked runs and `delegate remove` for existing runs. Expanded projection coverage for terminal duplicate representative tie-breakers across `completed-at`, `completed-seq`, `job-seq`, and `job-id`, plus final row ordering tie-breakers across `started-at`, `job-seq`, `job-id`, and `run-id`. Existing projection tests already covered malformed non-terminal workflow ids, missing canonical runs, and retained terminal malformed workflow-id hiding; marked the corresponding Slice 6 checklist items complete. Verification: `clj-paren-repair components/agent-session/test/psi/agent_session/workflow_delegate_list_test.clj` fixed formatting after edits; `clojure -M:test --focus psi.agent-session.workflow-delegate-list-test` green (18 tests, 66 assertions).

2026-05-30 verification update: after completing Slice 6 coverage, ran combined focused verification `clojure -M:test --focus psi.agent-session.workflow-delegate-list-test --focus psi.agent-session.workflow-async-path-test` green (22 tests, 85 assertions) and targeted `clj-kondo` over changed delegate workflow source/tests green with 0 errors/warnings. Slice 7 docs/coherence remains open for final documentation/checklist verification.

2026-05-31 implementation pass: completed Slice 7 docs/coherence verification. Confirmed `CHANGELOG.md` already contains the user-visible `delegate list`/`delegate remove` fix entry under `[Unreleased]`, and README/doc do not describe the lower-level delegate tool list output shape beyond the existing action surface, so no further user-doc edit was needed. Verification: `clojure -M:test --focus psi.agent-session.workflow-delegate-list-test --focus psi.agent-session.workflow-async-path-test` green (22 tests, 85 assertions); `clj-kondo --lint components/agent-session/src/psi/agent_session/workflow/core.clj components/agent-session/src/psi/agent_session/workflow/delegate_list.clj components/agent-session/src/psi/agent_session/workflow/text.clj components/agent-session/src/psi/agent_session/workflow/orchestration.clj components/agent-session/test/psi/agent_session/workflow_delegate_list_test.clj components/agent-session/test/psi/agent_session/workflow_async_path_test.clj` green (0 errors, 0 warnings). All task implementation checklist items are now complete; no further concrete implementation work remains.

2026-05-31 implementation review: found two actionable robustness gaps. First, `delegate list`/`delegate remove` only verify the query result contains `:psi.agent-session/background-jobs`; a nil or otherwise non-collection job payload is normalized as an empty list, which violates the design's requirement that non-shaped background-job read results surface as actionable errors rather than "No active runs"/unsafe removal. Second, `continue-blocked-run-async!` starts a new delegate background job before `psi.workflow/resume-run`; if `resume-run` returns `:psi.workflow/error` without throwing, the job is left non-terminal and `inflight-runs` is not cleaned up, so a listed run id can produce a permanently active/stale delegate attempt after a failed continue.

2026-05-31 implementation review follow-up: completed both actionable robustness items. `delegate list` and `delegate remove` now reject nil, scalar, and non-map element `:psi.agent-session/background-jobs` payloads as actionable background-job visibility shape errors instead of treating them as empty job sets; remove skips canonical workflow deletion on those errors. `continue-blocked-run-async!` now handles `psi.workflow/resume-run` error maps by marking the newly started delegate wrapper background job `:failed`, notifying, clearing `inflight-runs`, and refreshing widgets. Added focused coverage for malformed list/remove query payloads and failed blocked-run resume cleanup. Verification: `clj-paren-repair components/agent-session/src/psi/agent_session/workflow/core.clj components/agent-session/src/psi/agent_session/workflow/orchestration.clj components/agent-session/test/psi/agent_session/workflow_delegate_list_test.clj components/agent-session/test/psi/agent_session/workflow_async_path_test.clj` no changes needed; `clj-kondo --lint components/agent-session/src/psi/agent_session/workflow/core.clj components/agent-session/src/psi/agent_session/workflow/orchestration.clj components/agent-session/test/psi/agent_session/workflow_delegate_list_test.clj components/agent-session/test/psi/agent_session/workflow_async_path_test.clj` green (0 errors, 0 warnings); `clojure -M:test --focus psi.agent-session.workflow-delegate-list-test --focus psi.agent-session.workflow-async-path-test` green (24 tests, 102 assertions).

2026-05-31 implementation review: re-read task design/plan/steps/implementation and reviewed the delegate list/remove/continue implementation, pure projection, background-job model, text rendering, changelog entry, and focused tests. Verified focused delegate-list/async tests and targeted workflow lint remain green. No new actionable implementation feedback found after the previous follow-ups; existing review follow-up items are complete and no duplicate steps were added.

2026-05-31 test review: found one actionable coverage gap. Tests prove nil/scalar/non-map background-job payloads are rejected, but they do not cover the other required background-job read-surface failures: missing query function, query exception/unreadable surface, non-map query result, or query map missing `:psi.agent-session/background-jobs`. The implementation handles these paths, but the design explicitly reserves empty lists for successful no-visible-run projections, so these error branches need focused regression coverage for both `delegate list` and `delegate remove` where applicable.
