# Implementation notes

- 2026-06-06 architecture-fit review: ACTIONABLE_FEEDBACK. AR1: the design explicitly bypasses mementum's human approval gate for `memories`/`knowledge`, conflicting with AGENTS.md's MemoryArtifacts boundary (`mementum governs ... approval_gate`). Fit requires either a narrow protocol/meta amendment that authorizes this autonomous artifact-extraction path, or changing the workflow to emit approval-ready candidates instead of raw autonomous writes.
- 2026-06-06 AR1 executed. Chose the narrow protocol/meta amendment rather than weakening the workflow to approval-ready candidates, because autonomous completed-task extraction is the task's core intent. Updated AGENTS.md MemoryArtifacts boundary and the mementum protocol resource to authorize only `workflow(extract-task-knowledge)` over completed Munera task artifacts + git history, gated by gate-1/gate-2, project-generality, significance, dedupe recall, and `uncertain→skip`, with mementum commit conventions. Updated design.md's core tension/problem wording to reference the explicit exception, and marked AR1 done. No plan.md or steps.md touched. PASS_STATUS: FOLLOW_UPS_COMPLETE.
- 2026-06-06 ambiguity review: ACTIONABLE_FEEDBACK. Found AMB1–AMB4: clarify workflow file shape/naming (`.edn` + prompt `.md` risks a mixed-kind collision), completed-task slug resolution, task-specific git-history boundaries, and the `task-lifecycle` terminal-output/context contract after appending extraction. Did not review plan.md or steps.md.
- 2026-06-06 AMB1–AMB4 executed. Resolved workflow shape as a single prompt workflow `.psi/workflows/extract-task-knowledge.md` with no same-name `.edn`, plus only a trailing delegate step in `task-lifecycle.edn`. Defined completed-task resolution: exact slug match, duplicates/missing stop with no extraction; standalone extracts only from `munera/closed/`; lifecycle may extract from open task only when the successful `review-task-implementation` predecessor is present as completion proof. Defined git-history boundary to task-dir commits, task-id/slug message matches, and SHAs explicitly recorded in task artifacts. Defined lifecycle terminal contract: extraction receives review output as context and its final summary must preserve lifecycle/review outcome plus extraction result. Marked AMB1–AMB4 done. No plan.md or steps.md touched. PASS_STATUS: FOLLOW_UPS_COMPLETE.
- 2026-06-06 inconsistency review: ACTIONABLE_FEEDBACK. Found INC1/INC2. INC1: design says the protocol exception is for completed Munera task artifacts but also authorizes lifecycle extraction from a still-open task after implementation review; Munera's referenced state model defines task state by location (`open/` vs `closed/`), so open-path extraction is not currently covered by the completed-task exception. INC2: design scopes `doc/workflows.md` but omits the required CHANGELOG [Unreleased] entry for a new user-visible `/delegate` workflow and changed `task-lifecycle` behavior. Did not review plan.md or steps.md.

- 2026-06-06 INC1/INC2 executed. Reconciled the open-task lifecycle case by making it an explicit narrow autonomous-artifact-extraction source in AGENTS.md and the mementum protocol: closed task artifacts remain the standalone source, while still-open task artifacts are authorized only for `task-lifecycle` immediately after a successful `review-task-implementation` predecessor, with task-scoped git history and existing filters. Updated design.md to name Munera's location-based state and the lifecycle-local completion proof. Added the missing CHANGELOG `[Unreleased]` requirement for the new `/delegate extract-task-knowledge` workflow and changed `task-lifecycle` terminal behavior. Marked INC1/INC2 done. No plan.md or steps.md touched. PASS_STATUS: FOLLOW_UPS_COMPLETE.

- 2026-06-06 plan/steps ambiguity review: ACTIONABLE_FEEDBACK. Found PA1–PA3. PA1: plan forwards the original lifecycle `:input` while the extraction prompt requires an exact `NNN-slug`; existing `task-lifecycle-in-worktree` can pass `munera/open/NNN-slug`, so identifier normalization vs slug-only calling is unresolved. PA2: the single-step `.md` extraction prompt must explicitly consume lifecycle/review context (`{{original}}` or a named input var), otherwise the delegated review output may be invisible despite being added to `:context`. PA3: the open-task exception lacks an exact success marker for “immediately preceding review-task-implementation completed successfully” (delegate completion vs PASS_STATUS/final-summary/labeled context). Added unchecked PA1–PA3 to steps.md.

- 2026-06-06 PA1–PA3 executed. Resolved plan/steps ambiguity follow-ups without implementing older slices. PA1: extraction accepts and normalizes either an exact `NNN-slug` or exact `munera/{open|closed}/NNN-slug` task path; other shapes are rejected, so `task-lifecycle-in-worktree` can keep forwarding `munera/open/NNN-slug` unchanged. PA2: the markdown workflow must explicitly reference `{{original}}`, which compiles to `{:from :workflow-original}`, so lifecycle/review context is visible. PA3: the sole open-task success marker is `PASS_STATUS: REVIEW_COMPLETE` in the lifecycle-injected `review-task-implementation` yielded text supplied through `{{original}}`; delegate completion/final-summary prose/input text alone is insufficient. Updated plan.md and future implementation checklist items to lock these requirements in prompt/tests. No workflow/code/tests/docs changed in this follow-up pass. PASS_STATUS: FOLLOW_UPS_COMPLETE.

- 2026-06-06 plan/steps inconsistency review: REVIEW_COMPLETE. Reviewed `design.md`, `plan.md`, `steps.md`, prior `implementation.md`, `.psi/workflows/task-lifecycle.edn`, `task-lifecycle-in-worktree.edn`, workflow-loader/compiler tests around `{{original}}`, `doc/workflows.md`, and `CHANGELOG.md`. Found no new actionable cross-artifact inconsistencies; the plan and steps consistently carry PA1–PA3 identifier normalization, lifecycle `{{original}}` context, `PASS_STATUS: REVIEW_COMPLETE` open-task marker, tests, docs, changelog, and no same-name `.edn` workflow sibling requirements. PASS_STATUS: REVIEW_COMPLETE.

- 2026-06-06 implementation pass complete. Added `.psi/workflows/extract-task-knowledge.md` as the single-step markdown workflow with `read`/`bash`/`write`, exact slug/path normalization, closed-only standalone extraction, lifecycle-only open-task exception via `{{original}}` + `PASS_STATUS: REVIEW_COMPLETE`, task-scoped git-history lenses, mementum recall/dedupe, conservative gate-1/gate-2/project-generality/significance filters, autonomous mementum commit conventions, and zero-extraction success. Appended `extract-task-knowledge` as the final `task-lifecycle.edn` delegate while preserving the first five steps and passing the original input plus implementation-review yielded text context. Added workflow-loader tests for markdown loading/tools/context wiring/prompt contracts/no `.edn` sibling and updated task-lifecycle tests for the six-step terminal extraction wiring. Updated `doc/workflows.md` and CHANGELOG `[Unreleased]` Added/Changed entries. Verification: `clj-paren-repair components/workflow-loader/test/psi/workflow_loader/workflow_definitions_test.clj`; `clojure -M:test-paths -m scry.cli --namespace psi.workflow-loader.workflow-definitions-test` (12 tests/191 assertions); `clj-kondo --lint components/workflow-loader/test/psi/workflow_loader/workflow_definitions_test.clj`; EDN read check for `.psi/workflows/task-lifecycle.edn`. No mementum extension code changed and no same-name `.psi/workflows/extract-task-knowledge.edn` exists. PASS_STATUS: IMPLEMENTATION_COMPLETE.

- 2026-06-06 implementation review: ACTIONABLE_FEEDBACK. Found IR1. `task-lifecycle` forwards `:workflow-original` and the `review-task-implementation` yielded text as two raw delegate context sources; the callee markdown prompt sees them only through `{{original}}` as an unlabeled aggregate, so the open-task exception cannot prompt-visibly distinguish the lifecycle-injected review yield from arbitrary original context containing `PASS_STATUS: REVIEW_COMPLETE`. Add a prompt-visible label/wrapper (or equivalent unambiguous structure) for the review yield and lock it with workflow-loader tests. PASS_STATUS: ACTIONABLE_FEEDBACK.

- 2026-06-06 IR1 executed. Resolved the unlabeled-context authorization gap by moving the implementation-review yielded text out of raw delegate `:context` and into the extraction delegate `:prompt-string` map as a labeled `:implementation-review-yield` field. The extraction markdown prompt now declares `{{implementation_review_yield}}` from `[:implementation-review-yield]`, keeps `{{original}}` as ambient context only, and states that `PASS_STATUS: REVIEW_COMPLETE` in ambient `{{original}}` never authorizes open-task extraction. Updated workflow-loader tests to lock the new prompt variable, prompt content, and task-lifecycle wiring. Verification: `clj-paren-repair components/workflow-loader/test/psi/workflow_loader/workflow_definitions_test.clj`; `clojure -M:test-paths -m scry.cli --namespace psi.workflow-loader.workflow-definitions-test` (12 tests/195 assertions); `clj-kondo --lint components/workflow-loader/test/psi/workflow_loader/workflow_definitions_test.clj`; EDN read check for `.psi/workflows/task-lifecycle.edn`; markdown workflow parser/compiler smoke check. PASS_STATUS: FOLLOW_UPS_COMPLETE.

- 2026-06-06 implementation review pass 2: ACTIONABLE_FEEDBACK. Found IR2. IR1 correctly moved the authorization proof into labeled `:implementation-review-yield` prompt input, but `design.md`/`doc/workflows.md` and stale PA2/PA3 step wording still describe the implementation-review output as generic lifecycle context/`{{original}}` context. That cross-artifact drift weakens the source-distinction invariant IR1 fixed. Verification rerun: focused workflow definitions Scry 12/195 green; clj-kondo 0/0; `task-lifecycle.edn` read ok. PASS_STATUS: ACTIONABLE_FEEDBACK.

- 2026-06-06 IR2 executed. Synchronized the post-IR1 source-distinction wording across task artifacts and docs. `design.md` now states that the lifecycle implementation-review yield is carried as the labeled `:implementation-review-yield` prompt-string field, rendered/consumed through `{{implementation_review_yield}}`, while `{{original}}` / `:workflow-original` is ambient and non-authorizing. `doc/workflows.md` now documents the same lifecycle-open-task authorization boundary. Updated stale checked steps and PA2/PA3 notes so they no longer claim the review yield is supplied through `{{original}}` or raw delegate context. Verification: grep audit for stale `{{original}}`/raw-context authorization claims; no code/test/workflow changes required. PASS_STATUS: FOLLOW_UPS_COMPLETE.
- 2026-06-06 implementation review pass 3: REVIEW_COMPLETE. Reviewed task artifacts and referenced workflow/docs/tests after IR2: `.psi/workflows/extract-task-knowledge.md`, `.psi/workflows/task-lifecycle.edn`, workflow-loader definition tests, `doc/workflows.md`, CHANGELOG, AGENTS/mementum protocol exception, and runtime source-resolution/delegate yield semantics. The labeled `:implementation-review-yield` source distinction is now coherent across prompt, lifecycle wiring, tests, design, docs, and steps; no new actionable implementation issues found. Verification: focused workflow definitions Scry 12/195 green; clj-kondo 0/0; `task-lifecycle.edn` read ok. PASS_STATUS: REVIEW_COMPLETE.
- 2026-06-06 task-test-review: ACTIONABLE_FEEDBACK. Found TT1/TT2. Existing workflow-loader tests cover loading, tool set, source wiring, slug/path normalization, closed/open authorization, task-scoped history, dedupe/filter basics, zero-extraction success, and lifecycle delegate wiring, and focused Scry is green (12 tests/195 assertions). New gaps: tests do not lock the autonomous persistence/write contract (no approval, output file shapes/frontmatter/symbol constraints, mementum commit conventions, no commit on no changes) or the final-summary/reporting contract that preserves the dedicated implementation-review yield alongside extraction results. Added unchecked TT1/TT2 to steps.md. PASS_STATUS: ACTIONABLE_FEEDBACK.

- 2026-06-06 TT1/TT2 executed. Added workflow-loader prompt content-lock coverage for the autonomous persistence/write contract (no human approval, allowed memory/knowledge paths, memory symbol/one-insight/under-200 constraints, knowledge frontmatter, mementum commit conventions, and `Do not commit if nothing changed`) and the final-summary/reporting contract (resolved path, standalone vs lifecycle-authorized mode, extracted entries, updated/skipped duplicates, zero-extraction success, and preservation of the dedicated `{{implementation_review_yield}}` lifecycle/review outcome). Verification: `clj-paren-repair components/workflow-loader/test/psi/workflow_loader/workflow_definitions_test.clj`; `clojure -M:test-paths -m scry.cli --namespace psi.workflow-loader.workflow-definitions-test` (12 tests/213 assertions); `clj-kondo --lint components/workflow-loader/test/psi/workflow_loader/workflow_definitions_test.clj` (0/0). PASS_STATUS: FOLLOW_UPS_COMPLETE.

- 2026-06-06 task-test-review pass 2: ACTIONABLE_FEEDBACK. Found TT3. Re-read task artifacts plus `.psi/workflows/extract-task-knowledge.md`, `.psi/workflows/task-lifecycle.edn`, workflow-loader tests, docs, and changelog. Focused workflow definitions Scry is green (12 tests/213 assertions) and clj-kondo is clean, but the tests still do not lock the prompt's required task-artifact inspection surface (`design.md`, `plan.md`, `steps.md`, `implementation.md`). Dropping those lines would leave current content locks green while weakening the core extraction evidence for AC2/AC3. Added TT3 to `steps.md`. PASS_STATUS: ACTIONABLE_FEEDBACK.

- 2026-06-06 TT3 executed. Added workflow-loader prompt content-lock coverage for the required task-artifact inspection surface: before mining knowledge, the extraction workflow must read `design.md`, `plan.md`, `steps.md`, and `implementation.md` from the resolved task when present. Tightened `.psi/workflows/extract-task-knowledge.md` wording to make the before-mining/resolved-task boundary explicit. Verification: `clj-paren-repair components/workflow-loader/test/psi/workflow_loader/workflow_definitions_test.clj`; `clojure -M:test-paths -m scry.cli --namespace psi.workflow-loader.workflow-definitions-test` (12 tests/218 assertions); `clj-kondo --lint components/workflow-loader/test/psi/workflow_loader/workflow_definitions_test.clj` (0/0). PASS_STATUS: FOLLOW_UPS_COMPLETE.
- 2026-06-06 task-test-review pass 3: REVIEW_COMPLETE. Re-read the task-test-review skill, task artifacts, `.psi/workflows/extract-task-knowledge.md`, `.psi/workflows/task-lifecycle.edn`, workflow-loader tests, `doc/workflows.md`, and CHANGELOG after TT3. The tests now lock markdown workflow loading/no `.edn` sibling, exact tools, input/original/dedicated review-yield wiring, slug/path normalization, closed/open authorization boundaries, required task-artifact inspection, task-scoped history lenses, dedupe/filter/zero-extraction behavior, autonomous persistence/write conventions, final-summary preservation, and task-lifecycle trailing delegate wiring. Verification rerun: focused workflow definitions Scry 12/218 green; clj-kondo 0/0; `task-lifecycle.edn` read ok. No new actionable test-quality gaps found; no steps.md items added. PASS_STATUS: REVIEW_COMPLETE.
- 2026-06-06 test-shaper review: REVIEW_COMPLETE. Reviewed task artifacts plus `.psi/workflows/extract-task-knowledge.md`, `.psi/workflows/task-lifecycle.edn`, workflow-loader definition tests, `doc/workflows.md`, and CHANGELOG. Test shape is acceptable for prompt/workflow contracts: focused content locks are grouped by behavior, source wiring assertions distinguish input/original/review-yield, lifecycle wiring assertions are narrow and explicit, no redundant or flaky setup found. Verification rerun: focused workflow definitions Scry 12/218 green; clj-kondo 0/0; `task-lifecycle.edn` read ok. No new actionable test-shaping issues found; no steps.md items added. PASS_STATUS: REVIEW_COMPLETE.
- 2026-06-06 docs review: ACTIONABLE_FEEDBACK. Found DOC1. `CHANGELOG.md` `[Unreleased]` has the new `extract-task-knowledge` Added entry and the `task-lifecycle` Changed entry, but its earlier `task-lifecycle` Added entry still describes the workflow as only design → plan → implement → review by chaining five delegates. Because both entries are in the same unreleased section, that stale description is user-facing drift. Added DOC1 to `steps.md`; no duplicate docs issue found. PASS_STATUS: ACTIONABLE_FEEDBACK.

- 2026-06-06 DOC1 executed. Updated `CHANGELOG.md` `[Unreleased]` so the earlier `task-lifecycle` Added entry no longer describes the workflow as only design → plan → implement → review / five delegates. It now names the shipped design → plan → implement → review → extract-knowledge lifecycle and six delegated workflows, consistent with the Changed entry. Marked DOC1 done in `steps.md`. Verification: changelog grep for task-lifecycle Added/Changed wording and unchecked-step audit. PASS_STATUS: FOLLOW_UPS_COMPLETE.

- 2026-06-06 docs review pass 2: ACTIONABLE_FEEDBACK. Found DOC2. `doc/workflows.md` documents standalone extraction as closed-task-only but its usage example names the current in-flight task `216-extract-task-knowledge-workflow`; as a user-facing example, that can imply standalone extraction works on an open task and contradicts the documented/implemented closed-only boundary. Replace it with a generic `{NNN-slug}` completed-task placeholder or an explicitly closed-task example. Added DOC2 to `steps.md`. PASS_STATUS: ACTIONABLE_FEEDBACK.

- 2026-06-06 DOC2 executed. Replaced the `doc/workflows.md` `extract-task-knowledge` standalone usage example that named the in-flight open task `216-extract-task-knowledge-workflow` with the generic completed-task placeholder `{NNN-slug}`. This keeps the user-facing example consistent with the documented standalone closed-task-only boundary. No code/tests/changelog changes required. Verification: targeted grep of the workflow docs and unchecked-step audit. PASS_STATUS: FOLLOW_UPS_COMPLETE.
- 2026-06-06 docs review pass 3: REVIEW_COMPLETE. Re-read `review-task-docs` skill, task artifacts, `.psi/workflows/extract-task-knowledge.md`, `.psi/workflows/task-lifecycle.edn`, workflow-loader definition tests, `doc/workflows.md`, `CHANGELOG.md`, and README workflow surface. User-facing docs are coherent: `doc/workflows.md` documents standalone closed-task-only extraction, accepted input shapes, task-scoped evidence, conservative gates/dedupe, autonomous commits, zero-extraction success, and lifecycle-only open-task authorization through labeled `{{implementation_review_yield}}`; CHANGELOG `[Unreleased]` has Added/Changed entries matching the shipped workflow and lifecycle terminal behavior; README appropriately links to workflow docs without enumerating individual workflows. No stale examples or duplicate docs issues found; no steps.md items added. PASS_STATUS: REVIEW_COMPLETE.
