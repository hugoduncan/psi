- [ ] Make the canonical review/follow-up surface explicit in task artifacts: add `design-steps.md` / `implementation.md` to `design.md` and `plan.md` (or name an authoritative alternative), since this task currently requires ambiguity follow-ups in `design-steps.md` without declaring that artifact in the task itself.
- [ ] Resolve the trigger-boundary ambiguity for notification-dismiss maintenance: decide whether idle notification expiry/overflow cleanup should remain timerless and event-driven only, or whether a narrower explicit timer/event path is still required, and record the authoritative rule in `design.md` / `plan.md`.
- [ ] Resolve the proof-boundary ambiguity for current tick-provided behaviors: specify the minimum authoritative pre-change proof set and exact test targets/commands for UI snapshot refresh, notification maintenance, extension command-name refresh, and streaming spinner/progress behavior so acceptance is not satisfied by materially different focused suites.
