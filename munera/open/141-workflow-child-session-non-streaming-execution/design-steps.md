- [x] Decide whether workflow IR `session-spec-schema` must explicitly accept `:response-mode`; design/plan currently require workflow step/session config to accept it, but the cited schema currently omits that field.
- [x] Decide the canonical default contract for persisted child-session `:response-mode`; design says child-session resolution defaults to `:streaming` while the control-surface section also says ordinary sessions may leave it absent, so implementation/tests need one unambiguous persistence rule.
- [x] Decide the exact lower AI/provider seam for non-streaming execution (`psi.ai.core` API + provider contract + OpenAI transport helper) before implementing turn-runtime branching; current artifacts name turn-runtime branching but do not specify the new non-streaming call surface.
