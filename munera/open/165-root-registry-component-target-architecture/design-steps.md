- [x] Clarify the ownership transition rule on re-register: whether an existing id may be replaced by an entry from a different extension owner, and if not, what explicit failure status/result shape the shared component returns.
- [x] Clarify unknown-registry semantics for reads and writes: whether lookup on an unknown registry is always a nil miss while mutating operations fail, or whether all operations use a distinct unknown-registry failure contract.
- [x] Assess registry fit more concretely by recording, for tool/command/skill/prompt/workflow/deterministic-operation registries, whether each is a direct adopter, needs an adapter, or is intentionally out of scope for the shared component because of ordering/invoke/compatibility semantics.
- [x] Resolve the adopter assessment inconsistency around prompt and skill registries: the design classifies `prompt-registry` as an adapter-backed adopter even though the current prompt registry is the ordered `contributions` owner, and classifies `skill-registry` as a direct adopter even though the current skill registry is also registration-order-based first-registration-wins rather than id-keyed replace-by-id.
- [x] Reconcile the unknown-registry mutation rule with the storage-model text: the design says mutating operations require a recognized registry and fail on unknown registries, but also says a registry may become present on first successful registration depending on implementation choice.
