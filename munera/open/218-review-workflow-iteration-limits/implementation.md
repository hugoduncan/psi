2026-06-07 architecture review: Design fits the workflow architecture. It keeps changes localized to deterministic EDN workflow topology/guards plus workflow-definition checks/docs, preserves review/follow-up prompt behavior, uses existing `:max-iterations` iteration guards rather than runtime shims, and respects the task-artifact split by leaving implementation details for planning. No new actionable architectural-fit feedback. PASS_STATUS: REVIEW_COMPLETE

2026-06-07 ambiguity review: Found actionable design ambiguities around iteration counting and loop topology. The design states 10 implementation follow-up/review cycles and 6/5 full design/plan passes, but does not pin whether `:max-iterations` counts initial step entries or follow-up executions; it also leaves unclear whether design/plan loop-backs restart immediately after an actionable phase or after completing the current full phase sequence, and how the workflow records "any phase found feedback" once follow-up clears the item. Added AMB1-AMB3 to design-steps.md. PASS_STATUS: ACTIONABLE_FEEDBACK

2026-06-07 ambiguity follow-up: Completed AMB1-AMB3. Design now pins `:max-iterations` as total target-step entries including initial entry; implementation review should use `follow-up` → `review` `:max-iterations 10` for at most 10 total reviews. Design/plan review should finish all phases in the current pass before loop-back, preserve explicit pass-level "any feedback" routing instead of re-reading cleared follow-up files, guard loop-back to the first phase (`architecture-review` limit 6 for design, `ambiguity-review` limit 5 for plan), and fail through the existing iteration-limit path when the final allowed pass still yields feedback. PASS_STATUS: FOLLOW_UPS_COMPLETE

2026-06-07 inconsistency review: Rechecked design.md against the referenced review workflow EDNs, workflow iteration-counting runtime/tests, built-in routing operations, and workflow docs. After AMB1-AMB3, the design is internally consistent: total-entry `:max-iterations` semantics align with runtime behavior, implementation review's shared `review-step` surface currently matches the implementation-review-only delegate use, and design/plan full-pass loop requirements explicitly account for follow-up clearing by requiring route/state memory before guarded restart. No new actionable inconsistency feedback. PASS_STATUS: REVIEW_COMPLETE

2026-06-07 plan/steps ambiguity review: Found one new actionable ambiguity. Plan/steps say design/plan pass completion must be encoded in deterministic topology/routing rather than re-reading `design-steps.md` or `steps.md`, but the repository still contains `review-task-design-clarity-status.md` and `review-task-plan-clarity-status.md` prompt workflows whose wording explicitly instructs artifact re-reading after follow-up. The implementation plan does not say whether these stale prompt files should be removed, left unreachable, or rewritten/tested, leaving an ambiguous docs/tests/code cleanup boundary. Added PA1 to `steps.md`. PASS_STATUS: ACTIONABLE_FEEDBACK

2026-06-07 plan/steps follow-up PA1: Removed the stale standalone clarity-status prompt workflows (`review-task-design-clarity-status.md` and `review-task-plan-clarity-status.md`) because current design/plan EDN hosts encode `clarity-status` as deterministic invoke routing instead of artifact re-reading. Added workflow-definition regression coverage that those prompt files do not exist and are not referenced by the host EDNs. Updated `doc/workflows.md` to state that clarity-status is EDN invoke routing, not a prompt workflow. Marked PA1 done in `steps.md`.
Verification for PA1: `bb clojure:test:scry -v psi.workflow-loader.workflow-definitions-test/review-task-prompt-artifact-targets-test` passed (28 assertions). Targeted `clj-kondo --lint components/workflow-loader/test/psi/workflow_loader/workflow_definitions_test.clj` passed with 0 errors/0 warnings. A broader namespace run (`bb clojure:test:scry --namespace psi.workflow-loader.workflow-definitions-test`) still fails in pre-existing `task-lifecycle-test` assertions about the current task-lifecycle topology, unrelated to the clarity-status prompt cleanup.

2026-06-07 plan/steps inconsistency review: Rechecked `design.md`, `plan.md`, `steps.md`, prior implementation notes, current review workflow EDNs/tests, and `doc/workflows.md`. No new actionable inconsistency feedback beyond existing planned work: remaining mismatches in `review-step` limit, design/plan full-pass topology, tests, docs, and changelog are already represented by unchecked Slice 2-7 steps, and PA1's stale clarity-status prompt cleanup is already completed. No steps added. PASS_STATUS: REVIEW_COMPLETE

2026-06-07 implementation pass: Completed Slice 1 and Slice 2. Inspected workflow usages: only `.psi/workflows/review-task-implementation.edn` delegates to shared `review-step`, with five implementation/test/docs/shape review profiles, so the shared limit change is acceptable. Existing coverage seams were `workflow_definitions_test.clj` for authored EDN assertions and `workflow_review_step_routing_test.clj` for runtime iteration behavior. Updated `.psi/workflows/review-step.edn` follow-up loop to `:max-iterations 10`, updated definition coverage, and added focused runtime coverage proving ten total `review` entries are allowed while the next loop-back fails via `:iteration-exhausted`. Updated `doc/workflows.md` and `CHANGELOG.md` for the user-visible implementation-review limit. Verification: `clj-paren-repair components/agent-session/test/psi/agent_session/workflow_review_step_routing_test.clj` success; focused Scry `psi.agent-session.workflow-review-step-routing-test/review-step-actionable-feedback-runs-follow-up-and-loops-back-via-constant-routing-test` passed (14 assertions); focused Scry `psi.workflow-loader.workflow-definitions-test/review-step-test` passed (17 assertions); targeted `clj-kondo` on touched workflow routing/definition tests passed with 0 errors/0 warnings.

2026-06-07 implementation pass: Completed Slice 3, Slice 4, remaining Slice 5 coverage, docs/changelog sync, and verification. Added deterministic `workflow/pass-feedback-routing` operation for review pass-level memory: it inspects current pass review outputs for `PASS_STATUS: ACTIONABLE_FEEDBACK` and routes `REPEAT` if any phase found feedback, otherwise `DONE`. Reworked `review-task-design.edn` so each pass runs architecture, ambiguity, and inconsistency in order, preserves per-phase follow-ups, then routes from `clarity-status` back to `architecture-review` with `:max-iterations 6` or to `final-summary`. Reworked `review-task-plan.edn` similarly for ambiguity/inconsistency, guarded back to `ambiguity-review` with `:max-iterations 5`. Updated workflow definition tests for authored topology/limits and runtime routing tests for clean passes, earlier-phase actionable feedback completing later phases before restart, pass-level memory after follow-up, and final allowed pass iteration-limit failure. Updated `doc/workflows.md` and `CHANGELOG.md` for repeated full-pass behavior.
Verification: `clj-paren-repair components/agent-session/src/psi/agent_session/workflow/core.clj components/agent-session/test/psi/agent_session/workflow_review_step_routing_test.clj components/workflow-loader/test/psi/workflow_loader/workflow_definitions_test.clj` success; focused Scry (`review-task-design-test`, `review-task-plan-test`, `review-step-test`, review-step loop, design/plan full-pass, iteration-limit failure) passed 7 tests/126 assertions; namespace Scry `psi.agent-session.workflow-review-step-routing-test` passed 11 tests/78 assertions; focused workflow definition Scry passed 3 tests/87 assertions; targeted `clj-kondo` on touched test files passed 0 errors/0 warnings. Targeted lint including `workflow/core.clj` reports one pre-existing unrelated unresolved arity warning/error at `delegate-list-text` near line 368, outside this pass's changed forms.

2026-06-07 implementation-review pass: Rechecked task artifacts, `.psi/workflows/review-step.edn`, `review-task-design.edn`, `review-task-plan.edn`, deterministic routing operation wiring, workflow-loader definition tests, runtime routing tests, docs, and CHANGELOG. Implementation matches design: review-step allows 10 total review entries, design/plan full-pass loops finish all phases before guarded restart, pass-level feedback memory is deterministic route state rather than artifact re-reading, and final allowed pass failures surface through the iteration-limit path. Focused verification re-run: `bb clojure:test:scry --namespace psi.agent-session.workflow-review-step-routing-test` green (11/78) and focused workflow-definition vars green (3/87). `clj-kondo` including touched `workflow/core.clj` still reports the already-recorded unrelated pre-existing `delegate-list-text` arity issue outside the changed forms; no new actionable implementation feedback found. PASS_STATUS: REVIEW_COMPLETE
