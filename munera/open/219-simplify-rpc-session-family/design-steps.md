# Design follow-up steps

- [x] AR1: Tighten the design so any extracted/shared decision surface around `commands`, `command-results`, `command-tree`, `command-pickers`, `command-resume`, `navigation`, or `frontend-actions` must either remain a narrow RPC protocol-adaptation seam or move adapter-neutral selector/navigation/picker/action/result semantics toward existing `app-runtime` ownership; forbid creating a new RPC-owned domain/orchestration hub for semantics both TUI and Emacs need.
- [x] AR2: Tighten the design so simplification of `projections`, `streams`, and `emit` preserves the projection delivery architecture: canonical state/app-runtime public models remain authoritative, RPC only performs subscriber-aware fanout and protocol payload recomputation/adaptation with connection-local focus, and the refactor must not introduce RPC-local cached/source-of-truth projection state or polling-style delivery.
