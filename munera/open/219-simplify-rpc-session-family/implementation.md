2026-06-07 architecture-fit design review: found AR1–AR2. The design fits the behaviour-preserving Gordian/test-net/gate direction, but it must pin RPC/app-runtime ownership before planning: adapter-neutral selector/navigation/picker/result semantics should not be consolidated into new RPC-owned domain helpers, and projection/stream/emit simplification must keep RPC as transport fanout/adaptation over canonical state, not a duplicated projection source.

2026-06-07 design follow-ups AR1–AR2 executed. Tightened `design.md` ownership constraints before planning: shared selector/navigation/picker/action/result semantics must either remain narrow RPC protocol adaptation or move toward existing app-runtime ownership, never a new RPC-owned semantic hub; projection/stream/emit simplification must keep canonical state/app-runtime public models authoritative, with RPC limited to subscriber-aware fanout, payload recomputation/adaptation, framing, and connection-local focus. Marked AR1–AR2 done in `design-steps.md`; no plan/steps/code touched. PASS_STATUS FOLLOW_UPS_COMPLETE.

2026-06-07 ambiguity design review: found AMB1. The design names post-validation artifacts as `compare.edn` / `gate.edn`, while the architecture workflow/docs/review gates use `architecture-compare.edn` / `architecture-gate.edn`; planning needs one authoritative filename set so validation-capture and reviews read/write the same artifacts. PASS_STATUS ACTIONABLE_FEEDBACK.

2026-06-07 design follow-up AMB1 executed. Chose `architecture-compare.edn` and `architecture-gate.edn` as the authoritative post-implementation compare/gate artifact names, matching `.psi/workflows/reduce-architectural-complexity.edn` validation-capture and `doc/workflows.md` review-gate contract. Updated `design.md` validation commands, acceptance wording, and final artifact criterion; marked AMB1 done in `design-steps.md`; no plan/steps/code touched. PASS_STATUS FOLLOW_UPS_COMPLETE.

2026-06-07 inconsistency design review: no new actionable feedback. Rechecked `design.md` against `architecture-targets.edn`, `target-issues.edn`, the shipped `reduce-architectural-complexity` validation/review contract, architecture workflow docs, and the referenced RPC session source/test paths. The selected family membership, source-area blast radius, target-issues hypothesis boundary, ownership constraints, pre-simplification gates, and `architecture-compare.edn` / `architecture-gate.edn` artifact names are internally consistent after prior AR/AMB follow-ups. PASS_STATUS REVIEW_COMPLETE.
2026-06-07 plan/steps ambiguity review: found PA1–PA3. Ambiguities: focused RPC baseline/characterization verification is described behaviourally but not pinned to exact command(s)/namespace set; the coverage map may live in `implementation.md` or an unnamed task-local artifact; Slice 6 review gates do not name the exact architecture-level review-step skill sequence, leaving the architecture gate open to the design-only `review-task-architecture` instead of `review-implementation-architecture`. Added PA1–PA3 to `steps.md`. PASS_STATUS ACTIONABLE_FEEDBACK.

2026-06-07 plan/steps ambiguity follow-ups PA1–PA3 executed. PA1 pinned the focused RPC baseline/characterization command in `plan.md` to `bb clojure:test:scry --dir components/rpc/test` with namespaces `psi.rpc-command-results-test`, `psi.rpc-prompt-command-test`, `psi.rpc-prompt-test`, `psi.rpc-session-navigation-test`, `psi.rpc-events-test`, `psi.rpc-invariants-test`, `psi.rpc-ops-test`, and `psi.rpc-test`, matching the design-listed affected test files; any newly added characterization namespace must update the command before Slice 2 commit. PA2 chose `munera/open/219-simplify-rpc-session-family/coverage-map.md` as the authoritative coverage/gap artifact, created its Slice 2 template, and pinned its minimal sections plus Slice 2/5 references. PA3 pinned Slice 6 to the architecture workflow's ordered `review-step` skills: `task-implementation-review`, `task-test-review`, `review-implementation-architecture`, `test-shaper`, `review-task-docs`, and `code-shaper`; the architecture gate must use `review-implementation-architecture`, not design-only `review-task-architecture`. No code/tests/docs changed. PASS_STATUS FOLLOW_UPS_COMPLETE.

2026-06-07 plan/steps inconsistency review: found PI1–PI3. Inconsistencies: Slice 1 steps record worktree status but do not enforce the design/plan's clean pre-refactor baseline before running/claiming the baseline; Slice 2 plan says newly added characterization namespaces must propagate to the exact focused command and `characterization-baseline.edn`, while steps only generally rerun the pinned suite; the adjacent-source escape hatch is stricter in design/plan than in steps, which only says to update `plan.md` and does not require the exact file/reason/narrow contract-alignment/design synchronization before editing adjacent production code. Added PI1–PI3 to `steps.md`. PASS_STATUS ACTIONABLE_FEEDBACK.

2026-06-07 plan/steps inconsistency follow-ups PI1–PI3 executed. PI1 tightened the clean-baseline gate: `plan.md` and Slice 1 now require `git status --short --branch` to show only the branch header/no short-status entries before running or claiming a clean baseline, with pre-existing dirt recorded and stopped/resolved first. PI2 tightened focused-suite propagation: if Slice 2 adds a new RPC characterization namespace/file, `plan.md`, `steps.md`, `coverage-map.md`, and `characterization-baseline.edn` must share one authoritative command/namespace set before committing the characterization gate. PI3 tightened the adjacent-source escape hatch: any adjacent production edit requires stopping first and synchronizing `design.md`/`plan.md` with exact root-relative file, necessity, and narrow behaviour-preserving contract-alignment rationale, explicitly excluding broader redesign. No code/tests/docs changed; these were pre-implementation task-artifact consistency fixes. PASS_STATUS FOLLOW_UPS_COMPLETE.

2026-06-07 pre-characterization clean-source baseline recorded. Re-read `design.md`, `plan.md`, `steps.md`, `implementation.md`, `architecture-targets.edn`, `before-diagnose.edn`, and `target-issues.edn`; confirmed selected candidate `[:family "psi.rpc.session"]` / `:family`, fixed target namespaces/source areas, empty allowed-adjacent source areas, and the design-listed affected RPC test areas without recomputing membership. `git status --short` was empty before writing the baseline, and `git status --short -- <target source ∪ allowed adjacent source>` had no entries. Wrote `characterization-baseline.edn` with HEAD/status, selected candidate map, target metadata, file existence checks, and no pre-existing task-artifact/doc dirt. Target/source areas are clean enough to characterize. PASS_STATUS REVIEW_COMPLETE

2026-06-07 characterization-test-net review: CHARACTERIZATION_STATUS: FIXABLE_GAPS. Reviewed task artifacts (`design.md`, `plan.md`, `steps.md`, `implementation.md`, `coverage-map.md`, `characterization-baseline.edn`) and confirmed the selected architecture target is clear: candidate `[:family "psi.rpc.session"]` / `:family`; target/source areas are the eleven `components/rpc/src/psi/rpc/session/*.clj` files listed in the design; allowed adjacent production source areas are empty; affected behaviours under review are command dispatch/results, picker/model/thinking/frontend actions, command-tree/resume/navigation, prompt/stream progress, and projection/emit delivery. Inspected the pinned RPC test namespaces and ran the focused suite: `bb clojure:test:scry --dir components/rpc/test --namespace psi.rpc-command-results-test --namespace psi.rpc-prompt-command-test --namespace psi.rpc-prompt-test --namespace psi.rpc-session-navigation-test --namespace psi.rpc-events-test --namespace psi.rpc-invariants-test --namespace psi.rpc-ops-test --namespace psi.rpc-test` => green, 54 tests / 398 assertions. The test net is not yet sufficient for the architecture simplification gate because the authoritative coverage artifact `coverage-map.md` still contains only the Slice 2 template (`To be completed`) rather than a source-area/behaviour coverage review, and `characterization-baseline.edn` does not yet record the baseline command/result summary required by `steps.md`. Concrete coverage work required before simplification: fill `coverage-map.md` with one subsection per target source file mapping externally observable behaviours to test vars; record command/result, picker/model/thinking/frontend-action, command-tree/resume/navigation, prompt/stream, and projection/emit dispositions as `covered-by`, `added-test`, `accepted-existing-coverage`, or `infeasible-stop`; add any fixable characterization tests discovered by that map using real collaborators/nullable infrastructure seams and state/output assertions; update `characterization-baseline.edn` with the authoritative focused command and green pass/fail summary; rerun the pinned suite and commit the completed characterization gate before any production refactor. PASS_STATUS ACTIONABLE_FEEDBACK.

2026-06-07 characterization fix pass: added one targeted RPC characterization gap fix before simplification. Filled `coverage-map.md` with source-area and behaviour coverage, then added `psi.rpc-test/rpc-model-and-thinking-picker-frontend-actions-test` to characterize `/model` and `/thinking` command picker RPC outputs plus submitted `select-model` and `select-thinking-level` frontend-action results. Assertions are on externally observable RPC frames and canonical session/footer outputs. No production files changed and no testability seam was added. Updated `characterization-baseline.edn` with the authoritative focused command and green result summary. Verification: `bb clojure:test:scry --dir components/rpc/test --namespace psi.rpc-command-results-test --namespace psi.rpc-prompt-command-test --namespace psi.rpc-prompt-test --namespace psi.rpc-session-navigation-test --namespace psi.rpc-events-test --namespace psi.rpc-invariants-test --namespace psi.rpc-ops-test --namespace psi.rpc-test` => green, 55 tests / 422 assertions. Remaining coverage-review candidates are recorded in `coverage-map.md` for the next review pass; simplification remains forbidden until coverage review returns complete. PASS_STATUS REVIEW_COMPLETE

2026-06-07 characterization-test-net review pass 2: CHARACTERIZATION_STATUS: FIXABLE_GAPS. Re-read task artifacts (`design.md`, `plan.md`, `steps.md`, `coverage-map.md`, `characterization-baseline.edn`, `architecture-targets.edn`, `target-issues.edn`, `before-diagnose.edn`) and confirmed the selected architecture target remains clear: candidate `[:family "psi.rpc.session"]` / `:family`; target/source areas are the eleven captured `components/rpc/src/psi/rpc/session/*.clj` files; allowed adjacent production source areas are empty; affected behaviours are command dispatch/results, picker/model/thinking/frontend actions, command-tree/resume/navigation, prompt/stream, and projection/emit delivery. Inspected existing/new focused RPC tests and source for the recorded pending coverage candidates. The focused suite is green: `bb clojure:test:scry --dir components/rpc/test --namespace psi.rpc-command-results-test --namespace psi.rpc-prompt-command-test --namespace psi.rpc-prompt-test --namespace psi.rpc-session-navigation-test --namespace psi.rpc-events-test --namespace psi.rpc-invariants-test --namespace psi.rpc-ops-test --namespace psi.rpc-test` => 55 tests / 422 assertions. The net is still not sufficient for the architecture simplification gate because `coverage-map.md` intentionally leaves target-local edge/boundary behaviours as `pending-characterization-review`: `/tree name <session-id|prefix> <name>` rename output/state, `/tree <missing>` not-found text, `/tree <active-session>` already-active text, ambiguous/prefix selection where observable, and `frontend_action_result` cancelled/failed command-result/error payloads. These are externally observable outputs/state in target source areas (`command_tree.clj`, `frontend_actions.clj`) and are fixable with narrow RPC tests using the existing real session context/test harness, no mocks, and assertions on response frames, command-result events, session name state, and absence/presence of navigation events. Concrete coverage work required before simplification: add characterization cases in the pinned RPC suite for command-tree rename/missing/already-active/prefix-boundary behaviours; add characterization cases for cancelled and failed frontend-action result payloads for at least one representative action; update `coverage-map.md` dispositions from `pending-characterization-review` to `added-test` or `accepted-existing-coverage`; update `characterization-baseline.edn` result summary if assertion/test counts change; rerun the pinned focused suite green; commit the completed characterization gate before any production refactor. PASS_STATUS ACTIONABLE_FEEDBACK.


2026-06-07 characterization fix pass 2: added the next constrained characterization-test-net fixes before simplification. Added `psi.rpc-session-navigation-test/rpc-tree-command-edge-behaviour-test` to lock externally observable `/tree` edge behaviours: already-active text with no rehydration, missing-session text with no rehydration, rename text plus canonical session-name state mutation, and unique-prefix switch resume/rehydrate/context events. Added `psi.rpc-test/rpc-frontend-action-cancelled-and-failed-result-test` to lock cancelled/failed `frontend_action_result` accepted responses, command-result payloads, and no session/footer snapshot emission. Updated `coverage-map.md` dispositions and `characterization-baseline.edn` counts. No production files changed and no testability seam was added. Verification: focused navigation/RPC subset green (19 tests / 173 assertions); pinned focused suite green (57 tests / 439 assertions). PASS_STATUS REVIEW_COMPLETE

2026-06-07 characterization-test-net review pass 3: reviewed the completed pre-simplification characterization net and found it sufficient for the architecture simplification gate. Confirmed the selected target is clear and unchanged: candidate `[:family "psi.rpc.session"]` / `:family`; target source areas are the eleven captured `components/rpc/src/psi/rpc/session/*.clj` files; allowed adjacent production source areas remain empty; affected behaviours under review are command dispatch/results, picker/model/thinking/frontend actions, command-tree/resume/navigation, prompt/stream progress, and projection/emit delivery. Reviewed `coverage-map.md`, `characterization-baseline.edn`, the newly added tests `psi.rpc-test/rpc-model-and-thinking-picker-frontend-actions-test`, `psi.rpc-session-navigation-test/rpc-tree-command-edge-behaviour-test`, and `psi.rpc-test/rpc-frontend-action-cancelled-and-failed-result-test`, plus the existing pinned RPC namespaces. Coverage now maps every target source area to externally observable state/output assertions: command/result mapping and blank/error cases; picker command payloads and submitted model/thinking updates; command tree/resume/navigation nominal, edge, and boundary cases including new/switch/fork/resume, already-active, missing, rename, and unique-prefix matching; frontend action submitted/cancelled/failed result payloads; prompt slash/stream/tool/retry/footer behaviour; and event-driven projection/emit snapshots and invalidations. The net uses focused RPC subsystem/narrow integration tests with real session context/transport collaborators or existing nullable/provider seams, and assertions are on response frames, emitted events, canonical session state, or public payloads rather than implementation interactions. Commands run: `bb clojure:test:scry --dir components/rpc/test --namespace psi.rpc-command-results-test --namespace psi.rpc-prompt-command-test --namespace psi.rpc-prompt-test --namespace psi.rpc-session-navigation-test --namespace psi.rpc-events-test --namespace psi.rpc-invariants-test --namespace psi.rpc-ops-test --namespace psi.rpc-test` => green, 57 tests / 439 assertions. No production simplification/refactor work performed. PASS_STATUS REVIEW_COMPLETE
