- 2026-05-26: Reviewed design for ambiguities against current skill discovery/registry code. Found one actionable ambiguity: the design chooses collision precedence `project > user-global > built-in`, but does not specify how existing `discover-skills` first-discovered-wins loading order and `:extra-paths` precedence must change to realize that rule deterministically. `plan.md`, `steps.md`, and `design-steps.md` are still absent.
