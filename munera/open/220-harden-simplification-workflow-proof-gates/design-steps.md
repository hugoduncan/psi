- [ ] AR1: Pin terminal-stop failing-gate context to an architecture-conforming workflow boundary: either split terminal-stop summary steps per failing gate or pass an explicit prior-step source/map through existing workflow source/judge data-flow; do not require hidden runtime state, cross-step introspection, or ad-hoc inference from missing artifacts.
- [ ] AR2: Define proof-artifact synchronization as a no-op/verifying fixed-point before final summary: if artifacts are stale, update and commit them, then route back through the relevant review/proof gate or repeat proof-sync until clean; final success must not follow directly from a mutating proof-sync pass.
- [ ] AR3: Make committed task-local artifacts the authoritative proof-sync inputs and outputs; workflow/review yields may be context only. The design must not depend on ephemeral child-session prose to establish final proof coherence across session/workflow boundaries.
