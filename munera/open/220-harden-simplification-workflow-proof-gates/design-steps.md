- [x] AR1: Pin terminal-stop failing-gate context to an architecture-conforming workflow boundary: either split terminal-stop summary steps per failing gate or pass an explicit prior-step source/map through existing workflow source/judge data-flow; do not require hidden runtime state, cross-step introspection, or ad-hoc inference from missing artifacts.
- [x] AR2: Define proof-artifact synchronization as a no-op/verifying fixed-point before final summary: if artifacts are stale, update and commit them, then route back through the relevant review/proof gate or repeat proof-sync until clean; final success must not follow directly from a mutating proof-sync pass.
- [x] AR3: Make committed task-local artifacts the authoritative proof-sync inputs and outputs; workflow/review yields may be context only. The design must not depend on ephemeral child-session prose to establish final proof coherence across session/workflow boundaries.
- [x] AMB1: Resolve the incidental proof-artifact standard before planning: choose whether `coverage-map.md` is mandatory, conditionally required, or replaced by a named `implementation.md` coverage/proof section; define the trigger/required fields and the workflow-loader/content-lock tests for that choice.
- [x] AMB2: Name the task-local incidental after/baseline proof artifacts that must be parse-checked for A5/A2 and gate claims (for example after `bb gordian local --json` output, A2/A5 comparison result, and `gordian gate --baseline` output when claimed), including formats and whether proof-sync reads them.
- [x] AMB3: Pin concrete proof-sync topology and routing for each workflow: step name/location after review gates, PASS_STATUS mapping, and exact route after a mutating proof-sync pass for stale coverage/test proof, stale validation/Gordian proof, and pure bookkeeping updates.
- [x] AMB4: Choose the terminal-stop workflow shape now rather than leaving split steps vs single source-envelope open; if using a single step, specify the representable stop-source envelope/vars for every failing route, including malformed task-path extraction.
- [x] AMB5: Resolve selector-uncertainty semantics: remove or answer the open low-confidence architecture auto-stop question, and define what counts as a marginal incidental target plus the minimum rejected-false-positive/guard evidence generated designs must record.
