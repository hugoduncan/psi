- [x] Resolve the delegate-step output-surface contract across `design.md`, `doc/workflow-ir.md`, and `doc/workflow-grammar-concepts.md`: decide whether delegated steps re-expose callee step-local outputs to downstream `{:step ... :output ...}` refs, or whether first-cut delegate steps only yield the callee's yielded value unchanged and permit only explicit local outputs such as optional debug/result surfaces.
- [x] Resolve the `{:step ... :yield ...}` field-selection contract across `doc/workflow-ir.md`, `doc/workflow-grammar.md`, and tests: decide whether yield refs may target any keyword field in the yielded tagged union or only a constrained canonical subset, then align the docs and validation surface to that one rule.
