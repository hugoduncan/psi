- 2026-05-04 review: new actionable ambiguity — delegate-step output-surface contract is still inconsistent across artifacts. `design.md` says delegate steps should expose the delegated workflow's output surfaces through the delegated run, but `doc/workflow-ir.md` and `doc/workflow-grammar-concepts.md` currently describe first-cut delegate steps as yielding the callee's yielded value unchanged while not adding first-cut step-local outputs beyond optional later debug/result surfaces. Clarify whether downstream `{:step ... :output ...}` refs against delegate steps are intended to re-export callee outputs or to be invalid except for explicit local outputs.
- 2026-05-04 resolution: first-cut delegate steps do not re-export callee step-local outputs. A delegate step yields the delegated workflow's yielded value unchanged, and downstream `{:step ... :output ...}` refs against the delegate step are invalid unless the delegate step itself declares explicit local outputs in a later extension. Aligned `design.md`, `doc/workflow-ir.md`, `doc/workflow-grammar-concepts.md`, and loosened `doc/workflow-grammar.md` output-key grammar back to `keyword` so per-step-type validation, rather than the authoring grammar, owns the allowed output surface set.
- 2026-05-04 review: new actionable inconsistency — yield-field constraints still drift across task artifacts and docs. `doc/workflow-ir.md` allows `{:step ... :yield ...}` to select any keyword field from the yielded tagged union (`yield-field ::= keyword`), but `doc/workflow-grammar.md` still hard-codes `:data | :text | :error | :reason | :message | :details`. Decide whether yielded-value field selection is intentionally open at this stage or should be constrained canonically, then align the task docs/grammar/tests on that single rule.
- 2026-05-04 resolution: keep authored `yield-field` open as `keyword`, aligned with `doc/workflow-ir.md` and `workflow_ir.clj`. The grammar should not hard-code a closed subset because validity is determined semantically by the referenced step's actual yielded tagged-union form: data yields expose `:data`, text yields expose `:text`, error yields expose `:reason`, `:message`, and optional `:details`, and delegated yields expose no first-cut yield fields. Updated `doc/workflow-grammar.md` to `yield-field ::= keyword`; runtime/test validation already enforced the narrower per-step semantic contract.
- 2026-05-04 verification: focused workflow IR / target-IR-compiler proof ran via `clojure -M:test --focus psi.agent-session.workflow-ir-test --focus psi.agent-session.workflow-target-ir-compiler-test`.
- 2026-05-04 implementation: introduced canonical output/yield surface resolution helpers in `workflow_ir.clj` (`step-output-value`, `step-output-surfaces`, `step-yield-field-value`) so output-key validation and runtime value resolution share one contract instead of hand-coded per-callsite branches.
- 2026-05-04 implementation: rewired `workflow_step_prep.clj` source-spec resolution to use those helpers against canonical IR step defs, keeping `:output` and `:yield` resolution distinct while preserving the legacy stored `:outputs :text` fallback for canonical session `:final-llm-reply` during migration.
- 2026-05-04 implementation: updated Phase A session-step result recording in `workflow_statechart_runtime.clj` to store canonical session output keys (`:final-llm-reply`, `:result`) via normalized output surfaces instead of emitting only legacy `:outputs :text`.
- 2026-05-04 implementation: updated terminal workflow-result extraction in `mutations/canonical_workflows.clj` to read canonical yielded text semantics through `workflow-ir/step-yield-field-value` rather than hard-coding `accepted-result[:outputs :text]`; retained compatibility with legacy stored session results via the resolver fallback.
- 2026-05-04 proof: added focused tests for delegate output-ref invalidity, normalized output-surface resolution across invoke/session/delegate, canonical runtime session-result recording, and canonical output/yield materialization in step prep.
- 2026-05-04 review: new actionable test-drift — the review-task proofs still describe legacy current-grammar `:outputs {:text ...}` session envelopes in several runtime-adoption/step-prep fixtures even though this task made canonical session output naming `:final-llm-reply` and moved runtime resolution onto normalized output surfaces. Tighten those fixtures/assertions to author and assert canonical session output keys first, using `:text` only where a compatibility fallback is the behavior under test.
- 2026-05-04 implementation: updated runtime-adoption, step-prep, and statechart-runtime review-pass fixtures to author canonical session results with `:outputs {:final-llm-reply ...}` and to bind downstream refs through `:outputs :final-llm-reply`; where assertions still need normalized access they now assert through runtime materialization rather than raw legacy storage keys.
- 2026-05-04 proof: `clojure -M:test --focus psi.agent-session.workflow-ir-runtime-adoption-test --focus psi.agent-session.workflow-step-prep-test --focus psi.agent-session.workflow_statechart_runtime_test`
- 2026-05-04 code-shaper review: new actionable consistency drift — canonical output-surface normalization now standardizes session outputs on `:final-llm-reply`, but older compatibility-facing workflow authoring/session defaults and several workflow execution/compiler tests still hard-code prior-step session reads as `[:outputs :text]`. This duplicates the legacy storage key outside the normalized output-surface boundary and leaves mixed canonical/compat fixtures teaching two different defaults.
- 2026-05-04 implementation: switched legacy workflow authoring-session default prior-step `:input` binding to canonical `[:outputs :final-llm-reply]` and updated broad workflow execution/compiler/loader proofs to author canonical session-step defaults with `:final-llm-reply`, leaving `:outputs :text` only in explicit compatibility fallback assertions.
