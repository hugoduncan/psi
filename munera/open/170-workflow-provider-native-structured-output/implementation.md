# Implementation notes

- 2026-05-24 ambiguity review: actionable ambiguity found. Task 170 only has `design.md`; `plan.md`/`steps.md` are absent. Current design leaves open the exact IR policy keys, the JSON Schema source boundary between task 168 Malli schemas and task 169 explicit `:json-schema`, and the precise AI-result-to-workflow-envelope metadata mapping. Added design follow-up items in `design-steps.md`.

- 2026-05-24 ambiguity follow-up: completed all newly added design follow-ups. Added `plan.md`/`steps.md`; chose canonical policy/request keys `:json-schema`, `:strategy-preference`, `:fallback`, and `:require-provider-native?`; specified explicit JSON Schema source boundary with no Malli-to-JSON-Schema conversion; defined AI metadata to workflow envelope mapping for session steps and judges. Marked design-steps complete. Did not execute implementation items from `steps.md`.

- 2026-05-24 inconsistency review: found actionable inconsistencies. Plan/steps assign required-native unsupported detection to the workflow request-building helper even though native availability is selected from resolved model/provider capability later in the AI path. Design envelope mapping also names workflow `:raw-payload` as the parsed/native object while current AI structured-output metadata uses `:payload` for parsed/native data and `:raw-payload` for raw provider text, leaving contradictory copy semantics. Added design follow-ups.

- 2026-05-24 inconsistency follow-up: completed both newly added design follow-ups. Design/plan/steps now make the workflow request helper capability-blind: it fails local contract errors such as missing `:json-schema`, encodes required-native as `:fallback-allowed? false`, and leaves unsupported-native detection to turn execution / AI strategy selection after resolved model/provider capability is known. Envelope mapping now follows AI metadata naming: workflow `:payload` is parsed/native data and validation input; workflow `:raw-payload` is copied only for raw provider diagnostics. Marked design-steps complete. Did not execute implementation items from `steps.md`.

- 2026-05-24 ambiguity review repeat: found actionable ambiguity. Design/plan say missing `:json-schema` fails before generation and required-native unsupported propagates as a workflow step/judge failure, but they do not name the exact workflow result surfaces/reasons for actor steps versus LLM judges (blocked vs failed, `:missing-json-schema`/`:unsupported-structured-output` details, and judge `:routing-result` shape). Added design follow-up.

- 2026-05-24 ambiguity follow-up repeat: completed the newly added design follow-up. Design now defines exact structured-output failure surfaces: session-step request-shaping, unsupported required-native, and invalid validation failures use blocked pending actor results with stable reasons; LLM-judge failures use `:routing-result {:action :fail}` with matching reasons and do not enter prose no-match retries. Plan and steps now call out these envelope expectations. Marked the design-step complete. Did not execute implementation items from `steps.md`.

- 2026-05-24 inconsistency review repeat: found one actionable inconsistency. Design says `:fallback :none` forbids fallback by producing `:fallback-allowed? false`, and steps/tests call for unsupported/no-fallback coverage, but the structured-output failure surface text narrows `:unsupported-structured-output` to `:require-provider-native? true`; task files should clarify that any fallback-forbidden structured-output request (`:require-provider-native? true` or `:fallback :none`) fails with the same unsupported surface when native support is unavailable, or choose a distinct no-fallback surface.

- 2026-05-24 inconsistency follow-up repeat: completed the newly added no-fallback unsupported design follow-up. Design/plan now specify that any fallback-forbidden structured-output request uses the same `:unsupported-structured-output` failure surface when native support is unavailable: both `:require-provider-native? true` and `:fallback :none` encode no fallback and propagate through blocked session-step results or judge `:routing-result {:action :fail}`. Steps test wording now explicitly covers both policy forms. Marked the design-step complete. Did not execute implementation items from `steps.md`.

- 2026-05-24 ambiguity review repeat 2: found one actionable ambiguity. Task artifacts require workflow envelopes to copy actual AI structured-output strategy/payload metadata, but referenced turn-execution code currently returns `:assistant-text`, `:assistant-message`, and `:execution-result` without naming the exact workflow-visible field/path for provider top-level `:structured-output` metadata or streaming `:structured-output-result` events. Implementation could read from `:execution-result`, assistant message metadata, journal entries, or add a new bounded result key. Added a design follow-up to choose the authoritative turn-result metadata seam before wiring envelopes.
