# Implementation steps

- [x] Extend workflow structured-output IR/schema validation for `:json-schema`, `:strategy-preference`, `:fallback`, and `:require-provider-native?`.
- [x] Add workflow-runtime request-building helper for task-169 `:structured-output` options, including missing-json-schema errors and required-native fallback-forbidden request encoding.
- [x] Extend turn execution so `execute-actor-turn!` / `execute-judge-turn!` accept optional opts and forward them to `execute-session-turn!`; populate `:execution-result/structured-output` from non-streaming provider `:structured-output` and streaming `:structured-output-strategy` / `:structured-output-result` events; and expose top-level `:structured-output` on bounded actor/judge turn results.
- [x] Pass structured-output request options through session-step turn execution via the actor-turn opts argument, preserve the same opts across ranked model fallback candidates, and read AI metadata/payload only from the bounded turn result `:structured-output` seam.
- [x] Pass structured-output request options through LLM-judge turn execution via the judge-turn opts argument, keep structured failures out of prose no-match retry loops, and read AI metadata/payload only from the bounded turn result `:structured-output` seam.
- [x] Extend workflow structured-output envelopes with AI strategy metadata plus `:payload` parsed/native data and `:raw-payload` raw provider diagnostics mapping.
- [ ] Add focused workflow-runtime/agent-session tests for provider-native, fallback, unsupported fallback-forbidden behavior (`:require-provider-native? true` and `:fallback :none` both yielding `:unsupported-structured-output`), missing JSON Schema, invalid validation, downstream refs, persisted metadata behavior, bounded turn-result `:structured-output` seam usage, session-step blocked failure envelopes, and LLM-judge `:routing-result {:action :fail}` failure envelopes.
  - [x] Current pass added/updated focused coverage for request policy defaults, required-native and fallback-none fallback-forbidden encoding, missing JSON Schema, provider-native envelope metadata/payload validation, session-step invalid blocked envelope, and LLM-judge structured failure details.
  - [x] This pass made explicit JSON Schema required for every structured request and added direct assertions that session-step and LLM-judge execution pass provider-native structured-output opts through the actor/judge turn seams.
  - [x] This pass added direct assertions for unsupported fallback-forbidden session-step and LLM-judge failure surfaces, ranked fallback opts preservation, and downstream structured refs using provider-native metadata.
  - [ ] Remaining follow-up: add persisted/replay metadata behavior coverage.
- [x] Update workflow authoring and IR documentation for policy keys, JSON Schema boundary, strategy selection, fallback, required-native behavior, and envelope metadata.
