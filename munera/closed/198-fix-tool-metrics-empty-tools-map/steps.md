# Steps

- [x] Diagnose: trace why `:tools {}` is always empty in metrics.edn
- [x] Identify root cause: `dispatch-in "tool_call"/"tool_result"` never called on interactive path
- [x] Fix: bridge `:tool-start`/`:tool-result` lifecycle events → extension `dispatch-in` in `emit-tool-lifecycle!`
- [x] Lint: `clj-kondo` clean on changed file
- [x] Test: focused tool execution + extensions tests pass (`d16e90286`)
- [x] Add integration test: register a `"tool_call"` handler on the session ctx's `extension-registry`, call `run-tool-call!`, assert handler was invoked (regression guard for the `emit-tool-lifecycle!` bridge)
- [x] Add test: assert `{:block true}` from a `"tool_call"` handler on the interactive path does NOT block execution (documents intentional non-enforcement)
- [x] Close task
- [x] Add end-to-end test: register the metrics extension on a real session ctx (via `ext/init`), call `run-tool-call!`, and assert `ext/store` accumulates a `:tools` entry — covers the full path (adapter → bridge → metrics handler → store) as a single regression guard for the primary acceptance criterion
- [x] Fix e2e test so it actually runs: added `extensions/metrics/src` to the `:unit` suite `:source-paths` in `tests.edn` so the suite explicitly declares the metrics namespace. Verified: `clojure -M:test --focus psi.agent-session.tool-execution-test` → **12 tests, 61 assertions, 0 failures**, stable across repeated runs. The earlier "11 tests" count is no longer reproducible — the committed test (`4630d40c0`) loads and executes the e2e deftest under the standard test command.
- [x] Commit the uncommitted working-tree changes: already committed in `4630d40c0` ("⚒ 198: add end-to-end test"). No uncommitted test change existed at the start of this pass.
- [x] Resolve the unrelated `.clj-kondo/config.edn` change — MOOT: the final committed test (`4630d40c0`) never introduced it. Working tree is clean of any `.clj-kondo/config.edn` change.
- [x] Fix clj-kondo warnings in `tool_execution_test.clj` — MOOT: the committed test avoids the `use-fixtures` referral (`:refer [deftest testing is]`) and uses a quoted `'psi.metrics.extension/init` symbol (no inline-def warning). `clj-kondo --lint` on the file → 0 errors, 0 warnings.
- [x] Add error-path e2e test for AC2: drive `run-tool-call!` through the `emit-tool-lifecycle!` bridge with `execute-tool-runtime-in!` returning `:is-error true`, register the metrics extension on the session ctx, and assert `@metrics-ext/store` accumulates a `:tools "…" :errors` entry — closes the split-coverage gap where the error-counter increment is only tested via direct `fire-event` (bridge bypassed) and the bridge's `:is-error` propagation is only asserted on the success path. Added `metrics-extension-accumulates-errors-via-bridge-test`. `clojure -M:test --focus psi.agent-session.tool-execution-test` → **13 tests, 66 assertions, 0 failures**. `clj-kondo` clean.
- [x] Commit the uncommitted AC2 test: `metrics-extension-accumulates-errors-via-bridge-test` exists in the working tree (`tool_execution_test.clj`) and passes (13 tests, 66 assertions, 0 failures; clj-kondo clean), but is not committed. Commit it so the AC2 error-path guard survives the session boundary and the task's git state actually contains the coverage. — DONE: committed in `ad7d0f975`. Verified executing: `--focus psi.agent-session.tool-execution-test` → 13 tests, 66 assertions, 0 failures; clj-kondo 0/0. Working tree clean of the test.
- [x] Reduce duplication in the two e2e metrics tests: extracted the shared ceremony into `run-tool-call-through-metrics-ext!` (defonce-atom pre-run reset, registry-only `runtime-fns`, `load-init-var-extension-in!`, infra `with-redefs`, drives `run-tool-call!`). Each test now states only its tool-call map, runtime-result (incl. `:is-error`), and store assertions. Post-run reset kept in each test's `finally` — the helper returns before assertions run, which need the still-populated store; helper owns pre-run isolation, test owns post-run cleanup. Per test-shaper `helpers_that_compress(ceremony) ∧ ¬helpers_that_hide(intent)`: per-test intent stays explicit at the call site. `clojure -M:test --focus psi.agent-session.tool-execution-test` → **13 tests, 66 assertions, 0 failures**. `clj-kondo` clean.
- [x] Document the `psi/metrics` built-in extension in `doc/extensions.md`: add a "Built-in extensions in this repo" entry (matching the format of `commit-checks`/`mcp-tasks-run`) covering what `.psi/metrics.edn` is, where it lives (`worktree/.psi/`), and the `:tools {name {:invocations :errors :error-reasons}}` counters this fix now populates. The extension is activated via `.psi/extensions.edn` (`psi/metrics {}`) but has no user-facing docs anywhere — this fix turns it from inert (`:tools` always `{}`) into an observable feature, which is the natural point to document it. — DONE: added an entry under "Built-in extensions in this repo" before `plan-state-learning`, documenting the subscribed events (`tool_call`/`tool_result`/`session_turn_finished`/`provider_*`), persistence to `worktree/.psi/metrics.edn`, the `metrics/summary` operation, the `/metrics` command, the full persisted shape (`:tools`/`:workflows`/`:commands`/`:operations`/`:tokens`/`:providers`/`:updated-at`), and a note that the `:tools` map is populated by the task-198 `emit-tool-lifecycle!` bridge.
- [x] Resolve the cross-path `"tool_result"` payload-shape divergence (code-shaper, consistency/robustness): `dispatch-tool-result-in` (plan path) fires `"tool_result"` with `:input`, but the `emit-tool-lifecycle!` interactive/batch bridge fired it without `:input` — the same extension bus contract delivered different keys depending on the triggering path, so a handler reading `:input` worked on the plan path and silently got `nil` on the interactive path. Root cause: the `:tool-result` lifecycle event built in `tool-runtime/core` (`record-tool-call-result!`) carried no `:parsed-args`/args. — DONE via resolution (a): `record-tool-call-result!` now threads `:parsed-args` (from the shaped result's `:tool-call`) into the `:tool-result` lifecycle event, and `emit-tool-lifecycle!` now emits `:input (:parsed-args lifecycle-event)`, unifying both paths' `tool_result` shape (addition, not removal — preserves both existing consumers, removes the silent-`nil` hazard). Contract assertions added to `emit-tool-lifecycle-bridge-fires-extension-handlers-test`: both `tool_call` and `tool_result` events carry `:input` matching the parsed args. design.md updated with the chosen-resolution clarification. `clojure -M:test --focus psi.agent-session.tool-execution-test` → **13 tests, 68 assertions, 0 failures**; `psi.tool-runtime.core-test` → 6 tests, 27 assertions, 0 failures; `clj-kondo` clean on all three changed files.
- [x] (code-shaper, low/marginal) Decide and resolve the residual `:content` *value-shape* divergence on the `"tool_result"` bus event across the two disjoint paths: the interactive/batch bridge (`emit-tool-lifecycle!`) emits `:content` as normalised content-blocks (`(:content result-message)`), while the plan path (`dispatch-tool-result-in`) emitted raw un-normalised `(:content result)`. The prior `:input` unification (`6a517aa8d`) closed key-presence parity but not `:content` value semantics, so the same `consistent(data_shapes)` concern remained one field deeper. — DONE via resolution (a) normalise-by-addition: `dispatch-tool-result-in` now coerces `:content` through `tool-runtime/normalize-tool-content`, so the plan path delivers the canonical `[{:type :text :text …}]` block vector matching the interactive bridge. Idempotent for the already-normalised interactive path; eliminates the path-dependent value hazard rather than documenting it. Added focused contract proof `dispatch-tool-result-normalizes-content-test` (string `:content` → text blocks). design.md updated with the chosen-resolution clarification. `clojure -M:test --focus psi.agent-session.extensions-test --focus psi.agent-session.tool-execution-test --focus psi.tool-runtime.core-test` → **43 tests, 189 assertions, 0 failures**; `clj-kondo` clean on changed src + test files.
- [x] (code-shaper, low — third leg of the `:input`/`:content` triple) Decide and resolve the residual `:is-error` *value-type* divergence on the `"tool_result"` bus event across the two disjoint paths: the interactive/batch bridge (`emit-tool-lifecycle!`) emits `:is-error (boolean …)` (strict boolean), while the plan path (`dispatch-tool-result-in`) passes `is-error?` through raw — `run-tool-plan-step-in!` supplies uncoerced `(:is-error result)`, which can be `nil`. The same extension bus contract therefore delivers `:is-error` as a strict boolean on one path and a possibly-`nil` value on the other (`consistent(data_shapes)` violation, one field deeper than the closed `:input`/`:content` unifications). Low severity — the sole consumer `on-tool-result` reads it only in a truthiness `when`, so no live defect — but a real cross-path contract divergence at the untrusted-extension boundary. Recommended resolution (a): coerce in `dispatch-tool-result-in` (`:is-error (boolean is-error?)`) for symmetry with the prior two unifications and `one_way ¬ambiguity`; idempotent for the bridge path. Add a focused contract assertion (raw `nil`/non-boolean `:is-error` → strict `false`/`true` at the handler) and verify `clj-kondo` clean.
- [x] (code-shaper, low-medium — structural root cause beneath the closed `:input`/`:content`/`:is-error` triple) Single-source the `"tool_call"`/`"tool_result"` bus-event payload shape. `emit-tool-lifecycle!` (`tool_runtime_adapter.clj`) hand-builds both bus payloads inline via raw `ext/dispatch-in`, duplicating the canonical constructors `ext/dispatch-tool-call-in` / `ext/dispatch-tool-result-in` (`extensions.clj`, also used by the plan path in `tool_plan.clj`). This duplication is the structural cause of the three-commit per-field divergence saga; the value-alignment fixes treated symptoms, not the cause — any future field added to one constructor reopens the divergence class (`consistent(idioms)`/DRY; `one_way ¬ambiguity`; `robust → enforceable(invariants)` — contract not single-sourced). No live defect (triple currently aligned), standing regression hazard at the untrusted-extension boundary. — DONE via resolution (b) shared-builder: extracted `ext/tool-call-event` and `ext/tool-result-event` payload-builders in `extensions.clj` (the latter owns the `:content` normalize + `:is-error` boolean coercions). `dispatch-tool-call-in` / `dispatch-tool-result-in` now construct their payload via these builders; `emit-tool-lifecycle!` (bridge) routes both branches through the same builders (discarding the dispatch return — documented interactive-path non-enforcement). The cross-path payload shape is now defined once. Chose (b) over route-through-`dispatch-tool-*-in` (a) because the bridge needs the payload, not the dispatch-return semantics — a pure builder is the smaller, more orthogonal seam. Added `tool-event-payload-constructors-test` (canonical shape + content-normalization idempotence + is-error coercion) as the cross-path guard. design.md updated with the chosen-resolution clarification. `clojure -M:test --focus psi.agent-session.extensions-test --focus psi.agent-session.tool-execution-test --focus psi.tool-runtime.core-test` → **45 tests, 195 assertions, 0 failures**; `clj-kondo` clean on changed src + test files.
- [x] (code-shaper, low — readability/fragility) Simplify the `reason` truncation in `on-tool-result` (`extensions/metrics/src/psi/metrics/extension.clj`): the `subs` length bound recomputed `(str/trim (first (str/split-lines content)))` independently of the threaded subject, duplicating the first-line pipeline and coupling the truncation bound to a second copy of the same expression (latent `StringIndexOutOfBounds` hazard if the two copies ever diverged). — DONE: extracted a private `error-reason` helper that binds `first-line` once (`(-> (str content) str/split-lines first str/trim)`) and truncates that single value (`(subs first-line 0 (min 80 (count first-line)))`); `on-tool-result` now just calls `(error-reason (:content payload))`, restoring `xor(computation, flow_control)` (helper computes, handler controls) and local comprehensibility. Dropped the redundant `(or "")` — `(str nil)` → `""` and `(str/split-lines "")` → `[""]`, so the empty/nil path yields a safe empty reason without the extra guard. Added focused assertion `tool-result-error-reason-multiline-truncated-to-first-line-80-chars-test`: leading-whitespace multi-line content with a 100-char first line → single `:error-reasons` key equal to exactly 80 `x` chars (proves first-line selection, trim, and bound off the single value). `psi.metrics.extension-test` → **20 tests, 44 assertions, 0 failures**; `clj-kondo --lint` on changed src + test → 0 errors, 0 warnings.
