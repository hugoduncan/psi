- architectural review 2026-08-18 (design baseline): added 2 new design steps — (1) single-sourced retry termination predicate (fold total-time deadline into the existing count-cap decision fn, not a separate loop-body check); (2) retry deadline in canonical session state alongside `:retry`/`:resume-at` via the established state-update path, not a runtime-local binding. Scope boundary (uniform provider/model-independent default) is consistent with the config-driven architecture; no SCOPE_QUESTION. Injectible-clock deadline vs wall-clock sleep interplay deferred to the ambiguity/inconsistency turn.
- ambiguity review 2026-08-18 (design baseline): added 4 new design steps — (1) default count-cap value/semantics pinned (AC1 requires default to give up only at 10 min; a default of 3 fails it); (2) whether total-time budget bounds an in-flight attempt (check is only before sleeps); (3) loop ordering of deadline decision vs delay computation (oversized Retry-After known only after retry-metadata-for, after failure-reason-for); (4) Retry-After test sleep seam — provider-supplied delay is not config-shrinkable, must use `:provider-retry-sleep-fn`. No scope concern; no SCOPE_QUESTION. Resolves the injectable-clock-vs-wall-clock-sleep interplay deferred by the architecture review.
- inconsistency review 2026-08-18 (design baseline): added 2 new design steps — (1) 10-min window anchor: Approach 2 "first-attempt start" (incl. failing request's own execution + ambiguous initial-vs-first-retry) conflicts with Goal/AC1 "10 minutes of retrying"; (2) stop-before-deadline semantics: Approach 2 gives up when next sleep overshoots (may be < 10 min, ~9:40 with 60s pin) vs AC1 "only after ... 10 minutes total". All design.md↔code/config references verified accurate (config default 3, backoff formula, retryable kinds, interruptible sleep, sleep-fn seam); no artifact inconsistency. No scope concern; no SCOPE_QUESTION.
- slice-notes (design-review session complete → 8 design-steps: 2 arch + 4 ambiguity + 2 inconsistency): all design.md↔code references verified accurate at baseline. Fn-level retry seams not in design.md, useful when addressing steps: retry loop + `failure-reason-for` / `retry-metadata-for` / `sleep-for-retry!` / `interruptible-sleep-for-retry!` / `mark-active-retry!` / `clear-active-retry!` / `retry-attempt-for` / `retry-clear-needed?` / `provider-retry-cancelled?` / `active-turn-cancelled?` all in `components/turn-runtime/src/psi/turn_runtime/core.clj`; `exponential-backoff-ms` + `retry-metadata` (+ `:auto-retry-*` defaults: max-retries 3, base 2000, max 60000; `agent-session-schema`) in `components/session-state/src/psi/session_state/model.clj`. Canonical retry state = session-data `:retry` metadata map (`:resume-at`/`:delay-ms`/`:delay-source`) + `:retry-attempt` int, mutated via `apply-root-state-update-in!`/session-update, re-read at loop entry — the natural home for the total-time deadline (arch step 2). Injectable test seams on ctx: `:now-fn` (already used by retry-metadata-for) and `:provider-retry-sleep-fn` (used by current retry tests; not in design.md) — both needed to drive the 10-min window without real sleeps (ambiguity steps 1/4). Cancellation to preserve: `:provider-retry-abort-requested?` session flag, stream-handle active-turn-cancelled?, ctx `:provider-retry-cancelled?` fn. Existing retry tests to update/extend: `components/turn-runtime/test/psi/turn_runtime/response_mode_retry_test.clj` + `response_mode_test.clj` (sleep-fn fixtures) and `components/agent-session/test/psi/agent_session/eql_provider_retry_test.clj`.
- design-follow-up 2026-08-18 (design-review batch complete → 8/8 steps done): addressed all 8 design-steps by updating design.md. Implementation-relevant mechanics (not in design.md):
  - `failure-reason-for` (turn-runtime/core.clj) currently runs BEFORE `retry-metadata-for` and takes `{:retryable? :retry-enabled? :retry-attempt :max-retries}` returning nil | `:non-retryable` | `:retry-disabled` | `:retry-exhausted`. The single give-up predicate (Arch step 1) requires REORDERING so `retry-metadata-for` (which computes the would-be next delay, incl. `Retry-After`) runs first and feeds the predicate its `next-delay` + `deadline` inputs; `retry-metadata-for` is pure, so the reorder is safe. The predicate must still expose `:max-retries` / `:attempt-count` etc in the retry-outcome map.
  - Deadline field: extend the `:retry` map inside `agent-session-schema` (session-state model.clj) with `:retry-deadline-ms` (set on first retryable failure / window open; read back at loop entry like `:retry-attempt`). Update the malli schema or `valid-session?` will reject persisted sessions. Set alongside `mark-active-retry!`'s `:retry` write via the same `apply-root-state-update-in!`/`session-update` path.
  - Default count-cap 20 rationale: default backoff 2,4,8,16,32,60... cumulative passes 600 s at the 14th sleep, so max-retries=20 is never the limiter inside 10 min.
  - Truncated final sleep still goes through the existing wait mechanism (`interruptible-sleep-for-retry!` / `:provider-retry-sleep-fn`), just with a delay capped at `deadline - now`; it stays interruptible.
  - Test seams (verified in code): `:provider-retry-sleep-fn` is consumed by `sleep-for-retry!` and used in `response_mode_test.clj` (~lines 470, 592) and `response_mode_retry_test.clj` uses `:provider-retry-sleep? false`; `:now-fn` on ctx is read by `retry-metadata-for`. For the deadline window test, the injected `:now-fn` must advance across loop iterations to drive the give-up predicate's `now >= deadline` check.
  - No SCOPE_QUESTION items in this batch; scope boundary (provider/model-independent default) unchanged.
- architectural review 2026-08-18 (first turn, current design-review session): no new actionable architectural-fit feedback. Verified the two prior arch steps remain satisfied in design.md — single give-up predicate folds count-cap + deadline into `failure-reason-for`; deadline stored in canonical session state via `apply-root-state-update-in!`/`session-update` + optional `agent-session-schema` field (canonical placement was prior arch step 2; a loop-local binding alternative re-litigates that settled decision and is not new feedback). Config plumbing follows existing `:auto-retry-*` → ctx `:config` flow; retry termination stays in the runtime-owned execute-and-record boundary (consistent with the prompt-lifecycle convergence waypoint); additive `:exhausted-reason` on outcome/event follows addition-over-modification; optional schema field backward compatible; `:now-fn`/`:provider-retry-sleep-fn` reuse existing seams; cancellation preserved. Scope boundary (provider/model-independent default) consistent with the config-driven architecture; no SCOPE_QUESTION.
- ambiguity review 2026-08-18 (second turn, current design-review session): added 3 new design steps — (1) truncated-final-sleep scheduling through the single give-up predicate: the overshoot branch and the immediate "now >= deadline" branch return the same terminal (`:retry-exhausted`/`:exhausted-reason :deadline`) yet only overshoot sleeps + records/emits the truncated delay, which per loop flow runs on the non-final retry path (mark-active-retry!/provider_retry_scheduled only on non-final, core.clj:614-641; a final goes to clear-active-retry!+execution-result with no sleep) — the resolved "what delay is recorded" step pinned the value but not the scheduling mechanism; (2) outcome when cancellation interrupts the truncated final sleep: hardcoded `:deadline` give-up vs interruptible-seam `:retry-cancelled` (sleep-for-retry! core.clj:637-657) precedence unspecified; (3) window-opening deadline persistence on an immediate count-cap give-up: ensure-step "compute and persist" vs "persist with mark-active-retry!" (which a count-cap give-up never runs). No SCOPE_QUESTION.
- inconsistency review 2026-08-18 (third turn, current design-review session): added 1 new design step — design.md Context "529 overloaded maps to `:overloaded`" is inaccurate vs `provider-error-kind` (session-state/model.clj): HTTP 529 → `:provider-unavailable` (status branch `#{500 502 503 529}`), `:overloaded` only from message matching `#"(?i)overloaded"` (checked before the status branch). Both kinds retryable, so retry behavior unaffected; only the classification framing is wrong. Verified no other design↔artifact inconsistency: backoff formula 2^attempt×base capped 60s, default-config (max-retries 3/base 2000/max 60000), retryable set, "~14 attempts in 10 min", `:retry-deadline-ms` top-level optional `[:maybe :int]` schema, `:exhausted-reason` on retry-outcome + `provider_request_finished`, all agree with code. The truncated-recording-on-final contradiction is already captured by ambiguity step 1 (this session), not re-filed. No SCOPE_QUESTION.
- slice-notes (design-review session → 4 open steps: 3 ambiguity + 1 inconsistency). Principles to hold when addressing them: keep the single give-up predicate folded into `failure-reason-for` (arch step 1) — do not split the deadline check into a separate loop-body branch; keep deadline in canonical session state via `apply-root-state-update-in!`/`session-update` + optional `agent-session-schema` field (arch step 2); preserve cancellation through the existing interruptible seam; stay behavior-preserving for explicit small count caps and count-only mode; drive the window with injected `:now-fn` + `:provider-retry-sleep-fn`, never real sleeps. Mechanics for the 4 open steps (loop flow in `components/turn-runtime/src/psi/turn_runtime/core.clj` `execute-prepared-request!`):
  - **Ambiguity 1 (truncated-sleep scheduling).** Today the non-final retry path (core.clj:614-636) does `retry-metadata-for` → `provider_retry_scheduled` (emits full `:delay-ms`/`:resume-at`) → `mark-active-retry!` (stores full metadata) → `sleep-for-retry!` `:delay-ms` → `recur`. A `:retry-exhausted` final path (core.clj:596-613) goes straight to `clear-active-retry!` + `execution-result` — no sleep, no event. So "sleep remainder then finalize" cannot be the existing final path; it needs a distinct branch that (a) computes `truncated = min(full, deadline-now)`, (b) emits/stores the truncated delay (recompute `:resume-at = now + truncated`), (c) sleeps via the seam, then (d) finalizes with `:retry-exhausted :deadline` — without calling `mark-active-retry!`/`recur` (no next attempt). The give-up predicate must therefore distinguish "finalize now (now ≥ deadline, no sleep)" from "sleep-truncated-then-finalize (overshoot)", both returning `:retry-exhausted :deadline`.
  - **Ambiguity 2 (cancel during truncated sleep).** `sleep-for-retry!` (core.clj:638-644) returns `cancelled?`; the loop maps that to `:retry-cancelled` (core.clj:645-657). If the truncated sleep in ambiguity 1 routes through `sleep-for-retry!`, a cancel mid-sleep should follow the same `:retry-cancelled` outcome for consistency with the cancellation constraint and the existing cancelled path — the overshoot branch must not unconditionally hardcode `:deadline` after the sleep without re-checking `cancelled?`.
  - **Ambiguity 3 (window-open deadline persistence on count-cap give-up).** `mark-active-retry!` (core.clj:386-392) is the sole canonical writer of the `:retry` map/`:retry-attempt`; it runs only when a retry is actually scheduled. A window-opening failure that immediately gives up on the count cap never schedules, so never writes the deadline. Preferred resolution consistent with the "persist with mark-active-retry!" phrasing: the ensure-step computes the deadline in memory for the predicate and persists it only with `mark-active-retry!`; a count-cap give-up writes nothing (and `clear-active-retry!` clears anyway). `clear-active-retry!` (core.clj:402-412) + `retry-clear-needed?` (core.clj:395-400) must both gain the top-level `:retry-deadline-ms` dissoc/guard (prior resolved step).
  - **Inconsistency 4 (529 mapping).** Fix the design.md phrasing only — code change not required/desired. In `provider-error-kind` (session-state/model.clj) `:overloaded` is matched from the message pattern before the status branch, and 529 is in the `:provider-unavailable` status set; an "overloaded" 529 with a matching message yields `:overloaded`, a bare 529 yields `:provider-unavailable`. If a new test asserts `:error-kind :overloaded` for a 529 stub it must include an "overloaded" message; otherwise assert `:provider-unavailable`.
- architecture re-review 2026-08-18 (post-follow-up design): no new actionable architectural feedback. Verified both prior arch steps satisfied in updated design.md — single-sourced give-up predicate folded into `failure-reason-for` (turn-runtime/core.clj:361, per-attempt after `retry-metadata-for`), deadline in canonical session state via `apply-root-state-update-in!`/session-update + `agent-session-schema` field. Config plumbing follows existing `:auto-retry-*` → ctx `:config` flow (`(get-in ctx [:config :auto-retry-max-retries] 3)` core.clj:545); retry termination stays in the runtime-owned execute-and-record boundary (consistent with prompt-lifecycle convergence waypoint); injectable `:now-fn`/`:provider-retry-sleep-fn` reuse existing seams. Scope boundary (provider/model-independent default) consistent with config-driven architecture; no SCOPE_QUESTION.
- ambiguity re-review 2026-08-18 (post-follow-up): added 3 new design steps — (1) deadline lifecycle/window-open detection + clearing (clear-active-retry! clears only :retry-attempt/:retry/:provider-retry-abort-requested?, all session-scoped; :retry-deadline-ms must be cleared or a stale deadline leaks across turns; predicate must detect window-opening vs already-open); (2) outcome/telemetry distinguish count-cap vs deadline exhaustion (both map to :retry-exhausted/:exhausted? — no field like :exhausted-reason); (3) time-budget disable semantics (:auto-retry-total-timeout-ms value that disables it, or always-active). Prior 4 ambiguity steps + 2 inconsistency steps remain addressed; no scope concern, no SCOPE_QUESTION. All design.md↔code refs re-verified accurate (clear-active-retry! core.clj:402, failure-reason-for:361, agent-session-schema :retry map).
- inconsistency re-review 2026-08-18 (post-follow-up): added 2 new design steps — (1) truncated final sleep vs recorded/emitted delay: `mark-active-retry!` stores full `retry-metadata` (:delay-ms/:resume-at, core.clj:614-638) and `provider_retry_scheduled` emits full values, while `sleep-for-retry!` sleeps the full :delay-ms (core.clj:641); under truncation the actual sleep would be shorter than the recorded/emitted resume time — design must specify full-vs-truncated for :delay-ms/:resume-at in canonical :retry and the event; (2) deadline field schema placement ambiguous: top-level sibling of :retry vs key inside the :retry map — pins agent-session-schema shape, clear-active-retry!/retry-clear-needed? (must clear it, links to ambiguity step 1), and predicate read-back. Prior 2 inconsistency steps (anchor, stop-before-deadline) verified resolved in design.md; no scope concern, no SCOPE_QUESTION.
- slice-notes (design-review re-review → 5 open steps: 3 ambiguity + 2 inconsistency). Mechanics for addressing them (not in design.md):
  - **Schema placement (inconsistency 2 / ambiguity 1).** `agent-session-schema` `:retry` map spans model.clj:177-191 (`:resume-at` at 185; `:retry-attempt :int` at 177 as a top-level sibling); default-config at 240, `:auto-retry-*` keys 245-248; initial-session `:retry nil`/`:retry-attempt 0` at 289-291. The deadline field is a schema edit either as a top-level `:retry-deadline-ms :int` (sibling of `:retry-attempt`) or inside the `:retry` map (sibling of `:resume-at`) — pin one; `valid-session?` will reject persisted sessions if the schema and the write disagree.
  - **Deadline lifecycle / window-open (ambiguity 1).** Loop re-reads `:retry-attempt` at entry via `retry-attempt-for` (core.clj:333/550) — the deadline is read back the same way. The canonical write point for `:retry`/`:retry-attempt` is `mark-active-retry!` (core.clj:386-392) — the deadline store belongs there (window-opening failure), and `clear-active-retry!` (core.clj:402-412) + `retry-clear-needed?` (core.clj:395-400, currently only checks `:retry`/`:retry-attempt`/`:provider-retry-abort-requested?`) must clear it or a stale deadline leaks across turns. Anchor per design: deadline = first-retry-decision time + total-timeout, so "window open" is detected by `retryable? && retry-enabled? && no stored deadline`.
  - **Exhaustion distinguishability (ambiguity 2).** `failure-reason-for` (core.clj:361) currently returns a single keyword (`:retry-exhausted` for both count and deadline per design) and `retry-outcome`/event carry `:failure-reason` + `:exhausted?`. The outcome flows to `:execution-result/retry-outcome` and the `provider_request_finished` event (`:failure-reason`, `:exhausted?` at core.clj:599-604). Distinguishing count-vs-deadline needs either a richer predicate return or an extra outcome field (e.g. `:exhausted-reason`); the single give-up predicate already computes both so it can expose which fired.
  - **Truncated sleep vs recorded delay (inconsistency 1).** `retry-metadata` (model.clj) computes `:delay-ms` and `:resume-at = (+ now-ms delay-ms)` (model.clj:581) from the full delay; `provider_retry_scheduled` emits `:delay-ms`/`:resume-at` from that metadata (core.clj:633-634) and `sleep-for-retry!` sleeps `:delay-ms` (core.clj:641). Truncation only shortens the final sleep; if truncated values are recorded/emitted, `:resume-at` must be recomputed as `(+ now-ms remaining)`, else it will overstate when the retry resumes.
  - **Time-budget disable (ambiguity 3).** `:auto-retry-enabled` (session-data boolean, schema model.clj:193, default true) is the existing master on/off; `retry-metadata-for` (core.clj:369) shows the inline `(get-in ctx [:config ...] default)` read pattern for the new key, but design puts the default in default-config. If a disable value (e.g. nil/0 → count-only) is chosen, wire it through the same predicate input so count-only (large cap) stays expressible.
- design-follow-up 2026-08-18 (design-review re-review batch complete → 5/5 steps done: deadline lifecycle, exhaustion distinguishability, disable semantics, truncated-delay recording, schema placement). Addressed all 5 re-review design-steps in design.md. Implementation-relevant mechanics (not in design.md):
  - **Window-open persistence point.** The deadline is only persisted when a retry is actually scheduled: `mark-active-retry!` (core.clj:386-392) is the sole writer of the `:retry` map + `:retry-attempt` on non-final retryable failures, so store `:retry-deadline-ms` in that same `apply-root-state-update-in!`/`session-update`. The give-up predicate's "ensure deadline present" step computes the value in memory (window-open: `now + timeout`) before the predicate runs, but the canonical write happens with mark-active-retry!. A retryable failure that immediately gives up (count cap) never schedules a retry, so never stores a deadline — and clear-active-retry! clears any stored state anyway.
  - **Clearing must cover both fns.** `clear-active-retry!` (core.clj:402-412) and its `retry-clear-needed?` guard (core.clj:395-400) both need `:retry-deadline-ms` added (guard currently checks only `:retry`/`:retry-attempt`/`:provider-retry-abort-requested?`; if a session had deadline but zeroed attempt+nil retry it could skip the clear). Top-level placement (sibling of `:retry-attempt`, model.clj:177) means the dissoc belongs in the same `->` thread that clears `:provider-retry-abort-requested?`.
  - **Schema edit.** `agent-session-schema` (model.clj) top-level, adjacent to `[:retry-attempt :int]` (model.clj:177): `[:retry-deadline-ms {:optional true} [:maybe :int]]`. Optional/maybe because absent until a window opens; initial-session (model.clj:~289) can omit it.
  - **Exhaustion distinguishability.** `failure-reason-for` (core.clj:361) currently returns a single keyword and is fed by the loop; the single give-up predicate must additionally expose which boundary fired (`:count-cap | :deadline`) into `:exhausted-reason`. The retry-outcome map is assembled at core.clj:590-604 and the `provider_request_finished` event at core.clj:599-616 — add `:exhausted-reason` to both, conditioned on `(= :retry-exhausted failure-reason)`, alongside the existing `:exhausted?`. Note the retry-outcome and the event are built in two separate `cond->` forms (not shared), so both must be edited.
  - **Truncated-delay recording.** `provider_retry_scheduled` emits `:delay-ms`/`:resume-at` from `retry-metadata` (core.clj:633-634) and `sleep-for-retry!` sleeps `:delay-ms` (core.clj:641); `retry-metadata` (model.clj:~581) computes `:resume-at = (+ now-ms delay-ms)` from the full delay. To record the actually-slept delay (`min(full, deadline-now)`), the truncated values must be substituted where the sleep delay is chosen (after `retry-metadata-for`), and `:resume-at` recomputed as `now + slept` — otherwise the event/progress `:resume-at` overstates. `:delay-source` stays the underlying source. This applies to both the exponential and oversized-`Retry-After` truncation paths.
  - **Disable semantics wiring.** Read the new key via the existing inline pattern `(get-in ctx [:config :auto-retry-total-timeout-ms] 600000)` (cf. `retry-metadata-for` core.clj:369; max-retries read core.clj:545). Budget active ⇔ value > 0; nil/absent/<=0 → count-only (no deadline computed, predicate evaluates count cap only). `:auto-retry-enabled` (session-data boolean, model.clj:193) remains the master on/off.
  - No SCOPE_QUESTION items in this batch; scope boundary (provider/model-independent default) unchanged.
- design-follow-up 2026-08-18 (current-session batch complete → 4/4 steps done: truncated-sleep scheduling, cancel-during-truncated-sleep, window-open deadline persistence on count-cap, 529 classification). Addressed all 4 in design.md. Implementation-relevant mechanics (not in design.md):
  - **Truncated final sleep = loop-control change.** The overshoot branch is a distinct final-sleep outcome carrying `:final-sleep-ms`; the predicate returns a structured map (not a bare `:retry-exhausted` keyword) so the loop can distinguish "finalize now (no sleep)" (count-cap / immediate deadline, no `:final-sleep-ms`) from "sleep truncated remainder, record/emit it, then finalize" (overshoot, `:final-sleep-ms` present). The final-sleep outcome must route through the non-final retry path once (truncated `retry-metadata` → `mark-active-retry!` + `provider_retry_scheduled` → interruptible sleep → then finalize `:retry-exhausted :deadline` instead of `recur`). The current loop's final branch (core.clj:610-613) never sleeps/records, so the overshoot case cannot reuse it as-is.
  - **Cancel-during-truncated-sleep.** Because the final-sleep sleeps through `sleep-for-retry!`/`interruptible-sleep-for-retry!` (cancellable), a cancel mid-sleep must yield `:retry-cancelled` via the existing cancelled path (core.clj:645-657) — not a hardcoded `:deadline` — so the loop must re-check the `cancelled?` return from the truncated sleep before finalizing as `:deadline` (mirrors the non-final `(if cancelled? ... (recur ...))`).
  - **Window-open deadline persistence (count-cap give-up writes nothing).** Deadline computed in memory for the predicate on every window-opening failure; persisted only in the `mark-active-retry!` session-update when a retry is scheduled. An immediate count-cap give-up at window-open never stores a deadline; `clear-active-retry!` runs with nothing to clear. Keep the in-memory value threaded to the predicate independent of the canonical write.
  - **529 classification (design-only, no code change).** Verified `provider-error-kind` (session-state/model.clj): message-based `:overloaded` branch runs before the status-based `:provider-unavailable` branch (`#{500 502 503 529}`); bare 529 → `:provider-unavailable`, 529 with "overloaded" message → `:overloaded`. A new test asserting `:error-kind :overloaded` for a 529 stub must include an "overloaded" message; otherwise assert `:provider-unavailable`. design.md Context corrected; Goal/Acceptance examples qualified as "529 whose payload says 'overloaded'".
  - No SCOPE_QUESTION items in this batch; scope boundary (provider/model-independent default) unchanged.
- architecture review 2026-08-18 (first turn, current design-review session): added 1 new design step — the window-scoped canonical deadline conflicts with the existing inter-attempt `clear-active-retry!` lifecycle. Verified core.clj: `clear-active-retry!` fires after every non-final sleep (line 643, `(when-not (= false (:provider-retry-sleep? ctx)) (clear-active-retry! ...))`) plus final paths (571, 611). Adding `:retry-deadline-ms` to `clear-active-retry!` (per design Clearing section) wipes the deadline after the first sleep, so window-open detection recomputes a fresh 10-min window each attempt. `:retry-attempt` is carried via the loop's `recur` binding precisely because the same mid-window clear resets it to 0 in canonical state — the deadline needs the same loop-binding threading (clear canonical only on true final close) or a window-scoped clear split from the per-sleep cleanup. Prior arch steps (single give-up predicate folded into `failure-reason-for`; canonical deadline via `apply-root-state-update-in!`/`session-update` + optional `agent-session-schema` field) verified still satisfied in design.md. Config plumbing, runtime-owned execute-and-record boundary, additive `:exhausted-reason`, existing `:now-fn`/`:provider-retry-sleep-fn` seams, cancellation preservation all consistent with architecture. Scope boundary (provider/model-independent default) consistent with config-driven architecture; no SCOPE_QUESTION.
- ambiguity review 2026-08-18 (second turn, current design-review session): added 3 new design steps — (1) event/state semantics of routing the truncated final sleep through the non-final retry path: `mark-active-retry!` + `provider_retry_scheduled` fire with truncated delay, then finalize `:retry-exhausted :deadline` (core.clj:633-641) — consumers see "retry scheduled / active retry" for a final that never retries, and canonical `:retry-attempt`/`:retry :active?` transiently bump before clear; (2) truncated final sleep under `:provider-retry-sleep? false` (test mode): current non-final path skips clear-active-retry! when sleep disabled (core.clj:642-643), design's finalize-always-clear is unspecified there; (3) `:exhausted-reason` precedence when count-cap and deadline both hold at one attempt (branch order implies count-cap wins, unstated). Prior arch step (inter-attempt clear-active-retry! vs window-scoped deadline) remains open. No SCOPE_QUESTION.
- inconsistency review 2026-08-18 (third turn, current design-review session): added 1 new design step — Approach 4's default count-cap rationale ("max-retries=20 never reached in 10 min, ~14 attempts") assumes exponential backoff, but `retry-metadata` (session-state/model.clj:571-572) computes `delay-ms (or retry-after-ms exponential-delay-ms)` so a `Retry-After` fully overrides and is uncapped by `:auto-retry-max-delay-ms`; a fast `Retry-After` (e.g. 1 s) lets count reach 20 well inside the window, giving up `:count-cap` at ~20 s, contradicting Goal/AC1 "up to 10 minutes" and the "never reached" claim. Design must state whether total-time governs all delays or the 10-min goal is qualified for Retry-After. Prior design↔artifact checks re-verified consistent (backoff formula min(max, base·2^attempt), default-config, retryable set, `:exhausted-reason` on outcome+event, truncated-delay recording, schema placement, anchor, stop-at-deadline, 529 mapping). No SCOPE_QUESTION.
- slice-notes (design-review session → 5 open steps: 1 arch + 3 ambiguity + 1 inconsistency). Principles to hold when addressing: keep the single give-up predicate folded into `failure-reason-for` (prior arch step) — do not split the deadline check into a separate loop-body branch; keep the deadline in canonical session state via `apply-root-state-update-in!`/`session-update` + optional `agent-session-schema` field (prior arch step); preserve cancellation through the existing interruptible seam; stay behavior-preserving for explicit small count caps and count-only mode; drive the window with injected `:now-fn` + `:provider-retry-sleep-fn`, never real sleeps. Loop control flow (all in `components/turn-runtime/src/psi/turn_runtime/core.clj` `execute-prepared-request!`):
  - **Arch 1 (inter-attempt clear vs window deadline).** `clear-active-retry!` (402-412) runs after EVERY non-final sleep (`(when-not (= false (:provider-retry-sleep? ctx)) (clear-active-retry! ...))` at 642-643) and on the final paths (571, 611). The loop carries `retry-attempt` via its `recur` binding (665) precisely because this mid-window clear resets canonical `:retry-attempt` to 0 each iteration — so the window-scoped deadline has the same lifecycle problem. To make it survive inter-attempt cleanup, either thread it through the loop binding alongside `retry-attempt` (canonical `:retry-deadline-ms` written on window-open via `mark-active-retry!` 386-392, cleared only on true final close/turn end), or give `clear-active-retry!`/`retry-clear-needed?` (395-400) a window-aware form that does not wipe the deadline mid-window. Note the design's final-sleep path DOES want the deadline cleared on finalize, so the clear must distinguish mid-window vs final.
  - **Ambiguity 1 (final-sleep emit semantics).** Non-final path (614-641): `provider_retry_scheduled` (630-636) emits full retry-metadata `:delay-ms`/`:resume-at`/`:delay-source`; `mark-active-retry!` (638) stores retry-state (`:retry-attempt`→next-attempt, `:retry :active? true`); `sleep-for-retry!` (639) sleeps `:delay-ms`; `clear-active-retry!` (642-643) then resets; `recur` (665). The design's truncated-final routing goes through this branch once with `truncated = min(full, deadline-now)` and recomputed `:resume-at = now + truncated`, then finalizes `:retry-exhausted :deadline` (clear + `execution-result`) instead of `recur`. So `provider_retry_scheduled`/`:retry-updated` fire for a final that never retries, and `:retry-attempt`/`:retry :active?` transiently bump before the clear. If reusing the non-final emit for the final is not acceptable, the final-sleep outcome needs its own distinct event/record path (the `:final-sleep-ms` field in the predicate's structured outcome is the discriminator).
  - **Ambiguity 2 (test-mode sleep interplay).** `sleep-for-retry!` (443-451) is a no-op when `:provider-retry-sleep? false` (returns `provider-retry-cancelled?`), and the inter-attempt `clear-active-retry!` (642-643) is skipped in that mode — so in test mode the design's final-sleep path must still clear on finalize (the final-path clear at 611 is unconditional) and must decide whether to record/emit the truncated delay despite the disabled sleep seam. `response_mode_retry_test.clj` uses `:provider-retry-sleep? false`; `response_mode_test.clj` uses `:provider-retry-sleep-fn` (~470, 592) for the `Retry-After` path.
  - **Ambiguity 3 (`:exhausted-reason` precedence).** `failure-reason-for` (361) cond order is not-retryable → retry-disabled → count-cap → deadline; design's richer predicate (Approach 2) lists count-cap before deadline too, so `:count-cap` wins when both hold. Pin this explicitly; the retry-outcome map is assembled at 590-604 and the `provider_request_finished` event at 599-616 (two separate `cond->` forms — both must gain `:exhausted-reason`).
  - **Inconsistency 1 (Retry-After vs count-cap).** `retry-metadata` (session-state/model.clj:567-582) computes `delay-ms (or retry-after-ms exponential-delay-ms)` (571) — a `Retry-After` fully overrides and is NOT bounded by `:auto-retry-max-delay-ms`. So the "max-retries=20 never reached in 10 min" rationale only holds for the pure-exponential path; fast `Retry-After` delays can hit `:count-cap` early. Resolution options: make the total-time budget the effective limiter for all delays (predicate lets count-cap only fire when the budget is disabled, or the default cap is effectively non-limiting under any delay), or explicitly qualify Goal/AC1 "up to 10 minutes" for the `Retry-After` case. `:delay-source` reports `:retry-after` vs `:exponential-backoff` independently of truncation (retry-metadata 580).
- design-follow-up 2026-08-18 (current-session batch complete → 5/5 steps done: inter-attempt clear vs window deadline, final-sleep emit semantics, test-mode final-sleep, exhausted-reason precedence, Retry-After vs default count-cap). Addressed all 5 in design.md. Implementation-relevant mechanics (not in design.md):
  - **Deadline threading (arch 1).** `clear-active-retry!` (turn-runtime/core.clj:402-412) fires after EVERY non-final sleep (inter-attempt call ~643 inside `(when-not (= false (:provider-retry-sleep? ctx)) ...)`), plus final paths (~571 success, ~611 give-up). Because it resets canonical `:retry-attempt` to 0 each iteration, the loop carries `retry-attempt` via `recur` (665). The deadline is threaded the same way: `(loop [retry-attempt ... retry-deadline-ms (retry-deadline-for ...)])`, seeded from canonical, computed on the window-opening failure, carried across attempts. The per-sleep clear must PRESERVE `:retry-deadline-ms`; only the success (~571), final-give-up (~611), and cancel paths clear it (cancel = turn end). Implement via a `:clear-deadline?` param on `clear-active-retry!`/`retry-clear-needed?` or a separate final-clear step.
  - **Cancel path clears the deadline.** A cancelled retry ends the turn, so the `provider_request_cancelled` path (core.clj:645-657, which currently relies on the per-sleep clear ~643 — skipped under `:provider-retry-sleep? false`) must also clear `:retry-deadline-ms` on close; otherwise a cancelled turn leaks the deadline into the next turn's window-open detection. Watch the test-mode skip of the inter-attempt clear when wiring this.
  - **Final-sleep emit semantics (ambiguity 1).** Reusing `mark-active-retry!` + `provider_retry_scheduled` for the truncated final is INTENDED and accurate (the truncated delay is genuinely slept); the authoritative terminal is the subsequent `provider_request_finished` (`:final? true`, `:retry-exhausted`, `:exhausted-reason :deadline`). The transient `:retry-attempt`→next-attempt / `:retry :active? true` bump is the normal per-attempt lifecycle, cleared by the final-path clear right after the sleep. No new event type.
  - **Test mode (ambiguity 2).** Under `:provider-retry-sleep? false`, the truncated sleep is a no-op but the truncated delay is STILL recorded/emitted; the finalize clear runs UNCONDITIONALLY (final-path clear at ~611 is independent of the sleep? skip that only gates the inter-attempt clear ~643). `response_mode_retry_test.clj` uses `:provider-retry-sleep? false`; `response_mode_test.clj` uses `:provider-retry-sleep-fn` (~470, 592).
  - **Exhausted-reason precedence (ambiguity 3).** Ordered branches: non-retryable → retry-disabled → count-cap → deadline. When both count-cap and deadline hold at one attempt, `:count-cap` wins. retry-outcome (core.clj:590-604) and `provider_request_finished` event (~599-616) are two separate `cond->` forms — both must gain `:exhausted-reason`.
  - **Retry-After vs default count-cap (inconsistency 1).** `retry-metadata` (session-state/model.clj:571) computes `delay-ms (or retry-after-ms exponential-delay-ms)` — a `Retry-After` fully overrides and is NOT bounded by `:auto-retry-max-delay-ms`. To make "up to 10 minutes" hold for any delay, the count-cap branch fires only when `:auto-retry-max-retries` is EXPLICITLY configured (operator-set strict cap) or the total-time budget is disabled (count-only mode); the default value (20) is non-limiting while the budget is active. Implementation consequence: the give-up predicate / config plumbing must distinguish an explicit override from the default (e.g. presence of the key in effective config, or a `count-cap-set?` signal) so explicit small caps stay behavior-preserving while the default can't prematurely fire under fast `Retry-After`. Approach-4 "~14 attempts in 10 min" rationale is superseded (holds only for exponential).
  - No SCOPE_QUESTION items in this batch; scope boundary (provider/model-independent default) unchanged.
- architecture review 2026-08-18 (first turn, current design state): no new actionable architectural-fit feedback. Verified the three prior arch steps remain satisfied in the post-follow-up design.md — single give-up predicate folds count-cap + deadline into `failure-reason-for` (structured outcome, no separate loop-body check); deadline stored in canonical session state via `apply-root-state-update-in!`/session-update + optional `agent-session-schema` field; deadline threaded through the loop binding alongside `:retry-attempt` with canonical value cleared only on true window close (per-sleep clear preserves it). Config plumbing follows the existing `:auto-retry-*` → ctx `:config` flow; retry termination stays in the runtime-owned execute-and-record boundary; additive `:exhausted-reason` (addition-over-modification); optional backward-compatible schema field; `:now-fn`/`:provider-retry-sleep-fn` seam reuse; cancellation preserved. Scope boundary (provider/model-independent uniform default) is a user decision consistent with config-driven architecture; no SCOPE_QUESTION.
- ambiguity review 2026-08-18 (second turn, current design state): added 2 new design steps — (1) give-up predicate's "explicitly configured" `:auto-retry-max-retries` detection: default-config always supplies the key (3 today, raised to 20) and turn-runtime reads it via plain `(get-in ctx [:config :auto-retry-max-retries] 3)` (core.clj:546), so no mechanism preserves explicit-vs-default — the count-cap gating on "explicitly configured" needs a plumbed explicitness signal (effective-config key presence, count-cap-set? flag, or sentinel); (2) count-only-mode default: with the budget disabled and no explicit cap, the sole limiter is the raised default 20 (was 3) — a silent behavior change to the count-only path, needs a pinned default. Both are config-semantics of the new feature, within the frozen scope; no SCOPE_QUESTION. Prior ambiguity steps (deadline lifecycle, exhaustion distinguishability, disable semantics, truncated-sleep scheduling, cancel-during-truncated-sleep, test-mode final-sleep, exhausted-reason precedence) verified still addressed in design.md.
- inconsistency review 2026-08-18 (third turn, current design state): added 1 new design step — Approach 4's raise of default `:auto-retry-max-retries` 3→20 ("nominal safety value", justified by the budget-active window) contradicts its own explicit-only count-cap gating: the default is never a limiter in the budget-active path (deadline bounds), so the raise's stated purpose is vacuous there; its only real effect is count-only mode (budget disabled), where it silently changes the sole limiter 3→20 (ties to ambiguity step 2). Context discovery (not in design.md): `ctx :config` is `(merge session/default-config (or config {}))` (agent-session/context.clj:322) — default-config keys are always present in effective config, so (a) the inline fallback `(get-in ctx [:config :auto-retry-max-retries] 3)` (turn-runtime/core.clj:546) is unreachable once default-config is updated (updating model.clj:246 to 20 suffices), and (b) the explicitness signal for ambiguity step 1 CANNOT be key-presence in ctx `:config` (the key is always merged in) — it needs a separate mechanism (e.g. compare against default-config, or a distinct operator-only key). Verified remaining design↔artifact consistency: `retry-metadata` `delay-ms (or retry-after-ms exponential-delay-ms)` + `:resume-at (+ now-ms delay-ms)` (model.clj:571-582); `provider-error-kind` `:overloaded` from message pattern before the status branch, `:provider-unavailable` for 500/502/503/529 (model.clj:470-479) — matches the resolved 529 classification. No SCOPE_QUESTION.
- slice-notes (design-review session → 3 open steps: 2 ambiguity + 1 inconsistency). Principles to hold when addressing them: keep the count-cap branch firing ONLY on explicit operator config (or budget-disabled count-only mode) — the default must not prematurely gate under fast `Retry-After`; preserve behavior for explicit small caps (hard cap) and for count-only mode; keep the single give-up predicate folded into `failure-reason-for` (arch step 1) and the deadline threaded through the loop binding (arch step 3); the explicitness signal must be separate from ctx `:config` key presence (default-config keys are always merged in at agent-session/context.clj:322). Mechanics for the 3 open steps (all config-semantics of the new feature, no code-seam changes beyond the give-up predicate's inputs):
  - **Explicitness detection (ambiguity 1).** `ctx :config` = `(merge session/default-config (or config {}))` (agent-session/context.clj:322); default-config (session-state/model.clj:246) always supplies `:auto-retry-max-retries`. So key-presence in ctx `:config` is NOT a valid explicitness test. Options: (a) compare the effective value against the default (fragile if operator sets 20), (b) a distinct operator-only key/flag (e.g. `:auto-retry-max-retries-explicit?` set when the caller passes config), or (c) resolve explicitness at the point where caller config is merged (context.clj:322) and thread a flag into ctx `:config`. Note the inline fallback `(get-in ctx [:config :auto-retry-max-retries] 3)` (turn-runtime/core.clj:546) becomes dead once model.clj:246 is updated — the give-up predicate should read the merged value, not rely on the fallback.
  - **Count-only default (ambiguity 2) + default-raise contradiction (inconsistency 1).** These two steps converge on the same design decision: what the default `:auto-retry-max-retries` means in count-only mode (budget disabled) — 20 (raised, accepting 3→20 change) or a smaller count-only default. The design must state ONE answer and the raise's purpose (bind count-only mode, or unused nominal). Resolution here also pins the raise's "never reached in window" framing: under explicit-only gating the default is never the budget-active limiter, so the raise's only real effect is count-only.
  - No SCOPE_QUESTION items; scope boundary (provider/model-independent uniform default) unchanged.
- design-follow-up 2026-08-18 (current-session batch complete → 3/3 steps done: explicit-count-cap detection, count-only default, default-raise contradiction). Addressed all 3 in design.md (Approach 4 + Acceptance). Implementation-relevant mechanics (not in design.md):
  - **Explicitness = sentinel `nil` default.** `default-config`'s `:auto-retry-max-retries` becomes `nil` ("unset"). Effective config is `(merge default-config caller-config)` (agent-session/context.clj:322), so a non-`nil` effective value ⇔ operator/caller explicitly set it; `nil` ⇔ default. Give-up predicate: `nil` → count-cap branch never fires while budget active; non-`nil` → explicit hard cap, fires as today. All current retry tests/callers pass concrete explicit values (0,1,2,3) → read as explicit, behavior-preserving.
  - **NPE trap: `(long nil)` throws.** The existing `max-retries (long (get-in ctx [:config :auto-retry-max-retries] 3))` (turn-runtime/core.clj:546) throws NPE once the default is `nil` (key present-with-nil, `get-in` returns nil, `(long nil)` NPE). The rewritten give-up predicate must treat nil as "no cap" and never coerce to long. This inline fallback becomes dead once default-config supplies the key.
  - **Count-only fallback = 3, kept out of default-config.** Budget-disabled + cap-unset → sole limiter is a count-only fallback default of 3, resolved in the predicate (e.g. `(or explicit-cap 3)`), NOT the `default-config` value (which is now the sentinel nil). This preserves the prior count-only 3-attempt behavior — no silent 3→20 change. The "raise to 20" is dropped entirely; no nominal-safety rationale remains.
  - **Budget-active default = deadline-only.** With sentinel nil + budget active, AC1 "up to 10 minutes" holds regardless of delay source (fast `Retry-After` can't hit a default count cap). Explicit small caps still bound.
  - **Test seam note.** New deadline-window tests drive the window via injected `:now-fn` + `:provider-retry-sleep-fn`; existing retry tests set explicit small caps so they remain count-capped (unaffected by sentinel). A test asserting default (non-explicit) behavior must pass `:config {}` (or no override) so the effective cap is nil.
  - No SCOPE_QUESTION items in this batch; scope boundary (provider/model-independent uniform default) unchanged.
- architecture review 2026-08-18 (first turn, current design state — post sentinel-nil): no new actionable architectural-fit feedback. Verified the settled arch steps remain satisfied in design.md — single give-up predicate folded into `failure-reason-for` (turn-runtime/core.clj:361) with structured outcome, no separate loop-body deadline check; deadline in canonical session state via `apply-root-state-update-in!`/`session-update` + optional `agent-session-schema` field; deadline threaded through the loop binding alongside `:retry-attempt` so the inter-attempt `clear-active-retry!` (core.clj:402) doesn't reopen a fresh window (verified clear resets `:retry-attempt`/`:retry` mid-window). Sentinel-`nil` `:auto-retry-max-retries` + count-only fallback 3 are config-semantics within frozen scope (settled in prior ambiguity/inconsistency turns; `¬compat(backward)` makes the default change acceptable); config plumbing follows the existing `:auto-retry-*` → ctx `:config` flow; retry termination stays in the runtime-owned execute-and-record boundary; additive `:exhausted-reason` follows addition-over-modification; `:now-fn`/`:provider-retry-sleep-fn` seam reuse; cancellation preserved. Scope boundary (provider/model-independent uniform default) is a user decision consistent with config-driven architecture; no SCOPE_QUESTION.
- ambiguity review 2026-08-18 (second turn, current design state): added 1 new design step — loop-entry behavior for a persisted `:retry-deadline-ms` already in the past. Design clears the deadline only on the three retry-loop terminal paths (success / final-give-up / cancel) and keys fresh-window detection on *presence* of the loop-bound deadline; a window left open by a turn-end path outside those clears (external abort of an in-flight request, which the budget deliberately does not cut off; session persisted mid-window and rehydrated after the deadline) leaves a present-but-expired deadline in canonical state, so the next loop entry seeds it and the first retryable failure immediately hits `now >= deadline` → `:retry-exhausted :deadline` with zero retries — contradicting the "stale deadline never leaked" guarantee. Design must pin stale-vs-authoritative handling of a past deadline at entry and enumerate turn-end/rehydration clear paths. Prior ambiguity steps verified still addressed; no SCOPE_QUESTION.
- inconsistency review 2026-08-18 (third turn, current design state): added 1 new design step — Approach 4's fast-`Retry-After` rationale ("would otherwise let the attempt count reach a fixed default cap at ~20 s", design.md:312) is a stale vestige of the dropped raise-to-20. Under the current sentinel-`nil` default there is no default count cap at all (budget-active → count-cap never fires with nil), and a hypothetical fixed default would be 3 (~3 s), not 20 (~20 s) — the "~20 s / fixed default cap" premise only matches the dropped raised default. Reword so the illustration agrees with the sentinel design. Verified no other new internal or design↔artifact inconsistency: give-up predicate branch order + `:exhausted-reason` precedence (count-cap before deadline, count-cap wins), truncated-final routing + test-mode clear, deadline threading + per-sleep-preserve/final-clear, sentinel explicitness + count-only fallback 3 + `(or explicit-cap 3)` resolution, Retry-After override/truncation, anchor + AC1 "10 min from first retry decision, in-flight not cut off", backoff arithmetic (2,4,8,16,32,60…) + "~14 s for 3 attempts" all agree with code and internally. No SCOPE_QUESTION.
- slice-notes (design-review session → 2 open steps: 1 ambiguity + 1 inconsistency). Principles to hold when addressing them: keep the single give-up predicate folded into `failure-reason-for` — do not add a separate loop-body stale-deadline branch; keep the deadline in canonical session state and threaded through the loop binding; preserve cancellation precedence (cancel-during-any-sleep → `:retry-cancelled`, not `:deadline`); drive the window with injected `:now-fn`, never real sleeps. Mechanics for the 2 open steps (loop in `components/turn-runtime/src/psi/turn_runtime/core.clj` `execute-prepared-request!`):
  - **Ambiguity (past persisted deadline at loop entry).** Loop entry is `(loop [retry-attempt (retry-attempt-for ctx session-id)]` (core.clj:550); the deadline threads alongside (`retry-deadline-for`, mirroring `retry-attempt-for` at :333). Line 549 dissocs only `:provider-retry-abort-requested?` at entry — no retry-state/deadline clear, so a persisted deadline from a prior turn's window survives into the next loop. The cancel path (core.clj:652-658) fires only when cancellation arrives DURING a retry sleep (`(if cancelled? ... (recur ...))`); an abort landing while an in-flight request is executing (budget deliberately doesn't cut off in-flight) skips it, so a window genuinely can be left open past its deadline. Resolution naturally lives in the seed: `retry-deadline-for` (or the predicate) treats `deadline < now` at entry as stale → nil/fresh window, vs authoritative immediate `:deadline` give-up — design must pick one. Stale check uses the same `:now-fn` as the deadline computation, so it's test-deterministic. Note the `retry-outcome` map (core.clj:590-604) and `provider_request_finished` event (:599-616) are two separate `cond->` forms, both carrying `:retry-attempt`/`:attempt-count`/`:max-retries` — the loop-entry stale decision has no event of its own, so surface it only via the predicate outcome.
  - **Inconsistency (Approach 4 stale "~20 s / fixed default cap").** design.md-only wording fix (line ~312); no code change. Under sentinel-`nil` there is no default count cap in the budget-active path, so the rationale should say the count-cap can never prematurely fire (or reword the counterfactual against the old default 3), not reference a fixed ~20 default.
- design-follow-up 2026-08-18 (current-session batch complete → 2/2 steps done: past persisted deadline at loop entry, Approach 4 stale ~20 s rationale). Addressed both in design.md. Implementation-relevant mechanics (not in design.md):
  - **Stale deadline at loop entry (ambiguity).** Chose STALE (not authoritative): `retry-deadline-for` treats a persisted `:retry-deadline-ms` already in the past (`deadline < now`, via the same injected `:now-fn`) as stale — clears the canonical field and yields nil so the first retryable, retry-enabled failure opens a fresh window. This covers the two turn-end/rehydration paths outside the three terminal clears (success/final-give-up/cancel): external abort of an in-flight request (retry-loop cancel path never runs; budget deliberately doesn't cut off in-flight) and a session rehydrated after the deadline passed. No new explicit clears at those turn-end points — the seed's stale check runs before window-open detection. Implication for implementer: `retry-deadline-for` (loop-entry read-back, mirroring `retry-attempt-for` core.clj:333/550) is no longer a pure read — it must compare against `:now-fn` and, when expired, dissoc the canonical `:retry-deadline-ms` (a session-state write at loop entry, before any failure decision). A still-future deadline at entry stays authoritative (mid-window re-entry continues the same budget).
  - **Approach 4 wording (inconsistency).** design.md-only fix (Approach 4, was line ~312); no code change. Under sentinel-`nil` there is no default count cap in the budget-active path; the reworded rationale states the count-cap can never prematurely fire and re-frames the counterfactual fixed caps as old-3 (~3 s) and dropped-raise-20 (~20 s).
  - No SCOPE_QUESTION items in this batch; scope boundary (provider/model-independent uniform default) unchanged.
- architectural review 2026-08-18 (first turn, current design state): no new actionable architectural-fit feedback. Re-verified settled arch steps remain satisfied in design.md — single give-up predicate folded into `failure-reason-for` (structured outcome, no separate loop-body deadline branch); deadline as top-level canonical session field via `apply-root-state-update-in!`/`session-update` + optional `agent-session-schema` entry; deadline threaded through the loop binding so the inter-attempt `clear-active-retry!` (core.clj:402) does not reopen a fresh window. Config plumbing follows existing `:auto-retry-*` → ctx `:config` flow; retry termination stays in the runtime-owned execute-and-record boundary; additive `:exhausted-reason` (addition-over-modification); optional backward-compatible schema field; `:now-fn`/`:provider-retry-sleep-fn` seam reuse; cancellation preserved. Loop-entry `retry-deadline-for` stale-check dissoc is a canonical write inside the runtime boundary, consistent with existing retry-state mutations — not a new misfit. Scope boundary (provider/model-independent uniform default) is a user decision consistent with config-driven architecture; no SCOPE_QUESTION.
- ambiguity review 2026-08-18 (second turn, current design state): added 1 new design step — reported `:max-retries` on retry-outcome / `provider_request_finished` under the sentinel-nil default. Both surfaces carry `:max-retries` today, always 3 (core.clj:546 `(long (get-in ctx [:config :auto-retry-max-retries] 3))`, outcome assembled :590-604, event :599-616). With default-config sentinel nil, the effective value is nil in the budget-active path and the predicate resolves the cap as `(or explicit-cap count-only-default 3)` only in budget-disabled mode — but the design never pins what the REPORTED field holds on those surfaces (nil vs count-only 3 vs omitted) per mode, so a consumer/telemetry keying on `:max-retries` sees a silent 3→nil change. Note the outcome/event `:retry-attempt`/`:attempt-count` are built from the loop binding BEFORE the final-path clear, so they are NOT affected by the truncated-final clear — only `:max-retries` reporting is under-specified. Prior ambiguity steps verified still addressed; no SCOPE_QUESTION.
- inconsistency review 2026-08-18 (third turn, current design state): added 1 new design step — cancel-path deadline clearing vs the per-sleep clear it reuses. Design Clearing (design.md:280-290) lists cancel as a true-window-close clear via clear-active-retry! (:clear-deadline? true), but the code cancel path (core.clj:645-657) has NO independent clear — it relies on the inter-attempt per-sleep clear (core.clj:642-643) that the design re-purposes to PRESERVE the deadline and that is skipped under :provider-retry-sleep? false. So a literal reading leaks the deadline on cancel (and, in test mode, also :retry/:retry-attempt). Design calls out the truncated-final finalize clear as unconditional (design.md:146-156) but not the cancel clear. Resolution needs the cancel path to have its own unconditional clear-active-retry! (:clear-deadline? true) independent of the per-sleep clear / test-mode skip. Verified no other design↔artifact inconsistency in this pass: sentinel-nil resolution / count-only fallback 3, branch order + :exhausted-reason precedence, truncated-final routing + test-mode finalize clear, deadline threading + per-sleep-preserve/final-clear, Retry-After override/truncation, anchor + AC1, 529 classification, loop-entry stale deadline — all agree with code and internally. The :max-retries reporting question (ambiguity turn 2, still open) is a spec-gap not an internal contradiction; not re-filed. No SCOPE_QUESTION.
- slice-notes (design-review session → 2 open steps: 1 ambiguity + 1 inconsistency). Principles to hold when addressing them: keep the single give-up predicate folded into `failure-reason-for` — do not add a separate loop-body deadline check; keep the deadline in canonical session state and threaded through the loop binding; preserve cancellation precedence (cancel-during-any-sleep → `:retry-cancelled`, never `:deadline`); drive the window with injected `:now-fn`, never real sleeps. Mechanics for the 2 open steps (loop in `components/turn-runtime/src/psi/turn_runtime/core.clj` `execute-prepared-request!`):
  - **Ambiguity (reported `:max-retries` under sentinel-nil).** `:max-retries` is surfaced on **three** places, not two: the retry-outcome map (core.clj:590-604), the `provider_request_finished` event (:599-616), AND the cancelled path's `cancelled-retry-outcome` (:494-506, `:max-retries` at :503) + `provider_request_cancelled` event (:647-657). All are fed from the single `max-retries` binding at :546, today `(long (get-in ctx [:config :auto-retry-max-retries] 3))`. The ambiguity step lists the first two; the cancelled surface must resolve consistently with whatever value is chosen. The NPE trap (prior slice-note): once default-config's `:auto-retry-max-retries` is the sentinel `nil`, `(long nil)` throws — the resolution must treat nil as "no cap" and never coerce to long, so any reported value decision (nil | count-only 3 | omit) must flow from the same non-coercing predicate resolution the count-cap branch uses. Keeping the count-only fallback 3 as the *reported* value in budget-disabled mode, and deciding budget-active reporting, is exactly the ambiguity's open question.
  - **Inconsistency (cancel-path deadline clearing).** The cancel path (core.clj:645-657) has no independent `clear-active-retry!` — it relies on the per-sleep clear at :642-643 (the `(when-not (= false (:provider-retry-sleep? ctx)) ...)` before the `if cancelled?`), which the design re-purposes to PRESERVE the deadline and which is skipped under `:provider-retry-sleep? false`. So the cancel path must gain its own unconditional `clear-active-retry!` (`:clear-deadline? true`) independent of the per-sleep clear / test-mode skip. Note the per-sleep clear at :642-643 runs BEFORE the `if cancelled?` (:645) — if a `:clear-deadline?` param is added, the cancel branch is after that call, so the unconditional cancel clear is a separate call, not a modification of the per-sleep one. `clear-active-retry!` (:402-412) + `retry-clear-needed?` (:395-400) both need the window-scoped distinction.
  - No SCOPE_QUESTION items; scope boundary (provider/model-independent uniform default) unchanged.
- design-follow-up 2026-08-18 (current-session batch complete → 2/2 steps done: reported :max-retries under sentinel-nil, cancel-path deadline clear). Addressed both in design.md (Approach 4 + Clearing). Implementation-relevant mechanics (not in design.md):
  - **Reported `:max-retries` = effective count limiter.** `:max-retries` on the retry-outcome, `provider_request_finished`, cancelled-retry-outcome, and `provider_request_cancelled` surfaces reports the count limiter actually bounding the window: explicit non-`nil` cap → that value; budget-disabled + nil cap → count-only fallback 3 (preserves count-only consumers); budget-active default (nil cap) → `nil` (deadline alone bounds). All four surfaces flow from ONE resolved value (the same non-coercing predicate resolution as the count-cap branch — never `(long nil)`), so they agree. The single `max-retries` binding at core.clj:546 today coerces `(long ... 3)`; the sentinel-nil rewrite must resolve nil as "no cap" and feed the resolved value to all four surfaces (retry-outcome :590-604, event :599-616, cancelled-retry-outcome :494-506/:503, provider_request_cancelled :647-657). Known/acknowledged consumer-visible change: budget-active default reports nil instead of 3 (accurate — no count limiter on the default path).
  - **Cancel path owns an unconditional clear.** The cancel branch (core.clj `(if cancelled? ...)` ~:645-657) must gain its OWN `clear-active-retry!` (`:clear-deadline? true`) call inside the `if cancelled?` branch, AFTER the per-sleep clear (:642-643), not a modification of it — because the per-sleep clear PRESERVES the deadline and is skipped under `:provider-retry-sleep? false`. This makes the cancel-path clear unconditional (independent of sleep? / test-mode skip), mirroring the truncated-final finalize clear; without it the deadline (and in test mode `:retry`/`:retry-attempt`) leaks across a cancelled turn. `clear-active-retry!` (:402-412) + `retry-clear-needed?` (:395-400) gain the `:clear-deadline?`-style window-scoped distinction; the cancel-path call passes clear-deadline true.
  - No SCOPE_QUESTION items in this batch; scope boundary (provider/model-independent uniform default) unchanged.
- implementation complete 2026-08-18. All steps done; tests green. Implementation-relevant facts for review/closure:
  - **Retry machinery extracted to `psi.turn-runtime.retry` namespace** (`components/turn-runtime/src/psi/turn_runtime/retry.clj`): `now-ms`, `retry-deadline-for`, `give-up-decision`, `attempt-id-for`, `dispatch-provider-event!`, `provider-error-fields`, `retry-metadata-for`, `mark-active-retry!`, `retry-clear-needed?`, `clear-active-retry!`, `active-turn-cancelled?`, `provider-retry-cancelled?`, `sleep-for-retry!`, `interruptible-sleep-for-retry!`, `cancelled-retry-outcome`. core.clj requires it as `retry` and drives the loop (execute-prepared-request!). This separation happened mid-implementation (concurrent process on the worktree reorganized the same logic); verified complete, self-consistent (clj-kondo clean), and test-green before committing.
  - **Config resolution** (top of execute-prepared-request!): `budget-active? = (pos? (long (or (get-in ctx [:config :auto-retry-total-timeout-ms]) 0)))`; `explicit-cap = (get-in ctx [:config :auto-retry-max-retries])` (sentinel nil default); `count-cap = (cond (some? explicit-cap) explicit-cap (not budget-active?) 3 :else nil)`. `:max-retries` on retry-outcome + cancelled-retry-outcome reports `count-cap` (nil in budget-active default path). Never `(long nil)`.
  - **Deadline lifecycle**: canonical top-level `:retry-deadline-ms` (schema optional `[:maybe :int]`), written by `mark-active-retry!`, read at loop entry by `retry-deadline-for` (past deadline → dissoc canonical + nil → fresh window), threaded via `recur`, preserved by inter-attempt `clear-active-retry!` (`:clear-deadline? false`), cleared on success/final-give-up/cancel (`:clear-deadline? true`). Cancel path has its OWN unconditional `clear-active-retry!` `:clear-deadline? true` in the `if cancelled?` branch (independent of the `:provider-retry-sleep?` skip), so no deadline leaks across a cancelled turn.
  - **Give-up-decision**: single structured predicate, branch order non-retryable → retry-disabled → count-cap → deadline → overshoot (`:final-sleep-ms`). Count-cap wins over deadline when both hold. `retry-metadata-for` runs before the predicate so it sees the would-be next delay.
  - **Truncated final**: overshoot outcome routes the non-final path once (truncated `:delay-ms`/`:resume-at` = remaining window, recorded/emitted), sleeps via the interruptible seam, then emits the authoritative final `provider_request_finished` (`final? true`, `:retry-exhausted`, `:exhausted-reason :deadline`). The error-branch `provider_request_finished` for that attempt is NON-final (`final? immediate-final?`), so consumers see scheduled → final (supersedes). Cancel during the truncated sleep → `:retry-cancelled` (cancel precedence), then the unconditional cancel clear.
  - **`:exhausted-reason`** (`:count-cap | :deadline`) on retry-outcome + `provider_request_finished` event (both `cond->` surfaces) whenever `:retry-exhausted`.
  - **`:max-retries` surfaces** are ONLY the two outcome maps (retry-outcome, cancelled-retry-outcome); the `provider_request_finished` / `provider_request_cancelled` events do NOT carry `:max-retries` (corrected in design.md during implementation).
  - **Tests**: 7 new tests in response_mode_retry_test.clj (budget-active deadline termination + nil max-retries; truncated final recording + final event; explicit-cap hard-cap count-cap; count-only fallback 3; oversized Retry-After truncated to deadline; cancel clears deadline; deadline preserved inter-attempt + cleared on close; stale past deadline opens fresh window). All drive the window via injected `:now-fn` clock + `:provider-retry-sleep-fn` / `:provider-retry-sleep? false` — no real sleeps.
  - **Full-suite**: `bb test` (seed 536015077) → 2 failures, both PRE-EXISTING on baseline HEAD (verified in a clean worktree): `execute-prepared-request-streaming-error-event-provider-headers-drive-retry-test` (test-order-dependent attempts 2→3) and `delegate-review-task-implementation-completes-with-nullable-local-model-test` (model-registry unknown `deepseek/deepseek-v4-flash`). Not introduced by this change.
  - No SCOPE_QUESTION items; scope boundary (provider/model-independent uniform default) unchanged.
- review + closure 2026-08-18. Implementation review passed (direct review; no delegate tool in this session): all 10 design acceptance criteria satisfied by the committed code + tests; only safe (non-coercing) reads of the sentinel-`nil` `:auto-retry-max-retries`; no NPE risk; no new regressions (full-suite failures pre-existing on baseline). Knowledge extracted to mementum/knowledge/provider-retry-total-time-window.md. Task closed.
- ambiguity review 2026-08-18 (plan review, first turn): added 1 new design step — minimum per-attempt delay when a provider-sent integer `Retry-After` is 0/negative (no positive floor in `retry-after-delay-ms` integer branch; `(or 0 exponential)` keeps 0; budget-active default + `sleep-for-retry!` pos? guard → back-to-back retries until deadline, vs pre-change count-cap 3 bound). Plan.md otherwise verified consistent with the committed implementation (config resolution, give-up-decision branch order, truncated final-sleep routing, `:clear-deadline?` lifecycle, cancel-path unconditional clear, count-only fallback 3, test seams).
