2026-05-13: Created task after live reload attempt showed session worktree can differ from runtime source root, making explicit reload targeting hard to discover.
2026-05-13: Confirmed the gap live. `psi-tool(action: "reload-code", namespaces: ["psi.agent-session.psi-tool"], worktree-path: "/Users/duncan/projects/hugoduncan/psi/refactor")` failed because the running runtime had loaded that namespace from `/Users/duncan/projects/hugoduncan/psi/psi-main/...`, not this worktree. A follow-up reload with explicit worktree `/Users/duncan/projects/hugoduncan/psi/psi-main` succeeded.
2026-05-13: Revised decision after review: that successful follow-up was diagnostically interesting but policy-wrong. For psi self-development, the authoritative reload target should remain the session worktree. A different runtime checkout should be surfaced as a mismatch, not suggested as an alternate reload target.
2026-05-13: Cleanup plan: remove the temporary public `:psi.runtime/source-root` attr and guidance, retain `runtime-root` only for extension-install path resolution, and make reload mismatch errors explicitly explain the worktree-vs-runtime-checkout tension.
2026-05-13: Executed cleanup. Removed the public attr from resolvers, graph-surface expectations, docs, and prompt guidance. Revised reload mismatch messaging to keep worktree authority explicit and to recommend restarting psi from the edited worktree when the loaded namespace source comes from a different checkout. Focused verification passed: 66 tests, 2656 assertions, 0 failures.
2026-05-13: Follow-up polish for discoverability. Live-verified the reliable loop in the current refactor worktree by first querying `[:psi.agent-session/worktree-path]`, then successfully reloading a small namespace (`psi.prompt-assets.system-prompt`) with explicit worktree targeting. Added prompt/doc guidance that makes the loop explicit: discover target worktree first, start with one small already loaded namespace reload, widen to worktree reload only after success, and never retarget reload to a different checkout just because the current runtime was started there.
2026-05-13: Policy correction from user direction. Worktree-path is authoritative not only for targeting but also for source selection. Reload no longer treats loaded-source-path outside the target worktree as a hard failure. Instead, namespace reload resolves target source files from the requested/session worktree and loads those files into the running process, while surfacing loaded-source-path vs target-source-path mismatch as warning-only diagnostics.
