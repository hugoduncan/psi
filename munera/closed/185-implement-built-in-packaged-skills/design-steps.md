- [x] Clarify the built-in skill materialization cache contract: use a psi-owned user-global cache root under `~/.psi/agent/`, materialize built-in skills into deterministic version/content-addressed snapshots, reuse snapshots when packaged build/resources match, and refresh by selecting a new snapshot path when they differ so paths stay stable within one runtime while upgrades remain deterministic.
- [x] Align the source-class contract with current skill loading semantics before implementation: the task artifacts require built-in lowest precedence plus explicit `:extra-paths` > project > user-global > built-in selection, but current `discover-skills` behavior and tests are first-discovered-wins with user-global loaded before project. Decide whether task 185 changes canonical precedence to the new winner-takes-precedence contract or reshapes discovery/bootstrap seams so the finalized order is enforced coherently across code, diagnostics, and docs.
- [x] Define the canonical same-source tie-breaker precisely. Acceptance/plan/steps say same-source handling must be deterministic and documented, but they never choose the rule (e.g. path order, lexicographic path, first discovered within configured directory order, or error-on-duplicate), which leaves implementation/tests/docs free to diverge.
- [x] Align the built-in skill provenance surface named in the task with current skill introspection/read-model semantics. The task requires capability/introspection surfaces to distinguish built-in provenance, but existing skill surfaces expose `:source` groupings and enriched skill maps rather than a separate capability surface. Specify which existing skill surfaces must show built-in provenance and whether built-ins appear as `:source :built-in`, additional metadata, or both.
