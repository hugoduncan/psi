- [x] Add `plan.md` that translates the umbrella design into a concrete review/decomposition approach, including the order for inventorying domains and deciding whether each lands in membership-id, derived-payload, runtime-adapter, or out-of-scope classification.
- [x] Add `steps.md` with explicit checklist items for the intended ambiguity-resolution work so future passes can distinguish missing task setup from completed review/decomposition work.
- [x] Clarify in `design.md` or `plan.md` whether this task itself should produce refined follow-on task directories/updates for each affected domain, or only leave architectural guidance for later manual task creation.
- [x] Clarify the prompt-domain lifecycle rule so `design.md`/`plan.md`/`steps.md` do not describe `:prompt-contribution-ids` as the sole authoritative session state while child-session shaping and prompt mutation handlers still persist derived `:prompt-contributions` vectors as required compatibility projection state.
- [x] Clarify whether the prompt lifecycle contract intends child-session prompt-contribution inheritance/filtering to read from parent `:prompt-contribution-ids` membership or from the current derived contribution set, because `child_session_state.clj` currently copies parent ids/projections before any child-specific narrowing and the task artifacts do not state which surface owns that rule.
