- architectural review added 1 new design step
- ambiguity review added 2 new design steps
- ambiguity review observed additional existing oversized `extensions/` src/test files; this matters for interpreting the widened file-length check's expected final pass/fail state.
- inconsistency review added 1 new design step
- design-step handoff: keep follow-up resolution as design clarification, not scope reshaping; let a human answer the `SCOPE_QUESTION` before adding/removing doc/changelog work. Preserve `work_on_test.clj` test behaviour while choosing cohesive split namespaces; treat current filesystem counts as truth over stale prose. Relevant non-task files: `AGENTS.md`, `.psi/skills/clojure-coding-standards/SKILL.md`, `bb.edn`, `extensions/work-on/test/extensions/work_on_test.clj`.
- 2026-07-01 design follow-up: review-batch baseline identified as parent of `7fdbfe742` (`2c5477fe5`); candidate checklist was the four lines added to `design-steps.md` by `git diff 2c5477fe5..HEAD -- <task>/design-steps.md`. Completed the three non-scope design clarifications; left the `SCOPE_QUESTION` unchecked/deferred to the user.
- 2026-07-01 architecture review: no new architectural-fit feedback.
- 2026-07-01 ambiguity review shared-session turn: no new ambiguity feedback.
- 2026-07-01 inconsistency review shared-session turn: no new inconsistency feedback.
- 2026-07-01 design-step resolution handoff: keep the open `SCOPE_QUESTION` limited to doc/changelog artifact synchronization; if the human answers in-scope, apply AGENTS.md changelog/user-doc rules without changing the frozen split/enforcement boundary. Relevant non-task files: `CHANGELOG.md`, `README.md`, `doc/extensions.md`, `.psi/commit-checks.edn`.
- 2026-07-01 plan-review ambiguity turn: no new ambiguity feedback.
- 2026-07-01 plan-review inconsistency turn: no new inconsistency feedback.

- 2026-07-01 implementation pass: split `extensions/work-on/test/extensions/work_on_test.clj` (1298 lines, 21 deftests) into `work_on_test_support.clj` (123 lines, no deftests), `work_on_command_test.clj` (597 lines, 11 deftests), `work_on_tool_test.clj` (265 lines, 4 deftests), and `work_done_test.clj` (333 lines, 6 deftests). Preserved the original 21 deftest names with no missing/duplicate names. Shared helpers moved to `extensions.work-on-test-support`; helpers used across command/tool routing and done/rebase/status tests are public support fns, support-local `session-query` remains private, and the remote-base-ref-only git/cleanup helpers stay local to `work_on_command_test.clj`.
- Verification: baseline `bb clojure:test:scry --namespace extensions.work-on-test` passed before the split (21 tests, 119 assertions). Final `bb clojure:test:scry --namespace extensions.work-on-command-test --namespace extensions.work-on-tool-test --namespace extensions.work-done-test` passed (21 tests, 119 assertions). `clj-kondo --lint` on the new work-on test files plus `bb.edn` passed with 0 errors/0 warnings; `clj-paren-repair` succeeded on the new Clojure test files.
- `bb.edn` `commit-check:file-lengths` now scans `components`, `bases`, and `extensions` with the same `*/src/*`/`*/test/*` filter. A controlled 801-line file under `extensions/work-on/test/extensions/` made the check exit 1 and report that path, then the controlled file was removed.
- Real-tree `bb commit-check:file-lengths` initially failed only on pre-existing out-of-scope oversized extension files: `extensions/auto-session-name/test/extensions/auto_session_name_test.clj` (974), `extensions/dev-http/test/extensions/dev_http_test.clj` (816), `extensions/mcp-tasks-run/test/extensions/mcp_tasks_run_test.clj` (1020), and `extensions/mcp-tasks-run/src/extensions/mcp_tasks_run.clj` (2058). To keep commit checks usable without broad out-of-scope splits, those exact files are now recorded as a legacy ratchet baseline in `bb.edn`: they pass at the recorded line counts, fail if they grow, and any new scanned file still fails over 800 lines. The check no longer reports the split work-on test files.
- Broader extension-suite check: `bb clojure:test:extensions` executed the suite and reported `Assertions: 1037 passed, 0 failed, 0 errored; Tests: 271 passed, 0 failed, 0 errored, 1 unknown`, then exited non-zero with an empty `.scry-results/`; focused split work-on tests are green and provide the task-specific pass signal.

- 2026-07-01 implementation review: no new actionable issues.
- 2026-07-01 test review: added 2 steps to be addressed.
- 2026-07-01 commit-check repair: after commit `a56a44ba1`, the repository commit hook failed because the widened check made the four pre-existing oversized extension files blocking. Minimal repair kept the widened `extensions/` scan but added explicit legacy max-line ratchets for those four files. Verification: `bb commit-check:file-lengths` passes on the real tree; a controlled 801-line file under `extensions/work-on/test/extensions/` failed with `800 limit`; a temporary growth probe on `extensions/dev-http/test/extensions/dev_http_test.clj` failed above its recorded `816` legacy limit; probes were removed and `clj-kondo --lint bb.edn` passed.
- 2026-07-01 follow-up pass: addressed 2 test-review steps. Added executable `extensions.commit-checks-test` coverage for `bb commit-check:file-lengths` using isolated bb.edn fixtures: an oversized `extensions/*/test/*` file fails with the 800-line limit, a legacy ratcheted extension path passes at its recorded count and fails if it grows, and controlled oversized `components/*/src/*` plus `bases/*/test/*` files still fail after the extensions widening. Verification: `clj-paren-repair extensions/commit-checks/test/extensions/commit_checks_test.clj`, `clj-kondo --lint extensions/commit-checks/test/extensions/commit_checks_test.clj`, `bb clojure:test:scry --namespace extensions.commit-checks-test`, and real-tree `bb commit-check:file-lengths` all passed.
- 2026-07-01 test review: added 2 steps to be addressed.
- 2026-07-01 follow-up pass: addressed 2 test-review steps. Reworked `extensions.commit-checks-test` file-length coverage to invoke the repository's real `bb commit-check:file-lengths` task instead of a copied synthetic `bb.edn` task expression. Added real-task probes for controlled oversized files under `extensions/`, `components/`, and `bases/`, plus per-path legacy ratchet coverage for all four recorded oversized extension baselines proving the real tree passes at baseline and each path fails when temporarily grown. Verification: `clj-kondo --lint extensions/commit-checks/test/extensions/commit_checks_test.clj`, `bb clojure:test:scry --namespace extensions.commit-checks-test`, and `bb commit-check:file-lengths` passed.
- 2026-07-01 test review follow-up: added 2 steps to be addressed.

- 2026-07-01 follow-up pass: addressed 2 test-review steps. `extensions.commit-checks-test` now removes empty `components/commit_check_file_lengths_probe/` and `bases/commit_check_file_lengths_probe/` probe directory trees after real-task oversized-file checks, and derives legacy ratchet paths/limits by reading the real `bb.edn` `commit-check:file-lengths` task's `legacy-max-lines` binding instead of duplicating the map. Verification: `clj-paren-repair extensions/commit-checks/test/extensions/commit_checks_test.clj`, `clj-kondo --lint extensions/commit-checks/test/extensions/commit_checks_test.clj`, `bb clojure:test:scry --namespace extensions.commit-checks-test`, and `bb commit-check:file-lengths` passed.
- 2026-07-01 test review: added 1 step to be addressed.
- 2026-07-01 follow-up pass: addressed 1 test-review step. `bb commit-check:file-lengths` now uses shared helpers that skip absent scanned roots before invoking `find`, preserving current real-task behavior while supporting sparse repo/subtree shapes. Added executable `extensions.commit-checks-test` coverage for absent optional roots and present roots with no matching `src`/`test` files, evaluated against the same helper definitions used by `bb.edn`. Verification: `clj-paren-repair bb.edn extensions/commit-checks/test/extensions/commit_checks_test.clj`, `clj-kondo --lint bb.edn extensions/commit-checks/test/extensions/commit_checks_test.clj`, `bb clojure:test:scry --namespace extensions.commit-checks-test`, and `bb commit-check:file-lengths` passed.
- 2026-07-01 test review: added 1 sparse-find coverage step to be addressed.
- 2026-07-01 follow-up pass: addressed 1 test-review step. Sparse-shape coverage now executes the real shared `file-length-find-args` via `find` against temporary present scan roots with no matching `src`/`test` files, proving malformed `find` argument construction fails the test instead of only checking helper return values. Verification: `clj-paren-repair extensions/commit-checks/test/extensions/commit_checks_test.clj`, `clj-kondo --lint extensions/commit-checks/test/extensions/commit_checks_test.clj`, `bb clojure:test:scry --namespace extensions.commit-checks-test`, and `bb commit-check:file-lengths` passed.
- 2026-07-01 docs review: added 3 steps to be addressed.
