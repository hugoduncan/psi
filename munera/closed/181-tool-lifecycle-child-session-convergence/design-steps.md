# 181 design follow-up steps

- [x] Resolve whether task 181 should be rescoped or closed: five of six lifecycle surfaces listed in scope are already migrated by tasks 179+180; the only remaining `:tool-defs` flow is the internal workflow step-config pipeline (`workflow_step_session_config/core.clj` → `statechart_runtime.clj` → `attempts.clj`), which task 180 explicitly documented as a local data structure, not session authority. Decide: (a) rescope to narrow step-config cleanup, (b) close as satisfied, or (c) document why the step-config pipeline still constitutes a scope gap.
  - **Decision: (b) close as satisfied.** All six lifecycle surfaces are already migrated. The step-config pipeline is a local data-passing concern, not session authority — `:tool-defs` there are derived on-demand from `:tool-ids` + registry and never persisted to session state. Task 180 explicitly documented this as intentional. Added "Status: satisfied by tasks 179 + 180" and "Scope analysis" sections to design.md with detailed per-surface accounting.
- [x] Clarify the backward-compatibility mechanism for resuming sessions persisted before `:tool-ids` existed: is the implicit bootstrap-after-resume re-population (via `ensure-base-system-prompt` / `retarget-runtime-prompt-metadata`) the intended path, or does explicit migration logic belong in the resume handler?
  - **Clarified: implicit bootstrap-after-resume is the intended path**, but the mechanism is `select-keys` from `current-sd`, not `ensure-base-system-prompt`. `initialize-resumed-session-state` copies `:tool-ids` from the in-memory source session defaults (already bootstrapped at app startup via `adopt-startup-plan-into-session!` → `:session/set-active-tools`), not from the persisted journal. Pre-`:tool-ids` journals are handled correctly because the source session's `:tool-ids` are always populated by app startup. No explicit migration logic needed. Added "Resume backward compatibility" section to design.md.
- [x] Clarify what additional focused test coverage AC 6 requires beyond what tasks 179 and 180 already added (resolve-tool-defs unit tests, child-session mutation tests with parent set-active-tools dispatch).
  - **No additional coverage required.** Tasks 179+180 already provide comprehensive focused tests: `tool_defs_test.clj` (resolve-tool-defs unit tests), `tool_authority_handlers_test.clj` (set-active-tools/add-tool authority verification), `child_session_state_test.clj` (tool-ids coherence: default inheritance, explicit override, prompt-component-selection filtering, absence of `:tool-defs`/`:active-tools`), `child_session_mutation_test.clj` (create-child dispatch with `:tool-ids`), `child_session_contract_test.clj` (contract schema). Added "Test coverage" section to design.md.
