- [x] AMB1: Clarify whether the implementation-review limit means 10 follow-up executions after the initial review, or 10 total `review` step entries including the initial review; map the chosen meaning to the authored `:max-iterations` value and expected test assertion.
- [x] AMB2: Clarify the design/plan repeated-pass topology after a phase produces actionable feedback: whether the workflow should finish the remaining phases in the current pass before looping, or restart the next full pass immediately after that phase's follow-up; specify how completion detects that any phase in the pass found feedback after follow-up items have been cleared, including the architecture phase for design review.
- [x] AMB3: Clarify pass-limit counting for `review-task-design` (6) and `review-task-plan` (5): whether the initial pass counts toward the limit, which loop-back transition/step owns the guard, the maximum number of actual review executions per phase, and the expected failure behavior when the final allowed pass still yields actionable feedback.
