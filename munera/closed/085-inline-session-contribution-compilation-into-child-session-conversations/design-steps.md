- [x] Make the review/output surface explicit for this task: keep `implementation.md` and `design-steps.md` present in the task directory, or update the task artifacts to name the canonical alternatives so review-note and follow-up recording are unambiguous.
- [x] Reconcile the task's cited proof surfaces with canonical IR session shape: narrowed the proof-reference claim instead of forcing the target-authored fixture in `components/agent-session/test/psi/agent_session/workflow_source_resolution_test.clj` into IR shape. The file intentionally authors target-grammar session steps with top-level `:contributions`, while its assertions now continue to prove the compiled canonical IR surface under `:session :contributions`.
