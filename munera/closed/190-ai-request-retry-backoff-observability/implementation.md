# Implementation notes

2026-05-30 — Design ambiguity review: found two actionable ambiguities. The design moves retry execution to the prepared provider-request boundary but leaves the authoritative completed retry-history storage/EQL projection source underspecified, and it requires preserving active TUI/Emacs/app-runtime retry status without defining how lower-boundary retry sleep updates and clears the existing session retry projection.


2026-05-30 — Ambiguity follow-up complete: clarified that completed provider retry history is authoritative in session-owned provider telemetry captures, keyed by session/turn/provider-request/attempt and projected through EQL/`psi-tool` from telemetry rather than UI state. Also clarified active retry/backoff visibility: provider-boundary retry publishes pending delay metadata into the existing session retry projection before sleeping, preserves `:retrying`-style app-runtime/TUI/Emacs visibility, and clears active fields after retry resume or terminal outcome while telemetry remains the completed-history source.

2026-05-30 — Design inconsistency review: found three actionable inconsistencies. The design makes completed retry history authoritative in session-owned provider telemetry captures, but the referenced runtime separates HTTP provider request/reply captures from extension lifecycle events (`provider_request_started`, `provider_retry_scheduled`, `provider_request_finished`) and does not currently retain those lifecycle events as session captures for EQL projection. The design also changes attempt numbering to one-based `:attempt` while referenced telemetry contracts/code use `:retry-attempt` with first attempt `0`. Finally, the design's `provider_request_finished` final statuses (`:terminal`, `:exhausted`) conflict with the existing provider metrics telemetry contract that aggregates failures as `:status :failed` plus `:final?`/retry metadata.

2026-05-30 — Inconsistency follow-up complete: reconciled retry history with existing telemetry by making retained provider lifecycle events a separate session telemetry stream from HTTP provider request/reply captures, while keeping `/ext/provider-telemetry` dispatch for metrics/log consumers. Aligned attempt numbering to the existing zero-based `:retry-attempt` contract (`0` first execution, `1` first retry execution) and preserved metrics-compatible `provider_request_finished` statuses (`:succeeded`/`:failed`) with `:final?` plus explicit exhaustion metadata instead of new `:terminal`/`:exhausted` statuses.

2026-05-30 — Design ambiguity review: found one actionable ambiguity. The design alternates between configured maximum attempts / maximum attempt count and existing retry terminology, but the referenced runtime configuration is retry-count oriented (`:auto-retry-max-retries`); it does not explicitly define whether the configured limit includes the first provider execution attempt or only retry executions, creating an off-by-one risk for retry exhaustion and telemetry tests.

2026-05-30 — Ambiguity follow-up complete: clarified that `:auto-retry-max-retries` counts retry executions after the initial provider attempt, not total executions. Provider attempts remain zero-based via `:retry-attempt`: attempt `0` is the initial execution, retry attempts `1..N` are allowed for `N = :auto-retry-max-retries`, and a retryable failure on attempt `N` is final/exhausted rather than scheduling `N+1`.

2026-05-30 — Design inconsistency review: found two actionable inconsistencies. The design requires provider-boundary retry to preserve existing app-runtime/TUI/Emacs `:retrying` phase/status by publishing session retry fields, but referenced app-runtime status derives `:retrying` from the statechart configuration via `sc-phase-in`, while the existing statechart retrying path is also the old whole-agent-loop retry engine that the design says must not drive retries. The design also moves retry inside one prepared provider-request boundary and keys completed history by provider-request id plus zero-based `:retry-attempt`, but the referenced provider telemetry contract still requires `:attempt-id` as a unique provider execution-attempt id and current code sets it to the prepared request/turn id, leaving retry attempts under an in-place boundary retry with no unique `:attempt-id` contract.

2026-05-30 — Inconsistency follow-up complete: reconciled active retry visibility with statechart-derived UI projections by requiring provider-boundary retry to keep existing `ss/sc-phase-in`/app-runtime/TUI/Emacs surfaces reporting `:retrying` during pending backoff, while splitting that visibility from the old whole-agent-loop retry engine so provider retry resume must not dispatch `:runtime/agent-start-loop`. Also reconciled provider telemetry attempt identity: request id plus zero-based `:retry-attempt` is the canonical grouping coordinate, while `:attempt-id` remains a unique concrete provider-execution-attempt id derived from the request id and retry attempt for retried requests.

2026-05-30 — Design ambiguity review: found one actionable ambiguity. The design requires unknown provider/request failures to have an explicit default retry policy, but does not choose whether unknown failures are retryable, terminal/non-retryable, or conditionally retryable by subtype, leaving implementation and final-error/EQL semantics underspecified.

2026-05-30 — Ambiguity follow-up complete: defined unknown provider/request failures as terminal non-retryable by default. The retry boundary retries only failures that the shared provider-error classifier maps to an explicit retryable kind from observable stop reason/message/exception data/HTTP status. Classifier `:unknown` must not schedule retry; final error and telemetry/EQL should expose `:error-kind :unknown`, `:retryable? false`, and terminal/non-retryable failure reason so unknown-not-retried is distinguishable from retry exhaustion.

2026-05-30 — Design inconsistency review: no new actionable inconsistencies found. Re-read `design.md` against the referenced retry/telemetry/session-state artifacts, including the statechart retry path, turn-runtime provider telemetry emission, session retry schema/projection, existing provider request/reply EQL captures, provider-error classification, and task 154 provider telemetry contract; the current design reconciles the previously noted storage, attempt-numbering, status, statechart-visibility, attempt-id, limit, and unknown-failure seams.

2026-05-30 — Inconsistency follow-up execution pass: re-read `design-steps.md` after the preceding inconsistency-review pass and found no newly added unchecked inconsistency follow-up items. No `design.md` changes were required; all current design follow-up steps remain complete.

2026-05-30 — Design ambiguity review: found one actionable ambiguity. Provider-boundary retry introduces an in-request backoff delay, but the design does not define what happens if the user aborts/interrupts or the session is otherwise cancelled while that delay is pending: whether the sleep is cancellable, whether the next attempt is suppressed, how active retry fields are cleared, and what telemetry/final error is recorded.

2026-05-30 — Ambiguity follow-up complete: defined abort/interruption semantics for pending provider-boundary retry backoff. Pending retry delays must be cancellable by user/session abort, suppress the scheduled next provider attempt, clear active retry projection fields, preserve the retry schedule telemetry, and record a final failed/cancelled lifecycle outcome such as `:failure-reason :retry-cancelled` so cancellation is distinguishable from non-retryable terminal failure and retry exhaustion.

2026-05-30 — Design inconsistency review: no new actionable inconsistencies found. Re-read `design.md` against referenced retry/backoff, provider telemetry, session state/projection, provider-error classification, and task 154 metrics telemetry artifacts. The current design remains internally aligned on provider-boundary retry ownership, zero-based `:retry-attempt`, unique per-execution `:attempt-id`, metrics-compatible `:status :failed` final failures, retained lifecycle telemetry for EQL, active `:retrying` visibility without whole-agent-loop resume, retry-count limit semantics, unknown-failure terminal default, and cancellable pending backoff semantics.

2026-05-30 — Inconsistency follow-up execution pass: re-read `design-steps.md` after the preceding inconsistency-review pass and found no newly added unchecked inconsistency follow-up items. No `design.md` changes were required; all current design follow-up steps remain complete.

2026-05-30 — Design ambiguity review: found one actionable ambiguity. Streaming provider retries may occur after partial assistant text/thinking/tool-call deltas have already been emitted into the live turn/progress state, but the design does not specify whether failed-attempt partial output is discarded, hidden as transient attempt-local progress, retained for diagnostics only, or reconciled into the final transcript when a later retry succeeds.

2026-05-30 — Ambiguity follow-up complete: defined partial streaming output semantics across provider-boundary retry attempts. Failed streaming attempts keep text/thinking/tool-call deltas attempt-local: they may appear only as transient progress or clearly keyed telemetry/debug data, must be cleared/superseded before the next attempt, and must not be committed to canonical transcript/message assembly. A later successful retry owns the final assistant response and must not mix or duplicate partial output from failed attempts; failed-attempt tool calls must not execute before the normal completed-assistant-message handoff.

2026-05-30 — Design inconsistency review: no new actionable inconsistencies found. Re-read `design.md` against referenced retry/backoff, turn-runtime provider telemetry emission, session retry state/schema/projection, provider request/reply telemetry captures and EQL resolvers, provider-error classification, statechart retry guard/actions, app-runtime/RPC/Emacs retry surfaces, and task 154 provider telemetry contract. The design remains internally consistent on provider-boundary retry ownership, retained lifecycle telemetry as completed retry history, zero-based `:retry-attempt`, unique per-execution `:attempt-id`, metrics-compatible failed final statuses with exhaustion/cancellation metadata, active `:retrying` visibility without whole-agent-loop resume, retry-count limit semantics, unknown-failure terminal default, cancellable pending backoff, and attempt-local streaming partial output.

2026-05-30 — Inconsistency follow-up execution pass: re-read `design-steps.md` after the preceding inconsistency-review pass and found no newly added unchecked inconsistency follow-up items. No `design.md` changes were required; all current design follow-up steps remain complete.

2026-05-30 — Design ambiguity review: found one actionable ambiguity. The design defines `provider-request-id` as part of the authoritative retry-history identity, but the concrete provider lifecycle event payload requirements mostly say only session id and turn/request id; existing telemetry payloads use `:turn-id` and `:attempt-id`, so it is unclear whether implementation must add an explicit `:provider-request-id` field, alias it to `:turn-id`, or project provider-request identity only in EQL.

2026-05-30 — Ambiguity follow-up complete: specified concrete provider request identity for retry lifecycle telemetry and EQL. New lifecycle events must include explicit `:provider-request-id` alongside `:turn-id`; for current prepared requests it is normally equal to `:turn-id`, while EQL projects `:psi.provider-request/id` from `:provider-request-id` and only falls back to `:turn-id` for older retained telemetry. Grouping uses `:provider-request-id`, ordering uses zero-based `:retry-attempt`, and `:attempt-id` remains the unique concrete execution-attempt id.

2026-05-30 — Design inconsistency review: found one actionable inconsistency. The design requires cancellation during pending provider retry backoff to emit no `provider_request_started` for the suppressed next attempt, but also requires a final failed/cancelled lifecycle event for the prepared request; referenced provider telemetry contract says `provider_request_finished` is emitted exactly once per actual provider execution attempt, so a cancellation final event would either have no matching started attempt or duplicate the failed attempt's finish unless the design defines a separate request-level final/cancel event or explicitly updates the telemetry cardinality contract.

2026-05-30 — Inconsistency follow-up complete: reconciled pending-backoff cancellation telemetry with provider telemetry cardinality. Cancellation while a retry backoff is pending emits a distinct request-level final/cancel lifecycle event, `provider_request_cancelled`, and does not emit synthetic `provider_request_started`/`provider_request_finished` events for the suppressed next attempt or duplicate the failed attempt finish. `provider_request_finished` remains exactly once per actual provider execution attempt; EQL final-status projection derives cancelled retry outcome from the request-level cancel event plus preceding attempt/schedule events.

2026-05-30 — Design ambiguity review: found one actionable ambiguity. The design requires retry scheduling to use the configured backoff policy and preserve `:delay-source`, while referenced retry metadata already honors provider retry headers such as `Retry-After`; it does not specify whether provider-boundary retry must carry provider error headers into backoff calculation and telemetry, or whether it should ignore them and use exponential delay only.

2026-05-30 — Ambiguity follow-up complete: clarified provider retry header handling at the provider-request retry boundary. Provider-boundary retry must carry retry-relevant headers from streaming and non-streaming failure values into the shared retry metadata calculation, honor parseable `Retry-After` / `X-Retry-After` as `:delay-source :retry-after`, retain rate-limit reset/limit/remaining metadata for active retry projection, telemetry, and EQL, and fall back to configured exponential backoff with `:delay-source :exponential-backoff` when retry headers are absent or invalid without changing the original retryable classification.

2026-05-30 — Design inconsistency review: no new actionable inconsistencies found. Re-read `design.md` against referenced retry/backoff helpers, provider telemetry dispatch/retention, provider request/reply EQL captures, statechart retry guard/actions, app-runtime retry projection, retry header metadata handling, and pending-backoff cancellation telemetry. The current design remains internally consistent after the latest header-handling clarification; no new `design-steps.md` follow-up items were added.

2026-05-30 — Inconsistency follow-up execution pass: re-read `design-steps.md` after the preceding inconsistency-review pass and found no newly added unchecked inconsistency follow-up items. No `design.md` changes were required; all current design follow-up steps remain complete.

2026-05-30 — Design ambiguity review: found one actionable ambiguity. The design requires final user/developer-visible errors to distinguish terminal non-retryable, exhausted retries, and cancelled pending backoff, but it does not define the concrete caller-visible error data contract at the prepared-request boundary: which fields are added to the assistant error message and/or execution result, how retry exhaustion/cancellation metadata is preserved alongside the last provider cause, and what focused tests should assert beyond prose error text.

2026-05-30 — Ambiguity follow-up complete: defined the caller-visible final error data contract for provider-boundary retry outcomes. Prepared-request failures and assistant error messages must expose structured retry outcome metadata such as `:failure-reason`, `:retryable?`, `:error-kind`, `:http-status`, request identity, zero-based `:retry-attempt`, `:attempt-count`, `:max-retries`, last cause/message, and `:cancelled?` / `:exhausted?` where applicable, so tests can distinguish terminal non-retryable, retry-exhausted, and retry-cancelled outcomes without parsing prose.

2026-05-30 — Design inconsistency review: no new actionable inconsistencies found. Re-read `design.md` and existing `design-steps.md`, then checked referenced retry/backoff helpers, turn-runtime provider telemetry emission, statechart retry actions, app-runtime `sc-phase-in` status projection, provider request/reply EQL telemetry resolvers, retry header metadata helpers, and task 154 provider metrics telemetry contract. The current design remains internally consistent on provider-boundary retry ownership, retained lifecycle telemetry/EQL projection, explicit `:provider-request-id`, zero-based `:retry-attempt`, unique `:attempt-id`, metrics-compatible finished statuses, distinct pending-backoff cancellation event, active retry visibility without whole-agent-loop resume, retry-count limit semantics, conservative unknown-failure policy, header-derived delay handling, streaming partial-output isolation, and structured final error metadata.

2026-05-30 — Inconsistency follow-up execution pass: re-read `design-steps.md` after the preceding inconsistency-review pass and found no newly added unchecked inconsistency follow-up items. No `design.md` changes were required; all current design follow-up steps remain complete.

2026-05-30 — Design ambiguity review: found one actionable ambiguity. The design defines retry limit semantics for `:auto-retry-max-retries` and says outcomes apply "when retries were enabled", but it does not explicitly state how the existing per-session `:auto-retry-enabled` flag gates provider-boundary retry: whether disabled sessions still classify/telemeter terminal retryable failures without scheduling, which final `:failure-reason` is used, and how active retry/UI fields behave when retry is disabled.

2026-05-30 — Ambiguity follow-up complete: clarified how the existing `:auto-retry-enabled` session flag gates provider-boundary retry. Disabled retry still classifies the single provider failure and records normal attempt lifecycle telemetry, but it does not emit `provider_retry_scheduled`, does not set active retry/UI projection fields, and returns a structured skipped-retry outcome (`:failure-reason :retry-disabled`, `:retryable? true`, `:attempt-count 1`, configured `:max-retries`) for retryable causes. This is distinct from enabled retry with `:auto-retry-max-retries 0`, which is treated as retry exhaustion with zero allowed retry executions.

2026-05-30 — Design inconsistency review: found one actionable inconsistency. The caller-visible final error contract says `:failure-reason` is one of `:non-retryable`, `:retry-exhausted`, or `:retry-cancelled`, but the later `:auto-retry-enabled false` semantics introduce `:failure-reason :retry-disabled`; the design must reconcile the final error enum and outcome-specific requirements so disabled retry is either included as a provider-boundary retry outcome or represented by an existing reason.

2026-05-30 — Inconsistency follow-up complete: reconciled the caller-visible final error `:failure-reason` enum with disabled retry semantics. The final error contract now explicitly includes `:retry-disabled`, and outcome-specific requirements define it as the structured result for an otherwise retryable failure when `:auto-retry-enabled` is false, distinct from `:retry-exhausted` for enabled retry with zero allowed retry executions and from `:non-retryable` for terminal/unknown failures.

2026-05-30 — Design ambiguity review: no new actionable ambiguities found. Re-read `design.md` without reviewing plan/steps, checked existing ambiguity follow-ups in `design-steps.md`, and spot-checked referenced retry/backoff, turn-runtime, provider telemetry, provider error classification, session retry projection, streaming error accumulation, and EQL telemetry surfaces. The current design now explicitly covers provider-boundary retry ownership, classification defaults, retry limit/enabled semantics, active visibility, lifecycle telemetry/EQL identity, streaming partial output, cancellation, provider retry headers, and structured final error outcomes.

2026-05-30 — Ambiguity follow-up execution pass: re-read `design-steps.md` after the preceding ambiguity-review pass and found no newly added unchecked ambiguity follow-up items. No `design.md` changes were required; all current ambiguity design follow-up steps remain complete.

2026-05-30 — Design inconsistency review: no new actionable inconsistencies found. Re-read `design.md` against the referenced retry/backoff helpers, turn-runtime prepared-request telemetry boundary, statechart retry guard/actions, session phase/retry projection, app-runtime retry display, provider request/reply EQL telemetry captures, retry header metadata helper, provider-error classification, and existing retry-enabled/max-retries configuration. The current design remains internally consistent on provider-boundary retry ownership, lifecycle telemetry retention/EQL projection, explicit provider request identity, zero-based retry attempts, unique attempt ids, metrics-compatible attempt finish statuses, pending-backoff cancellation via distinct request-level event, active retry visibility without whole-agent-loop resume, retry-count/enabled semantics, conservative unknown-failure handling, provider header-derived delays, streaming partial-output isolation, and structured final error outcomes.

2026-05-30 — Inconsistency follow-up execution pass: re-read `design-steps.md` after the preceding inconsistency-review pass and found no newly added unchecked inconsistency follow-up items. No `design.md` changes were required; all current inconsistency design follow-up steps remain complete.

2026-05-30 — Design ambiguity review: no new actionable ambiguities found. Re-read `design.md` without reviewing plan/steps, checked existing ambiguity follow-ups in `design-steps.md`, and spot-checked referenced retry/backoff helpers, prepared-request execution, streaming/non-streaming error shaping, provider telemetry emission, session retry state/projection, app-runtime retry display, provider request/reply EQL captures, and statechart retry actions. The current design explicitly covers the previously ambiguous seams: provider-boundary retry ownership, retry classification defaults, retry-enabled and retry-limit semantics, active retry visibility, retained lifecycle telemetry/EQL identity, unique attempt identity, cancellation, provider retry headers, streaming partial output isolation, and structured final error outcomes.

2026-05-30 — Ambiguity follow-up execution pass: re-read `design-steps.md` after the preceding ambiguity-review pass and found no newly added unchecked ambiguity follow-up items. No `design.md` changes were required; all current ambiguity design follow-up steps remain complete.

2026-05-30 — Design inconsistency review: no new actionable inconsistencies found. Re-read `design.md` against referenced retry/backoff helpers, prepared-request provider telemetry emission, statechart retry guard/actions, app-runtime `sc-phase-in` retry visibility, provider request/reply telemetry/EQL surfaces, retry header metadata, and retry-enabled/max-retries semantics; existing design remains internally aligned, so no new `design-steps.md` items were added.

2026-05-30 — Inconsistency follow-up execution pass: re-read `design-steps.md` after the preceding inconsistency-review pass and found no newly added unchecked inconsistency follow-up items. No `design.md` changes were required; all current inconsistency design follow-up steps remain complete.

2026-05-30 — Design ambiguity review: no new actionable ambiguities found. Re-read `design.md` without reviewing task `plan.md` or `steps.md`, checked existing ambiguity follow-ups in `design-steps.md` only to avoid duplicates, and spot-checked referenced retry/backoff helpers, prepared-request execution, statechart retry actions, retry metadata/header handling, provider telemetry emission, session retry projection, and EQL session resolver surfaces. The design already specifies the previously ambiguous retry ownership, classification/defaults, retry-enabled/limit behavior, active visibility, lifecycle telemetry/EQL identity, cancellation, provider headers, streaming partial-output isolation, and structured final error outcomes.

2026-05-30 — Ambiguity follow-up execution pass: re-read `design-steps.md` after the preceding ambiguity-review pass and found no newly added unchecked ambiguity follow-up items. No `design.md` changes were required; all current ambiguity design follow-up steps remain complete.

2026-05-30 — Design inconsistency review: no new actionable inconsistencies found. Re-read `design.md` without reviewing task `plan.md` or `steps.md`, checked existing `design-steps.md` only for duplicate avoidance, and spot-checked referenced retry/backoff helpers, prepared-request provider telemetry emission, statechart retry guard/actions, session phase/retry projection, app-runtime retry display, provider request/reply telemetry/EQL surfaces, retry header metadata, and retry-enabled/max-retries semantics. The design remains internally consistent on provider-boundary retry ownership, lifecycle telemetry retention/EQL identity, explicit `:provider-request-id`, zero-based `:retry-attempt`, unique `:attempt-id`, metrics-compatible attempt finish statuses, distinct pending-backoff cancellation, active retry visibility without whole-agent-loop resume, retry-count/enabled semantics, conservative unknown-failure policy, provider header-derived delays, streaming partial-output isolation, and structured final error outcomes.

2026-05-30 — Inconsistency follow-up execution pass: re-read `design-steps.md` after the preceding inconsistency-review pass and found no newly added unchecked inconsistency follow-up items. No `design.md` changes were required; all current inconsistency design follow-up steps remain complete.

2026-05-30 — Plan/steps ambiguity review: no new actionable ambiguities found. Re-read `plan.md`, `steps.md`, `design.md`, existing `design-steps.md`, and prior implementation review notes; spot-checked referenced retry/backoff, prepared-request execution, provider telemetry emission, session phase/retry projection, app-runtime retry display, and EQL telemetry resolver surfaces. The current plan and steps give an actionable slice order with explicit retry ownership, lifecycle telemetry, active visibility, cancellation, header handling, streaming isolation, EQL exposure, and verification coverage; no new duplicate-free `design-steps.md` follow-up items were needed.

2026-05-30 — Ambiguity follow-up execution pass: re-read `design-steps.md` after the preceding plan/steps ambiguity-review pass and found no newly added unchecked ambiguity follow-up items. All current design follow-up steps were already complete, so no `design.md`, `plan.md`, or `steps.md` changes were required.

2026-05-30 — Plan/steps inconsistency review: no new actionable inconsistencies found. Re-read `plan.md`, `steps.md`, `design.md`, `design-steps.md`, and recent implementation notes, then spot-checked referenced retry placement and telemetry/status code (`execute-prepared-request!`, statechart retry actions, and `sc-phase-in`). The plan/steps remain aligned with the design on provider-boundary retry ownership, zero-based `:retry-attempt`, unique per-attempt identity, retained lifecycle telemetry/EQL projection, active retry visibility without whole-agent-loop replay, cancellation/header/streaming behavior, retry-enabled/max-retries semantics, and structured final error outcomes. No duplicate-free `design-steps.md` follow-up item was needed.

2026-05-30 — Inconsistency follow-up execution pass: re-read `design-steps.md` after the preceding plan/steps inconsistency-review pass (`0ffb795e`) and found no newly added unchecked inconsistency follow-up items. No `design.md`, `plan.md`, or `steps.md` changes were required; all current design follow-up steps remain complete.

2026-05-30 — Plan/steps ambiguity review: no new actionable ambiguities found. Re-read `plan.md`, `steps.md`, `design.md`, `design-steps.md`, and prior implementation notes, then spot-checked referenced retry/backoff seams in `execute-prepared-request!`, statechart retry actions, `sc-phase-in`, app-runtime retry projection, and session retry EQL config resolvers. The plan/steps remain actionable and unambiguous on slice order, provider-boundary retry ownership, telemetry/active visibility, cancellation/header/streaming behavior, EQL exposure, coverage, and cleanup; no duplicate-free unchecked `design-steps.md` follow-up item was needed.

2026-05-30 — Ambiguity follow-up execution pass: re-read `design-steps.md` after the preceding ambiguity-review pass and found no newly added unchecked ambiguity follow-up items. No `design.md`, `plan.md`, or `steps.md` changes were required; all current design follow-up steps remain complete.

2026-05-30 — Plan/steps inconsistency review: no new actionable inconsistencies found. Re-read `plan.md`, `steps.md`, `design.md`, existing `design-steps.md`, and prior implementation notes, then spot-checked referenced retry/backoff seams in `turn-runtime/execute-prepared-request!`, statechart retry actions, `sc-phase-in`, provider lifecycle telemetry emission, session retry fields, and telemetry EQL resolver surfaces. The plan/steps remain aligned with the design on provider-boundary retry ownership, retained lifecycle telemetry/EQL projection, active retry visibility without whole-agent-loop replay, cancellation/header/streaming behavior, retry-enabled/max-retries semantics, and structured final error outcomes. No duplicate-free unchecked `design-steps.md` follow-up item was needed.

2026-05-30 — Inconsistency follow-up execution pass: re-read `design-steps.md` after the preceding plan/steps inconsistency-review pass (`bf3c0234`) and found no newly added unchecked inconsistency follow-up items. No `design.md`, `plan.md`, or `steps.md` changes were required; all current design follow-up steps remain complete.

2026-05-30 — Plan/steps ambiguity review: no new actionable ambiguities found. Re-read `plan.md`, `steps.md`, `design.md`, existing `design-steps.md`, and prior implementation notes; spot-checked referenced retry/backoff seams in `turn-runtime/execute-prepared-request!`, provider-error/retry metadata config, statechart retry actions, `sc-phase-in`, app-runtime retry status projection, and session EQL retry/config resolver surfaces. The plan/steps remain actionable and unambiguous on provider-boundary retry ownership, slice ordering, telemetry/active visibility, cancellation/header/streaming behavior, EQL exposure, verification, and cleanup. No duplicate-free unchecked `design-steps.md` follow-up item was needed.

2026-05-30 — Ambiguity follow-up execution pass: re-read `design-steps.md` after the preceding ambiguity-review pass and found no newly added unchecked ambiguity follow-up items. No `design.md`, `plan.md`, or `steps.md` changes were required; all current design follow-up steps remain complete.

2026-05-30 — Plan/steps inconsistency review: no new actionable inconsistencies found. Re-read `plan.md`, `steps.md`, `design.md`, `design-steps.md`, and prior implementation notes, then spot-checked referenced retry/backoff seams in `turn-runtime/execute-prepared-request!`, statechart retry guard/actions, `sc-phase-in`, provider lifecycle telemetry emission, session retry fields, prompt lifecycle retry proof, and telemetry/EQL resolver surfaces. The plan and steps remain aligned with the design on provider-boundary retry ownership, retained lifecycle telemetry/EQL projection, explicit `:provider-request-id`, zero-based `:retry-attempt`, unique `:attempt-id`, active retry visibility without whole-agent-loop replay, cancellation/header/streaming behavior, retry-enabled/max-retries semantics, and structured final error outcomes. No duplicate-free unchecked `design-steps.md` follow-up item was needed.

2026-05-30 — Inconsistency follow-up execution pass: re-read `design-steps.md` after the preceding plan/steps inconsistency-review pass (`613a5b8e`) and found no newly added unchecked inconsistency follow-up items. No `design.md`, `plan.md`, or `steps.md` changes were required; all current design follow-up steps remain complete.

2026-05-30 — Plan/steps ambiguity review: no new actionable ambiguities found. Re-read `plan.md`, `steps.md`, `design.md`, existing `design-steps.md`, and prior implementation notes, then spot-checked referenced retry/backoff seams in `turn-runtime/execute-prepared-request!`, provider-error/retry config helpers, statechart retry guard/actions, `sc-phase-in`, app-runtime retry status projection, and existing provider telemetry emission. The current plan/steps remain unambiguous and actionable on provider-boundary retry ownership, slice order, telemetry/active visibility, cancellation/header/streaming behavior, EQL exposure, retry-enabled/max-retries semantics, final structured errors, verification, and cleanup. No duplicate-free unchecked `design-steps.md` follow-up item was needed.

2026-05-30 — Ambiguity follow-up execution pass: re-read `design-steps.md` after the preceding plan/steps ambiguity-review pass (`c38bccce`) and found no newly added unchecked ambiguity follow-up items. No `design.md`, `plan.md`, or `steps.md` changes were required; all current design follow-up steps remain complete.

2026-05-30 — Plan/steps inconsistency review: no new actionable inconsistencies found. Re-read `plan.md`, `steps.md`, `design.md`, existing `design-steps.md`, and prior implementation notes, then spot-checked referenced retry/backoff seams in `turn-runtime/execute-prepared-request!`, statechart retry guard/actions, `sc-phase-in`, active session retry projection, provider lifecycle telemetry emission, retry-enabled/max-retries config, and prompt lifecycle retry proof. The plan and steps remain aligned with the design on provider-boundary retry ownership, retained lifecycle telemetry/EQL projection, explicit `:provider-request-id`, zero-based `:retry-attempt`, unique `:attempt-id`, active retry visibility without whole-agent-loop replay, pending-backoff cancellation, provider retry headers, streaming partial-output isolation, retry-enabled/max-retries semantics, and structured final error outcomes. No duplicate-free unchecked `design-steps.md` follow-up item was needed.

2026-05-30 — Inconsistency follow-up execution pass: re-read `design-steps.md` after the preceding plan/steps inconsistency-review pass (`61119cf1`) and found no newly added unchecked inconsistency follow-up items. No `design.md`, `plan.md`, or `steps.md` changes were required; all current design follow-up steps remain complete.

2026-05-30 — Plan/steps ambiguity review: no new actionable ambiguities found. Re-read `plan.md`, `steps.md`, `implementation.md`, `design.md`, and `design-steps.md`, then spot-checked referenced code around `turn-runtime/execute-prepared-request!`, provider lifecycle telemetry emission, statechart retry actions, `sc-phase-in`, retry-enabled/max-retries config, provider-error header propagation, and active session retry projection. The plan/steps remain unambiguous and actionable on provider-boundary retry ownership, slice order, telemetry and active retry visibility, cancellation, provider retry headers, streaming isolation, EQL/`psi-tool` projection, structured final error outcomes, and verification. No duplicate-free unchecked `design-steps.md` follow-up item was needed.

2026-05-30 — Ambiguity follow-up execution pass: re-read `design-steps.md` after the preceding plan/steps ambiguity-review pass (`ff794d7d`) and found no newly added unchecked ambiguity follow-up items. No `design.md`, `plan.md`, or `steps.md` changes were required; all current design follow-up steps remain complete.

2026-05-30 — Plan/steps inconsistency review: no new actionable inconsistencies found. Re-read `plan.md`, `steps.md`, `implementation.md`, `design.md`, and `design-steps.md`, then spot-checked referenced code around prepared-request execution, provider lifecycle telemetry, statechart retry actions, `sc-phase-in`, retry-enabled/max-retries config, provider retry header metadata, active retry projection, and session EQL config/retry resolver surfaces. The plan and steps remain aligned with the design on provider-boundary retry ownership, slice order, retained lifecycle telemetry/EQL projection, active retry visibility without whole-agent-loop replay, cancellation/header/streaming behavior, retry-enabled/max-retries semantics, structured final error outcomes, verification, and cleanup. No duplicate-free unchecked `design-steps.md` follow-up item was needed.

2026-05-30 — Inconsistency follow-up execution pass: re-read `design-steps.md` after the preceding plan/steps inconsistency-review pass (`20417cb5`) and found no newly added unchecked inconsistency follow-up items. No `design.md`, `plan.md`, or `steps.md` changes were required; all current design follow-up steps remain complete.

2026-05-30 — Plan/steps ambiguity review: no new actionable ambiguities found. Re-read `plan.md`, `steps.md`, `implementation.md`, `design.md`, and `design-steps.md`, then spot-checked referenced code around `turn-runtime/execute-prepared-request!`, provider telemetry start/finish emission, existing statechart retry scheduling/resume actions, `ss/sc-phase-in` retry status derivation, retry-enabled/max-retries config, retry header metadata tests, and telemetry/EQL resolver surfaces. The plan/steps remain unambiguous and actionable on provider-boundary retry ownership, slice order, retained lifecycle telemetry/EQL projection, active retry visibility, cancellation/header/streaming behavior, retry-enabled/max-retries semantics, structured final error outcomes, and cleanup. No duplicate-free unchecked `design-steps.md` follow-up item was needed.

2026-05-30 — Ambiguity follow-up execution pass: re-read `design-steps.md` after the preceding plan/steps ambiguity-review pass (`d4cadf66`) and found no newly added unchecked ambiguity follow-up items. No `design.md`, `plan.md`, or `steps.md` changes were required; all current design follow-up steps remain complete.

2026-05-30 — Plan/steps inconsistency review: no new actionable inconsistencies found. Re-read `plan.md`, `steps.md`, `implementation.md`, `design.md`, and `design-steps.md`, then spot-checked referenced code around `turn-runtime/execute-prepared-request!`, statechart retry scheduling/resume, `ss/sc-phase-in`, provider lifecycle telemetry emission, retry-enabled/max-retries config, provider retry header metadata, active retry projection, and session EQL resolver surfaces. The plan and steps remain aligned with the design on provider-boundary retry ownership, slice order, retained lifecycle telemetry/EQL projection, active retry visibility without whole-agent-loop replay, cancellation/header/streaming behavior, retry-enabled/max-retries semantics, structured final error outcomes, verification, and cleanup. No duplicate-free unchecked `design-steps.md` follow-up item was needed.

2026-05-30 — Inconsistency follow-up execution pass: re-read `design-steps.md` after the preceding plan/steps inconsistency-review pass (`66651554`) and found no newly added unchecked inconsistency follow-up items. No `design.md`, `plan.md`, or `steps.md` changes were required; all current design follow-up steps remain complete.

2026-05-30 — Implementation pass: completed Slice 1 audit and a first provider-boundary retry slice. Re-ran the original prompt lifecycle proof before editing and reproduced the expected failure: retryable connection error left phase `:idle`, emitted no `provider_retry_scheduled`, and executed the provider seam once. Root cause category is statechart bypass / wrong retry placement: canonical prompt lifecycle converted the provider failure to an execution result and finished/reset without entering the old `:session/agent-event` retry guard.

Implemented initial retry ownership in `psi.turn-runtime.core/execute-prepared-request!`: each provider attempt runs through the prepared-request seam, retryable classified errors schedule bounded retries using existing retry metadata/backoff helpers, retries re-enter the provider execution seam with fresh per-attempt runtime state, and final failures carry structured `:execution-result/retry-outcome` plus assistant-message `:retry/outcome`. New lifecycle telemetry now includes explicit `:provider-request-id`, zero-based `:retry-attempt`, unique `:attempt-id`, failed/succeeded `provider_request_finished`, and `provider_retry_scheduled` payloads. Lifecycle events are retained under session telemetry `:provider-events` while still dispatching `/ext/provider-telemetry`.

Added active retry projection support by making `ss/sc-phase-in` report `:retrying` when session `:retry` is active, and provider-boundary retry writes pending retry metadata before the delay. Tests use `:provider-retry-sleep? false` to avoid real sleeps and keep pending retry status observable.

Added initial EQL provider retry projection over retained lifecycle telemetry: session retry count, retried request count, grouped request summaries, final status, and scheduled retry details. Focused coverage now proves retryable connection failure followed by success retries the prepared request and returns success, provider lifecycle telemetry compatibility, and retained lifecycle retry EQL projection. Remaining work: complete disabled/zero-max/exhaustion/non-retryable/unknown coverage, cancellation, header-specific assertions, streaming partial-output isolation, and cleanup/quarantine of obsolete whole-agent-loop retry behavior.

Verification:
- `clojure -M:test --focus psi.agent-session.prompt-lifecycle-test/prompt-execution-result-retryable-error-enters-retrying-and-schedules-retry-test` reproduced the pre-edit failing proof.
- `clojure -M:test --focus psi.agent-session.eql-introspection-test/provider-capture-eql-introspection-test --focus psi.turn-runtime.response-mode-test --focus psi.agent-session.prompt-lifecycle-test/prompt-execution-result-retryable-error-enters-retrying-and-schedules-retry-test` passed after edits.
- `clojure -M:test --focus psi.agent-session.statechart-actions-test --focus psi.session-state.model-test --focus psi.agent-session.retry-headers-test` passed after edits.
- `clojure -M:test --focus psi.agent-session.prompt-lifecycle-test --focus psi.turn-runtime.response-mode-test --focus psi.agent-session.eql-introspection-test/provider-capture-eql-introspection-test` passed (`29 tests, 161 assertions`).
- `clj-kondo --lint components/turn-runtime/src components/turn-runtime/test components/agent-session/src components/agent-session/test components/session-state/src components/session-state/test` passed with no errors/warnings; existing info-level findings remain in unrelated tests.
- Combined focused verification passed: `clojure -M:test --focus psi.agent-session.prompt-lifecycle-test/prompt-execution-result-retryable-error-enters-retrying-and-schedules-retry-test --focus psi.agent-session.prompt-lifecycle-test --focus psi.turn-runtime.response-mode-test --focus psi.agent-session.eql-introspection-test/provider-capture-eql-introspection-test --focus psi.agent-session.statechart-actions-test --focus psi.session-state.model-test --focus psi.agent-session.retry-headers-test` (`50 tests, 287 assertions`).

2026-05-30 — Implementation pass: completed the remaining non-cancellation Slice 2 retry outcome semantics and a small active-retry cleanup slice. Provider-boundary retry now clears both `:retry` and `:retry-attempt` after retry delay completion, first-attempt success, terminal/non-retryable failure, retry-disabled failure, and retry exhaustion. Added an injectable `:provider-retry-sleep-fn` test seam so focused tests can observe pending active retry state during backoff without real sleeps, while production still uses bounded `Thread/sleep` when no test seam is provided.

Added focused provider-boundary tests for terminal provider/client no-retry (`:failure-reason :non-retryable`), conservative unknown no-retry (`:error-kind :unknown`, `:retryable? false`), retry-disabled skipped scheduling (`:failure-reason :retry-disabled`, no `provider_retry_scheduled`), enabled zero-max retry exhaustion, repeated retryable exhaustion preserving the last cause and zero-based attempt coordinates, and clearing active retry fields before retry attempt execution and after terminal outcome. Cancellation outcome support remains pending for the dedicated cancellation slice.

Verification:
- `clojure -M:test --focus psi.turn-runtime.response-mode-test/execute-prepared-request-terminal-provider-error-is-not-retried-test --focus psi.turn-runtime.response-mode-test/execute-prepared-request-unknown-provider-error-is-not-retried-test --focus psi.turn-runtime.response-mode-test/execute-prepared-request-retry-disabled-classifies-without-scheduling-test --focus psi.turn-runtime.response-mode-test/execute-prepared-request-zero-max-retries-exhausts-without-scheduling-test --focus psi.turn-runtime.response-mode-test/execute-prepared-request-retry-exhaustion-preserves-last-cause-test --focus psi.turn-runtime.response-mode-test/execute-prepared-request-clears-active-retry-state-before-retry-attempt-test` passed (`6 tests, 46 assertions`).
- `clojure -M:test --focus psi.turn-runtime.response-mode-test` passed (`11 tests, 70 assertions`).

2026-05-30 — Implementation pass: added focused coverage for active retry visibility and retry header delay metadata. The existing provider-boundary active retry test now proves `ss/sc-phase-in` reports `:retrying` while the injected backoff sleep hook observes pending retry state, and still proves active retry fields are cleared before the retry attempt executes and after success. Added provider-boundary tests proving parseable `Retry-After` drives `:delay-ms`, `:delay-source :retry-after`, `:resume-at`, and retained rate-limit metadata on `provider_retry_scheduled`; invalid `Retry-After` preserves retryability and falls back to configured exponential backoff with `:delay-source :exponential-backoff`.

Verification:
- `clojure -M:test --focus psi.turn-runtime.response-mode-test/execute-prepared-request-clears-active-retry-state-before-retry-attempt-test --focus psi.turn-runtime.response-mode-test/execute-prepared-request-retry-after-header-drives-delay-test --focus psi.turn-runtime.response-mode-test/execute-prepared-request-invalid-retry-after-falls-back-test` passed (`3 tests, 18 assertions`).
- `clojure -M:test --focus psi.turn-runtime.response-mode-test` passed (`13 tests, 82 assertions`).
- `clj-kondo --lint components/turn-runtime/test/psi/turn_runtime/response_mode_test.clj` passed with no errors or warnings.

2026-05-30 — Implementation pass: added focused prompt-lifecycle coverage for the provider-boundary retry resume contract. The canonical retryable prompt lifecycle test now clears and inspects the dispatch event log to prove provider-boundary retry does not dispatch the old whole-agent-loop `:runtime/agent-start-loop` effect. Added a tool-result-post continuation test where the first provider request asks for a tool, the tool result is recorded once, the continuation provider request fails retryably once, and provider-boundary retry succeeds without rerunning the local tool. This covers the design rule that retrying a provider request containing already-recorded tool results must not replay tool side effects.

Verification:
- `clojure -M:test --focus psi.agent-session.prompt-lifecycle-test/prompt-execution-result-retryable-error-enters-retrying-and-schedules-retry-test --focus psi.agent-session.prompt-lifecycle-test/prompt-provider-retry-after-tool-result-does-not-rerun-tool-test` passed (`2 tests, 9 assertions`).

2026-05-30 — Implementation pass: completed the pending-backoff cancellation slice and added focused streaming partial-output isolation coverage. Provider-boundary retry now accepts an injected `:provider-retry-cancelled?` predicate checked before and after the retry delay; cancellation clears active retry state, suppresses the scheduled next provider execution attempt, emits a request-level `provider_request_cancelled` lifecycle event with `:failure-reason :retry-cancelled`, and returns structured retry-cancelled outcome metadata without emitting synthetic start/finish events for the suppressed retry attempt. Added a streaming retry proof using the real streaming accumulator path: the first attempt emits partial text then fails retryably, the retry succeeds, and the final assistant message contains only the successful retry content.

Verification:
- `clojure -M:test --focus psi.turn-runtime.response-mode-test/execute-prepared-request-cancels-pending-retry-backoff-test --focus psi.turn-runtime.response-mode-test/execute-prepared-request-streaming-retry-discards-failed-partial-output-test` passed (`2 tests, 18 assertions`).
- `clojure -M:test --focus psi.turn-runtime.response-mode-test` passed (`15 tests, 100 assertions`).
- `clj-kondo --lint components/turn-runtime/src components/turn-runtime/test components/agent-session/src/psi/agent_session/resolvers/provider_retries.clj` passed with no errors or warnings.

2026-05-30 — Implementation pass: completed the remaining Slice 4/6 proof gaps and documented the user-visible retry/EQL changes. Confirmed streaming and non-streaming failure normalization is already in place: streaming `:error` events carry provider headers through `make-provider-event-consumer` → turn error data → accumulator final message `:provider-error/headers`, and non-streaming error results map `:headers` to the same field before retry metadata calculation. Strengthened first-attempt success coverage so a successful provider request executes once, returns the normal assistant content with no retry outcome, emits exactly one start/finish lifecycle pair, schedules no retry, and leaves active retry state clear.

Extended provider retry EQL projection to include retained rate-limit metadata on `:psi.provider-retry/rate-limit`, satisfying the design requirement that header-derived rate-limit details are visible through `psi-tool` / EQL rather than only telemetry. Added a changelog entry for user-visible provider retry reliability and retry-history introspection.

Verification:
- `clojure -M:test --focus psi.turn-runtime.response-mode-test/execute-prepared-request-dispatches-provider-telemetry-test --focus psi.agent-session.eql-provider-retry-test/provider-retry-eql-introspection-test` passed (`2 tests, 15 assertions`).

2026-05-30 — Follow-up verification pass: broad `bb test` initially surfaced four retry-related expectation mismatches caused by the now-intentional active retry phase projection: app-runtime/RPC retry status tests still expected `phase:idle` while `ss/sc-phase-in` now correctly reports `phase:retrying` whenever active provider-boundary backoff state is present. Updated those tests to align with the design's existing TUI/Emacs/app-runtime retry visibility requirement. The two turn-runtime failures from that broad run did not reproduce under focused execution after the status-test fixes, and focused retry suites are green.

Verification:
- `clojure -M:test --focus psi.app-runtime.session-summary-test/session-summary-builds-retry-status-fragments-test --focus psi.rpc-events-test/session-updated-payload-includes-retry-contract-test` passed (`2 tests, 14 assertions`).
- `clojure -M:test --focus psi.turn-runtime.response-mode-test --focus psi.agent-session.eql-provider-retry-test --focus psi.agent-session.prompt-lifecycle-test --focus psi.agent-session.statechart-actions-test --focus psi.session-state.model-test --focus psi.agent-session.retry-headers-test --focus psi.app-runtime.session-summary-test --focus psi.rpc-events-test/session-updated-payload-includes-retry-contract-test` passed (`65 tests, 375 assertions`).
- `clj-kondo --lint components/turn-runtime/src components/turn-runtime/test components/agent-session/src components/agent-session/test components/session-state/src components/session-state/test components/app-runtime/test components/rpc/test` passed with no errors or warnings; existing info-level findings remain in unrelated tests.
- `bb test` was attempted and exposed the stale active-retry phase expectations above; not rerun yet after the focused fixes in this pass.

2026-05-30 — Implementation pass: completed Slice 7 cleanup/quarantine of obsolete whole-agent-loop retry behavior. The statechart retry guard now deliberately returns false, documenting that provider request retry is owned by `turn-runtime` at the prepared provider-request boundary. The compatibility `:retrying` state remains only as a visibility/protocol state, and `:session/retry-done` no longer invokes `:on-retry-resume` from the chart. Legacy `:on-retry-triggered` and `:on-retry-resume` handlers are registered as compatibility no-ops that clear stale active retry metadata without scheduling sleeps, emitting duplicate provider retry telemetry, incrementing session `:retry-attempt`, or dispatching `:runtime/agent-start-loop`. Removed the duplicated statechart retry metadata calculation helper so the provider-boundary coordinator is the authoritative retry policy implementation.

Updated statechart action tests to prove classified transport failures do not activate the old retry guard, stale retry-triggered events do not schedule backoff or telemetry, and retry resume does not replay the old whole-agent loop. Existing prompt lifecycle/provider-boundary retry tests continue to prove canonical retry succeeds and tool-result-post retry does not rerun local tools.

Verification:
- `clj-paren-repair components/agent-session/src/psi/agent_session/statechart.clj components/agent-session/src/psi/agent_session/dispatch_handlers/statechart_actions.clj components/agent-session/test/psi/agent_session/statechart_actions_test.clj` passed with no changes needed.
- `clojure -M:test --focus psi.agent-session.statechart-actions-test` passed (`8 tests, 39 assertions`).
- `clj-kondo --lint components/agent-session/src/psi/agent_session/statechart.clj components/agent-session/src/psi/agent_session/dispatch_handlers/statechart_actions.clj components/agent-session/test/psi/agent_session/statechart_actions_test.clj` passed with no errors or warnings.
- `clojure -M:test --focus psi.agent-session.statechart-actions-test --focus psi.agent-session.prompt-lifecycle-test/prompt-execution-result-retryable-error-enters-retrying-and-schedules-retry-test --focus psi.agent-session.prompt-lifecycle-test/prompt-provider-retry-after-tool-result-does-not-rerun-tool-test --focus psi.turn-runtime.response-mode-test` passed (`25 tests, 154 assertions`).
- Focused retry/status suite passed: `clojure -M:test --focus psi.agent-session.statechart-actions-test --focus psi.turn-runtime.response-mode-test --focus psi.agent-session.eql-provider-retry-test --focus psi.agent-session.prompt-lifecycle-test --focus psi.agent-session.statechart-actions-test --focus psi.session-state.model-test --focus psi.agent-session.retry-headers-test --focus psi.app-runtime.session-summary-test --focus psi.rpc-events-test/session-updated-payload-includes-retry-contract-test` (`65 tests, 371 assertions`).
- `bb test` passed.

2026-05-30 — Final implementation pass: re-read task design, plan, steps, and implementation notes; all implementation checklist items are complete. Ran broad verification again after the legacy retry quarantine and active retry status updates; `bb test` passed. No further concrete implementation work remains for task 190, so the task was moved from `munera/open/` to `munera/closed/` and removed from the open-task backlog in `munera/plan.md`.

2026-05-30 — Implementation review: found two actionable gaps. Pending-backoff cancellation is only proven through injected test seams (`:provider-retry-cancelled?` / `:provider-retry-sleep-fn`); production retry sleep still uses a single `Thread/sleep` and is not wired to the real session abort/shutdown signal, so user abort during backoff may remain invisible until the delay completes. Retry EQL is also session-summary-only: it exposes nested provider-request summaries from a session query, but does not provide direct turn/request-level resolver entry points for callers that know a `turn-id` or `provider-request-id`.

2026-05-30 — Implementation review follow-up pass: completed both review-added follow-up items. Provider-boundary retry backoff is now wired to the real session/turn abort path: `abort-active-turn-in!` marks the session with `:provider-retry-abort-requested?`, production retry sleep polls cancellation in small bounded intervals (`:provider-retry-sleep-poll-ms`, default 250ms) instead of one uninterruptible `Thread/sleep`, and cancellation suppresses the next provider attempt through the existing `provider_request_cancelled` outcome path. The marker is cleared when a new prepared request begins and whenever active retry state is cleared.

Added direct provider retry EQL resolvers for explicit `:psi.provider-request/id` and `:psi.provider-request/turn-id` entity seeds, so `psi-tool` callers that already know a request id or turn id can query retry count, final status, classification, and retry-attempt details without walking the nested session retry summary.

Verification:
- `clojure -M:test --focus psi.turn-runtime.response-mode-test/execute-prepared-request-cancels-pending-retry-backoff-test --focus psi.turn-runtime.response-mode-test/execute-prepared-request-production-backoff-observes-active-turn-abort-test --focus psi.agent-session.eql-provider-retry-test` passed (`4 tests, 27 assertions`).
- `clojure -M:test --focus psi.turn-runtime.response-mode-test --focus psi.agent-session.eql-provider-retry-test` passed (`18 tests, 120 assertions`).
- `clj-kondo --lint components/turn-runtime/src/psi/turn_runtime/core.clj components/turn-runtime/test/psi/turn_runtime/response_mode_test.clj components/agent-session/src/psi/agent_session/context.clj components/agent-session/src/psi/agent_session/resolvers/provider_retries.clj components/agent-session/test/psi/agent_session/eql_provider_retry_test.clj` passed with no errors or warnings.

2026-05-30 — Implementation review: found one new actionable gap after re-reading the review follow-up diff and focused retry/EQL tests. Direct provider retry EQL now declares `:psi.provider-retry/final?`, but the resolver derives retry-attempt rows only from `provider_retry_scheduled` events and marks final by comparing the schedule event map to the final lifecycle event map; because final lifecycle records are separate `provider_request_finished` / `provider_request_cancelled` events, scheduled retry rows appear unable to report a true final marker. Add focused coverage and align final-marker semantics, especially for pending-backoff cancellation / suppressed retry attempts. Verification run during review: `clojure -M:test --focus psi.turn-runtime.response-mode-test --focus psi.agent-session.eql-provider-retry-test` passed (`18 tests, 120 assertions`).

2026-05-30 — Implementation review follow-up pass: completed the remaining review-added EQL final-marker item. Provider retry attempt detail now marks `:psi.provider-retry/final?` by matching the scheduled retry attempt coordinate against the final lifecycle event's `:retry-attempt`, rather than comparing the distinct schedule and final event maps. This lets final `provider_request_finished` events mark exhausted/succeeded retry details and final `provider_request_cancelled` events mark the suppressed scheduled retry attempt as final. Added focused EQL coverage for successful retried requests, exhausted direct request queries, and pending-backoff cancellation via `provider_request_cancelled`.

Verification:
- `clojure -M:test --focus psi.agent-session.eql-provider-retry-test` passed (`3 tests, 12 assertions`).
- `clojure -M:test --focus psi.turn-runtime.response-mode-test --focus psi.agent-session.eql-provider-retry-test` passed (`19 tests, 122 assertions`).
- `clj-kondo --lint components/agent-session/src/psi/agent_session/resolvers/provider_retries.clj components/agent-session/test/psi/agent_session/eql_provider_retry_test.clj` passed with no errors or warnings.

2026-05-30 — Implementation review: found one new actionable gap. Provider-boundary retry now emits final `provider_request_finished` telemetry for terminal provider failures, but canonical prompt finish still routes the assistant error through `:on-agent-done`, whose compatibility handler emits another `provider_request_finished` event to `/ext/provider-telemetry` for terminal provider errors. That appears to duplicate metrics/log extension telemetry on the canonical terminal-error path even though retained session lifecycle history has only the provider-boundary event. Add focused prompt-lifecycle/extension telemetry coverage and remove or gate the compatibility emission so terminal provider attempts produce exactly one final lifecycle event for current prepared-request execution.

2026-05-30 — Implementation review follow-up pass: completed the terminal telemetry duplication follow-up. Added canonical prompt-lifecycle coverage proving a terminal provider failure produces exactly one `provider_request_finished` event through `/ext/provider-telemetry` while preserving the provider-boundary structured `:failure-reason :non-retryable`. Gated the legacy `:on-agent-done` compatibility provider-failure telemetry emission so it does not fire when the assistant error already carries provider-boundary retry outcome metadata; the compatibility path still emits terminal provider telemetry for older/historical statechart agent-done events without `:retry/outcome`.

Verification:
- `clj-paren-repair components/agent-session/src/psi/agent_session/dispatch_handlers/statechart_actions.clj components/agent-session/test/psi/agent_session/prompt_lifecycle_test.clj` passed with no changes needed.
- `clojure -M:test --focus psi.agent-session.prompt-lifecycle-test/prompt-lifecycle-terminal-provider-error-emits-one-finished-telemetry-event-test --focus psi.agent-session.statechart-actions-test/on-agent-done-emits-terminal-provider-failure-telemetry-test` passed (`2 tests, 11 assertions`).
- `clojure -M:test --focus psi.agent-session.prompt-lifecycle-test --focus psi.agent-session.statechart-actions-test --focus psi.turn-runtime.response-mode-test --focus psi.agent-session.eql-provider-retry-test` passed (`52 tests, 285 assertions`).
- `clj-kondo --lint components/agent-session/src/psi/agent_session/dispatch_handlers/statechart_actions.clj components/agent-session/test/psi/agent_session/prompt_lifecycle_test.clj` passed with no errors or warnings.

2026-05-30 — Implementation review: found one new actionable gap. Streaming provider retry header preservation is proven only for provider-emitted `:error` events with `:headers`; the generic streaming exception path (`psi.ai.streaming/exception->error-event`) drops header data from `ex-data`, so a thrown rate-limit/overload exception carrying `Retry-After` would be classified/retried without honoring provider header delay metadata. Add focused coverage for thrown streaming provider exceptions with retry headers and preserve headers into the retry boundary.

2026-05-30 — Implementation review follow-up: completed streaming exception retry-header coverage. Added a focused provider-boundary streaming test where `do-stream!` throws an `ex-info` with `Retry-After` and rate-limit headers in `ex-data`; the generic streaming exception path now converts thrown provider failures into the same turn error shape used by emitted `:error` events, preserving headers under the accumulator's `:provider-error/headers` path. Provider-boundary retry now honors `Retry-After` / rate-limit metadata for thrown streaming failures and retries successfully without falling back to exponential delay.

Verification:
- `clojure -M:test --focus psi.turn-runtime.response-mode-test/execute-prepared-request-streaming-exception-preserves-retry-headers-test` passed (`1 tests, 7 assertions`).
- `clojure -M:test --focus psi.turn-runtime.response-mode-test/execute-prepared-request-streaming-exception-preserves-retry-headers-test --focus psi.turn-runtime.response-mode-test/execute-prepared-request-retry-after-header-drives-delay-test --focus psi.turn-runtime.response-mode-test/execute-prepared-request-invalid-retry-after-falls-back-test --focus psi.turn-runtime.response-mode-test/execute-prepared-request-streaming-retry-discards-failed-partial-output-test` passed (`4 tests, 23 assertions`).
- `clj-kondo --lint components/turn-runtime/src/psi/turn_runtime/core.clj components/turn-runtime/test/psi/turn_runtime/response_mode_test.clj` passed with no errors or warnings.

2026-05-30 — Implementation review: found one new actionable gap after re-reading the streaming retry header follow-up. The focused test covers synchronous `do-stream!` exceptions caught by `turn-runtime/execute-live-turn!`, but the shared async streaming path `psi.ai.streaming/exception->error-event` still drops retry headers and `:http-status` from `ex-data` (it only maps `:status`). Provider exceptions thrown inside the normal background streaming future can therefore still reach the retry boundary without `Retry-After` / rate-limit metadata, contrary to the design's generic streaming exception requirement.

2026-05-30 — Implementation review follow-up: completed the async streaming exception metadata preservation item. `psi.ai.streaming/exception->error-event` now preserves `:headers`, normalized `:provider-error/headers`, `:http-status`, and `:status` from provider exception `ex-data` when an exception is thrown inside the normal background streaming future. Added focused coverage through `psi.ai.core/stream-response-in` proving a thrown rate-limit exception retains retry headers and status in the delivered `:error` event.

Verification:
- `clojure -M:test --focus psi.ai.core-test/test-stream-response-callback` passed (`1 tests, 10 assertions`).
- `clojure -M:test --focus psi.ai.core-test --focus psi.turn-runtime.response-mode-test --focus psi.agent-session.prompt-lifecycle-telemetry-test` passed (`27 tests, 189 assertions`).
- `clj-kondo --lint components/ai/src/psi/ai/streaming.clj components/ai/test/psi/ai/core_test.clj components/agent-session/test/psi/agent_session/prompt_lifecycle_telemetry_test.clj` passed with no errors or warnings.

2026-05-30 — Implementation review: found one new actionable gap after re-reading the async streaming metadata follow-up. `psi.ai.streaming/exception->error-event` now preserves both `:headers` and `:provider-error/headers`, but `psi.turn-runtime.core/make-provider-event-consumer` forwards only `:headers` from streaming `:error` events into the turn statechart. A normal background streaming provider exception whose ex-data contains only normalized `:provider-error/headers` can therefore still lose retry-header metadata before provider-boundary retry calculates delay. Add focused coverage for that shape and forward `:provider-error/headers` as a header source in the turn-runtime streaming error consumer.
