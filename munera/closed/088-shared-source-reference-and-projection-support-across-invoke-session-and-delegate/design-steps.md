- [x] Make the review/output surface explicit for this task: keep `implementation.md` and `design-steps.md` present in the task directory, or update the task instructions/steps to name the canonical alternatives so review and follow-up recording are unambiguous.
- [x] Decide and document the canonical shared resolution boundary for task 088: name the owning namespace/API for source-ref, source-spec, path, and projection resolution, and state which existing helpers remain authoring-only compatibility seams versus which must converge onto the shared runtime substrate in scope for this task.
- [x] Decide and document whether the legacy workflow-file `:session` projection contract in `components/agent-session/src/psi/agent_session/workflow_file_authoring_session.clj` (`:projection :text|:full|{:path ...}` on authored inputs) is intentionally preserved as a compatibility authoring surface for task 088 or must be converged/translated more explicitly to the canonical IR source-spec contract in `doc/workflow-ir.md` (`:path` sibling and canonical IR `:projection`). Update task design/plan/steps so the shared-semantics goal does not read as if those two projection languages are already the same thing.
