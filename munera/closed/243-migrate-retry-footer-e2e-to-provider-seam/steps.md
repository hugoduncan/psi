# Steps

## Slice 1 — Baseline + seam contract

- [x] Run full-suite `bb test` (pre-migration); record passed/failed/errored counts in implementation.md as the local baseline, noting the task-242 reference baseline (2450/24/38)
- [x] Read `psi.ai.streaming/stream-response` (and `stream-response-seq` if relevant) to pin the provider-impl map contract the stub must satisfy
- [x] Read `turn-runtime/make-provider-event-consumer`'s `:error` case to pin the exact stream `:error` event keys (`:http-status`, `:provider-error/headers`) and the successful-stream event vocabulary for the recovery turn
- [x] Record the pinned contract (map shape + event shapes for 429 and recovery) in implementation.md

## Slice 2 — Stub provider + ctx helper

- [x] Add a stub-provider constructor in `components/rpc/test/psi/rpc_prompt_test.clj` with an internal attempt counter: attempt 1 → `:error` event with `:http-status 429`, headers `Retry-After` = activation delay, `RateLimit-Limit`, `RateLimit-Remaining "0"`; attempt 2 → `:error` with `Retry-After` = changed delay and changed `RateLimit-Remaining`; attempt 3+ → successful recovery stream (text "recovered", stop)
- [x] Expose the attempt counter (or return it alongside the provider) so both tests keep asserting attempt counts as today
- [x] Add a ctx helper using `psi.ai.core/create-context {:anthropic stub}` yielding `{:provider-registry (atom {:anthropic stub})}`; this is the value to pass as the **first `ai-ctx` arg** of `execute-prepared-request!` (provider resolution consults only that arg), not merged into the app-runtime `ctx`; verify stub resolution via `resolve-provider` path works for model `{:provider :anthropic :id "stub"}`
- [x] Commit

## Slice 3 — Migrate the shared driver + focus-gate test

- [x] Rewrite `drive-provider-retry-through-progress-loop!`: remove the single `with-redefs [turn-runtime/execute-live-turn! ...]` site (all retry sub-tests reach the stub through this shared driver, so this migrates them all at once); pass the stub `ai-ctx` (`{:provider-registry ...}` from `create-context`) as the **first `execute-prepared-request!` arg**, replacing `{:provider-registry (atom {})}`, leaving the second app-runtime `ctx` unchanged; keep `start-progress-loop!` wiring, THREAD-AFFINITY invariant (drive on test thread), and `await-retry-footer-text!` sync via `:provider-retry-sleep-fn`
- [x] Verify `rpc-prompt-provider-retry-footer-reaches-focused-session-emit-boundary-test` still asserts all three retry frames for the focused session and zero frames for the background session, unchanged
- [x] Run `bb test --focus psi.rpc-prompt-test`; green
- [x] Run `clj-paren-repair` on the edited file; re-read; `clj-kondo` clean
- [x] Commit

## Slice 4 — Verify the sibling test + comment removal

- [x] Confirm the sibling `rpc-prompt-provider-retry-state-publishes-footer-updated-test` is already migrated by Slice 3 — it has no inline `with-redefs` to remove; it reaches the stub only via the shared `drive-provider-retry-through-progress-loop!` driver (~line 707)
- [x] Verify it still asserts the same activation/changed/clear footer sequence at the pre-gate `emit!`
- [x] Remove the task-242 "deliberate bounded exception to ¬mock/¬stub" comment block (and any other now-stale comments referencing the deferred seam migration)
- [x] Confirm `grep with-redefs` in the retry-footer tests shows no logic-boundary redefinition remains (the unrelated `session/query-in` site at ~line 91 is out of scope — leave it, note it in implementation.md)
- [x] Run `bb test --focus psi.rpc-prompt-test`; green
- [x] Commit

## Slice 5 — Harness consolidation (forwarded from task 242)

- [x] Replace `active-retry-text-prefix` length-subtraction derivation with a direct derivation from `psi.app-runtime.retry-display/retry-status-text` or an explicit literal
- [x] Collapse `focus-gated-emitter!` and `default-focus-emitter!` into one parameterized `focus-emitter! [session-id focus]` builder; update both call-site tests
- [x] Prune matcher/format/config-coupling helpers made redundant by the provider-seam harness (no churn beyond redundancy) — none found redundant; the retained helpers each still have a distinct single-authority role
- [x] Run `bb test --focus psi.rpc-prompt-test`; green; lint clean
- [x] Commit

## Slice 6 — Flakiness re-evaluation + close-out

- [x] Run full-suite `bb test` (post-migration); record passed/failed/errored counts in implementation.md
- [x] Record the before/after comparison against the baseline and state whether the parallel-isolation failure set is unchanged, reduced, or eliminated (unchanged ⇒ recorded finding: these two sites were not the isolation cause)
- [x] Verify all design.md acceptance criteria; note verification in implementation.md
- [x] Commit

## Implementation-review follow-ups (task-implementation-review pass)

- [x] Residual standing `¬mock/¬stub` violation, same file, untracked: `rpc-prompt-footer-updated-tolerates-keyword-sentinel-values-test` still `with-redefs [session/query-in …]` (`components/rpc/test/psi/rpc_prompt_test.clj` ~line 93) — a **logic-boundary** redefinition of a query/resolver, the same class of violation task 243 removed for `execute-live-turn!`. Correctly excluded from 243's frozen scope (Slice 4 noted it), but not tracked by any open task. Open a follow-up Munera task to migrate it onto the Pathom resolver/query seam (inject a stub footer-query source rather than redefining `session/query-in`), or record an explicit deferral rationale so the standing violation is tracked rather than silently carried.
      - Opened follow-up task `munera/open/244-migrate-footer-query-in-with-redefs-to-resolver-seam/` (design-only); registered in `munera/plan.md`. The standing violation is now tracked, not silently carried.
- [x] Robustness note (non-blocking) on `active-retry-text-prefix` derivation (rpc_prompt_test.clj ~line 219): it strips a trailing literal `"0s"` off `retry-status-text {:active? true :resume-at 0} 0`, so it silently breaks if `retry-display/format-relative-seconds` ever renders the zero-second case as a non-`"Ns"` form (e.g. `"now"`). This was a deliberate improvement over the length-subtraction idiom and is acceptable, but if a footer-format authority test does not already lock the zero-second rendering, add one so a future `retry_display.clj` change fails the authority test rather than silently desyncing this prefix.
      - No prior authority test locked the zero-second rendering. Added `components/app-runtime/test/psi/app_runtime/retry_display_test.clj` locking `format-relative-seconds 0`/`-500` → `"0s"` and `retry-status-text {:active? true :resume-at 0} 0` → `"retry in 0s"`. A future `retry_display.clj` change to the zero-second form now fails the authority test first.

## Implementation-review pass 2 (task-implementation-review)

- [x] Redundant assertion term in the added authority test (`components/app-runtime/test/psi/app_runtime/retry_display_test.clj`, `retry-status-text-locks-zero-second-active-prefix-test`, ~lines 23–27): the second `is` — `(= "retry in 0s" (retry-status-text {:active? true :resume-at 0} 0) (retry-status-text {:active? true} 0))` — repeats verbatim the whole first `is` in its first two args (`"retry in 0s"` = `(retry-status-text {:active? true :resume-at 0} 0)`), so the only new coverage the second form adds is the third term: that an **omitted** `:resume-at` (`{:active? true}`) renders identically to explicit `:resume-at 0`. Either drop the now-redundant first `is` (the second `is`'s 3-way `=` subsumes it) or drop the duplicated middle arg from the second `is` and keep the two forms distinct (literal-lock vs. resume-at-0-equals-omitted). Non-blocking test-clarity issue; the invariants are all still exercised, just stated twice.

## Task-test-review follow-ups (task-test-review skill pass)

- [x] Recovery-turn shape is never positively asserted (`components/rpc/test/psi/rpc_prompt_test.clj`, `retry-stub-provider-ai-ctx` ~lines 424–428 and both retry-footer tests). The stub's attempt-3+ branch emits a genuine recovery stream (`:text-delta "recovered"` → `:done :reason :stop`), and the docstring (~line 404) claims "a successful recovery stream" as modelled behaviour — but no test asserts the recovery turn's content (`"recovered"`) or its `:stop` reason. The only clear-path check is negative (`(not (retry-status-line? clear-footer))`, i.e. absence of stale `retry in` text), which passes whether the footer cleared because a real `:stop`-reason recovery turn landed **or** because retries were merely exhausted with no recovery. The `(= 3 attempts)` control proves the third `:stream` call fired but not that its recovery-shaped events (vs. a third error) were what cleared the retry state. Design.md Approach names "a successful recovery turn" and Acceptance names "recovery/clear", so this is a modelled behaviour with only indirect coverage. Add one positive assertion that the recovery turn actually completed as a success (e.g. assert the recovering assistant message / final frame carries the recovered text or a `:stop`/non-error stop-reason), so a future stub or pipeline change that clears the footer via exhaustion rather than recovery cannot pass green. Non-blocking: behaviour-preserving migration and all frozen frame/gate assertions are intact; this is a coverage-strengthening gap the seam migration made explicit (the prior fabricated-turn stub had the same gap).
      - `drive-provider-retry-through-progress-loop!` now returns `{:attempts N :recovery-result execution-result}` (was bare attempt count); the shaped recovery `execution-result` is captured from `execute-prepared-request!`. Added shared `assert-recovery-turn-succeeded!` positive control asserting `:execution-result/turn-outcome :turn.outcome/stop`, `:execution-result/stop-reason :stop`, and that the recovery assistant-message content carries `"recovered"`. Wired into both retry-footer tests (focused, default-session-id fallback, background, and the sibling pre-gate test). Verified non-vacuous: mutating the stub recovery text to `"NOTRECOVERED"` fails the new control. `bb test --focus psi.rpc-prompt-test` green (70 assertions, 5 tests); `clj-kondo` clean.

## Task-test-review follow-ups (test-shaper skill pass)

- [x] Inconsistent assertion messages on the three session-id-stamping checks (`components/rpc/test/psi/rpc_prompt_test.clj` lines ~592, ~633, ~786). Every other `is` in these two retry-footer tests carries a descriptive failure message stating the contract (e.g. "focused session must receive …"), but the session-id assertions do not: `(is (every? #(= session-id (get-in % [:data :session-id])) footer-events))` (lines 592, 633) and the 3-way `(is (= session-id (get-in first-retry-footer …) (get-in changed-retry-footer …) (get-in clear-footer …)))` (line 786) have no message. A stamping regression — exactly the emit.clj session-id correctness the comment above line 786 names as the control's purpose — would fail with only the raw `every?`/`=` form printed, not the contract it enforces. Per test-shaper `meaningful_failures` + `consistent(assertion_style)`, add a failure message to each of these three so a failing stamp reports the violated invariant. Non-blocking clarity/consistency: the assertions all still exercise the correct behaviour.
      - Added distinct failure messages to all three: focused `every?`, default-session-id-fallback `every?`, and the sibling 3-way `=` (which now names "stamped … not merely equal to each other", the actual correctness invariant the control guards).

- [x] Stale docstring on `retry-stub-provider-ai-ctx` (`components/rpc/test/psi/rpc_prompt_test.clj` ~line 388). The docstring says it returns `[ai-ctx attempts*]` "so callers can assert the same attempt counts the prior fabricated-turn `with-redefs` stub asserted", but no caller consumes the returned `attempts*` atom directly: the sole caller `drive-provider-retry-through-progress-loop!` reads `@attempts*` internally and surfaces it as the driver's `:attempts` return key, which is what the tests assert on. The docstring implies a caller-facing use of the returned atom that no longer exists. Per test-shaper `locally_comprehensible` (understanding the helper from its local source), update the docstring to state that the driver consumes `attempts*` and re-exports the count as `:attempts`, so a reader is not misled into looking for a direct-atom assertion site. Non-blocking documentation accuracy; behaviour unchanged.
      - Docstring updated: now states the sole caller `drive-provider-retry-through-progress-loop!` reads `@attempts*` internally and re-exports it as the driver's `:attempts` key (what the tests assert on), and that there is no caller-facing direct-atom assertion site.

- [x] Repeated `(= 3 attempts)` magic-number + recovery-control pairing across four driver call sites (`components/rpc/test/psi/rpc_prompt_test.clj` lines 558/562, 612/615, 701/704, 760/764; plus the attempts-only pre-gate control at line 685). The literal `3` (the modelled activate→change→clear attempt count) is hand-repeated at five sites with no single authority, and the two-line sequence `(is (= 3 attempts) "…") ` immediately followed by `(assert-recovery-turn-succeeded! recovery-result)` is copied verbatim at the four recovery-carrying sites. A change to the modelled attempt count or the full-sequence contract must be edited at up to five places and can drift between them. Per test-shaper `economical` (`minimal(incidental_variation)`) and `helpers_that_compress(ceremony) ∧ ¬helpers_that_hide(intent)`: introduce a single authority for the expected attempt count (e.g. an `expected-retry-attempts` const derived from the stub's 429→429→recovery shape) and consider folding the `attempts`+recovery pair into one `assert-full-retry-sequence-ran!` helper taking `{:attempts :recovery-result}` (keeping the pre-gate attempts-only control separate, since it has no recovery-result). Non-blocking: purely a consolidation of already-correct controls; do not weaken any of the four/five sites' assertions.
      - Added `expected-retry-attempts` const (single authority for the modelled `3`) alongside the other retry-shape consts. Added `assert-full-retry-sequence-ran! {:attempts :recovery-result}` folding the attempts check (vs the const) + `assert-recovery-turn-succeeded!` into one helper; wired it into all four recovery-carrying sites (focused, default-session-id fallback, background gated, sibling pre-gate). The pre-gate attempts-only control stays separate but now asserts against `expected-retry-attempts` (no literal `3`). No assertion weakened; verified non-vacuous (mutating stub recovery text to `NOTRECOVERED` fails 2 tests). `bb test --focus psi.rpc-prompt-test` green (66 assertions, 4 tests); `clj-kondo` clean.

## Task-test-review pass 4 (test-shaper skill)

- [x] Duplicated clear-footer control pair across the two focus-gate sub-tests (`components/rpc/test/psi/rpc_prompt_test.clj` lines ~550–554 and ~590–594). The two-line clear-footer contract — bind `clear-footer` via `clear-footer-produced-after-retry`, then `(is (some? clear-footer) …)` + `(is (not (retry-status-line? clear-footer)) …)` — is copied byte-for-byte at both the explicit-focus and default-session-id-fallback sub-tests, differing *only* in the message label prefix ("focused session" vs "default-session-id fallback"). This is the same class of copied two-line control pair the prior test-shaper pass folded into `assert-full-retry-sequence-ran!` (attempts + recovery), but the clear-footer pair was left unconsolidated, so a change to the clear-footer contract (e.g. what "distinguishable clear footer" means, or the stale-text predicate) must be edited at both sites and can drift between two sub-tests meant to exercise the *same* clear-path contract under explicit-vs-fallback focus. Per test-shaper `economical` (`minimal(incidental_variation)`) and `consistent(test_abstractions)` + `helpers_that_compress(ceremony) ∧ ¬helpers_that_hide(intent)`: fold the two focus-gate clear-footer pairs into one parameterized control (e.g. `assert-clear-footer-produced! [footer-events label]` yielding the two `is` forms with the label interpolated into the failure messages), matching the `assert-full-retry-sequence-ran!` consolidation already applied at these same sites. Scope note: the sibling pre-gate test (`rpc-prompt-provider-retry-state-publishes-footer-updated-test`, ~lines 719/735/750) interleaves its `clear-footer` binding with the session-id-stamping control and uses non-focus-gate messages, so it is *not* a drop-in third caller — either leave it out (the two focus-gate sites are the clean duplicate) or restructure it separately; do not force it into the shared helper if that would fragment its stamping-control locality. Non-blocking consolidation of already-correct controls; do not weaken any site's assertions.
      - Added `assert-clear-footer-produced! [footer-events label]` folding the two-line clear-footer pair (`some? clear-footer` + `not retry-status-line?`) into one authority, `label` interpolated into both failure messages. Wired into both focus-gate sub-tests (explicit-focus "focused session", default-session-id fallback "default-session-id fallback"). Per the follow-up's scope note, the sibling pre-gate test was left out (its `clear-footer` binding interleaves with the session-id-stamping 3-way `=` control). No assertion weakened. `bb test --focus psi.rpc-prompt-test` green (60 assertions, 3 tests); `clj-kondo` clean.

## Task-test-review pass 5 (test-shaper skill)

- [x] Stale `error-turn` terminology left by the task-243 migration (`components/rpc/test/psi/rpc_prompt_test.clj`, floating comment line ~190, and the `retry-rate-limit` / `changed-retry-remaining` docstrings lines ~210 and ~216). All three say the driver "derives its `error-turn` header from" / drives values "into the `error-turn` header", but task 243 removed the fabricated-turn `with-redefs` stub entirely: the stub now emits stream `:error` events via the local `error-event` fn (line ~366), and `execute-live-turn!`'s consumer (`make-provider-event-consumer` `:error` case) turns those into the assistant-message — there is no longer any `error-turn` map in this test. A reader searching for `error-turn` finds only `error-event`, and the description implies a fabricated turn map rather than the stream `:error` event the stub actually emits. Per test-shaper `locally_comprehensible` + `consistent(naming)`: rename these three `error-turn` references to the current construct (stub `:error` stream event / `error-event` headers), so the drift-prevention docstrings describe the seam the test now uses. Non-blocking documentation accuracy; behaviour unchanged.
      - Removed all three `error-turn` references. The floating comment now describes the stub's `:error` stream events (`error-event`) deriving 429 headers from the constants; the `retry-rate-limit` / `changed-retry-remaining` docstrings now say "driven into the stub's second `:error` stream event (`error-event`) header" instead of "driven into the `error-turn` header". No fabricated turn map exists post-migration; a reader now finds only `error-event`. `grep error-turn` → none. Test green (60 assertions, 3 tests); `clj-kondo` clean.

- [x] Redundant single-authority-drift rationale stated 3× across the retry-delay/rate-limit constants (`components/rpc/test/psi/rpc_prompt_test.clj` lines ~178–187 floating comment "Shared retry-frame matchers", lines ~189–196 floating comment "Single authorities for the retry delays / rate-limit metadata", and again in each of the four `activation-retry-delay-ms` / `changed-retry-delay-ms` / `retry-rate-limit` / `changed-retry-remaining` def docstrings ~197–220). The two floating `;;` blocks restate the same "driver derives its headers from these constants, matchers derive their text from them, so they cannot drift" argument the per-def docstrings already carry individually. Per test-shaper `economical` (`minimal(incidental_variation)`) and `locally_comprehensible` (understand each const from its own docstring): collapse the two floating blocks into one short group header (or drop them, keeping only the per-def docstrings), so the shared-authority rationale is stated once, not re-spelled at up to six sites where a wording change to the invariant must be edited in several places. Non-blocking economy/redundancy; do not weaken any docstring's factual content when consolidating.
      - Collapsed the two floating `;;` blocks ("Shared retry-frame matchers" + "Single authorities for the retry delays / rate-limit metadata") into one short group header stating the shared-authority rationale once, deferring specifics to the per-def docstrings. The redundant matcher-drift paragraph and the duplicate single-authority paragraph are gone; per-def docstrings retain their individual factual content unchanged. Test green (60 assertions, 3 tests); `clj-kondo` clean.

## Task-test-review pass 6 (test-shaper skill)

- [x] Obscured frame-lookup idiom inconsistent with the existence idiom used elsewhere (`components/rpc/test/psi/rpc_prompt_test.clj`, sibling `rpc-prompt-provider-retry-state-publishes-footer-updated-test`, lines ~709–710): `first-retry-footer` / `changed-retry-footer` are bound via `(some #(when (activation-retry-footer? %) %) footer-events)` — a "first frame matching pred" that works *only* because footer frames are always-truthy maps, so `(when pred %)` returns the frame rather than `true`. Every other retry-footer-match site expresses matching via the plain `(some activation-retry-footer? footer-events)` existence idiom (focus-gate tests lines ~538/540/582/584); this sibling instead needs the *frame itself* (for the `[:data :session-id]` stamping `get-in` and the `some?` presence checks), and encodes that need with the non-obvious `#(when (pred %) %)` form. Per test-shaper `consistent(test_abstractions)` + `locally_comprehensible`: replace both with the direct, standard "first matching element" spelling `(first (filter activation-retry-footer? footer-events))` (and likewise for `changed-retry-footer?`), which states the intent (first matching frame) without depending on map-truthiness, and reads consistently against the `some`-existence idiom used everywhere else. Non-blocking clarity/consistency; behaviour identical (both forms yield the first matching frame, `nil` if none — the `(some? …)` checks stay meaningful).
      - Replaced both `(some #(when (pred %) %) footer-events)` bindings with the direct `(first (filter pred footer-events))` spelling for `activation-retry-footer?` and `changed-retry-footer?`. States "first matching frame" intent without depending on map-truthiness; reads consistently against the `some`-existence idiom used at the focus-gate sites. Behaviour identical (first matching frame, `nil` if none; the `(some? …)` stamping checks stay meaningful). `bb test --focus psi.rpc-prompt-test` green (60 assertions, 3 tests); `clj-kondo` clean.

## Task-test-review pass 7 (test-shaper skill)

- [x] Duplicated focus-gate footer-verification block across the two focus-gate sub-tests (`components/rpc/test/psi/rpc_prompt_test.clj`, `rpc-prompt-provider-retry-footer-reaches-focused-session-emit-boundary-test`, explicit-focus arm ~lines 519–558 and default-session-id-fallback arm ~lines 559–598). After the prior test-shaper passes consolidated the *sub-pieces* (`focus-emitter!`, `assert-full-retry-sequence-ran!`, `assert-clear-footer-produced!`), the two arms still each carry a verbatim ~6-assertion footer-verification block: `(is (seq footer-events) …)` + `(is (some activation-retry-footer? …) …)` + `(is (some changed-retry-footer? …) …)` + `(is (activation-precedes-changed? …) …)` + `(assert-clear-footer-produced! footer-events label)` + `(is (every? #(= session-id (get-in % [:data :session-id])) footer-events) …)`, differing *only* in the `focus-emitter!` argument (explicit `session-id` vs `nil`) and the label-prefix strings ("focused session" vs "default-session-id fallback"). This is the same class of copied control block — identical contract, differing only by a label parameter — that pass 3 folded into `assert-full-retry-sequence-ran!` and pass 4 folded into `assert-clear-footer-produced!`; the enclosing block was left unconsolidated, so a change to the focus-gate crossing contract (which frames must cross, ordering, stamping) must be edited at both arms and can drift between two sub-tests meant to exercise the *same* gate contract under explicit-vs-fallback focus resolution. Per test-shaper `economical` (`minimal(incidental_variation)`), `consistent(test_abstractions)`, and `helpers_that_compress(ceremony) ∧ ¬helpers_that_hide(intent)`: fold the shared block into one parameterized control (e.g. `assert-retry-footers-crossed-gate! [footer-events label]` yielding the seq/activation/changed/ordering/clear/stamping assertions with `label` interpolated into the failure messages), matching the two consolidations already applied at these same call sites. Scope note: the background sub-test asserts the *opposite* contract (`(is (empty? footer-events) …)` suppression, not the produced-frame block) and the sibling pre-gate test interleaves its stamping 3-way `=` with a `clear-footer` binding and uses non-focus-gate messages — neither is a drop-in caller; leave both out, do not force them into the shared helper. Non-blocking consolidation of already-correct controls; do not weaken any arm's assertions.
      - Added `assert-retry-footers-crossed-gate! [footer-events session-id label]` folding the shared 6-assertion focus-gate footer-verification block (seq / activation / changed / activation-precedes-changed ordering / `assert-clear-footer-produced!` / per-frame session-id stamping) into one authority, `label` interpolated into all failure messages. Wired into both focus-gate arms (explicit-focus "focused session", default-session-id fallback "default-session-id fallback"); each arm now reduces to `assert-full-retry-sequence-ran!` + `assert-retry-footers-crossed-gate!`. Per the scope note, the background suppression sub-test and the sibling pre-gate test (interleaved stamping 3-way `=`) were left out. No assertion weakened — same 6 checks per arm. `bb test --focus psi.rpc-prompt-test` green (60 assertions, 3 tests — unchanged); `clj-kondo` clean.

## Task-test-review pass 8 (test-shaper skill)

- [x] Duplicated "index of the (first|last) frame matching a predicate" idiom lacking a single authority (`components/rpc/test/psi/rpc_prompt_test.clj`, `clear-footer-produced-after-retry` ~line 303 and `activation-precedes-changed?` ~lines 321–325). The `(some (fn [[i frame]] (when (pred frame) i)) (map-indexed vector footer-events))` "first index where pred holds" idiom is hand-spelled three times: twice forward in `activation-precedes-changed?` (byte-for-byte identical but for the predicate — `activation-retry-footer?` vs `changed-retry-footer?`), and once over `(reverse (map-indexed vector footer-events))` in `clear-footer-produced-after-retry` for the "last index where pred holds". This is the same class of copied element-lookup idiom that test-shaper pass 6 replaced in the sibling test — but pass 6 fixed only the *frame*-returning `(some #(when (pred %) %) …)` site there and did not touch these three *index*-returning sites, which need the index (not the frame) for the ordering/after-retry comparisons, so the `(first (filter …))` spelling pass 6 used does not apply directly. The nested destructure + `map-indexed vector` + `when-index` form is non-obvious (a reader must decode that `some` returns the first non-nil `i`, relying on index 0 being falsey-safe only because `(when pred 0)` yields `0` which `some` treats as truthy — 0 is truthy in Clojure, so this is correct but subtle) and is stated at three sites where a change to the match/index convention must be edited in several places and can drift. Per test-shaper `consistent(test_abstractions)`, `economical` (`minimal(incidental_variation)`), and `helpers_that_compress(ceremony) ∧ ¬helpers_that_hide(intent)`: introduce one small authority for "first index of a matching frame" (e.g. `(defn- first-frame-index [pred frames] (some (fn [[i frame]] (when (pred frame) i)) (map-indexed vector frames)))`) and derive the two forward sites from it; express the `clear-footer-produced-after-retry` "last matching index" as the same helper over reversed frames (or a `last-frame-index` sibling), so the destructure/index convention lives in one place. Scope note: preserve behaviour exactly — `activation-precedes-changed?` must still return false when either frame is absent, and `clear-footer-produced-after-retry` must still key off the *last* active-retry frame's successor; do not weaken either control. Non-blocking clarity/consistency/economy; behaviour identical.
      - Added `first-frame-index [pred frames]` (single authority for the `map-indexed`/`when`-index destructure, relying on index 0 being truthy) and `last-frame-index [pred frames]` (derived from `first-frame-index` over reversed frames, re-mapped to a forward index via `(- (dec count) rev-idx)`). Derived the two forward sites in `activation-precedes-changed?` and the last-matching site in `clear-footer-produced-after-retry` from them; the destructure/index convention now lives in one place. Behaviour preserved: `activation-precedes-changed?` still false when either frame absent; `clear-footer-produced-after-retry` still keys off the last active-retry frame's successor (verified the reversed-index re-mapping yields the identical forward index the prior in-place reversed `some` produced). `bb test --focus psi.rpc-prompt-test` green (60 assertions, 3 tests — unchanged); `clj-kondo` clean.

## Task-test-review pass 9 (test-shaper skill)

- [x] Activation 429's rate-limit headers are unexercised incidental setup — either dead stub config or an uncovered behaviour (`components/rpc/test/psi/rpc_prompt_test.clj`, `retry-stub-provider-ai-ctx` attempt-1 branch ~lines 377–379 and `activation-retry-footer?` ~lines 259–263). The stub's *first* 429 sets `"RateLimit-Limit" (str retry-rate-limit)` (5000) and `"RateLimit-Remaining" "0"`, and `make-provider-event-consumer` propagates those into the retry state, so per `retry_display/retry-status-text` the activation footer's status-line renders `"retry in 8s · remaining 0/5000"` — but `activation-retry-footer?` substring-matches **only** `"retry in 8s"` (`expected-retry-text`, delay-only), never the activation-attempt `remaining 0/5000` fragment. Consequences (both real test-shaper concerns): (a) `minimal_incidental_setup` — the first 429's `RateLimit-Limit`/`RateLimit-Remaining "0"` headers are setup no assertion reads, so a reader cannot tell from any assertion whether they matter; and (b) `economical`/signal — the *only* discriminator distinguishing the activation matcher from the changed matcher is the changed footer's `remaining 2/5000` fragment, so the test cannot catch a regression where the first-429 rate-limit metadata fails to propagate into the activation footer (activation `"retry in 8s"` with a *missing* or *wrong* remaining fragment still matches `activation-retry-footer?`). The changed→activation rate-limit *transition* (`0/5000` → `2/5000`) is a modelled behaviour (design names "changed metadata" as a distinct retry frame) but only the changed end is positively pinned. Per test-shaper `economical` + `minimal_incidental_setup` + `behavior_focused`: either (i) tighten `activation-retry-footer?` to also assert the activation footer's `remaining 0/5000` fragment (making the first-429 headers load-bearing and covering first-attempt rate-limit propagation + the `0/5000`→`2/5000` change), reusing `remaining-fragment` with an `activation-retry-remaining` const (`0`); or (ii) if first-attempt rate-limit rendering is deliberately out of the modelled contract, drop the unused `RateLimit-Limit`/`RateLimit-Remaining "0"` headers from the stub's first `error-event` so the stub setup carries only what an assertion exercises. Prefer (i): it strengthens coverage of the activation→changed rate-limit transition the design models, whereas (ii) merely removes the smell. Scope note: behaviour-preserving for product code — this only tightens a test matcher / trims stub setup; do not weaken any existing frame/gate/recovery/stamping assertion, and keep the changed matcher's `2/5000` fragment intact. Non-blocking: current assertions are all correct, this closes a signal gap the seam migration inherited from the fabricated-turn stub (which set the same unexercised headers).
      - Chose option (i). Added `activation-retry-remaining` const (`0`, single authority) and made the stub's first `:error` event derive `"RateLimit-Remaining"` from it (was hard-coded `"0"`), making the first-429 rate-limit metadata load-bearing. Tightened `activation-retry-footer?` to also assert the `remaining 0/5000` fragment (reusing `remaining-fragment`), so first-attempt rate-limit propagation and the `0/5000`→`2/5000` change are pinned at both ends, not only the changed end. Changed matcher's `2/5000` fragment untouched; no frame/gate/recovery/stamping assertion weakened. Verified non-vacuous (mutating the stub's first `RateLimit-Remaining` to `9` fails both retry-footer tests). `bb test --focus psi.rpc-prompt-test` green (60 assertions, 3 tests); `clj-kondo` clean.
