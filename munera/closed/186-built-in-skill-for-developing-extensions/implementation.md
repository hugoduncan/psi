- 2026-05-28 ambiguity review: found actionable planning gaps before implementation. The task currently has only `design.md`; `plan.md`, `steps.md`, and `implementation.md` were missing despite Munera requiring them before execution. Added `design-steps.md` follow-ups to define the canonical built-in skill identity/resource path, the minimum authoritative reference set (including the exact GitHub `doc/extension-api.md` URL and extension API / manifest / permission / dispatch seams), whether the skill covers modifying/debugging existing extensions in addition to creation, and the non-prose-fragile verification surfaces for "discoverable and usable" built-in provenance + ordinary readable-file semantics.

- 2026-05-28 design-step follow-up: completed all ambiguity-review additions in `design-steps.md`.
  - created missing `plan.md` and `steps.md` so the task now satisfies the required Munera artifact set before implementation work.
  - decided canonical built-in skill identity: runtime name `extension-development`, packaged resource `psi/skills/extension-development/SKILL.md`, expected provenance `:source :built-in`.
  - defined the minimum authoritative extension reference set around `doc/extension-api.md`, `doc/extensions.md`, `doc/extensions-install.md`, `doc/architecture.md`, and the built-in skill/discovery seams in `components/prompt-assets` plus discovery/listing seams in `components/agent-session`.
  - fixed the external documentation URL to `https://github.com/hugoduncan/psi/blob/main/doc/extension-api.md`.
  - clarified scope to include creating, modifying, and debugging extensions.
  - defined sufficient verification as structural proof of packaged resource presence, built-in provenance, ordinary materialized file readability, normal discovery/listing visibility, and normal invocation/read semantics, while avoiding brittle full-prose assertions.

- 2026-05-28 inconsistency review: found two new actionable inconsistencies across the refined task artifacts and the referenced built-in skill implementation seams. First, design/plan/steps consistently name the packaged built-artifact identity `psi/skills/extension-development/SKILL.md`, but they never fix the concrete source-tree authoring location even though the built-in packaging precedent from tasks 184/185 and the current resource layout expect `bases/main/resources/psi/skills/extension-development/SKILL.md`; without that, implementation can satisfy the nominal packaged path while writing the file in the wrong repo location. Second, the design's verification definition requires normal discovery/introspection plus command/UI-oriented listing visibility, but plan/steps currently only call out prompt-assets built-in discovery/materialization/invocation proof, leaving higher-surface verification scope inconsistent with the stated acceptance target. Added unchecked `design-steps.md` follow-ups for both mismatches.

- 2026-05-28 inconsistency follow-up execution: completed the newly added `design-steps.md` items without executing implementation `steps.md` work.
  - aligned `design.md` on the concrete source-tree authoring location `bases/main/resources/psi/skills/extension-development/SKILL.md` while preserving packaged built-in identity `psi/skills/extension-development/SKILL.md`
  - tightened the verification definition so packaged-resource proof names both source-tree authoring and packaged resource presence
  - made the required higher discovery/listing verification explicit by naming representative resolver/discovery surfaces (`:psi.agent-session/skills` / `:psi.skill/by-source`) and `/skills` command visibility in `design.md`
  - updated `plan.md` and `steps.md` so implementation proof must cover both prompt-assets materialization/invocation and representative higher listing surfaces

- 2026-05-28 implementation pass: added the built-in packaged skill source at `bases/main/resources/psi/skills/extension-development/SKILL.md` with repository-specific guidance for creating, modifying, and debugging psi extensions. The skill anchors on the canonical extension docs, capability/permission expectations, dispatch/handler/effect/resolver boundaries, manifest-installed versus built-in concerns, and the required GitHub extension API link.

- 2026-05-28 implementation pass: added structural verification for the production built-in skill path and packaged resource via `psi.prompt-assets.skills-test`, proving source-tree authoring presence, packaged resource enumeration, built-in provenance, materialized readable `:file-path`, and normal `/skill:extension-development` invocation semantics without freezing the full prose.

- 2026-05-28 implementation pass: added representative higher-surface proof in `psi.agent-session.resolvers-test` and `psi.agent-session.commands-test` so resolver discovery surfaces (`:psi.agent-session/skills`, `:psi.skill/by-source`) and `/skills` listing both expose `extension-development` as a built-in skill.

- 2026-05-28 verification: `clojure -M:test --focus psi.prompt-assets.skills-test --focus psi.agent-session.resolvers-test --focus psi.agent-session.commands-test` passed (`95 tests, 476 assertions`). `clj-kondo --lint bases/main/resources components/prompt-assets/test/psi/prompt_assets/skills_test.clj components/agent-session/test/psi/agent_session/commands_test.clj components/agent-session/test/psi/agent_session/resolvers_test.clj` passed with 0 warnings.

- 2026-05-28 task-implementation-review: re-read the task artifacts, built-in skill file, and referenced prompt-assets / resolver / command tests plus implementation seams in `components/prompt-assets/src/psi/prompt_assets/skills.clj`, `components/agent-session/src/psi/agent_session/resolvers/discovery.clj`, and `components/agent-session/src/psi/agent_session/commands.clj`. Re-ran the focused verification (`95 tests, 476 assertions`) and targeted lint (0 warnings). Found no new actionable implementation issues; built-in packaging, built-in provenance, ordinary readable file semantics, higher discovery/listing visibility, and `/skill:extension-development` invocation remain aligned with the task design and existing built-in skill architecture.

- 2026-05-28 follow-up execution pass: re-read `steps.md` plus the task design/plan/implementation context to execute any newly added actionable review items. Found no unchecked actionable `steps.md` items to execute, so no code or checklist changes were needed. This pass records the no-op state explicitly and leaves all task steps checked.

- 2026-05-28 task-test-review: re-read the task artifacts, built-in skill file, and the focused prompt-assets / resolver / command tests plus referenced implementation seams. Re-ran `clojure -M:test --focus psi.prompt-assets.skills-test --focus psi.agent-session.resolvers-test --focus psi.agent-session.commands-test` (`95 tests, 476 assertions`) and the targeted `clj-kondo` lint with 0 warnings. Found no new actionable test-quality issues: the proof set covers packaged resource presence, built-in provenance, ordinary readable file semantics, `/skill:extension-development` invocation, resolver discovery surfaces, and `/skills` listing visibility without brittle full-prose assertions.

- 2026-05-28 follow-up execution pass: re-read `steps.md`, `implementation.md`, `design.md`, and `plan.md` to execute newly added actionable review items. Found no unchecked actionable `steps.md` items to execute, so no code or checklist changes were needed. This pass records the current no-op state and leaves the task artifacts unchanged aside from this note.

- 2026-05-28 test-shaper review: re-read the task artifacts, built-in skill file, prompt-assets skill loading/discovery implementation, and focused prompt-assets / resolver / command tests. Re-ran `clojure -M:test --focus psi.prompt-assets.skills-test --focus psi.agent-session.resolvers-test --focus psi.agent-session.commands-test` (`95 tests, 476 assertions`). Found no new actionable test-shaping feedback: the proof set stays behavior-focused and structural, covering packaged resource presence, built-in provenance, readable materialized file semantics, normal `/skill:extension-development` invocation, resolver discovery visibility, and `/skills` listing without brittle full-prose assertions.

- 2026-05-28 follow-up execution pass: re-read `steps.md`, `implementation.md`, `design.md`, and `plan.md` to execute newly added actionable review items. Found no unchecked actionable `steps.md` items, so this pass is blocked on the absence of new follow-up work; no code or checklist changes were required.

- 2026-05-28 docs review (`review-task-docs`): no new actionable user-facing documentation issues found after re-reading the task artifacts, built-in skill file, and referenced user docs (`README.md`, `doc/extension-api.md`, `doc/extensions.md`, `doc/extensions-install.md`, `doc/architecture.md`, `CHANGELOG.md`) plus the prompt-assets / resolver / command implementation and tests. Existing docs already cover the extension API, install model, architecture boundaries, built-in `/skills` discovery, and the changelog entry for `review-task-docs`; task 186 adds a built-in skill artifact rather than a new user-facing command/flag/workflow behavior that requires additional README/doc/changelog updates.

- 2026-05-28 follow-up execution pass: re-read `steps.md` first, then `implementation.md`, `design.md`, and `plan.md` as needed. Found no newly added unchecked actionable `steps.md` items to execute; all task steps remain checked, so this pass is a no-op. No blocking implementation work was present to record.

- 2026-05-28 code-shaper review: found one actionable simplicity/robustness issue in the proof shape. Task verification for `/skills` command visibility covers the exact built-in listing entry, but there is no matching focused `/help` proof for `extension-development` even though `format-help` independently renders the Skills section and is part of the design’s representative command/UI-oriented listing surface. Add a focused command test that proves `/help` lists `extension-development` as a built-in skill so the two user-visible skill-list renderers cannot drift independently.

- 2026-05-28 follow-up execution pass: completed the newly added unchecked `steps.md` item from the code-shaper review by adding focused `format-help` proof in `psi.agent-session.commands-test` for the built-in `extension-development` skill entry in the Skills section. Re-ran `clojure -M:test --focus psi.agent-session.commands-test` (`50 tests, 196 assertions`) and `clj-kondo --lint components/agent-session/test/psi/agent_session/commands_test.clj` (0 warnings); both passed.

- 2026-05-29 task-189 authority-alignment follow-up: task 189 now treats deterministic `review-step` routing as design-only until `.psi/workflows/review-step.edn` and the loader/runtime proof surfaces are migrated in that task's implementation. Task 186's built-in skill work is complete and does not own review-step routing implementation state, so these task-186 notes should not be read as authoritative for that adjacent routing slice. Authority for the current intended topology and remaining implementation/proof work lives with `munera/open/189-deterministic-review-step-routing/`.
