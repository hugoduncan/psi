# 061 — Plan

1. Define the minimal projection vocabulary as `:projection` layered onto task-060 `{:from ...}` source-selection entries under `:session`.
2. Extend compiler translation from source+projection specs to canonical binding refs.
3. Validate malformed/unsupported projections while preserving task-060 source semantics.
4. Add focused compiler/loader tests, including named prior-step non-adjacent source selection with projection.
5. Re-check example workflows that benefit from structured extraction.
