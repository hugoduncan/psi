- 2026-05-07 inventory: canonical built-in workflow bootstrap still passes through pseudo-extension mechanics.
  - `components/agent-session/src/psi/agent_session/workflow/bootstrap.clj` calls `ext/register-extension-in!` with `built-in:workflow` and then constructs an extension API with `ext/create-extension-api`.
  - `components/agent-session/src/psi/agent_session/workflow/core.clj` installs built-in surfaces only through that extension-shaped API: `:register-tool`, `:register-command`, `:register-prompt-contribution`, and `:on` for `"session_switch"`.
- Surface classification from the inventory:
  - tool registration: built-in `delegate` tool is installed through API callback `(:register-tool api)` in `workflow/core.clj`; storage today is extension-registry-backed via `tool-registry/register-tool-in!`.
  - command registration: built-in `delegate` and `delegate-reload` commands are installed through API callback `(:register-command api)` in `workflow/core.clj`; storage today is extension-registry-backed via `command-registry/register-command-in!`.
  - prompt contribution registration: built-in workflow prompt contribution is refreshed through runtime-state callback `:register-prompt-contribution`, which today is sourced from extension API `:register-prompt-contribution`; actual stored contributions are session prompt-contribution state keyed by `ext-path`/`id`, not extension-registry maps.
  - lifecycle hook registration: built-in workflow currently registers `"session_switch"` handler via API `(:on api)`; invocation today is extension event dispatch from `session_lifecycle.clj` through `ext/dispatch-in`.
  - active tool aggregation: `workflow/bootstrap.clj` refreshes active tools by reading `tool-registry/all-tools-in (:extension-registry ctx)` and appending those to session built-ins; `psi_tool.clj` and `tool_plan.clj` also read extension-registry-backed tool lists.
  - command lookup/dispatch: command execution paths continue to resolve commands from `command-registry/get-command-in (:extension-registry ctx)`.
  - prompt rendering/projection: built-in workflow contribution text is stored in session prompt-contribution state and therefore already shares the prompt store with extensions, but its provenance is currently written via extension-shaped API semantics using `built-in:workflow` as `ext-path`.
  - reload/session-switch lifecycle: built-in workflow relies on extension event registration for `session_switch`; namespace reload preserves command/tool surfaces because they live in extension-registry-backed storage and the current reload path preserves the extension registry during namespace reload mode.
  - introspection/resolver projections: tool and command projections in `resolvers/extensions.clj` are extension-oriented and derive exclusively from extension-registry-backed stores; prompt-contribution projection is also extension-labeled even though the underlying storage is session state.
- Tests/proofs assuming extension ownership:
  - `workflow_built_in_targeting_test.clj` reaches directly into `[:extension-registry :state :extensions built-in:workflow :tools ...]` to fetch the built-in delegate tool.
  - `workflow_reload_runtime_test.clj` and `workflow_async_path_test.clj` prove built-in command/tool behavior through extension-registry-backed query helpers (`command-registry/get-command-in`, `command-registry/command-names-in`, `tool-registry/tool-names-in`).
- Initial design implication from the inventory:
  - tool and command surfaces are the strongest candidates for shared registry reuse with explicit built-in registration entrypoints/provenance, because runtime readers already aggregate from the registry helpers and a built-in path can stay small if those helpers learn built-in ownership cleanly.
  - prompt contributions likely do not need a separate built-in store; they already live in shared session prompt-contribution state, but need built-in-specific registration provenance/entrypoints instead of extension API wrapping.
  - lifecycle hooks are the one surface that cannot remain extension-shaped if this task is to satisfy its success criteria; built-in workflow needs an explicit runtime/session lifecycle registration/invocation path rather than `ext/dispatch-in` event subscription.
- Open implementation question sharpened by inventory:
  - whether the smallest clean path is to augment shared registries with owner/provenance-aware built-in entrypoints, or to add a tiny built-in composition owner that merges built-in command/tool surfaces at read time while keeping extension registries unchanged. The inventory favors the first option for commands/tools and a dedicated built-in lifecycle path for session-switch/reload behavior.
- 2026-05-09 review: task artifacts still miss one proof surface in the required inventory. `components/app-runtime/test/psi/gordian_launcher_manifest_runtime_boundary_test.clj` asserts `built-in:workflow` is present in extension-registry state, so the inventory/steps should explicitly classify that non-agent-session runtime-boundary proof and decide whether it migrates to a built-in projection/invariant or is recorded as a temporary exception.
- 2026-05-09 design-step follow-up executed: extended the required inventory/design language to include non-agent-session runtime-boundary proofs and classified `components/app-runtime/test/psi/gordian_launcher_manifest_runtime_boundary_test.clj` as a proof that must migrate to a built-in runtime-boundary invariant rather than stay as an extension exception.
- 2026-05-09 mechanism decision recorded from the completed design steps:
  - smallest clean abstraction: add a small higher-core built-in registration layer beside workflow bootstrap/runtime-state, not a new general extension rewrite.
  - commands/tools: reuse existing shared registries through built-in-specific provenance-aware entrypoints so runtime readers can stay small.
  - prompt contributions: keep shared session prompt-contribution storage, but install through a built-in-specific registration path instead of extension API wrapping.
  - lifecycle hooks: use a dedicated built-in lifecycle callback store/invocation path owned by higher-core workflow bootstrap/session lifecycle, because session-switch handling cannot remain extension-event-shaped and still satisfy task success.
- 2026-05-19 design decision sharpened:
  - do not extract a new common/shared registry framework as part of `136`.
  - generalize the existing command and tool registries locally only as far as built-in provenance-aware registration and lookup require.
  - if commonality remains obvious after `136`, that can be a separate follow-on refactor with evidence from the landed built-in workflow migration.
- 2026-05-09 remaining unchecked work is implementation, not design ambiguity: the newly added design-step follow-ups are now resolved in task artifacts; no blocker found for those design clarifications.
- 2026-05-09 review: prompt-contribution behavior is described inconsistently across task artifacts. `design.md` says prompt contribution behavior must be preserved and allows `# Extension Prompt Contributions` to remain temporarily only as recorded wording debt, but the task files never decide whether that label stays or changes, even though existing proof surfaces in `components/agent-session/test/psi/agent_session/prompt_lifecycle_test.clj` assert the extension-labeled heading exactly. Add an explicit design step to choose the label outcome and classify/update the affected prompt assembly proofs so the built-in provenance story and preserved behavior story do not conflict.
- 2026-05-09 design-step follow-up executed: resolved the prompt-contribution label decision and recorded the affected surfaces.
  - decision: keep `# Extension Prompt Contributions` unchanged in this task as deliberate temporary wording debt, because the slice is correcting built-in registration provenance rather than broadening into prompt-copy churn.
  - affected rendering/proof surfaces recorded in task artifacts: `components/prompt-assets/src/psi/prompt_assets/system_prompt.clj`, `components/agent-session/test/psi/agent_session/prompt_lifecycle_test.clj`, and `components/turn-runtime/test/psi/turn_runtime/request_test.clj`.
  - provenance consequence recorded: built-in workflow prompt contributions should continue to share the session prompt-contribution store, but must arrive there through a built-in-specific registration path instead of extension API wrapping or extension identity seeding.
- 2026-05-09 result: the newly added review follow-up items in `design-steps.md` are now completed in task artifacts; no blocker found for those design clarifications.
- 2026-05-09 review: `plan.md` and `steps.md` require built-in lifecycle hooks to move off extension-event registration, but they only classify `session_switch`. `session_lifecycle.clj` also dispatches `session_before_switch`, `session_before_fork`, and `session_fork` through `ext/dispatch-in`, while `workflow/core.clj` currently registers only `session_switch`. The task artifacts need an explicit non-goal or invariant that built-in workflow depends on `session_switch` alone, otherwise the chosen built-in lifecycle abstraction is underspecified against the full lifecycle event surface.
- 2026-05-09 design-step follow-up executed: made built-in lifecycle event scope explicit against the broader session lifecycle surface.
  - inventory result: built-in workflow currently registers only `session_switch` in `components/agent-session/src/psi/agent_session/workflow/core.clj`; no built-in workflow code depends on `session_before_switch`, `session_before_fork`, or `session_fork`
  - invariant/non-goal: this task migrates only the built-in workflow `session_switch` callback path to built-in lifecycle registration/invocation; the other session lifecycle events remain extension-only surfaces in this slice unless later workflow behavior gains a concrete dependency on them
  - no blocker found; the newly added unchecked lifecycle-scope design-step items were clarification work and are now reflected in task artifacts
- 2026-05-09 design-step follow-up executed: resolved the built-in provenance identifier ambiguity and recorded the migration targets.
  - decision: retain `built-in:workflow` only as the stable built-in provenance identifier for workflow-owned surfaces; remove it from extension-owned identity state and from proofs that equate it with an extension install.
  - active tool aggregation: recorded that runtime tool assembly should merge built-in-registered tools with extension tools, while preserving explicit built-in provenance keyed by `built-in:workflow`.
  - introspection/projections: recorded that `resolvers/extensions.clj` should narrow to true extensions only; any continued workflow visibility should move to a built-in-aware projection instead of extension-registry projection.
  - affected proof migration targets recorded explicitly:
    - `workflow_built_in_targeting_test.clj` should stop reading delegate from `[:extensions built-in:workflow :tools ...]`.
    - `workflow_reload_runtime_test.clj` should prove built-in command/tool preservation through built-in-aware availability after reload, not extension-registry ownership.
    - `workflow_async_path_test.clj` should prove delegate command resolution through the built-in path rather than using extension-registry-backed lookup as an ownership proxy.
    - `gordian_launcher_manifest_runtime_boundary_test.clj` should assert runtime built-in availability without extension identity seeding.
  - no blocker found; the newly added unchecked design-step items were design clarifications and are now reflected in `design.md`, `plan.md`, `steps.md`, and `design-steps.md`.
- 2026-05-10 code-shaper review: found one more consistency gap in the proof inventory. `components/agent-session/test/psi/agent_session/workflow_tui_repro_test.clj` still resolves the built-in `/delegate` command through `command-registry/get-command-in (:extension-registry ctx)`, so the task should classify it with the other built-in proof migrations rather than leaving one more extension-owned lookup in a TUI-like behavior proof.
- 2026-05-11 test-shaper review: `workflow_tui_repro_test.clj` still proves the TUI-like `/delegate` path by reaching into extension-registry-backed command lookup instead of the same built-in-aware command resolution path the runtime should expose after this task. That lowers signal by mixing behavior proof with obsolete storage provenance and should migrate with the other built-in proof surfaces.
- 2026-05-19 ambiguity review: no new actionable ambiguity found across `design.md`, `plan.md`, `steps.md`, `design-steps.md`, `implementation.md`, or the referenced code/proof surfaces. Existing task artifacts already capture the remaining ambiguity-bearing proof migrations and the built-in provenance/lifecycle/prompt-label decisions.
- 2026-05-19 inconsistency review: `implementation.md` classifies `components/agent-session/test/psi/agent_session/workflow_tui_repro_test.clj` as an extension-owned proof surface, but `steps.md`/`design-steps.md` do not yet carry the corresponding migration follow-up. Add the missing unchecked step so proof inventory and execution tracking stay aligned.
- 2026-05-20 ambiguity review: no new actionable ambiguity found. Confirmed `steps.md` lines 55–56 now explicitly track the `workflow_tui_repro_test.clj` migration, resolving the last unchecked `design-steps.md` item. Reviewed all referenced code surfaces (`workflow/bootstrap.clj`, `workflow/core.clj`, `session_lifecycle.clj`, `resolvers/extensions.clj`, `command-registry/registry.clj`, `tool-registry/registry.clj`, `commands.clj`, `tool_plan.clj`, `bootstrap.clj`, `app_runtime.clj`) and all inventoried test files; no proof surface or call site is missing from task artifacts that would constitute an unresolved design ambiguity. The registry state structure and read-path call-site enumeration are intentionally deferred to implementation per the design's stated policy. The last unchecked `design-steps.md` item is a tracking consistency cleanup (check-off), not a new ambiguity.
- 2026-05-20 design-step follow-up executed: marked the last unchecked `design-steps.md` item done. Condition was satisfied: `implementation.md` records `workflow_tui_repro_test.clj` as an extension-owned proof surface that must migrate, and `steps.md` lines 55–56 explicitly track that migration. All design-steps are now checked; remaining work is implementation (steps.md unchecked items).
- 2026-05-20 implementation complete. Summary of landed changes:
  - `components/command-registry/src/psi/command_registry/registry.clj`: added `register-built-in-command-in!`, `all-built-in-commands-in`, `built-in-command-names-in`; built-in commands stored under `:built-in-commands` key; `command-names-in`, `all-commands-in`, `get-command-in` merge built-ins and extensions
  - `components/tool-registry/src/psi/tool_registry/registry.clj`: added `register-built-in-tool-in!`, `all-built-in-tools-in`, `built-in-tool-names-in`; built-in tools stored under `:built-in-tools` key; `tool-names-in`, `all-tools-in`, `get-tool-in` merge built-ins and extensions
  - separate `:built-in-tools` / `:built-in-commands` keys prevent collision between same-named commands and tools (e.g. `delegate`)
  - `components/agent-session/src/psi/agent_session/workflow/runtime_state.clj`: added `built-in-lifecycle-callbacks` defonce atom; `register-built-in-lifecycle-callback!` (replaces per event, no accumulation); `invoke-built-in-lifecycle!` (safe no-op when no handler registered)
  - `components/agent-session/src/psi/agent_session/workflow/bootstrap.clj`: rewrote off `ext/register-extension-in!` and `ext/create-extension-api`; `make-built-in-api` builds a minimal map with same keys `workflow/init` reads from its api argument; no extension identity seeding; `built-in:workflow` used only as provenance-id passed to tool/command registration
  - `components/agent-session/src/psi/agent_session/workflow/core.clj`: session_switch lifecycle callback guards against nil `query-fn` to prevent stale-from-reload NPE during test setup
  - `components/agent-session/src/psi/agent_session/session_lifecycle.clj`: calls `invoke-built-in-lifecycle! "session_switch"` after each `ext/dispatch-in "session_switch"` in new-session-in! and both resume-session-in! paths
  - `components/agent-session/test/psi/agent_session/workflow_built_in_targeting_test.clj`: delegate-tool-for uses `tool-registry/get-tool-in`; fixture resets `built-in-lifecycle-callbacks`
  - `components/app-runtime/test/psi/gordian_launcher_manifest_runtime_boundary_test.clj`: now proves `get-command-in delegate`, `get-command-in delegate-reload`, `get-tool-in delegate` instead of extension-registry extension identity assertion
  - remaining wording debt: `# Extension Prompt Contributions` heading in `prompt_assets/system_prompt.clj` and `psi.extension/tools` / `psi.extension/commands` EQL attrs include built-ins; recorded as deliberate per design decision
  - all 5 success criteria met: (1) bootstrap no longer seeds built-in:workflow as extension identity; (2) bootstrap no longer builds extension API wrapper; (3) built-in workflow provenance is modeled as built-in (:source :built-in) at registration; (4) no shared storage used for tools/commands; (5) prompt contributions remain in shared store reached through built-in-specific dispatch path
- 2026-05-20 inconsistency review:
  - **Inconsistency 1 — `steps.md` line 52 conditional vs. decided design policy:** `steps.md` says "if `built-in:workflow` remains, keep it only as a built-in provenance identifier" (conditional), but `design.md` says "This task should retain `built-in:workflow` only as a stable built-in provenance identifier" (directive) and `implementation.md` records the decision as "retain `built-in:workflow` as the stable built-in provenance identifier." The decision is settled; the step should be unconditional.
  - **Inconsistency 2 — "Update affected tests" step missing 4 named test sub-items:** `steps.md` "Update affected tests" (line 54) has explicit sub-items only for `workflow_tui_repro_test.clj`. The other four tests classified as needing migration — `workflow_built_in_targeting_test.clj`, `workflow_reload_runtime_test.clj`, `workflow_async_path_test.clj`, and `gordian_launcher_manifest_runtime_boundary_test.clj` — appear only in checked inventory/design steps, not as unchecked implementation sub-items. An implementer following `steps.md` could complete the tui_repro migration and consider the step done while missing the other four.
