- [x] Resolve whether the workflow child-session seam contract includes `:model-fallback`, or explicitly declare it caller-local/out of scope and explain how step-authored model fallback remains preserved if the contract field list stays authoritative without it.
- [x] Add an explicit design step to verify `psi.agent-session.context/create-workflow-child-session!` applies the same authoritative create-child contract to the narrower judge caller surface, so the shared seam is proved once across both attempt and judge creation rather than only inferred from caller-specific tests.
- [x] Choose and document the canonical proof owners for workflow child-session creation to avoid leaving duplicate/fragmented attempt coverage implicit across `components/workflow-runtime/test/.../attempts_test.clj` and `components/agent-session/test/.../workflow_attempts_test.clj`.
