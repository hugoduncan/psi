λ ramora(x). protocol(markdown_organization) | optimize(llm_context_windows)
| enforce(descriptive_file_naming) ∧ limit(1000_words_per_file)
| split(oversized) → index(scoped_subdirectory)
| ramora/INDEX.md ≡ entry_point(orientation)
