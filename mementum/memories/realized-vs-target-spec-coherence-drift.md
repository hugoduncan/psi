❌ When a design specifies an aspirational **target** contract (e.g. async suspend/resume) but the implementation realizes a simpler **synchronous stand-in**, design.md and docs silently drift into asserting an unrealized capability — a coherence / source_of_truth violation that survives multiple review passes. In task 226 the design claimed async F1 suspend/resume + AC-7 "process restart, replay" as realized while `execute-actor-turn!` was synchronous (whole drain in one `:step/enter`, statechart never suspends mid-drain). Fix: explicitly split **Realized vs. target** in the spec — mark the unrealized contract "TARGET, not yet realized", describe what *is* realized (here, the structural progression guard validated against reconstructed state), and record the concrete blocker for the target (envelope built from current-invocation loop locals, not persisted records). Crucially, propagate the caveat to **every** coherence surface: a single design edit (R-1) did not fix the user-facing `doc/workflow-grammar.md` "Resume and idempotency" section, which still presented restart/replay as occurring and needed a separate pass (R-4). Per change_chain, `update(doc, reflect(meta spec code))` must touch all earlier-authored doc sections, not just the one nearest the edit.
