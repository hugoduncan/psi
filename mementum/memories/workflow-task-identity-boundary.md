🔁 Workflow prompts that create an artifact/task should not let downstream steps consume the creator's raw handoff as identity. Add an explicit extractor/validator step that accepts exactly one machine-readable path/id, yields only that value, and routes malformed handoffs to a terminal stop. Keep that extracted identity threaded through the entire task-consuming chain, including post-implementation validation/proof/final-summary steps and split terminal stops; the full handoff is context only. Task 218 added `extract-task-path`; task 220 caught the follow-on gap that proof/final paths also need content-locks to the extracted path. This prevents task-path guessing, stale prose parsing, and divergent task identity across delegates.