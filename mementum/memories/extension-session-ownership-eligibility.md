💡 Extension eligibility that depends on session topology must use the extension-safe session metadata/query contract, not direct atom reads or workflow internals. For auto-session-name, the reusable contract is: query parent session id plus workflow run/step/attempt ids and `workflow-owned?`; treat unresolved/empty/failed ownership queries conservatively as ineligible; keep the predicate local and cover both nullable extension fixtures and the real resolver projection. This preserves extension isolation while preventing delegated/workflow child sessions from triggering top-level-only behavior.
