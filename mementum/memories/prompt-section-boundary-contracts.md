🔁 When extending assembled text prompts, treat separators as a compatibility contract, not incidental formatting. Task 255’s command-local instructions interacted with newline-terminated output, newline-less output, truncated output, timeout results, multiline instructions, the next section, and the global trailer; small concatenation changes repeatedly broke blank-line boundaries while preserving semantic content. Normalize only at the new optional boundary, preserve legacy rendering when it is absent, and add exact adjacency assertions for each boundary variant. Ordering-only assertions are insufficient: test the literal output/footer/next-section and footer/global-trailer substrings, including absence of unintended extra blank lines.
