✅ Parser stress tests should assert deterministic bounded work, not wall-clock timeouts. Task 231 replaced `future`/`deref` timeout regressions for many textual-tool-call markers with parser-work instrumentation and monotonic marker scanning. This makes CI failures explain the violated parser contract (rescanning/quadratic work) instead of flaking under machine load. Use wall-clock only as a last-resort smoke guard; executable complexity contracts should count parser steps/advances where practical.