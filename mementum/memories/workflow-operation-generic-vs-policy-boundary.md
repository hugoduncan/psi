🔁 Deterministic workflow operations must expose generic mechanisms only; workflow-specific vocabulary (marker labels, allowed route tokens, topology, proof-artifact names) belongs in authored `.psi/workflows/*.edn` op `:args`, not in runtime code. Task 220 registered specialized `workflow/proof-sync-disposition-routing` / `validation-capture-disposition-routing` ops with hard-coded route vocabularies in `psi.agent-session.workflow.routing`; task 221 collapsed them into one parameterized `workflow/exact-marker-routing` taking `:marker-label` + `:allowed-routes` as authored args. Pattern: one generic operation + validate args up-front (tagged `:invalid-route-marker-args`, accumulate all field errors so callers fix every bad input from one diagnostic) + authored EDN supplies policy. Remove specialized ops immediately, no compat aliases — same-repo internal callers migrate in the same commit. Enforce with a boundary grep: generic runtime source contains no workflow-owned marker/route tokens; content-lock tests assert each workflow supplies expected labels/routes; route `:on` topology stays unchanged. Changed registered operation ids are user-visible via `/operations`, `/operation`, `psi-tool operation` → requires a CHANGELOG entry.
