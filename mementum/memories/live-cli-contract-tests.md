✅ Live CLI contract tests in psi should be optional, shape-focused, and availability-safe. Task 218's first `architecture-targets --edn` test only skipped on non-zero exit and only checked non-nil candidate id/type; review caught that missing `bb` could throw and that non-nil did not prove selector usability. Hardened pattern: wrap `shell/sh` in a helper that catches `IOException` and reports `:unavailable`; make unavailable/non-zero a non-failing skip; assert only stable envelope/interpretable schema (e.g. supported candidate type and id arity/tag), never repository-specific rankings, labels, scores, or counts.