🔁 A permanently red top-level test suite is not a harmless background condition: it weakens every later task's verification gate by forcing agents to compare failure sets manually instead of trusting exit status. When a stable pre-existing failure cluster is discovered during unrelated work, confirm it on a clean/stashed baseline, record the owning namespace/tests and reproduction, then open a dedicated Munera task to restore the green gate rather than normalizing carve-outs in each downstream task. Keep scope narrow: fix the cluster or document unchanged unrelated failures separately.