🔁 Make a resumable internal loop in the workflow runtime idempotent by re-reading **persisted per-prompt/per-item progression each iteration** from the canonical state atom (`get-in @state* (run-path run-id)`), never an in-memory counter. The loop selects the next un-run item from recorded turn records, so an item whose record already exists is never re-submitted and its non-deterministic effect (`ai/generate`) never re-fires — upholding the VSM `∀change → event → log → replayable` ethos. Record each item incrementally on success only (the aborting/failing item leaves no record; prior records are retained). Task 226's `drive-session-prompt-queue!` drained N prompts inside one statechart `:step/enter` action this way. Validate idempotency by re-driving against a **freshly reconstructed `state*`/`ctx`** (indices 0,1 pre-recorded ⇒ only index 2 fires; fully-recorded ⇒ zero turn-calls) — the same observable an async restart/replay would produce — rather than an occurring runtime restart, when the drain is still synchronous. Use a seam call-count (e.g. `:workflow-execute-actor-turn-fn`) as the no-re-fire observable.
