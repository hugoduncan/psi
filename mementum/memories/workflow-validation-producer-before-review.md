🔁 If a workflow review gate depends on generated evidence, make evidence production an explicit workflow-local step before the review chain. Do not assume `implement-task` will create validation artifacts as a side effect, and do not let review context pretend to inline large artifact contents. Task 218 inserted `validation-capture` after implementation to produce Gordian after/compare/gate EDN, write failure maps on unreadable/non-zero commands, commit task notes, and route failures back to implementation before any review-step gate could run. The architecture review skill then reads task-local artifact paths directly. Pattern: producer gate → failure repair loop → ordered reviews.