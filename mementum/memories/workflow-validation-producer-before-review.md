🔁 If a workflow review gate depends on generated evidence, make evidence production and validation an explicit workflow-local step before review. Do not trust command exit code alone: parse-check machine-readable artifacts after capture, because task 219's Gordian gate exited 0 while stdout EDN was truncated until the CLI flush bug was repaired. On non-zero or unreadable output, write a failure map, route back to implementation/repair, then recapture before review gates. Do not assume `implement-task` creates artifacts or let review context pretend to inline large artifact contents. Pattern: producer gate → parse-check/failure map → repair loop → ordered reviews.
