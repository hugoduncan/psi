🔁 Deterministic workflow route markers need line classification, not substring search. Exact-marker grammar should distinguish: exact marker line → route; malformed marker attempt (label at column 0 with bad colon/spacing/trailing prose) → malformed diagnostic; duplicate marker candidates → ambiguous with marker lines; ordinary prose merely mentioning the label → non-candidate/missing marker. Task 220 CS1-CS3 showed `str/includes?` coupled final-reply prose to route validation, then whitespace-before-colon escaped as ordinary prose. Keep pure parsers/classifiers in `psi.agent-session.workflow.routing`, leave `workflow.core` as registration/wiring, and test parser edge cases directly rather than through the broad live delegate harness.