🔁 Root-registry migrations can fail after the storage move is correct if higher read/introspection seams still consult legacy local state. Tasks 167/168 showed the dangerous gap: lower registry tests and primary API paths may pass while extension detail/projection helpers still read old extension-local maps. Future registry migrations need an explicit seam inventory plus at least one higher-surface coherence test and a full `bb test` pass before close.