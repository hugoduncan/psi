🎯 Model/provider compatibility quirks should be represented as explicit model capability data, not inferred from provider/model ids or transport names. Task 231 added `{:capabilities {:textual-tool-calls #{:xml}}}` so only opted-in local/custom models pay parsing risk/cost while provider-native/frontier models preserve normal behavior. Place pure capability helpers in a lower-level component (`components/ai`) so turn-runtime/provider-boundary code can consult the already-resolved runtime model without depending on `agent-session` or creating component cycles.