🔁 If a workflow final summary is reachable through multiple proof/verification predecessors, pass each possible predecessor's yield explicitly. Do not make the summarizer infer the final proof gate from topology, stale earlier prose, or committed artifact existence alone. Task 220 added a clean `proof-sync-fixed-point` route to final summaries, but initially only supplied `proof-sync` context; fixed-point-finalized runs would have had to infer the actual clean final gate. Add the fixed-point/source yield as prompt context and content-lock it, while still requiring the summary to read committed proof artifacts independently.