- 2026-06-07: Task 218 (`review-workflow-iteration-limits`) code-shaper review pass 2 found no new actionable feedback after CS1. Rechecked review workflow EDNs, deterministic routing operations, workflow-definition/runtime tests, docs, and CHANGELOG. Shape acceptable: shared guarded review-step loop, locally comprehensible design/plan full-pass topologies, constant-actor + routing-judge clarity-status, deterministic pass-level feedback memory. Focused verification green: review routing 11/82 and workflow-definition vars 87 assertions. PASS_STATUS REVIEW_COMPLETE.
- 2026-06-07: Task 218 (`review-workflow-iteration-limits`) code-shaper follow-up **CS1** executed. Simplified design/plan `clarity-status` invoke-step shape to constant actor + routing judge: actor uses `workflow/constant-routing` `DONE`; judge alone evaluates `workflow/pass-feedback-routing` over pass review outputs. Verification green: focused workflow-definition design/plan tests, runtime review routing namespace, targeted clj-kondo. PASS_STATUS FOLLOW_UPS_COMPLETE.
- 2026-06-07: Task 218 (`review-workflow-iteration-limits`) plan/steps follow-up **PA1** executed. Removed stale standalone clarity-status prompt workflows because design/plan review hosts now encode `clarity-status` as deterministic invoke routing instead of artifact re-reading; added workflow-definition regression coverage preventing those prompt files/references from returning; updated `doc/workflows.md`; marked PA1 done. Focused prompt-artifact Scry and targeted clj-kondo green. PASS_STATUS FOLLOW_UPS_COMPLETE.
- 2026-06-07: Task 218 (`review-workflow-iteration-limits`) design ambiguity follow-ups **AMB1–AMB3** executed. Design now pins max-iterations as total target-step entries including the initial entry, chooses 10 total review executions for implementation review, and specifies design/plan pass loops finish all phases before guarded loop-back with pass-level feedback memory and final-allowed-pass limit failure. PASS_STATUS FOLLOW_UPS_COMPLETE.
- 2026-06-07: Task 220 (`harden-simplification-workflow-proof-gates`) task-test-review follow-up **TT2** executed. Task-220 workflow-loader/content-lock identity tests now assert post-implementation validation/review/proof/final steps and split post-task terminal summaries consume the deterministic `extract-task-path` task identity rather than raw selection prose. Verification green: task-220 workflow-loader Scry 6/337, task-209/task-218/task-220 focused Scry 15/800, targeted clj-kondo clean, diff-check clean. PASS_STATUS FOLLOW_UPS_COMPLETE.
- 2026-06-07: Task 220 (`harden-simplification-workflow-proof-gates`) task-test-review pass 2 found actionable follow-up **TT2**. Task-220 identity tests lock extracted-path consumption for pre-implementation delegates/gates but not the post-implementation proof/final path (`review-task-implementation`, incidental validation/proof steps, final summaries, split post-task terminal summaries). Added TT2 to steps and committed review note. PASS_STATUS ACTIONABLE_FEEDBACK.
- 2026-06-07: Task 220 (`harden-simplification-workflow-proof-gates`) task-test-review follow-up **TT1** executed. Deterministic marker-routing operation tests now assert duplicate-marker diagnostics include exact duplicate lines via `:details :route-marker-lines` for both proof-sync and validation-capture routing operations. Verification green: operation+task-220 Scry 9/322, targeted clj-kondo clean, diff-check clean. PASS_STATUS FOLLOW_UPS_COMPLETE.
- 2026-06-07: Task 220 (`harden-simplification-workflow-proof-gates`) task-test-review found actionable follow-up **TT1**. Marker-routing operation tests prove duplicate markers return `:ambiguous-route-marker` but do not assert duplicate-marker error details include the duplicate marker lines, despite the design/plan diagnostic contract. Added TT1 to steps and committed review note. PASS_STATUS ACTIONABLE_FEEDBACK.
- 2026-06-07: Task 220 (`harden-simplification-workflow-proof-gates`) implementation review pass 2 found no new actionable feedback after IR1. Rechecked workflow EDNs, deterministic routing operations/tests, task-220 content locks, docs, and changelog; verification green: operation+task-220 Scry 9/320 and task-209/task-218/task-220 workflow-loader Scry 14/736. PASS_STATUS REVIEW_COMPLETE.
- 2026-06-07: Task 220 (`harden-simplification-workflow-proof-gates`) implementation review follow-up **IR1** executed. Both simplification workflow final-summary steps now source `proof-sync-fixed-point` yield alongside `proof-sync`, and task-220 content-lock tests prove final summaries receive clean fixed-point context. Verification green: task-220 workflow-loader Scry 5/273, task-209/task-218/task-220 focused Scry 14/736, targeted clj-kondo 0/0, EDN parse ok, diff-check clean. PASS_STATUS FOLLOW_UPS_COMPLETE.
- 2026-06-07: Task 220 (`harden-simplification-workflow-proof-gates`) implementation review found actionable follow-up **IR1**. Final-summary steps can be reached from clean `proof-sync-fixed-point` but do not include that yield as context, so fixed-point-finalized runs would have to infer the actual final proof gate. Added IR1 to steps and committed review note. PASS_STATUS ACTIONABLE_FEEDBACK.
- 2026-06-07: Task 220 (`harden-simplification-workflow-proof-gates`) Slice 7 complete. Updated user-facing workflow docs and CHANGELOG for hardened simplification proof gates; README required no edit because it already delegates workflow details to doc/workflows.md. Verification green: deterministic routing Scry 4/47, workflow-loader task-209/task-218/task-220 Scry 14/732, targeted clj-kondo 0/0, EDN parse ok, docs no-new-worktree/PR-claims grep ok, diff-check clean. PASS_STATUS IMPLEMENTATION_COMPLETE.
- 2026-06-07: Task 220 (`harden-simplification-workflow-proof-gates`) Slice 6 complete. Added focused workflow-loader/content-lock test namespace for both simplification workflows, locking deterministic task identity, proof/validation disposition operations and routes, proof-sync fixed-point topology, split terminal-stop contexts, mandatory coverage/proof artifacts, parse-check/failure-map wording, final-summary proof authority, and selector uncertainty contracts. Verification green: workflow-loader focused Scry 14/732, targeted clj-kondo 0/0, EDN parse ok, diff-check clean. PASS_STATUS MORE_WORK_REMAINS.
- 2026-06-07: Task 220 (`harden-simplification-workflow-proof-gates`) Slice 5 complete. Split terminal-stop/final-summary hardening completed for both simplification workflows: malformed task-path stops consume only select/extract context; stop-source prompts carry explicit failed-gate context and committed artifact obligations; incidental validation stop no longer includes unreachable proof-sync context; final summaries independently read committed proof artifacts and cannot claim proof coherence from review prose alone. Verification green: workflow-loader focused Scry 9/463, EDN parse ok, diff-check clean. PASS_STATUS MORE_WORK_REMAINS.
- 2026-06-07: Task 220 (`harden-simplification-workflow-proof-gates`) Slice 4 complete. Added proof-sync fixed-point topology to architecture and incidental simplification workflows: clean/no-op proof-sync can finalize; mutating proof-sync routes deterministically via `workflow/proof-sync-disposition-routing` to coverage review, validation recapture, or read-only fixed-point; terminal proof-sync stops are reachable only from fixed-point failure with proof-sync/fixed-point context. Verification green: workflow-loader focused Scry 9/463, targeted clj-kondo 0/0, EDN parse ok, diff-check clean. PASS_STATUS MORE_WORK_REMAINS.
- 2026-06-07: Task 220 (`harden-simplification-workflow-proof-gates`) Slice 3 complete. Architecture validation capture now parse-checks after-write EDN artifacts and routes fixable vs terminal failures through deterministic `workflow/validation-capture-disposition-routing`; incidental workflow now has `incidental-validation-capture` for parse-checked `after-local.json`, `incidental-burden-check.edn`, and `incidental-gate.edn`, with deterministic repair/terminal routing and content-lock tests. Verification green: workflow-loader focused Scry 9/426, targeted clj-kondo 0/0, EDN parse ok, diff-check clean. PASS_STATUS MORE_WORK_REMAINS.
- 2026-06-07: Task 220 (`harden-simplification-workflow-proof-gates`) Slice 2 complete. Added split terminal-stop prerequisite steps to both simplification workflows, deterministic incidental `extract-task-path` via `workflow/munera-open-task-path-routing`, downstream incidental task-identity threading from extracted path, mandatory first-writer `coverage-map.md` prompt contracts, parse-checked baseline/proof artifact naming, and selector uncertainty recording for architecture low-confidence / incidental marginal targets. Verification green: workflow-loader focused Scry 9/396, targeted clj-kondo 0/0, diff-check clean. PASS_STATUS MORE_WORK_REMAINS.
- 2026-06-07: Task 220 (`harden-simplification-workflow-proof-gates`) Slice 1 complete. Added registered deterministic operations `workflow/proof-sync-disposition-routing` and `workflow/validation-capture-disposition-routing` with exact whole-line marker parsing, tagged malformed/missing/duplicate/unsupported errors, and operation-level registration tests. Verification green: workflow delegate live Scry 4/47, targeted clj-kondo 0/0, diff-check clean. PASS_STATUS MORE_WORK_REMAINS.
- 2026-06-07: Task 220 (`harden-simplification-workflow-proof-gates`) plan/steps inconsistency follow-ups **PI1–PI3** executed. `plan.md`/`steps.md` now make architecture `coverage-map.md` first-writer/lifecycle mandatory, low-confidence architecture selector handling mandatory, and split terminal-stop definitions a Slice 2 topology prerequisite before any route targets them; topology slices now require workflow-loader/registry load verification, not parse-only. PASS_STATUS FOLLOW_UPS_COMPLETE.
- 2026-06-07: Task 220 (`harden-simplification-workflow-proof-gates`) plan/steps inconsistency review found actionable follow-ups **PI1–PI3**. Inconsistencies: architecture `coverage-map.md` required by proof-sync/final-summary without writer lifecycle, low-confidence architecture selector handling optionalized despite mandatory design/plan contract, and Slices 2-4 route to split terminal stops before Slice 5 creates them / before load checks. PASS_STATUS ACTIONABLE_FEEDBACK.
- 2026-06-07: Task 220 (`harden-simplification-workflow-proof-gates`) plan/steps ambiguity follow-ups **PA1–PA3** executed. `plan.md`/`steps.md` now pin exact proof-sync marker grammar (surrounding prose ok, exact marker line only), explicit `workflow/validation-capture-disposition-routing` for repair-vs-terminal validation failures, and incidental `select-and-create` as first writer for `coverage-map.md` plus lifecycle/test obligations. PASS_STATUS FOLLOW_UPS_COMPLETE.
- 2026-06-07: Task 220 (`harden-simplification-workflow-proof-gates`) plan/steps ambiguity review found actionable follow-ups **PA1–PA3**. Ambiguities: proof-sync route marker grammar/surrounding prose, validation-capture repair-vs-terminal deterministic routing, and incidental `coverage-map.md` first-writer/lifecycle. PASS_STATUS ACTIONABLE_FEEDBACK.
- 2026-06-07: Task 220 (`harden-simplification-workflow-proof-gates`) plan/steps created from stable design. Plan slices: preflight/deterministic proof-sync operation; incidental task identity and selector/proof generation contracts; parse-checked validation capture; proof-sync fixed-point topology; split terminal stops/final summaries; workflow-loader/content-lock tests; docs/changelog/verification. PASS_STATUS PLAN_CREATED.
- 2026-06-07: Task 220 (`harden-simplification-workflow-proof-gates`) inconsistency follow-ups **INC1–INC3** executed. `design.md` now keeps `proof-sync-fixed-point` read-only by moving blocking-note writes to mutating `proof-sync`, removes direct `PROOF_SYNC_ROUTE: TERMINAL` so proof-sync terminal stops pass through fixed-point, and names registered deterministic operation `workflow/proof-sync-disposition-routing` plus required operation/content-lock tests. PASS_STATUS FOLLOW_UPS_COMPLETE.
- 2026-06-07: Task 220 (`harden-simplification-workflow-proof-gates`) inconsistency design review found actionable follow-ups **INC1–INC3**. Inconsistencies: proof-sync-fixed-point read-only semantics conflict with committed blocking-note failure path, proof-sync terminal routing has both direct TERMINAL disposition and fixed-point-only terminal inputs, and proof-sync-disposition names an unspecified workflow-local routing operation inconsistent with current registered `:invoke` operation boundaries. PASS_STATUS ACTIONABLE_FEEDBACK.
- 2026-06-07: Task 220 (`harden-simplification-workflow-proof-gates`) ambiguity follow-ups **AMB1–AMB5** executed. `design.md` now requires mandatory incidental `coverage-map.md`, named parse-checked incidental proof artifacts (`after-local.json`, `incidental-burden-check.edn`, `incidental-gate.edn`), clean/no-op proof-sync finalization with mutating-pass disposition routing, split terminal-stop steps, and explicit selector uncertainty semantics. PASS_STATUS FOLLOW_UPS_COMPLETE.
- 2026-06-07: Task 220 (`harden-simplification-workflow-proof-gates`) ambiguity design review found actionable follow-ups **AMB1–AMB5**. Open ambiguities: incidental proof shape, incidental parse-checked after/gate artifact names, concrete proof-sync topology/routing, terminal-stop split-vs-envelope choice, and selector uncertainty semantics for low-confidence/marginal targets. PASS_STATUS ACTIONABLE_FEEDBACK.
- 2026-06-07: Task 220 (`harden-simplification-workflow-proof-gates`) architecture-fit design follow-ups **AR1–AR3** executed. `design.md` now pins terminal-stop failing-gate context to explicit workflow topology/data-flow, proof-sync to a clean/no-op fixed-point before final success, and committed task-local artifacts as proof authority over ephemeral workflow/review yields. PASS_STATUS FOLLOW_UPS_COMPLETE.
- 2026-06-07: Task 220 (`harden-simplification-workflow-proof-gates`) architecture-fit design review found actionable follow-ups **AR1–AR3**. Misfits: failing-gate terminal context must stay in explicit workflow data-flow/topology, proof-sync must verify a clean fixed point before final success rather than mutate then finalize, and committed task-local artifacts must be proof authority over ephemeral review yields. PASS_STATUS ACTIONABLE_FEEDBACK.
- 2026-06-07: Task 219 (`simplify-rpc-session-family`) code-shaper review pass 4 found no new actionable feedback after CS2. Rechecked code-shaper skill, task artifacts, coverage map, current RPC session family source, focused `/new` tests, prompt/navigation subset 13/146, pinned RPC suite 59/469, targeted clj-kondo 0/0, diff-check clean. PASS_STATUS REVIEW_COMPLETE.
- 2026-06-07: Task 219 (`simplify-rpc-session-family`) code-shaper follow-up **CS2** executed. `/new` command-result side effects now flow through one narrow `rpc.commands/handle-new-session-command-result!` helper owning the `:new-session` predicate, RPC focus movement, and rehydration emission order; payload/focus/emission internals are private. Verification green: prompt/navigation subset 13/146, pinned RPC suite 59/469, targeted clj-kondo 0/0, diff-check clean. PASS_STATUS FOLLOW_UPS_COMPLETE.
- 2026-06-07: Task 219 (`simplify-rpc-session-family`) code-shaper review pass 3 found actionable follow-up **CS2** after CS1. Remaining shape issue: command and prompt paths still duplicate the `/new` command-result invariant (`:new-session` check → focus movement → rehydration emission); added CS2 to collapse that sequence into one narrow RPC command helper and keep payload internals private where possible. PASS_STATUS ACTIONABLE_FEEDBACK.
- 2026-06-07: Task 219 (`simplify-rpc-session-family`) code-shaper follow-up **CS1** executed. Prompt-path `/new` now uses shared `rpc.commands/focus-new-session-command!` before rehydration emission, removing prompt-local `psi.rpc.events` dependency/manual rehydrate session-id extraction while preserving behaviour. Verification green: prompt-command 10/92, pinned RPC suite 59/469, targeted clj-kondo 0/0, diff-check clean. PASS_STATUS FOLLOW_UPS_COMPLETE.
- 2026-06-07: Task 219 (`simplify-rpc-session-family`) code-shaper review pass 2 found actionable follow-up **CS1**. `/new` rehydration helper shape is mostly narrow, but prompt still duplicates focus adaptation by manually extracting `[:rehydrate :session-id]` and calling `events/set-focus-session-id!`; added CS1 to route prompt through the shared command helper/focus seam. PASS_STATUS ACTIONABLE_FEEDBACK.
- 2026-06-07: Task 219 (`simplify-rpc-session-family`) docs review pass 2 found no new actionable feedback after TT1–TT4. Rechecked review-task-docs skill, task artifacts, final RPC source/test diff, coverage map, README, CHANGELOG, and relevant RPC docs; follow-ups added characterization coverage only, so no user-facing docs/changelog update is required. PASS_STATUS REVIEW_COMPLETE.
- 2026-06-07: Task 219 (`simplify-rpc-session-family`) test-shaper review pass 2 found no new actionable feedback after TT1–TT4. Rechecked test-shaper skill, task artifacts, coverage map, final RPC source/test diff, and focused RPC suite green 59/469; tests remain behavior/state-output focused, deterministic, real-harness based, and mock-free for logic. PASS_STATUS REVIEW_COMPLETE.
- 2026-06-07: Task 219 (`simplify-rpc-session-family`) post-TT review-implementation-architecture pass 2 found no new actionable feedback after TT1–TT4. Rechecked architecture skill, task artifacts, AGENTS/META/doc architecture sources, Gordian artifacts, source/test diff, and focused RPC suite green 59/469; gate remains pass with 0 new cycles/high/medium and target score 102.89 → 102.33. PASS_STATUS REVIEW_COMPLETE.
- 2026-06-07: Task 219 (`simplify-rpc-session-family`) task-test-review pass 6 found no new actionable feedback after TT4. Pinned focused RPC suite green 59/469; default/callback `/new` coverage now spans prompt-op and command-op paths, uses real RPC/session harness state/output assertions, and `coverage-map.md` is current. PASS_STATUS REVIEW_COMPLETE.
- 2026-06-07: Task 219 (`simplify-rpc-session-family`) task-test-review follow-up **TT4** executed. Refreshed `coverage-map.md` to record callback-backed command-op `/new` coverage via `psi.rpc-session-navigation-test/rpc-session-resume-and-rehydrate-events-test` and latest pinned focused RPC count 59/469; no code/tests/docs changed. PASS_STATUS FOLLOW_UPS_COMPLETE.
- 2026-06-07: Task 219 (`simplify-rpc-session-family`) task-test-review pass 5 found actionable follow-up **TT4**. Actual default/callback prompt-op and command-op `/new` tests are green and mock-free, but `coverage-map.md` is stale after TT3: it still reports 59/459 and omits callback-backed command-op `/new`; added TT4 to refresh the authoritative coverage proof. PASS_STATUS ACTIONABLE_FEEDBACK.
- 2026-06-07: Task 219 (`simplify-rpc-session-family`) task-test-review follow-up **TT3** executed. Added callback-backed command-op `/new` RPC coverage proving `run-command!` threads runtime `:on-new-session!` into slash resolution and emits callback-created rehydration/startup transcript/tool metadata, focus movement, accepted command response, and command-result output through the shared helper. Verification green: targeted navigation 1/42, pinned RPC suite 59/469, targeted clj-kondo 0/0, diff-check clean. PASS_STATUS FOLLOW_UPS_COMPLETE.
- 2026-06-07: Task 219 (`simplify-rpc-session-family`) task-test-review pass 4 found actionable follow-up **TT3**. Focused RPC suite green 59/459, targeted clj-kondo clean, diff-check clean; remaining gap is command-op `/new` with runtime `:on-new-session!` callback, because prompt `/new` and direct `new_session` callback tests do not prove `run-command!` threads the callback into slash resolution. PASS_STATUS ACTIONABLE_FEEDBACK.
- 2026-06-07: Task 219 (`simplify-rpc-session-family`) task-test-review follow-up **TT2** executed. Added callback-backed prompt-op `/new` RPC coverage proving runtime `:on-new-session!` callback source id, agent-loop suppression, callback-created rehydration, startup transcript/tool metadata, focus movement, and confirmation through the shared helper. Verification green: targeted TT2 1/10, pinned RPC suite 59/459, targeted clj-kondo 0/0, diff-check clean. PASS_STATUS FOLLOW_UPS_COMPLETE.
- 2026-06-07: Task 219 (`simplify-rpc-session-family`) task-test-review pass 3 found actionable follow-up **TT2**. Focused RPC suite green 58/449, but tests cover `new_session` callback payloads and default slash `/new` paths without locking callback-backed slash `/new`; add focused coverage proving shared helper emits callback-supplied startup transcript/tool metadata for slash `/new`. PASS_STATUS ACTIONABLE_FEEDBACK.
- 2026-06-07: Task 219 (`simplify-rpc-session-family`) task-test-review follow-up **TT1** executed. Added focused prompt-op `/new` RPC coverage through the existing prompt/session harness, proving agent-loop suppression plus rehydration/focus/session/footer outputs after the shared command-helper refactor. Verification green: targeted TT1 1/10, pinned RPC suite 58/449, targeted clj-kondo 0/0. PASS_STATUS FOLLOW_UPS_COMPLETE.
- 2026-06-07: Task 219 (`simplify-rpc-session-family`) post-repair task-test-review pass 2 found actionable follow-up **TT1**. Focused suite still green 57/439, but the final refactor changed the prompt-op `/new` slash-command rehydration path to use the shared command helper and current tests only lock command-op `/new` plus generic prompt command-result mapping; add focused prompt `/new` coverage for agent-loop suppression and rehydration/focus/snapshot outputs. PASS_STATUS ACTIONABLE_FEEDBACK.
- 2026-06-07: Task 219 (`simplify-rpc-session-family`) post-repair task-implementation-review pass 2 found no new actionable feedback. Rechecked task artifacts, final RPC source/test diff, validation recapture artifacts, and focused RPC suite; implementation remains target-local and behaviour-preserving, with parseable Gordian gate `:result :pass` and focused RPC Scry green 57/439. PASS_STATUS REVIEW_COMPLETE.
- 2026-06-07: Task 219 (`simplify-rpc-session-family`) REPAIR-VALIDATION-1 complete. Fixed local Gordian CLI stdout flushing before `System/exit` in `/Users/duncan/projects/hugoduncan/gordian/gordian-master` commit `42f1daa`, recaptured the exact architecture validation artifacts, and replaced `architecture-gate.edn` failure map with parseable raw EDN `:result :pass` (0 new cycles/high/medium). Focused RPC suite remains green 57/439. PASS_STATUS IMPLEMENTATION_COMPLETE.
- 2026-06-07: Task 219 (`simplify-rpc-session-family`) implementation complete. Shipped behaviour-preserving RPC session family simplification by centralizing `/new` command rehydration adaptation, preserving app-runtime ownership boundaries and target-local blast radius; review gates found no actionable follow-ups. Verification green: focused RPC Scry 57/439, targeted clj-kondo 0/0, `git diff --check` clean, Gordian gate pass with 0 new cycles/high/medium and target score 102.89 → 102.33. PASS_STATUS IMPLEMENTATION_COMPLETE.
- 2026-06-07: Task 219 (`simplify-rpc-session-family`) pre-simplification baseline/diff gate passed. Baseline `c513da4bb7e195956689cfa6455262f565e806ee` was clean for target/adjacent source areas; committed coverage-phase changes classified as characterization tests, task artifacts, and documentation/working-memory only; no source/refactor/testability seam changes; pinned RPC Scry green 57/439. PASS_STATUS REVIEW_COMPLETE.
- 2026-06-07: Task 219 (`simplify-rpc-session-family`) characterization-test-net review pass 3 found the pre-simplification net sufficient and green. Reviewed target clarity, `coverage-map.md`, `characterization-baseline.edn`, existing/new focused RPC tests; coverage spans command/results, picker/model/thinking/frontend actions, tree/resume/navigation, prompt/stream, projection/emit. Verification green: pinned RPC Scry 57/439. PASS_STATUS REVIEW_COMPLETE.
- 2026-06-07: Task 219 (`simplify-rpc-session-family`) plan/steps follow-ups **PI1–PI3** executed. Clean-baseline gate now requires `git status --short --branch` to show only branch header/no short-status entries before claiming baseline; new characterization namespaces must propagate to `plan.md`, `steps.md`, `coverage-map.md`, and `characterization-baseline.edn`; adjacent production edits require stop + design/plan sync with exact file, necessity, and narrow contract-alignment rationale. PASS_STATUS FOLLOW_UPS_COMPLETE.
- 2026-06-07: Task 219 (`simplify-rpc-session-family`) plan/steps inconsistency review found actionable follow-ups **PI1–PI3**. Inconsistencies: Slice 1 records but does not enforce clean pre-refactor baseline status; Slice 2 does not propagate newly added RPC characterization namespaces to the exact focused command/steps/`characterization-baseline.edn`; adjacent-source escape hatch in steps is weaker than design/plan and must require exact file, reason, narrow contract-alignment, and design sync before editing. PASS_STATUS ACTIONABLE_FEEDBACK.
- 2026-06-07: Task 219 (`simplify-rpc-session-family`) plan/steps follow-ups **PA1–PA3** executed. Focused RPC suite is pinned in `plan.md` to `bb clojure:test:scry --dir components/rpc/test` with eight RPC namespaces; authoritative coverage record is `coverage-map.md` with a Slice 2 template; Slice 6 review gates are pinned to `task-implementation-review`, `task-test-review`, `review-implementation-architecture`, `test-shaper`, `review-task-docs`, `code-shaper`. PASS_STATUS FOLLOW_UPS_COMPLETE.
- 2026-06-07: Task 219 (`simplify-rpc-session-family`) plan/steps ambiguity review found actionable follow-ups **PA1–PA3**. Ambiguities: focused RPC baseline command/namespace set not pinned, coverage map location/shape left as `implementation.md` or unnamed artifact, and Slice 6 review gates do not name exact architecture workflow skill sequence / `review-implementation-architecture`. PASS_STATUS ACTIONABLE_FEEDBACK.
- 2026-06-07: Task 219 (`simplify-rpc-session-family`) plan/steps created from stable design. Plan slices: preflight/clean baseline; coverage review and characterization gate; ownership seam selection; target-local architecture simplification; focused verification plus Gordian validation; review gates and closure. `plan.md` and `steps.md` committed with the plan creation commit. PASS_STATUS PLAN_CREATED.
- 2026-06-07: Task 219 (`simplify-rpc-session-family`) inconsistency design review found no new actionable feedback. Rechecked `design.md` against captured Gordian target/framing artifacts, shipped architecture workflow validation/review docs, and referenced RPC session source/test paths; selected membership, blast radius, hypothesis boundary, ownership constraints, pre-simplification gates, and `architecture-compare.edn` / `architecture-gate.edn` names are consistent after prior AR/AMB follow-ups. PASS_STATUS REVIEW_COMPLETE.
- 2026-06-07: Task 219 (`simplify-rpc-session-family`) design follow-up **AMB1** executed. `design.md` now uses `architecture-compare.edn` / `architecture-gate.edn` as the authoritative post-implementation compare/gate artifact names, matching the architecture workflow validation-capture and review-gate/docs contract. PASS_STATUS FOLLOW_UPS_COMPLETE.
- 2026-06-07: Task 219 (`simplify-rpc-session-family`) ambiguity design review found actionable follow-up **AMB1**. Design currently names post-validation outputs `compare.edn` / `gate.edn`, while the shipped architecture workflow/docs/review-gate contract uses `architecture-compare.edn` / `architecture-gate.edn`; design must choose one authoritative filename set before planning. PASS_STATUS ACTIONABLE_FEEDBACK.
- 2026-06-07: Task 219 (`simplify-rpc-session-family`) design follow-ups **AR1–AR2** executed. `design.md` now pins shared command/navigation/action/result semantics to either narrow RPC protocol adaptation or existing app-runtime ownership, forbids a new RPC-owned adapter-neutral semantic hub, and requires projection/stream/emit refactors to keep canonical state/app-runtime public models authoritative with RPC limited to subscriber-aware fanout/adaptation. PASS_STATUS FOLLOW_UPS_COMPLETE.
- 2026-06-07: Task 219 (`simplify-rpc-session-family`) architecture-fit design review found actionable follow-ups **AR1–AR2**. Design fits behaviour-preserving Gordian/test-net/gate direction, but must pin RPC/app-runtime ownership before planning: shared selector/navigation/picker/action/result semantics must not become a new RPC-owned domain hub, and projection/stream/emit simplification must preserve app-runtime/canonical-state authority with RPC as subscriber-aware protocol fanout/adaptation only. PASS_STATUS ACTIONABLE_FEEDBACK.
- 2026-06-07: Task 218 (`architecture-simplification-workflow`) code-shaper review found no new actionable feedback. Rechecked code-shaper skill, task artifacts, workflow EDN, architecture review skill, focused workflow-loader tests, docs/README/CHANGELOG, and review workflow wiring; shape acceptable with explicit orchestration, existing review-step reuse, extract-task-path boundary, validation-capture producer gate, and no adapter/shim complexity. Verification green: focused Scry 6/137, targeted clj-kondo 0/0, EDN read ok, diff check clean. PASS_STATUS REVIEW_COMPLETE.
- 2026-06-07: Task 218 (`architecture-simplification-workflow`) docs review found no new actionable feedback. Rechecked review-task-docs skill, task artifacts, workflow EDN, architecture review skill, focused tests, doc/workflows.md, README, and CHANGELOG; docs accurately cover invocation, invoking-worktree/no-PR boundary, architecture-vs-incidental distinction, test-net/validation/review sequence, and changelog entry. PASS_STATUS REVIEW_COMPLETE.
- 2026-06-07: Task 218 (`architecture-simplification-workflow`) test-shaper review found no new actionable feedback. Rechecked test-shaper skill, task artifacts, workflow EDN, dedicated architecture-review skill, focused workflow-loader tests, docs/README/CHANGELOG; verification green: focused Scry 6/137, targeted clj-kondo 0/0, diff check clean. PASS_STATUS REVIEW_COMPLETE.
- 2026-06-07: Task 218 (`architecture-simplification-workflow`) task-test-review found no new actionable feedback. Rechecked test skill, task artifacts, workflow EDN, dedicated architecture-review skill, focused workflow-loader tests, docs/README/CHANGELOG; verification green: focused Scry 6/137 and targeted clj-kondo 0/0. PASS_STATUS REVIEW_COMPLETE.
- 2026-06-07: Task 218 (`architecture-simplification-workflow`) implementation-review pass 2 found no new actionable feedback after IR1–IR2. Rechecked workflow/skill/tests/docs/changelog and verification stayed green: focused Scry 6/137, targeted clj-kondo 0/0, workflow EDN read ok, diff check clean. PASS_STATUS REVIEW_COMPLETE.
- 2026-06-07: Task 218 (`architecture-simplification-workflow`) implementation-review follow-ups **IR1–IR2** executed. Live `architecture-targets --edn` test now catches missing `bb`/Gordian as non-failing unavailable, includes deterministic unavailable-command coverage, and asserts selector-interpretable candidate id/type shapes for supported architecture candidate types without repository-specific ranking assertions. Verification green: focused Scry 6/137, targeted clj-kondo 0/0. PASS_STATUS FOLLOW_UPS_COMPLETE.
- 2026-06-07: Task 218 (`architecture-simplification-workflow`) implementation-review found actionable follow-ups **IR1–IR2**. Main workflow/skill/docs match the design, but live `architecture-targets --edn` test does not fully meet unavailable/shape contract: absent `bb` can throw instead of skip/fallback, and candidate id/type are only non-nil-checked rather than interpretable-shape-checked. Added IR1–IR2 to `steps.md`. PASS_STATUS ACTIONABLE_FEEDBACK.
- 2026-06-07: Task 218 (`architecture-simplification-workflow`) implementation complete. Shipped `reduce-architectural-complexity` workflow, `review-implementation-architecture` skill, workflow-loader/content-lock tests, docs, README pointer, and CHANGELOG entry. Final verification green: focused Scry 5/138, targeted clj-kondo 0/0, diff check clean. PASS_STATUS IMPLEMENTATION_COMPLETE.
- 2026-06-07: Task 218 (`architecture-simplification-workflow`) implementation Slice 5 complete. Added focused workflow-loader/content-lock tests for `reduce-architectural-complexity`, covering load/name, routing, prompt contracts, test-net, validation, six review gates, skill discovery, delegate co-load, and live `architecture-targets --edn` envelope shape. Verification green: focused Scry 5/138 and targeted clj-kondo 0/0. PASS_STATUS MORE_WORK_REMAINS.
- 2026-06-07: Task 218 (`architecture-simplification-workflow`) implementation Slice 4 complete. Added `implement-task` → `validation-capture` → six explicit `review-step` gates to `reduce-architectural-complexity`; validation writes after/compare/gate artifacts and routes failures back to implementation, while architecture review receives workflow-yield context only and reads Gordian artifacts from task-local files. Added final summary and verified workflow-loader load. PASS_STATUS MORE_WORK_REMAINS.
- 2026-06-07: Task 218 (`architecture-simplification-workflow`) implementation Slice 3 complete. Added pre-simplification test-net gates to `reduce-architectural-complexity`: clean baseline with `characterization-baseline.edn`, coverage review/disposition/fix loop, diff gate, and routing that makes `implement-task` unreachable until clean baseline, coverage, and diff gates pass. Workflow-loader parse/register verification green. PASS_STATUS MORE_WORK_REMAINS.
- 2026-06-07: Task 218 (`architecture-simplification-workflow`) implementation Slice 2 complete. Added `.psi/workflows/reduce-architectural-complexity.edn` through selection/task-generation shell: invoking-worktree `select-and-create`, top-level `:winner` / `:candidates` selector prompt, all-or-nothing pre-task capture, target-issues informational branch, generated task contract, `extract-task-path` identity boundary, design/plan delegates, malformed-handoff terminal stop, and loader parse/register verification. PASS_STATUS MORE_WORK_REMAINS.
- 2026-06-07: Task 218 (`architecture-simplification-workflow`) implementation Slice 1 complete. Added `.psi/skills/review-implementation-architecture/SKILL.md`, verified live `bb gordian architecture-targets --edn` top-level `:winner` / `:candidates` envelope and supported `target-issues --edn` for current family winner, and verified project skill discovery finds the new skill with no diagnostics. PASS_STATUS MORE_WORK_REMAINS.
- 2026-06-07: Task 218 (`architecture-simplification-workflow`) plan/steps inconsistency follow-ups **PI1–PI3** executed. Design/plan/steps now share one target-present topology (`select-and-create` DONE → `extract-task-path` → design review), `terminal-stop-summary` explicitly handles malformed extraction before any validated task path, and architecture review evidence uses workflow-yield context plus task-local file reads rather than artifact inlining. PASS_STATUS FOLLOW_UPS_COMPLETE.
- 2026-06-07: Task 218 (`architecture-simplification-workflow`) plan/steps inconsistency review found actionable follow-ups **PI1–PI3**. Inconsistencies: design success route still bypasses `extract-task-path`; malformed `extract-task-path` terminal-stop routing lacks a matching no-validated-task-path summary contract; architecture-review evidence delivery wording conflicts between context inlining and task-local file reads. Added PI1–PI3 to `steps.md`; appended review note. PASS_STATUS ACTIONABLE_FEEDBACK.
- 2026-06-07: Task 218 (`architecture-simplification-workflow`) plan/steps ambiguity follow-ups **PA1–PA4** executed. Plan/steps now pin all-or-nothing pre-task capture failure routing, explicit `extract-task-path` downstream task identity threading, architecture-review context-vs-file-read evidence mechanics, and fixture plus optional live narrow `architecture-targets --edn` envelope test shape. PASS_STATUS FOLLOW_UPS_COMPLETE.
- 2026-06-07: Task 218 (`architecture-simplification-workflow`) plan/steps ambiguity review found actionable follow-ups **PA1–PA4**. Ambiguities: required pre-task `before-diagnose.edn` capture failure routing, downstream generated-task path threading, architecture review evidence context mechanics, and live/fixture shape of the real `architecture-targets --edn` envelope test. Added PA1–PA4 to `steps.md`; appended review note. PASS_STATUS ACTIONABLE_FEEDBACK.
- 2026-06-07: Task 218 (`architecture-simplification-workflow`) plan/steps created from stable design. Plan slices: preflight/review skill; architecture workflow selection/task generation; pre-simplification test-net gates; validation-capture plus six review-step gates; workflow-loader/content-lock tests; docs/changelog/coherence. `plan.md` and `steps.md` committed in `6a08e988b`. PASS_STATUS PLAN_CREATED.
- 2026-06-07: Task 218 (`architecture-simplification-workflow`) design inconsistency follow-ups **INC1–INC3** executed. Design now consumes `bb gordian architecture-targets --edn` top-level `:winner` / `:candidates`, adds workflow-local `validation-capture` between `implement-task` and reviews, and requires tests to lock validation ordering plus all six post-implementation review-step gates. PASS_STATUS FOLLOW_UPS_COMPLETE.
- 2026-06-07: Task 218 (`architecture-simplification-workflow`) design inconsistency review found actionable follow-ups **INC1–INC3**. Inconsistencies: selector contract says `:architecture-target-ranking` / JSON while live/task-local EDN contract uses top-level `:winner` / `:candidates`; validation artifacts are required for review but no explicit post-implement producer/gate exists; acceptance-test bullet omits three of the six explicit post-implementation gates. Added INC1–INC3 to `design-steps.md`; appended review note. PASS_STATUS ACTIONABLE_FEEDBACK.
- 2026-06-07: Task 218 (`architecture-simplification-workflow`) ambiguity design follow-ups **AMB1–AMB5** executed. Design now pins selector routing (`ACTIONABLE_FEEDBACK` → `REPEAT` → done; `REVIEW_COMPLETE` → `DONE` → design review), candidate membership/source-area rules, task-local Gordian artifact filenames and root-relative paths, exact `review-implementation-architecture` skill path/value, and explicit post-implementation review-step sequence. PASS_STATUS FOLLOW_UPS_COMPLETE.
- 2026-06-07: Task 218 (`architecture-simplification-workflow`) ambiguity design review found actionable follow-ups **AMB1–AMB5**. Ambiguities: no-target selector routing token, candidate-type target/source membership for test-net gates, baseline/evidence artifact filenames/formats/root-relative paths, exact review skill identity/path, and precise post-implementation review sequence. Added AMB1–AMB5 to `design-steps.md`; appended review note. PASS_STATUS ACTIONABLE_FEEDBACK.
- 2026-06-07: Task 218 (`architecture-simplification-workflow`) design follow-up **AR1** executed. Design now anchors the implementation architecture review gate in existing `review-step` / `review-follow-up-steps` machinery, scopes it workflow-local to `reduce-architectural-complexity`, and forbids bespoke loops or generic `review-task-implementation` broadening without a separate decision. PASS_STATUS FOLLOW_UPS_COMPLETE.
- 2026-06-07: Task 218 (`architecture-simplification-workflow`) architecture-fit design review found actionable follow-up **AR1**. Design fits the Gordian architecture-target → generated Munera task → pre-simplification test-net → validation/review direction, but its new implementation architecture review gate needs anchoring in existing `review-step` / `review-follow-up-steps` review-loop architecture and must stay workflow-local rather than broadening generic implementation review by default. Added AR1 to `design-steps.md`; task artifacts committed `a90086f6f`. PASS_STATUS ACTIONABLE_FEEDBACK.
- 2026-06-07: Task 217 (`session-profiles-for-workflows`) code-shaper review pass 5 found no new actionable feedback after CS4. Rechecked neutral session-profile name/selection ownership, shared-config aliases, workflow loader/runtime boundaries, step resolution/deps, docs/changelog, and focused verification. Shape acceptable: deterministic workflow resolution consumes neutral snapshot selectors, and config IO/model-registry ownership stays at config/resolver/command boundaries. PASS_STATUS REVIEW_COMPLETE.
- 2026-06-07: Task 217 (`session-profiles-for-workflows`) code-shaper follow-up **CS4** executed. Pure resolved-profile selection now lives in neutral `psi.session-profile.selection/find-valid-profile`; shared-config aliases it for command/config callers; workflow-step-session-config consumes the neutral selector directly and no longer depends on shared-config. Verification green: focused shared-config, command, workflow-step session-profile tests and targeted clj-kondo. PASS_STATUS FOLLOW_UPS_COMPLETE.
- 2026-06-07: Task 217 (`session-profiles-for-workflows`) code-shaper review pass 4 found actionable follow-up **CS4**. After CS3, workflow step session-config resolution still depends on `psi.shared-config.session-profiles` solely for pure resolved-profile selection over a stored `:session-profile-snapshot`, pulling config IO/model-registry ownership into deterministic workflow resolution. Added CS4 to move that helper to neutral `psi.session-profile` ownership and drop the workflow-step-session-config -> shared-config dependency. PASS_STATUS ACTIONABLE_FEEDBACK.
- 2026-06-07: Task 217 (`session-profiles-for-workflows`) code-shaper follow-up **CS3** executed. Centralized complete selectable session-profile name validity in neutral `psi.session-profile.names`: reserved names now live there and `valid-profile-name?` excludes `:clear`; shared-config aliases that owner; workflow IR/compiler/markdown boundaries now reject session/delegate/markdown `:session-profile :clear` before run creation. Verification green: focused Scry suites and targeted clj-kondo. PASS_STATUS FOLLOW_UPS_COMPLETE.
- 2026-06-07: Task 217 (`session-profiles-for-workflows`) code-shaper review pass 3 found actionable follow-up **CS3**. After CS2, neutral `psi.session-profile.names/valid-profile-name?` enforces token syntax but not reserved-name exclusion, while shared-config rejects `:clear` separately; workflow IR/compiler/frontmatter can still accept `:session-profile :clear` and defer failure to step resolution. Added CS3 to centralize complete selectable-name validity and reject `:clear` before run creation. PASS_STATUS ACTIONABLE_FEEDBACK.
- 2026-06-07: Task 217 (`session-profiles-for-workflows`) code-shaper follow-up **CS2** executed. Moved selectable session-profile name grammar from `psi.workflow-registry.session-profile-names` to neutral component `psi/session-profile` (`psi.session-profile.names`), updated shared-config/workflow-loader/workflow-runtime callers, and removed the incidental `shared-config -> workflow-registry` dependency. Verification green: focused Scry suites and targeted clj-kondo. PASS_STATUS FOLLOW_UPS_COMPLETE.
- 2026-06-07: Task 217 (`session-profiles-for-workflows`) docs review pass 5 found no new actionable feedback after DOC4. Rechecked README, TUI/config/workflow docs, formal workflow grammar/IR docs, CHANGELOG, and representative implementation/tests. Docs now cover config shape/name rules, slash commands, workflow placement/precedence/snapshot/delegate semantics, and canonical IR grammar. PASS_STATUS REVIEW_COMPLETE.
- 2026-06-07: Task 217 (`session-profiles-for-workflows`) docs follow-up **DOC4** executed. Updated `doc/workflow-ir.md` suggested formal `session-spec` grammar to include canonical `:session-profile`, `:model`, and `:thinking-level`; marked DOC4 done. PASS_STATUS FOLLOW_UPS_COMPLETE.
- 2026-06-07: Task 217 (`session-profiles-for-workflows`) docs follow-up **DOC3** executed. Updated `doc/workflow-grammar.md` formal delegate grammar: `delegate-step` now includes `delegate-session-config-entry*`, and that nonterminal permits exactly compact `:session-profile`, `:model`, and `:thinking-level`; direct authored `:speed-mode`/`:effort-override` remain excluded while profile-derived speed/effort may flow through inherited-default shaping. PASS_STATUS FOLLOW_UPS_COMPLETE.
- 2026-06-07: Task 217 (`session-profiles-for-workflows`) docs review pass 3 found actionable follow-up **DOC3**. User-facing config/TUI/workflow/IR docs and CHANGELOG mostly match the feature after DOC2, but `doc/workflow-grammar.md` formal `delegate-step` production still omits compact delegate session config keys `:session-profile`, `:model`, and `:thinking-level`. Added unchecked DOC3 to steps and appended review note. PASS_STATUS ACTIONABLE_FEEDBACK.
- 2026-06-07: Task 217 (`session-profiles-for-workflows`) docs follow-up **DOC2** executed. Updated `doc/workflow-ir.md` delegate semantics: optional canonical `[:delegate :session]` now documents `:session-profile`, `:model`, and `:thinking-level`; it shapes delegated-run concrete `:inherited-defaults` and does not construct a delegate actor session. PASS_STATUS FOLLOW_UPS_COMPLETE.
- 2026-06-07: Task 217 (`session-profiles-for-workflows`) docs review found actionable follow-up **DOC1**. User-facing config/workflow/grammar docs and CHANGELOG cover the feature, but command discovery docs are stale: `doc/tui.md` omits `/session-profiles` and `/session-profile`, and README lacks a top-level session-profile pointer. Added unchecked DOC1 to steps and appended review note. PASS_STATUS ACTIONABLE_FEEDBACK.
- 2026-06-07: Task 217 (`session-profiles-for-workflows`) test-shaper review found no new actionable feedback after TT6. Rechecked task artifacts, session-profile profile/command/workflow runtime tests, docs/changelog, and focused verification. Test shape remains clear, behavior/state-focused, deterministic, and economical; no follow-up steps added. PASS_STATUS REVIEW_COMPLETE.
- 2026-06-07: Task 217 (`session-profiles-for-workflows`) task-test-review pass 6 found no new actionable feedback after TT6. Rechecked task artifacts, session-profile domain/command/workflow snapshot tests, docs/changelog, and focused verification. Existing coverage now locks config merge/validation, command parser and dispatch state safety, selected-profile lifecycle locality, grammar, snapshot/resume/delegate/mid-run determinism, invalid pre-child failures, and no-profile inheritance. PASS_STATUS REVIEW_COMPLETE.
- 2026-06-07: Task 217 (`session-profiles-for-workflows`) task-test-review pass 4 found actionable follow-up **TT5**. Existing tests cover snapshot capture, resume, delegate child snapshots, invalid pre-child failure, and selected-profile lifecycle locality, but do not directly lock a still-running top-level multi-step workflow resolving a later `:session-profile` step from the original run `:session-profile-snapshot` after mutable config edits. Added unchecked TT5 to steps and appended review note. PASS_STATUS ACTIONABLE_FEEDBACK.
- 2026-06-07: Task 217 (`session-profiles-for-workflows`) task-test-review follow-up **TT4** executed. Added lifecycle coverage proving selected-profile metadata remains session-local across new/fork/workflow-child sessions and cold journal resume while concrete settings inherit/restore only per existing lifecycle rules. Resolved profile model settings are now canonical session model maps, avoiding registry-only fields leaking into child/resume runtime creation. Verification green: focused command/shared-config/workflow-statechart Scry, workflow-step Kaocha, targeted clj-kondo. PASS_STATUS FOLLOW_UPS_COMPLETE.
- 2026-06-07: Task 217 (`session-profiles-for-workflows`) task-test-review pass 3 found actionable follow-up **TT4**. Existing tests prove selected-profile metadata storage/clear/non-journal entry, but do not lock session-local lifecycle semantics: new/fork/workflow-child sessions and cold resume must not inherit/restore `:selected-session-profile` metadata even when concrete model/thinking/speed/effort values inherit. Added unchecked TT4 to steps and appended review note. PASS_STATUS ACTIONABLE_FEEDBACK.
- 2026-06-07: Task 217 (`session-profiles-for-workflows`) task-test-review pass 2 found actionable follow-up **TT3**. Delegate/profile tests lock parent delegate `:inherited-defaults` projection but do not lock delegate runtime copying/deriving the parent `:session-profile-snapshot` into the child workflow run or callee profile resolution without config re-read. Added unchecked TT3 to steps and appended review note. PASS_STATUS ACTIONABLE_FEEDBACK.
- 2026-06-07: Task 217 (`session-profiles-for-workflows`) task-test-review follow-ups **TT1/TT2** executed. TT1 adds statechart/runtime coverage proving unknown/invalid workflow `:session-profile` failures are recorded before child execution-session creation, with nil `:execution-session-id`, no workflow-owned child session, and actionable failure data. TT2 removes `commands_session_profile_test.clj` `with-redefs` of `user-config/user-config-file`; command/resolver tests now use scoped temp `user.home` real config files. Verification green: focused command/statechart Scry, targeted clj-kondo 0/0. PASS_STATUS FOLLOW_UPS_COMPLETE.
- 2026-06-07: Task 217 (`session-profiles-for-workflows`) task-test-review found actionable follow-ups **TT1/TT2**. TT1: invalid workflow-profile tests stop at resolver exceptions and do not prove the runtime/statechart creates no child execution session. TT2: session-profile command tests stub `user-config/user-config-file` with `with-redefs` instead of using an injectable/nullable config-location seam or scoped real config location. Added unchecked TT1/TT2 to steps and appended review note. PASS_STATUS ACTIONABLE_FEEDBACK.
- 2026-06-07: Task 217 (`session-profiles-for-workflows`) implementation-review pass 7 found no new actionable feedback after IR8. Rechecked task artifacts, session profile resolver/commands, workflow snapshot/step-resolution/delegate paths, grammar/docs/changelog. Verification green: focused shared-config, command, workflow-loader, canonical snapshot/workflow-tools, workflow-step session-profile/inheritance suites; targeted clj-kondo 0/0. PASS_STATUS REVIEW_COMPLETE.
- 2026-06-07: Task 217 (`session-profiles-for-workflows`) implementation-review follow-up **IR8** executed. `/session-profile :clear` now fails as globally reserved/unavailable even when config does not define `:clear`: reserved-name lookup precedes profile-map presence and synthesizes the reserved invalid-profile diagnostic. Command coverage proves configured and absent `:clear` leave state unchanged and report reserved, not unknown. Verification green: shared-config + command Scry suites, targeted clj-kondo 0/0. PASS_STATUS FOLLOW_UPS_COMPLETE.
- 2026-06-07: Task 217 (`session-profiles-for-workflows`) implementation-review pass 6 found actionable follow-up **IR8**. `/session-profile :clear` currently reports the reserved-profile diagnostic only when config contains a `:clear` entry; with no configured `:clear`, it normalizes to the reserved keyword but falls through as unknown instead of globally reserved/unavailable. Added IR8 to task steps. PASS_STATUS ACTIONABLE_FEEDBACK.
- 2026-06-06: Task 217 (`session-profiles-for-workflows`) implementation-review follow-up **IR7** executed. Profile-name validation now fully matches `/session-profile` token grammar: unqualified keyword names must be command-selectable (`[A-Za-z0-9][A-Za-z0-9._-]*`), so `:fast+coding` is invalid with actionable diagnostics. `/session-profiles`, Pathom create-run snapshots, and psi-tool workflow snapshots surface it as invalid/unavailable; docs and CHANGELOG updated. Verification green: focused shared-config, command, Pathom snapshot, and psi-tool workflow Scry suites; targeted clj-kondo 0/0. PASS_STATUS FOLLOW_UPS_COMPLETE.
- 2026-06-06: Task 217 (`session-profiles-for-workflows`) implementation-review pass 5 found actionable follow-up **IR7**. Profile names with command-unparseable characters (for example `:fast+coding`) are still accepted as valid by profile resolution/snapshots but cannot be selected by `/session-profile`, creating another listed-valid/unselectable profile class after IR6. Added IR7 to task steps. PASS_STATUS ACTIONABLE_FEEDBACK.
- 2026-06-06: Task 217 (`session-profiles-for-workflows`) implementation-review follow-up **IR6** executed. Namespaced keyword profile names now align with command grammar: profile validation requires unqualified keywords, `:team/coding` is invalid with actionable diagnostics, `/session-profiles` displays `team/coding` as unavailable rather than selectable `coding`, and workflow snapshots retain it under invalid profile names. Verification green: focused shared-config, command, workflow snapshot, psi-tool workflow Scry suites; targeted clj-kondo 0/0. PASS_STATUS FOLLOW_UPS_COMPLETE.
- 2026-06-06: Task 217 (`session-profiles-for-workflows`) implementation-review pass 4 found actionable follow-up **IR6**. Namespaced keyword profile names (for example `:team/coding`) are accepted as valid by profile resolution/snapshots but command parsing rejects them and `/session-profiles` displays only the name part, leaving a misleading/unselectable valid profile. Added IR6 to task steps. PASS_STATUS ACTIONABLE_FEEDBACK.
- 2026-06-06: Task 217 (`session-profiles-for-workflows`) implementation-review follow-up **IR5** executed. Fixed mixed/non-keyword session-profile config names by replacing pre-validation plain `sorted-map` construction with deterministic heterogeneous-key ordering; non-keyword names now surface `:invalid-profile-name` diagnostics through `/session-profiles`, EQL/session snapshots, and workflow create-run snapshots instead of throwing. Verification green: focused shared-config, command, workflow snapshot, psi-tool workflow, workflow IR Scry suites; targeted clj-kondo 0/0. PASS_STATUS FOLLOW_UPS_COMPLETE.
- 2026-06-06: Task 217 (`session-profiles-for-workflows`) implementation-review pass 3 found actionable follow-up **IR5**. Non-keyword/mixed profile names in config currently crash `resolve-profiles` during plain `sorted-map` construction before invalid-profile-name diagnostics can be surfaced by `/session-profiles`, EQL snapshot, or workflow create-run. Added IR5 to task steps. PASS_STATUS ACTIONABLE_FEEDBACK.
- 2026-06-06: Task 217 (`session-profiles-for-workflows`) implementation-review follow-up **IR4** executed. Split LLM judge session validation from general session-step validation so canonical `[:judge :session :session-profile]` is rejected, added focused grammar coverage for that canonical placement, and kept existing judge model/thinking/tools fields valid. Verification green: session-profile grammar Scry, workflow-runtime IR focused test, target-IR compiler Scry suites, targeted clj-kondo 0/0. PASS_STATUS FOLLOW_UPS_COMPLETE.
- 2026-06-06: Task 217 (`session-profiles-for-workflows`) implementation-review follow-ups **IR1–IR3** executed. Fixed canonical session-step IR validation for direct `:thinking-level`, restored task-207 no-profile delegate speed/effort fallback by carrying internal profile-derived field provenance through effective config, and repaired invalid-profile test diagnostics shadowing. Verification green: focused session-profile, inheritance-snapshot, workflow-loader session-profile grammar, workflow-runtime IR tests; targeted clj-kondo 0/0; full unit suite green; file-length check green. PASS_STATUS FOLLOW_UPS_COMPLETE.
- 2026-06-06: Task 217 (`session-profiles-for-workflows`) implementation complete. Added workflow-run `:session-profile-snapshot` schema/storage, Pathom and psi-tool top-level snapshot capture, delegate snapshot copying, profile-aware step/delegate session-config resolution, invalid/unknown profile blocking before child-session creation, explicit-step override precedence, profile-derived speed/effort projection into child inherited defaults, and presence-aware nil effort propagation through attempt/dispatch/child-session creation. Updated configuration/workflow/grammar docs and CHANGELOG. Verification green: clj-paren-repair success, targeted clj-kondo 0/0, full unit Scry/Kaocha suite, file-length check. PASS_STATUS IMPLEMENTATION_COMPLETE.
- 2026-06-06: Task 217 (`session-profiles-for-workflows`) implementation Slice 3 complete. Added compact workflow `:session-profile` grammar for session steps, delegate steps at canonical `[:delegate :session]`, and markdown frontmatter; canonical workflow IR now validates profile fields and rejects unsupported nested/invoke/judge/direct speed-effort placements via closed step/session/judge schemas; focused grammar/parser/compiler/IR tests and checked-in workflow migration tests green; targeted clj-kondo 0/0. PASS_STATUS MORE_WORK_REMAINS.
- 2026-06-06: Task 217 (`session-profiles-for-workflows`) implementation Slice 2 complete. Added live `/session-profiles` and `/session-profile` backend command surface, selected-profile metadata, session-owned apply/clear dispatch handlers, EQL profile/snapshot/selected-profile resolvers, token normalization and atomic invalid-selection behavior, plus mock-free command/resolver tests. Verification green: shared-config session-profiles Scry 4/29; commands-session-profile Scry 5/36; commands-builtin-specs Scry 12/59; builtin-commands-resolver Scry 3/26; commands-speed-effort Scry 2/40; model-dispatch Scry 12/153; targeted clj-kondo 0/0; dispatch architecture parity pass; file-length check green. PASS_STATUS MORE_WORK_REMAINS.
- 2026-06-06: Task 217 (`session-profiles-for-workflows`) implementation Slice 1 complete. Added `psi.shared-config.session-profiles` profile domain/config resolver with supported-field filtering, reserved `:clear`, profile-specific user/project-shared/project-local deep merge, model-registry validation/materialization, diagnostics/readable settings, snapshot and selection helpers; added mock-free temp-file + real registry tests. Verification green: session-profiles Scry 4/29, shared-config resolution Scry 7/31, shared-config clj-kondo 0/0. PASS_STATUS MORE_WORK_REMAINS.
- 2026-06-06: Task 217 (`session-profiles-for-workflows`) plan/steps inconsistency follow-ups **PI1–PI4** executed. PI1 pins delegate canonical session-config to optional `[:delegate :session]` with schema/compiler/normalization/validation preservation obligations; PI2 carries explicit nil effort by key presence across snapshots/resolver/projection/child creation; PI3 adds the command-token normalizer/test matrix; PI4 names Pathom, psi-tool, pure create-run, and delegate snapshot-copy boundaries. Updated `plan.md`, `steps.md`, and `implementation.md`; no code/tests/docs changed because follow-ups refine pre-implementation obligations. PASS_STATUS FOLLOW_UPS_COMPLETE.
- 2026-06-06: Task 217 (`session-profiles-for-workflows`) plan/steps inconsistency review found actionable follow-ups **PI1–PI4**. Inconsistencies: PA1 delegate `[:delegate :session]` path not yet matched by current IR/runtime schemas; PA2 nil effort-clear needs presence-aware semantics through snapshots/resolver/child creation, not only validation/projection; PA3 command normalization lacks concrete command test/work items; PA5 snapshot-capture boundary lacks explicit Pathom/psi-tool/pure-create-run/delegate step coverage. Added unchecked PI1–PI4 to `steps.md`; appended review note. PASS_STATUS ACTIONABLE_FEEDBACK.
- 2026-06-06: Task 217 (`session-profiles-for-workflows`) plan/steps ambiguity follow-ups **PA1–PA5** executed. PA1 pins delegate canonical profile config to `[:delegate :session :session-profile]`; PA2 chooses explicit `:effort-override nil` as valid concrete effort-clear keyed by presence; PA3 accepts bare or EDN-style profile tokens with raw `clear` as action and `:clear` reserved; PA4 specifies validate-first atomic live application with model-before-thinking clamping/journaling semantics; PA5 pins profile snapshot capture to impure top-level workflow invocation boundaries and delegated copy/derive without config reads. Updated `plan.md`, `steps.md`, and `implementation.md`; no code/tests/docs changed. PASS_STATUS FOLLOW_UPS_COMPLETE.
- 2026-06-06: Task 217 (`session-profiles-for-workflows`) plan/steps ambiguity review found actionable follow-ups **PA1–PA5**. Ambiguities: delegate profile IR path/consumers unspecified; `:effort-override nil` valid-clear vs absent unclear; `/session-profile` token normalization unclear; model+thinking application/clamping order unclear; workflow profile snapshot capture boundary unclear. Added unchecked PA1–PA5 to `steps.md`; appended review note. PASS_STATUS ACTIONABLE_FEEDBACK.
- 2026-06-06: Task 217 (`session-profiles-for-workflows`) design inconsistency follow-ups **INC1–INC3** executed. Reserved `:clear` as unavailable for profiles; narrowed direct authored workflow overrides to currently supported `:model`/`:thinking-level` while preserving profile-derived speed/effort; specified delegate projection so profile-derived speed/effort in effective config outrank parent snapshot values, otherwise task-207 fallback remains. PASS_STATUS FOLLOW_UPS_COMPLETE.
- 2026-06-06: Task 217 (`session-profiles-for-workflows`) design inconsistency review found actionable follow-ups **INC1–INC3**. Inconsistencies: open keyword profile names conflict with reserved `/session-profile clear`; direct workflow `:speed-mode`/`:effort-override` support in design conflicts with current grammar/compiler artifacts; delegate profile speed/effort propagation conflicts with task-207 `effective-config->snapshot` sourcing speed/effort only from the parent snapshot. Added unchecked INC1–INC3 to `design-steps.md`; appended review note. PASS_STATUS ACTIONABLE_FEEDBACK.
- 2026-06-06: Task 217 (`session-profiles-for-workflows`) design ambiguity follow-ups **AMB1–AMB5** executed. Chose profile-specific deep merge only for `:agent-session :session-profiles`; limited supported fields to model provider/id, thinking, speed, effort; specified atomic invalid-profile behavior and diagnostics; pinned live-session persistence (model/thinking existing journal, speed/effort transient, selected-profile metadata non-journaled/non-inherited); and scoped workflow grammar to compact top-level `:session-profile` on session/delegate steps plus markdown frontmatter, excluding nested session maps, invoke, and LLM judge. PASS_STATUS FOLLOW_UPS_COMPLETE.
- 2026-06-06: Task 217 (`session-profiles-for-workflows`) design ambiguity review found actionable follow-ups **AMB1–AMB5**. Ambiguities: profile deep-merge ownership vs current config merge shape; exact supported profile key set; invalid/partial profile resolution behavior; live-session journaling/resume/inheritance semantics for apply/clear; workflow grammar placement/applicability and canonical IR storage for `:session-profile`. Added unchecked AMB1–AMB5 to `design-steps.md`; appended review note. PASS_STATUS ACTIONABLE_FEEDBACK.
- 2026-06-06: Task 217 (`session-profiles-for-workflows`) design architecture follow-ups **AR1/AR2** executed. AR1 pins `/session-profile` and `/session-profiles` to backend single-source built-in command specs, existing slash dispatch, session-owned mutation/dispatch state changes, and EQL resolver reads; adapters must not carry local profile lists/caches or direct atom writes. AR2 separates canonical workflow-run session-profile snapshots from narrow task-207 concrete `:inherited-defaults`: top-level/delegated runs resolve profile names from immutable workflow-run profile snapshots, while delegate inheritance passes only profile-derived concrete model/thinking/speed/effort defaults through `:inherited-defaults`. Marked design-steps AR1/AR2 done. PASS_STATUS FOLLOW_UPS_COMPLETE.
- 2026-06-06: Task 216 (`extract-task-knowledge-workflow`) code-shaper review pass 3 found no new actionable feedback after CS2. Rechecked task artifacts, extraction workflow, task-lifecycle and worktree wrapper wiring, workflow-loader tests, docs/changelog, and AGENTS/mementum exception. Shape remains simple/consistent/robust; verification green: focused workflow definitions Scry 12/226, task-209 workflow definitions Scry 3/217, clj-kondo 0/0, EDN read checks ok. PASS_STATUS REVIEW_COMPLETE.
- 2026-06-06: Task 216 (`extract-task-knowledge-workflow`) code-shaper follow-up **CS2** executed. Extraction prompt now requires `git status --short`, explicit pathspec staging/commit of extraction-owned `mementum/memories/` / `mementum/knowledge/` changes only, never `git add .` / `git add -A`, leaving unrelated dirt untouched and blocking/reporting if isolation is unsafe. Workflow-loader content locks updated; focused Scry workflow-definitions green (12/226), clj-kondo 0/0. PASS_STATUS FOLLOW_UPS_COMPLETE.
- 2026-06-06: Task 216 (`extract-task-knowledge-workflow`) code-shaper review pass 2 found actionable follow-up **CS2**. Prompt-as-code currently says to commit mementum writes but does not constrain staging/commit scope to extraction-owned `mementum/memories/` / `mementum/knowledge/` paths; a lifecycle-end `git add .` could sweep unrelated dirty worktree changes into a memory commit. Added CS2 to tighten prompt/tests for mementum-path-only staging/commit and leaving unrelated dirt untouched. PASS_STATUS ACTIONABLE_FEEDBACK.
- 2026-06-06: Task 216 (`extract-task-knowledge-workflow`) code-shaper follow-up **CS1** executed. Updated `task-lifecycle-in-worktree.edn` positive-path wrapper summary/tests to report design → plan → implement → review → extract knowledge and the extraction result preserved by the inner lifecycle yield; NO_TARGET branch unchanged. Focused task-209 workflow definitions Scry green (3/217), clj-kondo 0/0, EDN read ok. PASS_STATUS FOLLOW_UPS_COMPLETE.
- 2026-06-06: Task 216 (`extract-task-knowledge-workflow`) code-shaper review found actionable follow-up **CS1**. `task-lifecycle-in-worktree.edn` wrapper summary/tests still describe the inner lifecycle as design → plan → implement → review only, omitting the new extract-knowledge terminal stage; added CS1 to update wrapper summary contract/tests while preserving NO_TARGET. PASS_STATUS ACTIONABLE_FEEDBACK.
- 2026-06-06: Task 216 (`extract-task-knowledge-workflow`) docs review pass 3 found no new actionable feedback. Rechecked review-task-docs skill, task artifacts, extraction workflow, task-lifecycle wiring, workflow-loader tests, `doc/workflows.md`, `CHANGELOG.md`, and README workflow surface. Docs are coherent after DOC1/DOC2; no unchecked follow-ups added. PASS_STATUS REVIEW_COMPLETE.
- 2026-06-06: Task 216 (`extract-task-knowledge-workflow`) docs follow-up **DOC2** executed. Replaced the standalone `doc/workflows.md` usage example that named the in-flight open task with the generic completed-task placeholder `{NNN-slug}`, aligning the example with the closed-task-only standalone boundary; marked DOC2 done. PASS_STATUS FOLLOW_UPS_COMPLETE.
- 2026-06-06: Task 216 (`extract-task-knowledge-workflow`) docs review pass 2 found actionable follow-up **DOC2**. `doc/workflows.md` standalone usage example names the current in-flight open task `216-extract-task-knowledge-workflow`, conflicting with the documented closed-task-only standalone boundary; added DOC2 to replace it with a generic completed-task placeholder or clearly closed-task example. PASS_STATUS ACTIONABLE_FEEDBACK.
- 2026-06-06: Task 216 (`extract-task-knowledge-workflow`) docs follow-up **DOC1** executed. Updated CHANGELOG `[Unreleased]` `task-lifecycle` Added entry to include the shipped extraction stage / six delegates, matching the Changed entry; marked DOC1 done; no unchecked steps remain. PASS_STATUS FOLLOW_UPS_COMPLETE.
- 2026-06-06: Task 216 (`extract-task-knowledge-workflow`) test-shaper review found no new actionable feedback. Reviewed task artifacts plus extraction workflow, task-lifecycle wiring, workflow-loader tests, docs, and changelog. Test shape is acceptable: behavioral content-lock groups are focused, source-wiring assertions distinguish input/original/dedicated review-yield, lifecycle wiring checks are narrow, and no redundant/flaky setup was found. Verification green: focused workflow definitions Scry 12/218, clj-kondo 0/0, `task-lifecycle.edn` read ok. PASS_STATUS REVIEW_COMPLETE.
- 2026-06-06: Task 216 (`extract-task-knowledge-workflow`) task-test-review pass 3 found no new actionable feedback after TT3. Rechecked task artifacts plus extraction workflow, task-lifecycle wiring, workflow-loader tests, docs, and changelog. Tests now lock loading/no sibling, tools, source wiring, slug/path and open/closed authorization, required artifact reads, history/dedupe/filter/zero-extraction, persistence/write conventions, summary preservation, and lifecycle delegate wiring. Verification green: focused workflow definitions Scry 12/218, clj-kondo 0/0, `task-lifecycle.edn` read ok. PASS_STATUS REVIEW_COMPLETE.
- 2026-06-06: Task 216 (`extract-task-knowledge-workflow`) task-test-review follow-up **TT3** executed. Added workflow-loader prompt content-lock coverage requiring the extraction workflow to read `design.md`, `plan.md`, `steps.md`, and `implementation.md` from the resolved task before mining knowledge; tightened prompt wording to state the before-mining/resolved-task boundary. Focused Scry green (12 tests/218 assertions); clj-kondo 0/0. PASS_STATUS FOLLOW_UPS_COMPLETE.
- 2026-06-06: Task 216 (`extract-task-knowledge-workflow`) task-test-review follow-ups **TT1/TT2** executed. Added workflow-loader content-locks for autonomous persistence/write contract (no approval, memory/knowledge output shapes, frontmatter/symbol/one-insight constraints, commit conventions, no commit on no changes) and final-summary/reporting contract (resolved path, authorization mode, extracted/updated/skipped entries, zero-extraction success, preservation of dedicated implementation-review yield). Focused Scry green (12 tests/213 assertions); clj-kondo 0/0. PASS_STATUS FOLLOW_UPS_COMPLETE.
- 2026-06-06: Task 216 (`extract-task-knowledge-workflow`) task-test-review found actionable follow-ups **TT1/TT2**. Existing workflow-loader tests cover loading/tools/source wiring/eligibility/history/dedupe/filter basics/lifecycle wiring and focused Scry is green (12/195), but do not lock the autonomous persistence/write contract or final-summary preservation contract. Added TT1/TT2 to `steps.md`; appended review note. PASS_STATUS ACTIONABLE_FEEDBACK.
- 2026-06-06: Task 216 (`extract-task-knowledge-workflow`) implementation-review pass 3 found no new actionable feedback after IR2. Rechecked task artifacts plus `.psi/workflows/extract-task-knowledge.md`, `.psi/workflows/task-lifecycle.edn`, workflow-loader tests, docs/changelog, AGENTS/mementum protocol exception, and delegate source-resolution semantics. Verification green: focused workflow definitions Scry 12/195, clj-kondo 0/0, `task-lifecycle.edn` read ok. PASS_STATUS REVIEW_COMPLETE.
- 2026-06-06: Task 216 (`extract-task-knowledge-workflow`) implementation-review follow-up **IR2** executed. Synchronized source-distinction wording after IR1: `design.md`, `doc/workflows.md`, and `steps.md` now say only the labeled `:implementation-review-yield` prompt-string field rendered as `{{implementation_review_yield}}` authorizes lifecycle open-task extraction; `{{original}}` / `:workflow-original` is ambient and non-authorizing. Historical append-only `implementation.md` PA notes remain superseded by the new IR2 note. PASS_STATUS FOLLOW_UPS_COMPLETE.
- 2026-06-06: Task 216 (`extract-task-knowledge-workflow`) implementation-review pass 2 found actionable follow-up **IR2**. IR1 fixed runtime prompt-visible authorization by labeling `:implementation-review-yield`, but cross-artifact wording in `design.md`, `doc/workflows.md`, and stale PA2/PA3 notes still describes the review output as generic context/`{{original}}`; added IR2 to synchronize those artifacts so `{{original}}` remains ambient/non-authorizing. Verification rerun during review: focused workflow definitions Scry 12/195 green, clj-kondo 0/0, `task-lifecycle.edn` read ok. PASS_STATUS ACTIONABLE_FEEDBACK.
- 2026-06-06: Task 216 (`extract-task-knowledge-workflow`) implementation-review follow-up **IR1** executed. The `task-lifecycle` extraction delegate now passes the implementation-review yield as labeled `:implementation-review-yield` in the prompt-string map, while `:workflow-original` remains ambient context only. The extraction prompt binds `{{implementation_review_yield}}` from that field and states that `PASS_STATUS: REVIEW_COMPLETE` in ambient `{{original}}` cannot authorize open-task extraction. Workflow-loader tests lock the prompt-visible source distinction and lifecycle wiring. Verification green (focused Scry workflow-definitions 12/195, clj-kondo 0/0, EDN/read + markdown compile smoke). PASS_STATUS FOLLOW_UPS_COMPLETE.
- 2026-06-06: Task 216 (`extract-task-knowledge-workflow`) implementation-review pass found actionable follow-up **IR1**. The final `task-lifecycle` extraction delegate forwards `:workflow-original` and the implementation-review yield as two raw context sources; the markdown callee sees an unlabeled `{{original}}` aggregate, so prompt-visible authorization of open-task extraction cannot distinguish the lifecycle-injected review yield from arbitrary original context containing `PASS_STATUS: REVIEW_COMPLETE`. Added IR1 to `steps.md` and review note to `implementation.md`. PASS_STATUS ACTIONABLE_FEEDBACK.
- 2026-06-06: Task 216 (`extract-task-knowledge-workflow`) implementation complete. Added the single-step markdown `/delegate extract-task-knowledge` workflow, appended it as the final `task-lifecycle` delegate, locked workflow-loader coverage for prompt safety/context/history/dedupe contracts plus lifecycle wiring, and updated docs/CHANGELOG. Verification: focused Scry workflow-definitions test green (12/191), clj-kondo targeted 0/0, task-lifecycle EDN read ok. PASS_STATUS IMPLEMENTATION_COMPLETE.
- 2026-06-06: Task 216 (`extract-task-knowledge-workflow`) plan/steps inconsistency review found no new actionable feedback. Reviewed design/plan/steps/implementation plus referenced workflow/docs/changelog/test context; appended REVIEW_COMPLETE note to implementation.md; no steps.md follow-ups added. PASS_STATUS REVIEW_COMPLETE.
- 2026-06-05: Task 211 code-shaper review pass 2 found no new actionable issues after CS1. Appended REVIEW_COMPLETE note to implementation.md; no follow-up steps added. Focused Scry `psi.rpc-prompt-test` and `psi.turn-runtime.response-mode-test` green.
- 2026-06-05: Task 211 implementation-review pass found no new actionable issues. Appended REVIEW_COMPLETE note to implementation.md; no follow-up steps added. Focused `psi.rpc-prompt-test` Scry green; `bb emacs:test` full suite green (selector flag ignored by runner).
- 2026-06-05: Task 211 (`emacs-retry-backoff-footer-visible`) plan/steps follow-ups **PA1/PA2** executed. PA1 tightened Slice-1 backend/RPC trigger coverage to require driving the real provider retry-state mutation/invalidation edge used by retry scheduling, not direct `footer-updated-payload` construction or manual `footer/updated` emission. PA2 tightened Slice-2 changed-retry coverage to require a backend-published retry change that alters visible footer text, naming representative formatter inputs: wait/resume time, attempt/source, or rate-limit remaining/reset detail. Marked PA1/PA2 done in `steps.md`; updated `plan.md`/`steps.md`/`implementation.md`; no code/tests/docs/changelog touched.
- 2026-06-05: Task 211 (`emacs-retry-backoff-footer-visible`) plan/steps ambiguity review found two actionable follow-ups: **PA1** require Slice-1 backend/RPC trigger coverage to exercise the real provider retry-state mutation/invalidation edge (not direct payload construction/manual event emit), and **PA2** require Slice-2 changed-retry coverage to name visible-text-changing retry fields (wait/resume time, attempt/source, rate-limit remaining/reset) or explicitly narrow the representative case. Appended review note to `implementation.md` and unchecked PA1/PA2 to `steps.md`.
- 2026-06-05: Task 211 (`emacs-retry-backoff-footer-visible`) design follow-ups **DR1/DR2** executed. DR1 resolved countdown freshness: retry footer rendering is event-driven (activation, backend-published retry changes, clear), with no Emacs-local per-second countdown timer or parallel countdown model. DR2 added the required coverage matrix: backend/RPC trigger proof + Emacs-visible `footer/updated` active/change/clear coverage, and separate `session/updated` fallback coverage only if implementation relies on that fallback. Marked DR1/DR2 done in `design-steps.md`; no `plan.md`/`steps.md`/code touched.
- 2026-06-05: Task 211 (`emacs-retry-backoff-footer-visible`) design follow-up **AR1** executed. Tightened `design.md` fallback note: if `session/updated` is used because `footer/updated` is unavailable, it may only trigger/reuse delivery of the app-runtime-owned footer projection; Emacs must not synthesize retry footer wording or create a parallel retry display. Marked AR1 done in `design-steps.md`; no `plan.md`/`steps.md`/code touched.
- 2026-06-04: Updated psi Clojure test defaults to Scry-first: `bb clojure:test:unit`, `:extensions`, and `:integration` now invoke `clojure -X:test scry.cli/run :runner :kaocha :suite :<suite>` via `run-scry-kaocha-suite!`, preserving existing Kaocha suite config while making Scry the default execution/diagnostic path. The wrapper falls back to the old `bb.kaocha-runner/run!` only when stderr contains `scry CLI error:` (runner/adapter execution failure), NOT on ordinary test failures. Added `org.hugoduncan/scry-kaocha 0.1.26` to the full `:test` alias; kept `:test-paths` with core `scry` for focused/project-nREPL use. Verified `clojure -X:test scry.cli/run :runner :kaocha :suite :unit` and `bb clojure:test:unit` green; focused `bb clojure:test:scry --namespace psi.session-state.state-test` green; lint/format green.
- 2026-06-04: Added Scry test runner support to psi: `org.hugoduncan/scry {:mvn/version "0.1.26"}` is now on both `:test-paths` (project nREPL/focused classpath) and `:test` (full test alias) extra-deps; copied upstream Scry skill into `.psi/skills/scry/SKILL.md`; added `bb clojure:test:scry` wrapper (`clojure -M:test-paths -m scry.cli ...`) because psi's `:test` alias owns Kaocha as its main; documented Scry in `TESTING.md`, `doc/develop.md`, and CHANGELOG. Verified `bb clojure:test:scry --namespace psi.session-state.state-test` green (7 tests/15 assertions), `clojure -M:test-paths -e "(require 'scry.core)"` loads, `bb lint` 0 errors/0 warnings (one pre-existing info), and `bb fmt:check` green.
- 2026-06-03: ⚒ Rebased `simplifier` onto `origin/master` and renumbered this task **204 → 209**. Master already owns 204 twice (`closed/204-prompts-reload-command-and-mutation` legit + a stale leaked copy of THIS task at `open/204-incidental-complexity-simplification-workflow` from PR #150/`46883f403`). Rebase used `-X theirs` (my canonical versions win over master's stale leaked SKILL.md/task-lifecycle-in-worktree.edn); re-added the two master CHANGELOG Added entries (`/reload-prompts`, deterministic `/operations`) that `-X theirs` dropped; `doc/workflows.md` had no dropped master content. Renamed task dir + `task_204_workflow_definitions_test.clj`→`task_209_*`, ns/deftest/comments→`task-209-*`, doc headers→`# 209`. Historical 204 refs in append-only logs left as-is. Next free number was 209 (max 208).
- 2026-06-03: Task 204 test-review pass-26 follow-up TT-K complete: added `task-204-workflow-set-loads-together-test` co-loading the three task-204 delegate-chain EDNs (`reduce-incidental-complexity` → `task-lifecycle-in-worktree` → `task-lifecycle`), asserting empty errors, all three register, and each delegate `:target` ∈ combined definitions — the references-resolve check the isolated string-equality asserts couldn't give (loader does not validate targets at load time). Mirrors `review-workflow-set-loads-together-test`. Test-only; 3 tests/88 assertions green, clj-kondo 0, file-lengths clean (445<800). Commit 861e4ccd5.
- 2026-06-03: Task 204 test-review pass-27 follow-up **TT-L** complete: added `incidental-complexity-finder-real-lens-integration-test` — the testing-without-mocks **Narrow Integration Test** the synthetic recipe tests lacked. New `bb-available?` predicate + `run-recipe-over-lens-output` harness (sibling to `run-jq-recipe`, feeds the REAL `bb gordian` payload verbatim — live output already carries top-level `.units`, so NOT re-wrapped). When bb+jq present: runs real `bb gordian local --sort total --json` + `bb gordian complexity --json` (`:dir user.dir`), asserts both exit 0 + expose `"units"`, pipes actual output through the SKILL.md recipe, asserts exit 0 + JSON array (`[`…`]`, possibly `[]`) whose every element carries the projected evidence keys — validated via `jq -e 'type=="array" and all(.[]; has("ns") and …has("findings"))'`. STRUCTURE not target (live top-5 drifts → flaky; `[]` passes vacuously). bb/jq-absent fallback locks `$cc[0].units`/`$loc[0].units`. Closes the design's FIRST acceptance ("produces a target + evidence when run against this repository") + the real-lens-shape assumption every synthetic test builds on. Test-only; no production/skill/EDN change. Focused ns green (10 tests/89 assertions, +1/+11 over pass-17); clj-kondo 0; clj-paren-repair Success; file 626<800; file-lengths exit 0. 🔁 PATTERN (continues 204): a recipe wrapping an external CLI accumulated 9 synthetic-input tests but zero real-system exercise — TT-F even recorded the residual gap in prose ("never exercise the producing commands"); the design's *first* acceptance ("against this repository") is precisely the Narrow Integration Test the mock-free synthetic suite cannot give. Lock the real-system shape, not just synthetic fixtures over a stubbed input.

- 2026-06-05: Task 211 (`emacs-retry-backoff-footer-visible`) implementation pass committed `0fa58aea6` (`⚒ 211 publish retry footer updates`). Completed Slice 1 and activation/clear parts of Slice 2. Root cause: provider-boundary retry wrote canonical session `:retry` in `turn-runtime.core/mark-active-retry!` and cleared it in `clear-active-retry!`, but RPC progress delivery refreshed footer only on `:tool-result`; no progress event was emitted for retry state changes, so Emacs got no in-backoff `footer/updated`. Fix: `mark-active-retry!`/`clear-active-retry!` now emit a `:retry-updated` progress event (footer-refresh-only), RPC progress draining treats `:retry-updated` like `:tool-result` for footer refresh, and active retry maps now include `:active? true` so existing `retry-display/retry-status-text` renders. Added backend/RPC regression test driving real `execute-prepared-request!` provider retry scheduling through the real progress loop; asserts active footer contains `retry in`, `:session-id` matches, and clear footer removes stale retry text. Verified focused `psi.rpc-prompt-test`, `psi.turn-runtime.response-mode-test`, `psi.app-runtime.footer-test` green; clj-kondo targeted lint 0 errors/2 pre-existing unresolved-var warnings in turn-runtime core. Remaining task work: Slice 2 changed-retry coverage, Slice 3 Emacs-visible rendering tests, optional fallback decision already no fallback so record/skip, final verification.

# Mementum State

- 2026-06-07: Task 217 (`session-profiles-for-workflows`) docs follow-up **DOC1** executed. Updated `doc/tui.md` command discovery to list `/session-profiles` and `/session-profile [profile-name|clear]`, added a focused session-profile command explanation with config/workflow links, and added a README model-controls pointer for interactive `/session-profile` selection plus workflow `:session-profile` usage. PASS_STATUS FOLLOW_UPS_COMPLETE.
- 2026-06-07: Task 217 (`session-profiles-for-workflows`) task-test-review follow-up **TT6** executed. Added backend command-dispatch coverage proving malformed `/session-profile` tokens (`planning extra`, EDN map/vector/list, `fast+coding`) return parser errors and leave model/thinking/speed/effort plus selected-profile metadata unchanged. Verification green: focused command Scry 7/82, targeted clj-kondo 0/0. PASS_STATUS FOLLOW_UPS_COMPLETE.
- 2026-06-07: Task 217 (`session-profiles-for-workflows`) task-test-review pass 5 found actionable follow-up **TT6**. Parser seam tests invalid `/session-profile` tokens, but backend command-dispatch tests do not yet prove malformed tokens (`planning extra`, EDN map/vector/list, `fast+coding`) leave model/thinking/speed/effort and selected-profile metadata unchanged. Added unchecked TT6 to steps and appended review note. PASS_STATUS ACTIONABLE_FEEDBACK.
- 2026-06-07: Task 217 (`session-profiles-for-workflows`) task-test-review follow-up **TT5** executed. Added top-level still-running multi-step statechart coverage proving a later `:session-profile` step resolves model/thinking/speed/effort from the original run `:session-profile-snapshot` after the first step edits mutable project profile config. Focused Scry `psi.agent-session.workflow-statechart-runtime-test` green (15/65). PASS_STATUS FOLLOW_UPS_COMPLETE.
- 2026-06-07: Task 217 (`session-profiles-for-workflows`) task-test-review follow-up **TT3** executed. Added delegate runtime coverage proving `delegate-step-runtime-result` copies the parent run `:session-profile-snapshot` into the child workflow run and callee profile resolution uses that child snapshot after real user config changes, not mutable config re-reads. Focused verification green: `psi.workflow-step-session-config.session-profiles-test` 5/26; clj-paren-repair success. PASS_STATUS FOLLOW_UPS_COMPLETE.
- 2026-06-06: Task 216 (`extract-task-knowledge-workflow`) plan/steps ambiguity follow-ups **PA1–PA3** executed. Resolved extraction identifier shape to normalize exact `NNN-slug` or exact `munera/{open|closed}/NNN-slug` (reject other shapes), required the markdown workflow to reference `{{original}}` so lifecycle/review context compiles from `:workflow-original`, and chose `PASS_STATUS: REVIEW_COMPLETE` in the lifecycle-injected `review-task-implementation` yielded text as the sole open-task success marker. Updated `plan.md`, future checklist text, and `implementation.md`; no workflow/code/tests/docs implemented in this follow-up pass. Commit `65aa0cc47`. PASS_STATUS FOLLOW_UPS_COMPLETE.

- 2026-06-06: Task 216 (`extract-task-knowledge-workflow`) plan/steps created from stable design. Plan slices: standalone markdown extraction workflow prompt, task-lifecycle trailing delegate integration, workflow-loader definition tests, docs/changelog, verification/coherence. Steps.md now contains concrete unchecked implementation checklist. Commit `b3773df4e`.

- 2026-06-06: Task 216 (`extract-task-knowledge-workflow`) design follow-ups **INC1/INC2** executed. INC1 reconciled Munera location-based completion with lifecycle extraction by narrowing the autonomous mementum exception: standalone extraction remains closed-task-only; still-open task artifacts are authorized only for `task-lifecycle` immediately after successful `review-task-implementation`, with task-scoped git history and existing gates. INC2 added the required CHANGELOG `[Unreleased]` design requirement for `/delegate extract-task-knowledge` and changed `task-lifecycle` terminal behavior. Marked INC1/INC2 done; no `plan.md`/`steps.md` touched. PASS_STATUS FOLLOW_UPS_COMPLETE.

- 2026-06-06: Task 216 (`extract-task-knowledge-workflow`) design **inconsistency** review found two new actionable follow-ups. **INC1**: lifecycle extraction from a still-open task conflicts with the authoritative Munera/mementum completion boundary because Munera state is location-based (`open/` vs `closed/`) while the autonomous extraction exception is scoped to completed task artifacts. **INC2**: design scopes docs but omits required CHANGELOG `[Unreleased]` coverage for the new `/delegate extract-task-knowledge` workflow and changed `task-lifecycle` terminal behavior. Added unchecked INC1/INC2 to `design-steps.md`; appended review note to `implementation.md`; did not review `plan.md` or `steps.md`. PASS_STATUS ACTIONABLE_FEEDBACK.

- 2026-06-06: Task 216 (`extract-task-knowledge-workflow`) ambiguity follow-ups **AMB1–AMB4** executed. Design now specifies exact workflow shape (`.psi/workflows/extract-task-knowledge.md` single-step prompt; no same-name `.edn`), completed-task slug resolution (standalone closed-only; lifecycle open path only after successful implementation review), task-specific git-history lenses (task-dir commits, task-id/slug message matches, SHAs recorded in artifacts), and lifecycle terminal contract (extraction receives review output context and preserves review/lifecycle outcome in final summary). Marked AMB1–AMB4 done; no `plan.md`/`steps.md` touched. PASS_STATUS FOLLOW_UPS_COMPLETE.

- 2026-06-06: Task 216 (`extract-task-knowledge-workflow`) design follow-up **AR1** executed. Resolved architecture mismatch by amending the authoritative MemoryArtifacts/mementum boundary (AGENTS.md + mementum protocol resource) with a narrow `extract-task-knowledge` autonomous-artifact-extraction exception: only completed Munera task artifacts + git history, gated by gate-1/gate-2, project-generality, significance, dedupe recall, `uncertain→skip`, and mementum commit conventions. Design now references the protocol-authorized exception; `design-steps.md` AR1 checked; no `plan.md`/`steps.md` touched. PASS_STATUS FOLLOW_UPS_COMPLETE.

- 2026-06-06: Task 215 (`correct-incidental-complexity-a2-gate`) test-shaper re-pass follow-up **TS-S3** executed. Sole newly-added unchecked item (added by `a7d12ef65`; all slice 1–4 + PA1 + PI1–3 + RI1–3 + TR-T1–7 + TS-S1/S2 predate it). Resolved the redundant bare `before-max(k)` content-lock (~line 308, fully subsumed by the line-323 `before-max(k) >= B` exemption lock) by **re-anchoring** (not deleting) to the verbatim definition phrase `` `before-max(k)` := the maximum `lcc-total` among before-rows carrying `k` `` — locks a genuinely distinct invariant (a **maximum, not a sum**; complements TR-T7's "never a sum"/"not a per-line pairing") while removing the redundancy. Test-only; no production/doc/CHANGELOG. workflow-loader suite green (3 tests / 215 assertions / 0 fail / 0 error); clj-kondo 0/0; clj-paren-repair Success. All steps.md items now checked. PASS_STATUS FOLLOW_UPS_COMPLETE. 🔁 PATTERN: a redundant content-lock (substring of a stronger lock) is better re-anchored to an independent phrase the stronger lock omits (here the definition) than deleted, when that phrase encodes a distinct invariant worth guarding.

- 2026-06-06: Task 215 (`correct-incidental-complexity-a2-gate`) plan/steps **inconsistency** follow-ups **PI1/PI2/PI3** executed (commit `471cadbb9`). Sole newly-added items (added by inconsistency review `274211c8b`; all slice 1–4 + PA1 items predate it, left untouched). Artifact-only — no code/tests/docs (none referenced; all three target the residual two-vs-three net-sum content-lock count drift). **PI1**: plan.md Risks "Content-lock test coupling" bullet now reads "breaks **all three** net-sum A2 content-lock assertions (299/301 literal strings + line-295 key)", matching "Concrete edit targets" #2. **PI2**: scoped the slice-1 primary content-lock step to "the **two literal net-sum string** assertions (299/301)" + cross-ref that the third (line-295 key) is handled by PA1 (chose explicit-scope over folding-in, preserving the plan's existing PA1 split). **PI3**: replaced PA1's stale "Correct the plan's 'Two assertions … will break' to three" with a count-reconciliation note (targets #2 already reads Three; residual "two" is the Risks bullet, fixed by PI1). Also re-anchored PA1's opening "the plan's 'two assertions' count omits" → "the slice-1 step's two-literal-string scope omits" for full coherence. "Three" now single consistent count across plan.md + steps.md; no dangling reference to a removed plan phrase. PASS_STATUS FOLLOW_UPS_COMPLETE.

- 2026-06-05: Task 211 docs review found no new actionable documentation issues. CHANGELOG Unreleased Fixed already covers the user-visible retry footer bug fix; no README/doc update needed because no documented command/config/RPC topic/footer contract changed. Appended REVIEW_COMPLETE note; no follow-up steps added.
- 2026-06-05: Task 211 (`emacs-retry-backoff-footer-visible`) implementation complete. Extended backend/RPC regression to cover two real provider-boundary retry updates through `execute-prepared-request!` + real progress loop: `Retry-After: 8`/remaining 0 emits `footer/updated` with `retry in 8s`, then changed metadata `Retry-After: 4`/remaining 2 emits fresh `retry in 4s · remaining 2/5000`, then clear emits footer without stale retry text. Added Emacs-visible `footer/updated` rendering tests for active/change/clear that assert rendered buffer/projection footer text and prove Emacs consumes projected text only (no local retry formatter/fallback). No `session/updated` fallback required. Verified focused Clojure Scry namespaces, focused/full Emacs ERT, full `bb clojure:test:unit`, targeted lint (0 errors; 2 pre-existing unresolved-var warnings). PASS_STATUS IMPLEMENTATION_COMPLETE.
Bootstrapped on 2026-04-02.

- 2026-06-04: Task 210 (`fix-project-repl-start-readiness-timeout`) test-review **pass-8** follow-up **TR8** executed. Sole newly-added actionable item (added by task-test-review pass 8; all 5 slices + PA1–4 + PSI1 + IR1–2 + TR1–7 + TS1–10 already checked). Coverage-only (production already correct — pre-wait `:process`/`:pid` record + `stop-started-instance-in!` reaping branch both exist); no production/doc/CHANGELOG change. TR8 pins the **second half** of the IR2 process-reaping contract — the existing IR2 case asserted only `@destroyed*` (the failure-path `catch` reap via the outer `launched-process` volatile), leaving the **pre-wait `:process` record** and `stop-started-instance-in!`'s reaping branch uncovered. Implemented **both** options: (a) extended the IR2 `start-instance-in-test` case to also assert `(get-in instance [:runtime-handle :process])` present + `:pid 4321` on the failure-path instance (read via `instance-in`) — a regression reverting to the post-wait success-only record keeps `@destroyed*` green yet re-orphans any process that survives the catch, because `stop-started-instance-in!` reads `:process` from the runtime-handle; (b) added `stop-started-instance-in-test` exercising the previously-untested `(when (and process (.isAlive process)) (.destroy process))` branch — seeds a `:started` instance with an alive `fake-process` (`:destroyed*` atom), calls stop, asserts `@destroyed*` true + `instance-in` → nil (instance removed). No-mocks (real `Process` proxy). started-test 4 tests/52 assertions green (+1 deftest/+5 over pass-3's 47); full `--focus unit` green; clj-kondo 0/0; clj-paren-repair Success; file-lengths RC=0. 🔁 PATTERN: a fix with two stated mechanisms (immediate `catch` reap via an outer volatile + a recorded durable handle for deferred reap by `stop`) needs both pinned — testing only the immediate path lets a regression collapse it to one mechanism while the suite stays green, re-opening the exact leak for the deferred path; cover both the producing record (pre-wait `:process` on the failure instance) and the consuming reaper (`stop` destroys + removes). PASS_STATUS REVIEW_COMPLETE.

- 2026-06-04: Task 210 (`fix-project-repl-start-readiness-timeout`) impl-review **pass-3** follow-up **IR2** executed (commit `3c53f525a`). Sole newly-added actionable item (added by impl-review pass 3 `66383ab59`; all 5 slices + PA1–4 + PSI1 + IR1 + TR1–5 + TS1–10 already checked). **IR2** = launched child process leaked on the readiness-failure path. In `start-instance-in!` the `process` was bound in the inner `let` and only stored on the runtime-handle `:process` *after* `wait-for-started-endpoint!` returned, so when the wait threw (timeout/stale-port) for an **alive-but-port-less** hung/slow-boot child, the `catch` recorded `:last-error` + rethrew without `.destroy`ing or recording it → orphaned JVM; this task's `5000→120000` raise widened the leak window to 120 s. **Fix (both design options):** (1) hold the process in an outer-scope `volatile!` bound in the top-level `let` (merged there, not a nested `let`, to dodge a clj-kondo redundant-let warning) reachable by the `catch`; (2) record `:process`/`:pid` onto the runtime-handle **pre-wait** via a third launch-site `update-instance-in!` (moved off the post-wait success update — which now only adds `:launch-id`), so `stop-started-instance-in!` can also reap it; (3) the `catch` now `.destroy`s `@launched-process` when `(.isAlive process)` (no-op on already-exited → `process-exited?` self-exit path unaffected). No-mocks `started_test` case "reaps the alive launched process… (IR2)": alive `fake-process` w/ `:destroyed*` atom, launcher writes no `.nrepl-port`, `:timeout-ms 100`; asserts the timeout throws (`:phase :started-readiness`) **and** `@destroyed*` true. CHANGELOG `Fixed` + `doc/project-nrepl.md` note (user-observable: no orphaned JVMs on failed starts). started 3/47 (+2); ops/config/attach/commands 19/148 unchanged; consuming `project-nrepl-extension-install-test` 1/5 green (its `with-redefs` stub unaffected by the internal change); `--focus unit` exit 0; clj-kondo 0/0; clj-paren-repair Success; file-lengths exit 0. 🔁 PATTERN: a resource bound in an inner `let` and stored on the durable handle only *after* the success call is unreachable from the enclosing `try`'s `catch` — to reap it on the failure path, bind it in an outer scope the `catch` reaches AND record it on the durable handle *before* the throwing call (pre-wait), mirroring how the pre-wait status-field update was already shaped so the diagnostic survives the failure path. PASS_STATUS REVIEW_COMPLETE.

- 2026-06-03: Task 210 (`fix-project-repl-start-readiness-timeout`) test-shaper **pass-5** follow-ups **TS8/TS9** executed. Sole newly-added actionable items (added by test-shaper review `1a0d8ab79`; all prior TS1–7/TR/IR/PA/PSI items already checked). Shape-only/behaviour-preserving — test files + task artifacts only; no production/doc/CHANGELOG; assertions unchanged. **TS8**: `ops_test` and `config_test` each defined a private `write-project-config!` with DIFFERENT wrapping semantics (`ops_test` wrapped the inner `:project-nrepl` map in `{:agent-session {:project-nrepl …}}`; `config_test` spat its arg verbatim). Lifted three writers into `test_support` — `write-user-config!`/`write-project-config!`/new `write-local-config!` — on ONE convention: **caller passes the full on-disk map verbatim; helper writes EDN to the canonical path** (matches config_test's prior semantics, keeps asserted shape at the call site, `¬helpers_that_hide(intent)`). Only `ops_test`'s one call site changed (now passes the full map); dropped its now-unused `clojure.java.io` require. **TS9**: rewired all three `read-project-preferences-test` cases onto the TS8 single-file writers, removing the open-coded `shared-f`/`local-f` `let`+`.mkdirs`+`spit` frame. Chose single-file writers over a `write-project-prefs!` pair-writer because the two malformed cases need ONE valid file (writer) + ONE raw-invalid-EDN `spit` to the canonical path — a pair-writer taking two valid maps can't express the malformed half; the writer's `.mkdirs` still provisions the `.psi` dir for the sibling raw spit. Verified: clj-paren-repair Success; clj-kondo 0/0; focused green (config 8/41, ops 4/23, started 3/33, attach 2/18); full `--focus unit` exit 0; file-lengths exit 0 (config/ops shrank, test_support grew by the writers). 🔁 PATTERN: two same-named private helpers in sibling test files with DIFFERENT wrapping semantics is the exact `consistent(fixtures)` drift `test_support` prevents — unify on the verbatim full-map convention so only the wrapping site changes. 🔁 PATTERN: a pair-writer taking two valid maps can't serve malformed-fallback cases (one valid file + one raw-invalid spit); prefer per-file writers whose `.mkdirs` still provisions the shared dir. PASS_STATUS REVIEW_COMPLETE.

- 2026-06-03: Task 210 (`fix-project-repl-start-readiness-timeout`) test-shaper **pass-4** follow-up **TS7** executed (commit `01bd62df4`). Sole newly-added actionable item (added by test-shaper review `2a94eb416`; all TS1–6/TR1–5/IR1/PA1–4/PSI1 already checked). Shape-only/behaviour-preserving — test files + task artifacts + one clj-kondo `:lint-as` entry; no production/doc/CHANGELOG; no assertion change. Single-sourced the `(temp-dir …)` + `(try … (finally (delete-tree! dir)))` acquire/cleanup frame (~28 sites: started ×13, config ×7+2-multi, ops ×3, attach ×2, commands ×1) into new `test_support/with-temp-dir` — a `let`-style `[sym prefix …]` macro creating all dirs pre-body and guaranteeing `delete-tree!` of each in `finally` (reverse order). The two config_test `home`+`worktree` cases use one multi-binding call. Cases with extra non-temp `let` bindings keep them in an inner body `let`, so the dir binding stays visible (compress ceremony, ¬hide intent). Dropped now-unused `delete-tree!`/`temp-dir` `:refer`s (both stay public). All files shrank (started 273→234, config 276→247, etc.). 🔁 PATTERN: a `let`-style lifecycle macro whose bindings are `sym prefix` pairs lints cleanly with `:lint-as clojure.core/let` **only because** the second pair element is a value (string) not a symbol — clj-kondo reads each pair as `binding init` and the string init resolves; a macro binding `sym init-expr` pairs would also work, but one binding `sym` alone or `prefix sym` order would not. 🔁 PATTERN: that same `:lint-as let` expansion makes a *sole* nested `(let …)` directly inside the macro body read as a redundant-nested-let warning — merge the child binding into the body `let` when its inits are side-effect-free (here the TS3 `launched-at` capture). clj-paren-repair Success; clj-kondo 0/0; `clojure -M:test --focus unit` green; file-lengths exit 0. PASS_STATUS REVIEW_COMPLETE.

- 2026-06-03: Task 210 (`fix-project-repl-start-readiness-timeout`) test-shaper **pass-3** follow-up **TS6** executed. Sole newly-added actionable item (added by test-shaper review `54266be36`; all prior TR/TS1–5/IR/PA/PSI items already checked). Shape-only/behaviour-preserving — test files + task artifacts only; no production/doc/CHANGELOG; suite count unchanged (17 tests/115 assertions). Single-sourced the duplicated happy started-launcher arrange (the same launcher writing `"7777"` + `fake-process {:alive? true :exit-code 0 :pid 4321}`, open-coded across 4 `start-instance-in-test` happy cases + `ops_test`'s threading test) into new `test_support/started-launcher!` (default port 7777 pid 4321). Crucially it **composes** with — rather than hides — TS4's launch-instant capture via an optional `:on-launch` 0-arg pre-write hook, so the provenance case keeps its `launcher-at` atom without re-open-coding the launcher. Dropped the now-unused `fake-process` `:refer` in `ops_test`. The stale/exit/plain-deadline cases keep bespoke (non-happy/stale) launchers — only the *happy* arrange is single-sourced. 🔁 PATTERN: a helper that references another `defn` in the same test-support ns (`started-launcher!` → `fake-process`) must be ordered *after* it or clj-kondo flags an unresolved-symbol forward reference (clj-paren-repair stayed silent; only clj-kondo caught it) — moved the helper to file end. 🔁 PATTERN: when single-sourcing an N-site arrange, the one site with an extra concern (TS4's launch-instant capture) drives the helper's shape — give it an injected hook (`:on-launch`) so the helper compresses ceremony without hiding that case's distinct intent. PASS_STATUS REVIEW_COMPLETE.

- 2026-06-03: Task 210 (`fix-project-repl-start-readiness-timeout`) test-shaper **pass-2** follow-ups **TS4/TS5** executed (commit `b93ce4cb9`). Sole newly-added actionable items (added by test-shaper review `bafed7ecd`; all prior TR/TS1–3/IR/PA/PSI items already checked). Test-only, shape-and-assertion-only — no production/doc/CHANGELOG; suite behaviour preserved. **TS5** split the bundled `started_test` case ("records the effective :readiness-timeout-ms and launch-instant :started-at") into two named `testing` blocks (effective-timeout recording vs launch-instant provenance) so a failure names which contract broke. **TS4** replaced the bare `(instance? Instant …)` :started-at type check with a *provenance* assertion: the launcher seam captures its own invocation instant into a `launcher-at` atom (the true launch site — launcher runs *after* `launched-at (now)` is captured pre-wait and *before* gate-poll+connect); asserts `:started-at` is an Instant, **not after** `@launcher-at`, and **not before** a pre-call `before` wall-clock. A PA2 regression re-adding the removed post-wait connect-time `:started-at (now)` write records an instant strictly after the launcher fired → `.isAfter` guard fails green. started-test 3/33 green (+8 over pass-1's 25); ops/config/attach unchanged; clj-kondo 0/0; file 284<800. 🔁 PATTERN: a bare `(instance? Instant x)` type check cannot pin *which* instant was recorded — a source-swap regression (launch-instant → connect-instant) still yields *an* Instant and passes green. Pin provenance by having the relevant seam capture its own invocation instant and asserting an ordering relation (`recorded ≤ seam-observed`) only the correct source satisfies. PASS_STATUS REVIEW_COMPLETE.

- 2026-06-03: Task 210 (`fix-project-repl-start-readiness-timeout`) test-shaper follow-ups **TS1/TS2/TS3** executed (commit `67b036016`). Sole newly-added actionable items (added by the test-shaper review `017908179`; all prior TR/IR/PA/PSI items already checked). All shape-only/behaviour-preserving — test files + task artifacts only, no production/doc/CHANGELOG, suite counts unchanged (17 tests/113 assertions). **TS1**: lifted the duplicated parameterised 16-method `java.lang.Process` proxy into `test_support/fake-process`; `started_test` `:refer`s it, `ops_test` deleted `live-fake-process` (the strict alive/exit-0/pid-4321 special case) and calls the shared helper. **TS2**: added `test_support/age-file-back!` (+private `stale-port-offset-ms` 60000) and `spit-stale-port!`; replaced all six open-coded `(.setLastModified … (- now 60000))` sites across started(×4)/config/attach — kept the explicit `spit` in config/attach so the asserted port value stays at the call site, dropped redundant `port-file` bindings in the two started reject cases. **TS3**: added `touch-fresh!` (+1000 ms) and used it in the "accepts a fresh .nrepl-port" gate case so mtime≥floor is explicit by construction, removing the same-second wall-clock coupling (symmetric to the reject cases). 🔁 PATTERN: a test-shaper pass after N coverage passes legitimately targets *incidental duplication* the coverage passes accreted — a 16-method proxy written twice + a magic-offset `setLastModified` open-coded 6× — single-sourced into named test-support helpers (`fake-process`/`spit-stale-port!`/`age-file-back!`/`touch-fresh!`); the fresh/stale setter pair makes both gate directions explicit-by-construction. PASS_STATUS REVIEW_COMPLETE.

- 2026-06-03: Task 210 (`fix-project-repl-start-readiness-timeout`) impl-review follow-up **IR1** executed. Sole newly-added actionable item (all 5 slices + PA1–PA4 + PSI1 already checked). `wait-for-started-endpoint!`'s `process-exited?` branch threw `:phase :started-readiness` *before* the deadline branch, ignoring the already-computed `endpoint`/`fresh?` — so a process that wrote only a too-old `.nrepl-port` then exited lost A2's `:started-stale-port` distinction (the exit branch fired first). Fix (diagnostic-only; pre-launch removal already guards correctness): folded the stale-only condition into the exit branch — `(and endpoint (not fresh?))` at exit now throws `:phase :started-stale-port` (keeping `:command-exited? true`/`:exit-code` + rejected `:port-mtime-ms`/`:min-mtime-ms`/`:launched-at`), else stays `:started-readiness`. **Decision: stale-port wins** on exit-with-stale-port, so the projected `:phase` is deterministic on every path. Added `wait-for-started-endpoint!` unit test (`alive? false`, too-old port, injected `:launched-at`) asserting `:started-stale-port`/`:command-exited?`/message; pre-existing "exits before discovery" (no stale port) still pins `:started-readiness`. started-test 3/25 green (+3); lint 0/0; consuming extension-install 1/5 green; files 198/228 (<800). No design/doc/CHANGELOG delta — the `:phase` was already a documented A2 outcome; IR1 only makes it fire on one more code path. 🔁 PATTERN: a multi-branch wait loop checking `process-exited?` *before* the deadline branch will short-circuit a stale-port diagnostic that only the deadline branch computed; when two exit conditions can coincide (exit AND stale-only port), the earlier-checked branch must re-test the later branch's condition (or the richer diagnostic is silently lost). PASS_STATUS REVIEW_COMPLETE.
- 2026-06-03: **Task 210 (`fix-project-repl-start-readiness-timeout`) implemented** — all 5 slices, `steps.md` fully checked, PASS_STATUS IMPLEMENTATION_COMPLETE (pending review/closure). Two unconditional started-mode fixes in `components/project-nrepl`: (1) configurable readiness timeout — new `[:agent-session :project-nrepl :start-readiness-timeout-ms]` config key (`resolved-start-readiness-timeout-ms`, `[1000 600000]` integer, `{:phase :validate}`, attach-port idiom), `default-readiness-timeout-ms 5000→120000`, threaded `ops/start`→`start-instance-in!`→`wait-for-started-endpoint!` `:timeout-ms` opts; (2) stale-`.nrepl-port` ownership guard — single `launched-at (now)` at the launch site is BOTH the runtime-handle `:started-at` (moved off the post-wait success path) AND the mtime-gate reference threaded via `opts :launched-at` (INC1); pre-launch `.nrepl-port` removal; `wait-for-started-endpoint!` accepts a port only when `mtime ≥ (launched-at floored to whole seconds)` (AMB4), else polls on, throwing `:phase :started-stale-port` on a deadline reached with only a stale port. A2 status: new top-level `:readiness-timeout-ms` instance field written by a single new **pre-wait launch-site `update-instance-in!`** (so it survives the throwing failure path) + projected through `instance-payload`; stale-port phase rides existing `:last-error → :data`. ❌→✅ **Consuming-test arity break (deviation):** adding the 4th `opts` arg to the `ops/start`→`start-instance-in!` call threw `ArityException (4)` in `agent-session/.../project-nrepl-extension-install-test`, whose `with-redefs` stub of `start-instance-in!` was a fixed 3-arg fn — widened to `(fn [… & _opts] …)`. 🔁 PATTERN: **changing a callee's arity (even adding an *optional* arg the real fn already supports) breaks every fixed-arity `with-redefs` STUB of it in consuming components** — the real var has the optional arity, but a hand-rolled redef fn does not; after adding an arg to a cross-component seam call, grep consuming tests for `with-redefs […/that-fn]` and widen their stubs. 🔁 PATTERN: a single captured `(now)` reused for two purposes (status `:started-at` + a correctness gate) must be captured at the one site that owns the true event (the launch), written *pre*-side-effect so it survives a throwing path, and threaded (not re-derived) downstream — re-timing in the consumer references the wrong instant.

- 2026-06-02: Task 207 (`workflow-session-defaults-inheritance-snapshot`) **code-shaper follow-ups CS1+CS2 executed**. The code-shaper review commit (`65af82156`) only ADDED the findings (steps.md + implementation.md), not the fixes — CS1/CS2 were the genuinely-newest unchecked items (T7 was already done/committed `69fe49ec0`; my initial steps.md read was stale). **CS1** (idiom unification, `workflow-step-session-config/core.clj`): collapsed three snapshot-vs-live consumption shapes into one `inherited` map bound once — snapshot path `(select-keys snapshot inherited-defaults-snapshot-keys)`, live path carries only the 4 pre-task-live-inherited fields (`:model :prompt-mode :skills :tool-defs`), deliberately OMITTING `:thinking-level :speed-mode :effort-override` to preserve AC6 (snapshot-less runs emit no speed/effort, fall thinking-level→base-meta/:off). All 7 downstream reads now source from `inherited`. **CS2** (precedence): `:thinking-level` was the lone inversion (base-meta outranked the inherited value) vs `:model` (inherited > base-meta). Chose UNIFORMITY with `:model`: reordered to step → inherited → base-meta → :off. Behaviour change (snapshot path with a base-meta thinking-level — no prior test combined the two, none regressed); documented in design.md Decision 1a + CHANGELOG Fixed; added `snapshot-thinking-level-precedence-matches-model-test`. Full `bb clojure:test:unit` green; clj-kondo 0/0; both files <800 lines. **❌ REPEATED the documented stash mistake** (state.md 2026-06-02 line ~20): I ran `git stash push <path>`→test→`pop` to compare HEAD, but the targeted push captured nothing (the file was already committed), so the `pop` popped a PRE-EXISTING unrelated `wip: test-shaper` `stash@{0}` → conflict markers in two `app_runtime` test files. Recovered with `git checkout HEAD -- <files>`; both stashes stayed intact. **🔁 Lesson REINFORCED**: in a shared worktree, NEVER `git stash`/`pop` to inspect HEAD — `git stash list` first; use `git show HEAD:<path>` or a throwaway `git worktree`. The lesson was already in state.md and I still hit it; the trap is that `git stash push <path>` on an unchanged path silently no-ops, making the later `pop` operate on a foreign stash.
- 2026-06-02: Task 207 test-review-pass-6 follow-up (T6) executed/committed `f338e5f4b`. T6 was the only newly-added review item; its value-level capture-path tools/skills assertions existed only in the working tree (HEAD shipped the shape-only `vector?`/`sequential?` check while steps.md already marked T6 `[x] DONE` + implementation.md documented it — the recurring R5/T3 dirty-tree/incoherent-HEAD failure mode). Verified focused suite (10 tests, 53 assertions) + clj-kondo (0/0) green, committed the working-tree fix, tree now clean and HEAD self-consistent. No other unchecked steps; no source/doc/changelog delta. Task remains open pending closure.

- 2026-06-03: **Task 207 (`workflow-session-defaults-inheritance-snapshot`) CLOSED** (`23f659292`, moved open→closed, plan.md updated). All steps checked, `bb clojure:test:unit` fully green, all review loops clean (code-shaper pass 2, docs pass 2, test-shaper pass 10, test-review passes 8–9 — all "review complete / no actionable feedback"). Before closing I removed a **stale duplicate unchecked CS2 step** in steps.md (`fda4fd0fa`): a verbatim copy of the already-`[x]` CS2 item (thinking-level precedence reorder) whose code (`workflow_step_session_config/core.clj:259`) and test (`snapshot-thinking-level-precedence-matches-model-test`) both landed in `6cad7a672` — the duplicate was a dangling unchecked artifact, not remaining work. 🔁 Pattern: a single `grep -c '- [ ]'` showing 1 unchecked item turned out to be a stale steps.md duplicate of a completed item; always diff the unchecked text against `[x]` items before treating it as pending. Open now (4): 021, 197, 203, 206.

- 2026-06-06: Task 215 (`correct-incidental-complexity-a2-gate`) **implemented + knowledge captured**. Emitter A2 net-sum bullet replaced with per-unit A2a/A2b ceiling gate (`after(u) < B`, `B := before(target)` from committed `before-local.json`); knowledge page `gordian-net-sum-burden-gate-sub-additivity.md` reconciled (fix-landed, `after(target)` superseded by `B`, A1→A5). Implementation-mechanics knowledge captured: memory `workflow-edn-text-edit-verification` (💡 in-string `:text` span edits → `edn/read-string` round-trip gate, not clj-paren-repair), and new knowledge page `mementum/knowledge/editing-workflow-emitter-prompts.md` synthesizing content-lock substring attribution (emitted-text edits undercount broken locks — attribute each substring to the exact emitting bullet; `identified by`=A2-only vs sibling `keyed by`=A5) + loader-test cwd classpath (run from repo root; pre-existing `tool_execution_test`/`psi/metrics/extension` gap blocks full kaocha `--focus`, isolate the suite). The two source memories were folded into the page and `git rm`'d. 215 review (code-shaper, clean/no-actionable) added Trap 3 to the same page: an emitter `:text` may state a contract twice (what-vs-how — A2a/A2b as criterion + as mechanical steps), a `λ sync` hazard content-locks don't catch; edit every occurrence.

## Current orientation
- Project: psi
- Runtime: JVM Clojure

## Key files
- `README.md` — top-level user documentation
- `META.md` — project meta model
- `munera/plan.md` — active task orchestration
- `STATE.md` — project-local state file
- `AGENTS.md` — bootstrap/system instructions

## Current work state

- 2026-06-03: Task 204 (`incidental-complexity-simplification-workflow`) test-review pass-28 follow-up **TT-M** executed (commit `480cf09b8`). Sole newly-added actionable item (the trailing Contingency step-1 split predates every pass and is conditional/untriggered). Test-only substring lock on the shipped `reduce-incidental-complexity.edn` step-1 prompt — no production/skill/EDN change; all three strings verified present verbatim before locking. Added "select-and-create prompt locks the generated-design evidence clause + capture instruction (TT-M)" `testing` block to the TT-I/TR2 contract cluster in `reduce-incidental-complexity-test`, asserting `select-text` contains (1) "The target unit and the captured incidental-complexity evidence" (the generated design.md contract's FIRST clause, step-7), (2) "Capture the chosen target's evidence" (step-2 capture instruction), and (3) "and the coverage hint" (the generated task's test-net signal). Closes the same sub-clause-lock symmetry gap TT-I/TR2/TT-D/TT-G closed for the OTHER clauses of the same generated-design contract — a regress emitting a refactor task with no evidence block, or dropping the coverage hint, now fails green. SKILL.md coverage-hint side already locked (TR3); this locks the missing *workflow* prompt half. Focused task-204 ns green (3 tests, 91 assertions, 0 fail, +3 over pass-26/27's 88); clj-kondo 0; clj-paren-repair Success; file 459<800; `bb commit-check:file-lengths` exit 0. steps.md TT-M ticked; implementation.md PASS_STATUS: REVIEW_COMPLETE. 🔁 PATTERN (continues 204): a multi-clause generated-contract keeps yielding one unlocked named sub-clause per review pass — TT-M locked the contract's FIRST stated clause (target+evidence) and its upstream capture instruction, the half left after Blast-radius/Phase-0/A5/A2 were covered. Lock every named clause of a contract, and lock both the spec side (SKILL doc) AND the producing side (workflow prompt instruction).

- 2026-06-03: Task 204 (`incidental-complexity-simplification-workflow`) test-review pass-24 (task-test-review) follow-up **TT-I** executed. Sole newly-added actionable item (the trailing Contingency step-1 split predates every pass and is conditional/untriggered). Test-only substring locks on the shipped `reduce-incidental-complexity.edn` step-1 prompt — no production/skill/EDN change; all four strings verified present verbatim before locking, so each is a regression guard. Gap: TR2 locked the step-7 two-phase contract's Phase-0 characterization gate + behaviour-identical constraint, and F3 the A5/A2 key — but two further named clauses of the SAME generated-design contract were unlocked: (1) the **Blast-radius** scope fence ("the target unit PLUS the minimal surrounding helpers required to decomplect it; no unrelated cleanup") and (2) the Phase-0 **hard gate + untestable-tangle escape hatch** ("cannot be characterized safely" + "scope drift -> close per Munera" + "No refactor proceeds without a green net"). Added two `testing` blocks to `reduce-incidental-complexity-test`'s TR2 cluster (after the F3 lock). A regress admitting unrelated cleanup (dropping the blast-radius fence) or letting a refactor proceed on an uncharacterized unit without a green net now fails green. Focused task-204 ns green (2 tests, 76 assertions, 0 fail, +4 over pass-23's 72); clj-kondo 0; clj-paren-repair Success; file 386 lines (< 800); `bb commit-check:file-lengths` exit 0. steps.md TT-I ticked; implementation.md PASS_STATUS: REVIEW_COMPLETE. 🔁 PATTERN (continues 204): a multi-clause generated-contract has MANY named sub-clauses (Phase-0 gate, behaviour-identical, A5/A2 key+direction+derivation, blast-radius fence, untestable-tangle hard gate); each review pass locks the sibling clauses left uncovered by prior passes — lock every named clause of a contract, not the representative few.

- 2026-06-03: Task 204 (`incidental-complexity-simplification-workflow`) test-review pass-22 follow-up **TT-G** executed (commit `6d8ba8cdf`). Sole newly-added actionable item (the trailing Contingency step-1 split predates every pass and is conditional/untriggered). Test-only substring lock on the shipped `reduce-incidental-complexity.edn` step-1 prompt — no production/skill/EDN change; verified present before locking, so a regression guard. Added "select-and-create prompt locks the metric-derived touched-set discriminator (TT-G)" `testing` block to the F3/TT-D cluster in `reduce-incidental-complexity-test`, asserting `select-text` contains `the set is computed from the metric, not from the diff/touched files`. F3 locks only the `(ns,var,arity,line)` join key, TT-D only the strictly-less direction — neither anchored the A2 *derivation* (Locked decision 4 / "Net burden (A2)"). A paraphrase to "units whose source/files changed" — defeating the global-recompute net check by hiding relocated burden in an unedited caller — now fails green. Focused task-204 ns green (2 tests, 69 assertions, 0 fail, +1 over pass-21's 68); clj-kondo 0; clj-paren-repair Success; file 351 lines (< 800); `bb commit-check:file-lengths` exit 0. steps.md TT-G ticked; implementation.md PASS_STATUS: REVIEW_COMPLETE. 🔁 PATTERN (continues 204): a single acceptance criterion (A2) has THREE facets — *key* (F3: `(ns,var,arity,line)`), *direction* (TT-D: strictly-less), and *set-derivation* (TT-G: metric-not-file); a test locking only key+direction silently under-covers the discriminator that defines WHICH units the net sum ranges over. Lock all facets of a multi-clause acceptance, not just the arithmetic.

- 2026-06-03: Task 204 (`incidental-complexity-simplification-workflow`) test-review pass-21 follow-up **TT-F** executed (commit `73b7920f5`). Sole newly-added actionable item (the trailing Contingency step-1 split predates every pass and is conditional/untriggered). Test-only — no production/skill/EDN change; SKILL.md §1 already emits both lens commands verbatim, so each lock is a regression guard. Gap: `incidental-complexity-finder-skill-content-lock-test` locked the gap method/thresholds/scope/`(ns,var,arity,line)`@line join key/top-5 guard/evidence+coverage-hint — but NOT the design Deliverable-1 step-1 **two-lens invocation commands** (`bb gordian local --sort total --json` selector lens + `bb gordian complexity --json` cc lens) that produce the jq recipe's `/tmp/icf-*.json` inputs; the recipe-execution tests rewrite temp paths and never exercise the producing commands. Added a `testing` block "encodes the step-1 two-lens invocation commands (TT-F)" asserting both verbatim. Companion to the workflow test's TT-C *baseline* capture-command lock — closes the symmetry where only a TT-C comment mentioned `local --sort total`. A regress (wrong subcommand, dropped `--json`, or losing the `--sort total`/bare selector-vs-baseline A5/A2 distinction) now fails green. Focused `incidental-complexity-finder-skill-test` green (9 tests, 80 assertions, 0 fail, +2 over pass-17's 78); clj-kondo 0; clj-paren-repair Success; file 537 lines (< 800); `bb commit-check:file-lengths` exit 0. steps.md TT-F ticked; implementation.md PASS_STATUS: REVIEW_COMPLETE. 🔁 PATTERN (continues 204): a content-lock test that exercises a recipe's *consumption* (jq over rewritten temp paths) silently under-covers the recipe's *production* commands — the input-generating CLI invocations are never asserted; a later task-test-review re-derives `∀b∈design.∃t.covers` and finds the unlocked producing side. Lock both the producing commands and the consuming recipe.
- 2026-06-03: Task 204 (`incidental-complexity-simplification-workflow`) test-review pass-19 (task-test-review) follow-ups **TT-B / TT-C / TT-D** executed (commit `10b76c055`). Three newly-added actionable items (the trailing Contingency step-1 split predates every pass and is conditional/untriggered). All test-only substring locks on the shipped `reduce-incidental-complexity.edn` step-1 prompt — no production/skill/EDN change; each verified present in the EDN before locking, so all three are regression guards. Added to `reduce-incidental-complexity-test` (`task_204_workflow_definitions_test.clj`): **TT-B** locks the origin/master base-refresh (`git fetch origin master` + "Treat `origin/master` as the authoritative base" + "Base the worktree on `origin/master`"); **TT-C** locks the baseline *capture commands* (`bb gordian local --json` / `bb gordian diagnose --edn` — distinct from the already-locked output filenames, so a regress to the forbidden selector call `local --sort total --json` fails green); **TT-D** locks the A5/A2 *direction-of-change* ("decreased versus its `before-local.json` value" / "after total is strictly less than the before total" — companion to the existing F3 *key* lock, which covers only `(ns,var,arity,line)`, not direction). Focused task-204 ns green: **2 tests, 67 assertions, 0 fail** (+7 over pass-18's 60); clj-kondo 0; clj-paren-repair Success; file 337 lines (< 800); `bb commit-check:file-lengths` exit 0. steps.md TT-B/TT-C/TT-D ticked; implementation.md PASS_STATUS: REVIEW_COMPLETE. 🔁 PATTERN (continues 204): a test that locks one *facet* of a multi-facet behaviour silently under-covers the rest — here the gate/baseline tests locked output *filenames* but not the *capture commands*, and the F3 lock covered the join *key* but not the *direction*; a later task-test-review re-derives `∀b∈design.∃t.covers` and finds the unlocked facets. Lock command-and-output, key-and-direction, not just one side.
- 2026-06-03: Task 204 (`incidental-complexity-simplification-workflow`) test-review pass-18 (task-test-review) follow-up **TT-A** executed (commit `13df6745a`). Sole newly-added actionable item (the trailing Contingency step-1 split predates every pass and is conditional/untriggered). Test-only — no production/skill/EDN change; the EDN is correct. The design (Deliverable 2, Step 1) names THREE required step-1 `:skills` — `incidental-complexity-finder` (selection recipe), `gordian` (selection methodology), `code-shaper` (refactor shaping) — and the shipped `reduce-incidental-complexity.edn` declares all three, but `reduce-incidental-complexity-test` locked only `incidental-complexity-finder`, so a regress dropping `gordian`/`code-shaper` passed green. Extended the existing `select-and-create` skill `testing` block (renamed "… + all three design-named skills") with `(some #{"gordian"} …)` + `(some #{"code-shaper"} …)` assertions. Focused `task-204-workflow-definitions-test` green: **2 tests, 60 assertions, 0 fail** (+2 over pass-17's 58); clj-kondo 0; clj-paren-repair Success; task-204 file 301 lines (< 800); `bb commit-check:file-lengths` exit 0. steps.md TT-A ticked; implementation.md PASS_STATUS: REVIEW_COMPLETE. 🔁 PATTERN (continues 204): a membership/enumeration lock that asserts only ONE of several design-named set members (here 1 of 3 `:skills`) silently under-covers — a later task-test-review re-derives `∀b∈design.∃t.covers` and finds the unlocked members; lock the full named set, not a representative element.
- 2026-06-02: Task 204 (`incidental-complexity-simplification-workflow`) test-review pass-17 (test-shaper) follow-up **TR21** executed. Sole newly-added actionable item (the trailing Contingency step-1 split predates every pass and is conditional/untriggered). Test-only — no production/skill/EDN change; the recipe is correct. The SKILL.md `gap` recipe ends with a `map({...})` projection re-emitting the chosen target's evidence with a dash→underscore rename (`end-line`→`end_line`, `lcc-total`→`lcc_total`, six `*-burden`/`working-set`→`*_burden`/`working_set`, plus `findings`/`cc`/`gap`) — the design's named step-5 acceptance, consumed verbatim by the workflow step-1 prompt to build the generated task's evidence block. Every other recipe test asserted only `ns`/`var`/`line`/`cc`/`gap` survival; the rename-and-re-emit projection was uncovered, so a dropped/mis-renamed field passed green while degrading the generated task's evidence. Added `incidental-complexity-finder-recipe-projection-contract-test` reusing the `run-jq-recipe` harness. **Fixture-shape discovery:** the existing `named-local-unit-json` hard-codes every burden to `1`, so reusing it could not catch a per-dimension field swap → added a dedicated `evidence-local-unit-json` builder giving the six burdens DISTINCT values (11..16), pinning the rename mapping per dimension. jq-present: feed one qualifying matched unit (lcc 30/cc 4→gap 7.5), assert all projected keys survive with expected values. jq-absent fallback (per TR12/16/17/18 convention): lock each projected key name verbatim incl. the eight `<underscore>: .["<dash>"]` renames. Focused ns green: **9 tests, 78 assertions, 0 fail** (+1 deftest over pass-16's 8); full `bb clojure:test:unit` green; clj-kondo 0; clj-paren-repair Success; skill-test file 524 lines (< 800); `bb commit-check:file-lengths` exit 0. steps.md TR21 ticked; implementation.md PASS_STATUS: REVIEW_COMPLETE. 🔁 PATTERN (continues 204): a recipe's final `map({...})` projection is the observable contract output (the evidence the generated task is built from), yet coverage tests asserted only `ns/var/line/cc/gap` survival — data *present in the input fixture* ≠ data *asserted in the output*, which is exactly what hid the gap. 🔁 PATTERN: a projection-rename test needs a fixture with DISTINCT per-field values; reusing one that hard-codes a shared sentinel (all burdens = 1) cannot catch a field-swap regress.
- 2026-06-02: Task 204 (`incidental-complexity-simplification-workflow`) test-review pass-13 (test-shaper) follow-up **TR15** executed (commit `b489b3a2c`). Sole newly-added actionable item. Test-only: `reduce-incidental-complexity-test` never locked the OUTER `select-and-create` `:session` step's `{{input}}` → `:workflow-input` wiring (the workflow entry-point data-flow contract). The sibling `task-lifecycle-in-worktree-test` locks the analogous wrapper-step wiring via `step-has-input-var-wired?`, but the outer step was uncovered → a regress dropping `:vars`/`{{input}}` or mis-wiring passed green. **Discovery (shape asymmetry):** the outer step wires `input` to the **bare** `{:from :workflow-input}` (NO `:path`), distinct from the wrapper steps' `{:from :workflow-input :path [:input]}` (which select the `:input` field of a delegated `:map`). So `step-has-input-var-wired?` (requires `:path [:input]`) could NOT be reused — confirmed by a failing first attempt — asserted the bare shape directly instead. File-length guard: new block pushed `workflow_definitions_test.clj` 797→814 (> 800 `components/` guard) → trimmed verbose TR7/TR8/TR10/TR11/TR13/TR14/TR2 comment headroom (assertions untouched) → exactly **800** lines. Focused green: 18 tests/261 assertions/0 fail (+2 over pass-12's 259); clj-kondo 0; clj-paren-repair Success. steps.md TR15 ticked; implementation.md PASS_STATUS: ACTIONABLE_FEEDBACK. 🔁 PATTERN (continues 204): there are TWO `:workflow-input` wiring shapes — the **bare** `{:from :workflow-input}` at the top-level entry step vs. the `{:path [:input]}` `:map`-field selector at delegate/wrapper steps; the shared `step-has-input-var-wired?` helper only matches the latter, so the entry-point step needs a direct shape assertion. 🔁 PATTERN (continues): adding a `testing` block near the 800-line `components/` guard forces trimming verbose TR-comment prose to stay under.
- 2026-06-02: Task 204 (`incidental-complexity-simplification-workflow`) test-review pass-11 (test-shaper) follow-up **TR13** executed (commit `766457abe`). Sole newly-added actionable item (the trailing Contingency step predates every pass and is conditional/untriggered). Test-only: `reduce-incidental-complexity-test` asserted the `lifecycle-in-worktree` `:delegate` only on `:type`/`:target`/`:prompt-string` — its `:context` was uncovered, so a regress dropping the second source `{:step "select-and-create" :yield :text}` (which propagates the step-1 structured handoff blob into the delegated wrapper's context — companion to the `:input` wiring, part of the verified cross-`:delegate` worktree-continuity mechanism, Locked decision 11) would pass green. Grounded against the live EDN (`:context` = `[{:from :workflow-original} {:from {:step "select-and-create" :yield :text}}]`) then added a `testing` block locking the full two-source vector, mirroring `task-lifecycle-test`'s own `:context` lock. File-length guard: the assertion+comment first hit 802 lines (> 800 `components/` guard) → trimmed the comment to land at **799**. Focused green: 13 tests/211 assertions/0 fail (+3 over pass-9's 208); clj-kondo 0; clj-paren-repair Success. steps.md TR13 ticked; implementation.md PASS_STATUS: REVIEW_COMPLETE. 🔁 PATTERN (continues 204): a `:delegate` step's data flow has TWO surfaces — `:prompt-string :input` AND `:context` — and a test that locks only the former leaves the handoff-propagating `:context` source un-covered; `task-lifecycle-test` already showed the `:context`-lock idiom to mirror. 🔁 PATTERN (continues): adding an assertion+comment to a `components/` test near the 800-line guard forces trimming the explanatory comment to stay under.
- 2026-06-02: Task 204 (`incidental-complexity-simplification-workflow`) test-review pass-2 follow-up **TR3** executed. Sole newly-added actionable item (the trailing Contingency step predates every review pass and is conditional). Test-only: TR1's `incidental-complexity-finder-skill-content-lock-test` locked the gap method/thresholds/single-unit-scope/A1-drop/F2-`@line`-key but **omitted two named Deliverable-1 behaviours** — the step-5 top-5 essential-vs-incidental **judgment guard** (the design's core discriminator; without it the skill degenerates to the `gordian complexity` ranking it exists *not* to be) and the step-6 evidence + **coverage-hint** emission ("produces a target + evidence"). Added two `testing` blocks locking: (5) "top 5 qualifying units by `gap`", "Reject as **essential**", "Choose the first unit (highest `gap`) that passes the guard", "If none of the top 5 pass"; (6) "coverage hint", "sibling test namespace exists for the target", "any test references the target `var`". SKILL.md already carried every string → no SKILL/design/workflow change. NS deviation from C1's "same ns": the skill's content-lock+determinism tests had already been **split** out of `workflow_definitions_test.clj` into `incidental_complexity_finder_skill_test.clj` (combined ~873 lines > the 800-line `components/` file-length guard); added TR3 in the split file's home. Focused green: skill-test 3/27 + definitions 13/198 = 16 tests/225 assertions/0 fail; clj-kondo 0; file-length guard clean. steps.md TR3 ticked; implementation.md PASS_STATUS: RESOLVED. 🔁 PATTERN: content-lock tests that *enumerate* the strings to assert silently under-cover — a later review re-derives "∀b∈design.∃t.covers" and finds the *omitted* named behaviours (here the judgment-guard procedure vs. only its rationale string). 🔁 PATTERN (continues): a file-length guard forces a test-ns **split**, which legitimately supersedes a "no new ns" review constraint (C1) when the constraint's intent (no new *production* code, extend existing test surface) still holds.
- 2026-06-01: Task 204 (`incidental-complexity-simplification-workflow`) impl-review pass-4 follow-up **F4** executed (commit `84c58398c`). Sole newly-added actionable item. F4 = design(spec)↔SKILL(mechanism) coherence gap: `design.md` Deliverable-1 **selector** procedure step 2 (line 62) still joined on the pre-F2 `(ns, var, arity)` key while the implemented `incidental-complexity-finder` SKILL recipe (F2) and the A5/A2 acceptance prose (F3) key on `(ns, var, arity, line)` for null-arity `defmethod` determinism (51 `execute-effect!` defmethods collapse without `line`). Fix: design step 2 → `(ns, var, arity, line)` + one-clause disambiguation note + cross-ref to A5/A2 & SKILL §2/§3. Bare `(ns, var, arity)` mentions (lines 64, 222) deliberately retained — they denote a unit's **logical identity** (the collision `line` resolves), per the SKILL §3 identity-vs-join-key distinction. `design.md` prose only; no code/test/doc outside task artifacts (SKILL recipe already keyed on `@line`). steps.md F4 ticked; implementation.md resolution recorded. 🔁 PATTERN (continues prior 204 pattern): F2→F3→F4 is a key-change *propagation chain* — a determinism fix in the mechanism (SKILL recipe) must be chased through every spec surface that quotes the key (acceptance prose, then the selector description); review passes catch one un-propagated surface at a time. Remaining 204 unchecked: only the conditional Contingency step-1 split (predates this pass).
- 2026-06-01: Task 204 (`incidental-complexity-simplification-workflow`) plan/steps ambiguity (pass 1) follow-up P1–P4 executed. All four are plan/steps SPECIFICATION resolutions (slices 1–5 unbuilt, so no code/test/doc outside task artifacts touched; design.md untouched — design review already REVIEW_COMPLETE). P1: wrapper gets a `summary` step → **three-step** wrapper (resolve-worktree → lifecycle → summary), because outer step-2 is `reduce-incidental-complexity`'s terminal step so it needs a user-facing terminal summary; mirrors `implement-task-in-worktree.md` (verified it's actually 3-step, corrected plan's "two-step" mischaracterization); propagated to steps Slice-2/Slice-4 + plan. P2: annotated both `local` sites — `--sort total` is selector-only display, `before-local.json` uses bare `local --json`; sort irrelevant to `(ns,var,arity)`-keyed before/after (carries design inconsistency-review conclusion into steps). P3: NNN allocated by scanning the **worktree's** `open/ ∪ closed/` (origin/master-based, where `work-on` is active), not the outer checkout. P4: task dir created **and committed on the work-on worktree branch** so `munera_task_path:` resolves for step-2's resolve-worktree. P1–P4 checked. 🔁 PATTERN: plan/steps ambiguity follow-ups resolve as spec edits to plan/steps (+implementation.md), not code, when the referenced slices aren't built yet; design stays frozen and supersession is recorded in plan/steps.
- 2026-06-01: Task 204 (`incidental-complexity-simplification-workflow`) plan.md + steps.md created from the stable design.md (commit `0d0a04965`). Design review was complete (architecture-fit + A1–A5 + I1 all REVIEW_COMPLETE; design-steps unchecked 0) so the plan gate was satisfied. Plan = 5 dependency-first vertical slices: (1) `incidental-complexity-finder` SKILL.md (`.psi/skills/`, gap=lcc-total/max(cc,1) recipe + qual filter lcc≥5∧gap≥2 + top-5 essential-vs-incidental guard + inner-join-on-local unmatched-row drop); (2) `task-lifecycle-in-worktree.md` wrapper (`.psi/workflows/`, resolve-worktree `:session`+work-on → lifecycle `:delegate :target task-lifecycle`, mirrors `implement-task-in-worktree`); (3) `reduce-incidental-complexity.edn` outer two-step (step-1 `:session` select+worktree+baselines+task+structured-handoff w/ early-stop; step-2 `:delegate :target task-lifecycle-in-worktree` via `:prompt-string {:type :map :fields {:input {:from {:step … :yield :text}}}}`); (4) workflow definition tests; (5) docs+CHANGELOG. No production Clojure — all artifacts are skill md + workflow edn/md, verified by workflow-loader definition tests. Non-blocking open Q carried forward: step-1 granularity (keep fat single step unless unwieldy → contingency split). 🔁 Build order dependency-first so each artifact's skill/target refs resolve as added.
- 2026-06-01: Task 204 (`incidental-complexity-simplification-workflow`) design inconsistency (pass 1) follow-up I1 executed (commit `fcf82c301`). I1 was the only newly-added unchecked `design-steps.md` item (architecture-fit + A1–A5 predate this pass, already checked). Finding: design claimed a `:delegate` to `task-lifecycle` inherits step-1's worktree as "verified behaviour" citing `implement-task-in-worktree.md`, but that precedent proves the opposite — a worktree-resolving WRAPPER whose `resolve-worktree` `:session` step re-calls `work-on` from a threaded `worktree_path:` handoff field before sub-delegating. Grounded: `task-lifecycle.edn` sub-workflows read only `{:input <path>}` (no `work-on`); `complexity-reduction-pr.edn` does everything in ONE `:session` step (never crosses delegate post-worktree); runtime `child_session_state.clj:131` DOES copy `(:worktree-path parent-sd)` into child sessions, but a *direct* `task-lifecycle` delegate relying on it is unverified/no-precedent. Resolution (a): adopt the verified wrapper pattern + correct citation — Step 1 emits structured handoff (`worktree_path:`+`munera_task_path:`), Step 2 delegates to a thin `task-lifecycle-in-worktree` wrapper (resolve-worktree+work-on → lifecycle delegate), structurally identical to `implement-task-in-worktree`. Propagated across Verified-facts (2 bullets), Step 1/2, Scope, Acceptance (+wrapper-exists criterion), two-step paragraph, locked decision 11. design-steps unchecked count 0. No steps.md/plan.md touched. 🔁 PATTERN: "X inherits Y as verified behaviour" claims citing a wrapper precedent are usually wrong — the wrapper RE-ESTABLISHES Y explicitly; check whether the precedent crosses a `:delegate` boundary directly or via a re-resolving `:session` step.
- 2026-06-01: Task 200 (closed) C2 code-review follow-up executed (commit `e33571360`). Single-sourced the tool-result override *value semantics*: extracted `modifiable-tool-result-coercions` table (`:content`→`normalize-tool-content`, `:is-error`→`boolean`) in `agent-session/extensions.clj`; both `tool-result-event` (inbound) and `merge-tool-result-override` (override application) now derive coercions via `coerce-tool-result-value`. Closes the inbound/applied asymmetry — override `:content`/`:is-error` now normalized identically to the inbound payload. Added 2 focused merge coercion tests; updated 2 pre-existing merge assertions (now expect normalized content-blocks); CHANGELOG [Unreleased] Changed entry added. clj-kondo 0/0; full unit suite green. C2 was the only newly-added unchecked steps.md item; all task 200 items now checked.
- 2026-06-01: Task 198 (`task-lifecycle-chained-workflow`) docs review pass 4 (`review-task-docs`): REVIEW_COMPLETE. All three prior docs follow-ups landed and verified — (1) `:map` `:prompt-string` form documented in `doc/workflow-grammar-concepts.md`; (2) pass 2 no feedback; (3) pass-3 CHANGELOG entry moved from stranded `[0.1.2166]` released section into `[Unreleased] → Added` (commit `0dee7f1ee`). CHANGELOG description/names/`/delegate` invocation all match `.psi/workflows/task-lifecycle.edn`. `doc/workflows.md` correctly not required (example-led guide). No new actionable docs feedback; no new steps. Commit `e19e8ad50`.
- 2026-06-01: Task 199 (`metrics-turn-finished-handler-logging-idiom`) implemented in one slice. Replaced the raw `println "DEBUG [psi/metrics]..."` swallowed-exception log in `make-turn-finished-handler` (`extensions/metrics/src/psi/metrics/extension.clj`) with project-standard `(timbre/warn e "skipping token tracking for session" session-id)`; added `[taoensso.timbre :as timbre]` require and explicit `com.taoensso/timbre {:mvn/version "6.8.0"}` to `extensions/metrics/deps.edn :deps`. Verified: standalone+`:test` classpaths both resolve timbre 6.8.0 (no conflict), ns loads standalone, clj-kondo clean, behaviour unchanged (exception swallowed, `nil` returned). Out of scope (excluded counter-examples): `persistence.clj:47,51` sibling `println` sites. Implementation complete pending review/closure.
- 2026-06-01: Task 201 design inconsistency review (pass 1). design.md is now internally/cross-artifact consistent after the ambiguity follow-up (A1–A6 resolutions verified against `review-task-design.edn`, shared `review-follow-up-design.md`, `statechart.clj` start-step/leaf fall-through, `final-summary` `:contributions`, `doc/workflows.md`, and the workflow-definitions test). One new actionable inconsistency: I1 — AC2a says the new `architecture-review` prompt "ends with exactly one `PASS_STATUS: A | B` line," contradicting Scope/Architectural-alignment's demand for consistency with the existing `review-task-design-*-review.md` prompts, which use a two-line menu. Added implementation.md note + design-steps.md I1. PASS_STATUS: ACTIONABLE_FEEDBACK.
- 2026-06-01: Task 201 design ambiguity review (pass 1, commit `f5ac72e1b`). Created implementation.md + design-steps.md (A1–A6). Central finding: design's "dedicated per-aspect follow-up pattern" premise is STALE post-199 — ambiguity/inconsistency follow-ups already share `review-follow-up-design.md`. Other actionable ambiguities: AC2a names a `review-task-design-architecture-follow-up.md` that contradicts the shared profile (A2); `clarity-status`→`final-summary` transition mechanism unspecified since current clarity-status has no `:on` and final-summary follows positionally (A3); workflow entry/start-step determination unspecified (A4); architecture-follow-up DONE `:goto` target unspecified (A5); whether final-summary adds an architecture-review contribution source unspecified (A6). PASS_STATUS: ACTIONABLE_FEEDBACK.

- 2026-06-01: Task 202 test-review follow-up (T1/T2/T3) executed (commit `a93412e7f`). Added regression locks to `workflow_definitions_test.clj`: T1 asserts the predate-exclusion guard ("predate the preceding review pass") in all three host follow-up bodies (design/plan/review-step); T2 asserts the steps-profile R1 broadening ("code, tests, and docs") in plan + review-step follow-up bodies; T3 extends `review-task-prompt-artifact-targets-test` to assert the four removed per-aspect files leave no orphan refs in the three rewired host `.edn`s. Suite green (9 tests, 131 assertions, up from 111); test ns lint clean. Only the test file changed. Task remains closed.

- 2026-06-01: Task 202 implementation-review follow-up (R1) executed (commit `3db8ee341`). The shared `steps`-profile follow-up (`review-follow-up-steps.md`) hosts BOTH plan review and implementation review (via `review-step` → `review-task-implementation`); its narrowed "update plan.md/steps.md" wording risked misleading impl follow-ups into not editing real code/tests/docs. Fix: body now explicitly permits editing referenced code/tests/docs; design.md steps-profile writable set + AC4 + doc/workflows.md profile tables updated to match. Only `.md` changed; workflow-definitions suite green (9 tests, 111 assertions). Task remains closed.

- 2026-06-01: Task 108 sixth test-shaper follow-up executed (the single newly-added unchecked `steps.md` item). Consolidated the sextupled session-with-resolvable-worktree construction in `commands_test.clj` into `psi.project-nrepl.test-support/session-ctx-at` (`[worktree-path] → [ctx session-id]`), single-sourcing the `{:persist? false :session-defaults {:worktree-path …}}` shape. The six `commands_test` sites now `:refer`+call `(session-ctx-at <wt>)`; dropped the now-unused agent-session `test-support` alias. Re-expressed `make-ctx` on `session-ctx-at` (passing `user.dir`, discarding session-id) so `create-test-session` is called directly only inside `session-ctx-at`. Focused project-nrepl suite (8 ns) 26 tests/151 assertions/0 failures (unchanged — pure helper extraction); lint 0/0. Step checked; detail in task `implementation.md`. PASS: sixth-pass review was ACTIONABLE_FEEDBACK; this follow-up closes that finding.
- 2026-06-01: Task 108 fifth test-shaper follow-up executed (the single newly-added unchecked `steps.md` item). Consolidated the triplicated happy nullable-connector construction into `psi.project-nrepl.test-support/fake-connector` (`([])`/`([session-id])`, default `"nrepl-session-1"`): returns a fn of `_endpoint` yielding a constant `{:transport {:transport :fake} :client (fn ([] nil) ([_] nil)) :client-session (session-fn-with-id session-id)}` map (built once + closed over, so `client_test` can invoke it once to recover the exact objects for its equality assertions). Folded onto the helper: `client_test/connect-instance-in-test`, `attach_test/attach-instance-in-test`, `started_test/start-instance-in-test` — `session-fn-with-id` no longer `:refer`-ed by those three. Distinct-behaviour connectors stay inline (metadata-less missing-session-id connector; throwing attach-failure connector). Focused project-nrepl suite (8 ns) 26 tests/151 assertions/0 failures (unchanged — pure helper extraction); lint 0/0. Step checked; detail in task `implementation.md`. PASS: prior fifth-pass review was ACTIONABLE_FEEDBACK; this follow-up closes that finding.
- 2026-06-01: Task 108 third test-shaper review pass executed. Re-read all 8 project-nrepl `*_test.clj` + `test_support.clj`. Suite strong (¬with-redefs, ¬interaction-atoms, state-based, deterministic, single-concern). One new actionable economy/consistency finding: `client_test.clj`'s two `connect-instance-in!` tests duplicate the full connector-seed ceremony (`ensure-instance-in!` + `update-instance-in!` seeding `[:runtime-handle :nrepl-connector]`) — the one seam-seeding idiom never folded into `test-support`. Recommended a shared `seed-connector!` helper mirroring `install-instance!`. Added one unchecked steps.md follow-up + review note in implementation.md. Deliberately did NOT raise the pre-existing `user.dir` instance-key idiom or `disconnect`'s `closed*` real-effect check (scope/precedent). Focused client+config re-run green (10 tests/46 assertions). PASS_STATUS: ACTIONABLE_FEEDBACK.
- 2026-06-01: Task 108 test-review follow-up (task-test-review pass) executed — the single newly-added unchecked `steps.md` item. Added `client_test.clj` `connect-instance-in-missing-session-id-test`: seeds a `[:runtime-handle :nrepl-connector]` returning `:client-session (fn [_] nil)` (no `:nrepl.core/taking-until` metadata) and asserts `connect-instance-in!` throws `ExceptionInfo` `#"did not expose a session id"` via `thrown-with-msg?`. Closes the design-retained missing-session-id throw coverage gap through the existing seam (no new seam). `client_test`: 3 tests/14 assertions (up from 2/12); focused project-nrepl suite (8 ns) green; lint 0/0. Step checked; detail in task `implementation.md`.
- 2026-06-01: Task 108 second-pass implementation-review follow-ups executed (the two newly-added unchecked `steps.md` items). (1) `client_test.clj` `connect-instance-in-test` now uses shared `test-support/session-fn-with-id` instead of an inline `:nrepl.core/taking-until` `with-meta` copy — no inline copies of that metadata shape remain. (2) Hand-rolled temp-dir create/delete in the previously-untouched bodies folded onto shared `temp-dir`/`delete-tree!`: `config_test` `read-project-preferences-test` (×3) + `read-dot-nrepl-port-test` (×2), and `commands_test` missing-start-command test (dropped now-unused `clojure.java.io` require). Each file now uses one temp-dir idiom. Verification: focused config/client/commands 11 tests/64 assertions green; full project-nrepl suite 25 tests/153 assertions green; lint clean. Both items checked in `steps.md`; details in task `implementation.md`.
- 2026-06-01: Task 108 (`project-nrepl-testing-without-mocks`) implementation complete — all 12 slices executed; `steps.md` fully checked. De-mocked all six in-scope `components/project-nrepl/test/` files (config, client, attach, started, commands, ops) → zero `with-redefs`. Three production changes: (1) `client.clj` `real-nrepl-connector` seam default + `[:runtime-handle :nrepl-connector]` resolution (session-id derivation stays in `connect-instance-in!`); (2) `started.clj` `real-process-launcher` seam default + `start-instance-in!` runtime-handle merge-not-overwrite; (3) optional `:runtime-handle` seam seed on `attach-instance-in!` (new 4th-positional `opts` arity) and `start-instance-in!`. Canonical seam idiom = fn value in `[:runtime-handle <key>]`, seeded at acquisition, resolved via `(or seeded real-default)` — mirrors `eval_test.clj`'s `:client-session`. Verification: all 8 project-nrepl test ns green (25 tests, 154 assertions), `psi.agent-session.project-nrepl-extension-install-test` consuming-path green (1 test, 5 assertions), `clj-kondo` clean. Two per-seam strategy notes + "no remaining exceptions" recorded in task `implementation.md`. Discovered real-behavior fact: `eval-instance-in!` result omits `:ns`, so public `eval-op` `:ns` is always nil (prior mock fabricated `:ns "user"`). Ready for review/closure.
- 2026-05-31: Munera open-task reconciliation audit (commit `48745611`). plan.md had drifted badly from the `open/` directory and concurrent branches had reused NNNs. Actions: (1) closed completed 164, 187, 188, 190, 191-fix-judge-structured-output-non-object-schema, 193; (2) renumbered colliding incomplete tasks 154→195 (fix-workflow-max-iterations), 194→196 (deterministic-review-step-routing), 191-ui-action→197 (ui-action-invocation); (3) rebuilt plan.md from authoritative open/. NOTE: closed/ has many historical internal NNN collisions (016×3, 154×2, 166×3, etc.) left as-is — only active-work ambiguity was resolved. NOTE: prior state.md "Task 190" worklog conflates TWO different 190s — closed/190-ai-request-retry-backoff AND now-closed open/190-conditional-review-follow-ups. Current open (3): 021, 108, 197. (157 closed by 刀 decision — jar-owned deps release startup work landed via PRs #109/#110/#112; steps.md never executed in-task.)
- 2026-05-31: Closed completed tasks 173, 175, 186, 189 (`git mv` open→closed) and reconciled `munera/plan.md` open backlog. All four had fully checked `steps.md` (173 had only the open-ended "continue to next task and repeat" audit-loop item left, closed by 刀 decision). Commit `061aa929`. Remaining open: 021, 108, 154, 157, 001, 002, 003, 006, 005, 191.
- 2026-05-30: Task 190 code-shaper follow-up executed: design/plan per-reviewer `workflow/pass-status-routing` judges now pass `:allowed-statuses ["ACTIONABLE_FEEDBACK" "REVIEW_COMPLETE"]`; globally known implementation tokens now fail those review steps with deterministic `:invalid-pass-status`, while callers that omit `:allowed-statuses` preserve global token behavior. Focused workflow tests and targeted lint are green; code-shaper follow-up item is checked.
- 2026-05-30: Task 190 docs review follow-ups completed and committed (`accfd3d8`): CHANGELOG [Unreleased] now documents user-visible conditional per-reviewer follow-up behavior and plan-review `steps.md` targeting; `doc/workflow-ir.md` now reflects executed invoke-judge runtime support. All docs follow-up items are checked.
- 2026-05-30: Task 190 docs review applied `review-task-docs`: found actionable documentation follow-ups for CHANGELOG user-visible conditional design/plan review behavior plus `doc/workflow-ir.md` invoke-judge runtime support note; added unchecked `steps.md` items and recorded review note.
- 2026-05-30: Task 190 third test-shaper follow-up executed: workflow definition tests now assert the full actual `clarity-status` `:on` map for both design/plan workflows (`REPEAT` → `ambiguity-review` with `:max-iterations 6`, `DONE` → `final-summary`); focused workflow tests and targeted lint are green, and the new `steps.md` item is checked.
- 2026-05-30: Task 190 second test-shaper follow-up executed: workflow definition tests now assert each design/plan follow-up step uses the full `workflow/constant-routing` judge shape with `:args {:route "DONE"}`; focused workflow tests and targeted lint are green, and the new `steps.md` item is checked.
- 2026-05-30: Task 190 test-shaper follow-up executed: workflow definition tests now assert each design/plan per-reviewer `workflow/pass-status-routing` judge reads that same step's own `:final-llm-reply`; focused workflow tests and targeted lint are green, and the new `steps.md` item is checked.
- 2026-05-30: Task 190 plan inconsistency review pass re-read plan/steps/implementation/design artifacts plus workflow docs/definitions/prompts/tests and found no new actionable plan inconsistencies. Recorded review note only; no new `design-steps.md` follow-up items were needed.
- 2026-05-30: Task 190 design ambiguity follow-up execution completed after the latest ambiguity-review pass: `design-steps.md` now exists, but its only item predates the preceding ambiguity review and is already checked from the earlier inconsistency follow-up; no newly added unchecked ambiguity items were executed. Recorded in `implementation.md`.
- 2026-05-30: Task 190 implementation pass completed: design/plan review workflows now conditionally run per-reviewer follow-ups via deterministic `workflow/pass-status-routing`, plan-review prompts target `steps.md`, focused routing/definition tests and targeted lint are green. Ready for implementation review/closure consideration.
- 2026-05-30: Task 190 design inconsistency follow-up execution completed as a blocked/no-op after the latest inconsistency-review pass: `design-steps.md` is absent, so there were no newly added unchecked design inconsistency items to execute or mark done. Recorded in `implementation.md`; commit `e08009b3`.
- Registry unification arc through task 177 is complete in implementation/review terms:
  - 164: registry semantics audit is the migration-rules source of truth and now includes post-migration guidance from 169–173
  - 165: root-registry target architecture captured
  - 166: standalone `root-registry` component built
  - 167: command-registry migrated onto root-registry storage
  - 168: tool-registry migrated onto root-registry storage; higher extension-detail projections read canonical tool-registry data
  - 169: workflow-registry migrated onto root-registry storage while preserving canonical compatibility path `[:workflows :definitions]`; higher semantic seams route through workflow-registry
  - 170: root-registry semantic alignment added distinct duplicate-rejecting `insert` versus replace-capable `register`, clarifying future adopter contracts
  - 171: deterministic-operation-registry migrated to shared root-registry storage; canonical operations live in root-registry, while invoke-miss throwing, duplicate-throw translation, and extension projection synchronization remain adapter-owned
  - 172: deterministic-operation registration-order semantics removed; public operation listing now preserves unordered membership/count coherence rather than insertion order
  - 173: skill registration-order semantics removed; registry/projection/model-visible skill-list surfaces use canonical exact skill-name ordering while duplicate-ignore and `:added?` / `:changed?` remain preserved
  - 174: skill-registry migrated to adapter-backed root-registry storage; canonical skill definitions now live in `root-registry` while sessions own membership through `:skill-ids`
  - 176: prompt-registry simplified toward root-registry semantics — canonical identity is now string-coerced `id` alone, cross-owner same-id coexistence is disallowed via explicit ownership conflict, and higher prompt-contribution projections were aligned to shared canonical ordering
  - 177: prompt-registry migrated onto shared `root-registry` storage via `prompt-registry.root-storage`; sessions now own canonical prompt membership through `:prompt-contribution-ids`, while `:prompt-contributions` vectors remain derived compatibility projection only across new/resume/fork/child flows and higher read/introspection seams
- Bootstrap simplification arc (159–163) complete:
  - 159: in-process bootstrap simplification
  - 160: removed mutation-mediated bootstrap resource loading
  - 161: single-pass startup, `bootstrap-in!` and `refresh-active-tools-in!` removed
  - 162: `bootstrap-runtime-session!` collapsed to single `(ctx ai-model opts)` arity
  - 163: `start-tui-runtime!` refactored — dead `ai-ctx` removed, nullable exec mode extracted
- Structured-output arc status:
  - task 169 is complete and closed: model/provider structured-output capability surface implemented
  - task 171 is complete and closed: Anthropic JSON Schema native output implemented and live-verified
  - task 170 remains the active adjacent workflow adoption slice; latest follow-ups clarified unsupported/fallback-forbidden behavior, top-level `:structured-output` turn-result seams, and success-path envelope propagation
- Tasks 194, 188, 151, 145, 140, 139, 138, 136, 134, 130, 128, 125 also complete and closed

## Test health

- `bb test` is green after closing task 177.
- `bb test` was green after closing task 173.
- Focused registry/projection tests passed during tasks 169–173.
- Task 176 recorded focused prompt-registry / projection verification passing:
  - `clojure -M:test --focus psi.prompt-registry.contributions-test --focus psi.agent-session.query-graph-tools-test --focus psi.agent-session.model-dispatch-test`
  - `clj-kondo --lint components/prompt-registry/src components/prompt-registry/test components/agent-session/src components/agent-session/test components/extension-test-helpers/src`
- Task 176 review loops (implementation review, test review, test-shaper, code-shaper) recorded no new actionable feedback.
- Task 177 recorded focused prompt/session/workflow/nullable-helper verification, targeted lint, and a final full `bb test` pass before close.
- Tasks 176 and 177 review loops (implementation review, test review, test-shaper, code-shaper) recorded no new actionable feedback.
- Focused structured-output/model tests passed during tasks 169 and 171.
- Task 170 follow-up verification recorded focused workflow tests green for structured-output envelope propagation and failure-surface behavior.
- Task 158 addressed persistence test garbage (still open but test-review showed no actionable feedback).

## Suggested next step
- For task 191, structured-output non-object schema implementation/review is complete; PR is open for merge.
- Task `194-core-queryable-ui` is complete and closed; it made UI capabilities/actions queryable from core for extensions, including discoverable "make UI visible" action descriptors.
- Registry unification arc: use task `164-registry-semantics-unification-audit` plus completed outcomes through `177` to choose the next cleanup target.
- Likely next registry cleanup candidates:
  - close or move any remaining Munera tasks whose implementation/review state is already complete
  - `skill-registry` shared-substrate/helper cleanup, now that insertion order is no longer semantic
  - prompt-registry normalization/shared collection-helper audit
  - remaining root-registry adopter polish if `164` identifies unresolved seams
  - decide whether out-of-scope registries (`model-registry`, memory provider registry, extension handler registry) need a separate audit rather than direct adoption
- Structured-output arc: task 170 remains the active adjacent workflow adoption slice; its test-shaper follow-up is complete and the remaining work is within that task.
- Backlog: `105-agent-session-component-extraction-map`, `124-turn-execution-contract-extraction`, `149-reload-fixup-inventory-and-safety`, `141`/`144`/`147` workflow items, `186-built-in-skill-for-developing-extensions`

## Latest session notes
- 2026-06-01: Task 108 implementation-review follow-ups complete. (1) Consolidated duplicated project-nrepl test helpers into shared `psi.project-nrepl.test-support` (`make-ctx`/`install-instance!`/`temp-dir`/`delete-tree!`/`session-fn-with-id`); all seven `*_test.clj` files (incl. runtime_test) now `:refer` it, removing the canonical seeded `:runtime-handle` drift risk. (2) Dropped `client_test`'s `@calls*` input-capture interaction assertion (state assertions already prove connector result use). 25 tests, 153 assertions, 0 failures; lint clean. Both `steps.md` follow-ups checked.
- 2026-05-30: Task 190 code-shaper follow-up complete: compaction retained-suffix normalization now advances past retained assistant tool-use plus contiguous `toolResult` messages before placing preserved `:mid-system`; added direct rebuild and journal replay coverage; focused compaction test/lint green; checked the follow-up in `steps.md`.
- 2026-05-30: Task 190 code-shaper review found actionable compaction boundary issue: mid-system preservation normalization drops only through last assistant, so retained assistant/toolResult tails can rebuild as `summary user → system → toolResult`. Added unchecked `steps.md` follow-up and implementation review note.
- 2026-05-30: Task 190 test-shaper pass after capability-predicate follow-up found no new actionable test-shaping feedback; focused speed/effort/mid-system/compaction/provider verification passed (`65 tests, 496 assertions`); implementation.md records the pass and steps.md was unchanged.
- 2026-05-30: Task 190 code-shaper follow-up complete: `supports-mid-system-messages?` now lets explicit `:supports-mid-conversation-system-messages false` override OpenAI chat-completions inference; added focused resolver coverage for a custom disabled OpenAI chat model; focused test/lint green; checked the follow-up in `steps.md`.
- 2026-05-30: Task 190 task-test-review pass found no new actionable test feedback after re-reading task artifacts and focused speed/effort/mid-system/compaction/provider tests; implementation.md records the pass and steps.md was unchanged.
- 2026-05-30: Task 190 code-shaper follow-up complete: aligned `psi.extension/inject-mid-system-message` Pathom `::pco/params` with implemented optional `:source`/`:ext-path` provenance inputs, added contract coverage in `extensions_test.clj`, focused test/lint green, and checked the follow-up in `steps.md`.
- 2026-05-30: Task 190 docs follow-up complete: aligned `doc/extension-api.md` mid-system examples and changelog with implemented extension API key `:inject-mid-system-message` (no bang), recorded implementation note, and checked the follow-up in `steps.md`.
- 2026-05-30: Task 190 docs follow-up complete: corrected `doc/configuration.md` to state no `psi.extension/set-speed-mode` or `psi.extension/set-effort-override` mutation exists, direct users to `/speed` and `/effort`, recorded implementation note, and checked the follow-up in `steps.md`.
- 2026-05-30: Task 190 docs review found actionable doc accuracy issue: `doc/configuration.md` claims extension/runtime mutation surfaces for speed/effort, but no `psi.extension/set-speed-mode` or `psi.extension/set-effort-override` mutations are registered. Added unchecked docs follow-up and implementation review note.
- 2026-05-30: Task 190 test-shaper review found no new actionable test feedback; implementation.md records the pass and steps.md was unchanged.
- 2026-05-30: Task 190 ambiguity follow-up execution after the post-effort-coverage ambiguity review found no unchecked design-steps (`unchecked count 0`); implementation.md records the no-op pass and design/plan/steps were unchanged.
- 2026-05-30: Task 190 test review found actionable effort override coverage gap: provider tests prove `:xhigh` ceiling/default paths but not non-`xhigh` override precedence for Anthropic adaptive, OpenAI chat-completions, and Codex/responses. Added unchecked `steps.md` follow-up and implementation review note.
- 2026-05-30: Task 190 ambiguity follow-up execution after post-test-review ambiguity pass found no unchecked design-steps (`unchecked count 0`); implementation.md records the no-op pass, design/plan/steps were unchanged, and existing uncommitted OpenAI test edits were left untouched.
- 2026-05-30: Task 190 plan/steps inconsistency review found no new actionable inconsistencies; implementation.md records the pass, design-steps.md was unchanged, and existing uncommitted test edits were left untouched. Commit `b959502d`.
- 2026-05-30: Task 190 test review found actionable coverage gap: scoped `/speed` and `/effort` tests prove command/session state but not project/user persistence writes or explicit default masks. Added unchecked follow-up in `steps.md` and committed review note (`b8d39a66`).
- 2026-05-30: Task 190 inconsistency follow-up complete: reran final-state `bb test` after the latest compaction replay code/test follow-up, full suite passed, and checked the post-final full-suite verification item in `design-steps.md`. Commit `ca2c68b6`.
- 2026-05-30: Task 190 plan/steps inconsistency review found one new actionable inconsistency: Slice 5 `bb test` verification is checked/recorded before the later compaction replay post-history code/test follow-up, so final-state full-suite verification evidence is stale. Added unchecked `design-steps.md` follow-up and implementation review note.
- 2026-05-30: Task 190 Slice 5 integration/coherence pass complete: README/doc/configuration/doc/tui/doc/extension-api/CHANGELOG updated for Opus 4.8, `/speed`, `/effort`, and mid-system extension injection; RPC xhigh display tests aligned; focused tests, targeted lint, and full `bb test` green.
- 2026-05-30: Task 190 latest inconsistency follow-up execution found no unchecked design-steps (`unchecked count 0`); implementation.md records the no-op pass and design/plan/steps were unchanged. Existing uncommitted test-file modifications were left untouched.
- 2026-05-30: Task 190 Slice 4 foundation pass complete: added schema-valid `:system` AI messages, `:mid-system` journal projection, prepared-turn `user → system` tail preservation, Anthropic/OpenAI inline system transforms/schema, and focused tests/lint green. Remaining Slice 4 work is capability resolver/dispatch/extension mutation/compaction.
- 2026-05-30: Task 190 Slice 3 is complete. Added `/effort` session/config/startup/resolver/footer/request-shaping stack; adaptive Anthropic `:xhigh` now sends `"highest"`, OpenAI/Codex cap override `:xhigh` to `"high"`; focused tests/lint green.
- 2026-05-30: Task 190 Slice 2 is complete. Added cold-resume transience proof: resume now excludes session-transient `:speed-mode`, preserving nil canonical state with resolver display `:normal`; focused session lifecycle test/lint green. Commit `ed099c63`.
- 2026-05-30: Task 190 plan/steps latest repeat inconsistency review found no new actionable inconsistencies; implementation.md records the pass and design-steps.md was unchanged. Commit `589c6268`.
- 2026-05-30: Task 190 latest ambiguity follow-up execution found no newly added unchecked ambiguity design-steps (`unchecked count 0`); implementation.md records the no-op pass and design/plan/steps were unchanged.
- 2026-05-30: Task 190 plan/steps repeat inconsistency verification found no new actionable inconsistencies; implementation.md records the pass and design-steps.md was unchanged.

- 2026-05-30: Task 190 follow-up completed the final unchecked streaming header item: `turn-runtime/make-provider-event-consumer` now forwards normalized `:provider-error/headers` from streaming `:error` events into `:turn/error`, and the accumulator preserves those headers for retry metadata when raw `:headers` is absent. Added focused coverage proving `Retry-After`/rate-limit metadata on provider-header-only background streaming errors drives retry scheduling. Verification: focused new test, full `psi.turn-runtime.response-mode-test` (`18 tests, 123 assertions`), and turn-runtime clj-kondo green. Committed `469f3a9b`.

- 2026-05-30: Task 190 implementation review found one new actionable gap after the async streaming metadata follow-up: `psi.ai.streaming/exception->error-event` now preserves normalized `:provider-error/headers`, but `turn-runtime/make-provider-event-consumer` forwards only `:headers` from streaming `:error` events into `:turn/error`. Added task follow-up to cover background streaming errors carrying only `:provider-error/headers` and forward those headers before retry metadata calculation. Committed `294b836a`.

- 2026-05-30: Task 190 review follow-up completed async streaming exception metadata preservation: `psi.ai.streaming/exception->error-event` now carries `:headers`, `:provider-error/headers`, `:http-status`, and `:status` from provider exception ex-data through the background streaming future path. Focused AI/turn-runtime/prompt telemetry tests and targeted clj-kondo passed.

- 2026-05-30: Task 190 implementation-review follow-up completed terminal telemetry duplication fix: canonical prompt-lifecycle terminal provider failures now prove exactly one `provider_request_finished` event is dispatched to `/ext/provider-telemetry`, and legacy `:on-agent-done` compatibility telemetry is gated when provider-boundary retry outcome metadata is already present. Focused prompt/statechart/turn-runtime/EQL verification passed (`52 tests, 285 assertions`) and targeted clj-kondo passed.

- 2026-05-30: Task 190 follow-up pass completed remaining header/introspection/success-path proof gaps: EQL provider retry attempts now expose `:psi.provider-retry/rate-limit`, first-attempt success telemetry test proves no retry outcome/schedule and active retry cleanup, changelog documents provider-boundary retry reliability and retry-history graph visibility, and app-runtime/RPC retry status tests now expect the intentional active `phase:retrying`. Focused retry/app-runtime/RPC verification passed (`65 tests, 375 assertions`) and targeted clj-kondo passed with only existing info findings; broad `bb test` was attempted before the status-test fixes and not rerun afterward. Remaining task 190 work is cleanup/quarantine of obsolete whole-agent-loop retry policy duplication.
- 2026-05-30: Task 190 implementation pass completed pending-backoff cancellation and streaming partial-output isolation. Provider-boundary retry now supports injected `:provider-retry-cancelled?`, suppresses the scheduled next attempt when cancellation is observed during retry sleep, clears active retry state, emits `provider_request_cancelled` without synthetic start/finish events, and returns structured `:retry-cancelled` outcome metadata. Added streaming proof that failed-attempt partial text is discarded and the successful retry owns final assistant content. Verification passed: focused cancellation/streaming tests (`2 tests, 18 assertions`), full `psi.turn-runtime.response-mode-test` (`15 tests, 100 assertions`), and targeted clj-kondo over turn-runtime plus provider retry resolver code.
- 2026-05-30: Task 190 implementation pass completed remaining non-cancellation provider-boundary retry outcome semantics: terminal provider/client errors and unknown errors do not retry, disabled retry classifies without scheduling as `:retry-disabled`, enabled zero-max retries returns `:retry-exhausted`, repeated retryable failures exhaust after `1 + :auto-retry-max-retries` attempts while preserving last cause, and active retry state now clears both `:retry` and `:retry-attempt` before retry attempt execution and after terminal outcome. Added injectable `:provider-retry-sleep-fn` for deterministic active-backoff tests. Verification passed: focused new retry outcome tests (`6 tests, 46 assertions`), `psi.turn-runtime.response-mode-test` (`11 tests, 70 assertions`), focused retry/EQL/prompt/statechart/session suite (`57 tests, 333 assertions`), and targeted clj-kondo over changed turn-runtime/agent-session files.
- 2026-05-30: Created Munera task 190 (`ai-request-retry-backoff-observability`) to investigate and fix AI request retry/backoff reliability and visibility for transient request/connection failures. Initial audit found retry/backoff is currently session-statechart-terminal-event based rather than wrapped around provider request execution, and the canonical prompt lifecycle likely bypasses the `:session/agent-event` guard path that schedules retries. Added focused failing proof `prompt-execution-result-retryable-error-enters-retrying-and-schedules-retry-test`: retryable connection error leaves phase `:idle` and emits no `provider_retry_scheduled`. Placement decision: retry unit is one prepared provider request owned by `turn-runtime/execute-prepared-request!`; retry tool-result-post provider requests but never rerun local tools or replay whole agent loops. Added requirement that retry history be introspectable via `psi-tool` / EQL at session/turn/request granularity, including counts, retried errors, classifications, backoff delays, and final status; existing TUI/Emacs/app-runtime active retry surfacing must be preserved or explicitly replaced.

- 2026-05-30: Task 190 docs review follow-up completed: `doc/workflow-ir.md` suggested grammar now states both `llm-judge` and `invoke-judge` are current executed runtime IR, matching the invoke-judge runtime support note; the new `steps.md` item is checked.
- 2026-05-30: Task 190 independent implementation verification pass found no unchecked task/design follow-up steps and no further concrete implementation slice. Re-verified workflow topology, prompt artifact targets, focused workflow routing tests, and targeted lint green; implementation remains complete pending review/closure.
- 2026-05-30: Task 190 independent implementation verification pass re-read task artifacts/workflow definitions/tests, found no unchecked task/design follow-up steps and no concrete implementation work remaining, and reran focused workflow tests plus targeted lint green. Implementation remains complete pending review/closure.
- 2026-05-30: Task 190 independent implementation verification pass found no unchecked task/design follow-up steps and no concrete implementation work remaining. Re-verified plan/design prompt artifact targets, focused workflow routing tests, and targeted lint green; implementation remains complete pending review/closure.
- 2026-05-30: Task 190 independent implementation pass found no remaining concrete implementation work. Re-verified plan/design prompt artifact targets, focused workflow routing tests, and targeted lint green; implementation remains complete pending review/closure.
- 2026-05-30: Task 190 ambiguity follow-up execution found no newly added unchecked ambiguity design-steps (`unchecked count 0`); implementation.md records the no-op pass and design/plan/steps were unchanged.

- 2026-05-30: Task 190 latest plan/steps inconsistency review found no new actionable inconsistencies; implementation.md records the pass and design-steps.md was unchanged.

- 2026-05-30: Task 190 ambiguity follow-up execution found no unchecked ambiguity design-steps after the latest plan/steps ambiguity review; implementation.md records the no-op pass and design/plan/steps were unchanged.

- 2026-05-30: Task 190 plan/steps latest ambiguity verification found no new actionable ambiguities; implementation.md records the pass and design-steps.md was unchanged.

- 2026-05-30: Task 190 inconsistency follow-up execution found no unchecked inconsistency design-steps after the post-plan verification pass; implementation.md records the no-op pass and design/plan/steps were unchanged.

- 2026-05-30: Task 190 plan/steps inconsistency review found no new actionable inconsistencies; implementation.md records the pass and design-steps.md was unchanged.

- 2026-05-30: Task 190 ambiguity follow-up execution found no unchecked ambiguity design-steps after the plan/steps verification pass; implementation.md records the no-op pass and plan/steps/design-steps were unchanged.

- 2026-05-30: Task 190 inconsistency follow-up completed: documentation ownership now belongs to Slice 5 integration/coherence in both plan.md and steps.md; design-step checked.

- 2026-05-30: Task 190 ambiguity follow-up completed: plan/steps slice-count ambiguity resolved by making integration/coherence explicit Slice 5 in plan.md; design-step checked.

- 2026-05-30: Task 190 plan/steps ambiguity review found one new actionable ambiguity: `plan.md` says four vertical slices while the plan list and `steps.md` define five slices including integration/coherence. Added unchecked `design-steps.md` follow-up.

- 2026-05-30: Task 190 ambiguity follow-up execution found no unchecked ambiguity design-steps after the post-final independent verification pass; implementation.md records the no-op pass and design/design-steps were unchanged.

- 2026-05-30: Task 190 post-final independent ambiguity review found no new actionable ambiguities; implementation.md records the pass and design-steps.md was unchanged.

- 2026-05-30: Task 190 final independent inconsistency review found no new actionable inconsistencies; implementation.md records the pass and design-steps.md was unchanged.

- 2026-05-30: Task 190 ambiguity follow-up execution found no unchecked ambiguity design-steps after the post-compaction verification pass; implementation.md records the no-op pass and design/design-steps were unchanged.

- 2026-05-30: Task 190 post-compaction ambiguity verification found no new actionable ambiguities; implementation.md records the pass and design-steps.md was unchanged.

- 2026-05-30: Task 190 inconsistency follow-up completed: compaction now avoids retroactive `retained user → system → retained assistant` insertion by normalizing retained suffix/cut placement to the next valid generation boundary; design-step checked.

- 2026-05-30: Task 190 ambiguity review found one new actionable ambiguity: mid-system injection placement is unspecified when non-conversational journal entries (model/thinking/label/logprobs) occur after the latest user turn but before assistant response. Added unchecked `design-steps.md` follow-up.

- 2026-05-30: Task 190 ambiguity review found one new actionable ambiguity: the Part 4 compaction acceptance criterion still says pre-cut mid-system instructions are coalesced after the summary user, while the detailed rule now conditionally reattaches them after the first retained user when retained history starts with a user. Added unchecked `design-steps.md` follow-up.

- 2026-05-30: Task 190 inconsistency review found one new actionable design inconsistency: compaction can rebuild `summary user → system → retained user`, which contradicts the latest-user mid-system placement contract unless explicitly declared valid or reattached. Added unchecked `design-steps.md` follow-up.

- 2026-05-30: Task 190 inconsistency review found effort-override mapping for extended-thinking models contradicts the no-effort-sending rule, and `thinking-level->effort-default` rename creates dead code since adaptive models use the new xhigh table exclusively. Added unchecked design-step follow-up.

- 2026-05-30: Task 190 inconsistency review found one new actionable design inconsistency: OpenAI mid-system support claims any-position inline system messages, but the shared injection API only permits the Anthropic-safe after-latest-user tail shape. Added unchecked design-step follow-up.

- 2026-05-30: Task 190 ambiguity follow-up execution found no unchecked ambiguity design-steps; appended implementation.md note only.

- 2026-05-30: Task 190 latest ambiguity verification review found no new actionable ambiguities beyond already resolved/captured design concerns; appended implementation.md note and committed `92fe44fa`.

- 2026-05-30: Task 190 inconsistency follow-up completed: compaction boundary mid-system preservation now merges any retained post-cut boundary `:mid-system` entries into the coalesced summary-boundary `:mid-system`, preventing rebuilt `summary user → system → system` sequences; design-step checked.

- 2026-05-30: Task 190 verification ambiguity review found no new actionable ambiguities beyond existing captured/latest follow-ups; appended implementation.md note and added no duplicate design-steps.

- 2026-05-30: Task 190 latest ambiguity review pass found three new actionable design ambiguities: missing extension mutation bridge for `inject-mid-system-message!`, explicit `:normal` speed config presence semantics, and current-session state after scoped `/speed normal`. Added unchecked `design-steps.md` follow-ups.

- 2026-05-30: Task 190 latest ambiguity review pass found two new actionable ambiguities: explicit nil `:effort-override` config resolution through merged shared-config, and mid-system capability semantics for custom/runtime OpenAI chat-completions models. Added unchecked follow-ups to `design-steps.md`.
- 2026-05-30: Task 190 latest inconsistency follow-up pass found no unchecked inconsistency design-steps; implementation.md records that no design changes were required.

- 2026-05-30: Task 190 ambiguity follow-up completed: design now specifies startup application for persisted speed/effort, runtime-resolved model lookup for mid-system capability gating, and `:mid-system` source provenance inference/storage; all current ambiguity design-steps are checked.

- 2026-05-30: Task 190 third ambiguity follow-up completed and committed `2fbc7d20`: design now requires Anthropic request schema to admit inline `role: system` messages and prepared-turn assembly to replace `... user, system` tails as `... current-user, system`; all current ambiguity design-steps are checked.

- 2026-05-30: Task 190 third ambiguity review pass found two new actionable ambiguities: Anthropic local request schema must admit inline system messages in `messages`, and prepared-turn current-user replacement must handle a pending `:mid-system` entry after the user while preserving `user → system` order. Added unchecked follow-ups to `design-steps.md` and committed `12cf9fb2`.

- 2026-05-30: Task 190 requested code-shaper review found no new actionable feedback after closed-schema invocation/action descriptor follow-ups; focused UI/extension/runtime/RPC tests and targeted lint passed.
- 2026-05-30: Task 190 requested code-shaper review found no new actionable code-shaping feedback after the same-namespace action descriptor key follow-up; focused UI/extension/runtime/RPC tests and targeted lint passed.
- 2026-05-30: Task 190 code-shaper review found one new actionable robustness gap: action descriptors reject unqualified/foreign extra keys but still allow extra same-namespace non-schema `:psi.ui.action/...` keys at the untrusted provider boundary. Added an unchecked follow-up.
- 2026-05-30: Task 190 invocation-key follow-up complete: provider invocation validation now rejects extra same-namespace non-schema `:psi.ui.invocation/...` keys for every supported invocation kind, with focused UI capability tests and targeted lint green.
- 2026-05-30: Task 190 requested code-shaper review found no new distinct code-shaping feedback beyond the existing unchecked same-namespace invocation-key follow-up; focused UI/extension/runtime/RPC tests and targeted lint passed.
- 2026-05-30: Task 190 docs follow-up complete: `doc/extensions.md` and `doc/extension-api.md` now state task 190 is query/descriptor-only and side-effecting UI descriptor submission is deferred to `191-ui-action-invocation`; the new steps item is checked.
- 2026-05-30: Task 190 implementation-review pass found no new distinct actionable issue beyond the existing unchecked provider collection-shape follow-up; targeted UI/query/RPC/TUI verification and lint passed, while broad app-runtime focus exposed unrelated commit-checks/TUI test interference.
- 2026-05-30: Task 190 test-shaper review found one new actionable test-quality gap: provider normalization lacks executable coverage that non-vector `:psi.ui/capabilities` / `:psi.ui/actions` fail closed, while implementation vectorizes before validation. Added an unchecked follow-up to cover/fix collection-shape drift.
- 2026-05-30: Task 190 test-review follow-up added TUI exceptional lifecycle coverage: `start-tui-runtime!` now has regression proof that the active UI capability provider is cleared when `tui-start-fn!` throws, so post-failure queries return no-provider semantics. Focused app-runtime tests and targeted lint passed.
- 2026-05-30: Task 190 test-review follow-up added real extension API coverage proving `ext/create-extension-api` with `runtime-fns/make-extension-runtime-fns` can query documented `:psi.ui/...` capability/action attrs without explicit session-id input and with extension `allowed-events` empty; focused extension/UI/nullable tests and targeted lint passed.
- 2026-05-30: Task 190 implementation-review follow-up fixed nested invocation descriptor key validation: `:psi.ui.action/invocation` maps now reject unqualified, foreign namespaced, and string keys before EQL exposure, failing closed to provider-error. Focused UI capability tests and targeted lint passed.

- 2026-05-30: Task 190 implementation-review follow-up fixed RPC/Emacs pre-install provider semantics: RPC contexts can suppress static default UI provider, runtime installs the late-bound connection-correlated Emacs RPC provider before bootstrap, and pre-install/no-attached/bootstrap coverage plus targeted lint are green.
- 2026-05-30: Task 190 implementation-review follow-up fixed contradictory unavailable UI provider advertisements: provider normalization now treats `:psi.ui/available? false` with capabilities or available actions as provider-error semantics, with focused coverage and targeted lint green. Commit `79b5e7be`.
- 2026-05-30: Task 190 implementation-review follow-up updated `create-nullable-extension-api` to support documented UI capability queries including `:psi.ui/diagnostic`, returning the nullable attached-console unsupported-make-visible map with nil diagnostic; added nullable API coverage and included extension-test-helpers tests in Kaocha config. Focused nullable/API tests and targeted lint passed.
- 2026-05-30: Task 190 implementation-review follow-up added provider-error diagnostic redaction before exposing `:psi.ui/diagnostic`: stack frames, frontend object printed forms, token/secret/password/API-key values, bearer/token-looking values, secret-bearing paths, and arbitrary exception data are redacted/bounded. Focused UI capability tests and targeted lint passed.
- 2026-05-30: Task 190 Emacs RPC provider lifecycle pass added late-bound active-session-aware Emacs UI capability provider installation in RPC runtime. Focused-session descriptors now carry invocation session correlation; missing focused session downgrades to no-attached UI; RPC loop exit clears provider to avoid stale detached make-visible advertisements. Focused UI capability/graph/RPC invariant tests, targeted lint, and full `bb test` passed.
- 2026-05-30: Task 190 compatibility regression pass added `psi.agent-session.ui-capabilities-test` coverage proving new `:psi.ui/...` capability/action attrs compose with existing extension UI snapshot attrs and legacy `:psi.agent-session/ui-type` in one real dispatch/query flow. Focused UI capability, graph-surface, and resolver tests plus targeted lint passed. Commit `04f754aa`.
- 2026-05-30: Task 190 provider-normalization test pass expanded `psi.agent-session.ui-capabilities-test` for no-attached/unsupported/supported states, invocation-kind schemas, malformed invocation data, duplicate ids, and capability/action coherence. Focused UI capability + graph-surface tests and targeted lint passed.
- 2026-05-30: Task 190 implementation pass added core queryable UI capability/action attrs under `:psi.ui/...`, provider normalization, agent-session provider slot, default Emacs/TUI/console provider behaviour, `psi-emacs-show-active`, nullable/query tests, graph discovery coverage, changelog entry, and follow-up task 191 for side-effecting UI action invocation. Focused tests/lint/Emacs byte-compile passed; more test/front-end lifecycle polish remains in task 190.
- 2026-05-30: Task 190 ambiguity follow-up execution after `c64c3d7b` found no newly added unchecked ambiguity items in `design-steps.md`; recorded no-op status in task implementation notes and committed `1de0dca1`.
- 2026-05-29: Task 188 is complete and ready in PR #132: built-in packaged `workflow` skill added at `bases/main/resources/psi/skills/workflow/SKILL.md`, project-local `.psi/skills/workflow/SKILL.md` removed in the same slice to avoid precedence shadowing, and focused proof added across built-in skill discovery/materialization, `:psi.agent-session/skills`, `:psi.skill/by-source`, `/skills`, and `/help` (`96 tests, 497 assertions`).
- 2026-05-29: Task 188 requested independent implementation review re-read task artifacts/code/tests/docs/changelog, reran focused model/provider/turn-runtime tests and targeted AI lint green, and found no new actionable implementation feedback; remaining unchecked Codex non-streaming `:execute` test item remains conditional future work.
- 2026-05-29: Fixed the live `/delegate review-task-implementation 189-deterministic-review-step-routing` failure on this branch. Root cause was built-in workflow bootstrap not registering the new deterministic review-step operations into the runtime deterministic-operation registry, so live delegated `review-step` runs failed with missing invoke operations even though focused unit tests passed by locally registering test ops. Added built-in `:register-operation` support in workflow bootstrap, registered `workflow/pass-status-routing` and `workflow/constant-routing` during built-in workflow init, and added a focused bootstrap proof that those operations are present after init. Verification: `clojure -M:test --focus psi.workflow-loader.workflow-definitions-test --focus psi.agent-session.workflow-review-step-routing-test --focus psi.agent-session.workflow-invoke-runtime-test --focus psi.agent-session.workflow-delegate-review-step-live-test`; targeted `clj-kondo` green.

- 2026-05-29: Task 189 inconsistency follow-up complete: deterministic `follow-up` loopback is now in scope through built-in `workflow/constant-routing`, returning the literal configured route (`"REPEAT"` for `review-step`) and using the same invoke-judge error surface for invalid route input; all current design-steps are checked.

- 2026-05-29: Task 189 ambiguity follow-up complete: parser cardinality now rejects duplicate `PASS_STATUS:` lines (including identical duplicates) and known+malformed extra status lines as deterministic failures, and invoke-judge `workflow/pass-status-routing` errors now map to recorded judge output plus terminal workflow failure without executing follow-up.

- 2026-05-29: Task 189 ambiguity follow-up complete: design now keeps `review-step` structured/text mismatch validation in this slice by requiring same-step invoke-judge source resolution for `:structured-status {:from {:step "review" :output :review-result} :path [:status]}`, wiring it into `workflow/pass-status-routing`, and adding mismatch/self-ref tests.

- 2026-05-29: Task 189 inconsistency follow-up complete: native structured-output/prose `PASS_STATUS` conflict resolved by removing same-response model-produced `:review-result` from this slice; review actor remains prose/text, `PASS_STATUS` is authoritative, and `workflow/pass-status-routing` provides the structured deterministic control result.

- 2026-05-29: Task 189 inconsistency follow-up complete: review-step max-iteration protection now belongs on deterministic `follow-up` → `review` loopback (`:max-iterations 6`) because runtime iteration counts are target-step keyed; `review` → `follow-up` routes actionable feedback without the loop guard.

- 2026-05-29: Task 189 ambiguity follow-up complete: design now requires `review-step` `:review-result` to be provider-native only with `:fallback :none` / `:require-provider-native? true`, preserving the prose final reply plus legacy `PASS_STATUS:` token and failing unsupported native structured output before routing/follow-up.

- 2026-05-29: Task 189 ambiguity review found one new actionable ambiguity: the `review` actor must keep a prose final reply ending with `PASS_STATUS`, but adding `:review-result` without an explicit strategy can use prompted-JSON fallback whose JSON-only final text conflicts with that token/prose routing surface. Added an unchecked `design-steps.md` follow-up and committed the review.

- 2026-05-29: Task 189 inconsistency follow-up complete: aligned `review-step` structured status routing to source the validated `:review-result` value via `:path [:status]`, documented that raw structured-output envelope internals are not part of the workflow ref contract, and marked the design-step done.
- 2026-05-30: Task 190 design inconsistency review pass re-read `design.md`, task-design/workflow guidance, current design/plan review workflow definitions and prompts, final-summary/clarity prompts, workflow grammar docs, workflow-loader tests, and PASS_STATUS routing code. No new actionable design inconsistencies found. Commit `42b07b9b` recorded the review note only; `design-steps.md` remains absent because no follow-up items were needed.
- 2026-05-30: Task 190 design inconsistency review pass re-read `design.md`, task-design/workflow guidance, current design/plan review workflow definitions/prompts, workflow grammar docs, workflow-loader definition tests, review-step routing tests, and PASS_STATUS routing implementation. No new actionable inconsistencies found; committed review note `ec86b825`. `design-steps.md` remains absent because no follow-up items were needed.
- 2026-05-30: Task 190 plan ambiguity review pass re-read plan.md, steps.md, implementation.md, design.md, workflow-authoring docs, current design/plan review workflow definitions/prompts, workflow definition tests, review-step routing tests, and PASS_STATUS routing implementation. No new actionable plan ambiguities found; committed review note only. `design-steps.md` remains absent because no follow-up items were needed.
- 2026-05-30: Task 190 design ambiguity follow-up execution completed as blocked/no-op after the latest ambiguity-review pass: `design-steps.md` is absent, so there were no newly added unchecked ambiguity follow-up items to execute or mark done. Recorded in `implementation.md`; task design/plan/steps unchanged.
- 2026-05-30: Task 190 design inconsistency follow-up execution completed after the latest inconsistency-review pass: `design-steps.md` contains no newly added unchecked items; the only item predates the pass and is already checked, so no follow-up was re-executed. Recorded in `implementation.md`.
- 2026-05-30: Task 190 independent implementation pass found no unchecked `steps.md` or `design-steps.md` items and no remaining concrete implementation slice. Re-verified prompt artifact targets plus focused workflow tests and targeted lint green; implementation appears complete pending review/closure.
- 2026-05-30: Task 190 design ambiguity follow-up execution completed after the latest ambiguity-review pass: `design-steps.md` contains no newly added unchecked items; the only item predates the pass and is already checked, so no follow-up was re-executed. Recorded in `implementation.md`.
- 2026-05-30: Task 190 independent completion check found no remaining implementation steps or design follow-ups; focused workflow tests and targeted lint are green. Implementation is complete pending review/closure.
- 2026-05-30: Task 190 independent implementation pass re-confirmed implementation complete: no unchecked `steps.md` or `design-steps.md` items remain, prompt artifact targets and conditional routing still match design, focused workflow tests and targeted lint green. Remaining work is review/closure only.
- 2026-05-30: Task 190 independent implementation verification pass found no unchecked task/design follow-up steps and no further concrete implementation slice. Re-verified prompt artifact targets, focused workflow routing tests, and targeted lint green; implementation remains complete pending review/closure.
- 2026-05-30: Task 190 test-shaper review found one actionable test-shaping gap: workflow definition tests should assert each per-reviewer PASS_STATUS invoke judge sources its own step's `:final-llm-reply`; added unchecked follow-up to `steps.md` and committed `d7ebfe9c`.

- 2026-05-30: Task 190 code-shaper review found one actionable robustness follow-up: design/plan per-reviewer PASS_STATUS routing currently accepts implementation-only tokens via the global routing union; added unchecked `steps.md` item and review note.
- 2026-05-30: Task 190 generic review-step code-shaper follow-up completed: `.psi/workflows/review-step.edn` now constrains `workflow/pass-status-routing` to review statuses only (`ACTIONABLE_FEEDBACK` / `REVIEW_COMPLETE`); focused runtime proof shows `IMPLEMENTATION_COMPLETE` fails before follow-up for review-step, while implement-task still accepts implementation statuses. Focused workflow tests and targeted lint are green; follow-up item checked.
- 2026-05-30: Task 190 code-shaper re-review after review-step allowed-status follow-up found no new actionable code-shaping issues. Focused workflow tests and targeted lint green; review note committed as `fb32b792`.

- 2026-05-30: Task 190 design ambiguity review pass found three new actionable ambiguities: `/effort` behaviour for OpenAI Codex/responses, invalid-placement semantics for `inject-mid-system-message!`, and compaction lifetime for pre-cut `:mid-system` instructions. Added follow-ups to `design-steps.md`; note that `design.md` already had uncommitted refinements before this pass.
- 2026-05-30: Task 190 ambiguity follow-up complete: design now applies `/effort` to OpenAI Codex/responses with the same override mapping, rejects invalid `inject-mid-system-message!` placements before journal mutation, and preserves pre-cut mid-system instructions across compaction by coalescing them after the summary user turn.
- 2026-05-30: Task 190 second ambiguity review pass found three new actionable ambiguities: Anthropic request schema must admit or explicitly route `output_config.effort = "highest"`, mid-system provider-style maps need a schema-valid AI conversation representation/normalization path, and scoped persisted clears for `/speed normal` / `/effort none` need explicit lower-precedence config semantics. Added unchecked follow-ups to `design-steps.md`.

- 2026-05-30: Task 190 second inconsistency follow-up complete: design now specifies `:psi.agent-session/speed-mode` resolver coerces nil session state to `:normal` while request shaping still treats nil/`:normal` as provider default, and `:supports-mid-conversation-system-messages` is optional model metadata with absent-as-false semantics. Both second-pass design-steps are checked.
- 2026-05-30: Task 190 third ambiguity review pass found three new actionable ambiguities: persisted speed/effort startup wiring, mid-system capability lookup source under runtime model overrides, and mid-system journal `:source` provenance contract. Added unchecked follow-ups to `design-steps.md`.

- 2026-05-30: Task 190 latest inconsistency follow-up completed and committed `d8b342ac`: design now requires plain adaptive Anthropic `thinking-level :xhigh` (nil `/effort` override) to send `output_config.effort = "highest"`, preserving the task goal that `:xhigh` is distinct from `:high`; `design-steps.md` is checked for the latest inconsistency item.

- 2026-05-30: Task 190 ambiguity follow-up no-op pass: `design-steps.md` had no unchecked ambiguity follow-up items, so no design changes were needed; implementation.md records the pass.

- 2026-05-30: Task 190 source-provenance inconsistency review found one new actionable design inconsistency: `inject-mid-system-message!` source inference requires extension provenance, but the specified Pathom mutation params omit `:ext-path`. Added unchecked follow-up to `design-steps.md` and implementation review note.

- 2026-05-30: Task 190 inconsistency follow-up completed: `psi.extension/inject-mid-system-message` mutation params now include optional `:ext-path`; provenance inference is owned by the extension mutation surface (`:source` > `:ext-path` > `:extension`), and the source-provenance design-step is checked.

- 2026-05-30: Task 190 inconsistency review found one new actionable design inconsistency: compaction reattaches preserved pre-cut `:mid-system` instructions after the first retained user even when a retained assistant response follows, retroactively changing history rather than attaching to the next-generation/latest-user boundary. Added unchecked `design-steps.md` follow-up and committed `b39786ee`.
- 2026-05-30: Task 190 inconsistency follow-up execution found no unchecked inconsistency design-steps after the post-final independent no-op review; implementation.md records the no-op pass and design/design-steps were unchanged.

- 2026-05-30: Task 190 plan/steps independent ambiguity verification found no new actionable ambiguities; implementation.md records the pass and design-steps.md was unchanged. Commit `f35989f1`.

- 2026-05-30: Task 190 latest inconsistency follow-up execution found no unchecked design-steps (`unchecked count 0`); implementation.md records the no-op pass and design/plan/steps were unchanged.

- 2026-05-30: Task 190 plan/steps final independent ambiguity review found no new actionable ambiguities; implementation.md records the pass and design-steps.md was unchanged. Commit `1056bcfe`.

- 2026-05-30: Task 190 inconsistency follow-up execution found no newly added unchecked inconsistency design-steps (`unchecked count 0`); implementation.md records the no-op pass and design/plan/steps were unchanged.

- 2026-05-30: Task 190 plan/steps repeat independent ambiguity review found no new actionable ambiguities; implementation.md records the pass and design-steps.md was unchanged.

- 2026-05-30: Task 190 ambiguity follow-up execution after latest no-new-feedback review found no unchecked ambiguity design-steps (`unchecked count 0`); implementation.md records the no-op pass and design/plan/steps were unchanged.

- 2026-05-30: Task 190 Slice 1 implementation complete: added Claude Opus 4.8 model catalog metadata, native Anthropic JSON Schema registration, mid-system capability metadata/schema support, focused catalog tests, and gated Anthropic Models API tests. Focused AI tests and targeted clj-kondo passed. Next: Slice 2 speed-mode stack.

- 2026-05-30: Task 190 Slice 2 first implementation pass complete: core speed-mode session/config/startup/resolver/footer/request-shaping stack is implemented, with Anthropic fast-mode beta/header/schema support and OpenAI chat-completions `service_tier: flex`; focused tests/lint passed. Remaining Slice 2 work: cold-resume transience proof and any additional persistence assertions.
- 2026-05-30: Task 190 Slice 4 capability/API pass complete: added shared runtime model mid-system predicate/resolver, dispatch injection gating/placement validation, extension API + Pathom mutation + runtime EQL routing, and focused tests/lint green. Remaining Slice 4 work is compaction preservation/tests.

- 2026-05-30: Task 190 inconsistency follow-up completed: verified Slice 4 mid-system projection/current-user/conversation/provider transform tests exist and pass (65 tests, 322 assertions); marked the three corresponding `steps.md` test items checked and checked the `design-steps.md` follow-up. Restored missing Anthropic `message_transform.clj` namespace required by current working tree verification.

- 2026-05-30: Task 190 Slice 4 compaction pass complete: `:mid-system` compaction rebuild now preserves pre-cut active instructions, normalizes retained suffixes to avoid retroactive insertion before assistant history, merges boundary mid-system entries, and focused compaction/Anthropic tests pass. Injection persistence follow-up added journal IO effect coverage. Remaining task work is broader Slice 4 verification and Slice 5 docs/changelog/full verification.

- 2026-05-30: Task 190 follow-up implementation complete: successful `:session/inject-mid-system-message` now emits equivalent `:persist/session-journal-io` for flushed session journals, and `:mid-system` entries count as persistence anchors. Focused Slice 4 verification passed (`153 tests, 822 assertions`) and targeted lint is clean. Remaining task work is Slice 5 docs/changelog/full verification.
- 2026-05-30: Task 190 compaction replay follow-up complete: `rebuild-messages-from-journal-entries` now reuses mid-system preservation/coalescing semantics so pre-cut active `:mid-system` instructions survive cold journal replay after compaction; focused compaction test and targeted lint green.
- 2026-05-30: Task 190 inconsistency follow-up execution after the latest no-new-feedback review found no unchecked design-steps (`unchecked count 0`); implementation.md records the no-op pass and design/plan/steps were unchanged.
- 2026-05-30: Task 190 plan/steps post-test-review ambiguity review found no new actionable ambiguities; implementation.md records the pass, design-steps.md was unchanged, and existing uncommitted test edits were left untouched.
- 2026-05-30: Task 190 test-review follow-up executed: added scoped `/speed` and `/effort` command persistence tests covering project/user writes, explicit `/speed normal` `:normal` masks, and `/effort none` nil masks with key presence. Focused commands test and targeted lint green.
- 2026-05-30: Task 190 effort-override test follow-up complete: added non-`xhigh` override precedence coverage for Anthropic adaptive (`:high`), OpenAI chat-completions (`:medium`), and Codex/responses (`:medium`); focused provider tests and targeted lint green; steps.md follow-up checked.
- 2026-05-30: Task 190 final code-shaper review pass found no new actionable code-shaping feedback after re-reading task artifacts and referenced code/tests/docs; focused compaction/model-dispatch/prompt-request/extensions/Anthropic/OpenAI verification passed (`103 tests, 633 assertions`); implementation.md records the pass and steps.md was unchanged. Commit `565ba0ec`.
- 2026-05-30: Task 190 requested plan/steps ambiguity review completed. Re-read plan, steps, implementation notes, design artifacts/docs, and sampled UI/query/runtime seams; found no new actionable ambiguity feedback. Appended review note and committed `⊨ review 190 plan ambiguity`.
- 2026-05-30: Task 190 requested ambiguity follow-up execution found no newly added unchecked ambiguity items in `design-steps.md`; recorded the no-op follow-up status in task implementation notes.
- 2026-05-30: Task 190 requested ambiguity follow-up execution after `0c8284d9` found no newly added unchecked ambiguity items in `design-steps.md`; recorded no-op status in task implementation notes.
- 2026-05-30: Task 190 implementation review found one new actionable issue: UI provider normalization can expose `:psi.ui/available? false` together with advertised capabilities/available actions. Added task follow-up for validation/coverage; focused UI/RPC/nullable tests and targeted lint still pass.
- 2026-05-30: Task 190 action descriptor foreign-key follow-up complete: UI action descriptor validation now rejects unqualified, foreign-namespaced, or string extra descriptor keys before EQL exposure; focused UI capability tests and targeted lint passed.
- 2026-05-30: Task 190 TUI provider lifecycle follow-up complete: TUI runtime suppresses default static provider during context/bootstrap, installs attached-but-unsupported provider only when frontend wiring is ready, and clears provider on TUI exit; app-runtime/UI capability tests and targeted lint passed.
- 2026-05-30: Task 190 test-review follow-up complete: provider normalization now defaults omitted `:ui-event` payload and `:mutation` params to `{}` in exposed invocation descriptors, with focused coverage and targeted lint green.
- 2026-05-30: Task 190 test-shaper follow-up complete: provider normalization now rejects non-vector `:psi.ui/capabilities` / `:psi.ui/actions` collection shapes instead of silently vectorizing, with focused UI capability tests and targeted lint green. Commit `cf20854f`.
- 2026-05-30: Task 190 action descriptor same-namespace key follow-up complete: provider action descriptors now use closed available/unavailable key sets, rejecting extra same-namespace non-schema `:psi.ui.action/...` keys before EQL exposure; focused UI capability tests and targeted lint passed.

- 2026-06-01: Task 108 code-shaper follow-up execution complete (commit `6ab6206aa`): executed the 3 follow-ups from `be0cb7284` — (1) `client.clj connect-instance-in!` destructure moved below the `instance` existence guard; (2) dropped redundant `session-fn` alias, reading `:nrepl.core/taking-until` session-id directly off `client-session`; (3) `started.clj wait-for-started-endpoint!` binds `effective-timeout-ms` once for both deadline and reported `:timeout-ms` (default-timeout callers no longer report nil). Focused client/started + commands/runtime/eval/ops/attach tests green; lint clean; behaviour/boundary unchanged; all 3 steps.md items checked.

- 2026-06-02: Task 206 (`emacs-buffer-local-widget-mutation-timers`) test-shaper review follow-ups (S1, S2) executed (commit `92cbb3ca8`). Both pure test-scaffold refactors; no source/coverage change; full `bb emacs:test` 340/340; byte-compile clean; reloaded. **S2**: added generic `psi-test--should-not-error (&rest body)` macro to shared `test/psi-test-support.el` (NOT per-file — the no-op idiom is shared by both widget-projection test files → shared support gives `consistent(assertion_style)`); wired `(require 'psi-test-support)` + `test/` load-path into both `psi-widget-projection-timers-test.el` (6 sites) and `psi-widget-projection-test.el` (1 site, `:86`). Zero hand-rolled `(should-not (condition-case err (progn … nil)(error err)))` remain. **S1**: added timers-file-local `pwpt--with-psi-buffer (var &rest body)` (generate + setq-local fresh state + unwind-protect kill; nestable) + `pwpt--seed-button-in-flight (id key)` (spec + sync-lstates + set-lstate in-flight); folded into the 3 two-buffer tests + 2 cross-buffer arrange blocks. Assertions kept inline (intent). Deliberately left `…-response-noop-when-buffer-dead` inline `generate-new-buffer`/`kill-buffer` — its mid-test kill is the ACT not teardown. **🔁 Lesson**: a generic test idiom (no-op assertion) shared across files → shared `psi-test-support.el`; a domain-specific scaffold (buffer-local widget state) → file-local helper. Both S1/S2 checked. All 206 steps now checked; only Acceptance-verification gate + closure remain (task still open).
- 2026-06-02: Task 202 docs review (`review-task-docs`) found one new actionable scheduler-doc accuracy issue outside the absolute-`:at` bounds text: `doc/scheduler.md` lists supported scheduler `session-config` key `:tool-defs`, but implementation accepts `:tool-ids` and rejects unsupported keys. Added unchecked follow-up to fix the doc list; absolute-`:at` docs and no-CHANGELOG decision verified coherent.

- 2026-06-02: Task 207 (`workflow-session-defaults-inheritance-snapshot`) FULLY IMPLEMENTED across slices S1–S7 (commits a683959c8..46f2edb2b). Captures a workflow run's inherited default session details (model, prompt-mode, tools, skills, thinking-level, speed-mode, effort-override) as a snapshot at invoke time on the run's canonical state, so later parent/default-model changes don't leak into a running workflow (AC1/2/3); nested delegations inherit the delegating step's effective config = run snapshot ⊕ step overrides (AC4); explicit overrides still win (AC5); no-snapshot runs fall back to live reads (AC6); model-query selection context comes from the snapshot (AC7); resume reuses the snapshot, continue captures fresh (AC8/Decision 5). Key files: session-state/init.clj (promoted common-inherited-fields/model-identity-fields public), workflow-step-session-config/core.clj (inherited-defaults-source-keys/-snapshot-keys constants + resolve-inherited-defaults-snapshot + effective-config->snapshot + per-field source swap in resolve-step-session-config with wholesale parent-session-model replacement, P4), workflow-runtime/model.clj (inherited-defaults-schema) + core.clj (create-run :inherited-defaults opt, stays pure), canonical_workflows.clj + psi_tool_workflow.clj (top-level capture), delegate.clj + statechart_runtime.clj + context.clj (S6 injected resolve-inherited-defaults-fn — avoids the certain wssc↔workflow-runtime require cycle, P1). **Discovery/deviation**: AC3 required threading speed-mode/effort-override END-TO-END through the workflow child-session create path (child_session_contract request-schema, attempts.clj, context.clj create-workflow-child-session!, session_lifecycle.clj :session/create-child, child_session_state.clj) because workflow children build state via child-session-base-state* which (unlike init.clj lifecycle paths) did NOT inherit speed/effort — without this the snapshot was decorative for those two fields. Recorded in implementation.md S5. Task open, all steps checked; ready for review/closure.

- 2026-06-02: Task 207 test-review pass 9 follow-ups executed (commit `7a3bcca08`). Pass 9 (`72bb8ba17`) added exactly two new unchecked items — **T8** (contradictory contract comment) + **T9** (steps.md filename drift). **T8**: rewrote the `continue-terminal-run-captures-fresh-snapshot-test` deftest comment in `canonical_workflows_snapshot_test.clj:44` from "non-session-scoped `psi.workflow/create-run`" → "session-scoped" (reconciling with the file docstring + `runtime_eql/session-scoped-extension-mutation-ops`); comment-only, lint clean. **T9**: corrected steps.md T4's DONE note (`:496`) to cite the sibling `canonical_workflows_snapshot_test.clj` (split out of `canonical_workflows_test.clj` for the 800-line limit) where the test actually lives; docs-only. Both checked. Deliberately left untouched: the unchecked duplicate **CS2** entry between the code-shaper section and the pass-9 header — it predates pass 9 (introduced by code-shaper review `65af82156`), CS2's work is already done/checked, and the "don't execute items predating the preceding review pass" constraint applies. No source/behaviour change this pass; both follow-ups are correctness-of-documentation fixes.

- 2026-06-05: Task 211 (`emacs-retry-backoff-footer-visible`) plan/steps created from stable `design.md`. `plan.md` keeps app-runtime footer projection as owner, primary `footer/updated` path, no Emacs-local retry wording/countdown, optional constrained `session/updated` fallback only if investigation proves necessary. `steps.md` slices: (1) trace/lock backend-RPC trigger gap, (2) publish footer on retry activation/change/clear, (3) Emacs-visible `footer/updated` active/change/clear rendering coverage, (4) optional constrained fallback, (5) final verification/docs-changelog assessment. No code touched.
- 2026-06-05: Task 211 code-shaper follow-up **CS1** executed. `clear-active-retry!` now emits `:retry-updated` only when existing session state actually has retry-related state (`:retry`, positive `:retry-attempt`, or `:provider-retry-abort-requested?`), avoiding ordinary non-retry success footer churn while preserving clear-after-retry/abort cleanup publication. Added response-mode regression for non-retry success emitting no `:retry-updated`. Verified focused Scry turn-runtime + RPC retry footer tests green, paren repair success, targeted clj-kondo 0 errors/3 pre-existing unresolved-var warnings. PASS_STATUS REVIEW_COMPLETE.
- 2026-06-05: Task 214 (`simplify-start-nrepl`, closed) post-review follow-up pass executed the 3 items added by impl-review `5819ea007`. (1) A2-redefinition ratification: SURFACED only — left unchecked as a human gate; task closure is PROVISIONAL pending human ratify-vs-revert. (2) Framework-level A2 defect ESCALATED via durable knowledge: created `mementum/knowledge/gordian-net-sum-burden-gate-sub-additivity.md` (active) — the emitted net-sum A2 gate is provably unsatisfiable by ANY behaviour-preserving decomplecting extraction because Gordian's per-dimension `log1p-over-scale` is concave/sub-additive, so splitting raw burden across target+seam raises the summed normalized burden → A2 forbids the very refactor the task selects for; genuine intent is A1 (target burden down) + "each seam simpler than residual target" + A3, and the real fix is to correct the EMITTED criterion at its source (workflow/task-design), not per-task. (3) Optional endpoint-map dedup DECLINED (pre-existing, scope-expanding/A5, counterproductive on metric — variant B lcc 6.1276). No production/test/doc code changed; only task artifacts + new knowledge page. 🌀 META: a metric-driven acceptance gate can be self-contradictory with the task it gates — capture as framework-level knowledge, don't silently rewrite the bar per-task.

- 2026-06-06: Task 216 (`extract-task-knowledge-workflow`) plan/steps ambiguity review found three actionable follow-ups. **PA1**: resolve lifecycle task identifier shape (`NNN-slug` vs `munera/open/NNN-slug` path from `task-lifecycle-in-worktree`) before writing extraction prompt/tests. **PA2**: specify how the single-step markdown workflow consumes lifecycle/review context (`{{original}}` or named input var) and test compiled context wiring. **PA3**: define the exact observable success marker authorizing the lifecycle open-task exception, so arbitrary user context cannot accidentally permit open-task extraction. Added unchecked PA1–PA3 to `steps.md`; appended ACTIONABLE_FEEDBACK note to `implementation.md`.
- 2026-06-07: Task 218 (`review-workflow-iteration-limits`) code-shaper review found actionable follow-up **CS1**. Design/plan `clarity-status` invoke steps currently run `workflow/pass-feedback-routing` twice (actor invoke result plus judge invoke routing) even though only the judge controls routing; added CS1 to simplify to the existing constant-actor + routing-judge idiom and update definition tests. PASS_STATUS ACTIONABLE_FEEDBACK.
- 2026-06-07: Task 219 (`simplify-rpc-session-family`) Slice 4 production simplification pass complete. Gate-safe target-local subset implemented: centralized duplicated `/new` command-result rehydration shaping in `psi.rpc.session.commands` and reused it from prompt command handling; reverted a broader `/resume`/`/tree` app-runtime navigation route before commit because Gordian gate reported a new medium `psi.rpc.events` SDP finding. Verification green: pinned RPC Scry 57/439, targeted clj-kondo 0/0, diff check clean. PASS_STATUS MORE_WORK_REMAINS.
- 2026-06-07: Task 219 (`simplify-rpc-session-family`) Slice 5 validation passed after Slice 4. Pinned RPC Scry green 57/439, targeted clj-kondo 0/0, diff check clean. Gordian artifacts captured; gate parse result pass with no new cycles/high/medium findings; compare has one new low outside target; selected family remains rank 1 but score improved 102.89 → 102.33. Note: `bb gordian gate --edn` stdout truncated invalid EDN in this environment, so the committed gate artifact was regenerated via the same Gordian gate functions with unbounded EDN printing. PASS_STATUS MORE_WORK_REMAINS.
